
# === PYTHON FILES ===

# __init__.py



# db\__init__.py

# 1 import gc
# 2 from sqlalchemy import create_engine
# 3 from app.db.connection import DB_PATH, close_connection
# 4 from app.db.models import Base
# 5 
# 6 def init_db(reset: bool = False):
# 7     #  Explicitly close lingering DB-API connections
# 8     close_connection()
# 9 
# 10     #  Explicitly create and dispose of engine before deletion
# 11     engine = create_engine(f"sqlite:///{DB_PATH}")
# 12     engine.dispose()
# 13 
# 14     if reset and DB_PATH.exists():
# 15         try:
# 16             DB_PATH.unlink()
# 17         except PermissionError as e:
# 18             print(f"⚠️ Unable to delete {DB_PATH}: {e}")
# 19             print("⚠️ Attempting to force garbage collection.")
# 20             gc.collect()
# 21             DB_PATH.unlink()  #  try again after GC
# 22 
# 23     #  Recreate engine after deletion
# 24     engine = create_engine(f"sqlite:///{DB_PATH}")
# 25     Base.metadata.create_all(bind=engine)
# 26     
# 27     return engine


# db\base.py

# 1 from sqlalchemy.orm import declarative_base
# 2 
# 3 Base = declarative_base()


# db\connection.py

# 1 from pathlib import Path
# 2 import sqlite3
# 3 
# 4 DB_PATH = Path("experiments") / "codecritic.sqlite3"
# 5 _CONN: sqlite3.Connection | None = None
# 6 
# 7 def get_connection() -> sqlite3.Connection:
# 8     global _CONN
# 9     try:
# 10         if _CONN is None:
# 11             raise RuntimeError
# 12         _CONN.execute("SELECT 1")
# 13     except (sqlite3.ProgrammingError, RuntimeError):
# 14         _CONN = sqlite3.connect(DB_PATH, check_same_thread=False)
# 15     return _CONN
# 16 
# 17 def close_connection() -> None:
# 18     global _CONN
# 19     if _CONN:
# 20         _CONN.close()
# 21         _CONN = None


# db\models.py

# 1 from datetime import datetime, timezone
# 2 from typing import Optional
# 3 from sqlalchemy import Column, DateTime, Enum, Float, Integer, String, JSON
# 4 from sqlalchemy.dialects.sqlite import TEXT
# 5 from app.db.base import Base
# 6 from uuid import uuid4
# 7 
# 8 from app.enums.agent_enums import AGENT
# 9 from app.enums.logging_enums import PROVIDER_TYPE
# 10 from app.enums.system_enums import SYSTEM
# 11 from sqlalchemy.orm import Mapped, mapped_column
# 12 
# 13 # #  PROMPT CONFIGS
# 14 
# 15 class AgentPrompt(Base):
# 16     __tablename__ = 'agent_prompt'
# 17     id = Column(Integer, primary_key=True, index=True)
# 18     guid = Column(TEXT, unique=True, default=lambda: str(uuid4()))
# 19     name = Column(String, nullable=False)
# 20     description = Column(String)
# 21     artifact_path = Column(String, nullable=False)
# 22     tags = Column(JSON, nullable=True)
# 23 
# 24 class SystemPrompt(Base):
# 25     __tablename__ = 'system_prompt'
# 26     id = Column(Integer, primary_key=True, index=True)
# 27     guid = Column(TEXT, unique=True, default=lambda: str(uuid4()))
# 28     name = Column(String, nullable=False)
# 29     system_type = Column(Enum(SYSTEM), nullable=False) 
# 30     description = Column(String)
# 31     artifact_path = Column(String, nullable=False)
# 32     tags = Column(JSON, nullable=True)
# 33 
# 34 
# 35 # #  PROVIDER CONFGIS
# 36 
# 37 class PromptProviderConfig(Base):
# 38     __tablename__ = "prompt_provider_config"
# 39 
# 40     id = Column(Integer, primary_key=True, index=True)
# 41     guid = Column(String, unique=True, nullable=False)
# 42     name = Column(String, nullable=False)
# 43     description = Column(String, nullable=True)
# 44     artifact_path = Column(String, nullable=False)
# 45     config = Column(JSON, nullable=False) 
# 46     tags = Column(JSON, nullable=True)
# 47     provider_type = Column(Enum(PROVIDER_TYPE), nullable=False, default=PROVIDER_TYPE.AGENT)
# 48 
# 49 class ToolProviderConfig(Base):
# 50     __tablename__ = "tool_provider_config"
# 51     id = Column(Integer, primary_key=True)
# 52     guid = Column(String, unique=True, nullable=False)
# 53     name = Column(String, nullable=False)
# 54     description = Column(String, nullable=True)
# 55     config = Column(JSON, nullable=True)
# 56     artifact_path = Column(String, nullable=False)
# 57     tags = Column(JSON, nullable=True)
# 58     provider_type = Column(Enum(PROVIDER_TYPE), nullable=False, default=PROVIDER_TYPE.AGENT)
# 59 
# 60 class ScoreProviderConfig(Base):
# 61     __tablename__ = "score_provider_config" 
# 62     id = Column(Integer, primary_key=True)
# 63     guid = Column(String, unique=True, nullable=False)
# 64     name = Column(String, nullable=False)
# 65     description = Column(String, nullable=True)
# 66     config = Column(JSON, nullable=True)
# 67     artifact_path = Column(String, nullable=False)
# 68     tags = Column(JSON, nullable=True)
# 69     provider_type = Column(Enum(PROVIDER_TYPE), nullable=False, default=PROVIDER_TYPE.AGENT)
# 70 
# 71 class ContextProviderConfig(Base):
# 72     __tablename__ = "context_provider_config" 
# 73     id = Column(Integer, primary_key=True)
# 74     guid = Column(String, unique=True, nullable=False)
# 75     name = Column(String, nullable=False)
# 76     description = Column(String, nullable=True)
# 77     config = Column(JSON, nullable=True)
# 78     artifact_path = Column(String, nullable=False)
# 79     tags = Column(JSON, nullable=True)
# 80     provider_type = Column(Enum(PROVIDER_TYPE), nullable=False, default=PROVIDER_TYPE.AGENT)
# 81 
# 82 class AgentEngineProviderConfig(Base):
# 83     __tablename__ = "agent_engine_provider_config" 
# 84 
# 85     id = Column(Integer, primary_key=True)
# 86     guid = Column(String, unique=True, nullable=False)
# 87     name = Column(String, nullable=False)
# 88     description = Column(String, nullable=True)
# 89     model= Column(String, nullable=False)
# 90     config = Column(JSON, nullable=True)
# 91     cost_per_1k_tokens = Column(Float, nullable=True, default=0.0)
# 92     artifact_path = Column(String, nullable=False)
# 93     tags = Column(JSON, nullable=True)
# 94     provider_type = Column(Enum(PROVIDER_TYPE), nullable=False, default=PROVIDER_TYPE.AGENT)
# 95 
# 96 
# 97 class AgentProviderConfig(Base):
# 98     __tablename__ = "agent_provider_config" 
# 99 
# 100     id = Column(Integer, primary_key=True)
# 101     guid = Column(String, unique=True, nullable=False)
# 102     name = Column(String, nullable=False)
# 103     description = Column(String, nullable=True)
# 104     agent_type = Column(Enum(AGENT), nullable=False) 
# 105     config = Column(JSON, nullable=True)
# 106     artifact_path = Column(String, nullable=False)
# 107     tags = Column(JSON, nullable=True)
# 108     provider_type = Column(Enum(PROVIDER_TYPE), nullable=False, default=PROVIDER_TYPE.AGENT)
# 109 
# 110 class StateProviderConfig(Base):
# 111     __tablename__ = "state_provider_config"
# 112 
# 113     id = Column(Integer, primary_key=True)
# 114     guid = Column(TEXT, unique=True, default=lambda: str(uuid4()))
# 115     name = Column(String, nullable=False)
# 116     description = Column(String, nullable=True)
# 117     config = Column(JSON, nullable=True)
# 118     artifact_path = Column(String, nullable=False)
# 119     tags = Column(JSON, nullable=True)
# 120     provider_type = Column(Enum(PROVIDER_TYPE), nullable=False, default=PROVIDER_TYPE.AGENT)
# 121 
# 122 class SystemProviderConfig(Base):
# 123     __tablename__ = "system_provider_config"
# 124 
# 125     id = Column(Integer, primary_key=True)
# 126     guid = Column(TEXT, unique=True, default=lambda: str(uuid4()))
# 127     name = Column(String, nullable=False)
# 128     description = Column(String, nullable=True)
# 129     config = Column(JSON, nullable=True)
# 130     artifact_path = Column(String, nullable=False)
# 131     tags = Column(JSON, nullable=True)
# 132     system_type = Column(String, nullable=False)
# 133     provider_type = Column(Enum(PROVIDER_TYPE), nullable=False, default=PROVIDER_TYPE.AGENT)
# 134 
# 135 class ControllerProviderConfig(Base):
# 136     __tablename__ = "controller_provider_config"
# 137 
# 138     id = Column(Integer, primary_key=True)
# 139     guid = Column(TEXT, unique=True, default=lambda: str(uuid4()))
# 140     name = Column(String, nullable=False)
# 141     description = Column(String, nullable=True)
# 142     config = Column(JSON, nullable=True)   
# 143     artifact_path = Column(String, nullable=False)
# 144     tags = Column(JSON, nullable=True)
# 145     provider_type = Column(Enum(PROVIDER_TYPE), nullable=False, default=PROVIDER_TYPE.AGENT)
# 146 
# 147 class ProgramProviderConfig(Base):
# 148     __tablename__ = "program_provider_config"
# 149 
# 150     id = Column(Integer, primary_key=True)
# 151     guid = Column(TEXT, unique=True, default=lambda: str(uuid4()))
# 152     name = Column(String, nullable=False)
# 153     description = Column(String, nullable=True)
# 154     config = Column(JSON, nullable=True)
# 155     artifact_path = Column(String, nullable=False)
# 156     tags = Column(JSON, nullable=True)
# 157     provider_type = Column(Enum(PROVIDER_TYPE), nullable=False, default=PROVIDER_TYPE.AGENT)
# 158 
# 159 
# 160 class SessionConfig(Base):
# 161     __tablename__ = "session_config"
# 162 
# 163     id = Column(Integer, primary_key=True)
# 164     program_provider_id = Column(String, unique=True, nullable=False)
# 165     name = Column(String, nullable=False)
# 166     description = Column(String, nullable=True)
# 167     environment_type = Column(String, nullable=False)  #  e.g., "experiment", "live", "staging"
# 168     tags = Column(JSON, nullable=True)
# 169 
# 170 
# 171 # #  LOGS
# 172 
# 173 class ProviderLog(Base):
# 174     __tablename__ = "provider_log"
# 175 
# 176     id = Column(Integer, primary_key=True, index=True)
# 177     session_id = Column(String, nullable=False)
# 178     timestamp = Column(DateTime, nullable=False, default=datetime.now(timezone.utc))
# 179     provider_id = Column(Integer, nullable=False)
# 180     provider_type = Column(String, nullable=False)
# 181     input = Column(String, nullable=True)
# 182     output = Column(String, nullable=True)
# 183     output_schema = Column(String, nullable=True) 
# 184     latency_ms = Column(Integer, nullable=True)   
# 185     config_hash = Column(String, nullable=True)   
# 186     file_name = Column(String, nullable=True)
# 187     called_by_type = Column(String, nullable=True)
# 188     called_by_id = Column(Integer, nullable=True)
# 189     run_id = Column(TEXT, nullable=False, index=True)
# 190     file_log_id = Column(TEXT, nullable=True)
# 191     parent_id = Column(TEXT, nullable=True)
# 192     execution_chain = Column(JSON, nullable=False, default=list)
# 193 
# 194     
# 195 class StateTransitionLog(Base):
# 196     __tablename__ = "state_transition_log"
# 197 
# 198     id = Column(Integer, primary_key=True, index=True)
# 199     session_id = Column(String, nullable=False)
# 200     timestamp = Column(DateTime, nullable=False, default=datetime.now(timezone.utc))
# 201     entity_type = Column(String, nullable=False)
# 202     entity_id = Column(Integer, nullable=False)
# 203     from_state = Column(String, nullable=False)
# 204     to_state = Column(String, nullable=False)
# 205     reason = Column(String, nullable=True)
# 206     decision = Column(String, nullable=True)    
# 207     triggered_by = Column(String, nullable=True)   
# 208     step = Column(Integer, nullable=True)   
# 209     transition_metadata: Mapped[Optional[dict]] = mapped_column(JSON, nullable=True)
# 210     input_snapshot_id = Column(String, nullable=True)
# 211     output_snapshot_id = Column(String, nullable=True)
# 212     run_id = Column(TEXT, nullable=False, index=True)
# 213     called_by_type = Column(String, nullable=True)
# 214     called_by_id = Column(Integer, nullable=True) 
# 215     file_log_id = Column(TEXT, nullable=True)
# 216     parent_id = Column(TEXT, nullable=True)
# 217     execution_chain = Column(JSON, nullable=False, default=list)
# 218 
# 219 class AgentConversationLog(Base):
# 220     __tablename__ = "agent_conversation_log"
# 221 
# 222     id = Column(Integer, primary_key=True, index=True)
# 223     session_id = Column(String, nullable=False)
# 224     agent_provider_config_id = Column(Integer, nullable=False)
# 225     agent_type = Column(String, nullable=False) 
# 226     content = Column(String, nullable=False)
# 227     timestamp = Column(DateTime, nullable=False, default=datetime.now(timezone.utc))
# 228     run_id = Column(TEXT, nullable=False, index=True)
# 229     file_log_id = Column(TEXT, nullable=True)
# 230     
# 231 
# 232 class ErrorLog(Base):
# 233     __tablename__ = "error_log"
# 234 
# 235     id = Column(Integer, primary_key=True, index=True)
# 236     session_id = Column(String, nullable=False)
# 237     timestamp = Column(DateTime, nullable=False, default=datetime.now(timezone.utc))
# 238     latency_ms = Column(Integer, nullable=True) 
# 239     provider_id = Column(Integer, nullable=True)
# 240     provider_type = Column(String, nullable=True)
# 241     error_type = Column(String, nullable=False)
# 242     message = Column(String, nullable=False)
# 243     file_path = Column(String, nullable=True)
# 244     called_by_type = Column(String, nullable=True)
# 245     called_by_id = Column(Integer, nullable=True)
# 246     run_id = Column(TEXT, nullable=False, index=True)
# 247     file_log_id = Column(TEXT, nullable=True)
# 248     parent_id = Column(TEXT, nullable=True)
# 249     execution_chain = Column(JSON, nullable=False, default=list)
# 250 
# 251 class SnapshotMetrics(Base):
# 252     __tablename__ = "snapshot_metrics"
# 253 
# 254     id = Column(Integer, primary_key=True)
# 255     session_id = Column(String, nullable=False)
# 256     snapshot_id = Column(String, nullable=False)
# 257     system = Column(String, nullable=True)
# 258     agent = Column(String, nullable=True)
# 259     agent_type = Column(Enum(AGENT), nullable=True)
# 260     agent_id = Column(Integer, nullable=True)
# 261     score = Column(Float, nullable=True)
# 262     state = Column(String, nullable=True)
# 263     decision = Column(String, nullable=True)
# 264     timestamp = Column(DateTime, nullable=False, default=datetime.now(timezone.utc))
# 265     file_log_id = Column(TEXT, nullable=True)
# 266 
# 267     #  structural metrics
# 268     line_count_before = Column(Integer)
# 269     line_count_after = Column(Integer)
# 270     function_count_before = Column(Integer)
# 271     function_count_after = Column(Integer)
# 272     symbol_count_before = Column(Integer)
# 273     symbol_count_after = Column(Integer)
# 274     branch_count_before = Column(Integer)
# 275     branch_count_after = Column(Integer)
# 276     comment_count_before = Column(Integer)
# 277     comment_count_after = Column(Integer)
# 278 
# 279     #  deltas
# 280     line_count_delta = Column(Integer)
# 281     function_count_delta = Column(Integer)
# 282     symbol_count_delta = Column(Integer)
# 283     branch_count_delta = Column(Integer)
# 284     comment_count_delta = Column(Integer)
# 285 
# 286 
# 287 class FileLog(Base):
# 288     __tablename__ = "file_log"
# 289 
# 290     id = Column(Integer, primary_key=True, autoincrement=True)
# 291     session_id = Column(String, nullable=False)
# 292     file_name = Column(String, nullable=False)
# 293     original_path = Column(String)
# 294     length_bytes = Column(Integer)
# 295     created_at = Column(DateTime, default=datetime.now(timezone.utc))


# db\schemas.py

# 1 from dataclasses import dataclass, field
# 2 from datetime import datetime, timezone
# 3 from typing import Any, Dict, List, Optional
# 4 from pathlib import Path
# 5 from uuid import UUID, uuid4
# 6 from pydantic import BaseModel, Field, computed_field, field_validator
# 7 
# 8 from app.enums.fsm_enums import DECISION_TYPE, STATE_TYPE, TRANSITION_REASON_TYPE
# 9 from app.enums.logging_enums import ERROR_TYPE, PROVIDER_TYPE, RunContext
# 10 from app.enums.scoring_enums import SCORING_METRIC_TYPE
# 11 from app.enums.controller_enums import CONTROLLER
# 12 from app.enums.system_enums import SYSTEM
# 13 from app.enums.state_enums import STATE
# 14 from app.enums.agent_enums import AGENT
# 15 from app.enums.agent_engine_enums import AGENT_ENGINE_MODEL
# 16 
# 17 
# 18 class AgentPromptSchema(BaseModel):
# 19     id: Optional[int] = None
# 20     guid: UUID = Field(default_factory=uuid4)
# 21     name: str
# 22     description: Optional[str] = None
# 23     artifact_path: Path
# 24     tags: Optional[List[str]] = None
# 25 
# 26     @field_validator("artifact_path")
# 27     @classmethod
# 28     def validate_artifact_path(cls, v: Path) -> Path:
# 29         if not v.is_absolute() and ".." in v.parts:
# 30             raise ValueError("Invalid artifact path")
# 31         return v
# 32 
# 33 class SystemPromptSchema(BaseModel):
# 34     id: Optional[int] = None
# 35     guid: UUID = Field(default_factory=uuid4)
# 36     name: str
# 37     system_type: SYSTEM
# 38     description: Optional[str] = None
# 39     artifact_path: Path
# 40     tags: Optional[List[str]] = None
# 41 
# 42     @field_validator("artifact_path")
# 43     @classmethod
# 44     def validate_artifact_path(cls, v: Path) -> Path:
# 45         if not v.is_absolute() and ".." in v.parts:
# 46             raise ValueError("Invalid artifact path")
# 47         return v
# 48 
# 49 class PromptProviderConfig(BaseModel):
# 50     id: Optional[int] = None
# 51     guid: UUID = Field(default_factory=uuid4)
# 52     name: str
# 53     description: Optional[str] = None
# 54     artifact_path: Path
# 55     config: Optional[Dict[str, Any]] = None
# 56     tags: Optional[List[str]] = Field(default_factory=list)
# 57     provider_type: PROVIDER_TYPE = PROVIDER_TYPE.PROMPT
# 58 
# 59     @field_validator("artifact_path")
# 60     @classmethod
# 61     def validate_artifact_path(cls, v: Path) -> Path:
# 62         if not v.is_absolute() and ".." in v.parts:
# 63             raise ValueError("Invalid artifact path")
# 64         return v
# 65 
# 66 class ToolProviderConfig(BaseModel):
# 67     id: Optional[int] = None
# 68     guid: UUID = Field(default_factory=uuid4)
# 69     name: str
# 70     description: Optional[str] = None
# 71     config: Optional[Dict[str, Any]] = None
# 72     artifact_path: Path
# 73     tags: Optional[List[str]] = None
# 74     provider_type: PROVIDER_TYPE = PROVIDER_TYPE.TOOL
# 75 
# 76     @field_validator("artifact_path")
# 77     @classmethod
# 78     def validate_artifact_path(cls, v: Path) -> Path:
# 79         if not v.is_absolute() and ".." in v.parts:
# 80             raise ValueError("Invalid artifact path")
# 81         return v
# 82 
# 83 
# 84 class ScoreProviderConfig(BaseModel):
# 85     id: Optional[int] = None
# 86     guid: UUID = Field(default_factory=uuid4)
# 87     name: str
# 88     description: Optional[str] = None
# 89     artifact_path: Optional[Path] = None
# 90     tags: Optional[List[str]] = None
# 91     table_name: str = "score_provider"
# 92     provider_type: PROVIDER_TYPE = PROVIDER_TYPE.SCORE
# 93 
# 94     @field_validator("artifact_path")
# 95     @classmethod
# 96     def validate_artifact_path(cls, v: Optional[Path]) -> Optional[Path]:
# 97         if v is None:
# 98             return v
# 99         if not v.is_absolute() and ".." in v.parts:
# 100             raise ValueError("Invalid artifact path")
# 101         return v
# 102         
# 103 class AgentEngineProviderConfig(BaseModel):
# 104     id: Optional[int] = None
# 105     guid: UUID = Field(default_factory=uuid4)
# 106     name: str
# 107     description: Optional[str] = None
# 108     model: AGENT_ENGINE_MODEL  #  ✅ Enum enforced
# 109     config: Optional[Dict[str, Any]] = None
# 110     cost_per_1k_tokens: Optional[float] = Field(default=0.0)
# 111     artifact_path: Path
# 112     tags: Optional[List[str]] = None
# 113     provider_type: PROVIDER_TYPE = PROVIDER_TYPE.AGENT_ENGINE
# 114 
# 115     @field_validator("artifact_path")
# 116     @classmethod
# 117     def validate_artifact_path(cls, v: Path) -> Path:
# 118         if not v.is_absolute() and ".." in v.parts:
# 119             raise ValueError("Invalid artifact path")
# 120         return v
# 121 
# 122 
# 123 class AgentProviderConfigSchema(BaseModel):
# 124     id: Optional[int] = None
# 125     guid: UUID = Field(default_factory=uuid4)
# 126     name: str
# 127     description: Optional[str] = None
# 128     config: Optional[Dict[str, Any]] = None
# 129     artifact_path: Path
# 130     tags: Optional[list[str]] = None
# 131     agent_type: AGENT = AGENT.UNKNOWN 
# 132     provider_type: PROVIDER_TYPE = PROVIDER_TYPE.AGENT
# 133 
# 134     @field_validator("artifact_path")
# 135     @classmethod
# 136     def validate_artifact_path(cls, v: Path) -> Path:
# 137         if not v.is_absolute() and ".." in v.parts:
# 138             raise ValueError("Invalid artifact path")
# 139         return v
# 140 
# 141 class StateProviderConfigSchema(BaseModel):
# 142     id: Optional[int] = None
# 143     guid: UUID = Field(default_factory=uuid4)
# 144     name: str
# 145     description: Optional[str] = None
# 146     config: Optional[Dict[str, Any]] = None
# 147     artifact_path: Path
# 148     tags: Optional[list[str]] = None
# 149     provider_type: PROVIDER_TYPE = PROVIDER_TYPE.STATE
# 150 
# 151     @classmethod
# 152     def validate_artifact_path(cls, v: Path) -> Path:
# 153         if not v.is_absolute() and ".." in v.parts:
# 154             raise ValueError("Invalid artifact path")
# 155         return v
# 156 
# 157 class SystemProviderConfigSchema(BaseModel):
# 158     id: Optional[int] = None
# 159     guid: UUID = Field(default_factory=uuid4)
# 160     name: str
# 161     description: Optional[str] = None
# 162     config: Optional[Dict[str, Any]] = None
# 163     artifact_path: Path
# 164     tags: Optional[list[str]] = None
# 165     system_type: SYSTEM
# 166     provider_type: PROVIDER_TYPE = PROVIDER_TYPE.SYSTEM
# 167 
# 168     @classmethod
# 169     def validate_artifact_path(cls, v: Path) -> Path:
# 170         if not v.is_absolute() and ".." in v.parts:
# 171             raise ValueError("Invalid artifact path")
# 172         return v
# 173 
# 174 class ControllerProviderConfigSchema(BaseModel):
# 175     id: Optional[int] = None
# 176     guid: UUID = Field(default_factory=uuid4)
# 177     name: str
# 178     description: Optional[str] = None
# 179     config: Optional[Dict[str, Any]] = None
# 180     artifact_path: Path
# 181     tags: Optional[list[str]] = None
# 182     provider_type: PROVIDER_TYPE = PROVIDER_TYPE.CONTROLLER
# 183 
# 184     @classmethod
# 185     def validate_artifact_path(cls, v: Path) -> Path:
# 186         if not v.is_absolute() and ".." in v.parts:
# 187             raise ValueError("Invalid artifact path")
# 188         return v
# 189 
# 190 class ProgramProviderConfigSchema(BaseModel):
# 191     id: Optional[int] = None
# 192     guid: UUID = Field(default_factory=uuid4)
# 193     name: str
# 194     description: Optional[str] = None
# 195     config: Optional[Dict[str, Any]] = None
# 196     artifact_path: Path
# 197     tags: Optional[List[str]] = None
# 198     provider_type: PROVIDER_TYPE = PROVIDER_TYPE.PROGRAM
# 199 
# 200     @classmethod
# 201     def validate_artifact_path(cls, v: Path) -> Path:
# 202         if not v.is_absolute() and ".." in v.parts:
# 203             raise ValueError("Invalid artifact path")
# 204         return v
# 205 
# 206 class SessionConfigSchema(BaseModel):
# 207     id: Optional[int] = None
# 208     session_id: str
# 209     name: str
# 210     description: Optional[str] = None
# 211     environment_type: str  #  e.g., "experiment", "live", "staging"
# 212     config: Optional[Dict[str, Any]] = None
# 213     tags: Optional[List[str]] = None
# 214     
# 215 
# 216 #  LOGS
# 217 
# 218 @dataclass
# 219 class ProviderLogSchema:
# 220     session_id: str
# 221     provider_id: int
# 222     provider_type: PROVIDER_TYPE
# 223     input: Optional[str] = None
# 224     output: Optional[str] = None
# 225     output_schema: Optional[str] = None
# 226     latency_ms: Optional[int] = None
# 227     config_hash: Optional[str] = None
# 228     file_name: Optional[str] = None
# 229     timestamp: datetime = field(default_factory=lambda: datetime.now(timezone.utc))
# 230     called_by_type: Optional[PROVIDER_TYPE] = None
# 231     called_by_id: Optional[int] = None
# 232     run_id: str = field(default_factory=lambda: str(uuid4()))
# 233     file_log_id: Optional[str] = None
# 234     parent_id: Optional[str] = None
# 235     execution_chain: List[str] = field(default_factory=list)
# 236 
# 237 @dataclass
# 238 class StateTransitionLogSchema:
# 239     session_id: str
# 240     entity_type: PROVIDER_TYPE  #  e.g., "StateProvider", "SystemProvider", etc.
# 241     entity_id: int
# 242     from_state: str
# 243     to_state: str
# 244     reason: TRANSITION_REASON_TYPE
# 245     decision: DECISION_TYPE = DECISION_TYPE.UNKNOWN
# 246     triggered_by: str | None = None
# 247     step: int | None = None
# 248     transition_metadata: dict | None = None
# 249     timestamp: datetime = field(default_factory=lambda: datetime.now(timezone.utc))
# 250     run_id: str = field(default_factory=lambda: str(uuid4()))
# 251     called_by_type: PROVIDER_TYPE | None = None
# 252     called_by_id: int | None = None 
# 253     file_log_id: Optional[str] = None
# 254     parent_id: Optional[str] = None
# 255     execution_chain: List[str] = field(default_factory=list)
# 256 
# 257 @dataclass
# 258 class AgentConversationLogSchema:
# 259     session_id: str
# 260     agent_type: AGENT
# 261     agent_provider_config_id: int
# 262     content: str
# 263     timestamp: datetime = field(default_factory=lambda: datetime.now(timezone.utc))
# 264     run_id: str = field(default_factory=lambda: str(uuid4()))
# 265     file_log_id: Optional[str] = None
# 266 
# 267 @dataclass
# 268 class ErrorLogSchema:
# 269     session_id: str
# 270     error_type: ERROR_TYPE
# 271     message: str
# 272     file_path: str | None = None
# 273     provider_id: int | None = None
# 274     provider_type: PROVIDER_TYPE = PROVIDER_TYPE.UNKNOWN
# 275     timestamp: datetime = field(default_factory=lambda: datetime.now(timezone.utc))
# 276     latency_ms: Optional[int] = None
# 277     called_by_type: Optional[PROVIDER_TYPE] = None
# 278     called_by_id: Optional[int] = None
# 279     run_id: str = field(default_factory=lambda: str(uuid4()))
# 280     file_log_id: Optional[str] = None
# 281     parent_id: Optional[str] = None
# 282     execution_chain: List[str] = field(default_factory=list)
# 283     
# 284 @dataclass
# 285 class SnapshotMetricsSchema:
# 286     session_id: str
# 287     snapshot_id: str
# 288     system: SYSTEM
# 289     agent: str
# 290     agent_type: AGENT 
# 291     agent_id: int
# 292     score: float
# 293     state: str 
# 294     decision: DECISION_TYPE
# 295     timestamp: datetime
# 296     file_log_id: str
# 297 
# 298     line_count_before: int
# 299     line_count_after: int
# 300     function_count_before: int
# 301     function_count_after: int
# 302     symbol_count_before: int
# 303     symbol_count_after: int
# 304     branch_count_before: int
# 305     branch_count_after: int
# 306     comment_count_before: int
# 307     comment_count_after: int
# 308 
# 309     line_count_delta: int
# 310     function_count_delta: int
# 311     symbol_count_delta: int
# 312     branch_count_delta: int
# 313     comment_count_delta: int
# 314 
# 315 @dataclass
# 316 class FileLogSchema:
# 317     session_id: str
# 318     file_name: str
# 319     id: Optional[int] = None
# 320     original_path: Optional[str] = None
# 321     length_bytes: Optional[int] = None
# 322 
# 323 
# 324 #  Provider output schemas (for logs)
# 325 
# 326 class ToolOutputSchema(BaseModel):
# 327     return_code: int
# 328     stdout: Optional[str] = None
# 329     stderr: Optional[str] = None
# 330     violations: Optional[List[str]] = None  #  for linters like ruff
# 331     metrics: Optional[Dict[str, Any]] = None  #  for tools like sonarcloud
# 332     summary: Optional[str] = None  #  optional human-readable summary
# 333 
# 334 
# 335 class AgentEngineOutput(BaseModel):
# 336     response: str
# 337     token_count: int
# 338     cost_usd: float
# 339     snapshot_id: Optional[str] = None
# 340     summary: Optional[str] = None
# 341     content: Optional[str] = None 
# 342     decision: Optional[str] = None
# 343     log: Optional[str] = None
# 344 
# 345 
# 346 class AgentOutputSchema(BaseModel):
# 347     agent_type: Optional[AGENT] = AGENT.UNKNOWN 
# 348     decision: DECISION_TYPE = DECISION_TYPE.UNKNOWN
# 349     score: Optional[float] = None
# 350     response: Optional[str] = None
# 351     log: Optional[str] = None
# 352     file_path: Optional[str] = None
# 353 
# 354 
# 355 class ContextOutputSchema(BaseModel):
# 356     context: Dict[str, Any]
# 357     summary: Optional[str] = None
# 358 
# 359 
# 360 class PromptOutputSchema(BaseModel):
# 361     prompt: str
# 362     summary: Optional[str] = None
# 363 
# 364 
# 365 class FSMOutputSchema(BaseModel):
# 366     state: STATE
# 367     previous_state: Optional[STATE] = None
# 368     state_type: STATE_TYPE = STATE_TYPE.INTERMEDIATE
# 369     decision: Optional[DECISION_TYPE] = DECISION_TYPE.UNKNOWN
# 370     steps: int = Field(..., ge=0)
# 371     max_steps: int = Field(default=20, gt=0)
# 372     summary: Optional[str] = None
# 373     output: Optional[Dict[str, Any]] = None
# 374     provider_name: Optional[str] = None
# 375 
# 376 class SystemOutputSchema(FSMOutputSchema): pass
# 377 
# 378 class StateOutputSchema(FSMOutputSchema): 
# 379     previous_state: Optional[AGENT] = None
# 380 class ProgramOutputSchema(FSMOutputSchema):
# 381     previous_state: Optional[CONTROLLER] = None
# 382 class ControllerOutputSchema(FSMOutputSchema): 
# 383     previous_state: Optional[SYSTEM] = None
# 384 
# 385 
# 386 #  DTOs
# 387 
# 388 class Snapshot(BaseModel):
# 389     timestamp: datetime
# 390     file: str
# 391     score: float
# 392     decision: DECISION_TYPE
# 393 
# 394 class SnapshotContext(BaseModel):
# 395     context: RunContext
# 396     decision: str
# 397     log: str
# 398     state: str
# 399     agent_name: str
# 400     system: str
# 401     before_path: Path
# 402     after_content: str
# 403 
# 404     class Config:
# 405         arbitrary_types_allowed = True
# 406 
# 407 class SystemState(BaseModel):
# 408     system: str
# 409     file_path: str
# 410     working_file: str
# 411     final_file: Optional[str] = None
# 412     snapshots: List[Snapshot]
# 413     state: Optional[str] = "active"
# 414 
# 415 class AgentEngineExtraction(BaseModel):
# 416     content: str
# 417     decision: str
# 418     log: str
# 419 
# 420 
# 421 #  Score Providers
# 422 
# 423 from typing import Annotated, Union, Optional, Literal
# 424 from pydantic import BaseModel, Field, computed_field
# 425 from app.enums.scoring_enums import SCORING_METRIC_TYPE
# 426 
# 427 #  ─────────────────────────────────────────────────────────────
# 428 #  🧩 BASE SCHEMAS
# 429 #  ─────────────────────────────────────────────────────────────
# 430 
# 431 class ScoreConfigBase(BaseModel):
# 432     """Base schema for input config (weights, thresholds, etc.)"""
# 433     pass
# 434 
# 435 class ScoreComponentsBase(BaseModel):
# 436     """Base schema for output components (results/metrics)"""
# 437     pass
# 438 
# 439 
# 440 #  ─────────────────────────────────────────────────────────────
# 441 #  🧪 Linting Score
# 442 #  ─────────────────────────────────────────────────────────────
# 443 
# 444 class LintingScoreConfig(ScoreConfigBase):
# 445     weight_ruff: float
# 446     weight_black: float
# 447     weight_mypy: float
# 448     fail_threshold: float = 0.5
# 449 
# 450 class LintingScoreComponents(ScoreComponentsBase):
# 451     type: Literal["linting"]
# 452     ruff_score: float
# 453     ruff_violations: List[str]
# 454     black_score: float
# 455     black_violations: List[str]
# 456     mypy_score: float
# 457     mypy_violations: List[str]
# 458     tool_failure_count: int
# 459     weight_ruff: float
# 460     weight_black: float
# 461     weight_mypy: float
# 462     
# 463 
# 464 
# 465 #  ─────────────────────────────────────────────────────────────
# 466 #  🧬 Code Stability Score
# 467 #  ─────────────────────────────────────────────────────────────
# 468 
# 469 class CodeStabilityScoreConfig(ScoreConfigBase):
# 470     failure_penalty: float
# 471     diff_penalty: float
# 472     test_weight: float
# 473     coverage_weight: float
# 474 
# 475 class CodeStabilityScoreComponents(ScoreComponentsBase):
# 476     type: Literal["code_stability"]
# 477     utf8_valid: float
# 478     syntax_ok: float
# 479     py_compile_ok: float
# 480     can_import: float
# 481 
# 482 
# 483 #  ─────────────────────────────────────────────────────────────
# 484 #  🧾 Final Output Schema
# 485 #  ─────────────────────────────────────────────────────────────
# 486 
# 487 class ScoreOutputSchema(BaseModel):
# 488     name: SCORING_METRIC_TYPE = Field(..., description="The type of scoring metric used")
# 489     value: float = Field(..., description="Final weighted score (normalized 0.0–1.0 scale)")
# 490     components: Union[
# 491         Annotated[LintingScoreComponents, Field(discriminator="type")],
# 492         Annotated[CodeStabilityScoreComponents, Field(discriminator="type")]
# 493     ] = Field(..., description="Structured component scores (schema depends on provider)")
# 494     summary: Optional[str] = None


# db\seed.py

# 1 from sqlalchemy.orm import Session
# 2 from sqlalchemy import create_engine
# 3 from pathlib import Path
# 4 from app.db.seeders.seed_controller_providers import seed_controller_providers
# 5 from app.db.seeders.seed_program_provider import seed_program_providers
# 6 from app.db.seeders.seed_state_providers import seed_state_providers
# 7 from app.db.seeders.seed_agent_engine_providers import seed_agent_engine_providers
# 8 from app.db.seeders.seed_context_providers import seed_context_providers
# 9 from app.db.seeders.seed_prompt_providers import seed_prompt_providers
# 10 from app.db.seeders.seed_score_providers import seed_score_providers
# 11 from app.db.seeders.seed_system_providers import seed_system_providers
# 12 from app.db.seeders.seed_tool_providers import seed_tool_providers
# 13 from app.db.seeders.seed_prompts import seed_prompts
# 14 
# 15 #  Ensure your project root matches explicitly
# 16 PROJECT_ROOT = Path(__file__).resolve().parents[2]
# 17 DB_PATH = PROJECT_ROOT / "experiments" / "codecritic.sqlite3"
# 18 EXTENSIONS_DIR = PROJECT_ROOT / "extensions"
# 19 
# 20 def main():
# 21     engine = create_engine(f"sqlite:///{DB_PATH}")
# 22     with Session(bind=engine) as session:
# 23         seed_prompts(session)
# 24         seed_prompt_providers(session)
# 25         seed_tool_providers(session)
# 26         seed_score_providers(session)
# 27         seed_context_providers(session)
# 28         seed_agent_engine_providers(session)
# 29         seed_state_providers(session)
# 30         seed_system_providers(session)
# 31         seed_controller_providers(session)
# 32         seed_program_providers(session)
# 33 
# 34     print(f"Database seeded successfully at {DB_PATH.resolve()}")
# 35 
# 36 if __name__ == "__main__":
# 37     main()


# db\seeders\__init__.py



# db\seeders\files\__init__.py



# db\seeders\files\agent_engine_providers\__init__.py



# db\seeders\files\agent_engine_providers\basic_agent_engine_provider.py

# 1 from app.enums.logging_enums import RunContext
# 2 from app.db.schemas import AgentEngineOutput
# 3 from app.providers.agent_engine_provider_base import AgentEngineProviderBase
# 4 
# 5 class BasicAgentEngineProvider(AgentEngineProviderBase):
# 6     def _run(self, input: dict, context: RunContext | None = None) -> AgentEngineOutput:
# 7         if not self.prompt_provider:
# 8             raise ValueError("Prompt provider is required to extract engine output.")
# 9 
# 10         #  Simulate a basic LLM response format
# 11         response = """[AGENT_DECISION]accept[/AGENT_DECISION]
# 12 [CONVERSATION_LOG_ENTRY]Basic agent executed successfully.[/CONVERSATION_LOG_ENTRY]
# 13 [CODE]#  no code provided[/CODE]"""
# 14 
# 15         token_count = len(response.split())
# 16         cost_usd = token_count * (self._config.cost_per_1k_tokens or 0.0) / 1000
# 17 
# 18         extracted = self.prompt_provider._extract(response)
# 19 
# 20         return AgentEngineOutput(
# 21             response=response,
# 22             token_count=token_count,
# 23             cost_usd=cost_usd,
# 24             content=extracted.content,
# 25             decision=extracted.decision,
# 26             log=extracted.log
# 27         )


# db\seeders\files\agent_engine_providers\claude_3_sonnet_agent_engine_provider.py

# 1 import os
# 2 import json
# 3 import boto3
# 4 
# 5 from app.enums.logging_enums import RunContext
# 6 from app.db.schemas import AgentEngineOutput
# 7 from app.providers.agent_engine_provider_base import AgentEngineProviderBase
# 8 
# 9 
# 10 class Claude3SonnetAgentEngineProvider(AgentEngineProviderBase):
# 11     def _run(self, input: dict, context: RunContext | None = None) -> AgentEngineOutput:
# 12         if not self.prompt_provider:
# 13             raise ValueError("Prompt provider is required to extract engine output.")
# 14 
# 15         #  Step 1: Generate prompt using prompt provider
# 16         prompt_output = self.prompt_provider.run(input=input, context=context)
# 17         prompt = prompt_output.prompt
# 18 
# 19         #  Step 2: Call Claude via Bedrock
# 20         region = os.getenv("AWS_REGION", "us-east-1")
# 21         client = boto3.client("bedrock-runtime", region_name=region)
# 22         body = {
# 23             "anthropic_version": "bedrock-2023-05-31",
# 24             "messages": [{"role": "user", "content": prompt}],
# 25             "max_tokens": 1024,
# 26         }
# 27 
# 28         response_raw = client.invoke_model(
# 29             modelId="anthropic.claude-3-sonnet-20240229-v1:0",
# 30             contentType="application/json",
# 31             accept="application/json",
# 32             body=json.dumps(body),
# 33         )
# 34 
# 35         raw_response = json.loads(response_raw["body"].read())["content"][0]["text"].strip()
# 36         token_count = len(raw_response.split())
# 37         cost_usd = token_count * (self._config.cost_per_1k_tokens or 0.0) / 1000
# 38 
# 39         #  Step 3: Extract structured response using prompt provider
# 40         extracted = self.prompt_provider._extract(raw_response)
# 41 
# 42         return AgentEngineOutput(
# 43             response=raw_response,
# 44             token_count=token_count,
# 45             cost_usd=cost_usd,
# 46             content=extracted.content,
# 47             decision=extracted.decision,
# 48             log=extracted.log
# 49         )


# db\seeders\files\agent_engine_providers\gemini_1_5_pro_agent_engine_provider.py

# 1 import os
# 2 import requests
# 3 
# 4 from app.enums.logging_enums import RunContext
# 5 from app.db.schemas import AgentEngineOutput
# 6 from app.providers.agent_engine_provider_base import AgentEngineProviderBase
# 7 
# 8 
# 9 class Gemini1_5ProAgentEngineProvider(AgentEngineProviderBase):
# 10     def _run(self, input: dict, context: RunContext | None = None) -> AgentEngineOutput:
# 11         if not self.prompt_provider:
# 12             raise ValueError("Prompt provider is required to extract engine output.")
# 13 
# 14         #  Step 1: Run prompt provider to generate prompt
# 15         prompt_output = self.prompt_provider.run(input=input, context=context)
# 16         prompt = prompt_output.prompt
# 17 
# 18         #  Step 2: Call Gemini API with prompt
# 19         api_key = os.getenv("GOOGLE_API_KEY")
# 20         if not api_key:
# 21             raise EnvironmentError("GOOGLE_API_KEY is not set in the environment.")
# 22 
# 23         url = (
# 24             "https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-pro:generateContent"
# 25             f"?key={api_key}"
# 26         )
# 27         response_raw = requests.post(
# 28             url,
# 29             headers={"Content-Type": "application/json"},
# 30             json={"contents": [{"parts": [{"text": prompt}]}]},
# 31         )
# 32         response_raw.raise_for_status()
# 33         raw_response = response_raw.json()["candidates"][0]["content"]["parts"][0]["text"].strip()
# 34 
# 35         #  Step 3: Extract structured response using prompt provider
# 36         extracted = self.prompt_provider._extract(raw_response)
# 37 
# 38         #  Step 4: Build engine output
# 39         token_count = len(raw_response.split())
# 40         cost_usd = token_count * (self._config.cost_per_1k_tokens or 0.0) / 1000
# 41 
# 42         return AgentEngineOutput(
# 43             response=raw_response,
# 44             token_count=token_count,
# 45             cost_usd=cost_usd,
# 46             content=extracted.content,
# 47             decision=extracted.decision,
# 48             log=extracted.log
# 49         )


# db\seeders\files\agent_engine_providers\openai_gpt4o_agent_engine_provider.py

# 1 from app.db.schemas import AgentEngineOutput
# 2 from app.enums.fsm_enums import DECISION_TYPE
# 3 from app.providers.agent_engine_provider_base import AgentEngineProviderBase
# 4 from app.enums.logging_enums import RunContext
# 5 from openai import OpenAI
# 6 import os
# 7 
# 8 class OpenAIGPT4oAgentEngineProvider(AgentEngineProviderBase):
# 9     def _run(self, input: dict, context: RunContext | None = None) -> AgentEngineOutput:
# 10         prompt_provider = input.get("prompt_provider") 
# 11         input = {k: v for k, v in input.items() if k != "prompt_provider"}
# 12 
# 13         if not prompt_provider:
# 14             raise ValueError("Prompt provider is required to extract engine output.")
# 15 
# 16         try:
# 17             prompt_output = prompt_provider.run(input=input, context=context)
# 18             if prompt_output is None:
# 19                 raise RuntimeError("❌ prompt_provider.run() returned None")
# 20             prompt = prompt_output.prompt
# 21         except Exception as e:
# 22             print("❌ Error during prompt generation:")
# 23             print(e)
# 24             raise
# 25 
# 26         api_key = os.getenv("OPENAI_API_KEY")
# 27         if not api_key:
# 28             raise EnvironmentError("OPENAI_API_KEY is not set in the environment.")
# 29 
# 30         try:
# 31             client = OpenAI(api_key=api_key)
# 32             response = client.chat.completions.create(
# 33                 model="gpt-4o",
# 34                 messages=[
# 35                     {"role": "system", "content": "You are a helpful assistant."},
# 36                     {"role": "user", "content": prompt},
# 37                 ],
# 38                 temperature=0.2,
# 39                 max_tokens=2048,
# 40             )
# 41             raw_response = response.choices[0].message.content.strip()
# 42         except Exception as e:
# 43             print("❌ Error during OpenAI API call:")
# 44             print(e)
# 45             raise
# 46 
# 47         try:
# 48             extracted = prompt_provider._extract(raw_response)
# 49         except Exception as e:
# 50             print("❌ Error during response extraction:")
# 51             print(e)
# 52             print(f"🔍 Raw response was:\n{raw_response}")
# 53             raise
# 54 
# 55         token_count = len(raw_response.split())
# 56         cost_usd = token_count * (self._config.cost_per_1k_tokens or 0.0) / 1000
# 57 
# 58         return AgentEngineOutput(
# 59             response=raw_response,
# 60             token_count=token_count,
# 61             cost_usd=cost_usd,
# 62             content=extracted.content,
# 63             decision=DECISION_TYPE.UNKNOWN,
# 64             log=extracted.log
# 65         )


# db\seeders\files\agent_providers\__init__.py



# db\seeders\files\agent_providers\basic_agent_provider.py

# 1 from pathlib import Path
# 2 from app.enums.logging_enums import RunContext  #  ✅ Add for context typing
# 3 from app.providers.agent_provider_base import AgentProviderBase
# 4 from app.db.schemas import AgentOutputSchema
# 5 
# 6 
# 7 class BasicAgentProvider(AgentProviderBase):
# 8     def _run(self, input: dict, context: RunContext | None = None) -> AgentOutputSchema:
# 9         raw_file_path = input.get("before") or input.get("file_name")
# 10         relative_file_path = None
# 11 
# 12         if raw_file_path:
# 13             try:
# 14                 relative_file_path = str(Path(raw_file_path).resolve().relative_to(Path.cwd()))
# 15             except ValueError:
# 16                 relative_file_path = str(Path(raw_file_path).resolve())
# 17 
# 18         response = (
# 19             "[AGENT_DECISION]accept[/AGENT_DECISION]\n"
# 20             "[CONVERSATION_LOG_ENTRY]Basic agent executed successfully.[/CONVERSATION_LOG_ENTRY]\n"
# 21             f"[CODE]{input.get('code', '#  no code provided')}[/CODE]"
# 22         )
# 23 
# 24         return AgentOutputSchema(
# 25             response=response,
# 26             log="Basic agent executed successfully.",
# 27             decision="accept",
# 28             snapshot_id=None,
# 29             file_path=relative_file_path,
# 30         )


# db\seeders\files\agent_providers\code_stability_agent_provider.py

# 1 from pathlib import Path
# 2 from app.enums.fsm_enums import DECISION_TYPE
# 3 from app.providers.agent_provider_base import AgentProviderBase
# 4 from app.db.schemas import AgentOutputSchema
# 5 from app.enums.logging_enums import RunContext  #  needed to type context
# 6 
# 7 
# 8 class CodeStabilityAgentProvider(AgentProviderBase):
# 9     def _run(self, input: dict, context: RunContext | None = None) -> AgentOutputSchema:
# 10         score_result = self._score_provider.run(input, context=self.fork_context())
# 11 
# 12         raw_file_path = input.get("file_path") or input.get("file_name") or input.get("before")
# 13         relative_file_path = None
# 14         if raw_file_path:
# 15             try:
# 16                 relative_file_path = str(Path(raw_file_path).resolve().relative_to(Path.cwd()))
# 17             except ValueError:
# 18                 relative_file_path = str(Path(raw_file_path).resolve())
# 19 
# 20         if score_result.value >= 1.0:
# 21             return AgentOutputSchema(
# 22                 response="""[AGENT_DECISION]accept[/AGENT_DECISION]
# 23 [CONVERSATION_LOG_ENTRY]Code passed all stability checks.[/CONVERSATION_LOG_ENTRY]""",
# 24                 log="Code passed all stability checks.",
# 25                 decision=DECISION_TYPE.ACCEPTED,
# 26                 snapshot_id=None,
# 27                 file_path=relative_file_path,
# 28                 score=score_result.value
# 29             )
# 30 
# 31         return AgentOutputSchema(
# 32             response=f"""[AGENT_DECISION]reject[/AGENT_DECISION]
# 33 [CONVERSATION_LOG_ENTRY]Failed stability checks: {score_result.components}[/CONVERSATION_LOG_ENTRY]""",
# 34             log=f"Failed stability checks: {score_result.components}",
# 35             decision=DECISION_TYPE.REJECTED,
# 36             snapshot_id=None,
# 37             file_path=relative_file_path,
# 38             score=score_result.value
# 39         )


# db\seeders\files\agent_providers\linting_discriminator_agent_provider.py

# 1 from pathlib import Path
# 2 import uuid
# 3 import shutil
# 4 from datetime import datetime
# 5 
# 6 from app.enums.fsm_enums import DECISION_TYPE
# 7 from app.enums.logging_enums import RunContext
# 8 from app.providers.agent_provider_base import AgentProviderBase
# 9 from app.db.schemas import AgentOutputSchema
# 10 from app.utilities.diff_utils import summarize_diff
# 11 from app.utilities.metadata.snapshots.snapshot_archive import SnapshotArchive
# 12 
# 13 LINTING_PASS_THRESHOLD = 0.85
# 14 
# 15 class LintingDiscriminatorAgentProvider(AgentProviderBase):
# 16     def _run(self, input: dict, context: RunContext | None = None) -> AgentOutputSchema:
# 17         snapshot = SnapshotArchive(self._engine).read_latest(session_id=self._session_id)
# 18 
# 19         if not snapshot:
# 20             full_log = "No snapshot found."
# 21             return AgentOutputSchema(
# 22                 decision=DECISION_TYPE.REJECTED,
# 23                 log=full_log,
# 24                 response=(
# 25                     f"[AGENT_DECISION]reject[/AGENT_DECISION]\n"
# 26                     f"[CONVERSATION_LOG_ENTRY]{full_log}[/CONVERSATION_LOG_ENTRY]"
# 27                 ),
# 28                 file_path=None,
# 29                 score=None
# 30             )
# 31 
# 32         before_code = snapshot["before"]
# 33         after_code = snapshot["after"]
# 34         before_path = Path(snapshot["before_path"]).resolve()
# 35         after_path = Path(snapshot["after_path"]).resolve()
# 36 
# 37         before_result = self._score_provider.run(
# 38             {"file_path": str(before_path)},
# 39             context=self.fork_context()
# 40         )
# 41         after_result = self._score_provider.run(
# 42             {"file_path": str(after_path)},
# 43             context=self.fork_context()
# 44         )
# 45 
# 46         before_score = before_result.value
# 47         after_score = after_result.value
# 48 
# 49         def safe_relative(path: Path) -> str:
# 50             try:
# 51                 return str(path.relative_to(Path.cwd()))
# 52             except ValueError:
# 53                 return str(path)
# 54 
# 55         if before_code == after_code:
# 56             decision = DECISION_TYPE.ACCEPTED if before_score >= LINTING_PASS_THRESHOLD else DECISION_TYPE.REJECTED
# 57             full_log = (
# 58                 "No meaningful change, but score already passing."
# 59                 if decision == DECISION_TYPE.ACCEPTED
# 60                 else "No meaningful change and score below threshold."
# 61             )
# 62             return AgentOutputSchema(
# 63                 decision=decision,
# 64                 log=full_log,
# 65                 response=(
# 66                     f"[AGENT_DECISION]{decision}[/AGENT_DECISION]\n"
# 67                     f"[CONVERSATION_LOG_ENTRY]{full_log}[/CONVERSATION_LOG_ENTRY]"
# 68                 ),
# 69                 file_path=safe_relative(before_path),
# 70                 score=before_score
# 71             )
# 72 
# 73         summary = summarize_diff(before_code, after_code).strip()
# 74         if not summary:
# 75             decision = DECISION_TYPE.ACCEPTED if before_score >= LINTING_PASS_THRESHOLD else DECISION_TYPE.REJECTED
# 76             full_log = (
# 77                 "Diff could not be summarized, but prior version passes."
# 78                 if decision == DECISION_TYPE.ACCEPTED
# 79                 else "Diff could not be summarized and prior version fails."
# 80             )
# 81             return AgentOutputSchema(
# 82                 decision=decision,
# 83                 log=full_log,
# 84                 response=(
# 85                     f"[AGENT_DECISION]{decision}[/AGENT_DECISION]\n"
# 86                     f"[CONVERSATION_LOG_ENTRY]{full_log}[/CONVERSATION_LOG_ENTRY]"
# 87                 ),
# 88                 file_path=safe_relative(before_path),
# 89                 score=before_score
# 90             )
# 91 
# 92         after_components = getattr(after_result, "components", {})
# 93         violation_msgs = []
# 94 
# 95         def get_violations(field) -> list[str]:
# 96             val = getattr(after_components, field, None)
# 97             if isinstance(val, list):
# 98                 return val
# 99             if isinstance(val, int):
# 100                 return [f"{val} issues"]
# 101             return []
# 102 
# 103         def append_messages(name: str, msgs: list[str]):
# 104             if msgs:
# 105                 joined = "\n    - " + "\n    - ".join(msgs)
# 106                 violation_msgs.append(f"{name}:\n{joined}")
# 107 
# 108         append_messages("ruff", get_violations("ruff_violations"))
# 109         append_messages("black", get_violations("black_violations"))
# 110         append_messages("mypy", get_violations("mypy_violations"))
# 111 
# 112         violations_summary = (
# 113             f"\n\n\u26a0\ufe0f Remaining violations:\n\n" + "\n\n".join(violation_msgs)
# 114             if violation_msgs else "No Remaining violations"
# 115         )
# 116 
# 117         if after_score >= LINTING_PASS_THRESHOLD and after_score >= before_score:
# 118             decision = DECISION_TYPE.ACCEPTED
# 119             chosen_code = after_code
# 120             score = after_score
# 121         elif after_score < LINTING_PASS_THRESHOLD and after_score > before_score:
# 122             decision = DECISION_TYPE.IMPROVED
# 123             chosen_code = after_code
# 124             score = after_score
# 125         else:
# 126             decision = DECISION_TYPE.REJECTED
# 127             chosen_code = before_code
# 128             score = before_score
# 129 
# 130         temp_path = Path("working_files") / f"temp_agent_{datetime.now().strftime('%H%M%S%f')[:10]}.py"
# 131         temp_path.parent.mkdir(parents=True, exist_ok=True)
# 132         source_path = after_path if chosen_code == after_code else before_path
# 133         shutil.copyfile(source_path, temp_path)
# 134 
# 135         full_log = (
# 136             f"✅ Score improved from {before_score:.2f} to {after_score:.2f} and passes threshold."
# 137             f"{violations_summary}\n\n{summary}"
# 138         )
# 139 
# 140         return AgentOutputSchema(
# 141             decision=decision,
# 142             log=full_log,
# 143             response=(
# 144                 f"[AGENT_DECISION]{decision}[/AGENT_DECISION]\n"
# 145                 f"[CONVERSATION_LOG_ENTRY]{full_log}[/CONVERSATION_LOG_ENTRY]"
# 146             ),
# 147             file_path=safe_relative(temp_path),
# 148             score=score
# 149         )


# db\seeders\files\agent_providers\linting_generator_agent_provider.py

# 1 from pathlib import Path
# 2 
# 3 from app.enums.fsm_enums import DECISION_TYPE
# 4 from app.enums.logging_enums import RunContext
# 5 from app.providers.agent_provider_base import AgentProviderBase
# 6 from app.db.schemas import AgentOutputSchema, SnapshotContext
# 7 from app.utilities.metadata.snapshots.snapshot_archive import SnapshotArchive
# 8 
# 9 
# 10 class LintingGeneratorAgentProvider(AgentProviderBase):
# 11     """Runs a generation round using the linting system prompt, context, and snapshot."""
# 12 
# 13     def _run(self, input: dict, context: RunContext | None = None) -> AgentOutputSchema:
# 14         file_path = input.get("file_path")
# 15 
# 16         system = input.get("system", "linting")
# 17 
# 18         if not self._prompt_provider:
# 19             raise ValueError("Prompt provider is not set")
# 20         if not self._agent_engine:
# 21             raise ValueError("Agent engine is not set")
# 22 
# 23         #  🔧 Generate prompt and run engine
# 24         prompt_output = self._prompt_provider.run(input=input, context=context)
# 25         engine_output = self._agent_engine.run(
# 26             input={
# 27                 "prompt": prompt_output.prompt,
# 28                 "file_path": file_path,
# 29                 "agent_type": self._config.agent_type,
# 30                 "agent_id": self._config.id,
# 31                 "system": system,
# 32                 "state_context": input.get("state_context", {}),
# 33                 "prompt_provider": self._prompt_provider, 
# 34             },
# 35             context=context
# 36         )
# 37 
# 38         #  🔧 Write snapshot
# 39         before_path = Path(file_path).resolve()
# 40         snapshot = SnapshotContext(
# 41             context=context,
# 42             decision=engine_output.decision,
# 43             log=engine_output.log,
# 44             state=input.get("state_context", {}).get("state", "unknown"),
# 45             agent_name=self._config.name,
# 46             system=system,
# 47             before_path=before_path,
# 48             after_content=engine_output.content,
# 49         )
# 50 
# 51         snapshot_id = SnapshotArchive(self._engine).record(snapshot=snapshot)
# 52 
# 53         return AgentOutputSchema(
# 54             response=engine_output.response,
# 55             log=engine_output.log,
# 56             decision=DECISION_TYPE(engine_output.decision),
# 57             snapshot_id=snapshot_id,
# 58             file_path=str(before_path),
# 59             score=getattr(engine_output, "score", None)
# 60         )


# db\seeders\files\context_providers\__init__.py



# db\seeders\files\context_providers\basic_context_provider.py

# 1 from app.enums.logging_enums import RunContext  #  ✅ Needed for context typing
# 2 from app.providers.context_provider_base import ContextProviderBase
# 3 from app.db.schemas import ContextOutputSchema
# 4 
# 5 class BasicContextProvider(ContextProviderBase):
# 6     def _run(self, input: dict, context: RunContext | None = None) -> ContextOutputSchema:
# 7         return ContextOutputSchema(
# 8             context={"file_path": input.get("file_path")},
# 9             summary="Basic context with file_path only."
# 10         )


# db\seeders\files\context_providers\linting_context_provider.py

# 1 from __future__ import annotations
# 2 from pathlib import Path
# 3 import json
# 4 
# 5 from sqlalchemy.orm import Session
# 6 from app.enums.logging_enums import RunContext
# 7 from app.providers.context_provider_base import ContextProviderBase
# 8 from app.utilities.metadata.logging.conversation_log import get_conversation_log
# 9 from app.db.schemas import ContextOutputSchema
# 10 
# 11 
# 12 class LintingContextProvider(ContextProviderBase):
# 13     def _run(self, input: dict, context: RunContext | None = None) -> ContextOutputSchema:
# 14         file_path = Path(input["file_path"]).resolve()
# 15 
# 16         if not file_path.exists():
# 17             raise FileNotFoundError(f"❌ File not found: {file_path}")
# 18 
# 19         source_code = file_path.read_text(encoding="utf-8")
# 20 
# 21         #  🔧 propagate context
# 22         score_result = self.score_provider.run(
# 23             input={"file_path": str(file_path)},
# 24             context=context
# 25         )
# 26 
# 27         with Session(bind=self._engine) as session:
# 28             convo_log = get_conversation_log(
# 29                 session=session,
# 30                 session_id=self._session_id,
# 31                 file_log_id=context.file_log_id
# 32             )
# 33 
# 34         context_data = {
# 35             "file_path": str(file_path),
# 36             "source_code": source_code,
# 37             "score": score_result.model_dump(),
# 38             "conversation_log": convo_log
# 39         }
# 40 
# 41         return ContextOutputSchema(
# 42             context=context_data,
# 43             summary=f"Context for {file_path.name}, {score_result.value} score, {len(convo_log)} log entries"
# 44         )


# db\seeders\files\controller_providers\__init__.py



# db\seeders\files\controller_providers\preprocessing_controller_provider.py

# 1 from app.enums.fsm_enums import DECISION_TYPE, TRANSITION_REASON_TYPE
# 2 from app.enums.system_enums import SYSTEM
# 3 from app.providers.controller_provider_base import ControllerProviderBase
# 4 
# 5 class PreprocessingControllerProvider(ControllerProviderBase):
# 6     def _transition(self, state, sys_output):
# 7         current = SYSTEM(state["state"])
# 8         transition = {}
# 9 
# 10         #  Transition from SYSTEM.START to SYSTEM.LINTING
# 11         if current == SYSTEM.START:
# 12             transition = {
# 13                 "state": SYSTEM.LINTING,
# 14                 "reason": TRANSITION_REASON_TYPE.INITIALIZATION,
# 15             }
# 16 
# 17         #  Transition from SYSTEM.LINTING to SYSTEM.END based on system output decision
# 18         elif current == SYSTEM.LINTING:
# 19             #  Check sys_output directly for decision
# 20             if sys_output:
# 21                 #  Directly check for 'decision' attribute in sys_output
# 22                 decision = sys_output.decision if hasattr(sys_output, 'decision') else DECISION_TYPE.UNKNOWN
# 23 
# 24                 #  Determine the transition reason based on the decision
# 25                 if decision == DECISION_TYPE.ACCEPTED:
# 26                     transition = {
# 27                         "state": SYSTEM.END,
# 28                         "reason": TRANSITION_REASON_TYPE.SUCCESSFUL,
# 29                     }
# 30                 elif decision == DECISION_TYPE.REJECTED:
# 31                     transition = {
# 32                         "state": SYSTEM.END,
# 33                         "reason": TRANSITION_REASON_TYPE.UNSUCCESSFUL,
# 34                     }
# 35                 else:
# 36                     transition = {
# 37                         "state": SYSTEM.END,
# 38                         "reason": TRANSITION_REASON_TYPE.CUSTOM_RULE,
# 39                     }
# 40 
# 41                 #  Propagate the decision and file path to the next state
# 42                 transition["file_path"] = sys_output.file_path if hasattr(sys_output, 'file_path') else state.get("file_path")
# 43                 transition["decision"] = decision
# 44             else:
# 45                 #  Default case if no valid output is found
# 46                 transition = {
# 47                     "state": SYSTEM.END,
# 48                     "reason": TRANSITION_REASON_TYPE.CUSTOM_RULE,
# 49                 }
# 50 
# 51         else:
# 52             #  Handle any other states that don't match SYSTEM.START or SYSTEM.LINTING
# 53             transition = {
# 54                 "state": SYSTEM.END,
# 55                 "reason": TRANSITION_REASON_TYPE.CUSTOM_RULE,
# 56             }
# 57 
# 58         return transition


# db\seeders\files\program_providers\__init__.py



# db\seeders\files\program_providers\codecritic_program_provider.py

# 1 from app.enums.controller_enums import CONTROLLER
# 2 from app.enums.fsm_enums import DECISION_TYPE, TRANSITION_REASON_TYPE
# 3 from app.providers.program_provider_base import ProgramProviderBase
# 4 
# 5 class CodeCriticProgramProvider(ProgramProviderBase):
# 6     def _transition(self, state, ctrl_output):
# 7         current = CONTROLLER(state["state"])
# 8         transition = {}
# 9 
# 10         if current == CONTROLLER.START:
# 11             return {
# 12                 "state": CONTROLLER.PREPROCESSING,
# 13                 "reason": TRANSITION_REASON_TYPE.INITIALIZATION,
# 14             }
# 15 
# 16         elif current == CONTROLLER.PREPROCESSING:
# 17             #  Safely extract decision
# 18             decision = getattr(ctrl_output, "decision", DECISION_TYPE.UNKNOWN)
# 19 
# 20             #  Route based on decision
# 21             if decision == DECISION_TYPE.ACCEPTED:
# 22                 transition = {
# 23                     "state": CONTROLLER.END,
# 24                     "reason": TRANSITION_REASON_TYPE.SUCCESSFUL,
# 25                 }
# 26             elif decision == DECISION_TYPE.REJECTED:
# 27                 transition = {
# 28                     "state": CONTROLLER.END,
# 29                     "reason": TRANSITION_REASON_TYPE.UNSUCCESSFUL,
# 30                 }
# 31             else:
# 32                 transition = {
# 33                     "state": CONTROLLER.END,
# 34                     "reason": TRANSITION_REASON_TYPE.CUSTOM_RULE,
# 35                 }
# 36 
# 37             transition["file_path"] = getattr(ctrl_output, "file_path", state.get("file_path"))
# 38             transition["decision"] = decision
# 39             return transition
# 40 
# 41         else:
# 42             return {
# 43                 "state": CONTROLLER.END,
# 44                 "reason": TRANSITION_REASON_TYPE.CUSTOM_RULE,
# 45             }
# 46     


# db\seeders\files\prompt_providers\__init__.py



# db\seeders\files\prompt_providers\basic_prompt_provider.py

# 1 from app.enums.logging_enums import RunContext  #  ✅ for type annotation
# 2 from app.providers.prompt_provider_base import PromptProviderBase
# 3 from app.db.schemas import AgentEngineExtraction, PromptOutputSchema
# 4 
# 5 class BasicPromptProvider(PromptProviderBase):
# 6     def _run(self, input: dict, context: RunContext | None = None) -> PromptOutputSchema:
# 7         agent_config = input.get("agent_config", {})
# 8         system_config = input.get("system_config", {})
# 9 
# 10         prompt = (
# 11             f"User: {agent_config.get('input', 'No input provided')}\n"
# 12             f"System: Response mode: {system_config.get('setting', 'default')}"
# 13         )
# 14 
# 15         return PromptOutputSchema(
# 16             prompt=prompt,
# 17             summary="Basic prompt using agent and system config"
# 18         )
# 19     
# 20     def _extract(self, response: str) -> AgentEngineExtraction:
# 21         return AgentEngineExtraction(
# 22             content="#  no code provided",
# 23             decision="accept",
# 24             log="Basic agent executed successfully."
# 25         )


# db\seeders\files\prompt_providers\linting_prompt_provider.py

# 1 from __future__ import annotations
# 2 import json
# 3 from pathlib import Path
# 4 
# 5 from app.enums.logging_enums import RunContext
# 6 from app.providers.prompt_provider_base import PromptProviderBase
# 7 from app.db.schemas import AgentEngineExtraction, PromptOutputSchema
# 8 from app.utilities.extract_code_block import extract_code_blocks
# 9 
# 10 
# 11 class LintingPromptProvider(PromptProviderBase):
# 12     def _run(self, input: dict, context: RunContext | None = None) -> PromptOutputSchema:
# 13         session_id = self._session_id
# 14         system = input.get("system", "unknown")
# 15         file_path = input.get("file_path")
# 16 
# 17         if not self.agent_text or not self.system_text or not self._context_provider:
# 18             raise ValueError("Missing required prompt text or context provider")
# 19 
# 20         #  ✅ propagate context to context provider
# 21         context_output = self._context_provider.run(
# 22             {
# 23                 "file_path": file_path,
# 24                 "system": system
# 25             },
# 26             context=context
# 27         )
# 28 
# 29         context = (
# 30             context_output.context
# 31             if hasattr(context_output, "context")
# 32             else json.loads(context_output)
# 33         )
# 34 
# 35         convo_section = "\n".join(context.get("conversation_log", [])) or "(none)"
# 36 
# 37         full_prompt = f"""
# 38 {self.system_text}
# 39 
# 40 ---
# 41 
# 42 {self.agent_text}
# 43 
# 44 ---
# 45 
# 46 [CONTEXT]
# 47 {json.dumps(context, indent=2)}
# 48 
# 49 ---
# 50 
# 51 [CONVERSATION LOG]
# 52 {convo_section}
# 53 """.strip()
# 54 
# 55         #  🧾 Construct summary based on the file path and system
# 56         short_path = Path(file_path).name if file_path else "unknown file"
# 57         summary = f"Linting prompt generated for {short_path} (system: {system})"
# 58 
# 59         return PromptOutputSchema(prompt=full_prompt, summary=summary)
# 60 
# 61     def _extract(self, response: str) -> AgentEngineExtraction:
# 62         return extract_code_blocks(response)


# db\seeders\files\prompts\__init__.py



# db\seeders\files\score_providers\__init__.py



# db\seeders\files\score_providers\code_stability_score_provider.py

# 1 import ast
# 2 import json
# 3 import py_compile
# 4 import importlib.util
# 5 from pathlib import Path
# 6 from typing import Dict
# 7 import contextlib
# 8 import sys
# 9 import io
# 10 import uuid
# 11 
# 12 from pydantic import BaseModel
# 13 
# 14 from app.enums.logging_enums import RunContext
# 15 from app.providers.score_provider_base import ScoreProviderBase
# 16 from app.db.schemas import CodeStabilityScoreConfig, ScoreOutputSchema, CodeStabilityScoreComponents
# 17 from app.enums.scoring_enums import SCORING_METRIC_TYPE
# 18 from app.utilities.metadata.footer.code_annnotation_utils import split_content_and_notes
# 19 
# 20 
# 21 @contextlib.contextmanager
# 22 def suppress_output():
# 23     original_stdout = sys.stdout
# 24     original_stderr = sys.stderr
# 25     sys.stdout = io.StringIO()
# 26     sys.stderr = io.StringIO()
# 27     try:
# 28         yield
# 29     finally:
# 30         sys.stdout = original_stdout
# 31         sys.stderr = original_stderr
# 32 
# 33 
# 34 class CodeStabilityScoreProvider(ScoreProviderBase):
# 35     ConfigSchema = CodeStabilityScoreConfig
# 36     def _run(self, input: dict, context: RunContext | None = None) -> ScoreOutputSchema:
# 37         original_path = Path(input["file_path"]).resolve()
# 38         bool_components: Dict[str, bool] = {}
# 39 
# 40         #  Read raw code and verify UTF-8
# 41         try:
# 42             raw_code = original_path.read_text(encoding="utf-8")
# 43             bool_components["utf8_valid"] = True
# 44         except Exception:
# 45             bool_components["utf8_valid"] = False
# 46             return self._final_score(bool_components)
# 47 
# 48         #  Strip footer annotations without modifying actual code structure
# 49         stripped_code, _ = split_content_and_notes(raw_code)
# 50 
# 51         #  Always write a clean temp file with newline termination
# 52         safe_path = Path("working_files") / f"{uuid.uuid4().hex}_stripped.py"
# 53         safe_path.write_text(stripped_code.rstrip() + "\n", encoding="utf-8")
# 54 
# 55         #  Reload stripped_code from disk to ensure parity with lint input
# 56         stripped_code = safe_path.read_text(encoding="utf-8")
# 57 
# 58         with suppress_output():
# 59             #  Syntax and compile check
# 60             try:
# 61                 compile(stripped_code, str(safe_path), "exec")
# 62                 ast.parse(stripped_code)
# 63                 bool_components["syntax_ok"] = True
# 64             except (SyntaxError, IndentationError):
# 65                 bool_components["syntax_ok"] = False
# 66                 return self._final_score(bool_components)
# 67 
# 68             #  py_compile
# 69             try:
# 70                 py_compile.compile(str(safe_path), doraise=True)
# 71                 bool_components["py_compile_ok"] = True
# 72             except Exception:
# 73                 bool_components["py_compile_ok"] = False
# 74                 return self._final_score(bool_components)
# 75 
# 76             #  Import stripped code
# 77             try:
# 78                 spec = importlib.util.spec_from_file_location("mod", str(safe_path))
# 79                 module = importlib.util.module_from_spec(spec)
# 80                 spec.loader.exec_module(module)  #  type: ignore
# 81                 bool_components["can_import"] = True
# 82             except Exception:
# 83                 bool_components["can_import"] = False
# 84                 return self._final_score(bool_components)
# 85 
# 86         return self._final_score(bool_components)
# 87 
# 88     def _final_score(self, bool_components: Dict[str, bool]) -> ScoreOutputSchema:
# 89         #  📦 Convert to float for schema compatibility
# 90         components = CodeStabilityScoreComponents(
# 91             type="code_stability", 
# 92             utf8_valid=float(bool_components.get("utf8_valid", False)),
# 93             syntax_ok=float(bool_components.get("syntax_ok", False)),
# 94             py_compile_ok=float(bool_components.get("py_compile_ok", False)),
# 95             can_import=float(bool_components.get("can_import", False))
# 96         )
# 97         #  ✅ Overall score decision
# 98         critical_keys = [
# 99             components.utf8_valid,
# 100             components.syntax_ok,
# 101             components.py_compile_ok,
# 102             components.can_import
# 103         ]
# 104         passed = all(v >= 1.0 for v in critical_keys)
# 105         value = 1.0 if passed else 0.0
# 106         summary = "✅ Executable" if passed else "❌ Not executable"
# 107 
# 108         return ScoreOutputSchema(
# 109             name=SCORING_METRIC_TYPE.CODE_STABILITY_SCORE,
# 110             value=value,
# 111             components=components,
# 112             summary=summary
# 113         )
# 114 


# db\seeders\files\score_providers\linting_score_provider.py

# 1 from __future__ import annotations
# 2 
# 3 from copy import deepcopy
# 4 import uuid
# 5 from pathlib import Path
# 6 from typing import Dict, List, Tuple, Any
# 7 
# 8 from app.enums.logging_enums import RunContext
# 9 from app.providers.score_provider_base import ScoreProviderBase
# 10 from app.db.schemas import (
# 11     LintingScoreConfig,
# 12     ScoreOutputSchema,
# 13     LintingScoreComponents
# 14 )
# 15 from app.enums.scoring_enums import SCORING_METRIC_TYPE
# 16 from app.utilities.metadata.footer.code_annnotation_utils import split_content_and_notes
# 17 
# 18 
# 19 class LintingScoreProvider(ScoreProviderBase):
# 20     ConfigSchema = LintingScoreConfig 
# 21     _DEFAULT_WEIGHTS: Dict[str, float] = {"ruff": 0.7, "black": 0.2, "mypy": 0.1}
# 22     _DEFAULT_THRESHOLD: float | None = None
# 23     _MAX_RUFF_VIOLATIONS_CONSIDERED = 10
# 24 
# 25     def _run(self, input: dict, context: RunContext | None = None) -> ScoreOutputSchema:
# 26         file_path = input["file_path"]
# 27         file_path = self._make_path_from_raw(file_path)
# 28         full_code = file_path.read_text(encoding="utf-8")
# 29         clean_code, _ = split_content_and_notes(full_code)
# 30 
# 31         stripped_path = self._write_working_copy(clean_code)
# 32 
# 33         available = {tp._config.name.lower(): tp for tp in self.tool_providers}
# 34         ruff_score, ruff_violations, tool_failures = self._run_ruff(available, stripped_path, context)
# 35         black_score, black_violations = self._run_tool(
# 36             "black", available, stripped_path, tool_failures, context, collect_violations=True
# 37         )
# 38         mypy_score, mypy_violations = self._run_tool(
# 39             "mypy", available, stripped_path, tool_failures, context, collect_violations=True
# 40         )
# 41 
# 42         weights = self._parse_weights(input.get("weights"))
# 43         weighted_score = round(
# 44             ruff_score * weights["ruff"]
# 45             + black_score * weights["black"]
# 46             + mypy_score * weights["mypy"],
# 47             3,
# 48         )
# 49 
# 50         threshold = input.get("threshold", self._DEFAULT_THRESHOLD)
# 51         meets_threshold = (threshold is None) or (weighted_score >= threshold)
# 52 
# 53         #  🧩 Structured component output
# 54         components = LintingScoreComponents(
# 55             type="linting",
# 56             ruff_score=ruff_score,
# 57             ruff_violations=ruff_violations,            
# 58             black_score=black_score,
# 59             black_violations=black_violations,    
# 60             mypy_score=mypy_score,
# 61             mypy_violations=mypy_violations,
# 62             tool_failure_count=len(tool_failures),
# 63             weight_ruff=weights["ruff"],
# 64             weight_black=weights["black"],
# 65             weight_mypy=weights["mypy"],
# 66         )
# 67 
# 68         summary = self._build_summary(
# 69             ruff_violations=ruff_violations,
# 70             tool_failures=tool_failures,
# 71             meets_threshold=meets_threshold,
# 72             threshold=threshold,
# 73         )
# 74 
# 75         return ScoreOutputSchema(
# 76             name=SCORING_METRIC_TYPE.LINTING_SCORE,
# 77             value=weighted_score,
# 78             components=components,
# 79             summary=summary,
# 80         )
# 81 
# 82     def _make_path_from_raw(self, maybe_code: str) -> Path:
# 83         if Path(maybe_code).exists() and "\n" not in maybe_code:
# 84             return Path(maybe_code).resolve()
# 85 
# 86         tmp = Path("experiments/snapshots") / f"{uuid.uuid4().hex}.py"
# 87         tmp.parent.mkdir(parents=True, exist_ok=True)
# 88         tmp.write_text(maybe_code, encoding="utf-8")
# 89         print(f"\U0001f4c4  Created temp file for raw code: {tmp}")
# 90         return tmp.resolve()
# 91 
# 92     def _write_working_copy(self, clean_code: str) -> Path:
# 93         stripped = Path("working_files") / f"{uuid.uuid4().hex}_stripped.py"
# 94         stripped.parent.mkdir(parents=True, exist_ok=True)
# 95         stripped.write_text(clean_code.rstrip() + "\n", encoding="utf-8")
# 96         return stripped.resolve()
# 97 
# 98     def _run_ruff(
# 99         self,
# 100         available: Dict[str, Any],
# 101         target_path: Path,
# 102         context: RunContext | None,
# 103     ) -> Tuple[float, List[str], Dict[str, str]]:
# 104         tool_failures: Dict[str, str] = {}
# 105         score, violations = self._run_tool_with_context(
# 106             tool=available["ruff"],
# 107             input={"target": str(target_path), "check": True},
# 108             context=context,
# 109             collect_violations=True,
# 110         )
# 111         score = 1.0 - min(1.0, len(violations) / self._MAX_RUFF_VIOLATIONS_CONSIDERED)
# 112         return score, violations, tool_failures
# 113 
# 114     def _run_tool(
# 115         self,
# 116         name: str,
# 117         available: Dict[str, Any],
# 118         target_path: Path,
# 119         failures: Dict[str, str],
# 120         context: RunContext | None,
# 121         collect_violations: bool = False,
# 122     ) -> Tuple[float, List[str]]:
# 123         if name not in available:
# 124             failures[name] = "not_available"
# 125             return 0.0, []
# 126 
# 127         try:
# 128             score, violations = self._run_tool_with_context(
# 129                 tool=available[name],
# 130                 input={"target": str(target_path), "check": True},
# 131                 context=context,
# 132                 collect_violations=collect_violations,
# 133             )
# 134         except Exception as exc:
# 135             failures[name] = f"crashed: {exc}"
# 136             return 0.0, []
# 137 
# 138         if score == 0.0 and not violations:
# 139             failures[name] = "failed or no output"
# 140             return (0.0, []) if not collect_violations else (0.0, violations)
# 141 
# 142         return (score, violations) if collect_violations else (score, [])
# 143 
# 144     def _run_tool_with_context(
# 145         self,
# 146         tool,
# 147         input: dict,
# 148         context: RunContext | None = None,
# 149         collect_violations: bool = False
# 150     ) -> Tuple[float, List[str]]:
# 151         ctx = self.fork_context()
# 152         result = tool.run(input=input, context=ctx)
# 153 
# 154         violations = getattr(result, "violations", []) or []
# 155         rc = getattr(result, "return_code", 0)
# 156 
# 157         if rc not in {0, 1} and not violations:
# 158             return 0.0, violations
# 159 
# 160         count = len(violations)
# 161         score = 1.0 - min(1.0, 0.1 * count)
# 162         return (score, violations) if collect_violations else (score, [])
# 163 
# 164     def _parse_weights(self, custom: Dict[str, float] | None) -> Dict[str, float]:
# 165         weights = {**self._DEFAULT_WEIGHTS}
# 166         if custom:
# 167             weights.update(
# 168                 {k.lower(): float(v) for k, v in custom.items() if k.lower() in weights}
# 169             )
# 170         total = sum(weights.values()) or 1.0
# 171         return {k: v / total for k, v in weights.items()}
# 172 
# 173     def _build_summary(
# 174         self,
# 175         *,
# 176         ruff_violations: List[str],
# 177         tool_failures: Dict[str, str],
# 178         meets_threshold: bool,
# 179         threshold: float | None,
# 180     ) -> str:
# 181         lines: List[str] = []
# 182 
# 183         if ruff_violations:
# 184             top_codes = {}
# 185             for code in ruff_violations:
# 186                 top_codes[code] = top_codes.get(code, 0) + 1
# 187             top_sorted = sorted(top_codes.items(), key=lambda kv: kv[1], reverse=True)
# 188             lines.append(
# 189                 "Top Ruff violations: "
# 190                 + ", ".join(f"{c}×{n}" for c, n in top_sorted[:3])
# 191             )
# 192         else:
# 193             lines.append("No Ruff violations detected")
# 194 
# 195         if tool_failures:
# 196             lines.append("⚠️  Tool failures: " + ", ".join(tool_failures))
# 197 
# 198         if threshold is not None:
# 199             state = "✅ meets" if meets_threshold else "❌ below"
# 200             lines.append(f"{state} threshold {threshold:.2f}")
# 201 
# 202         return "\n".join(lines)


# db\seeders\files\state_providers\__init__.py



# db\seeders\files\state_providers\basic_state_provider.py

# 1 from app.factories.base_provider_factory import BaseProviderFactory
# 2 from app.db.models import StateProviderConfig
# 3 from app.providers.state_provider_base import StateProviderBase
# 4 
# 5 class StateProviderFactory(BaseProviderFactory):
# 6     config_model = StateProviderConfig
# 7     base_class = StateProviderBase


# db\seeders\files\state_providers\code_stability_state_provider.py

# 1 from app.enums.fsm_enums import STATE_TYPE, DECISION_TYPE, TRANSITION_REASON_TYPE
# 2 from app.enums.agent_enums import AGENT
# 3 from app.providers.state_provider_base import StateProviderBase
# 4 from app.db.schemas import AgentOutputSchema
# 5 
# 6 class CodeStabilityStateProvider(StateProviderBase):
# 7     def _transition(self, state: dict, agent_output: AgentOutputSchema | None) -> dict:
# 8         current = AGENT(state.get("state"))
# 9 
# 10         #  If current state is START, transition to STABILITY state
# 11         if current == AGENT.START:
# 12             return {
# 13                 "state": AGENT.STABILITY,
# 14                 "reason": TRANSITION_REASON_TYPE.EVALUATING
# 15             }
# 16 
# 17         #  Get the decision from the agent's output, defaulting to UNKNOWN
# 18         decision = getattr(agent_output, "decision", DECISION_TYPE.UNKNOWN)
# 19 
# 20         #  Determine the next state and transition reason based on the decision
# 21         transition_reason = (
# 22             TRANSITION_REASON_TYPE.SUCCESSFUL if decision == DECISION_TYPE.ACCEPTED
# 23             else TRANSITION_REASON_TYPE.UNSUCCESSFUL
# 24         )
# 25 
# 26         #  Return the transition details
# 27         return {
# 28             "state": AGENT.END,
# 29             "state_type": STATE_TYPE.END,
# 30             "decision": decision,
# 31             "reason": transition_reason
# 32         }


# db\seeders\files\state_providers\linting_discriminator_state_provider.py

# 1 from app.enums.fsm_enums import DECISION_TYPE, TRANSITION_REASON_TYPE
# 2 from app.enums.agent_enums import AGENT
# 3 from app.providers.state_provider_base import StateProviderBase
# 4 from app.db.schemas import AgentOutputSchema
# 5 
# 6 class LintingDiscriminatorStateProvider(StateProviderBase):
# 7     def _transition(self, state: dict, agent_output: AgentOutputSchema | None) -> dict:
# 8         if agent_output is None:
# 9             return {
# 10                 "state": AGENT.DISCRIMINATOR,
# 11                 "reason": TRANSITION_REASON_TYPE.INITIALIZATION
# 12             }
# 13 
# 14         #  Handle the different decision types returned by the agent
# 15         if agent_output.decision == DECISION_TYPE.ACCEPTED:
# 16             return {
# 17                 "state": AGENT.END,
# 18                 "reason": TRANSITION_REASON_TYPE.SUCCESSFUL,
# 19                 "decision": DECISION_TYPE.ACCEPTED,
# 20                 "file_path": agent_output.file_path,
# 21             }
# 22 
# 23         elif agent_output.decision == DECISION_TYPE.IMPROVED:
# 24             return {
# 25                 "state": AGENT.END,
# 26                 "reason": TRANSITION_REASON_TYPE.SUCCESSFUL,  #  Could be improved, still valid
# 27                 "decision": DECISION_TYPE.IMPROVED,
# 28                 "file_path": agent_output.file_path,
# 29             }
# 30 
# 31         #  If the decision is REJECTED, handle accordingly
# 32         return {
# 33             "state": AGENT.END,
# 34             "reason": TRANSITION_REASON_TYPE.UNSUCCESSFUL,
# 35             "decision": DECISION_TYPE.REJECTED,
# 36             "file_path": agent_output.file_path,
# 37         }


# db\seeders\files\state_providers\linting_generator_state_provider.py

# 1 from app.enums.fsm_enums import STATE_TYPE, DECISION_TYPE, TRANSITION_REASON_TYPE
# 2 from app.enums.agent_enums import AGENT
# 3 from app.providers.state_provider_base import StateProviderBase
# 4 from app.db.schemas import AgentOutputSchema
# 5 
# 6 class LintingGeneratorStateProvider(StateProviderBase):
# 7     def _transition(self, state: dict, agent_output: AgentOutputSchema | None) -> dict:
# 8         current = AGENT(state.get("state"))
# 9 
# 10         if current == AGENT.START:
# 11             return {
# 12                 "state": AGENT.GENERATOR,
# 13                 "reason": TRANSITION_REASON_TYPE.INITIALIZATION
# 14             }
# 15 
# 16         if current == AGENT.GENERATOR:
# 17             return {
# 18                 "state": AGENT.END,
# 19                 "state_type": STATE_TYPE.END,
# 20                 "decision": DECISION_TYPE.UNKNOWN,
# 21                 "reason": TRANSITION_REASON_TYPE.SUCCESSFUL
# 22             }
# 23 
# 24         return {
# 25             "state": AGENT.END,
# 26             "state_type": STATE_TYPE.END,
# 27             "decision": DECISION_TYPE.UNKNOWN,
# 28             "reason": TRANSITION_REASON_TYPE.CUSTOM_RULE
# 29         }


# db\seeders\files\system_providers\__init__.py



# db\seeders\files\system_providers\linting_system_provider.py

# 1 from pathlib import Path
# 2 import json
# 3 
# 4 from app.enums.fsm_enums import TRANSITION_REASON_TYPE, DECISION_TYPE
# 5 from app.enums.state_enums import STATE
# 6 from app.providers.system_provider_base import SystemProviderBase
# 7 
# 8 
# 9 class LintingSystemProvider(SystemProviderBase):
# 10     def _transition(self, state: dict, state_output: dict | None) -> dict:
# 11         current = STATE(state.get("state"))
# 12         last = STATE(state.get("_last_state")) if state.get("_last_state") else None
# 13 
# 14         result = DECISION_TYPE.UNKNOWN
# 15         score = 0.0
# 16 
# 17         if state_output and hasattr(state_output, "output"):
# 18             output_obj = state_output.output
# 19             if isinstance(output_obj, str):
# 20                 try:
# 21                     output_obj = json.loads(output_obj)
# 22                 except Exception:
# 23                     output_obj = {}
# 24 
# 25             if isinstance(output_obj, dict):
# 26                 result = output_obj.get("decision") or output_obj.get("result", DECISION_TYPE.UNKNOWN)
# 27                 score = output_obj.get("score", 0.0)
# 28             else:
# 29                 result = getattr(output_obj, "decision", DECISION_TYPE.UNKNOWN)
# 30                 score = getattr(output_obj, "score", 0.0)
# 31 
# 32         transition = {}
# 33 
# 34         #  START → GENERATE
# 35         if current == STATE.START:
# 36             transition = {
# 37                 "state": STATE.GENERATE,
# 38                 "reason": TRANSITION_REASON_TYPE.INITIALIZATION,
# 39                 "file_path": state.get("file_path")
# 40             }
# 41 
# 42 
# 43         #  DISCRIMINATE (first pass) → CODE_STABILITY or GENERATE
# 44         elif current == STATE.DISCRIMINATE and last == STATE.START:
# 45             if score == 1.0:
# 46                 transition = {
# 47                     "state": STATE.CODE_STABILITY,
# 48                     "reason": TRANSITION_REASON_TYPE.EVALUATING,
# 49                     "file_path": state.get("file_path")
# 50                 }
# 51             else:
# 52                 transition = {
# 53                     "state": STATE.GENERATE,
# 54                     "reason": TRANSITION_REASON_TYPE.GENERATING,
# 55                     "file_path": state.get("file_path")
# 56                 }
# 57 
# 58         #  DISCRIMINATE (loop) → CODE_STABILITY or GENERATE
# 59         elif current == STATE.DISCRIMINATE:
# 60             if result == DECISION_TYPE.ACCEPTED:
# 61                 transition = {
# 62                     "state": STATE.CODE_STABILITY,
# 63                     "reason": TRANSITION_REASON_TYPE.EVALUATING,
# 64                     "file_path": state.get("file_path")
# 65                 }
# 66             elif result == DECISION_TYPE.IMPROVED:
# 67                 transition = {
# 68                     "state": STATE.GENERATE,
# 69                     "reason": TRANSITION_REASON_TYPE.GENERATING,
# 70                     "file_path": state.get("file_path")
# 71                 }
# 72             else:
# 73                 transition = {
# 74                     "state": STATE.GENERATE,
# 75                     "reason": TRANSITION_REASON_TYPE.UNSUCCESSFUL,
# 76                     "file_path": state.get("file_path")
# 77                 }
# 78 
# 79         #  GENERATE → DISCRIMINATE
# 80         elif current == STATE.GENERATE:
# 81             transition = {
# 82                 "state": STATE.DISCRIMINATE,
# 83                 "reason": TRANSITION_REASON_TYPE.EVALUATING,
# 84                 "file_path": state.get("file_path")
# 85             }
# 86 
# 87         #  CODE_STABILITY → END or fallback to GENERATE if rejected
# 88         elif current == STATE.CODE_STABILITY:
# 89             if result == DECISION_TYPE.ACCEPTED:
# 90                 transition = {
# 91                     "state": STATE.END,
# 92                     "reason": TRANSITION_REASON_TYPE.SUCCESSFUL,
# 93                     "file_path": state.get("file_path")
# 94                 }
# 95             else:
# 96                 transition = {
# 97                     "state": STATE.GENERATE,
# 98                     "reason": TRANSITION_REASON_TYPE.UNSUCCESSFUL,
# 99                     "file_path": state.get("file_path")
# 100                 }
# 101 
# 102         if not transition:
# 103             transition = {
# 104                 "state": STATE.END,
# 105                 "reason": TRANSITION_REASON_TYPE.CUSTOM_RULE,
# 106                 "decision": result if isinstance(result, DECISION_TYPE) else DECISION_TYPE.UNKNOWN
# 107             }
# 108 
# 109         return transition


# db\seeders\files\tool_providers\__init__.py



# db\seeders\files\tool_providers\black_tool.py

# 1 from __future__ import annotations
# 2 import subprocess
# 3 import sys
# 4 import re
# 5 from typing import Any, Dict, List, Optional
# 6 from app.providers.tool_provider_base import ToolProviderBase
# 7 from app.db.schemas import ToolOutputSchema
# 8 from app.enums.logging_enums import RunContext
# 9 
# 10 
# 11 class BlackToolProviderV2(ToolProviderBase):
# 12     """
# 13     Runs `black` either in --check (diff‑only) mode or full formatting mode and
# 14     maps Black’s native exit‑codes onto the 1 / 0 contract.
# 15     """
# 16 
# 17     def _run(self, input: dict, context: RunContext | None = None) -> ToolOutputSchema:
# 18         target: str = input.get("target")
# 19         check: bool = bool(input.get("check", False))
# 20 
# 21         #  Optionally track context lineage
# 22         if context:
# 23             context.parent_id = self._run_id
# 24             context.execution_chain = context.execution_chain[:] + [self._run_id]
# 25 
# 26         cmd = [
# 27             sys.executable,
# 28             "-m",
# 29             "black",
# 30             "--diff" if check else "--quiet",
# 31             "--check" if check else "",
# 32             target,
# 33         ]
# 34         cmd = [c for c in cmd if c]  #  strip empties
# 35 
# 36         try:
# 37             proc = subprocess.run(
# 38                 cmd, capture_output=True, text=True, encoding="utf‑8", errors="ignore"
# 39             )
# 40         except Exception as exc:
# 41             return self._runtime_error(str(exc))
# 42 
# 43         raw_code = proc.returncode
# 44         stdout = (proc.stdout or "").strip()
# 45         stderr = (proc.stderr or "").strip()
# 46 
# 47         if check:
# 48             if raw_code == 0:
# 49                 return self._success(
# 50                     summary="✅ Black check passed (no reformatting needed)",
# 51                     metrics={
# 52                         "files_checked": 1,
# 53                         "files_reformatted": 0,
# 54                         "lines_changed": 0,
# 55                         "raw_return_code": raw_code,
# 56                     },
# 57                 )
# 58 
# 59             if raw_code == 1:
# 60                 diff_hunks = len(re.findall(r"^@@", stdout, re.MULTILINE))
# 61                 return self._failure(
# 62                     stdout=stdout,
# 63                     summary=(
# 64                         f"❌ Black would reformat the file "
# 65                         f"({diff_hunks} changed hunk{'s' if diff_hunks != 1 else ''})"
# 66                     ),
# 67                     violations=[f"WOULD_REFORMAT:{target}"],
# 68                     metrics={
# 69                         "files_checked": 1,
# 70                         "files_reformatted": 1,
# 71                         "lines_changed": diff_hunks,
# 72                         "raw_return_code": raw_code,
# 73                     },
# 74                 )
# 75 
# 76             return self._runtime_error(stderr or stdout, raw_code)
# 77 
# 78         if raw_code == 0:
# 79             return self._success(
# 80                 summary="✅ Black formatted the file successfully",
# 81                 metrics={
# 82                     "files_reformatted": 1,
# 83                     "raw_return_code": raw_code,
# 84                 },
# 85             )
# 86 
# 87         return self._runtime_error(stderr or stdout, raw_code)
# 88 
# 89     #  ────────────────────────────────────────────────────────────── helpers
# 90     def _success(
# 91         self,
# 92         summary: str,
# 93         stdout: Optional[str] = None,
# 94         metrics: Optional[Dict[str, Any]] = None,
# 95     ) -> ToolOutputSchema:
# 96         return ToolOutputSchema(
# 97             return_code=1,
# 98             stdout=stdout,
# 99             stderr=None,
# 100             violations=None,
# 101             metrics=metrics,
# 102             summary=summary,
# 103         )
# 104 
# 105     def _failure(
# 106         self,
# 107         stdout: Optional[str],
# 108         summary: str,
# 109         violations: List[str],
# 110         metrics: Dict[str, Any],
# 111     ) -> ToolOutputSchema:
# 112         return ToolOutputSchema(
# 113             return_code=0,
# 114             stdout=stdout,
# 115             stderr=None,
# 116             violations=violations,
# 117             metrics=metrics,
# 118             summary=summary,
# 119         )
# 120 
# 121     def _runtime_error(
# 122         self,
# 123         message: str,
# 124         raw_code: int | None = None,
# 125     ) -> ToolOutputSchema:
# 126         return ToolOutputSchema(
# 127             return_code=0,
# 128             stdout=None,
# 129             stderr=message,
# 130             violations=["RUNTIME_ERROR"],
# 131             metrics={"raw_return_code": raw_code, "exception": 1},
# 132             summary=f"❌ Black execution failed: {message}",
# 133         )


# db\seeders\files\tool_providers\docformatter_tool.py

# 1 import subprocess
# 2 import sys
# 3 from copy import deepcopy
# 4 
# 5 from app.providers.tool_provider_base import ToolProviderBase
# 6 from app.db.schemas import ToolOutputSchema
# 7 from app.enums.logging_enums import RunContext
# 8 
# 9 
# 10 class DocFormatterToolProvider(ToolProviderBase):
# 11     def _run(self, input: dict, context: RunContext | None = None) -> ToolOutputSchema:
# 12         target = input.get("target")
# 13         check = input.get("check", False)
# 14 
# 15         #  ✅ Propagate context
# 16         if context:
# 17             context = deepcopy(context)
# 18             context.parent_id = self._run_id
# 19             context.execution_chain = context.execution_chain[:] + [self._run_id]
# 20 
# 21         cmd = [sys.executable, "-m", "docformatter", target, "--in-place"]
# 22         if check:
# 23             cmd.append("--check")
# 24 
# 25         proc = subprocess.run(cmd, capture_output=True, text=True)
# 26         stdout = proc.stdout.strip()
# 27         stderr = proc.stderr.strip()
# 28         raw_code = proc.returncode
# 29 
# 30         if check:
# 31             if raw_code == 0:
# 32                 norm_code = 1
# 33                 summary = "✅ Docstring format check passed"
# 34             elif raw_code == 1:
# 35                 norm_code = 0
# 36                 summary = "❌ Docstring format check failed"
# 37             else:
# 38                 return self._runtime_error(f"Docformatter check error ({raw_code}): {stderr or stdout}")
# 39         else:
# 40             if raw_code == 0:
# 41                 norm_code = 1
# 42                 summary = "✅ Docstrings formatted successfully"
# 43             else:
# 44                 return self._runtime_error(f"Docformatter formatting error ({raw_code}): {stderr or stdout}")
# 45 
# 46         return ToolOutputSchema(
# 47             return_code=norm_code,
# 48             stdout=stdout,
# 49             stderr=stderr,
# 50             summary=summary
# 51         )
# 52 
# 53     def _runtime_error(self, message: str) -> ToolOutputSchema:
# 54         return ToolOutputSchema(
# 55             return_code=0,
# 56             stdout=None,
# 57             stderr=message,
# 58             violations=["RUNTIME_ERROR"],
# 59             metrics={"exception": 1},
# 60             summary=f"❌ docformatter execution failed: {message}",
# 61         )


# db\seeders\files\tool_providers\mypy_tool.py

# 1 import subprocess
# 2 import sys
# 3 from typing import Any, Dict, List, Optional
# 4 from copy import deepcopy
# 5 
# 6 from app.enums.logging_enums import RunContext
# 7 from app.providers.tool_provider_base import ToolProviderBase
# 8 from app.db.schemas import ToolOutputSchema
# 9 
# 10 
# 11 class MypyToolProviderV2(ToolProviderBase):
# 12     """
# 13     Runs `mypy` in *strict* mode and normalises exit‑codes:
# 14     0 ⇒ pass ⇒ return_code 1
# 15     1 ⇒ type errors ⇒ return_code 0
# 16     >1 ⇒ internal / runtime error ⇒ return_code 0 + RUNTIME_ERROR violation
# 17     """
# 18 
# 19     STRICT_ARGS: list[str] = [
# 20         "--strict",
# 21         "--disallow-untyped-defs",
# 22         "--disallow-incomplete-defs",
# 23         "--disallow-untyped-calls",
# 24         "--disallow-untyped-decorators",
# 25         "--disallow-any-generics",
# 26         "--warn-unused-ignores",
# 27         "--warn-return-any",
# 28         "--no-implicit-optional",
# 29         "--strict-equality",
# 30     ]
# 31 
# 32     def _run(self, input: dict, context: RunContext | None = None) -> ToolOutputSchema:
# 33         target: str = input.get("target")
# 34 
# 35         #  ✅ Propagate RunContext
# 36         if context:
# 37             context = deepcopy(context)
# 38             context.parent_id = self._run_id
# 39             context.execution_chain = context.execution_chain[:] + [self._run_id]
# 40 
# 41         cmd = [sys.executable, "-m", "mypy", target, *self.STRICT_ARGS]
# 42 
# 43         try:
# 44             proc = subprocess.run(
# 45                 cmd, capture_output=True, text=True, encoding="utf-8", errors="ignore"
# 46             )
# 47         except Exception as exc:
# 48             return self._runtime_error(str(exc))
# 49 
# 50         raw_code = proc.returncode
# 51         stdout = (proc.stdout or "").strip()
# 52         stderr = (proc.stderr or "").strip()
# 53 
# 54         if raw_code == 0:
# 55             return self._success(
# 56                 summary="✅ Mypy passed — no type errors",
# 57                 metrics={"error_count": 0, "raw_return_code": raw_code},
# 58                 stdout=stdout or None,
# 59             )
# 60 
# 61         if raw_code == 1:
# 62             violations = [line for line in stdout.splitlines() if line.strip()]
# 63             return self._failure(
# 64                 summary=f"❌ Mypy failed with {len(violations)} error(s)",
# 65                 violations=violations,
# 66                 metrics={
# 67                     "error_count": len(violations),
# 68                     "raw_return_code": raw_code,
# 69                 },
# 70                 stdout=stdout or None,
# 71             )
# 72 
# 73         return self._runtime_error(stderr or stdout, raw_code)
# 74 
# 75     #  ────────────────────────────────────────────────────────────── helpers
# 76     def _success(
# 77         self,
# 78         summary: str,
# 79         metrics: Dict[str, Any],
# 80         stdout: Optional[str] = None,
# 81     ) -> ToolOutputSchema:
# 82         return ToolOutputSchema(
# 83             return_code=1,
# 84             stdout=stdout,
# 85             stderr=None,
# 86             violations=None,
# 87             metrics=metrics,
# 88             summary=summary,
# 89         )
# 90 
# 91     def _failure(
# 92         self,
# 93         summary: str,
# 94         violations: List[str],
# 95         metrics: Dict[str, Any],
# 96         stdout: Optional[str] = None,
# 97     ) -> ToolOutputSchema:
# 98         return ToolOutputSchema(
# 99             return_code=0,
# 100             stdout=stdout,
# 101             stderr=None,
# 102             violations=violations,
# 103             metrics=metrics,
# 104             summary=summary,
# 105         )
# 106 
# 107     def _runtime_error(self, message: str, raw_code: int | None = None) -> ToolOutputSchema:
# 108         return ToolOutputSchema(
# 109             return_code=0,
# 110             stdout=None,
# 111             stderr=message,
# 112             violations=["RUNTIME_ERROR"],
# 113             metrics={"raw_return_code": raw_code, "exception": 1},
# 114             summary=f"❌ Mypy execution failed: {message}",
# 115         )


# db\seeders\files\tool_providers\radon_tool.py

# 1 import subprocess
# 2 import sys
# 3 from pathlib import Path
# 4 from copy import deepcopy
# 5 
# 6 from app.enums.logging_enums import RunContext
# 7 from app.providers.tool_provider_base import ToolProviderBase
# 8 from app.db.schemas import ToolOutputSchema
# 9 
# 10 
# 11 class RadonToolProvider(ToolProviderBase):
# 12     def _run(self, input: dict, context: RunContext | None = None) -> ToolOutputSchema:
# 13         target = input.get("target")
# 14         if not Path(target).exists():
# 15             raise FileNotFoundError(f"{target} not found")
# 16 
# 17         #  ✅ Clone context and inject ancestry
# 18         if context:
# 19             context = deepcopy(context)
# 20             context.parent_id = self._run_id
# 21             context.execution_chain = context.execution_chain[:] + [self._run_id]
# 22 
# 23         cmd = [sys.executable, "-m", "radon", "cc", "-s", target]
# 24         proc = subprocess.run(cmd, capture_output=True, text=True)
# 25 
# 26         stdout = proc.stdout.strip()
# 27         stderr = proc.stderr.strip()
# 28         raw_code = proc.returncode
# 29 
# 30         if raw_code != 0:
# 31             raise RuntimeError(f"radon failed: {stderr or stdout or 'unknown error'}")
# 32 
# 33         #  Determine worst grade
# 34         grades = []
# 35         for line in stdout.splitlines():
# 36             if line.strip() and "(" in line and ")" in line:
# 37                 try:
# 38                     grade = line.strip().split("(", 1)[1].split(")")[0]
# 39                     grades.append(grade)
# 40                 except Exception:
# 41                     continue
# 42 
# 43         worst_grade = max(grades, default="A")
# 44         passing = worst_grade in ("A", "B")
# 45 
# 46         norm_code = 1 if passing else 0
# 47         summary = (
# 48             f"Radon complexity OK (worst: {worst_grade})"
# 49             if passing else
# 50             f"Radon complexity too high (worst: {worst_grade})"
# 51         )
# 52 
# 53         return ToolOutputSchema(
# 54             return_code=norm_code,
# 55             stdout=stdout,
# 56             stderr=stderr,
# 57             summary=summary,
# 58         )


# db\seeders\files\tool_providers\ruff_tool.py

# 1 import subprocess
# 2 import json
# 3 import uuid
# 4 from copy import deepcopy
# 5 from typing import Any, Dict, List, Optional
# 6 
# 7 from app.enums.logging_enums import RunContext
# 8 from app.providers.tool_provider_base import ToolProviderBase
# 9 from app.db.schemas import ToolOutputSchema
# 10 
# 11 
# 12 class RuffToolProviderV2(ToolProviderBase):
# 13     """
# 14     Executes `ruff check --output-format json` and normalises results:
# 15     return_code 1 ⇒ pass, 0 ⇒ violation or runtime error.
# 16     """
# 17 
# 18     def _run(self, input: dict, context: RunContext | None = None) -> ToolOutputSchema:
# 19         target: str = input.get("target")
# 20         cmd = ["ruff", "check", "--output-format", "json", target]
# 21 
# 22         #  ✅ Isolate context to avoid corrupting upstream execution_chain
# 23         if context:
# 24             context = deepcopy(context)
# 25             context.parent_id = self._run_id
# 26             context.execution_chain = context.execution_chain[:] + [str(uuid.uuid4())]
# 27 
# 28         try:
# 29             proc = subprocess.run(
# 30                 cmd, capture_output=True, text=True, encoding="utf-8", errors="ignore"
# 31             )
# 32         except Exception as exc:
# 33             return self._runtime_error(str(exc))
# 34 
# 35         raw_code = proc.returncode
# 36         stdout = (proc.stdout or "").strip()
# 37         stderr = (proc.stderr or "").strip()
# 38 
# 39         #  ───────────────────────────────────── parse Ruff JSON output
# 40         try:
# 41             parsed = json.loads(stdout or "[]")
# 42             violations: List[str] = [
# 43                 v["code"]
# 44                 for file_item in parsed
# 45                 for v in file_item.get("violations", [])
# 46             ]
# 47         except json.JSONDecodeError:
# 48             return self._runtime_error("Failed to parse Ruff JSON output", raw_code)
# 49 
# 50         violation_count = len(violations)
# 51 
# 52         #  ───────────────────────────────────── outcome map
# 53         if raw_code == 0:
# 54             return self._success(
# 55                 summary="✅ Ruff passed — no violations",
# 56                 metrics={"violation_count": 0, "raw_return_code": raw_code},
# 57             )
# 58 
# 59         if raw_code == 1:
# 60             return self._failure(
# 61                 summary=f"❌ Ruff failed with {violation_count} violation(s)",
# 62                 violations=violations or ["UNKNOWN_VIOLATION"],
# 63                 metrics={
# 64                     "violation_count": violation_count,
# 65                     "raw_return_code": raw_code,
# 66                 },
# 67                 stdout=stdout or None,
# 68             )
# 69 
# 70         return self._runtime_error(stderr or stdout, raw_code)
# 71 
# 72     #  ───────────────────────────────────── helpers
# 73     def _success(self, summary: str, metrics: Dict[str, Any]) -> ToolOutputSchema:
# 74         return ToolOutputSchema(
# 75             return_code=1,
# 76             stdout=None,
# 77             stderr=None,
# 78             violations=None,
# 79             metrics=metrics,
# 80             summary=summary,
# 81         )
# 82 
# 83     def _failure(
# 84         self,
# 85         summary: str,
# 86         violations: List[str],
# 87         metrics: Dict[str, Any],
# 88         stdout: Optional[str] = None,
# 89     ) -> ToolOutputSchema:
# 90         return ToolOutputSchema(
# 91             return_code=0,
# 92             stdout=stdout,
# 93             stderr=None,
# 94             violations=violations,
# 95             metrics=metrics,
# 96             summary=summary,
# 97         )
# 98 
# 99     def _runtime_error(self, message: str, raw_code: int | None = None) -> ToolOutputSchema:
# 100         return ToolOutputSchema(
# 101             return_code=0,
# 102             stdout=None,
# 103             stderr=message,
# 104             violations=["RUNTIME_ERROR"],
# 105             metrics={"raw_return_code": raw_code, "exception": 1},
# 106             summary=f"❌ Ruff execution failed: {message}",
# 107         )


# db\seeders\files\tool_providers\sonarcloud_tool.py

# 1 import os
# 2 import subprocess
# 3 import time
# 4 import uuid
# 5 import json
# 6 import tempfile
# 7 from pathlib import Path
# 8 from typing import Dict
# 9 from copy import deepcopy
# 10 
# 11 from app.enums.logging_enums import RunContext
# 12 from app.providers.tool_provider_base import ToolProviderBase
# 13 from app.db.schemas import ToolOutputSchema
# 14 
# 15 
# 16 class SonarCloudToolProvider(ToolProviderBase):
# 17     def _run(self, input: dict, context: RunContext | None = None) -> ToolOutputSchema:
# 18         #  ✅ Clone context to maintain execution chain isolation
# 19         if context:
# 20             context = deepcopy(context)
# 21             context.parent_id = self._run_id
# 22             context.execution_chain = context.execution_chain[:] + [str(uuid.uuid4())]
# 23 
# 24         target_path = Path(input.get("target"))
# 25         if not target_path.exists():
# 26             raise FileNotFoundError(f"{target_path} does not exist")
# 27 
# 28         github_token, sonar_token, sonar_project, sonar_org, github_user = self._load_env()
# 29 
# 30         metrics = {}
# 31         stdout_msg = ""
# 32 
# 33         with tempfile.TemporaryDirectory() as tmpdir:
# 34             repo_url = f"https://github.com/{github_user}/codecritic_scoring"
# 35             subprocess.run(["gh", "repo", "clone", repo_url, tmpdir], check=True)
# 36 
# 37             filename = f"test_{uuid.uuid4()}.py"
# 38             src_dir = Path(tmpdir) / "src"
# 39             src_dir.mkdir(parents=True, exist_ok=True)
# 40             target_dest = src_dir / filename
# 41             target_dest.write_text(target_path.read_text(encoding="utf-8"))
# 42 
# 43             self._git_push(filename, cwd=src_dir)
# 44             self._wait_for_scan_completion(sonar_token, sonar_project)
# 45             metrics = self._poll_sonarcloud_metrics(sonar_token, sonar_project)
# 46             stdout_msg = f"Scan complete for {filename}"
# 47 
# 48             if metrics:
# 49                 self._git_cleanup(filename, cwd=src_dir)
# 50 
# 51         violations_present = any(
# 52             metrics.get(key, 0) > 0 for key in ("bugs", "vulnerabilities", "code_smells")
# 53         )
# 54 
# 55         return_code = 0 if violations_present else 1
# 56         summary = (
# 57             "SonarCloud scan clean" if return_code == 1
# 58             else "SonarCloud found issues"
# 59         )
# 60 
# 61         return ToolOutputSchema(
# 62             return_code=return_code,
# 63             stdout=stdout_msg,
# 64             stderr=None,
# 65             violations=None,
# 66             metrics=metrics,
# 67             summary=summary,
# 68         )
# 69 
# 70     def _load_env(self) -> tuple[str, str, str, str, str]:
# 71         github_token = os.getenv("GITHUB_TOKEN")
# 72         sonar_token = os.getenv("SONAR_TOKEN")
# 73         sonar_project = os.getenv("SONAR_PROJECT_KEY")
# 74         sonar_org = os.getenv("SONAR_ORGANIZATION_KEY")
# 75         github_user = os.getenv("GITHUB_USERNAME")
# 76         if not all([github_token, sonar_token, sonar_project, sonar_org, github_user]):
# 77             raise EnvironmentError("Missing one or more required environment variables")
# 78         return github_token, sonar_token, sonar_project, sonar_org, github_user
# 79 
# 80     def _git_push(self, filename: str, cwd: Path) -> None:
# 81         subprocess.run(["git", "checkout", "main"], cwd=cwd.parent, check=True)
# 82         subprocess.run(["git", "pull"], cwd=cwd.parent, check=True)
# 83         subprocess.run(["git", "add", f"src/{filename}"], cwd=cwd.parent, check=True)
# 84         subprocess.run(["git", "commit", "-m", f"Add test file src/{filename}"], cwd=cwd.parent, check=True)
# 85         subprocess.run(["git", "push"], cwd=cwd.parent, check=True)
# 86 
# 87     def _git_cleanup(self, filename: str, cwd: Path) -> None:
# 88         subprocess.run(["git", "rm", f"src/{filename}"], cwd=cwd.parent, check=True)
# 89         subprocess.run(["git", "commit", "-m", f"Remove test file src/{filename}"], cwd=cwd.parent, check=True)
# 90         subprocess.run(["git", "push"], cwd=cwd.parent, check=True)
# 91 
# 92     def _wait_for_scan_completion(self, sonar_token: str, sonar_project: str) -> None:
# 93         url = f"https://sonarcloud.io/api/ce/component?component={sonar_project}"
# 94         for _ in range(20):
# 95             time.sleep(10)
# 96             result = subprocess.run(
# 97                 ["curl", "-s", "-u", f"{sonar_token}:", url],
# 98                 capture_output=True,
# 99                 text=True
# 100             )
# 101             try:
# 102                 parsed = json.loads(result.stdout)
# 103                 status = parsed.get("current", {}).get("status")
# 104                 if status == "SUCCESS":
# 105                     return
# 106             except Exception as e:
# 107                 print(f"⚠️ Error reading scan status: {e}")
# 108 
# 109     def _poll_sonarcloud_metrics(self, sonar_token: str, sonar_project: str) -> Dict[str, int]:
# 110         metric_keys = ",".join([
# 111             "bugs", "vulnerabilities", "security_hotspots", "code_smells",
# 112             "coverage", "line_coverage", "lines_to_cover", "tests",
# 113             "test_errors", "test_failures", "skipped_tests",
# 114             "lines", "ncloc", "functions", "classes", "statements",
# 115             "complexity", "cognitive_complexity",
# 116             "duplicated_lines", "duplicated_blocks", "duplicated_files", "duplicated_lines_density"
# 117         ])
# 118         api_url = (
# 119             f"https://sonarcloud.io/api/measures/component"
# 120             f"?component={sonar_project}&metricKeys={metric_keys}"
# 121         )
# 122 
# 123         for _ in range(10):
# 124             time.sleep(5)
# 125             result = subprocess.run(
# 126                 ["curl", "-s", "-u", f"{sonar_token}:", api_url],
# 127                 capture_output=True,
# 128                 text=True
# 129             )
# 130             try:
# 131                 parsed = json.loads(result.stdout)
# 132                 measures = parsed.get("component", {}).get("measures", [])
# 133                 return {m["metric"]: float(m["value"]) for m in measures}
# 134             except Exception as e:
# 135                 print(f"⚠️ Error parsing metrics: {e}")
# 136         return {}


# db\seeders\files\tool_providers\symbol_graph.py

# 1 from __future__ import annotations
# 2 
# 3 import ast
# 4 import json
# 5 from pathlib import Path
# 6 from typing import Any, Dict
# 7 from copy import deepcopy
# 8 
# 9 from app.enums.logging_enums import RunContext
# 10 from app.providers.tool_provider_base import ToolProviderBase
# 11 from app.db.schemas import ToolOutputSchema
# 12 
# 13 
# 14 class SymbolGraph:
# 15     def __init__(self) -> None:
# 16         self.graph: Dict[str, Any] = {}
# 17 
# 18     def parse_file(self, filepath: str | Path) -> None:
# 19         from ast import parse
# 20 
# 21         filepath = Path(filepath)
# 22         source = filepath.read_text(encoding="utf-8")
# 23         tree = parse(source, filename=str(filepath))
# 24 
# 25         visitor = _SymbolGraphVisitor(filepath.stem, str(filepath), self.graph)
# 26         visitor.visit(tree)
# 27 
# 28 
# 29 class SymbolGraphToolProvider(ToolProviderBase):
# 30     def _run(self, input: dict, context: RunContext | None = None) -> ToolOutputSchema:
# 31         if context:
# 32             context = deepcopy(context)
# 33             context.parent_id = self._run_id
# 34             context.execution_chain = context.execution_chain[:] + [str(self._run_id)]
# 35 
# 36         target = input.get("target")
# 37         path = Path(target)
# 38         if not path.exists():
# 39             raise FileNotFoundError(f"{target} does not exist")
# 40 
# 41         symbol_graph_util = SymbolGraph()
# 42         symbol_graph_util.parse_file(target)
# 43 
# 44         result_json = json.dumps(symbol_graph_util.graph, indent=2)
# 45 
# 46         return ToolOutputSchema(
# 47             return_code=1,
# 48             stdout=result_json,
# 49             stderr=None,
# 50             violations=None,
# 51             metrics=symbol_graph_util.graph,
# 52             summary="Symbol graph extraction successful"
# 53         )
# 54 
# 55 
# 56 class _SymbolGraphVisitor(ast.NodeVisitor):
# 57     def __init__(self, module: str, file_path: str, graph: Dict[str, Any]) -> None:
# 58         self.module = module
# 59         self.file_path = file_path
# 60         self.graph = graph
# 61         self.scope: list[str] = []
# 62         self.current: str | None = None
# 63 
# 64     def visit_FunctionDef(self, node: ast.FunctionDef) -> None:
# 65         self._process_function_or_async_function(node)
# 66         self.generic_visit(node)
# 67         self.scope.pop()
# 68         self.current = None
# 69 
# 70     def visit_AsyncFunctionDef(self, node: ast.AsyncFunctionDef) -> None:
# 71         self._process_function_or_async_function(node)
# 72         self.generic_visit(node)
# 73         self.scope.pop()
# 74         self.current = None
# 75 
# 76     def _process_function_or_async_function(self, node: ast.FunctionDef | ast.AsyncFunctionDef) -> None:
# 77         qual = self._qualify(node.name)
# 78         self.graph[qual] = {
# 79             "name": node.name,
# 80             "type": "async_function" if isinstance(node, ast.AsyncFunctionDef) else "function",
# 81             "file": self.file_path,
# 82             "lineno": node.lineno,
# 83             "col_offset": node.col_offset,
# 84             "end_lineno": getattr(node, "end_lineno", node.lineno),
# 85             "end_col_offset": getattr(node, "end_col_offset", node.col_offset),
# 86             "scope": ".".join([self.module, *self.scope]) if self.scope else self.module,
# 87             "calls": [],
# 88         }
# 89         self.scope.append(node.name)
# 90         self.current = qual
# 91 
# 92     def visit_ClassDef(self, node: ast.ClassDef) -> None:
# 93         qual = self._qualify(node.name)
# 94         self._record(qual, "class", node)
# 95         self.scope.append(node.name)
# 96         self.current = qual
# 97         self.generic_visit(node)
# 98         self.scope.pop()
# 99         self.current = None
# 100 
# 101     def _qualify(self, name: str) -> str:
# 102         return ".".join([self.module, *self.scope, name])
# 103 
# 104     def _record(self, qual: str, typ: str, node: ast.AST) -> Dict[str, Any]:
# 105         info = {
# 106             "type": typ,
# 107             "file": self.file_path,
# 108             "lineno": getattr(node, "lineno", None),
# 109             "col_offset": getattr(node, "col_offset", None),
# 110             "end_lineno": getattr(node, "end_lineno", getattr(node, "lineno", None)),
# 111             "end_col_offset": getattr(node, "end_col_offset", getattr(node, "col_offset", None)),
# 112             "scope": ".".join([self.module, *self.scope]) if self.scope else self.module,
# 113             "calls": [],
# 114         }
# 115         self.graph[qual] = info
# 116         return info


# db\seeders\seed_agent_engine_providers.py

# 1 from uuid import uuid4
# 2 from pathlib import Path
# 3 import shutil
# 4 from sqlalchemy.orm import Session
# 5 from app.db.models import AgentEngineProviderConfig
# 6 from app.enums.logging_enums import PROVIDER_TYPE
# 7 
# 8 SEED_FILES_DIR = Path(__file__).resolve().parent / "files/agent_engine_providers"
# 9 PROJECT_ROOT = SEED_FILES_DIR.parent.parent.parent.parent.parent
# 10 EXTENSIONS_DIR = PROJECT_ROOT / "extensions"
# 11 
# 12 AGENT_ENGINES = [
# 13     {
# 14         "id": 1,
# 15         "filename": "basic_agent_engine_provider.py",
# 16         "name": "basic_agent_engine_provider",
# 17         "description": "Returns a mock LLM response.",
# 18         "model": "mock-llm",
# 19         "tags": ["default"],
# 20         "cost_per_1k_tokens": 0.0
# 21     },
# 22     {
# 23         "id": 2,
# 24         "filename": "openai_gpt4o_agent_engine_provider.py",
# 25         "name": "openai_gpt_4o_agent_engine",
# 26         "description": "Runs GPT-4o via OpenAI API.",
# 27         "model": "gpt-4o",
# 28         "tags": ["openai", "production"],
# 29         "cost_per_1k_tokens": 0.005
# 30     },
# 31     {
# 32         "id": 3,
# 33         "filename": "gemini_1_5_pro_agent_engine_provider.py",
# 34         "name": "gemini_1_5_pro_agent_engine",
# 35         "description": "Runs Gemini 1.5 Pro via Google AI API.",
# 36         "model": "gemini-1.5-pro",
# 37         "tags": ["google", "production"],
# 38         "cost_per_1k_tokens": 0.007
# 39     },
# 40     {
# 41         "id": 4,
# 42         "filename": "claude_3_sonnet_agent_engine_provider.py",
# 43         "name": "claude_3_sonnet_agent_engine",
# 44         "description": "Runs Claude 3 Sonnet via AWS Bedrock.",
# 45         "model": "claude-3-sonnet",
# 46         "tags": ["aws", "bedrock", "production"],
# 47         "cost_per_1k_tokens": 0.003
# 48     },
# 49 ]
# 50 
# 51 def seed_agent_engine_providers(db_session: Session):
# 52     EXTENSIONS_DIR.mkdir(parents=True, exist_ok=True)
# 53 
# 54     for entry in AGENT_ENGINES:
# 55         guid = str(uuid4())
# 56         source_file = SEED_FILES_DIR / entry["filename"]
# 57         dest_file = EXTENSIONS_DIR / f"{guid}.py"
# 58 
# 59         if not source_file.exists():
# 60             raise FileNotFoundError(f"Agent engine script not found: {source_file}")
# 61 
# 62         shutil.copy(source_file, dest_file)
# 63 
# 64         config = AgentEngineProviderConfig(
# 65             id=entry["id"],
# 66             guid=guid,
# 67             name=entry["name"],
# 68             description=entry["description"],
# 69             model=entry["model"],
# 70             config={},
# 71             cost_per_1k_tokens=entry["cost_per_1k_tokens"],
# 72             artifact_path=str(dest_file),
# 73             tags=entry["tags"],
# 74             provider_type=PROVIDER_TYPE.AGENT_ENGINE,
# 75         )
# 76 
# 77         db_session.add(config)
# 78 
# 79     db_session.commit()
# 80     print("✅ Seeded agent engine configurations successfully.")


# db\seeders\seed_agent_providers.py

# 1 from uuid import uuid4
# 2 from pathlib import Path
# 3 import shutil
# 4 from sqlalchemy.orm import Session
# 5 from app.db.models import AgentProviderConfig
# 6 from app.enums.logging_enums import PROVIDER_TYPE
# 7 
# 8 SEED_FILES_DIR = Path(__file__).resolve().parent / "files/agent_providers"
# 9 PROJECT_ROOT = SEED_FILES_DIR.parent.parent.parent.parent.parent
# 10 EXTENSIONS_DIR = PROJECT_ROOT / "extensions"
# 11 
# 12 AGENT_PROVIDERS = [
# 13     {
# 14         "id": 1,
# 15         "filename": "basic_agent_provider.py",
# 16         "name": "basic_agent_provider",
# 17         "description": "Returns a hardcoded result.",
# 18         "agent_type": "basic",
# 19         "tags": ["test"],
# 20         "config": {
# 21             "components": {},
# 22             "params": {}
# 23         }
# 24     },
# 25     {
# 26         "id": 2,
# 27         "filename": "linting_generator_agent_provider.py",
# 28         "name": "linting_generator_agent_provider",
# 29         "description": "Runs GPT-4o to generate linting corrections.",
# 30         "agent_type": "generator",
# 31         "tags": ["linting", "generator"],
# 32         "config": {
# 33             "components": {
# 34                 "agent_engine_provider_id": 2,
# 35                 "prompt_provider_id": 2,
# 36                 "context_provider_id": 2,
# 37                 "score_provider_id": 1,
# 38                 "tool_provider_ids": {
# 39                     "black": 1,
# 40                     "ruff": 3,
# 41                     "mypy": 5,
# 42                     "radon": 4
# 43                 }
# 44             },
# 45             "params": {}
# 46         }
# 47     },
# 48     {
# 49         "id": 3,
# 50         "filename": "linting_discriminator_agent_provider.py",
# 51         "name": "linting_discriminator_agent_provider",
# 52         "description": "Evaluates generator output for acceptance.",
# 53         "agent_type": "discriminator",
# 54         "tags": ["linting", "discriminator"],
# 55         "config": {
# 56             "components": {
# 57                 "score_provider_id": 1
# 58             },
# 59             "params": {}
# 60         }
# 61     },
# 62     {
# 63         "id": 4,
# 64         "filename": "code_stability_agent_provider.py",
# 65         "name": "code_stability_agent_provider",
# 66         "description": "Rejects output if code is not parseable, importable, compilable, or type-valid.",
# 67         "agent_type": "stability",
# 68         "tags": ["stability"],
# 69         "config": {
# 70             "components": {
# 71                 "score_provider_id": 2
# 72             },
# 73             "params": {}
# 74         }
# 75     }
# 76 ]
# 77 
# 78 def seed_agent_providers(db_session: Session):
# 79     EXTENSIONS_DIR.mkdir(parents=True, exist_ok=True)
# 80 
# 81     for entry in AGENT_PROVIDERS:
# 82         guid = str(uuid4())
# 83         dest_filename = f"{guid}.py"
# 84         source_file = SEED_FILES_DIR / entry["filename"]
# 85         dest_file = EXTENSIONS_DIR / dest_filename
# 86 
# 87         if not source_file.exists():
# 88             raise FileNotFoundError(f"Agent provider script not found: {source_file}")
# 89 
# 90         shutil.copy(source_file, dest_file)
# 91 
# 92         config = AgentProviderConfig(
# 93             id=entry["id"],
# 94             guid=guid,
# 95             name=entry["name"],
# 96             description=entry["description"],
# 97             artifact_path=dest_filename,
# 98             tags=entry["tags"],
# 99             config=entry["config"],
# 100             agent_type=entry["agent_type"],
# 101             provider_type=PROVIDER_TYPE.AGENT,
# 102         )
# 103 
# 104         db_session.add(config)
# 105 
# 106     db_session.commit()
# 107     print("Seeded agent provider configurations successfully.")


# db\seeders\seed_context_providers.py

# 1 from uuid import uuid4
# 2 from pathlib import Path
# 3 import shutil
# 4 from sqlalchemy.orm import Session
# 5 from app.db.models import ContextProviderConfig
# 6 from app.enums.logging_enums import PROVIDER_TYPE
# 7 
# 8 SEED_FILES_DIR = Path(__file__).resolve().parent / "files/context_providers"
# 9 PROJECT_ROOT = SEED_FILES_DIR.parent.parent.parent.parent.parent
# 10 EXTENSIONS_DIR = PROJECT_ROOT / "extensions"
# 11 
# 12 CONTEXT_PROVIDERS = [
# 13     {
# 14         "id": 1,
# 15         "filename": "basic_context_provider.py",
# 16         "name": "basic_context_provider",
# 17         "description": "Returns static context for testing.",
# 18         "tags": ["default"],
# 19         "config": {
# 20             "components": {},
# 21             "params": {}
# 22         }
# 23     },
# 24     {
# 25         "id": 2,
# 26         "filename": "linting_context_provider.py",
# 27         "name": "linting_context_provider",
# 28         "description": "Generates context for the linting system.",
# 29         "tags": ["linting", "score-aware"],
# 30         "config": {
# 31             "components": {
# 32                 "score_provider_id": 1,
# 33                 "tool_provider_ids": None
# 34             },
# 35             "params": {}
# 36         }
# 37     }
# 38 ]
# 39 
# 40 def seed_context_providers(db_session: Session):
# 41     EXTENSIONS_DIR.mkdir(parents=True, exist_ok=True)
# 42 
# 43     for entry in CONTEXT_PROVIDERS:
# 44         guid = str(uuid4())
# 45         dest_filename = f"{guid}.py"
# 46         source_file = SEED_FILES_DIR / entry["filename"]
# 47         dest_file = EXTENSIONS_DIR / dest_filename
# 48 
# 49         if not source_file.exists():
# 50             raise FileNotFoundError(f"Context provider script not found: {source_file}")
# 51 
# 52         shutil.copy(source_file, dest_file)
# 53 
# 54         config = ContextProviderConfig(
# 55             id=entry["id"],
# 56             guid=guid,
# 57             name=entry["name"],
# 58             description=entry["description"],
# 59             artifact_path=dest_filename,
# 60             tags=entry["tags"],
# 61             config=entry["config"],
# 62             provider_type=PROVIDER_TYPE.CONTEXT,
# 63         )
# 64 
# 65         db_session.add(config)
# 66 
# 67     db_session.commit()
# 68     print("Seeded context provider configurations successfully.")


# db\seeders\seed_controller_providers.py

# 1 from uuid import uuid4
# 2 from pathlib import Path
# 3 import shutil
# 4 from sqlalchemy.orm import Session
# 5 from app.db.models import ControllerProviderConfig
# 6 from app.enums.logging_enums import PROVIDER_TYPE
# 7 from app.enums.system_enums import SYSTEM 
# 8 
# 9 SEED_FILES_DIR = Path(__file__).parent / "files/controller_providers"
# 10 PROJECT_ROOT   = SEED_FILES_DIR.parents[4]
# 11 EXTENSIONS_DIR = PROJECT_ROOT / "extensions"
# 12 
# 13 CONTROLLER_PROVIDERS = [
# 14     {
# 15         "id": 1,
# 16         "filename": "preprocessing_controller_provider.py",
# 17         "name": "preprocessing_controller",
# 18         "description": "Runs only the preprocessing step (e.g. linting).",
# 19         "tags": ["preprocessing", "controller"],
# 20         "config": {
# 21             "components": {
# 22                 "score_provider_id": 1,
# 23                 "systems": {
# 24                     SYSTEM.LINTING.value: 1
# 25                 }
# 26             },
# 27             "params": {
# 28                 "max_steps": 20
# 29             }
# 30         }
# 31     }
# 32 ]
# 33 
# 34 def seed_controller_providers(db_session: Session):
# 35     EXTENSIONS_DIR.mkdir(parents=True, exist_ok=True)
# 36     for entry in CONTROLLER_PROVIDERS:
# 37         guid = str(uuid4())
# 38         src = SEED_FILES_DIR / entry["filename"]
# 39         dest = EXTENSIONS_DIR / f"{guid}.py"
# 40         if not src.exists():
# 41             raise FileNotFoundError(f"Missing script: {src}")
# 42         shutil.copy(src, dest)
# 43 
# 44         rec = ControllerProviderConfig(
# 45             id=entry["id"],
# 46             guid=guid,
# 47             name=entry["name"],
# 48             description=entry["description"],
# 49             config=entry["config"],
# 50             artifact_path=dest.name,
# 51             tags=entry["tags"],
# 52             provider_type=PROVIDER_TYPE.CONTROLLER,
# 53         )
# 54         db_session.add(rec)
# 55     db_session.commit()
# 56     print("✅ Seeded controller providers")


# db\seeders\seed_program_provider.py

# 1 from uuid import uuid4
# 2 from pathlib import Path
# 3 import shutil
# 4 from sqlalchemy.orm import Session
# 5 from app.db.models import ProgramProviderConfig
# 6 from app.enums.controller_enums import CONTROLLER
# 7 from app.enums.logging_enums import PROVIDER_TYPE  #  ✅ Import your FSM state enum
# 8 
# 9 SEED_FILES_DIR = Path(__file__).resolve().parent / "files/program_providers"
# 10 PROJECT_ROOT   = SEED_FILES_DIR.parents[4]
# 11 EXTENSIONS_DIR = PROJECT_ROOT / "extensions"
# 12 
# 13 PROGRAM_PROVIDERS = [
# 14     {
# 15         "id": 1,
# 16         "filename": "codecritic_program_provider.py",
# 17         "name": "codecritic_program",
# 18         "description": "Top-level orchestration of all system controllers.",
# 19         "tags": ["program", "codecritic"],
# 20         "config": {
# 21             "components": {
# 22                 "score_provider_id": 1,
# 23                 "controllers": {
# 24                     CONTROLLER.PREPROCESSING.value: 1
# 25                 }
# 26             },
# 27             "params": {
# 28                 "max_steps": 20
# 29             }
# 30         }
# 31     }
# 32 ]
# 33 
# 34 def seed_program_providers(db_session: Session):
# 35     EXTENSIONS_DIR.mkdir(parents=True, exist_ok=True)
# 36 
# 37     for entry in PROGRAM_PROVIDERS:
# 38         guid = str(uuid4())
# 39         src  = SEED_FILES_DIR / entry["filename"]
# 40         dst  = EXTENSIONS_DIR / f"{guid}.py"
# 41 
# 42         if not src.exists():
# 43             raise FileNotFoundError(f"❌ Missing file: {src}")
# 44 
# 45         shutil.copy(src, dst)
# 46 
# 47         record = ProgramProviderConfig(
# 48             id            = entry["id"],
# 49             guid          = guid,
# 50             name          = entry["name"],
# 51             description   = entry["description"],
# 52             config        = entry["config"],
# 53             artifact_path = dst.name,
# 54             tags          = entry["tags"],
# 55             provider_type = PROVIDER_TYPE.PROGRAM,
# 56         )
# 57         db_session.add(record)
# 58 
# 59     db_session.commit()
# 60     print("✅ Seeded program providers")


# db\seeders\seed_prompt_providers.py

# 1 from uuid import uuid4
# 2 from pathlib import Path
# 3 import shutil
# 4 from sqlalchemy.orm import Session
# 5 from app.db.models import PromptProviderConfig
# 6 from app.enums.logging_enums import PROVIDER_TYPE
# 7 
# 8 CURRENT_DIR = Path(__file__).resolve().parent
# 9 SEED_FILES_DIR = CURRENT_DIR / "files/prompt_providers"
# 10 PROJECT_ROOT = SEED_FILES_DIR.parent.parent.parent.parent.parent
# 11 EXTENSIONS_DIR = PROJECT_ROOT / "extensions"
# 12 
# 13 PROVIDERS = [
# 14     {
# 15         "id": 1,
# 16         "filename": "basic_prompt_provider.py",
# 17         "name": "basic_prompt_provider",
# 18         "description": "Returns a simple prompt response.",
# 19         "tags": ["default"],
# 20         "config": {
# 21             "components": {},
# 22             "params": {}
# 23         }
# 24     },
# 25     {
# 26         "id": 2,
# 27         "filename": "linting_prompt_provider.py",
# 28         "name": "linting_prompt_provider",
# 29         "description": "Combines linting system and agent prompts.",
# 30         "tags": ["linting", "codequality"],
# 31         "config": {
# 32             "components": {
# 33                 "agent_prompt_id": 2,
# 34                 "system_prompt_id": 2,
# 35                 "context_provider_id": 2
# 36             },
# 37             "params": {}
# 38         }
# 39     }
# 40 ]
# 41 
# 42 def seed_prompt_providers(db_session: Session):
# 43     EXTENSIONS_DIR.mkdir(parents=True, exist_ok=True)
# 44 
# 45     for entry in PROVIDERS:
# 46         guid = str(uuid4())
# 47         dest_filename = f"{guid}.py"
# 48         source_file = SEED_FILES_DIR / entry["filename"]
# 49         dest_file = EXTENSIONS_DIR / dest_filename
# 50 
# 51         if not source_file.exists():
# 52             raise FileNotFoundError(f"Prompt provider script not found: {source_file}")
# 53 
# 54         shutil.copy(source_file, dest_file)
# 55 
# 56         config = PromptProviderConfig(
# 57             id=entry["id"],
# 58             guid=guid,
# 59             name=entry["name"],
# 60             description=entry["description"],
# 61             config=entry["config"],
# 62             artifact_path=dest_filename,
# 63             tags=entry["tags"],
# 64             provider_type=PROVIDER_TYPE.PROMPT, 
# 65         )
# 66 
# 67         db_session.add(config)
# 68 
# 69     db_session.commit()
# 70     print("Seeded prompt provider configurations successfully.")


# db\seeders\seed_prompts.py

# 1 from pathlib import Path
# 2 import shutil
# 3 from sqlalchemy.orm import Session
# 4 from uuid import uuid4
# 5 from app.db.models import AgentPrompt, SystemPrompt
# 6 from app.enums.system_enums import SYSTEM
# 7 
# 8 #  Define paths relative to this script's file location explicitly
# 9 CURRENT_DIR = Path(__file__).resolve().parent
# 10 SEED_FILES_DIR = CURRENT_DIR / "files/prompts"
# 11 PROJECT_ROOT = CURRENT_DIR.parent.parent.parent
# 12 EXTENSIONS_DIR = PROJECT_ROOT / "extensions"
# 13 
# 14 #  Define new prompt files to seed
# 15 AGENT_PROMPT_FILES = [
# 16     {
# 17         "id": 1,
# 18         "name": "generate",
# 19         "filename": "basic_agent_prompt.txt",
# 20         "description": "Prompt for basic code generation.",
# 21         "tags": ["linting", "generator"]
# 22     },
# 23     {
# 24         "id": 2,
# 25         "name": "linting_generator_agent",
# 26         "filename": "linting_generator_agent_prompt.txt",
# 27         "description": "Prompt for linting generator agent.",
# 28         "tags": ["linting", "generator"]
# 29     }
# 30 ]
# 31 
# 32 SYSTEM_PROMPT_FILES = [
# 33     {
# 34         "id": 1,
# 35         "name": "format",
# 36         "filename": "default_system_prompt.txt",
# 37         "system_type": SYSTEM.LINTING,
# 38         "description": "Default prompt for formatting tasks.",
# 39         "tags": ["formatting", "default"]
# 40     },
# 41     {
# 42         "id": 2,
# 43         "name": "linting_system",
# 44         "filename": "linting_system_prompt.txt",
# 45         "system_type": SYSTEM.LINTING,
# 46         "description": "System prompt governing the linting subsystem.",
# 47         "tags": ["linting", "system"]
# 48     }
# 49 ]
# 50 
# 51 
# 52 def seed_prompts(db_session: Session):
# 53     EXTENSIONS_DIR.mkdir(parents=True, exist_ok=True)
# 54 
# 55     for entry in AGENT_PROMPT_FILES:
# 56         source_file = SEED_FILES_DIR / entry["filename"]
# 57         if not source_file.exists():
# 58             raise FileNotFoundError(f"Agent prompt source file not found: {source_file}")
# 59 
# 60         guid = str(uuid4())
# 61         dest_filename = f"{guid}.txt"
# 62         dest_file = EXTENSIONS_DIR / dest_filename
# 63         shutil.copy(source_file, dest_file)
# 64 
# 65         prompt = AgentPrompt(
# 66             id=entry["id"],
# 67             guid=guid,
# 68             name=entry["name"],
# 69             description=entry["description"],
# 70             artifact_path=dest_filename,  #  Store relative path
# 71             tags=entry["tags"]
# 72         )
# 73         db_session.add(prompt)
# 74         print(f"Seeded AgentPrompt '{entry['name']}' with ID: {entry['id']} and GUID: {guid}")
# 75 
# 76     for entry in SYSTEM_PROMPT_FILES:
# 77         source_file = SEED_FILES_DIR / entry["filename"]
# 78         if not source_file.exists():
# 79             raise FileNotFoundError(f"System prompt source file not found: {source_file}")
# 80 
# 81         guid = str(uuid4())
# 82         dest_filename = f"{guid}.txt"
# 83         dest_file = EXTENSIONS_DIR / dest_filename
# 84         shutil.copy(source_file, dest_file)
# 85 
# 86         prompt = SystemPrompt(
# 87             id=entry["id"],
# 88             guid=guid,
# 89             name=entry["name"],
# 90             system_type=entry["system_type"],
# 91             description=entry["description"],
# 92             artifact_path=dest_filename,  #  Store relative path
# 93             tags=entry["tags"]
# 94         )
# 95         db_session.add(prompt)
# 96         print(f"Seeded SystemPrompt '{entry['name']}' with ID: {entry['id']} and GUID: {guid}")
# 97 
# 98     db_session.commit()


# db\seeders\seed_score_providers.py

# 1 from uuid import uuid4
# 2 from pathlib import Path
# 3 import shutil
# 4 from sqlalchemy.orm import Session
# 5 from app.db.models import ScoreProviderConfig
# 6 from app.db.schemas import LintingScoreConfig, CodeStabilityScoreConfig
# 7 from app.enums.logging_enums import PROVIDER_TYPE
# 8 
# 9 CURRENT_DIR = Path(__file__).resolve().parent
# 10 SEED_FILES_DIR = CURRENT_DIR / "files/score_providers"
# 11 PROJECT_ROOT = SEED_FILES_DIR.parent.parent.parent.parent.parent
# 12 EXTENSIONS_DIR = PROJECT_ROOT / "extensions"
# 13 
# 14 SCORES = [
# 15     {
# 16         "id": 1,
# 17         "filename": "linting_score_provider.py",
# 18         "name": "linting_score_provider",
# 19         "description": "Scores lint compliance.",
# 20         "config": {
# 21             "components": {
# 22                 "tool_provider_ids": {
# 23                     "black": 1,
# 24                     "ruff": 3,
# 25                     "mypy": 5,
# 26                     "radon": 4
# 27                 }
# 28             },
# 29             "params": LintingScoreConfig(
# 30                 weight_ruff=0.7,
# 31                 weight_black=0.2,
# 32                 weight_mypy=0.1,
# 33                 fail_threshold=0.75
# 34             ).model_dump()
# 35         },
# 36         "tags": ["linting"]
# 37     },
# 38     {
# 39         "id": 2,
# 40         "filename": "code_stability_score_provider.py",
# 41         "name": "code_stability_score_provider",
# 42         "description": "Ensures code is structurally and syntactically safe for downstream use.",
# 43         "config": {
# 44             "components": {},
# 45             "params": CodeStabilityScoreConfig(
# 46                 failure_penalty=0.25,
# 47                 diff_penalty=0.3,
# 48                 test_weight=0.25,
# 49                 coverage_weight=0.2
# 50             ).model_dump()
# 51         },
# 52         "tags": ["stability", "safety"]
# 53     }
# 54 ]
# 55 
# 56 def seed_score_providers(db_session: Session):
# 57     EXTENSIONS_DIR.mkdir(parents=True, exist_ok=True)
# 58 
# 59     for entry in SCORES:
# 60         guid = str(uuid4())
# 61         dest_filename = f"{guid}.py"
# 62         source_file = SEED_FILES_DIR / entry["filename"]
# 63         dest_file = EXTENSIONS_DIR / dest_filename
# 64 
# 65         if not source_file.exists():
# 66             raise FileNotFoundError(f"Score provider script not found: {source_file}")
# 67 
# 68         shutil.copy(source_file, dest_file)
# 69 
# 70         score = ScoreProviderConfig(
# 71             id=entry["id"],
# 72             guid=guid,
# 73             name=entry["name"],
# 74             description=entry["description"],
# 75             config=entry["config"],
# 76             artifact_path=dest_filename,
# 77             tags=entry["tags"],
# 78             provider_type=PROVIDER_TYPE.SCORE,
# 79         )
# 80 
# 81         db_session.add(score)
# 82 
# 83     db_session.commit()
# 84     print("Seeded score providers successfully.")


# db\seeders\seed_session_configs.py

# 1 #  app/db/seeders/seed_session_configs.py
# 2 
# 3 from sqlalchemy.orm import Session
# 4 from app.db.models import SessionConfig
# 5 
# 6 SESSION_CONFIGS = [
# 7     {
# 8         "id": 1,
# 9         "program_provider_id": 1,
# 10         "name": "codecritic_test_session",
# 11         "description": "Test session for program 1",
# 12         "environment_type": "experiment",
# 13         "tags": ["test", "experiment"]
# 14     }
# 15 ]
# 16 
# 17 def seed_session_configs(db_session: Session):
# 18     for sess in SESSION_CONFIGS:
# 19         record = SessionConfig(
# 20             id               = sess["id"],
# 21             program_provider_id = sess["program_provider_id"],
# 22             name             = sess["name"],
# 23             description      = sess["description"],
# 24             environment_type = sess["environment_type"],
# 25             tags             = sess["tags"],
# 26         )
# 27         db_session.add(record)
# 28 
# 29     db_session.commit()
# 30     print("✅ Seeded session configs")


# db\seeders\seed_state_providers.py

# 1 from uuid import uuid4
# 2 from pathlib import Path
# 3 import shutil
# 4 from sqlalchemy.orm import Session
# 5 from app.db.models import StateProviderConfig
# 6 from app.enums.logging_enums import PROVIDER_TYPE
# 7 from app.enums.state_enums import STATE
# 8 from app.enums.agent_enums import AGENT
# 9 
# 10 SEED_FILES_DIR = Path(__file__).resolve().parent / "files/state_providers"
# 11 PROJECT_ROOT = SEED_FILES_DIR.parent.parent.parent.parent.parent
# 12 EXTENSIONS_DIR = PROJECT_ROOT / "extensions"
# 13 
# 14 STATE_PROVIDERS = [
# 15     {
# 16         "id": 1,
# 17         "filename": "linting_generator_state_provider.py",
# 18         "name": "LintingGeneratorStateProvider",
# 19         "description": "Runs the generator agent in its own FSM wrapper.",
# 20         "tags": ["linting", "generator"],
# 21         "config": {
# 22             "components": {
# 23                 "score_provider_id": 1,
# 24                 "agents": {
# 25                     AGENT.GENERATOR.value: 2
# 26                 }
# 27             },
# 28             "params": {
# 29                 "max_steps": 20
# 30             }
# 31         }
# 32     },
# 33     {
# 34         "id": 2,
# 35         "filename": "linting_discriminator_state_provider.py",
# 36         "name": "LintingDiscriminatorStateProvider",
# 37         "description": "Runs the discriminator agent in its own FSM wrapper.",
# 38         "tags": ["linting", "discriminator"],
# 39         "config": {
# 40             "components": {
# 41                 "score_provider_id": 1,
# 42                 "agents": {
# 43                     AGENT.DISCRIMINATOR.value: 3
# 44                 }
# 45             },
# 46             "params": {
# 47                 "max_steps": 20
# 48             }
# 49         }
# 50     },
# 51     {
# 52         "id": 3,
# 53         "filename": "code_stability_state_provider.py",
# 54         "name": "CodeStabilityStateProvider",
# 55         "description": "Gates code based on parse, compile, import, and static validation success.",
# 56         "tags": ["stability"],
# 57         "config": {
# 58             "components": {
# 59                 "score_provider_id": 1,
# 60                 "agents": {
# 61                     AGENT.STABILITY.value: 4
# 62                 }
# 63             },
# 64             "params": {
# 65                 "max_steps": 20
# 66             }
# 67         }
# 68     }
# 69 ]
# 70 
# 71 def seed_state_providers(db_session: Session):
# 72     EXTENSIONS_DIR.mkdir(parents=True, exist_ok=True)
# 73 
# 74     for entry in STATE_PROVIDERS:
# 75         guid = str(uuid4())
# 76         source_file = SEED_FILES_DIR / entry["filename"]
# 77         dest_filename = f"{guid}.py"
# 78         dest_file = EXTENSIONS_DIR / dest_filename
# 79 
# 80         if not source_file.exists():
# 81             raise FileNotFoundError(f"State provider script not found: {source_file}")
# 82 
# 83         shutil.copy(source_file, dest_file)
# 84 
# 85         record = StateProviderConfig(
# 86             id=entry["id"],
# 87             guid=guid,
# 88             name=entry["name"],
# 89             description=entry["description"],
# 90             config=entry["config"],
# 91             artifact_path=dest_filename,
# 92             tags=entry["tags"],
# 93             provider_type=PROVIDER_TYPE.STATE, 
# 94         )
# 95 
# 96         db_session.add(record)
# 97 
# 98     db_session.commit()
# 99     print("✅ Seeded state provider configurations successfully.")


# db\seeders\seed_system_providers.py

# 1 from uuid import uuid4
# 2 from pathlib import Path
# 3 import shutil
# 4 from sqlalchemy.orm import Session
# 5 from app.db.models import SystemProviderConfig
# 6 from app.enums.logging_enums import PROVIDER_TYPE
# 7 from app.enums.state_enums import STATE
# 8 from app.enums.system_enums import SYSTEM
# 9 
# 10 SEED_FILES_DIR = Path(__file__).resolve().parent / "files/system_providers"
# 11 PROJECT_ROOT = SEED_FILES_DIR.parent.parent.parent.parent.parent
# 12 EXTENSIONS_DIR = PROJECT_ROOT / "extensions"
# 13 
# 14 SYSTEM_PROVIDERS = [
# 15     {
# 16         "id": 1,
# 17         "filename": "linting_system_provider.py",
# 18         "name": "linting_system_provider",
# 19         "description": "System to iteratively lint and evaluate Python code.",
# 20         "system_type": SYSTEM.LINTING.value,
# 21         "tags": ["linting", "system"],
# 22         "config": {
# 23             "components": {
# 24                 "score_provider_id": 1,
# 25                 "states": {
# 26                     STATE.GENERATE.value: 1,
# 27                     STATE.DISCRIMINATE.value: 2,
# 28                     STATE.CODE_STABILITY.value: 3
# 29                 }
# 30             },
# 31             "params": {
# 32                 "max_steps": 20
# 33             }
# 34         }
# 35     }
# 36 ]
# 37 
# 38 def seed_system_providers(db_session: Session):
# 39     EXTENSIONS_DIR.mkdir(parents=True, exist_ok=True)
# 40 
# 41     for entry in SYSTEM_PROVIDERS:
# 42         guid = str(uuid4())
# 43         source_file = SEED_FILES_DIR / entry["filename"]
# 44         dest_filename = f"{guid}.py"
# 45         dest_file = EXTENSIONS_DIR / dest_filename
# 46 
# 47         if not source_file.exists():
# 48             raise FileNotFoundError(f"System provider script not found: {source_file}")
# 49 
# 50         shutil.copy(source_file, dest_file)
# 51 
# 52         record = SystemProviderConfig(
# 53             id=entry["id"],
# 54             guid=guid,
# 55             name=entry["name"],
# 56             description=entry["description"],
# 57             system_type=entry["system_type"],
# 58             config=entry["config"],
# 59             artifact_path=dest_filename,
# 60             tags=entry["tags"],
# 61             provider_type=PROVIDER_TYPE.SYSTEM,
# 62         )
# 63 
# 64         db_session.add(record)
# 65 
# 66     db_session.commit()
# 67     print("✅ Seeded system provider configurations successfully.")


# db\seeders\seed_tool_providers.py

# 1 from uuid import uuid4
# 2 from pathlib import Path
# 3 import shutil
# 4 from sqlalchemy.orm import Session
# 5 from app.db.models import ToolProviderConfig
# 6 from app.enums.logging_enums import PROVIDER_TYPE
# 7 
# 8 CURRENT_DIR = Path(__file__).resolve().parent
# 9 SEED_FILES_DIR = CURRENT_DIR / "files/tool_providers"
# 10 PROJECT_ROOT = SEED_FILES_DIR.parent.parent.parent.parent.parent
# 11 EXTENSIONS_DIR = PROJECT_ROOT / "extensions"
# 12 
# 13 TOOLS = [
# 14     {
# 15         "id": 1,
# 16         "filename": "black_tool.py",
# 17         "name": "black",
# 18         "description": "Formats Python code using Black.",
# 19         "config": {"components": {}, "params": {}}
# 20     },
# 21     {
# 22         "id": 2,
# 23         "filename": "sonarcloud_tool.py",
# 24         "name": "sonarcloud",
# 25         "description": "Static analysis via SonarCloud.",
# 26         "config": {"components": {}, "params": {}}
# 27     },
# 28     {
# 29         "id": 3,
# 30         "filename": "ruff_tool.py",
# 31         "name": "ruff",
# 32         "description": "Python linting with Ruff.",
# 33         "config": {"components": {}, "params": {}}
# 34     },
# 35     {
# 36         "id": 4,
# 37         "filename": "radon_tool.py",
# 38         "name": "radon",
# 39         "description": "Analyzes Python code complexity.",
# 40         "config": {"components": {}, "params": {}}
# 41     },
# 42     {
# 43         "id": 5,
# 44         "filename": "mypy_tool.py",
# 45         "name": "mypy",
# 46         "description": "Static type checking with mypy.",
# 47         "config": {"components": {}, "params": {}}
# 48     },
# 49     {
# 50         "id": 6,
# 51         "filename": "docformatter_tool.py",
# 52         "name": "docformatter",
# 53         "description": "Formats docstrings using docformatter.",
# 54         "config": {"components": {}, "params": {}}
# 55     },
# 56     {
# 57         "id": 7,
# 58         "filename": "symbol_graph.py",
# 59         "name": "symbol_graph",
# 60         "description": "Extracts and analyzes Python symbols into structured graphs.",
# 61         "config": {"components": {}, "params": {}}
# 62     }
# 63 ]
# 64 
# 65 def seed_tool_providers(db_session: Session):
# 66     EXTENSIONS_DIR.mkdir(parents=True, exist_ok=True)
# 67 
# 68     for entry in TOOLS:
# 69         guid = str(uuid4())
# 70         dest_filename = f"{guid}.py"
# 71         source_file = SEED_FILES_DIR / entry["filename"]
# 72         dest_file = EXTENSIONS_DIR / dest_filename
# 73 
# 74         if not source_file.exists():
# 75             raise FileNotFoundError(f"Tool script not found: {source_file}")
# 76 
# 77         shutil.copy(source_file, dest_file)
# 78 
# 79         tool = ToolProviderConfig(
# 80             id=entry["id"],
# 81             guid=guid,
# 82             name=entry["name"],
# 83             description=entry["description"],
# 84             config=entry["config"],
# 85             artifact_path=dest_filename,
# 86             provider_type=PROVIDER_TYPE.TOOL,
# 87         )
# 88 
# 89         db_session.add(tool)
# 90 
# 91     db_session.commit()
# 92     print("Seeded tool configurations successfully.")


# enums\__init__.py



# enums\agent_engine_enums.py

# 1 from enum import Enum
# 2 
# 3 
# 4 class AGENT_ENGINE_MODEL(str, Enum):
# 5     MOCK = "mock-llm"
# 6     GPT_4O = "gpt-4o"
# 7     GEMINI_1_5_PRO = "gemini-1.5-pro"
# 8     CLAUDE_3_SONNET = "claude-3-sonnet"


# enums\agent_enums.py

# 1 from __future__ import annotations
# 2 
# 3 from enum import Enum
# 4 
# 5 
# 6 class AGENT(str, Enum):
# 7     START = "start"
# 8     END = "end"
# 9     GENERATOR = "generator"
# 10     DISCRIMINATOR = "discriminator"
# 11     MEDIATOR = "mediator"
# 12     STABILITY = "stability"
# 13     BASIC = "basic"
# 14     UNKNOWN = "unknown"
# 15 
# 16 class AGENT_STATE(Enum):
# 17     INIT = "init"
# 18     RUNNING = "running"
# 19     COMPLETE = "complete"


# enums\controller_enums.py

# 1 from enum import Enum
# 2 
# 3 class CONTROLLER(str, Enum):
# 4     START = "start"
# 5     END = "end"
# 6     PREPROCESSING = "preprocessing_controller"
# 7     TESTING = "testing_controller"
# 8     OBSERVABILITY = "observability_controller"
# 9     DOCUMENTATION = "documentation_controller"
# 10     REVIEW = "review_controller"
# 11     PLANNING = "planning_controller"
# 12     MLOPS = "mlops_controller"
# 13     SAFETY = "safety_controller"
# 14     MISC = "misc_controller"


# enums\fsm_enums.py

# 1 from enum import Enum
# 2 
# 3 class STATE_TYPE(str, Enum):
# 4     START = "start"
# 5     INTERMEDIATE = "intermediate"
# 6     END = "end"
# 7 
# 8 
# 9 class DECISION_TYPE(str, Enum):
# 10     INITIAL = "initial"
# 11     IMPROVED = "improved"
# 12     ACCEPTED = "accepted"
# 13     REJECTED = "rejected"
# 14     UNKNOWN = "unknown"
# 15 
# 16 class TRANSITION_REASON_TYPE(str, Enum):
# 17     INITIALIZATION = "initialization"
# 18     SUCCESSFUL = "completed successfully"
# 19     UNSUCCESSFUL = "completed unsuccessfully"
# 20     GENERATING = "generating"
# 21     EVALUATING = "evaluating" 
# 22     ERROR = "error"
# 23     CUSTOM_RULE = "custom_rule"
# 24 


# enums\logging_enums.py

# 1 from enum import Enum
# 2 from typing import List, Optional
# 3 
# 4 class LOG_TYPE(str, Enum):
# 5     ERROR = "error_log"
# 6     PROVIDER = "provider_log"
# 7     STATE_TRANSITION = "state_transition_log"
# 8     AGENT_CONVERSATION = "agent_conversation_log"
# 9     SNAPSHOT_METRICS = "snapshot_metrics"
# 10     FILE = "file_log"
# 11 
# 12 class PROVIDER_TYPE(str, Enum):
# 13     TOOL = "tool"
# 14     AGENT_ENGINE = "agent_engine"
# 15     AGENT = "agent"
# 16     PROMPT = "prompt"
# 17     CONTEXT = "context"
# 18     SCORE = "score"
# 19     STATE = "state"
# 20     SYSTEM = "system"
# 21     CONTROLLER = "controller"
# 22     PROGRAM = "program"
# 23     SESSION = "session"
# 24     UNKNOWN = "unknown"
# 25 
# 26 class ERROR_TYPE(str, Enum):
# 27     RUNTIME = "runtime"
# 28     VALIDATION = "validation"
# 29     CONFIGURATION = "configuration"
# 30     CONNECTION = "connection"
# 31     AUTHENTICATION = "authentication"
# 32     TIMEOUT = "timeout"
# 33     UNKNOWN = "unknown"
# 34 
# 35 class RunContext:
# 36     def __init__(
# 37         self,
# 38         *,
# 39         called_by_type: PROVIDER_TYPE,
# 40         called_by_id: int,
# 41         session_id: str,
# 42         file_log_id: str,
# 43         parent_id: Optional[str] = None,
# 44         execution_chain: Optional[List[str]] = None,
# 45     ):
# 46         self.called_by_type = called_by_type
# 47         self.called_by_id = called_by_id
# 48         self.session_id = session_id
# 49         self.file_log_id = file_log_id
# 50         self.parent_id = parent_id
# 51         self.execution_chain = execution_chain or []


# enums\scoring_enums.py

# 1 from enum import Enum
# 2 
# 3 class SCORING_METRIC_TYPE(str, Enum):
# 4     LINTING_SCORE = "linting_score"
# 5     CODE_STABILITY_SCORE = "code_stability_score"


# enums\state_enums.py

# 1 from enum import Enum
# 2 
# 3 class STATE(str, Enum):
# 4     START = "start"
# 5     END = "end"
# 6     PREPROCESS = "preprocess"
# 7     CODE_STABILITY = "code_stability"
# 8     GENERATE = "generate"
# 9     DISCRIMINATE = "discriminate"


# enums\system_enums.py

# 1 from __future__ import annotations
# 2 from enum import Enum
# 3 
# 4 
# 5 class SYSTEM(str, Enum):
# 6     START = "start"
# 7     END = "end"
# 8 
# 9     #  Core Code Transformation
# 10     LINTING = "linting"
# 11     FORMATTING = "formatting"
# 12     DOCSTRING = "docstring"
# 13     TYPE_ANNOTATION = "type_annotation"
# 14     REFACTORING = "refactoring"
# 15     DEAD_CODE_REMOVAL = "dead_code_removal"
# 16     COMPLEXITY_REDUCTION = "complexity_reduction"
# 17 
# 18     #  Testing and Validation
# 19     UNIT_TESTING = "unit_testing"
# 20     EDGE_TESTING = "edge_testing"
# 21     INTEGRATION_TESTING = "integration_testing"
# 22     TEST_FIX_GENERATION = "test_fix_generation"
# 23     MOCK_GENERATION = "mock_generation"
# 24     STUB_EXTRACTION = "stub_extraction"
# 25 
# 26     #  Observability and Instrumentation
# 27     LOGGING_INJECTION = "logging_injection"
# 28     TRACE_ANNOTATION = "trace_annotation"
# 29     ERROR_HANDLING = "error_handling"
# 30 
# 31     #  Documentation and Explanation
# 32     INLINE_COMMENTING = "inline_commenting"
# 33     FUNCTION_SUMMARIZATION = "function_summarization"
# 34     FILE_SUMMARIZATION = "file_summarization"
# 35     CHANGE_SUMMARIZATION = "change_summarization"
# 36 
# 37     #  Review and Oversight
# 38     CODE_REVIEW = "code_review"
# 39     STATIC_ANALYSIS = "static_analysis"
# 40     REGRESSION_ANALYSIS = "regression_analysis"
# 41     MEDIATOR_RECONCILIATION = "mediator_reconciliation"
# 42     SCORING_EVALUATION = "scoring_evaluation"
# 43 
# 44     #  Planning and Organization
# 45     SYMBOL_GRAPH_GENERATION = "symbol_graph_generation"
# 46     DEPENDENCY_MAPPING = "dependency_mapping"
# 47     FILE_RESTRUCTURING = "file_restructuring"
# 48     FUNCTION_SPLITTING = "function_splitting"
# 49 
# 50     #  MLOps-Specific
# 51     DATA_VALIDATION = "data_validation"
# 52     METADATA_ANNOTATION = "metadata_annotation"
# 53     MODEL_DOC_GENERATION = "model_doc_generation"
# 54     PIPELINE_TESTING = "pipeline_testing"
# 55     TRAINING_CONFIG_AUDIT = "training_config_audit"
# 56     FEATURE_TRACEABILITY = "feature_traceability"
# 57 
# 58     #  Production Safety
# 59     SECURITY_CHECK = "security_check"
# 60     POLICY_COMPLIANCE = "policy_compliance"
# 61     LICENSE_CHECK = "license_check"
# 62 
# 63     #  Misc / Cross-cutting
# 64     PATCHING = "patching"
# 65     PROMPT_REFINEMENT = "prompt_refinement"
# 66     AGENT_RECOMMENDATION = "agent_recommendation"
# 67     SEMANTIC_DIFF = "semantic_diff"
# 68     CONTEXT_FILTERING = "context_filtering"
# 69 
# 70     UNKNOWN = "unknown"
# 71 
# 72 
# 73 
# 74 


# enums\tool_enums.py

# 1 from enum import Enum
# 2 
# 3 class ToolProvider(Enum):
# 4     BLACK = 1
# 5     SONARCLOUD = 2
# 6     RUFF = 3
# 7     RADON = 4
# 8     MYPY = 5
# 9     DOCFORMATTER = 6
# 10     SYMBOL_GRAPH = 7
# 11 
# 12     @classmethod
# 13     def get_name(cls, id_):
# 14         for tool in cls:
# 15             if tool.value == id_:
# 16                 return tool.name.lower()
# 17         return f"Unknown ({id_})"


# factories\__init__.py



# factories\agent_engine_provider_factory.py

# 1 from app.db.models import AgentEngineProviderConfig
# 2 from app.enums.logging_enums import RunContext
# 3 from app.factories.base_provider_factory import BaseProviderFactory
# 4 from app.providers.agent_engine_provider_base import AgentEngineProviderBase
# 5 
# 6 
# 7 class AgentEngineProviderFactory(BaseProviderFactory):
# 8     config_model = AgentEngineProviderConfig
# 9     base_class = AgentEngineProviderBase
# 10 
# 11     @classmethod
# 12     def create(
# 13         cls,
# 14         id: int,
# 15         *,
# 16         context: RunContext,
# 17         **kwargs
# 18     ) -> AgentEngineProviderBase:
# 19         instance = super().create(
# 20             id,
# 21             context=context,
# 22             **kwargs
# 23         )
# 24 
# 25         #  🔧 Extract components and params from config for consistency (if needed downstream)
# 26         raw_config = instance._config.config or {}
# 27         components = raw_config.get("components") or {}
# 28         params     = raw_config.get("params") or {}
# 29 
# 30         #  Optionally inject into instance if base class expects them split
# 31         instance._components = components
# 32         instance._params = params
# 33 
# 34         return instance


# factories\agent_provider_factory.py

# 1 from app.factories.base_provider_factory import BaseProviderFactory
# 2 from app.db.models import AgentProviderConfig
# 3 from app.providers.agent_provider_base import AgentProviderBase
# 4 from app.enums.logging_enums import PROVIDER_TYPE, RunContext
# 5 
# 6 
# 7 class AgentProviderFactory(BaseProviderFactory):
# 8     config_model = AgentProviderConfig
# 9     base_class = AgentProviderBase
# 10 
# 11     @classmethod
# 12     def create(
# 13         cls,
# 14         id: int,
# 15         *,
# 16         context: RunContext,
# 17         **kwargs
# 18     ) -> AgentProviderBase:
# 19         from app.factories.agent_engine_provider_factory import AgentEngineProviderFactory
# 20         from app.factories.prompt_provider_factory import PromptProviderFactory
# 21         from app.factories.context_provider_factory import ContextProviderFactory
# 22         from app.factories.score_provider_factory import ScoreProviderFactory
# 23         from app.factories.tool_provider_factory import ToolProviderFactory
# 24 
# 25         preload_instance = super().create(id, context=context)
# 26 
# 27         #  ✅ Use already-injected structured config
# 28         components = preload_instance._components
# 29         provider_id = preload_instance._config.id
# 30         provider_type = preload_instance._provider_type
# 31 
# 32         #  ─── Child context ─────────────────────────────────────────────
# 33         child_context = RunContext(
# 34             called_by_type=provider_type,
# 35             called_by_id=provider_id,
# 36             session_id=context.session_id,
# 37             file_log_id=context.file_log_id,
# 38             parent_id=preload_instance._run_id,
# 39             execution_chain=context.execution_chain.copy()
# 40         )
# 41 
# 42         #  ─── Create subproviders ───────────────────────────────────────
# 43         agent_engine = (
# 44             AgentEngineProviderFactory.create(
# 45                 components["agent_engine_provider_id"],
# 46                 context=child_context,
# 47             ) if components.get("agent_engine_provider_id") else None
# 48         )
# 49 
# 50         prompt_provider = (
# 51             PromptProviderFactory.create(
# 52                 components["prompt_provider_id"],
# 53                 context=child_context,
# 54             ) if components.get("prompt_provider_id") else None
# 55         )
# 56 
# 57         context_provider = (
# 58             ContextProviderFactory.create(
# 59                 components["context_provider_id"],
# 60                 context=child_context,
# 61             ) if components.get("context_provider_id") else None
# 62         )
# 63 
# 64         score_provider = (
# 65             ScoreProviderFactory.create(
# 66                 components["score_provider_id"],
# 67                 context=child_context,
# 68             ) if components.get("score_provider_id") else None
# 69         )
# 70 
# 71         tool_providers = [
# 72             ToolProviderFactory.create(
# 73                 tid,
# 74                 context=child_context,
# 75             )
# 76             for tid in (components.get("tool_provider_ids") or {}).values()
# 77         ]
# 78 
# 79         #  ─── Final instance construction ───────────────────────────────
# 80         cls_type = type(preload_instance)
# 81         return cls_type(
# 82             config=preload_instance._config,
# 83             agent_engine=agent_engine,
# 84             prompt_provider=prompt_provider,
# 85             context_provider=context_provider,
# 86             score_provider=score_provider,
# 87             tool_providers=tool_providers,
# 88             context=context
# 89         )


# factories\base_provider_factory.py

# 1 from sqlalchemy.orm import Session
# 2 from importlib.util import spec_from_file_location, module_from_spec
# 3 from pathlib import Path
# 4 
# 5 from app.db import init_db  #  global DB engine instance
# 6 from app.providers.base_provider import BaseProvider
# 7 from app.enums.logging_enums import PROVIDER_TYPE, RunContext
# 8 
# 9 PROJECT_ROOT = Path(__file__).resolve().parents[2]
# 10 EXTENSIONS_DIR = PROJECT_ROOT / "extensions"
# 11 
# 12 engine = init_db(reset=False)
# 13 
# 14 
# 15 class BaseProviderFactory:
# 16     config_model = None
# 17     base_class = BaseProvider
# 18 
# 19     @classmethod
# 20     def create(
# 21         cls,
# 22         id: int,
# 23         *,
# 24         context: RunContext,
# 25         **kwargs
# 26     ) -> BaseProvider:
# 27         with Session(bind=engine) as session:
# 28             config = session.get(cls.config_model, int(id))
# 29             if not config:
# 30                 raise ValueError(f"{cls.config_model.__name__} ID {id} not found")
# 31 
# 32             ext_path = (EXTENSIONS_DIR / config.artifact_path).resolve()
# 33             if not ext_path.exists():
# 34                 raise FileNotFoundError(f"Extension not found: {ext_path}")
# 35 
# 36         spec = spec_from_file_location(ext_path.stem, ext_path)
# 37         if spec is None or spec.loader is None:
# 38             raise ImportError(f"Cannot load module spec for {ext_path}")
# 39 
# 40         module = module_from_spec(spec)
# 41         spec.loader.exec_module(module)
# 42 
# 43         provider_class = next(
# 44             (obj for obj in vars(module).values()
# 45              if isinstance(obj, type) and issubclass(obj, cls.base_class) and obj is not cls.base_class),
# 46             None
# 47         )
# 48 
# 49         if provider_class is None:
# 50             raise ImportError(f"No valid subclass of {cls.base_class.__name__} found in {ext_path}")
# 51 
# 52         #  🎯 Extract and normalize config structure
# 53         full_config = config.config or {}
# 54         components = full_config.get("components") or {}
# 55         params     = full_config.get("params") or {}
# 56 
# 57         #  ✅ Create instance and attach structured config access
# 58         instance = provider_class(config=config, context=context, **kwargs)
# 59         instance._components = components
# 60         instance._params = params
# 61 
# 62         return instance


# factories\context_provider_factory.py

# 1 from app.factories.base_provider_factory import BaseProviderFactory
# 2 from app.db.models import ContextProviderConfig
# 3 from app.providers.context_provider_base import ContextProviderBase
# 4 from app.enums.logging_enums import RunContext
# 5 
# 6 
# 7 class ContextProviderFactory(BaseProviderFactory):
# 8     config_model = ContextProviderConfig
# 9     base_class = ContextProviderBase
# 10 
# 11     @classmethod
# 12     def create(
# 13         cls,
# 14         id: int,
# 15         *,
# 16         context: RunContext,
# 17         **kwargs
# 18     ) -> ContextProviderBase:
# 19         from app.factories.score_provider_factory import ScoreProviderFactory
# 20         from app.factories.tool_provider_factory import ToolProviderFactory
# 21 
# 22         preload_instance = super().create(id, context=context)
# 23         components = preload_instance._components  #  injected by BaseProviderFactory
# 24         provider_id = preload_instance._config.id
# 25         provider_type = preload_instance._provider_type
# 26 
# 27         #  ─── Child context for subproviders ─────────────────────────────
# 28         child_context = RunContext(
# 29             called_by_type=provider_type,
# 30             called_by_id=provider_id,
# 31             session_id=context.session_id,
# 32             file_log_id=context.file_log_id,
# 33             parent_id=preload_instance._run_id,
# 34             execution_chain=context.execution_chain.copy()
# 35         )
# 36 
# 37         if (score_id := components.get("score_provider_id")):
# 38             if isinstance(score_id, int) and score_id > 0:
# 39                 score = ScoreProviderFactory.create(
# 40                     score_id,
# 41                     context=child_context
# 42                 )
# 43                 preload_instance.set_score_provider(score)
# 44                 if hasattr(score, "set_context_provider"):
# 45                     score.set_context_provider(preload_instance)
# 46 
# 47         for _, tool_id in (components.get("tool_provider_ids") or {}).items():
# 48             tool = ToolProviderFactory.create(
# 49                 tool_id,
# 50                 context=child_context
# 51             )
# 52             preload_instance.set_tool_provider(tool)
# 53 
# 54         return preload_instance


# factories\controller_provider_factory.py

# 1 from app.factories.base_provider_factory import BaseProviderFactory
# 2 from app.db.models import ControllerProviderConfig
# 3 from app.providers.controller_provider_base import ControllerProviderBase
# 4 from app.enums.logging_enums import RunContext
# 5 
# 6 
# 7 class ControllerProviderFactory(BaseProviderFactory):
# 8     config_model = ControllerProviderConfig
# 9     base_class = ControllerProviderBase
# 10 
# 11     @classmethod
# 12     def create(
# 13         cls,
# 14         id: int,
# 15         *,
# 16         context: RunContext,
# 17         **kwargs
# 18     ) -> ControllerProviderBase:
# 19         from app.factories.context_provider_factory import ContextProviderFactory
# 20         from app.factories.score_provider_factory import ScoreProviderFactory
# 21         from app.factories.tool_provider_factory import ToolProviderFactory
# 22         from app.factories.system_provider_factory import SystemProviderFactory
# 23 
# 24         preload_instance = super().create(id, context=context)
# 25         components = preload_instance._components
# 26         provider_id = preload_instance._config.id
# 27         provider_type = preload_instance._provider_type
# 28 
# 29         #  ─── Child context ─────────────────────────────────────────────
# 30         child_context = RunContext(
# 31             called_by_type=provider_type,
# 32             called_by_id=provider_id,
# 33             session_id=context.session_id,
# 34             file_log_id=context.file_log_id,
# 35             parent_id=preload_instance._run_id,
# 36             execution_chain=context.execution_chain.copy()
# 37         )
# 38 
# 39         #  ─── Subproviders ──────────────────────────────────────────────
# 40         context_provider = (
# 41             ContextProviderFactory.create(
# 42                 components["context_provider_id"],
# 43                 context=child_context
# 44             ) if components.get("context_provider_id") else None
# 45         )
# 46 
# 47         score_provider = (
# 48             ScoreProviderFactory.create(
# 49                 components["score_provider_id"],
# 50                 context=child_context
# 51             ) if components.get("score_provider_id") else None
# 52         )
# 53 
# 54         tool_providers = [
# 55             ToolProviderFactory.create(
# 56                 tid,
# 57                 context=child_context
# 58             )
# 59             for tid in (components.get("tool_provider_ids") or {}).values()
# 60         ]
# 61 
# 62         system_providers = {
# 63             name: SystemProviderFactory.create(
# 64                 sys_id,
# 65                 context=child_context
# 66             )
# 67             for name, sys_id in (components.get("systems") or {}).items()
# 68         }
# 69 
# 70         #  ─── Final instantiation ───────────────────────────────────────
# 71         cls_type = type(preload_instance)
# 72         return cls_type(
# 73             config=preload_instance._config,
# 74             context_provider=context_provider,
# 75             score_provider=score_provider,
# 76             tool_providers=tool_providers,
# 77             system_providers=system_providers,
# 78             context=context
# 79         )


# factories\program_provider_factory.py

# 1 from app.factories.base_provider_factory import BaseProviderFactory
# 2 from app.db.models import ProgramProviderConfig
# 3 from app.providers.program_provider_base import ProgramProviderBase
# 4 from app.enums.logging_enums import RunContext
# 5 
# 6 
# 7 class ProgramProviderFactory(BaseProviderFactory):
# 8     config_model = ProgramProviderConfig
# 9     base_class = ProgramProviderBase
# 10 
# 11     @classmethod
# 12     def create(
# 13         cls,
# 14         id: int,
# 15         *,
# 16         context: RunContext,
# 17         **kwargs
# 18     ) -> ProgramProviderBase:
# 19         from app.factories.context_provider_factory import ContextProviderFactory
# 20         from app.factories.score_provider_factory import ScoreProviderFactory
# 21         from app.factories.tool_provider_factory import ToolProviderFactory
# 22         from app.factories.controller_provider_factory import ControllerProviderFactory
# 23 
# 24         preload_instance = super().create(id, context=context)
# 25         components = preload_instance._components  #  structured access injected by BaseProviderFactory
# 26         provider_id = preload_instance._config.id
# 27         provider_type = preload_instance._provider_type
# 28 
# 29         #  ─── Child context ─────────────────────────────────────────────
# 30         child_context = RunContext(
# 31             called_by_type=provider_type,
# 32             called_by_id=provider_id,
# 33             session_id=context.session_id,
# 34             file_log_id=context.file_log_id,
# 35             parent_id=preload_instance._run_id,
# 36             execution_chain=context.execution_chain.copy()
# 37         )
# 38 
# 39         #  ─── Subproviders ──────────────────────────────────────────────
# 40         context_provider = (
# 41             ContextProviderFactory.create(
# 42                 components["context_provider_id"],
# 43                 context=child_context
# 44             ) if components.get("context_provider_id") else None
# 45         )
# 46 
# 47         score_provider = (
# 48             ScoreProviderFactory.create(
# 49                 components["score_provider_id"],
# 50                 context=child_context
# 51             ) if components.get("score_provider_id") else None
# 52         )
# 53 
# 54         tool_providers = [
# 55             ToolProviderFactory.create(
# 56                 tid,
# 57                 context=child_context
# 58             )
# 59             for tid in (components.get("tool_provider_ids") or {}).values()
# 60         ]
# 61 
# 62         controller_providers = {
# 63             name: ControllerProviderFactory.create(
# 64                 controller_id,
# 65                 context=child_context
# 66             )
# 67             for name, controller_id in (components.get("controllers") or {}).items()
# 68         }
# 69 
# 70         #  ─── Final instantiation ───────────────────────────────────────
# 71         cls_type = type(preload_instance)
# 72         instance = cls_type(
# 73             config=preload_instance._config,
# 74             context_provider=context_provider,
# 75             score_provider=score_provider,
# 76             tool_providers=tool_providers,
# 77             controller_providers=controller_providers,
# 78             context=context
# 79         )
# 80 
# 81         return instance


# factories\prompt_provider_factory.py

# 1 from app.db.models import PromptProviderConfig
# 2 from app.factories.base_provider_factory import BaseProviderFactory
# 3 from app.providers.prompt_provider_base import PromptProviderBase
# 4 from app.factories.context_provider_factory import ContextProviderFactory
# 5 from app.enums.logging_enums import RunContext
# 6 from pathlib import Path
# 7 
# 8 
# 9 class PromptProviderFactory(BaseProviderFactory):
# 10     config_model = PromptProviderConfig
# 11     base_class = PromptProviderBase
# 12 
# 13     @classmethod
# 14     def create(
# 15         cls,
# 16         id: int,
# 17         *,
# 18         context: RunContext,
# 19         **kwargs
# 20     ) -> PromptProviderBase:
# 21         preload_instance = super().create(id, context=context)
# 22         components = preload_instance._components  #  injected by BaseProviderFactory
# 23         provider_id = preload_instance._config.id
# 24         provider_type = preload_instance._provider_type
# 25 
# 26         #  ─── Child context ─────────────────────────────────────────────
# 27         child_context = RunContext(
# 28             called_by_type=provider_type,
# 29             called_by_id=provider_id,
# 30             session_id=context.session_id,
# 31             file_log_id=context.file_log_id,
# 32             parent_id=preload_instance._run_id,
# 33             execution_chain=context.execution_chain.copy()
# 34         )
# 35 
# 36         #  ─── Prompt loading ────────────────────────────────────────────
# 37         def load_prompt(prompt_id: int, table: str) -> str | None:
# 38             if not prompt_id:
# 39                 return None
# 40             conn = preload_instance._engine.raw_connection()
# 41             try:
# 42                 cur = conn.cursor()
# 43                 cur.execute(f"SELECT artifact_path FROM {table} WHERE id = ?", (prompt_id,))
# 44                 row = cur.fetchone()
# 45                 if row:
# 46                     artifact_path = row[0]
# 47                     if artifact_path:
# 48                         return (Path("extensions") / artifact_path).read_text(encoding="utf-8").strip()
# 49             finally:
# 50                 conn.close()
# 51             return None
# 52 
# 53         agent_prompt_text = load_prompt(components.get("agent_prompt_id"), "agent_prompt")
# 54         system_prompt_text = load_prompt(components.get("system_prompt_id"), "system_prompt")
# 55 
# 56         context_provider = None
# 57         if components.get("context_provider_id"):
# 58             context_provider = ContextProviderFactory.create(
# 59                 components["context_provider_id"],
# 60                 context=child_context
# 61             )
# 62 
# 63         cls_type = type(preload_instance)
# 64         return cls_type(
# 65             config=preload_instance._config,
# 66             agent_text=agent_prompt_text,
# 67             system_text=system_prompt_text,
# 68             context_provider=context_provider,
# 69             context=context
# 70         )


# factories\score_provider_factory.py

# 1 from app.factories.base_provider_factory import BaseProviderFactory
# 2 from app.db.models import ScoreProviderConfig
# 3 from app.providers.score_provider_base import ScoreProviderBase
# 4 from app.enums.logging_enums import RunContext
# 5 
# 6 
# 7 class ScoreProviderFactory(BaseProviderFactory):
# 8     config_model = ScoreProviderConfig
# 9     base_class = ScoreProviderBase
# 10 
# 11     @classmethod
# 12     def create(
# 13         cls,
# 14         id: int,
# 15         *,
# 16         context: RunContext,
# 17         **kwargs
# 18     ) -> ScoreProviderBase:
# 19         from app.factories.tool_provider_factory import ToolProviderFactory
# 20         from app.factories.context_provider_factory import ContextProviderFactory
# 21 
# 22         preload_instance = super().create(id, context=context)
# 23         components = preload_instance._components  #  provided by BaseProviderFactory
# 24         provider_id = preload_instance._config.id
# 25         provider_type = preload_instance._provider_type
# 26 
# 27         #  ─── Child context ─────────────────────────────────────────────
# 28         child_context = RunContext(
# 29             called_by_type=provider_type,
# 30             called_by_id=provider_id,
# 31             session_id=context.session_id,
# 32             file_log_id=context.file_log_id,
# 33             parent_id=preload_instance._run_id,
# 34             execution_chain=context.execution_chain.copy()
# 35         )
# 36 
# 37         if (ctx_id := components.get("context_provider_id")):
# 38             context_provider = ContextProviderFactory.create(
# 39                 ctx_id,
# 40                 context=child_context
# 41             )
# 42             preload_instance.set_context_provider(context_provider)
# 43             if hasattr(context_provider, "set_score_provider"):
# 44                 context_provider.set_score_provider(preload_instance)
# 45 
# 46         for _, tool_id in (components.get("tool_provider_ids") or {}).items():
# 47             tool = ToolProviderFactory.create(
# 48                 tool_id,
# 49                 context=child_context
# 50             )
# 51             preload_instance.set_tool_provider(tool)
# 52 
# 53         return preload_instance


# factories\state_provider_factory.py

# 1 from app.factories.base_provider_factory import BaseProviderFactory
# 2 from app.db.models import StateProviderConfig
# 3 from app.providers.state_provider_base import StateProviderBase
# 4 from app.enums.logging_enums import RunContext
# 5 
# 6 
# 7 class StateProviderFactory(BaseProviderFactory):
# 8     config_model = StateProviderConfig
# 9     base_class = StateProviderBase
# 10 
# 11     @classmethod
# 12     def create(
# 13         cls,
# 14         id: int,
# 15         *,
# 16         context: RunContext,
# 17         **kwargs
# 18     ) -> StateProviderBase:
# 19         from app.factories.agent_provider_factory import AgentProviderFactory
# 20         from app.factories.context_provider_factory import ContextProviderFactory
# 21         from app.factories.score_provider_factory import ScoreProviderFactory
# 22         from app.factories.tool_provider_factory import ToolProviderFactory
# 23 
# 24         preload_instance = super().create(id, context=context)
# 25         components = preload_instance._components  #  structured config
# 26         provider_id = preload_instance._config.id
# 27         provider_type = preload_instance._provider_type
# 28 
# 29         #  ─── Child context ─────────────────────────────────────────────
# 30         child_context = RunContext(
# 31             called_by_type=provider_type,
# 32             called_by_id=provider_id,
# 33             session_id=context.session_id,
# 34             file_log_id=context.file_log_id,
# 35             parent_id=preload_instance._run_id,
# 36             execution_chain=context.execution_chain.copy()
# 37         )
# 38 
# 39         #  ─── Subproviders ──────────────────────────────────────────────
# 40         agent_providers = {
# 41             name: AgentProviderFactory.create(
# 42                 agent_id,
# 43                 context=child_context
# 44             )
# 45             for name, agent_id in components.get("agents", {}).items()
# 46         }
# 47 
# 48         context_provider = (
# 49             ContextProviderFactory.create(
# 50                 components["context_provider_id"],
# 51                 context=child_context
# 52             )
# 53             if components.get("context_provider_id") else None
# 54         )
# 55 
# 56         score_provider = (
# 57             ScoreProviderFactory.create(
# 58                 components["score_provider_id"],
# 59                 context=child_context
# 60             )
# 61             if components.get("score_provider_id") else None
# 62         )
# 63 
# 64         tool_providers = [
# 65             ToolProviderFactory.create(
# 66                 tool_id,
# 67                 context=child_context
# 68             )
# 69             for _, tool_id in (components.get("tool_provider_ids") or {}).items()
# 70         ]
# 71 
# 72         #  ─── Final instantiation ───────────────────────────────────────
# 73         cls_type = type(preload_instance)
# 74         return cls_type(
# 75             config=preload_instance._config,
# 76             agent_providers=agent_providers,
# 77             context_provider=context_provider,
# 78             score_provider=score_provider,
# 79             tool_providers=tool_providers,
# 80             context=context
# 81         )


# factories\system_provider_factory.py

# 1 from app.factories.base_provider_factory import BaseProviderFactory
# 2 from app.db.models import SystemProviderConfig
# 3 from app.providers.system_provider_base import SystemProviderBase
# 4 from app.enums.logging_enums import RunContext
# 5 
# 6 
# 7 class SystemProviderFactory(BaseProviderFactory):
# 8     config_model = SystemProviderConfig
# 9     base_class = SystemProviderBase
# 10 
# 11     @classmethod
# 12     def create(
# 13         cls,
# 14         id: int,
# 15         *,
# 16         context: RunContext,
# 17         **kwargs
# 18     ) -> SystemProviderBase:
# 19         #  ⏱️ Delayed imports to avoid circular dependencies
# 20         from app.factories.state_provider_factory import StateProviderFactory
# 21         from app.factories.context_provider_factory import ContextProviderFactory
# 22         from app.factories.score_provider_factory import ScoreProviderFactory
# 23         from app.factories.tool_provider_factory import ToolProviderFactory
# 24 
# 25         preload_instance = super().create(id, context=context)
# 26         components = preload_instance._components
# 27         provider_id = preload_instance._config.id
# 28         provider_type = preload_instance._provider_type
# 29 
# 30         #  ─── Child context ─────────────────────────────────────────────
# 31         child_context = RunContext(
# 32             called_by_type=provider_type,
# 33             called_by_id=provider_id,
# 34             session_id=context.session_id,
# 35             file_log_id=context.file_log_id,
# 36             parent_id=preload_instance._run_id,
# 37             execution_chain=context.execution_chain.copy()
# 38         )
# 39 
# 40         #  ─── Subproviders ──────────────────────────────────────────────
# 41         context_provider = (
# 42             ContextProviderFactory.create(
# 43                 components["context_provider_id"],
# 44                 context=child_context
# 45             ) if components.get("context_provider_id") else None
# 46         )
# 47 
# 48         score_provider = (
# 49             ScoreProviderFactory.create(
# 50                 components["score_provider_id"],
# 51                 context=child_context
# 52             ) if components.get("score_provider_id") else None
# 53         )
# 54 
# 55         tool_providers = [
# 56             ToolProviderFactory.create(
# 57                 tool_id,
# 58                 context=child_context
# 59             )
# 60             for _, tool_id in (components.get("tool_provider_ids") or {}).items()
# 61         ]
# 62 
# 63         state_providers = {
# 64             name: StateProviderFactory.create(
# 65                 state_id,
# 66                 context=child_context
# 67             )
# 68             for name, state_id in (components.get("states") or {}).items()
# 69         }
# 70 
# 71         #  ─── Final instantiation ───────────────────────────────────────
# 72         cls_type = type(preload_instance)
# 73         return cls_type(
# 74             config=preload_instance._config,
# 75             context_provider=context_provider,
# 76             score_provider=score_provider,
# 77             tool_providers=tool_providers,
# 78             state_providers=state_providers,
# 79             context=context
# 80         )


# factories\tool_provider_factory.py

# 1 from app.factories.base_provider_factory import BaseProviderFactory
# 2 from app.db.models import ToolProviderConfig
# 3 from app.providers.tool_provider_base import ToolProviderBase
# 4 from app.enums.logging_enums import RunContext
# 5 
# 6 
# 7 class ToolProviderFactory(BaseProviderFactory):
# 8     config_model = ToolProviderConfig
# 9     base_class = ToolProviderBase
# 10 
# 11     @classmethod
# 12     def create(
# 13         cls,
# 14         id: int,
# 15         *,
# 16         context: RunContext,
# 17         **kwargs
# 18     ) -> ToolProviderBase:
# 19         from app.factories.context_provider_factory import ContextProviderFactory
# 20         from app.factories.score_provider_factory import ScoreProviderFactory
# 21 
# 22         preload_instance = super().create(id, context=context)
# 23         components = preload_instance._components
# 24         provider_id = preload_instance._config.id
# 25         provider_type = preload_instance._provider_type
# 26 
# 27         #  ─── Child context ─────────────────────────────────────────────
# 28         child_context = RunContext(
# 29             called_by_type=provider_type,
# 30             called_by_id=provider_id,
# 31             session_id=context.session_id,
# 32             file_log_id=context.file_log_id,
# 33             parent_id=preload_instance._run_id,
# 34             execution_chain=context.execution_chain.copy()
# 35         )
# 36 
# 37         if (ctx_id := components.get("context_provider_id")):
# 38             context_provider = ContextProviderFactory.create(
# 39                 ctx_id,
# 40                 context=child_context
# 41             )
# 42             preload_instance.set_context_provider(context_provider)
# 43             if hasattr(context_provider, "set_tool_provider"):
# 44                 context_provider.set_tool_provider(preload_instance)
# 45 
# 46         if (score_id := components.get("score_provider_id")):
# 47             if isinstance(score_id, int) and score_id > 0:
# 48                 score_provider = ScoreProviderFactory.create(
# 49                     score_id,
# 50                     context=child_context
# 51                 )
# 52                 preload_instance.set_score_provider(score_provider)
# 53                 if hasattr(score_provider, "set_tool_provider"):
# 54                     score_provider.set_tool_provider(preload_instance)
# 55 
# 56         return preload_instance


# providers\__init__.py



# providers\agent_engine_provider_base.py

# 1 from __future__ import annotations
# 2 from abc import abstractmethod
# 3 from pathlib import Path
# 4 from app.db.schemas import AgentEngineOutput
# 5 from app.enums.logging_enums import RunContext
# 6 from app.enums.agent_enums import AGENT
# 7 from app.providers.base_provider import BaseProvider
# 8 
# 9 
# 10 class AgentEngineProviderBase(BaseProvider):
# 11     def __init__(
# 12         self,
# 13         config=None,
# 14         prompt_provider=None,
# 15         context_provider=None,
# 16         score_provider=None,
# 17         tool_providers=None,
# 18         context: RunContext = None,
# 19         **kwargs
# 20     ):
# 21         super().__init__(config=config, context=context, **kwargs)
# 22         self.prompt_provider = prompt_provider
# 23         self.context_provider = context_provider
# 24         self.score_provider = score_provider
# 25         self.tool_providers = tool_providers or []
# 26 
# 27     def _run_provider(self, input: dict) -> AgentEngineOutput:
# 28 
# 29         output = self._run(input=input, context=self.fork_context())
# 30 
# 31         if not isinstance(output, AgentEngineOutput):
# 32             raise ValueError("Expected AgentEngineOutput from _run")
# 33 
# 34         if not output.content or not output.decision or not output.log:
# 35             self._log.error("❌ Extraction failed: missing one or more required fields in AgentEngineOutput")
# 36             raise ValueError("Extraction failed: missing content or decision or log entry")
# 37 
# 38         return output
# 39 
# 40 
# 41     @abstractmethod
# 42     def _run(self, input: dict, context: RunContext | None = None) -> AgentEngineOutput:
# 43         raise NotImplementedError


# providers\agent_provider_base.py

# 1 from copy import deepcopy
# 2 from datetime import datetime, timezone
# 3 from pathlib import Path
# 4 from abc import abstractmethod
# 5 from sqlalchemy.orm import Session
# 6 
# 7 from app.db.schemas import AgentConversationLogSchema, AgentOutputSchema
# 8 from app.enums.logging_enums import LOG_TYPE, RunContext
# 9 from app.enums.agent_enums import AGENT
# 10 from app.providers.base_provider import BaseProvider
# 11 
# 12 
# 13 class AgentProviderBase(BaseProvider):
# 14     def __init__(
# 15         self,
# 16         config=None,
# 17         agent_engine=None,
# 18         prompt_provider=None,
# 19         context_provider=None,
# 20         score_provider=None,
# 21         tool_providers=None,
# 22         context: RunContext = None,
# 23         **kwargs
# 24     ):
# 25         super().__init__(config=config, context=context, **kwargs)
# 26         self._agent_engine = agent_engine
# 27         self._prompt_provider = prompt_provider
# 28         self._context_provider = context_provider
# 29         self._score_provider = score_provider
# 30         self._tool_providers = tool_providers or []
# 31 
# 32     def _run_provider(self, input: dict) -> AgentOutputSchema:
# 33         context = self.fork_context()
# 34         output = self._run(input=input, context=context)
# 35         if not hasattr(output, "score") or output.score is None:
# 36             output.score = -1.0
# 37 
# 38         self._log.debug(f"\U0001f9fb _run_provider called for: {self._config.name if self._config else 'unknown'}")
# 39 
# 40         #  Log conversation
# 41         log_content = getattr(output, "log", None)
# 42         if log_content:
# 43             self.logger.write(LOG_TYPE.AGENT_CONVERSATION, AgentConversationLogSchema(
# 44                 session_id=self._session_id,
# 45                 file_log_id=self._file_log_id,
# 46                 agent_type=self._config.agent_type if hasattr(self._config, "agent_type") else AGENT.UNKNOWN,
# 47                 agent_provider_config_id=self._config.id if self._config else -1,
# 48                 content=log_content,
# 49                 timestamp=datetime.now(timezone.utc),
# 50             ))
# 51             self._log.debug("✅ AGENT_CONVERSATION log write complete")
# 52 
# 53         output.agent_type = self._config.agent_type if hasattr(self._config, "agent_type") else AGENT.UNKNOWN
# 54         return output
# 55 
# 56     @abstractmethod
# 57     def _run(self, input: dict, context: RunContext | None = None) -> AgentOutputSchema:
# 58         raise NotImplementedError


# providers\base_provider.py

# 1 from abc import ABC, abstractmethod
# 2 from copy import deepcopy
# 3 from datetime import datetime, timezone
# 4 import hashlib
# 5 import json
# 6 import logging
# 7 import time
# 8 from pathlib import Path
# 9 from typing import Dict, Optional
# 10 from uuid import uuid4
# 11 
# 12 from app.db import init_db
# 13 from app.utilities.metadata.logging.logging_provider import LoggingMixin, LOG_TYPE, LoggingProvider
# 14 from app.db.schemas import ProviderLogSchema, ErrorLogSchema
# 15 from app.enums.logging_enums import PROVIDER_TYPE, ERROR_TYPE, RunContext
# 16 
# 17 DEFAULT_MAX_RETRIES = 2
# 18 DEFAULT_BACKOFF_SECONDS = 0.25
# 19 
# 20 class BaseProvider(ABC):
# 21     def __init__(
# 22         self,
# 23         config=None,
# 24         context: RunContext = None,
# 25         **kwargs
# 26     ):
# 27         self._config = config
# 28         self._context = deepcopy(context) if context else None
# 29         self._run_id = str(uuid4())
# 30         self._engine = init_db(reset=False)
# 31         self.logger = LoggingProvider()
# 32         self._log = logging.getLogger(self.__class__.__name__)
# 33         self._provider_type = getattr(config, "provider_type", PROVIDER_TYPE.UNKNOWN)
# 34 
# 35         if self._context:
# 36             if not self._context.execution_chain or self._context.execution_chain[-1] != self._run_id:
# 37                 self._context.parent_id = self._context.execution_chain[-1] if self._context.execution_chain else None
# 38                 self._context.execution_chain.append(self._run_id)
# 39             self._session_id = self._context.session_id
# 40             self._file_log_id = self._context.file_log_id
# 41             self._called_by_type = self._context.called_by_type
# 42             self._called_by_id = self._context.called_by_id
# 43         else:
# 44             self._session_id = None
# 45             self._file_log_id = None
# 46             self._called_by_type = None
# 47             self._called_by_id = None
# 48 
# 49     def run(self, input: dict | None = None, context: RunContext = None) -> str:
# 50         input = input or {}
# 51         max_retries = input.get("max_retries", DEFAULT_MAX_RETRIES)
# 52         backoff_seconds = input.get("backoff_seconds", DEFAULT_BACKOFF_SECONDS)
# 53 
# 54         if context:
# 55             self._context = deepcopy(context)
# 56             self._session_id     = self._context.session_id
# 57             self._file_log_id    = self._context.file_log_id
# 58             self._called_by_type = self._context.called_by_type
# 59             self._called_by_id   = self._context.called_by_id
# 60 
# 61         for attempt in range(max_retries + 1):
# 62             self._run_id = str(uuid4())
# 63 
# 64             if self._context:
# 65                 if not self._context.execution_chain or self._context.execution_chain[-1] != self._run_id:
# 66                     self._context.parent_id = self._context.execution_chain[-1] if self._context.execution_chain else None
# 67                     self._context.execution_chain.append(self._run_id)
# 68 
# 69             start_time = datetime.now(timezone.utc)
# 70             start_clock = time.perf_counter()
# 71 
# 72             try:
# 73                 output = self._run_provider(input)
# 74                 break
# 75             except Exception as exc:
# 76                 latency_ms = int((time.perf_counter() - start_clock) * 1000)
# 77 
# 78                 #  🔴 Error Log
# 79                 try:
# 80                     self.logger.write(
# 81                         LOG_TYPE.ERROR,
# 82                         ErrorLogSchema(
# 83                             session_id=self._session_id,
# 84                             file_log_id=self._file_log_id,
# 85                             error_type=self._map_error_type(exc).value,
# 86                             message=str(exc),
# 87                             file_path=str(Path(__file__).relative_to(Path.cwd())),
# 88                             provider_id=self._config.id if self._config else None,
# 89                             provider_type=self._provider_type,
# 90                             timestamp=start_time,
# 91                             latency_ms=latency_ms,
# 92                             called_by_type=self._called_by_type if self._called_by_type else None,
# 93                             called_by_id=self._called_by_id,
# 94                             run_id=self._run_id,
# 95                             parent_id=self._context.parent_id if self._context else None,
# 96                             execution_chain=self._context.execution_chain[:] if self._context else [],
# 97                         ),
# 98                     )
# 99                 except Exception:
# 100                     pass
# 101 
# 102                 #  📜 Provider Log for failure (explicit marker)
# 103                 try:
# 104                     self.logger.write(
# 105                         LOG_TYPE.PROVIDER,
# 106                         ProviderLogSchema(
# 107                             session_id=self._session_id,
# 108                             file_log_id=self._file_log_id,
# 109                             provider_id=self._config.id if self._config else -1,
# 110                             provider_type=self._provider_type,
# 111                             input=json.dumps(input, default=str),
# 112                             output="ERROR",  #  🚨 Clear indicator
# 113                             output_schema=None,
# 114                             latency_ms=latency_ms,
# 115                             config_hash=self._compute_config_hash(getattr(self._config, "config", {})),
# 116                             file_name=getattr(self._config, "artifact_path", "").split("/")[-1]
# 117                             if getattr(self._config, "artifact_path", None)
# 118                             else None,
# 119                             timestamp=start_time,
# 120                             called_by_type=self._called_by_type if self._called_by_type else None,
# 121                             called_by_id=self._called_by_id,
# 122                             run_id=self._run_id,
# 123                             parent_id=self._context.parent_id if self._context else None,
# 124                             execution_chain=self._context.execution_chain[:] if self._context else [],
# 125                         ),
# 126                     )
# 127                 except Exception:
# 128                     pass
# 129 
# 130                 if attempt == max_retries:
# 131                     raise
# 132 
# 133                 time.sleep(backoff_seconds * (2 ** attempt))
# 134                 input["retry_count"] = attempt + 1
# 135 
# 136         latency_ms = int((time.perf_counter() - start_clock) * 1000)
# 137 
# 138         try:
# 139             input_str = json.dumps(
# 140                 {k: v.model_dump() if hasattr(v, "model_dump") else v for k, v in input.items()},
# 141                 default=str,
# 142             )
# 143         except Exception:
# 144             input_str = ""
# 145 
# 146         try:
# 147             if hasattr(output, "model_dump"):
# 148                 dumped = output.model_dump()
# 149                 output_str = json.dumps(dumped, default=str)
# 150                 output_schema = output.__class__.__name__
# 151             else:
# 152                 output_str = json.dumps(output, default=str)
# 153                 output_schema = None
# 154         except Exception:
# 155             output_str = ""
# 156             output_schema = None
# 157 
# 158         try:
# 159 
# 160             self.logger.write(
# 161                 LOG_TYPE.PROVIDER,
# 162                 ProviderLogSchema(
# 163                     session_id=self._session_id,
# 164                     file_log_id=self._file_log_id,
# 165                     provider_id=self._config.id if self._config else -1,
# 166                     provider_type=self._provider_type,
# 167                     input=input_str,
# 168                     output=output_str,
# 169                     output_schema=output_schema,
# 170                     latency_ms=latency_ms,
# 171                     config_hash=self._compute_config_hash(getattr(self._config, "config", {})),
# 172                     file_name=getattr(self._config, "artifact_path", "").split("/")[-1]
# 173                     if getattr(self._config, "artifact_path", None)
# 174                     else None,
# 175                     timestamp=start_time,
# 176                     called_by_type=self._called_by_type if self._called_by_type else None,
# 177                     called_by_id=self._called_by_id,
# 178                     run_id=self._run_id,
# 179                     parent_id=self._context.parent_id if self._context else None,
# 180                     execution_chain=self._context.execution_chain[:] if self._context else [],
# 181                 ),
# 182             )
# 183         except Exception as e:
# 184             print(f"❌ Logging failed: {e}")
# 185             raise
# 186 
# 187         self._log.debug("✅ Provider run logged")
# 188         return output
# 189 
# 190     def _map_error_type(self, exc: Exception) -> ERROR_TYPE:
# 191         msg = str(exc).lower()
# 192         if "timeout" in msg or "timed out" in msg:
# 193             return ERROR_TYPE.TIMEOUT
# 194         elif "auth" in msg or "unauthorized" in msg or "forbidden" in msg:
# 195             return ERROR_TYPE.AUTHENTICATION
# 196         elif "connect" in msg or "unreachable" in msg or "network" in msg:
# 197             return ERROR_TYPE.CONNECTION
# 198         elif "config" in msg or "invalid argument" in msg:
# 199             return ERROR_TYPE.CONFIGURATION
# 200         elif "validation" in msg or "schema" in msg:
# 201             return ERROR_TYPE.VALIDATION
# 202         elif "runtime" in msg or "execution failed" in msg:
# 203             return ERROR_TYPE.RUNTIME
# 204         return ERROR_TYPE.UNKNOWN
# 205 
# 206     def _compute_config_hash(self, config: dict | None) -> str:
# 207         """
# 208         Computes a fingerprint hash based solely on structural components.
# 209         Ignores param keys like weights or thresholds.
# 210         """
# 211         config = config or {}
# 212         components = config.get("components", {})
# 213 
# 214         config_str = json.dumps(components, sort_keys=True)
# 215         return hashlib.md5(config_str.encode("utf-8")).hexdigest()
# 216 
# 217     def fork_context(self) -> RunContext:
# 218         forked = deepcopy(self._context or RunContext())
# 219         forked.parent_id = self._run_id
# 220         forked.called_by_type = self._provider_type
# 221         forked.called_by_id = getattr(self._config, "id", None)
# 222         return forked


# providers\context_provider_base.py

# 1 from __future__ import annotations
# 2 
# 3 from abc import abstractmethod
# 4 
# 5 from app.db.schemas import ContextOutputSchema
# 6 from app.enums.logging_enums import RunContext
# 7 from app.providers.base_provider import BaseProvider
# 8 
# 9 from typing import TYPE_CHECKING
# 10 if TYPE_CHECKING:
# 11     from app.providers.score_provider_base import ScoreProviderBase
# 12     from app.providers.tool_provider_base import ToolProviderBase
# 13 
# 14 class ContextProviderBase(BaseProvider):
# 15     def __init__(self, config=None, context: RunContext = None, **kwargs):
# 16         super().__init__(config=config, context=context, **kwargs)
# 17 
# 18     def _run_provider(self, input: dict) -> ContextOutputSchema:
# 19         return self._run(input=input, context=self.fork_context())
# 20     
# 21     def set_tool_provider(self, provider: "ToolProviderBase") -> None:
# 22         if not hasattr(self, "tool_providers"):
# 23             self.tool_providers = []
# 24         self.tool_providers.append(provider)
# 25 
# 26     def set_score_provider(self, provider: "ScoreProviderBase") -> None:
# 27         self.score_provider = provider
# 28 
# 29 
# 30     @abstractmethod
# 31     def _run(self, input: dict, context: RunContext | None = None) -> ContextOutputSchema:
# 32         raise NotImplementedError


# providers\controller_provider_base.py

# 1 from __future__ import annotations
# 2 from abc import abstractmethod
# 3 from copy import deepcopy
# 4 from datetime import datetime
# 5 from pathlib import Path
# 6 import shutil
# 7 from typing import Dict
# 8 
# 9 from app.enums.fsm_enums import DECISION_TYPE, STATE_TYPE
# 10 from app.enums.logging_enums import RunContext
# 11 from app.enums.system_enums import SYSTEM
# 12 from app.providers.fsm_provider_base import FSMProviderBase
# 13 from app.db.schemas import ControllerOutputSchema
# 14 from app.utilities.extract_base_filename import extract_base_filename
# 15 from app.utilities.select_best_file_by_score import select_best_file_by_score
# 16 
# 17 
# 18 class ControllerProviderBase(FSMProviderBase):
# 19     def __init__(
# 20         self,
# 21         config=None,
# 22         system_providers=None,
# 23         context_provider=None,
# 24         score_provider=None,
# 25         tool_providers=None,
# 26         context: RunContext = None,
# 27         **kwargs
# 28     ):
# 29         super().__init__(config=config, context=context, **kwargs)
# 30         self._systems = system_providers or {}
# 31         self.context_provider = context_provider
# 32         self.score_provider = score_provider
# 33         self.tool_providers = tool_providers or []
# 34         self._generated_files = []
# 35 
# 36     def _run_provider(self, input: dict) -> ControllerOutputSchema:
# 37         session_id = input.get("session_id")
# 38 
# 39         incoming_file = input.get("file_path") or input.get("file_name") or input.get("before")
# 40         if not incoming_file:
# 41             raise ValueError("❌ ControllerProvider requires 'file_path', 'file_name', or 'before' in input")
# 42 
# 43         self.incoming_file = incoming_file
# 44         src = Path(incoming_file).resolve()
# 45         timestamp = datetime.now().strftime('%H%M%S%f')[:10]
# 46         working_dir = Path("working_files").resolve()
# 47         working_dir.mkdir(parents=True, exist_ok=True)
# 48         root = extract_base_filename(src)
# 49         self.working_file = working_dir / f"{root}__ctrl_{timestamp}{src.suffix}"
# 50         shutil.copy(src, self.working_file)
# 51         self._generated_files.append(self.working_file)
# 52 
# 53         input_path = Path(incoming_file)
# 54         try:
# 55             relative_path = str(input_path.relative_to(Path.cwd()))
# 56         except ValueError:
# 57             relative_path = str(input_path)
# 58 
# 59         state = {
# 60             "state": SYSTEM.START,
# 61             "file_path": str(self.working_file),
# 62             "session_id": session_id,
# 63             "reason": input.get("reason", SYSTEM.START.value),
# 64             "steps": input.get("steps", 0),
# 65             "retry_count": input.get("retry_count", 0),
# 66             "_last_state": input.get("_last_state", SYSTEM.START),
# 67             "decision": DECISION_TYPE.UNKNOWN,
# 68             "original_file": relative_path,
# 69             "run_id": self._run_id,
# 70         }
# 71 
# 72         step_count = 0
# 73 
# 74         while True:
# 75             current = state.get("state")
# 76 
# 77             if step_count >= self._max_steps:
# 78                 transition = self.transition(state, output)
# 79                 state.update({
# 80                     **transition,
# 81                     "state": SYSTEM.END,
# 82                     "state_type": STATE_TYPE.END,
# 83                     "reason": f"Max steps ({self._max_steps}) reached"
# 84                 })
# 85                 return ControllerOutputSchema(
# 86                     state=SYSTEM.END,
# 87                     previous_state=current,
# 88                     state_type=SYSTEM.END,
# 89                     decision=DECISION_TYPE.REJECTED,
# 90                     steps=state.get("steps", step_count),
# 91                     max_steps=self._max_steps,
# 92                     summary=f"Max steps ({self._max_steps}) reached",
# 93                     output=state,
# 94                     provider_name=self._config.name,
# 95                 )
# 96 
# 97             if current == SYSTEM.END:
# 98                 best_file = select_best_file_by_score(
# 99                     file_a=state["file_path"],
# 100                     file_b=self.incoming_file,
# 101                     score_provider=self.score_provider,
# 102                     context=self._context
# 103                 )
# 104 
# 105                 temp_path = Path("working_files") / f"temp_ctrl_{datetime.now().strftime('%H%M%S%f')[:10]}.py"
# 106                 shutil.copy(Path(best_file), temp_path)
# 107                 state["file_path"] = str(temp_path)
# 108 
# 109                 for f in Path("working_files").glob("temp_ctrl_*.py"):
# 110                     if f.resolve() != temp_path.resolve():
# 111                         try:
# 112                             f.unlink()
# 113                         except Exception:
# 114                             pass
# 115 
# 116                 for f in Path("working_files").glob("*_stripped.py"):
# 117                     try:
# 118                         f.unlink()
# 119                     except Exception:
# 120                         pass
# 121 
# 122                 for path in self._generated_files:
# 123                     if path.exists():
# 124                         try:
# 125                             path.unlink()
# 126                         except Exception:
# 127                             pass
# 128 
# 129                 return ControllerOutputSchema(
# 130                     state=SYSTEM.END,
# 131                     previous_state=state.get("_last_state", SYSTEM.START),
# 132                     state_type=STATE_TYPE.END,
# 133                     decision=state.get("decision", DECISION_TYPE.UNKNOWN),
# 134                     steps=state.get("steps", step_count),
# 135                     max_steps=self._max_steps,
# 136                     summary=state.get("summary", "Completed"),
# 137                     output=state,
# 138                     provider_name=self._config.name,
# 139                 )
# 140 
# 141             step_count += 1
# 142 
# 143             if current == SYSTEM.START:
# 144                 transition = self.transition(state, None)
# 145                 state.update(transition)
# 146                 state["_last_state"] = SYSTEM.START
# 147                 continue
# 148 
# 149             provider = self._systems.get(current.value)
# 150             if not provider:
# 151                 raise ValueError(f"No system provider registered for state: {current.value}")
# 152 
# 153             provider_input = {k: v for k, v in state.items() if k != "state"}
# 154             output = provider.run(input=provider_input, context=self.fork_context())
# 155 
# 156             flat_output = output.model_dump(exclude={"output"}) if hasattr(output, "model_dump") else dict(output)
# 157             promoted_path = Path("working_files") / f"temp_ctrl_{datetime.now().strftime('%H%M%S%f')[:10]}.py"
# 158             if "file_path" in output.output:
# 159                 final_path = Path(output.output["file_path"]).resolve()
# 160                 shutil.copy(final_path, promoted_path)
# 161                 self._generated_files.append(promoted_path)
# 162                 state["file_path"] = str(promoted_path)
# 163 
# 164             if hasattr(output, "decision") and output.decision is not None:
# 165                 state["decision"] = output.decision
# 166 
# 167             transition_result = self.transition(state, output)
# 168             transition_result.pop("file_path", None)
# 169 
# 170             transition_metadata = transition_result.get("transition_metadata", {}) or {}
# 171             transition_metadata.update({
# 172                 "agent_output": flat_output.get("output", {}),
# 173                 "file_path": flat_output.get("file_path"),
# 174             })
# 175             transition_result["transition_metadata"] = transition_metadata
# 176 
# 177             raw_state = transition_result.get("state", current)
# 178             state_enum = raw_state if isinstance(raw_state, SYSTEM) else SYSTEM(raw_state)
# 179 
# 180             state = {
# 181                 **state,
# 182                 **transition_result,
# 183                 "state": state_enum,
# 184                 "_last_state": current,
# 185                 "state_output": flat_output,
# 186             }
# 187 
# 188 
# 189     @abstractmethod
# 190     def _transition(self, state: dict, output: dict | None) -> dict:
# 191         ...


# providers\fsm_provider_base.py

# 1 from __future__ import annotations
# 2 
# 3 from abc import abstractmethod
# 4 from datetime import datetime, timezone
# 5 from typing import Optional
# 6 
# 7 from app.enums.fsm_enums import TRANSITION_REASON_TYPE, DECISION_TYPE
# 8 from app.enums.logging_enums import LOG_TYPE, PROVIDER_TYPE, RunContext
# 9 from app.db.schemas import StateTransitionLogSchema
# 10 from app.providers.base_provider import BaseProvider
# 11 
# 12 
# 13 class FSMProviderBase(BaseProvider):
# 14     """
# 15     Base class for FSM-enabled providers (Program, Controller, System, State).
# 16     Provides a shared `transition` method and logging of state transitions.
# 17     """
# 18 
# 19     def __init__(
# 20         self,
# 21         config=None,
# 22         context: RunContext = None,
# 23         **kwargs
# 24     ) -> None:
# 25         super().__init__(
# 26             config=config,
# 27             context=context,
# 28             **kwargs
# 29         )
# 30 
# 31         #  🔄 Structured config access (Task 9)
# 32         raw_config = getattr(config, "config", {}) or {}
# 33         self._params = raw_config.get("params", {})  #  fallback safe
# 34         self._components = raw_config.get("components", {})
# 35 
# 36         self._max_steps = self._params.get("max_steps", 100)
# 37 
# 38     def transition(self, state: dict, result: dict | None) -> dict:
# 39         next_state = self._transition(state, result)
# 40 
# 41         required_keys = {"state", "reason"}
# 42         missing_keys = required_keys - next_state.keys()
# 43         if missing_keys:
# 44             raise ValueError(f"❌ Transition result missing keys: {missing_keys}\nReturned: {next_state}")
# 45         state["steps"] = state.get("steps", 0) + 1
# 46 
# 47         #  Ensure original_file is carried forward
# 48         original_file = state.get("original_file")
# 49 
# 50         #  Initialize or update transition_metadata
# 51         transition_metadata = next_state.get("transition_metadata", {}) or {}
# 52         transition_metadata["original_file"] = original_file
# 53 
# 54         #  Optional: Add run_id to the metadata as well
# 55         transition_metadata["run_id"] = state.get("run_id")
# 56 
# 57         #  Optional: agent_output propagation (if result has a dumpable output)
# 58         if isinstance(result, dict):
# 59             transition_metadata["agent_output"] = result.get("output")
# 60             transition_metadata["file_path"] = result.get("file_path")
# 61 
# 62         next_state["transition_metadata"] = transition_metadata
# 63 
# 64         is_terminal = next_state.get("state_type") == "end" or next_state.get("state") == "end"
# 65 
# 66         decision = getattr(result, "decision", DECISION_TYPE.UNKNOWN)
# 67         #  Log the state transition with relevant details
# 68         self.logger.write(
# 69             LOG_TYPE.STATE_TRANSITION,
# 70             StateTransitionLogSchema(
# 71                 session_id=self._session_id,
# 72                 file_log_id=self._file_log_id, 
# 73                 entity_type=self._provider_type,
# 74                 entity_id=self._config.id,
# 75                 from_state=state.get("state"),
# 76                 to_state=next_state.get("state", "unknown"),
# 77                 reason=TRANSITION_REASON_TYPE(next_state["reason"]),
# 78                 decision=decision,
# 79                 triggered_by=self._config.name,
# 80                 step=state["steps"],
# 81                 transition_metadata=transition_metadata,
# 82                 timestamp=datetime.now(timezone.utc),
# 83                 run_id=self._run_id,
# 84                 called_by_type=self._called_by_type if self._called_by_type else None,
# 85                 called_by_id=self._called_by_id,
# 86                 parent_id=self._context.parent_id if self._context else None,
# 87                 execution_chain=self._context.execution_chain[:] if self._context else [],
# 88             ),
# 89         )
# 90         return next_state
# 91 
# 92     @abstractmethod
# 93     def _transition(self, state: dict, result: dict | None) -> dict:
# 94         raise NotImplementedError


# providers\program_provider_base.py

# 1 from __future__ import annotations
# 2 from abc import abstractmethod
# 3 from copy import deepcopy
# 4 from datetime import datetime
# 5 from pathlib import Path
# 6 import shutil
# 7 from typing import Dict
# 8 
# 9 from app.enums.controller_enums import CONTROLLER
# 10 from app.enums.fsm_enums import STATE_TYPE, DECISION_TYPE
# 11 from app.enums.logging_enums import RunContext, LOG_TYPE
# 12 from app.providers.fsm_provider_base import FSMProviderBase
# 13 from app.db.schemas import ProgramOutputSchema, FileLogSchema
# 14 from app.utilities.extract_base_filename import extract_base_filename
# 15 from app.utilities.select_best_file_by_score import select_best_file_by_score
# 16 from app.utilities.metadata.logging.logging_provider import LoggingProvider
# 17 
# 18 class ProgramProviderBase(FSMProviderBase):
# 19     def __init__(
# 20         self,
# 21         config=None,
# 22         controller_providers: Dict[str, object] = None,
# 23         context_provider=None,
# 24         score_provider=None,
# 25         tool_providers=None,
# 26         context: RunContext = None,
# 27     ):
# 28         super().__init__(
# 29             config=config,
# 30             context=context
# 31         )
# 32         self._states = controller_providers or {}
# 33         self.context_provider = context_provider
# 34         self.score_provider = score_provider
# 35         self.tool_providers = tool_providers or []
# 36         self._session_id = context.session_id
# 37         self._file_log_id = context.file_log_id
# 38         self._generated_files: list[Path] = []
# 39 
# 40     def _run_provider(self, input: dict) -> ProgramOutputSchema:
# 41         session_id = self._session_id
# 42 
# 43         incoming_file = input.get("file_path")
# 44         self.incoming_file = incoming_file
# 45 
# 46         output_path = input.get("output_path", "working_files")
# 47 
# 48         src = Path(incoming_file).resolve()
# 49         root = extract_base_filename(src)
# 50         timestamp = datetime.now().strftime('%H%M%S%f')[:10]
# 51         working_dir = Path("working_files").resolve()
# 52         working_dir.mkdir(parents=True, exist_ok=True)
# 53         self.working_file = working_dir / f"{root}.__prog_{timestamp}{src.suffix}"
# 54         shutil.copy(src, self.working_file)
# 55         self._generated_files.append(self.working_file)
# 56 
# 57         #  🔹 Log file and get file_log_id
# 58         original_path = str(src)
# 59         file_name = src.name
# 60         length_bytes = src.stat().st_size
# 61 
# 62         file_log = FileLogSchema(
# 63             session_id=session_id,
# 64             file_name=file_name,
# 65             original_path=original_path,
# 66             length_bytes=length_bytes
# 67         )
# 68         self._file_log_id = LoggingProvider().write(LOG_TYPE.FILE, file_log)
# 69         self._context.file_log_id = self._file_log_id
# 70         self.context = self._context
# 71 
# 72         input_path = Path(incoming_file)
# 73         try:
# 74             relative_path = str(input_path.relative_to(Path.cwd()))
# 75         except ValueError:
# 76             relative_path = str(input_path)
# 77 
# 78         #  🔹 Build initial FSM state
# 79         state = {
# 80             "state": CONTROLLER.START,
# 81             "file_path": str(self.working_file),
# 82             "session_id": session_id,
# 83             "file_log_id": self._file_log_id,
# 84             "reason": input.get("reason", CONTROLLER.START.value),
# 85             "steps": input.get("steps", 0),
# 86             "retry_count": input.get("retry_count", 0),
# 87             "_last_state": input.get("_last_state", CONTROLLER.START),
# 88             "decision": DECISION_TYPE.UNKNOWN,
# 89             "original_file": relative_path,
# 90             "run_id": self._run_id,
# 91         }
# 92 
# 93         step_count = 0
# 94 
# 95         while True:
# 96             current = state.get("state")
# 97 
# 98             if step_count >= self._max_steps:
# 99                 transition = self.transition(state, output)
# 100                 state.update({
# 101                     **transition,
# 102                     "state": CONTROLLER.END,
# 103                     "state_type": STATE_TYPE.END,
# 104                     "reason": f"Max steps ({self._max_steps}) reached"
# 105                 })
# 106                 return ProgramOutputSchema(
# 107                     state=CONTROLLER.END,
# 108                     previous_state=current,
# 109                     state_type=STATE_TYPE.END,
# 110                     decision=DECISION_TYPE.UNKNOWN,
# 111                     steps=state.get("steps", step_count),
# 112                     max_steps=self._max_steps,
# 113                     summary=f"Max steps ({self._max_steps}) reached",
# 114                     output=state,
# 115                     provider_name=self._config.name
# 116                 )
# 117 
# 118             if current == CONTROLLER.END:
# 119                 final_decision = state.get("decision")
# 120                 nested = state.get("output") or {}
# 121                 if isinstance(nested, dict):
# 122                     final_decision = final_decision or nested.get("decision")
# 123 
# 124                 best_file = select_best_file_by_score(
# 125                     file_a=state.get("file_path"),
# 126                     file_b=self.incoming_file,
# 127                     score_provider=self.score_provider,
# 128                     context=self._context
# 129                 )
# 130 
# 131                 final_path = Path(output_path) / Path(self.incoming_file).name
# 132                 shutil.copy(Path(best_file).resolve(), final_path)
# 133                 state["file_path"] = str(final_path)
# 134                 state["decision"] = final_decision
# 135 
# 136                 for f in Path("working_files").glob("temp_*.py"):
# 137                     if f.resolve() != final_path.resolve():
# 138                         try:
# 139                             f.unlink()
# 140                         except Exception:
# 141                             pass
# 142 
# 143                 for f in Path("working_files").glob("*_stripped.py"):
# 144                     try:
# 145                         f.unlink()
# 146                     except Exception:
# 147                         pass
# 148 
# 149                 for path in self._generated_files:
# 150                     if path.exists():
# 151                         try:
# 152                             path.unlink()
# 153                         except Exception:
# 154                             pass
# 155 
# 156                 return ProgramOutputSchema(
# 157                     state=CONTROLLER.END,
# 158                     previous_state=state.get("_last_state", CONTROLLER.START),
# 159                     state_type=STATE_TYPE.END,
# 160                     decision=final_decision or state.get("decision", DECISION_TYPE.UNKNOWN),
# 161                     steps=state.get("steps", step_count),
# 162                     max_steps=self._max_steps,
# 163                     summary=state.get("reason", "Completed"),
# 164                     output=state,
# 165                     provider_name=self._config.name
# 166                 )
# 167 
# 168             step_count += 1
# 169 
# 170             if current == CONTROLLER.START:
# 171                 transition = self.transition(state, None)
# 172                 state.update(transition)
# 173                 state["_last_state"] = CONTROLLER.START
# 174                 continue
# 175 
# 176             provider = self._states.get(current)
# 177             if not provider:
# 178                 raise ValueError(f"No controller provider registered for state: {current}")
# 179 
# 180             provider_input = {k: v for k, v in state.items() if k != "state"}
# 181             output = provider.run(input=provider_input, context=self.fork_context())
# 182 
# 183             flat_output = output.model_dump(exclude={"output"}) if hasattr(output, "model_dump") else dict(output)
# 184             promoted_path = Path("working_files") / f"temp_prog_{datetime.now().strftime('%H%M%S%f')[:10]}.py"
# 185             if "file_path" in output.output:
# 186                 src_path = Path(output.output["file_path"]).resolve()
# 187                 shutil.copy(src_path, promoted_path)
# 188                 self._generated_files.append(promoted_path)
# 189                 state["file_path"] = str(promoted_path)
# 190 
# 191             transition_result = self.transition(state, output)
# 192             transition_result.pop("file_path", None)
# 193 
# 194             transition_metadata = transition_result.get("transition_metadata", {}) or {}
# 195             transition_metadata.update({
# 196                 "agent_output": flat_output.get("output", {}),
# 197                 "file_path": flat_output.get("file_path"),
# 198             })
# 199             transition_result["transition_metadata"] = transition_metadata
# 200 
# 201             state = {
# 202                 **state,
# 203                 **transition_result,
# 204                 "state": CONTROLLER(transition_result.get("state", current)),
# 205                 "_last_state": current,
# 206                 "state_output": flat_output,
# 207             }
# 208 
# 209     @abstractmethod
# 210     def _transition(self, state: dict, output: dict | None) -> dict:
# 211         ...


# providers\prompt_provider_base.py

# 1 from __future__ import annotations
# 2 from abc import abstractmethod
# 3 from copy import deepcopy
# 4 from sqlalchemy.orm import Session
# 5 from pathlib import Path
# 6 
# 7 from app.db.models import AgentPrompt, SystemPrompt
# 8 from app.db.schemas import AgentEngineExtraction, PromptOutputSchema
# 9 from app.enums.logging_enums import RunContext
# 10 from app.providers.base_provider import BaseProvider
# 11 from app.factories.context_provider_factory import ContextProviderFactory
# 12 
# 13 
# 14 class PromptProviderBase(BaseProvider):
# 15     def __init__(
# 16         self,
# 17         config=None,
# 18         agent_text=None,
# 19         system_text=None,
# 20         context_provider=None,
# 21         context: RunContext = None,
# 22         **kwargs
# 23     ):
# 24         super().__init__(config=config, context=context, **kwargs)
# 25         self.agent_text = agent_text
# 26         self.system_text = system_text
# 27         self._context_provider = context_provider
# 28 
# 29     def _run_provider(self, input: dict) -> PromptOutputSchema:
# 30         result = self._run(input=input, context=self.fork_context())
# 31         return result
# 32 
# 33     @abstractmethod
# 34     def _run(self, input: dict, context: RunContext | None = None) -> PromptOutputSchema:
# 35         raise NotImplementedError
# 36     
# 37     @abstractmethod
# 38     def _extract(self, response: str) -> AgentEngineExtraction:
# 39         raise NotImplementedError


# providers\score_provider_base.py

# 1 from __future__ import annotations
# 2 from abc import abstractmethod
# 3 from copy import deepcopy
# 4 from typing import TYPE_CHECKING, Type, Optional
# 5 
# 6 from app.enums.logging_enums import RunContext
# 7 from app.providers.base_provider import BaseProvider
# 8 from app.db.schemas import ScoreOutputSchema, ScoreComponentsBase, ScoreProviderConfig
# 9 
# 10 if TYPE_CHECKING:
# 11     from app.providers.context_provider_base import ContextProviderBase
# 12     from app.providers.tool_provider_base import ToolProviderBase
# 13 
# 14 
# 15 class ScoreProviderBase(BaseProvider):
# 16     ConfigSchema: Type[ScoreComponentsBase] = ScoreComponentsBase  #  ⬅️ override in subclasses
# 17 
# 18     def __init__(
# 19         self,
# 20         config=None,
# 21         context_provider=None,
# 22         tool_providers=None,
# 23         context: RunContext = None,
# 24         **kwargs
# 25     ):
# 26         super().__init__(config=config, context=context, **kwargs)
# 27         self._context_provider = context_provider
# 28         self._tool_providers = tool_providers or []
# 29 
# 30         #  ✅ Only pass "params" (schema-enforced tunables) to Pydantic
# 31         raw_params = (getattr(config, "config", {}) or {}).get("params", {})
# 32         self.config: ScoreComponentsBase = self._parse_config(raw_params)
# 33 
# 34     def _parse_config(self, raw_config: dict | ScoreProviderConfig) -> ScoreComponentsBase:
# 35         if hasattr(raw_config, "config"):
# 36             raw_config = raw_config.config  #  unwrap SQLAlchemy wrapper
# 37         return self.ConfigSchema.model_validate(raw_config)
# 38 
# 39     def _run_provider(self, input: dict) -> ScoreOutputSchema:
# 40         caller = self._context.called_by_type.name if self._context and self._context.called_by_type else "UNKNOWN"
# 41         return self._run(input=input, context=self.fork_context())
# 42 
# 43     def set_context_provider(self, provider: ContextProviderBase) -> None:
# 44         self.context_provider = provider
# 45 
# 46     def set_tool_provider(self, provider: ToolProviderBase) -> None:
# 47         if not hasattr(self, "tool_providers"):
# 48             self.tool_providers = []
# 49         self.tool_providers.append(provider)
# 50 
# 51     @abstractmethod
# 52     def _run(self, input: dict, context: RunContext | None = None) -> ScoreOutputSchema:
# 53         raise NotImplementedError


# providers\state_provider_base.py

# 1 from __future__ import annotations
# 2 from abc import abstractmethod
# 3 from copy import deepcopy
# 4 from datetime import datetime
# 5 from pathlib import Path
# 6 import shutil
# 7 from typing import Dict
# 8 
# 9 from app.enums.fsm_enums import STATE_TYPE, DECISION_TYPE, TRANSITION_REASON_TYPE
# 10 from app.enums.logging_enums import RunContext
# 11 from app.enums.state_enums import STATE
# 12 from app.enums.agent_enums import AGENT
# 13 from app.providers.fsm_provider_base import FSMProviderBase
# 14 from app.db.schemas import StateOutputSchema
# 15 from app.utilities.extract_base_filename import extract_base_filename
# 16 from app.utilities.select_best_file_by_score import select_best_file_by_score
# 17 
# 18 
# 19 class StateProviderBase(FSMProviderBase):
# 20     def __init__(
# 21         self,
# 22         config=None,
# 23         agent_providers=None,
# 24         context_provider=None,
# 25         score_provider=None,
# 26         tool_providers=None,
# 27         context: RunContext = None,
# 28         **kwargs
# 29     ):
# 30         super().__init__(config=config, context=context, **kwargs)
# 31         self._agents = agent_providers or {}
# 32         self.context_provider = context_provider
# 33         self.score_provider = score_provider
# 34         self.tool_providers = tool_providers or []
# 35         self._generated_files = []
# 36 
# 37     def _run_provider(self, input: dict) -> StateOutputSchema:
# 38         session_id = input.get("session_id")
# 39 
# 40         incoming_file = input.get("file_path") or input.get("file_name") or input.get("before")
# 41         if not incoming_file:
# 42             raise ValueError("❌ StateProvider requires 'file_path', 'file_name', or 'before' in input")
# 43 
# 44         self.incoming_file = incoming_file
# 45         src = Path(incoming_file).resolve()
# 46         timestamp = datetime.now().strftime('%H%M%S%f')[:10]
# 47         working_dir = Path("working_files").resolve()
# 48         working_dir.mkdir(parents=True, exist_ok=True)
# 49         root = extract_base_filename(src)
# 50         self.working_file = working_dir / f"{root}__state_{timestamp}{src.suffix}"
# 51         shutil.copy(src, self.working_file)
# 52         self._generated_files.append(self.working_file)
# 53 
# 54         input_path = Path(incoming_file)
# 55         try:
# 56             relative_path = str(input_path.relative_to(Path.cwd()))
# 57         except ValueError:
# 58             relative_path = str(input_path)
# 59 
# 60         state = {
# 61             "state": AGENT.START,
# 62             "file_path": str(self.working_file),
# 63             "session_id": session_id,
# 64             "reason": input.get("reason", AGENT.START.value),
# 65             "steps": input.get("steps", 0),
# 66             "retry_count": input.get("retry_count", 0),
# 67             "_last_state": input.get("_last_state", AGENT.START),
# 68             "decision": DECISION_TYPE.UNKNOWN,
# 69             "original_file": relative_path,
# 70             "run_id": self._run_id,
# 71         }
# 72 
# 73         step_count = 0
# 74 
# 75         while True:
# 76             current = state["state"]
# 77 
# 78             if step_count >= self._max_steps:
# 79                 transition = self.transition(state, None)
# 80                 state.update({
# 81                     **transition,
# 82                     "state": STATE.END,
# 83                     "state_type": STATE_TYPE.END,
# 84                     "reason": TRANSITION_REASON_TYPE.UNSUCCESSFUL
# 85                 })
# 86                 return StateOutputSchema(
# 87                     state=AGENT.END,
# 88                     previous_state=current,
# 89                     state_type=STATE_TYPE.END,
# 90                     decision=DECISION_TYPE.REJECTED,
# 91                     steps=state.get("steps", step_count),
# 92                     max_steps=self._max_steps,
# 93                     summary=f"Max steps ({self._max_steps}) reached",
# 94                     output=state,
# 95                     provider_name=self._config.name,
# 96                 )
# 97 
# 98             if current == STATE.END:
# 99                 if self.score_provider:
# 100                     best_file = select_best_file_by_score(
# 101                         file_a=state["file_path"],
# 102                         file_b=self.incoming_file,
# 103                         score_provider=self.score_provider,
# 104                         context=self._context
# 105                     )
# 106 
# 107                     temp_path = Path("working_files") / f"temp_state_{datetime.now().strftime('%H%M%S%f')[:10]}.py"
# 108                     shutil.copy(Path(best_file), temp_path)
# 109                     state["file_path"] = str(temp_path)
# 110 
# 111                     for f in Path("working_files").glob("temp_state_*.py"):
# 112                         if f.resolve() != temp_path.resolve():
# 113                             try:
# 114                                 f.unlink()
# 115                             except Exception:
# 116                                 pass
# 117 
# 118                     for f in Path("working_files").glob("temp_agent_*.py"):
# 119                         if f.resolve() != temp_path.resolve():
# 120                             try:
# 121                                 f.unlink()
# 122                             except Exception:
# 123                                 pass
# 124 
# 125                     for f in Path("working_files").glob("*_stripped.py"):
# 126                         try:
# 127                             f.unlink()
# 128                         except Exception:
# 129                             pass
# 130 
# 131                     ctx = self._context or RunContext()
# 132 
# 133                 for path in self._generated_files:
# 134                     if path.exists():
# 135                         try:
# 136                             path.unlink()
# 137                         except Exception:
# 138                             pass
# 139 
# 140                 return StateOutputSchema(
# 141                     state=AGENT.END,
# 142                     previous_state=state.get("_last_state", AGENT.START),
# 143                     state_type=STATE_TYPE.END,
# 144                     decision=state.get("decision", DECISION_TYPE.UNKNOWN),
# 145                     steps=state.get("steps", step_count),
# 146                     max_steps=self._max_steps,
# 147                     summary=state.get("summary", "Completed"),
# 148                     output=state,
# 149                     provider_name=self._config.name,
# 150                 )
# 151 
# 152             step_count += 1
# 153 
# 154             if current == AGENT.START:
# 155                 transition = self.transition(state, None)
# 156                 state.update(transition)
# 157                 state["_last_state"] = AGENT.START
# 158                 continue
# 159 
# 160             provider = self._agents.get(current.value)
# 161 
# 162             provider_input = {k: v for k, v in state.items() if k != "state"}
# 163             forked = self.fork_context()
# 164 
# 165             output = provider.run(input=provider_input, context=forked)
# 166 
# 167             transition_result = self.transition(state, output)
# 168 
# 169             flat_output = output.model_dump(exclude={"output"}) if hasattr(output, "model_dump") else dict(output or {})
# 170             agent_file_path = flat_output.get("file_path")
# 171 
# 172             if agent_file_path:
# 173                 src_path = Path(agent_file_path).resolve()
# 174                 if src_path.exists():
# 175                     promoted_path = Path("working_files") / f"temp_state_{datetime.now().strftime('%H%M%S%f')[:10]}.py"
# 176                     shutil.copy(src_path, promoted_path)
# 177                     self._generated_files.append(promoted_path)
# 178                     state["file_path"] = str(promoted_path)
# 179 
# 180             if hasattr(output, "decision") and output.decision:
# 181                 state["decision"] = output.decision
# 182 
# 183             transition_metadata = transition_result.get("transition_metadata", {}) or {}
# 184             transition_metadata.update({
# 185                 "agent_output": flat_output.get("output", {}),
# 186                 "file_path": flat_output.get("file_path"),
# 187             })
# 188             transition_result["transition_metadata"] = transition_metadata
# 189 
# 190             raw_state = transition_result.get("state", current)
# 191             state_enum = raw_state if isinstance(raw_state, AGENT) else AGENT(raw_state)
# 192 
# 193             transition_result.pop("file_path", None)
# 194 
# 195             state = {
# 196                 **state,
# 197                 **transition_result,
# 198                 "state": state_enum,
# 199                 "_last_state": current,
# 200                 "state_output": flat_output,
# 201             }
# 202 
# 203     @abstractmethod
# 204     def _transition(self, state: dict, output: dict | None) -> dict:
# 205         ...


# providers\system_provider_base.py

# 1 from __future__ import annotations
# 2 from abc import abstractmethod
# 3 from copy import deepcopy
# 4 from datetime import datetime
# 5 from pathlib import Path
# 6 import shutil
# 7 from typing import Dict
# 8 
# 9 from app.enums.fsm_enums import STATE_TYPE, DECISION_TYPE, TRANSITION_REASON_TYPE
# 10 from app.enums.logging_enums import RunContext
# 11 from app.enums.state_enums import STATE
# 12 from app.enums.system_enums import SYSTEM
# 13 from app.providers.fsm_provider_base import FSMProviderBase
# 14 from app.db.schemas import FSMOutputSchema, SystemOutputSchema
# 15 from app.utilities.extract_base_filename import extract_base_filename
# 16 from app.utilities.select_best_file_by_score import select_best_file_by_score
# 17 
# 18 
# 19 class SystemProviderBase(FSMProviderBase):
# 20     def __init__(
# 21         self,
# 22         config=None,
# 23         state_providers=None,
# 24         context_provider=None,
# 25         score_provider=None,
# 26         tool_providers=None,
# 27         context: RunContext = None,
# 28         **kwargs
# 29     ):
# 30         super().__init__(config=config, context=context, **kwargs)
# 31         self._states= state_providers or {}
# 32         self.context_provider = context_provider
# 33         self.score_provider = score_provider
# 34         self.tool_providers = tool_providers or []
# 35         self._generated_files = []
# 36 
# 37     def _run_provider(self, input: dict) -> SystemOutputSchema:
# 38         session_id = input.get("session_id")
# 39 
# 40         incoming_file = input.get("file_path") or input.get("file_name") or input.get("before")
# 41         if not incoming_file:
# 42             raise ValueError("❌ SystemProvider requires 'file_path', 'file_name', or 'before' in input")
# 43 
# 44         self.incoming_file = incoming_file
# 45         src = Path(incoming_file).resolve()
# 46         timestamp = datetime.now().strftime('%H%M%S%f')[:10]
# 47         working_dir = Path("working_files").resolve()
# 48         working_dir.mkdir(parents=True, exist_ok=True)
# 49         root = extract_base_filename(src)
# 50         self.working_file = working_dir / f"{root}.__sys_{timestamp}{src.suffix}"
# 51         shutil.copy(src, self.working_file)
# 52         self._generated_files.append(self.working_file)
# 53 
# 54         input_path = Path(incoming_file)
# 55         try:
# 56             relative_path = str(input_path.relative_to(Path.cwd()))
# 57         except ValueError:
# 58             relative_path = str(input_path)
# 59 
# 60         state = {
# 61             "state": STATE.START,
# 62             "file_path": str(self.working_file),
# 63             "session_id": session_id,
# 64             "reason": input.get("reason", STATE.START.value),
# 65             "steps": input.get("steps", 0),
# 66             "retry_count": input.get("retry_count", 0),
# 67             "_last_state": input.get("_last_state", STATE.START),
# 68             "decision": DECISION_TYPE.UNKNOWN,
# 69             "original_file": relative_path,
# 70             "run_id": self._run_id,
# 71         }
# 72 
# 73         step_count = 0
# 74 
# 75         while True:
# 76             current = state["state"]
# 77 
# 78             if step_count >= self._max_steps:
# 79                 transition = self.transition(state, output)
# 80                 state.update({
# 81                     **transition,
# 82                     "state": STATE.END,
# 83                     "state_type": STATE_TYPE.END,
# 84                     "reason": TRANSITION_REASON_TYPE.UNSUCCESSFUL
# 85                 })
# 86                 return SystemOutputSchema(
# 87                     state=STATE.END,
# 88                     previous_state=current,
# 89                     state_type=STATE_TYPE.END,
# 90                     decision=DECISION_TYPE.REJECTED,
# 91                     steps=state.get("steps", step_count),
# 92                     max_steps=self._max_steps,
# 93                     summary=f"Max steps ({self._max_steps}) reached",
# 94                     output=state,
# 95                     provider_name=self._config.name
# 96                 )
# 97 
# 98             if current == STATE.END:
# 99                 transition = self.transition(state, output)
# 100                 state.update(transition)
# 101 
# 102                 best_file = select_best_file_by_score(
# 103                     file_a=state["file_path"],
# 104                     file_b=self.incoming_file,
# 105                     score_provider=self.score_provider,
# 106                     context=self._context
# 107                 )
# 108 
# 109                 temp_path = Path("working_files") / f"temp_sys_{datetime.now().strftime('%H%M%S%f')[:10]}.py"
# 110                 shutil.copy(Path(best_file), temp_path)
# 111                 state["file_path"] = str(temp_path)
# 112 
# 113                 #  Clean up old temporary files
# 114                 for f in Path("working_files").glob("temp_sys_*.py"):
# 115                     if f.resolve() != temp_path.resolve():
# 116                         try:
# 117                             f.unlink()
# 118                         except Exception:
# 119                             pass
# 120 
# 121                 for f in Path("working_files").glob("*_stripped.py"):
# 122                     try:
# 123                         f.unlink()
# 124                     except Exception:
# 125                         pass
# 126 
# 127                 for path in self._generated_files:
# 128                     if path.exists():
# 129                         try:
# 130                             path.unlink()
# 131                         except Exception:
# 132                             pass
# 133 
# 134                 return SystemOutputSchema(
# 135                     state=STATE.END,
# 136                     previous_state=state.get("_last_state", STATE.START),
# 137                     state_type=STATE_TYPE.END,
# 138                     decision=state.get("decision", DECISION_TYPE.UNKNOWN),
# 139                     steps=state.get("steps", step_count),
# 140                     max_steps=self._max_steps,
# 141                     summary=state.get("summary", "Completed"),
# 142                     output=state,
# 143                     provider_name=self._config.name
# 144                 )
# 145 
# 146             step_count += 1
# 147 
# 148             if current == STATE.START:
# 149                 transition = self.transition(state, None)
# 150                 state.update(transition)
# 151                 state["_last_state"] = STATE.START
# 152                 continue
# 153 
# 154             provider = self._states.get(current.value)
# 155             if not provider:
# 156                 raise ValueError(f"No state provider registered for state: {current.value}")
# 157 
# 158             provider_input = {k: v for k, v in state.items() if k != "state"}
# 159             output = provider.run(input=provider_input, context=self.fork_context())
# 160 
# 161 
# 162             new_file_path = output.output.get("file_path")
# 163             if new_file_path:
# 164                 state["file_path"] = str(new_file_path)
# 165 
# 166             if hasattr(output, "decision") and output.decision:
# 167                 state["decision"] = output.decision
# 168 
# 169             transition_result = self.transition(state, output)
# 170             flat_output = output.model_dump(exclude={"output"}) if hasattr(output, "model_dump") else dict(output)
# 171 
# 172             transition_metadata = transition_result.get("transition_metadata", {}) or {}
# 173             transition_metadata.update({
# 174                 "agent_output": flat_output.get("output", {}),
# 175                 "file_path": flat_output.get("file_path"),
# 176             })
# 177             transition_result["transition_metadata"] = transition_metadata
# 178 
# 179             raw_state = transition_result.get("state", current)
# 180             state_enum = raw_state if isinstance(raw_state, STATE) else STATE(raw_state)
# 181 
# 182             transition_result.pop("file_path", None)
# 183 
# 184             state = {
# 185                 **state,
# 186                 **transition_result,
# 187                 "state": state_enum,
# 188                 "_last_state": current,
# 189                 "state_output": flat_output,
# 190             }
# 191 
# 192 
# 193 
# 194     @abstractmethod
# 195     def _transition(self, state: dict, output: dict | None) -> dict:
# 196         ...


# providers\tool_provider_base.py

# 1 from __future__ import annotations
# 2 from abc import abstractmethod
# 3 from copy import deepcopy
# 4 from typing import TYPE_CHECKING
# 5 
# 6 from app.enums.logging_enums import RunContext
# 7 from app.providers.base_provider import BaseProvider
# 8 from app.db.schemas import ToolOutputSchema
# 9 
# 10 if TYPE_CHECKING:
# 11     from app.providers.context_provider_base import ContextProviderBase
# 12     from app.providers.score_provider_base import ScoreProviderBase
# 13 
# 14 
# 15 class ToolProviderBase(BaseProvider):
# 16     def __init__(
# 17         self,
# 18         config=None,
# 19         context_provider: ContextProviderBase | None = None,
# 20         score_provider: ScoreProviderBase | None = None,
# 21         context: RunContext = None,
# 22         **kwargs
# 23     ):
# 24         super().__init__(config=config, context=context, **kwargs)
# 25         self._context_provider = context_provider
# 26         self._score_provider = score_provider
# 27 
# 28     def _run_provider(self, input: dict) -> ToolOutputSchema:
# 29         #  ✅ Explicitly pass self._context
# 30         output = self._run(input=input, context=self.fork_context())
# 31 
# 32         if not isinstance(output, ToolOutputSchema):
# 33             raise TypeError(f"Expected ToolOutputSchema, got {type(output).__name__}")
# 34         if output.return_code is None:
# 35             raise ValueError("Tool output is missing `return_code`")
# 36         if output.summary is None:
# 37             output.summary = "⚠️ Missing summary"
# 38         if output.metrics is None:
# 39             output.metrics = {}
# 40 
# 41         return output
# 42 
# 43     def set_context_provider(self, provider: ContextProviderBase) -> None:
# 44         self.context_provider = provider
# 45 
# 46     def set_score_provider(self, provider: ScoreProviderBase) -> None:
# 47         self.score_provider = provider
# 48 
# 49     @abstractmethod
# 50     def _run(self, input: dict, context: RunContext | None = None) -> ToolOutputSchema:
# 51         raise NotImplementedError


# utilities\__init__.py



# utilities\diff_utils.py

# 1 import difflib
# 2 
# 3 def summarize_diff(before: str, after: str) -> str:
# 4     """Returns a human-readable summary of what changed between two versions."""
# 5     before_lines = before.strip().splitlines()
# 6     after_lines = after.strip().splitlines()
# 7     diff = list(difflib.unified_diff(before_lines, after_lines, lineterm=""))
# 8     if not diff:
# 9         return "No differences detected."
# 10 
# 11     summary = ["Changes:"]
# 12     for line in diff:
# 13         if line.startswith("+ ") and not line.startswith("+++"):
# 14             summary.append(f"Added: {line[2:]}")
# 15         elif line.startswith("- ") and not line.startswith("---"):
# 16             summary.append(f"Removed: {line[2:]}")
# 17 
# 18     return "\n".join(summary[:10])  #  Limit to first 10 lines


# utilities\extension_loader.py

# 1 from importlib.util import spec_from_file_location, module_from_spec
# 2 from pathlib import Path
# 3 from typing import Type
# 4 
# 5 def import_class_from_extension(artifact_path: str | Path, base_cls: Type) -> Type:
# 6     file_path = Path("extensions") / f"{artifact_path}.py"
# 7     if not file_path.exists():
# 8         raise FileNotFoundError(f"Extension file not found: {file_path}")
# 9 
# 10     spec = spec_from_file_location("dynamic_module", file_path)
# 11     if spec is None or spec.loader is None:
# 12         raise ImportError(f"Unable to load: {file_path}")
# 13 
# 14     module = module_from_spec(spec)
# 15     spec.loader.exec_module(module)
# 16 
# 17     for attr in module.__dict__.values():
# 18         if isinstance(attr, type) and issubclass(attr, base_cls) and attr is not base_cls:
# 19             return attr
# 20 
# 21     raise ImportError(f"No subclass of {base_cls.__name__} found in: {file_path}")


# utilities\extract_base_filename.py

# 1 from pathlib import Path
# 2 import re
# 3 
# 4 
# 5 def extract_base_filename(path: Path) -> str:
# 6     name = path.stem
# 7     #  Strip known suffixes like __state_12345
# 8     return re.sub(r"(__state_|__ctrl_|__sys_|__prog_)\d{10}$", "", name)


# utilities\extract_code_block.py

# 1 import re
# 2 from pydantic import BaseModel
# 3 
# 4 from app.db.schemas import AgentEngineExtraction
# 5 
# 6 
# 7 def extract_code_blocks(response: str) -> AgentEngineExtraction:
# 8     def extract_block(start: str, end: str) -> str:
# 9         match = re.search(re.escape(start) + r"(.*?)" + re.escape(end), response, re.DOTALL)
# 10         return match.group(1).strip() if match else ""
# 11 
# 12     content = extract_block("[CODE]", "[/CODE]")
# 13     decision = extract_block("[AGENT_DECISION]", "[/AGENT_DECISION]") 
# 14     log = extract_block("[CONVERSATION_LOG_ENTRY]", "[/CONVERSATION_LOG_ENTRY]")
# 15 
# 16     if not content:
# 17         raise ValueError("Extraction failed: missing content block")
# 18 
# 19     return AgentEngineExtraction(
# 20         content=content,
# 21         decision=decision or "DECISION_TYPE.UNKNOWN", 
# 22         log=log or "(no log provided)"
# 23     )


# utilities\file_management\__init__.py



# utilities\metadata\__init__.py



# utilities\metadata\footer\__init__.py



# utilities\metadata\footer\code_annnotation_utils.py

# 1 import ast
# 2 import uuid
# 3 from pathlib import Path
# 4 from typing import Tuple
# 5 
# 6 FOOTER_START = "#  --- Agent Notes"
# 7 FOOTER_END = "#  -----------------------------------------------"
# 8 
# 9 def split_content_and_notes(file_content: str) -> Tuple[str, str]:
# 10     """Return (content_with_safe_footer_removed, extracted_notes), preserving syntax."""
# 11     footer_start = None
# 12     line_sep = "\r\n" if "\r\n" in file_content else "\n"
# 13     lines = file_content.splitlines(keepends=True)
# 14 
# 15     for i, line in enumerate(lines):
# 16         if line.lstrip().startswith(FOOTER_START):
# 17             footer_start = i
# 18             break
# 19 
# 20     if footer_start is not None:
# 21         code_part = "".join(lines[:footer_start])
# 22         notes_part = "".join(lines[footer_start:])
# 23 
# 24         try:
# 25             ast.parse(code_part)
# 26             return code_part, notes_part
# 27         except SyntaxError:
# 28             safe_footer = (
# 29                 f"{line_sep}{FOOTER_START} (removed safely)\n"
# 30                 f"pass  #  [Agent notes stripped for syntax integrity]{line_sep}"
# 31                 f"{FOOTER_END}{line_sep}"
# 32             )
# 33             return code_part + safe_footer, notes_part
# 34 
# 35     return file_content, ""
# 36 
# 37 def append_agent_note(file_content: str, system: str, agent_name: str, note: str) -> str:
# 38     base, _ = split_content_and_notes(file_content)  #  strip if possible, fallback-safe
# 39 
# 40     formatted_note = (
# 41         f"{FOOTER_START} ({system} / {agent_name}) ---\n" +
# 42         "".join(f"#  {line}\n" for line in note.strip().splitlines()) +
# 43         f"{FOOTER_END}\n"
# 44     )
# 45 
# 46     return base.rstrip() + "\n\n" + formatted_note
# 47 
# 48 
# 49 #  def prepare_file_for_linting(original_path: str) -> str:
# 50 #      original = Path(original_path)
# 51 #      raw = original.read_text(encoding="utf-8")
# 52 
# 53 #      from .code_annnotation_utils import split_content_and_notes
# 54 #      code_part, _ = split_content_and_notes(raw)
# 55 
# 56 #      try:
# 57 #          ast.parse(code_part)
# 58 #      except SyntaxError:
# 59 #          #  Append safety pass to guarantee valid syntax
# 60 #          code_part += "\npass  #  [Auto-patched for linting]\n"
# 61 
# 62 #      temp_file = Path("working_files") / f"{uuid.uuid4().hex}_stripped.py"
# 63 #      temp_file.write_text(code_part.rstrip() + "\n", encoding="utf-8")
# 64 
# 65 #      return str(temp_file)
# 66 
# 67 


# utilities\metadata\logging\__init__.py



# utilities\metadata\logging\conversation_log.py

# 1 from __future__ import annotations
# 2 from typing import List
# 3 from sqlalchemy.orm import Session
# 4 from app.db.models import AgentConversationLog
# 5 
# 6 def get_conversation_log(session: Session, session_id: str, file_log_id: str) -> List[str]:
# 7     """Returns logged conversation entries for the specified session and file_log_id."""
# 8     logs = (
# 9         session.query(AgentConversationLog)
# 10         .filter_by(session_id=session_id, file_log_id=file_log_id)
# 11         .order_by(AgentConversationLog.timestamp.asc())
# 12         .all()
# 13     )
# 14 
# 15     return [log.content for log in logs if log.content]


# utilities\metadata\logging\logging_provider.py

# 1 from __future__ import annotations
# 2 
# 3 from datetime import datetime
# 4 import json
# 5 import logging
# 6 import sqlite3
# 7 from dataclasses import asdict, is_dataclass
# 8 from enum import Enum
# 9 from pathlib import Path
# 10 from typing import Any, ClassVar, Iterable
# 11 from app.db.connection import get_connection
# 12 from app.enums.logging_enums import LOG_TYPE
# 13 from app.db.schemas import (
# 14     SnapshotMetricsSchema,
# 15     StateTransitionLogSchema,
# 16     AgentConversationLogSchema,
# 17     ErrorLogSchema,
# 18     ProviderLogSchema,
# 19     FileLogSchema
# 20 )
# 21 
# 22 LOG_CONFIG_MAP = {
# 23     LOG_TYPE.STATE_TRANSITION: {
# 24         "schema": StateTransitionLogSchema,
# 25         "table": "state_transition_log",
# 26     },
# 27     LOG_TYPE.AGENT_CONVERSATION: {
# 28         "schema": AgentConversationLogSchema,
# 29         "table": "agent_conversation_log",
# 30     },
# 31     LOG_TYPE.PROVIDER: {
# 32         "schema": ProviderLogSchema,
# 33         "table": "provider_log",
# 34     },
# 35     LOG_TYPE.ERROR: {
# 36         "schema": ErrorLogSchema,
# 37         "table": "error_log",
# 38     },
# 39     LOG_TYPE.SNAPSHOT_METRICS: {
# 40         "schema": SnapshotMetricsSchema,  
# 41         "table": "snapshot_metrics",   
# 42     },
# 43     LOG_TYPE.FILE: {  
# 44         "schema": FileLogSchema,
# 45         "table": "file_log",
# 46     },
# 47 }
# 48 
# 49 class LoggingProvider:
# 50     """Centralized provider for structured logging."""
# 51 
# 52     _instance: ClassVar["LoggingProvider" | None] = None
# 53 
# 54     def __new__(cls, *args, **kwargs):
# 55         if cls._instance is None:
# 56             cls._instance = super().__new__(cls)
# 57         return cls._instance
# 58 
# 59     def __init__(
# 60         self,
# 61         db_path: str | Path = "experiments/codecritic.sqlite3",
# 62         output_path: str | Path | None = None,
# 63         connection: sqlite3.Connection | None = None,
# 64     ) -> None:
# 65         if getattr(self, "_initialized", False):
# 66             if connection is not None:
# 67                 self.conn = connection
# 68             return
# 69 
# 70         self.db_path = Path(db_path)
# 71         self.db_path.parent.mkdir(exist_ok=True, parents=True)
# 72 
# 73         self._conn = connection or get_connection()
# 74 
# 75         self.output_path = Path(output_path) if output_path else None
# 76         if self.output_path:
# 77             self.output_path.parent.mkdir(parents=True, exist_ok=True)
# 78 
# 79         self._initialized = True
# 80 
# 81     @property
# 82     def conn(self):
# 83         if getattr(self, "_conn", None) is None:
# 84             from app.db import init_db
# 85             self._conn = init_db(reset=False).raw_connection()
# 86         try:
# 87             self._conn.cursor()
# 88         except Exception:
# 89             from app.db import init_db
# 90             self._conn = init_db(reset=False).raw_connection()
# 91         return self._conn
# 92 
# 93     def _serialize(self, obj: Any) -> dict:
# 94         def _safe(v: Any) -> Any:
# 95             if isinstance(v, (Enum, Path)):
# 96                 return str(v)
# 97             elif isinstance(v, datetime):
# 98                 return v.isoformat()
# 99             elif isinstance(v, list):
# 100                 #  ✅ Serialize list directly
# 101                 return json.dumps([_safe(i) for i in v])
# 102             elif isinstance(v, dict):
# 103                 #  ✅ Serialize dict directly
# 104                 return json.dumps({k: _safe(val) for k, val in v.items()})
# 105             return v
# 106 
# 107         if not is_dataclass(obj) or isinstance(obj, type):
# 108             raise TypeError(f"Expected dataclass instance, got {type(obj)}")
# 109 
# 110         raw = asdict(obj)
# 111         return {k: _safe(v) for k, v in raw.items()}
# 112 
# 113 
# 114 
# 115     def _insert_many(self, table: str, items: Iterable[dict]) -> None:
# 116         items = list(items)
# 117         if not items:
# 118             return
# 119 
# 120         keys = list(items[0].keys())
# 121         cols = ",".join(keys)
# 122         placeholders = ",".join(["?"] * len(keys))
# 123         values = [tuple(i[k] for k in keys) for i in items]
# 124         cur = self.conn.cursor()
# 125         cur.executemany(f"INSERT INTO {table} ({cols}) VALUES ({placeholders})", values)
# 126         self.conn.commit()
# 127         if self.output_path:
# 128             with self.output_path.open("a", encoding="utf-8") as fh:
# 129                 for item in items:
# 130                     fh.write(json.dumps(item) + "\n")
# 131 
# 132     def write(self, log_type: LOG_TYPE, entries: list[Any] | Any) -> int | None:
# 133         if not isinstance(entries, list):
# 134             entries = [entries]
# 135 
# 136         config = LOG_CONFIG_MAP.get(log_type)
# 137         if config is None:
# 138             raise ValueError(f"Unsupported log type: {log_type}")
# 139 
# 140         schema_cls = config["schema"]
# 141         table_name = config["table"]
# 142 
# 143         for entry in entries:
# 144             if not is_dataclass(entry):
# 145                 raise TypeError(f"Expected a dataclass instance, got {type(entry)}")
# 146             if entry.__class__.__name__ != schema_cls.__name__:
# 147                 logging.getLogger("LoggingProvider").warning(
# 148                     f"⚠️ Log schema mismatch: expected {schema_cls.__name__}, got {entry.__class__.__name__}"
# 149                 )
# 150 
# 151         serialized = [self._serialize(e) for e in entries]
# 152         
# 153         cur = self.conn.cursor()
# 154 
# 155         if len(serialized) == 1:
# 156             keys = list(serialized[0].keys())
# 157             cols = ",".join(keys)
# 158             placeholders = ",".join(["?"] * len(keys))
# 159             values = tuple(serialized[0][k] for k in keys)
# 160 
# 161             cur.execute(f"INSERT INTO {table_name} ({cols}) VALUES ({placeholders})", values)
# 162             self.conn.commit()
# 163 
# 164             return cur.lastrowid  #  ✅ Return the inserted row ID
# 165 
# 166         #  If multiple, just bulk insert without return
# 167         self._insert_many(table_name, serialized)
# 168         return None
# 169 
# 170     def log_provider(self, log: ProviderLogSchema) -> None:
# 171         self.write(LOG_TYPE.PROVIDER, log)
# 172 
# 173     def log_state_transition(self, log: StateTransitionLogSchema) -> None:
# 174         self.write(LOG_TYPE.STATE_TRANSITION, log)
# 175 
# 176     def log_agent_conversation(self, log: AgentConversationLogSchema) -> None:
# 177         self.write(LOG_TYPE.AGENT_CONVERSATION, log)
# 178 
# 179     def log_error(self, log: ErrorLogSchema) -> None:
# 180         self.write(LOG_TYPE.ERROR, log)
# 181 
# 182     def log_snapshot_metrics(self, log: SnapshotMetricsSchema) -> None:
# 183         self.write(LOG_TYPE.SNAPSHOT_METRICS, log)
# 184     
# 185     def log_file(self, log: FileLogSchema) -> None:
# 186         self.write(LOG_TYPE.FILE, log)
# 187 
# 188     def close(self) -> None:
# 189         self.conn.close()
# 190 
# 191 
# 192 class LoggingMixin:
# 193     """Mixin to provide access to a shared LoggingProvider instance."""
# 194 
# 195     def __init__(self, logger: LoggingProvider | None = None) -> None:
# 196         self.logger = logger or LoggingProvider()
# 197         self._log = logging.getLogger(self.__class__.__name__)
# 198 
# 199     def log_provider(self, log: ProviderLogSchema) -> None:
# 200         self.logger.log_provider(log)
# 201 
# 202     def log_state_transition(self, log: StateTransitionLogSchema) -> None:
# 203         self.logger.log_state_transition(log)
# 204 
# 205     def log_agent_conversation(self, log: AgentConversationLogSchema) -> None:
# 206         self.logger.log_agent_conversation(log)
# 207 
# 208     def log_error(self, log: ErrorLogSchema) -> None:
# 209         self.logger.log_error(log)
# 210 
# 211     def log_snapshot_metrics(self, log: SnapshotMetricsSchema) -> None:
# 212         self.logger.log_snapshot_metrics(log)
# 213     
# 214     def log_file(self, log: FileLogSchema) -> None:
# 215         self.logger.log_file(log)


# utilities\metadata\snapshots\__init__.py



# utilities\metadata\snapshots\analyze_code_metrics.py

# 1 from __future__ import annotations
# 2 
# 3 import ast
# 4 import tokenize
# 5 from io import StringIO
# 6 from typing import Any, Dict
# 7 
# 8 
# 9 def analyze_code(source: str) -> Dict[str, Any]:
# 10     """Analyze Python source code and return structural metrics safely."""
# 11     metrics: Dict[str, Any] = {
# 12         #  Default all metrics to zero
# 13         "line_count": len(source.strip().splitlines()),
# 14         "function_count": 0,
# 15         "class_count": 0,
# 16         "branch_count": 0,
# 17         "call_count": 0,
# 18         "assignment_count": 0,
# 19         "return_count": 0,
# 20         "exception_count": 0,
# 21         "docstring_count": 0,
# 22         "symbol_count": 0,
# 23         "comment_count": 0,
# 24         "import_count": 0,
# 25         "analysis_failed": False,
# 26     }
# 27 
# 28     #  === AST-based metrics ===
# 29     try:
# 30         tree = ast.parse(source)
# 31         symbols = set()
# 32 
# 33         for node in ast.walk(tree):
# 34             if isinstance(node, ast.FunctionDef):
# 35                 metrics["function_count"] += 1
# 36                 symbols.add(node.name)
# 37                 if ast.get_docstring(node):
# 38                     metrics["docstring_count"] += 1
# 39             elif isinstance(node, ast.ClassDef):
# 40                 metrics["class_count"] += 1
# 41                 symbols.add(node.name)
# 42                 if ast.get_docstring(node):
# 43                     metrics["docstring_count"] += 1
# 44             elif isinstance(node, (ast.If, ast.For, ast.While, ast.Match, ast.With, ast.Try)):
# 45                 metrics["branch_count"] += 1
# 46             elif isinstance(node, ast.Call):
# 47                 metrics["call_count"] += 1
# 48             elif isinstance(node, ast.Assign):
# 49                 metrics["assignment_count"] += 1
# 50             elif isinstance(node, ast.Return):
# 51                 metrics["return_count"] += 1
# 52             elif isinstance(node, (ast.Try, ast.ExceptHandler, ast.Raise)):
# 53                 metrics["exception_count"] += 1
# 54 
# 55         metrics["symbol_count"] = len(symbols)
# 56 
# 57     except (SyntaxError, ValueError) as e:
# 58         metrics["analysis_failed"] = True
# 59         return metrics
# 60 
# 61     #  === Token-based metrics ===
# 62     try:
# 63         tokens = tokenize.generate_tokens(StringIO(source).readline)
# 64         for toknum, tokval, *_ in tokens:
# 65             if toknum == tokenize.COMMENT:
# 66                 metrics["comment_count"] += 1
# 67             elif toknum == tokenize.NAME and tokval in {"import", "from"}:
# 68                 metrics["import_count"] += 1
# 69     except tokenize.TokenError:
# 70         metrics["analysis_failed"] = True
# 71 
# 72     return metrics
# 73 
# 74 def compute_deltas(before_metrics: dict, after_metrics: dict) -> dict:
# 75     """Compute deltas between before/after metric sets."""
# 76     return {
# 77         f"{key}_delta": after_metrics.get(key, 0) - before_metrics.get(key, 0)
# 78         for key in after_metrics
# 79         if key in before_metrics
# 80     }


# utilities\metadata\snapshots\snapshot_archive.py

# 1 import json
# 2 import uuid
# 3 from datetime import datetime, timezone
# 4 from pathlib import Path
# 5 from sqlalchemy.orm import Session
# 6 from dataclasses import dataclass
# 7 
# 8 from app.db.models import SnapshotMetrics
# 9 from app.db.schemas import DECISION_TYPE, SnapshotContext
# 10 from app.enums.logging_enums import RunContext
# 11 from app.utilities.metadata.snapshots.analyze_code_metrics import analyze_code, compute_deltas
# 12 from app.utilities.metadata.footer.code_annnotation_utils import append_agent_note
# 13 
# 14 
# 15 SNAPSHOT_ROOT = Path(__file__).resolve().parents[4] / "experiments" / "snapshots"
# 16 
# 17 
# 18 class SnapshotArchive:
# 19     def __init__(self, engine, root: Path | str | None = None) -> None:
# 20         self.root = Path(root) if root else SNAPSHOT_ROOT
# 21         self.root.mkdir(parents=True, exist_ok=True)
# 22         self.engine = engine
# 23 
# 24     def record(self, *, snapshot: SnapshotContext) -> str:
# 25         if not snapshot.before_path.exists():
# 26             raise FileNotFoundError(f"Cannot snapshot non-existent file: {snapshot.before_path}")
# 27 
# 28         before_content = snapshot.before_path.read_text(encoding="utf-8")
# 29 
# 30         if not snapshot.after_content or not snapshot.decision or not snapshot.log:
# 31             raise ValueError("Extraction failed: missing content or decision or log entry")
# 32 
# 33         after_content = snapshot.after_content.rstrip() + "\n\n"
# 34         after_content = append_agent_note(after_content, system=snapshot.system, agent_name=snapshot.agent_name, note=snapshot.log)
# 35 
# 36         before_metrics = analyze_code(before_content)
# 37         after_metrics = analyze_code(after_content)
# 38         deltas = compute_deltas(before_metrics, after_metrics)
# 39 
# 40         timestamp = datetime.now(timezone.utc)
# 41         context = snapshot.context
# 42 
# 43         metadata = {
# 44             "system": snapshot.system,
# 45             "agent": snapshot.agent_name,
# 46             "state": snapshot.state,
# 47             "decision": snapshot.decision,
# 48             "timestamp": timestamp.isoformat(),
# 49             **{f"{k}_before": v for k, v in before_metrics.items()},
# 50             **{f"{k}_after": v for k, v in after_metrics.items()},
# 51             **deltas,
# 52         }
# 53 
# 54         session_root = self.root / str(context.session_id)
# 55         session_root.mkdir(parents=True, exist_ok=True)
# 56         snapshot_id = str(uuid.uuid4())
# 57 
# 58         (session_root / f"{snapshot_id}.before").write_text(before_content, encoding="utf-8")
# 59         (session_root / f"{snapshot_id}.after").write_text(after_content, encoding="utf-8")
# 60         (session_root / f"{snapshot_id}.meta.json").write_text(json.dumps(metadata, indent=2), encoding="utf-8")
# 61 
# 62         with Session(bind=self.engine) as session:
# 63             entry = SnapshotMetrics(
# 64                 session_id=context.session_id,
# 65                 file_log_id=context.file_log_id,
# 66                 snapshot_id=snapshot_id,
# 67                 system=snapshot.system,
# 68                 agent=snapshot.agent_name,
# 69                 score=None,
# 70                 state=snapshot.state,
# 71                 decision=DECISION_TYPE(snapshot.decision),
# 72                 timestamp=timestamp,
# 73                 **{
# 74                     k: metadata.get(k)
# 75                     for k in SnapshotMetrics.__table__.columns.keys()
# 76                     if k.endswith("_before") or k.endswith("_after") or k.endswith("_delta")
# 77                 }
# 78             )
# 79             session.add(entry)
# 80             session.commit()
# 81 
# 82         return snapshot_id
# 83 
# 84     def read_latest(self, session_id: str) -> dict | None:
# 85         session_root = self.root / str(session_id)
# 86         if not session_root.exists():
# 87             return None
# 88 
# 89         snapshots = sorted(session_root.glob("*.before"), key=lambda p: p.stat().st_mtime, reverse=True)
# 90         if not snapshots:
# 91             return None
# 92 
# 93         latest_id = snapshots[0].stem
# 94         before_path = session_root / f"{latest_id}.before"
# 95         after_path = session_root / f"{latest_id}.after"
# 96 
# 97         return {
# 98             "before": before_path.read_text(encoding="utf-8"),
# 99             "after": after_path.read_text(encoding="utf-8"),
# 100             "before_path": str(before_path),
# 101             "after_path": str(after_path),
# 102         }


# utilities\metadata\snapshots\snapshot_reader.py

# 1 #  from pathlib import Path
# 2 
# 3 #  SNAPSHOT_ROOT = Path(__file__).resolve().parents[4] / "experiments" / "snapshots"
# 4 
# 5 #  def read_latest_snapshot(session_id: str, root: Path | str | None = None):
# 6 #      session_root = Path(root) if root else SNAPSHOT_ROOT
# 7 #      session_root = session_root / str(session_id)
# 8     
# 9 #      if not session_root.exists():
# 10 #          return None
# 11 
# 12 #      snapshots = sorted(session_root.glob("*.before"), key=lambda p: p.stat().st_mtime, reverse=True)
# 13 
# 14 #      if not snapshots:
# 15 #          return None
# 16 
# 17 #      latest_snapshot_id = snapshots[0].stem
# 18 #      before_path = session_root / f"{latest_snapshot_id}.before"
# 19 #      after_path = session_root / f"{latest_snapshot_id}.after"
# 20 
# 21 #      return {
# 22 #          "before": before_path.read_text(encoding="utf-8"),
# 23 #          "after": after_path.read_text(encoding="utf-8"),
# 24 #          "before_path": str(before_path),
# 25 #          "after_path": str(after_path)
# 26 #      }
# 27 


# utilities\metadata\snapshots\snapshot_writer.py

# 1 #  import json
# 2 #  import uuid
# 3 #  from datetime import datetime, timezone
# 4 #  from pathlib import Path
# 5 
# 6 #  SNAPSHOT_ROOT = Path(__file__).resolve().parents[4] / "experiments" / "snapshots"
# 7 
# 8 #  class SnapshotWriter:
# 9 #      def __init__(self, root: Path | str | None = None) -> None:
# 10 #          self.root = Path(root) if root else SNAPSHOT_ROOT
# 11 #          self.root.mkdir(parents=True, exist_ok=True)
# 12 
# 13 #      def write_snapshot(
# 14 #          self, *, before: str, after: str, session_id: str, metadata: dict | None = None
# 15 #      ) -> str:
# 16 
# 17 #          if before == after:
# 18 #              return ""
# 19 #          session_root = self.root / str(session_id) 
# 20 #          session_root.mkdir(parents=True, exist_ok=True)
# 21 
# 22 #          snapshot_id = str(uuid.uuid4())
# 23 
# 24 #          (session_root / f"{snapshot_id}.before").write_text(before, encoding="utf-8")
# 25 #          (session_root / f"{snapshot_id}.after").write_text(after, encoding="utf-8")
# 26 
# 27 #          if metadata:
# 28 #              metadata.setdefault("timestamp", datetime.now(timezone.utc).isoformat())
# 29 #              meta_path = session_root / f"{snapshot_id}.meta.json"
# 30 #              meta_path.write_text(json.dumps(metadata, indent=2), encoding="utf-8")
# 31 
# 32 #          return snapshot_id


# utilities\provider_mixin_injector.py

# 1 class ProviderContextInjectorWrapper:
# 2     def __init__(self, provider, session_id=None, file_log_id=None):
# 3         self._provider = provider
# 4         self._injected_session_id = session_id
# 5         self._injected_file_log_id = file_log_id
# 6 
# 7         if session_id:
# 8             setattr(self._provider, "_session_id", session_id)
# 9         if file_log_id:
# 10             setattr(self._provider, "_file_log_id", file_log_id)
# 11         if hasattr(self._provider, "propagate_file_log_id") and file_log_id:
# 12             self._provider.propagate_file_log_id(file_log_id)
# 13 
# 14     def run(self, input: dict | None = None):
# 15         input = input or {}
# 16         input.setdefault("session_id", self._injected_session_id)
# 17         input.setdefault("file_log_id", self._injected_file_log_id)
# 18 
# 19         #  Inject into provider directly
# 20         self._provider._session_id = self._injected_session_id
# 21         self._provider._file_log_id = self._injected_file_log_id
# 22 
# 23         return self._provider.run(input=input)
# 24 
# 25     def __getattr__(self, attr):
# 26         return getattr(self._provider, attr)


# utilities\pydantic_compat.py

# 1 from __future__ import annotations
# 2 
# 3 from typing import TYPE_CHECKING, Callable
# 4 
# 5 try:  #  pragma: no cover - pydantic v1 compatibility
# 6     from pydantic import BaseModel
# 7 
# 8     if not hasattr(BaseModel, "model_dump"):
# 9         from typing import Callable
# 10 
# 11         def _model_dump(self: BaseModel, **kwargs) -> dict:
# 12             return BaseModel.dict(self, **kwargs)
# 13 
# 14         setattr(BaseModel, "model_dump", _model_dump)  #  type: ignore[attr-defined]
# 15 except Exception:  #  pragma: no cover - ignore if pydantic v2
# 16     pass
# 17 
# 18 if TYPE_CHECKING:
# 19     from pydantic import field_validator, model_validator  #  type: ignore
# 20 else:  #  pragma: no cover - runtime fallback for pydantic v1
# 21     try:
# 22         from pydantic import field_validator, model_validator  #  type: ignore
# 23     except ImportError:  #  pragma: no cover - pydantic v1
# 24         from pydantic import validator, root_validator
# 25 
# 26         def field_validator(*fields: str, **kwargs) -> Callable:
# 27             kwargs.pop("mode", None)
# 28             return validator(*fields, **kwargs)
# 29 
# 30         def model_validator(*fields, **kwargs) -> Callable:
# 31             mode = kwargs.pop("mode", "after")
# 32 
# 33             def decorator(func: Callable) -> Callable:
# 34                 if mode == "after":
# 35 
# 36                     def wrapper(cls, values):
# 37                         obj = cls.construct(**values)
# 38                         func(obj)
# 39                         return obj.dict()
# 40 
# 41                     return root_validator(pre=False, allow_reuse=True)(wrapper)
# 42 
# 43                 def wrapper(cls, values):
# 44                     return func(values)
# 45 
# 46                 return root_validator(pre=True, allow_reuse=True)(wrapper)
# 47 
# 48             return decorator


# utilities\run_context.py

# 1 #  from contextvars import ContextVar
# 2 
# 3 #  #  Global context variables
# 4 #  _session_id: ContextVar[str | None] = ContextVar("session_id", default=None)
# 5 #  _file_log_id: ContextVar[str | None] = ContextVar("file_log_id", default=None)
# 6 
# 7 #  #  ─────────────────────────────────────────────────────────── Accessors
# 8 
# 9 #  def set_run_context(session_id: str | None = None, file_log_id: str | None = None) -> None:
# 10 #      if session_id is not None:
# 11 #          _session_id.set(session_id)
# 12 #      if file_log_id is not None:
# 13 #          _file_log_id.set(file_log_id)
# 14 
# 15 #  def clear_run_context() -> None:
# 16 #      _session_id.set(None)
# 17 #      _file_log_id.set(None)
# 18 
# 19 #  def get_session_id() -> str | None:
# 20 #      return _session_id.get()
# 21 
# 22 #  def get_file_log_id() -> str | None:
# 23 #      return _file_log_id.get()
# 24 
# 25 #  #  ─────────────────────────────────────────────────────────── Propagation
# 26 
# 27 #  def propagate_run_context_if_needed(obj: object) -> None:
# 28 #      from app.providers.base_provider import BaseProvider
# 29 #      try:
# 30 #          from app.utilities.provider_mixin_injector import ProviderContextInjectorWrapper
# 31 #      except ImportError:
# 32 #          ProviderContextInjectorWrapper = None
# 33 
# 34 #      session_id = get_session_id()
# 35 #      file_log_id = get_file_log_id()
# 36 
# 37 #      #  Unwrap injector wrappers
# 38 #      if ProviderContextInjectorWrapper and isinstance(obj, ProviderContextInjectorWrapper):
# 39 #          for attr in ('_wrapped_instance', 'instance', '_wrapped', 'wrapped'):
# 40 #              inner = getattr(obj, attr, None)
# 41 #              if inner:
# 42 #                  propagate_run_context_if_needed(inner)
# 43 #                  return
# 44 #          return
# 45 
# 46 #      #  Only proceed for BaseProvider instances
# 47 #      if not isinstance(obj, BaseProvider):
# 48 #          return
# 49 
# 50 #      #  Apply context
# 51 #      if session_id and not getattr(obj, '_session_id', None):
# 52 #          obj._session_id = session_id
# 53 #      if file_log_id and not getattr(obj, '_file_log_id', None):
# 54 #          obj._file_log_id = file_log_id
# 55 
# 56 #      #  Propagate to subcomponents
# 57 #      for attr_name in dir(obj):
# 58 #          try:
# 59 #              attr = getattr(obj, attr_name)
# 60 #              if isinstance(attr, BaseProvider):
# 61 #                  propagate_run_context_if_needed(attr)
# 62 #              elif isinstance(attr, list):
# 63 #                  for item in attr:
# 64 #                      if isinstance(item, BaseProvider):
# 65 #                          propagate_run_context_if_needed(item)
# 66 #              elif isinstance(attr, dict):
# 67 #                  for item in attr.values():
# 68 #                      if isinstance(item, BaseProvider):
# 69 #                          propagate_run_context_if_needed(item)
# 70 #          except Exception:
# 71 #              continue
# 72 
# 73 #  #  ─────────────────────────────────────────────────────────── Patch BaseProvider
# 74 
# 75 #  def _patch_base_provider_class():
# 76 #      from app.providers.base_provider import BaseProvider
# 77 #      from app.utilities.run_context import get_session_id, get_file_log_id
# 78 
# 79 #      orig_init = BaseProvider.__init__
# 80 #      def wrapped_init(self, *args, **kwargs):
# 81 #          orig_init(self, *args, **kwargs)
# 82 #          propagate_run_context_if_needed(self)
# 83 #      BaseProvider.__init__ = wrapped_init
# 84 
# 85 #      if hasattr(BaseProvider, 'run'):
# 86 #          orig_run = BaseProvider.run
# 87 #          def wrapped_run(self, *args, **kwargs):
# 88 #              propagate_run_context_if_needed(self)
# 89 #              session_id = get_session_id()
# 90 #              file_log_id = get_file_log_id()
# 91 #              if 'input' in kwargs and isinstance(kwargs['input'], dict):
# 92 #                  inp = kwargs['input']
# 93 #                  if session_id and not inp.get('session_id'):
# 94 #                      inp['session_id'] = session_id
# 95 #                  if file_log_id and not inp.get('file_log_id'):
# 96 #                      inp['file_log_id'] = file_log_id
# 97 #              return orig_run(self, *args, **kwargs)
# 98 #          BaseProvider.run = wrapped_run
# 99 
# 100 #  _patch_base_provider_class()


# utilities\schema\__init__.py

# 1 from .create_schema import initialize_database, create_tables, load_seed_data
# 2 
# 3 __all__ = ["initialize_database", "create_tables", "load_seed_data"]


# utilities\schema\create_schema.py

# 1 #  from pydantic import BaseModel, Field
# 2 #  from pathlib import Path
# 3 #  from typing import Dict, List, Optional, ClassVar
# 4 #  from uuid import UUID, uuid4
# 5 #  from app.enums.system_enums import SystemType
# 6 #  from app.utilities.pydantic_compat import field_validator
# 7 
# 8 #  class AgentPromptSchema(BaseModel):
# 9 #      id: Optional[int]
# 10 #      guid: UUID = Field(default_factory=uuid4)
# 11 #      name: str
# 12 #      description: Optional[str]
# 13 #      artifact_path: Path
# 14 #      tags: Optional[List[str]] = None
# 15 
# 16 #      table_name: ClassVar[str] = "agent_prompt"
# 17 
# 18 #      @field_validator("artifact_path")
# 19 #      @classmethod
# 20 #      def _check_path(cls, v: Path) -> Path:
# 21 #          if not v.is_absolute() and ".." in v.parts:
# 22 #              raise ValueError("Invalid artifact path")
# 23 #          return v
# 24 
# 25 #  class SystemPromptSchema(BaseModel):
# 26 #      id: Optional[int]
# 27 #      guid: UUID = Field(default_factory=uuid4)
# 28 #      name: str
# 29 #      system_type: SystemType
# 30 #      description: Optional[str]
# 31 #      artifact_path: Path
# 32 #      tags: Optional[List[str]] = None
# 33 
# 34 #      table_name: ClassVar[str] = "system_prompt"
# 35 
# 36 #      @field_validator("artifact_path")
# 37 #      @classmethod
# 38 #      def _check_path(cls, v: Path) -> Path:
# 39 #          if not v.is_absolute() and ".." in v.parts:
# 40 #              raise ValueError("Invalid artifact path")
# 41 #          return v
# 42 
# 43 #  class ToolConfig(BaseModel):
# 44 #      id: Optional[int] = None
# 45 #      guid: UUID = Field(default_factory=uuid4)
# 46 #      name: str
# 47 #      description: Optional[str] = None
# 48 #      config: Optional[Dict] = None
# 49 #      artifact_path: Path
# 50 
# 51 #      table_name: ClassVar[str] = "tool_config"
# 52 
# 53 #      @field_validator("artifact_path")
# 54 #      @classmethod
# 55 #      def _check_path(cls, v: Path) -> Path:
# 56 #          if not v.is_absolute() and ".." in v.parts:
# 57 #              raise ValueError("Invalid artifact path")
# 58 #          return v
# 59 
# 60 #  class ScoreProvider(BaseModel):
# 61 #      id: Optional[int] = None
# 62 #      guid: UUID = Field(default_factory=uuid4)
# 63 #      name: str
# 64 #      description: Optional[str] = None
# 65 #      artifact_path: Optional[Path] = None
# 66 
# 67 #      table_name: ClassVar[str] = "score_provider"
# 68 
# 69 #      @field_validator("artifact_path")
# 70 #      @classmethod
# 71 #      def _check_path(cls, v: Optional[Path]) -> Optional[Path]:
# 72 #          if v is None:
# 73 #              return v
# 74 #          p = Path(v)
# 75 #          if not p.is_absolute() and ".." in p.parts:
# 76 #              raise ValueError("artifact_path must be absolute or project relative")
# 77 #          return p
# 78 
# 79 #      def model_dump(self, **kwargs) -> dict:
# 80 #          return super().model_dump(**kwargs)


# utilities\select_best_file_by_score.py

# 1 from pathlib import Path
# 2 from typing import Callable
# 3 
# 4 from app.providers.score_provider_base import ScoreProviderBase
# 5 
# 6 
# 7 def select_best_file_by_score(
# 8     file_a: str,
# 9     file_b: str,
# 10     score_provider: ScoreProviderBase,
# 11     context
# 12 ) -> str:
# 13     """
# 14     Compare two file paths using a scoring provider (with .run(input, session_id).value)
# 15     and return the file path with the higher score.
# 16 
# 17     Args:
# 18         file_a (str): First file path to compare.
# 19         file_b (str): Second file path to compare.
# 20         scoring_provider: Object with a .run(dict, session_id) method that returns .value.
# 21         system (str): System identifier passed into the scoring input.
# 22         session_id (str): Session identifier passed into the scoring call.
# 23 
# 24     Returns:
# 25         str: File path with the higher score.
# 26     """
# 27     path_a = Path(file_a).resolve()
# 28     path_b = Path(file_b).resolve()
# 29 
# 30     score_a = score_provider.run({"file_path": str(path_a)}, context=context).value
# 31     score_b = score_provider.run({"file_path": str(path_b)}, context=context).value
# 32 
# 33     return str(path_a if score_a >= score_b else path_b)



# === PYTHON-NOTEBOOKS FILES ===


# === NON-PYTHON FILES ===

# db\seeders\files\prompts\basic_agent_prompt.txt

# 1 You are a code generation agent that produces basic, clean, formatted Python code snippets based on given instructions.


# db\seeders\files\prompts\default_system_prompt.txt

# 1 You are the default formatting system. Your task is to ensure all provided Python code strictly adheres to PEP8 standards.


# db\seeders\files\prompts\linting_generator_agent_prompt.txt

# 1 You are the Generator Agent in the Linting System.
# 2 
# 3 You specialize in incrementally improving Python code quality by addressing lint violations, enhancing readability, and promoting best practices.
# 4 
# 5 Your strategy is:
# 6 
# 7 Focus on minimal, targeted improvements that preserve original structure
# 8 
# 9 Apply type hints and clarify variable names where appropriate
# 10 
# 11 Use reasoning to resolve tool-reported issues when they conflict
# 12 
# 13 You will be evaluated by a Discriminator Agent and may be prompted to revise your output. If you are in a retry round, incorporate prior feedback before retrying.
# 14 
# 15 Maintain a helpful, confident tone in your comments. Your improvements should be easy for a human developer to understand and accept.
# 16 
# 17 🚫 Your response must strictly follow this format:
# 18 
# 19 [CODE]
# 20 #  your revised Python code here
# 21 [/CODE]
# 22 
# 23 [CONVERSATION_LOG_ENTRY]
# 24 #  bullet list of what changed, why, and tradeoffs if any
# 25 [/CONVERSATION_LOG_ENTRY]
# 26 
# 27 ⚠️ Anything outside these blocks will be ignored.
# 28 


# db\seeders\files\prompts\linting_system_prompt.txt

# 1 You are operating within the Linting System, which enforces strict Python code quality and formatting compliance.
# 2 
# 3 All Generator Agents must:
# 4 - Conform to Python PEP8 guidelines
# 5 - Use tools such as `black`, `ruff`, `mypy`, and `radon` for diagnostic feedback
# 6 - Preserve original code intent and structure; avoid stylistic or structural rewrites unless necessary
# 7 - Produce valid, executable Python code
# 8 
# 9 Your response must include your corrected code inside a [CODE] block. Any content outside this block will be ignored by the system:
# 10 
# 11 [CODE]
# 12 #  your corrected Python code
# 13 [/CODE]
# 14 
# 15 After the code, include a structured log entry describing your decisions:
# 16 
# 17 [CONVERSATION_LOG_ENTRY]
# 18 Summarize what you changed, why you changed it, and any tradeoffs or notable decisions you made.
# 19 [/CONVERSATION_LOG_ENTRY]


