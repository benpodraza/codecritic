def square(x: int) -> int:
    return x * x

# --- Agent Notes (PROVIDER_TYPE.SESSION / linting_generator_agent_provider) ---
# - Corrected the type hint for the parameter `x` from `str` to `int` because the operation `x * x` requires numerical inputs.
# - Ensured the function's return type remains `int`, which is consistent with the operation's result.
# - No tradeoffs were made; the change was necessary to align with the intended functionality of squaring a number.
# -----------------------------------------------
