def compute(x: float, y: float) -> float:
    return x * y

# --- Agent Notes (PROVIDER_TYPE.SESSION / linting_generator_agent_provider) ---
# - Added type hints to the function `compute` to specify that it takes two float arguments and returns a float. This improves code clarity and helps with static type checking, addressing the mypy score.
# - Preserved the original structure and logic of the function to maintain its intent.
# -----------------------------------------------
