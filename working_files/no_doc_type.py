def compute(x: float, y: float) -> float:
    return x * y

# --- Agent Notes (PROVIDER_TYPE.SESSION / linting_generator_agent_provider) ---
# - Added type hints to the `compute` function to specify that it takes two floats and returns a float. This enhances code readability and helps with static type checking.
# - No tradeoffs were made; the change maintains the original intent and functionality of the code.
# -----------------------------------------------
