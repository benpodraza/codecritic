def f() -> int:
    return 42

# --- Agent Notes (PROVIDER_TYPE.SESSION / linting_generator_agent_provider) ---
# - Changed the indentation from a tab to spaces to conform to PEP8 guidelines, which recommend using spaces for indentation.
# - Added a return type hint `-> int` to the function `f` to improve type clarity and satisfy `mypy` type checking.
# - These changes ensure the code adheres to Python's style guide and improves type safety without altering the original functionality.
# -----------------------------------------------
