
# === PYTHON FILES ===

# __init__.py



# db\__init__.py

# 1 import gc
# 2 from sqlalchemy import create_engine
# 3 from app.db.connection import DB_PATH, close_connection
# 4 from app.db.models import Base
# 5 
# 6 def init_db(reset: bool = False):
# 7     #  Explicitly close lingering DB-API connections
# 8     close_connection()
# 9 
# 10     #  Explicitly create and dispose of engine before deletion
# 11     engine = create_engine(f"sqlite:///{DB_PATH}")
# 12     engine.dispose()
# 13 
# 14     if reset and DB_PATH.exists():
# 15         try:
# 16             DB_PATH.unlink()
# 17         except PermissionError as e:
# 18             print(f"⚠️ Unable to delete {DB_PATH}: {e}")
# 19             print("⚠️ Attempting to force garbage collection.")
# 20             gc.collect()
# 21             DB_PATH.unlink()  #  try again after GC
# 22 
# 23     #  Recreate engine after deletion
# 24     engine = create_engine(f"sqlite:///{DB_PATH}")
# 25     Base.metadata.create_all(bind=engine)
# 26     
# 27     return engine


# db\base.py

# 1 from sqlalchemy.orm import declarative_base
# 2 
# 3 Base = declarative_base()


# db\connection.py

# 1 from pathlib import Path
# 2 import sqlite3
# 3 
# 4 DB_PATH = Path("experiments") / "codecritic.sqlite3"
# 5 _CONN: sqlite3.Connection | None = None
# 6 
# 7 def get_connection() -> sqlite3.Connection:
# 8     global _CONN
# 9     try:
# 10         if _CONN is None:
# 11             raise RuntimeError
# 12         _CONN.execute("SELECT 1")
# 13     except (sqlite3.ProgrammingError, RuntimeError):
# 14         _CONN = sqlite3.connect(DB_PATH, check_same_thread=False)
# 15     return _CONN
# 16 
# 17 def close_connection() -> None:
# 18     global _CONN
# 19     if _CONN:
# 20         _CONN.close()
# 21         _CONN = None


# db\models.py

# 1 from datetime import datetime, timezone
# 2 from sqlalchemy import Boolean, Column, DateTime, Enum, Float, Integer, String, JSON
# 3 from sqlalchemy.dialects.sqlite import TEXT
# 4 from app.db.base import Base
# 5 from uuid import uuid4
# 6 
# 7 from app.enums.system_enums import SystemType
# 8 
# 9 # #  PROMPT CONFIGS
# 10 
# 11 class AgentPrompt(Base):
# 12     __tablename__ = 'agent_prompt'
# 13     id = Column(Integer, primary_key=True, index=True)
# 14     guid = Column(TEXT, unique=True, default=lambda: str(uuid4()))
# 15     name = Column(String, nullable=False)
# 16     description = Column(String)
# 17     artifact_path = Column(String, nullable=False)
# 18     tags = Column(JSON, nullable=True)
# 19 
# 20 class SystemPrompt(Base):
# 21     __tablename__ = 'system_prompt'
# 22     id = Column(Integer, primary_key=True, index=True)
# 23     guid = Column(TEXT, unique=True, default=lambda: str(uuid4()))
# 24     name = Column(String, nullable=False)
# 25     system_type = Column(Enum(SystemType), nullable=False) 
# 26     description = Column(String)
# 27     artifact_path = Column(String, nullable=False)
# 28     tags = Column(JSON, nullable=True)
# 29 
# 30 
# 31 # #  PROVIDER CONFGIS
# 32 
# 33 class PromptProviderConfig(Base):
# 34     __tablename__ = "prompt_provider_config"
# 35 
# 36     id = Column(Integer, primary_key=True, index=True)
# 37     guid = Column(String, unique=True, nullable=False)
# 38     name = Column(String, nullable=False)
# 39     description = Column(String, nullable=True)
# 40     artifact_path = Column(String, nullable=False)
# 41     config = Column(JSON, nullable=False) 
# 42     tags = Column(JSON, nullable=True)
# 43 
# 44 class ToolProviderConfig(Base):
# 45     __tablename__ = "tool_provider_config"
# 46     id = Column(Integer, primary_key=True)
# 47     guid = Column(String, unique=True, nullable=False)
# 48     name = Column(String, nullable=False)
# 49     description = Column(String, nullable=True)
# 50     config = Column(JSON, nullable=True)
# 51     artifact_path = Column(String, nullable=False)
# 52     tags = Column(JSON, nullable=True)
# 53 
# 54 class ScoreProviderConfig(Base):
# 55     __tablename__ = "score_provider_config" 
# 56     id = Column(Integer, primary_key=True)
# 57     guid = Column(String, unique=True, nullable=False)
# 58     name = Column(String, nullable=False)
# 59     description = Column(String, nullable=True)
# 60     config = Column(JSON, nullable=True)
# 61     artifact_path = Column(String, nullable=False)
# 62     tags = Column(JSON, nullable=True)
# 63 
# 64 class ContextProviderConfig(Base):
# 65     __tablename__ = "context_provider_config" 
# 66     id = Column(Integer, primary_key=True)
# 67     guid = Column(String, unique=True, nullable=False)
# 68     name = Column(String, nullable=False)
# 69     description = Column(String, nullable=True)
# 70     config = Column(JSON, nullable=True)
# 71     artifact_path = Column(String, nullable=False)
# 72     tags = Column(JSON, nullable=True)
# 73 
# 74 class AgentEngineProviderConfig(Base):
# 75     __tablename__ = "agent_engine_provider_config" 
# 76 
# 77     id = Column(Integer, primary_key=True)
# 78     guid = Column(String, unique=True, nullable=False)
# 79     name = Column(String, nullable=False)
# 80     description = Column(String, nullable=True)
# 81     model= Column(String, nullable=False)
# 82     config = Column(JSON, nullable=True)
# 83     artifact_path = Column(String, nullable=False)
# 84     tags = Column(JSON, nullable=True)
# 85 
# 86 
# 87 class AgentProviderConfig(Base):
# 88     __tablename__ = "agent_provider_config" 
# 89 
# 90     id = Column(Integer, primary_key=True)
# 91     guid = Column(String, unique=True, nullable=False)
# 92     name = Column(String, nullable=False)
# 93     description = Column(String, nullable=True)
# 94     config = Column(JSON, nullable=True)
# 95     artifact_path = Column(String, nullable=False)
# 96     tags = Column(JSON, nullable=True)
# 97 
# 98 class StateProviderConfig(Base):
# 99     __tablename__ = "state_provider_config"
# 100 
# 101     id = Column(Integer, primary_key=True)
# 102     guid = Column(TEXT, unique=True, default=lambda: str(uuid4()))
# 103     name = Column(String, nullable=False)
# 104     description = Column(String, nullable=True)
# 105     config = Column(JSON, nullable=True)
# 106     artifact_path = Column(String, nullable=False)
# 107     tags = Column(JSON, nullable=True)
# 108 
# 109 class SystemProviderConfig(Base):
# 110     __tablename__ = "system_provider_config"
# 111 
# 112     id = Column(Integer, primary_key=True)
# 113     guid = Column(TEXT, unique=True, default=lambda: str(uuid4()))
# 114     name = Column(String, nullable=False)
# 115     description = Column(String, nullable=True)
# 116     config = Column(JSON, nullable=True)
# 117     artifact_path = Column(String, nullable=False)
# 118     tags = Column(JSON, nullable=True)
# 119 
# 120 class ControllerProviderConfig(Base):
# 121     __tablename__ = "controller_provider_config"
# 122 
# 123     id = Column(Integer, primary_key=True)
# 124     guid = Column(TEXT, unique=True, default=lambda: str(uuid4()))
# 125     name = Column(String, nullable=False)
# 126     description = Column(String, nullable=True)
# 127     config = Column(JSON, nullable=True)        #  expects {"systems": {"name": id, …}, …}
# 128     artifact_path = Column(String, nullable=False)
# 129     tags = Column(JSON, nullable=True)
# 130 
# 131 
# 132 class ProgramProviderConfig(Base):
# 133     __tablename__ = "program_provider_config"
# 134 
# 135     id = Column(Integer, primary_key=True)
# 136     guid = Column(TEXT, unique=True, default=lambda: str(uuid4()))
# 137     name = Column(String, nullable=False)
# 138     description = Column(String, nullable=True)
# 139     config = Column(JSON, nullable=True)
# 140     artifact_path = Column(String, nullable=False)
# 141     tags = Column(JSON, nullable=True)
# 142 
# 143 class SessionConfig(Base):
# 144     __tablename__ = "session_config"
# 145 
# 146     id = Column(Integer, primary_key=True)
# 147     program_provider_id = Column(String, unique=True, nullable=False)
# 148     name = Column(String, nullable=False)
# 149     description = Column(String, nullable=True)
# 150     environment_type = Column(String, nullable=False)  #  e.g., "experiment", "live", "staging"
# 151     tags = Column(JSON, nullable=True)
# 152 
# 153 
# 154 # #  LOGS
# 155 
# 156 class ProviderLog(Base):
# 157     __tablename__ = "provider_log"
# 158 
# 159     id = Column(Integer, primary_key=True, index=True)
# 160     session_id = Column(String, nullable=False)
# 161     timestamp = Column(DateTime, nullable=False, default=datetime.now(timezone.utc))
# 162     provider_id = Column(Integer, nullable=False)
# 163     provider_type = Column(String, nullable=False)
# 164     input = Column(String, nullable=True)
# 165     output = Column(String, nullable=True)
# 166     snapshot_id = Column(String, nullable=True)
# 167     file_path = Column(String, nullable=True)
# 168     transition_from = Column(String, nullable=True)
# 169     transition_to = Column(String, nullable=True)
# 170     
# 171 class StateTransitionLog(Base):
# 172     __tablename__ = "state_transition_log"
# 173 
# 174     id = Column(Integer, primary_key=True, index=True)
# 175     session_id = Column(String, nullable=False)
# 176     timestamp = Column(DateTime, nullable=False, default=datetime.now(timezone.utc))
# 177     entity_type = Column(String, nullable=False)
# 178     entity_id = Column(Integer, nullable=False)
# 179     from_state = Column(String, nullable=False)
# 180     to_state = Column(String, nullable=False)
# 181     reason = Column(String, nullable=True)
# 182     input_snapshot_id = Column(String, nullable=True)
# 183     output_snapshot_id = Column(String, nullable=True)
# 184 
# 185 class AgentConversationLog(Base):
# 186     __tablename__ = "agent_conversation_log"
# 187 
# 188     id = Column(Integer, primary_key=True, index=True)
# 189     session_id = Column(String, nullable=False)
# 190     system = Column(String, nullable=False)
# 191     agent_provider_config_id = Column(Integer, nullable=False)  
# 192     agent_name = Column(String, nullable=False)
# 193     content = Column(String, nullable=False)
# 194     timestamp = Column(DateTime, nullable=False, default=datetime.now(timezone.utc))
# 195 
# 196 class ErrorLog(Base):
# 197     __tablename__ = "error_log"
# 198 
# 199     id = Column(Integer, primary_key=True, index=True)
# 200     session_id = Column(String, nullable=False)
# 201     timestamp = Column(DateTime, nullable=False, default=datetime.now(timezone.utc))
# 202     provider_id = Column(Integer, nullable=True)
# 203     provider_type = Column(String, nullable=True)
# 204     error_type = Column(String, nullable=False)
# 205     message = Column(String, nullable=False)
# 206     file_path = Column(String, nullable=True)


# db\schemas.py

# 1 from dataclasses import dataclass, field
# 2 from datetime import datetime, timezone
# 3 from typing import Any, Dict, List, Optional
# 4 from pathlib import Path
# 5 from uuid import UUID, uuid4
# 6 from pydantic import BaseModel, Field, field_validator
# 7 from app.enums.agent_enums import AgentRole
# 8 from app.enums.system_enums import Decision, SystemType
# 9 
# 10 
# 11 class AgentPromptSchema(BaseModel):
# 12     id: Optional[int] = None
# 13     guid: UUID = Field(default_factory=uuid4)
# 14     name: str
# 15     description: Optional[str] = None
# 16     artifact_path: Path
# 17     tags: Optional[List[str]] = None
# 18 
# 19     @field_validator("artifact_path")
# 20     @classmethod
# 21     def validate_artifact_path(cls, v: Path) -> Path:
# 22         if not v.is_absolute() and ".." in v.parts:
# 23             raise ValueError("Invalid artifact path")
# 24         return v
# 25 
# 26 class SystemPromptSchema(BaseModel):
# 27     id: Optional[int] = None
# 28     guid: UUID = Field(default_factory=uuid4)
# 29     name: str
# 30     system_type: SystemType
# 31     description: Optional[str] = None
# 32     artifact_path: Path
# 33     tags: Optional[List[str]] = None
# 34 
# 35     @field_validator("artifact_path")
# 36     @classmethod
# 37     def validate_artifact_path(cls, v: Path) -> Path:
# 38         if not v.is_absolute() and ".." in v.parts:
# 39             raise ValueError("Invalid artifact path")
# 40         return v
# 41 
# 42 class PromptProviderConfig(BaseModel):
# 43     id: Optional[int] = None
# 44     guid: UUID = Field(default_factory=uuid4)
# 45     name: str
# 46     description: Optional[str] = None
# 47     artifact_path: Path
# 48     config: Optional[Dict[str, Any]] = None
# 49     tags: Optional[List[str]] = Field(default_factory=list)
# 50 
# 51     @field_validator("artifact_path")
# 52     @classmethod
# 53     def validate_artifact_path(cls, v: Path) -> Path:
# 54         if not v.is_absolute() and ".." in v.parts:
# 55             raise ValueError("Invalid artifact path")
# 56         return v
# 57 
# 58 class ToolProviderConfig(BaseModel):
# 59     id: Optional[int] = None
# 60     guid: UUID = Field(default_factory=uuid4)
# 61     name: str
# 62     description: Optional[str] = None
# 63     config: Optional[Dict[str, Any]] = None
# 64     artifact_path: Path
# 65 
# 66     @field_validator("artifact_path")
# 67     @classmethod
# 68     def validate_artifact_path(cls, v: Path) -> Path:
# 69         if not v.is_absolute() and ".." in v.parts:
# 70             raise ValueError("Invalid artifact path")
# 71         return v
# 72 
# 73 class ScoreOutputSchema(BaseModel):
# 74     name: str = Field(..., description="The name of the scoring metric (e.g., 'linting_score').")
# 75     value: float = Field(..., description="The final weighted score, normalized to a 0.0–1.0 scale.")
# 76     components: Dict[str, float] = Field(
# 77         ...,
# 78         description="Component scores used to compute the overall score. Keys should match tool names."
# 79     )
# 80 
# 81 class ScoreProviderConfig(BaseModel):
# 82     id: Optional[int] = None
# 83     guid: UUID = Field(default_factory=uuid4)
# 84     name: str
# 85     description: Optional[str] = None
# 86     artifact_path: Optional[Path] = None
# 87     table_name: str = "score_provider"
# 88 
# 89     @field_validator("artifact_path")
# 90     @classmethod
# 91     def validate_artifact_path(cls, v: Optional[Path]) -> Optional[Path]:
# 92         if v is None:
# 93             return v
# 94         if not v.is_absolute() and ".." in v.parts:
# 95             raise ValueError("Invalid artifact path")
# 96         return v
# 97         
# 98 class AgentEngineProviderConfig(BaseModel):
# 99     id: Optional[int] = None
# 100     guid: UUID = Field(default_factory=uuid4)
# 101     name: str
# 102     description: Optional[str] = None
# 103     model: str
# 104     config: Optional[Dict[str, Any]] = None
# 105     artifact_path: Path
# 106 
# 107     @field_validator("artifact_path")
# 108     @classmethod
# 109     def validate_artifact_path(cls, v: Path) -> Path:
# 110         if not v.is_absolute() and ".." in v.parts:
# 111             raise ValueError("Invalid artifact path")
# 112         return v
# 113 
# 114 class AgentProviderConfigSchema(BaseModel):
# 115     id: Optional[int] = None
# 116     guid: UUID = Field(default_factory=uuid4)
# 117     name: str
# 118     description: Optional[str] = None
# 119     config: Optional[Dict[str, Any]] = None
# 120     artifact_path: Path
# 121     tags: Optional[list[str]] = None
# 122 
# 123     @field_validator("artifact_path")
# 124     @classmethod
# 125     def validate_artifact_path(cls, v: Path) -> Path:
# 126         if not v.is_absolute() and ".." in v.parts:
# 127             raise ValueError("Invalid artifact path")
# 128         return v
# 129 
# 130 class StateProviderConfigSchema(BaseModel):
# 131     id: Optional[int] = None
# 132     guid: UUID = Field(default_factory=uuid4)
# 133     name: str
# 134     description: Optional[str] = None
# 135     config: Optional[Dict[str, Any]] = None
# 136     artifact_path: Path
# 137     tags: Optional[list[str]] = None
# 138 
# 139     @classmethod
# 140     def validate_artifact_path(cls, v: Path) -> Path:
# 141         if not v.is_absolute() and ".." in v.parts:
# 142             raise ValueError("Invalid artifact path")
# 143         return v
# 144 
# 145 class SystemProviderConfigSchema(BaseModel):
# 146     id: Optional[int] = None
# 147     guid: UUID = Field(default_factory=uuid4)
# 148     name: str
# 149     description: Optional[str] = None
# 150     config: Optional[Dict[str, Any]] = None
# 151     artifact_path: Path
# 152     tags: Optional[list[str]] = None
# 153 
# 154     @classmethod
# 155     def validate_artifact_path(cls, v: Path) -> Path:
# 156         if not v.is_absolute() and ".." in v.parts:
# 157             raise ValueError("Invalid artifact path")
# 158         return v
# 159 
# 160 class ControllerProviderConfigSchema(BaseModel):
# 161     id: Optional[int] = None
# 162     guid: UUID = Field(default_factory=uuid4)
# 163     name: str
# 164     description: Optional[str] = None
# 165     config: Optional[Dict[str, Any]] = None
# 166     artifact_path: Path
# 167     tags: Optional[list[str]] = None
# 168 
# 169     @classmethod
# 170     def validate_artifact_path(cls, v: Path) -> Path:
# 171         if not v.is_absolute() and ".." in v.parts:
# 172             raise ValueError("Invalid artifact path")
# 173         return v
# 174 
# 175 class ProgramProviderConfigSchema(BaseModel):
# 176     id: Optional[int] = None
# 177     guid: UUID = Field(default_factory=uuid4)
# 178     name: str
# 179     description: Optional[str] = None
# 180     config: Optional[Dict[str, Any]] = None
# 181     artifact_path: Path
# 182     tags: Optional[List[str]] = None
# 183 
# 184     @classmethod
# 185     def validate_artifact_path(cls, v: Path) -> Path:
# 186         if not v.is_absolute() and ".." in v.parts:
# 187             raise ValueError("Invalid artifact path")
# 188         return v
# 189 
# 190 class SessionConfigSchema(BaseModel):
# 191     id: Optional[int] = None
# 192     session_id: str
# 193     name: str
# 194     description: Optional[str] = None
# 195     environment_type: str  #  e.g., "experiment", "live", "staging"
# 196     config: Optional[Dict[str, Any]] = None
# 197     tags: Optional[List[str]] = None
# 198     
# 199 
# 200 #  LOGS
# 201 
# 202 @dataclass
# 203 class ProviderLogSchema:
# 204     session_id: str
# 205     provider_id: int
# 206     provider_type: str
# 207     input: Optional[str] = None
# 208     output: Optional[str] = None
# 209     snapshot_id: Optional[str] = None
# 210     file_path: Optional[str] = None
# 211     transition_from: Optional[str] = None
# 212     transition_to: Optional[str] = None
# 213     timestamp: datetime = field(default_factory=lambda: datetime.now(timezone.utc))
# 214 
# 215 @dataclass
# 216 class StateTransitionLogSchema:
# 217     session_id: str
# 218     entity_type: str
# 219     entity_id: int
# 220     from_state: str
# 221     to_state: str
# 222     reason: str | None = None
# 223     timestamp: datetime = field(default_factory=lambda: datetime.now(timezone.utc))
# 224 
# 225 @dataclass
# 226 class AgentConversationLogSchema:
# 227     session_id: str
# 228     system: str
# 229     agent_provider_config_id: int
# 230     agent_name: str
# 231     content: str
# 232     timestamp: datetime = field(default_factory=lambda: datetime.now(timezone.utc))
# 233 
# 234 @dataclass
# 235 class ErrorLogSchema:
# 236     session_id: str             
# 237     error_type: str                   
# 238     message: str                    
# 239     file_path: str | None = None       
# 240     timestamp: datetime = field(default_factory=lambda: datetime.now(timezone.utc))
# 241 
# 242 #  DTOs
# 243 
# 244 class Snapshot(BaseModel):
# 245     timestamp: datetime
# 246     file: str
# 247     score: float
# 248     decision: Decision
# 249 
# 250 class SystemState(BaseModel):
# 251     system: str
# 252     file_path: str
# 253     working_file: str
# 254     final_file: Optional[str] = None
# 255     snapshots: List[Snapshot]
# 256     state: Optional[str] = "active"


# db\seed.py

# 1 from sqlalchemy.orm import Session
# 2 from sqlalchemy import create_engine
# 3 from pathlib import Path
# 4 from app.db.seeders.seed_controller_providers import seed_orchestrator_providers
# 5 from app.db.seeders.seed_state_providers import seed_state_providers
# 6 from app.db.seeders.seed_agent_engine_providers import seed_agent_engine_providers
# 7 from app.db.seeders.seed_context_providers import seed_context_providers
# 8 from app.db.seeders.seed_prompt_providers import seed_prompt_providers
# 9 from app.db.seeders.seed_score_providers import seed_score_providers
# 10 from app.db.seeders.seed_tool_providers import seed_tool_providers
# 11 from app.db.seeders.seed_prompts import seed_prompts
# 12 
# 13 #  Ensure your project root matches explicitly
# 14 PROJECT_ROOT = Path(__file__).resolve().parents[2]
# 15 DB_PATH = PROJECT_ROOT / "experiments" / "codecritic.sqlite3"
# 16 EXTENSIONS_DIR = PROJECT_ROOT / "extensions"
# 17 
# 18 def main():
# 19     engine = create_engine(f"sqlite:///{DB_PATH}")
# 20     with Session(bind=engine) as session:
# 21         seed_prompts(session)
# 22         seed_prompt_providers(session)
# 23         seed_tool_providers(session)
# 24         seed_score_providers(session)
# 25         seed_context_providers(session)
# 26         seed_agent_engine_providers(session)
# 27         seed_state_providers(session)
# 28         seed_orchestrator_providers(session)
# 29 
# 30     print(f"Database seeded successfully at {DB_PATH.resolve()}")
# 31 
# 32 if __name__ == "__main__":
# 33     main()


# db\seeders\__init__.py



# db\seeders\files\__init__.py



# db\seeders\files\agent_engine_providers\__init__.py



# db\seeders\files\agent_engine_providers\basic_agent_engine_provider.py

# 1 from app.providers.agent_engine_provider_base import AgentEngineProviderBase
# 2 
# 3 class BasicAgentEngineProvider(AgentEngineProviderBase):
# 4     def _run(self, prompt: str) -> str:
# 5         #  Mock response for testing
# 6         return f"[Basic Response] You said: {prompt}"


# db\seeders\files\agent_engine_providers\openai_gpt4o_agent_engine_provider.py

# 1 import os
# 2 from openai import OpenAI
# 3 from app.providers.agent_engine_provider_base import AgentEngineProviderBase
# 4 
# 5 class OpenAIGPT4oAgentEngineProvider(AgentEngineProviderBase):
# 6     def _run(self, input: dict) -> str:
# 7         prompt = input["prompt"]
# 8         api_key = os.getenv("OPENAI_API_KEY")
# 9 
# 10         if not api_key:
# 11             raise EnvironmentError("OPENAI_API_KEY is not set in the environment.")
# 12 
# 13         client = OpenAI(api_key=api_key)
# 14 
# 15         response = client.chat.completions.create(
# 16             model="gpt-4o",
# 17             messages=[
# 18                 {"role": "system", "content": "You are a high-precision agent executing structured code refinement tasks."},
# 19                 {"role": "user", "content": prompt}
# 20             ],
# 21             temperature=0.2,
# 22             max_tokens=2048,
# 23         )
# 24 
# 25         return response.choices[0].message.content.strip()


# db\seeders\files\agent_providers\__init__.py



# db\seeders\files\agent_providers\basic_agent_provider.py

# 1 from app.providers.agent_provider_base import AgentProviderBase
# 2 
# 3 class BasicAgentProvider(AgentProviderBase):
# 4     def _run(self, agent_config: dict) -> str:
# 5         return f"[Agent] Executed with config: {agent_config}"


# db\seeders\files\agent_providers\linting_discriminator_agent_provider.py

# 1 from app.providers.agent_provider_base import AgentProviderBase
# 2 from app.utilities.metadata.snapshots.snapshot_reader import read_latest_snapshot
# 3 from app.utilities.diff_utils import summarize_diff
# 4 
# 5 class LintingDiscriminatorAgentProvider(AgentProviderBase):
# 6     def __init__(self, config=None, engine=None, **kwargs):
# 7         super().__init__(config=config, engine=engine, **kwargs)
# 8 
# 9     def _run(self, input: dict) -> str:
# 10         snapshot = read_latest_snapshot(session_id=self._session_id)
# 11         if not snapshot:
# 12             return (
# 13                 "[AGENT_DECISION]reject[/AGENT_DECISION]\n\n"
# 14                 "[CONVERSATION_LOG_ENTRY]\nNo snapshot found.\n[/CONVERSATION_LOG_ENTRY]"
# 15             )
# 16 
# 17         before, after = snapshot["before"], snapshot["after"]
# 18         if before == after:
# 19             return (
# 20                 "[AGENT_DECISION]reject[/AGENT_DECISION]\n\n"
# 21                 "[CONVERSATION_LOG_ENTRY]\nNo meaningful change.\n[/CONVERSATION_LOG_ENTRY]"
# 22             )
# 23 
# 24         summary = summarize_diff(before, after).strip()
# 25         if not summary:
# 26             return (
# 27                 "[AGENT_DECISION]reject[/AGENT_DECISION]\n\n"
# 28                 "[CONVERSATION_LOG_ENTRY]\nDiff could not be summarized.\n[/CONVERSATION_LOG_ENTRY]"
# 29             )
# 30 
# 31         return (
# 32             "[AGENT_DECISION]accept[/AGENT_DECISION]\n\n"
# 33             f"[CONVERSATION_LOG_ENTRY]\n{summary}\n[/CONVERSATION_LOG_ENTRY]"
# 34         )


# db\seeders\files\agent_providers\linting_generator_agent_provider.py

# 1 from app.providers.agent_provider_base import AgentProviderBase
# 2 import textwrap
# 3 
# 4 class LintingGeneratorAgentProvider(AgentProviderBase):
# 5     """Runs a GPT-4o generation round using the linting system prompt, context, and snapshot."""
# 6 
# 7     def __init__(self, config=None, engine=None, **kwargs):
# 8         super().__init__(config=config, engine=engine, **kwargs)
# 9 
# 10     def _run(self, input: dict) -> str:
# 11         session_id = self._session_id
# 12         file_path = input["file_path"]
# 13         system = input.get("system", "linting")
# 14 
# 15         if not self._prompt_provider:
# 16             raise ValueError("Prompt provider is not set")
# 17         if not self._agent_engine:
# 18             raise ValueError("Agent engine is not set")
# 19 
# 20         #  🧠 Build final prompt
# 21         final_prompt = self._prompt_provider.run(
# 22             input=input,  #  pass through exactly what was received
# 23             session_id=session_id
# 24         )
# 25 
# 26         #  🤖 Run LLM engine
# 27         return self._agent_engine.run(
# 28             input={
# 29                 "prompt": final_prompt,
# 30                 "before": file_path
# 31             },
# 32             session_id=session_id
# 33         )


# db\seeders\files\context_providers\__init__.py



# db\seeders\files\context_providers\basic_context_provider.py

# 1 from app.providers.context_provider_base import ContextProviderBase
# 2 import json
# 3 
# 4 class BasicContextProvider(ContextProviderBase):
# 5     def _run(self, input: dict) -> str:
# 6         return json.dumps({
# 7             "file_path": "tests/example.py",
# 8             "source_code": "def example():\n    pass\n"
# 9         })


# db\seeders\files\context_providers\linting_context_provider.py

# 1 from __future__ import annotations
# 2 from pathlib import Path
# 3 import json
# 4 
# 5 from sqlalchemy.orm import Session
# 6 from app.providers.context_provider_base import ContextProviderBase
# 7 from app.utilities.metadata.logging.conversation_log import get_conversation_log
# 8 
# 9 class LintingContextProvider(ContextProviderBase):
# 10     def _run(self, input: dict) -> str:
# 11         file_path = Path(input["file_path"]).resolve()
# 12         session_id = input["session_id"]
# 13         system = input["system"]
# 14 
# 15         if not file_path.exists():
# 16             raise FileNotFoundError(f"❌ File not found: {file_path}")
# 17 
# 18         source_code = file_path.read_text(encoding="utf-8")
# 19 
# 20         score_result = self.score_provider.run(
# 21             input={"file_path": str(file_path)},
# 22             session_id=session_id
# 23         )
# 24 
# 25         with Session(bind=self._engine) as session:
# 26             convo_log = get_conversation_log(session, session_id=session_id, system=system)
# 27 
# 28         context = {
# 29             "file_path": str(file_path),
# 30             "source_code": source_code,
# 31             "score": score_result.model_dump(),
# 32             "conversation_log": convo_log
# 33         }
# 34 
# 35         return json.dumps(context, indent=2)


# db\seeders\files\controller_providers\__init__.py



# db\seeders\files\controller_providers\preprocessing_controller_provider.py

# 1 from app.providers.controller_provider_base import ControllerProviderBase
# 2 
# 3 class PreprocessingControllerProvider(ControllerProviderBase):
# 4     def _transition(self, state, sys_output):
# 5         if state["state"] == "start":
# 6             return {"state": "preprocess", "reason": "kickoff"}
# 7         if state["state"] == "preprocess":
# 8             return {"state": "end", "reason": "after preprocessing"}
# 9         return {"state": "end", "reason": "fallback"}


# db\seeders\files\program_providers\__init__.py



# db\seeders\files\program_providers\codecritic_program_provider.py

# 1 #  extensions/codecritic_program_provider.py
# 2 
# 3 from app.providers.program_provider_base import ProgramProviderBase
# 4 
# 5 class CodeCriticProgramProvider(ProgramProviderBase):
# 6     def _transition(self, state, ctrl_output):
# 7         if state["state"] == "start":
# 8             return {"state": "preprocessing", "reason": "start of program"}
# 9 
# 10         if state["state"] == "preprocessing":
# 11             return {"state": "end", "reason": "preprocessing complete"}
# 12 
# 13         return {"state": "end", "reason": "unexpected state"}


# db\seeders\files\prompt_providers\__init__.py



# db\seeders\files\prompt_providers\basic_prompt_provider.py

# 1 from app.providers.prompt_provider_base import PromptProviderBase
# 2 
# 3 class BasicPromptProvider(PromptProviderBase):
# 4     def _run(self, input: dict) -> str:
# 5         agent_config = input.get("agent_config", {})
# 6         system_config = input.get("system_config", {})
# 7 
# 8         return f"User: {agent_config.get('input', 'No input provided')}\nSystem: Response mode: {system_config.get('setting', 'default')}"


# db\seeders\files\prompt_providers\linting_prompt_provider.py

# 1 from __future__ import annotations
# 2 from pathlib import Path
# 3 from app.providers.prompt_provider_base import PromptProviderBase
# 4 import textwrap
# 5 import json
# 6 
# 7 class LintingPromptProvider(PromptProviderBase):
# 8     def __init__(self, config, engine=None):
# 9         super().__init__(config=config, engine=engine)
# 10 
# 11         EXTENSIONS_DIR = Path(__file__).resolve().parent
# 12 
# 13         if not self._agent_prompt:
# 14             raise ValueError("PromptProvider is missing required AgentPrompt")
# 15         if not self._system_prompt:
# 16             raise ValueError("PromptProvider is missing required SystemPrompt")
# 17         if not self._context_provider:
# 18             raise ValueError("PromptProvider is missing required ContextProvider")
# 19 
# 20         self.agent_prompt_path = (EXTENSIONS_DIR / self._agent_prompt.artifact_path).resolve()
# 21         self.system_prompt_path = (EXTENSIONS_DIR / self._system_prompt.artifact_path).resolve()
# 22 
# 23     def _run(self, input: dict) -> str:
# 24         session_id = input.get("session_id")
# 25         system = input.get("system", "unknown")
# 26         file_path = input.get("file_path")
# 27 
# 28         if not session_id or not file_path:
# 29             raise ValueError("PromptProvider requires both session_id and file_path")
# 30 
# 31         if not self.agent_prompt_path.exists():
# 32             raise FileNotFoundError(f"Agent prompt not found: {self.agent_prompt_path}")
# 33         if not self.system_prompt_path.exists():
# 34             raise FileNotFoundError(f"System prompt not found: {self.system_prompt_path}")
# 35 
# 36         #  🎯 Build context using the injected context provider
# 37         raw_context = self._context_provider.run(
# 38             {
# 39                 "file_path": file_path,
# 40                 "session_id": session_id,
# 41                 "system": system
# 42             }
# 43         )
# 44         context = json.loads(raw_context)
# 45 
# 46         agent_text = self.agent_prompt_path.read_text(encoding="utf-8").strip()
# 47         system_text = self.system_prompt_path.read_text(encoding="utf-8").strip()
# 48         convo_section = "\n".join(context.get("conversation_log", [])) or "(none)"
# 49         score = context.get("score", {}).get("value", "unknown")
# 50         source_code = context.get("source_code", "")
# 51 
# 52         return textwrap.dedent(f"""\
# 53 
# 54             {agent_text}
# 55 
# 56             ---
# 57 
# 58             {system_text}
# 59 
# 60             ---
# 61 
# 62             Score: {score}
# 63             Conversation Log:
# 64             {convo_section}
# 65 
# 66             ---
# 67 
# 68             [SOURCE_CODE]
# 69             {source_code.rstrip()}
# 70             [/SOURCE_CODE]
# 71         """)


# db\seeders\files\prompts\__init__.py



# db\seeders\files\score_providers\__init__.py



# db\seeders\files\score_providers\linting_score_provider.py

# 1 import json
# 2 from app.providers.score_provider_base import ScoreProviderBase
# 3 from app.db.schemas import ScoreOutputSchema
# 4 
# 5 
# 6 class LintingScoreProvider(ScoreProviderBase):
# 7     """Computes weighted linting score using injected tool providers."""
# 8 
# 9     def _run(self, input: dict) -> ScoreOutputSchema:
# 10         file_path = input["file_path"]
# 11 
# 12         required = ["ruff", "mypy", "black", "radon"]
# 13         available = {
# 14             tool.__class__.__name__.lower(): tool
# 15             for tool in getattr(self, "tool_providers", [])
# 16         }
# 17 
# 18         #  Simple normalization to lookup by config name
# 19         resolved = {}
# 20         for name in required:
# 21             match = next((t for t in available.values() if name in t.__class__.__name__.lower()), None)
# 22             if not match:
# 23                 raise ValueError(f"Missing tool provider: {name}")
# 24             resolved[name] = match
# 25 
# 26         def parse_output(output: str, fallback_score: float = 1.0) -> float:
# 27             try:
# 28                 result = json.loads(output)
# 29                 violations = result["stdout"].count("\n")
# 30                 return max(0.0, 1.0 - (violations / 20))
# 31             except Exception:
# 32                 return fallback_score
# 33 
# 34         #  Run each tool
# 35         ruff_score = parse_output(resolved["ruff"].run({"target": file_path}, session_id=self._session_id))
# 36         mypy_score = parse_output(resolved["mypy"].run({"target": file_path}, session_id=self._session_id))
# 37         black_score = parse_output(resolved["black"].run({"target": file_path}, session_id=self._session_id))
# 38         radon_score = parse_output(resolved["radon"].run({"target": file_path}, session_id=self._session_id))
# 39 
# 40         weighted_score = round(
# 41             ruff_score * 0.4 +
# 42             mypy_score * 0.3 +
# 43             black_score * 0.2 +
# 44             radon_score * 0.1, 3
# 45         )
# 46 
# 47         return ScoreOutputSchema(
# 48             name="linting_score",
# 49             value=weighted_score,
# 50             components={
# 51                 "ruff": ruff_score,
# 52                 "mypy": mypy_score,
# 53                 "black": black_score,
# 54                 "radon": radon_score
# 55             }
# 56         )


# db\seeders\files\state_providers\__init__.py



# db\seeders\files\state_providers\basic_state_provider.py

# 1 from app.factories.base_provider_factory import BaseProviderFactory
# 2 from app.db.models import StateProviderConfig
# 3 from app.providers.state_provider_base import StateProviderBase
# 4 
# 5 class StateProviderFactory(BaseProviderFactory):
# 6     config_model = StateProviderConfig
# 7     base_class = StateProviderBase


# db\seeders\files\state_providers\linting_discriminator_state_provider.py

# 1 
# 2 from app.providers.state_provider_base import StateProviderBase
# 3 from app.factories.agent_provider_factory import AgentProviderFactory
# 4 
# 5 class LintingDiscriminatorStateProvider(StateProviderBase):
# 6     def _transition(self, state: dict, agent_output: str | None) -> dict:
# 7         if agent_output and "[AGENT_DECISION]accept" in agent_output:
# 8             return {"state": "end", "reason": "discriminator accepted the change"}
# 9         return {"state": "end", "reason": "discriminator rejected the change"}


# db\seeders\files\state_providers\linting_generator_state_provider.py

# 1 from app.providers.state_provider_base import StateProviderBase
# 2 
# 3 class LintingGeneratorStateProvider(StateProviderBase):
# 4     def _transition(self, state: dict, agent_output: str | None) -> dict:
# 5         return {"state": "end", "reason": "generation complete"}


# db\seeders\files\system_providers\__init__.py



# db\seeders\files\system_providers\linting_system_provider.py

# 1 from app.providers.system_provider_base import SystemProviderBase
# 2 from app.enums.system_enums import Decision
# 3 
# 4 
# 5 class LintingSystemProvider(SystemProviderBase):
# 6     def _transition(self, state: dict, agent_output: str | None) -> dict:
# 7         if state["state"] == "start":
# 8             return {"state": "generate", "reason": "initial entry"}
# 9 
# 10         if state["state"] == "generate":
# 11             return {"state": "discriminate", "reason": "completed generation"}
# 12 
# 13         if state["state"] == "discriminate":
# 14             if agent_output and "[AGENT_DECISION]accept" in agent_output:
# 15                 return {"state": "end", "reason": "accepted by discriminator"}
# 16             return {"state": "generate", "reason": "rejected by discriminator, retrying generation"}
# 17 
# 18         return {"state": "end", "reason": "unknown state or fallback"}


# db\seeders\files\tool_providers\__init__.py



# db\seeders\files\tool_providers\black_tool.py

# 1 from __future__ import annotations
# 2 import subprocess
# 3 import sys
# 4 import json
# 5 from app.providers.tool_provider_base import ToolProviderBase
# 6 
# 7 class BlackToolProvider(ToolProviderBase):
# 8     def _run(self, input: dict) -> str:
# 9         target = input.get("target")
# 10         check = input.get("check", False)
# 11 
# 12         cmd = [sys.executable, "-m", "black", "--quiet", target]
# 13         if check:
# 14             cmd.append("--check")
# 15 
# 16         proc = subprocess.run(
# 17             cmd,
# 18             capture_output=True,
# 19             text=True,
# 20             encoding="utf-8",
# 21             errors="ignore"
# 22         )
# 23 
# 24         self._log.debug(proc.stdout)
# 25         if proc.stderr:
# 26             self._log.error(proc.stderr)
# 27 
# 28         result = {
# 29             "stdout": proc.stdout,
# 30             "stderr": proc.stderr,
# 31             "return_code": proc.returncode
# 32         }
# 33 
# 34         if proc.returncode != 0:
# 35             raise RuntimeError(f"black failed: {proc.stderr}")
# 36 
# 37         return json.dumps(result)


# db\seeders\files\tool_providers\docformatter_tool.py

# 1 import subprocess
# 2 import sys
# 3 import json
# 4 from app.providers.tool_provider_base import ToolProviderBase
# 5 
# 6 class DocFormatterToolProvider(ToolProviderBase):
# 7     def _run(self, input: dict) -> str:
# 8         target = input.get("target")
# 9         check = input.get("check", False)
# 10 
# 11         cmd = [sys.executable, "-m", "docformatter", target, "--in-place"]
# 12         if check:
# 13             cmd.append("--check")
# 14 
# 15         proc = subprocess.run(cmd, capture_output=True, text=True)
# 16 
# 17         if proc.stdout:
# 18             self._log.debug(proc.stdout)
# 19         if proc.stderr:
# 20             self._log.error(proc.stderr)
# 21 
# 22         result = {
# 23             "stdout": proc.stdout,
# 24             "stderr": proc.stderr,
# 25             "return_code": proc.returncode
# 26         }
# 27 
# 28         if proc.returncode != 0:
# 29             raise RuntimeError(f"docformatter failed: {proc.stderr}")
# 30 
# 31         return json.dumps(result)


# db\seeders\files\tool_providers\mypy_tool.py

# 1 import subprocess
# 2 import sys
# 3 import json
# 4 from app.providers.tool_provider_base import ToolProviderBase
# 5 
# 6 class MypyToolProvider(ToolProviderBase):
# 7     def _run(self, input: dict) -> str:
# 8         target = input.get("target")
# 9 
# 10         cmd = [sys.executable, "-m", "mypy", target]
# 11         proc = subprocess.run(cmd, capture_output=True, text=True)
# 12 
# 13         if proc.stdout:
# 14             self._log.debug(proc.stdout)
# 15         if proc.stderr:
# 16             self._log.error(proc.stderr)
# 17 
# 18         result = {
# 19             "stdout": proc.stdout,
# 20             "stderr": proc.stderr,
# 21             "return_code": proc.returncode
# 22         }
# 23 
# 24         if proc.returncode not in (0, 1):  #  1 means lint issues; not failure
# 25             raise RuntimeError(f"mypy execution error: {proc.stderr}")
# 26 
# 27         return json.dumps(result)


# db\seeders\files\tool_providers\radon_tool.py

# 1 import subprocess
# 2 import sys
# 3 import json
# 4 from pathlib import Path
# 5 from app.providers.tool_provider_base import ToolProviderBase
# 6 
# 7 class RadonToolProvider(ToolProviderBase):
# 8     def _run(self, input: dict) -> str:
# 9         target = input.get("target")
# 10         if not Path(target).exists():
# 11             raise FileNotFoundError(f"{target} not found")
# 12 
# 13         cmd = [sys.executable, "-m", "radon", "cc", target]
# 14         proc = subprocess.run(cmd, capture_output=True, text=True)
# 15 
# 16         if proc.returncode != 0:
# 17             raise RuntimeError(f"radon failed: {proc.stderr.strip()}")
# 18 
# 19         result = {
# 20             "stdout": proc.stdout,
# 21             "stderr": proc.stderr,
# 22             "return_code": proc.returncode
# 23         }
# 24 
# 25         return json.dumps(result)


# db\seeders\files\tool_providers\ruff_tool.py

# 1 import subprocess
# 2 import sys
# 3 import json
# 4 from app.providers.tool_provider_base import ToolProviderBase
# 5 
# 6 class RuffToolProvider(ToolProviderBase):
# 7     def _run(self, input: dict) -> str:
# 8         target = input.get("target")
# 9 
# 10         cmd = [sys.executable, "-m", "ruff", "check", target]
# 11         proc = subprocess.run(cmd, capture_output=True, text=True)
# 12 
# 13         if proc.stdout:
# 14             self._log.debug(proc.stdout)
# 15         if proc.stderr:
# 16             self._log.error(proc.stderr)
# 17 
# 18         #  Interpret output
# 19         result = {
# 20             "stdout": proc.stdout,
# 21             "stderr": proc.stderr,
# 22             "return_code": proc.returncode,
# 23         }
# 24 
# 25         #  Only raise if ruff failed due to actual error (not rule violations)
# 26         if proc.returncode > 1:
# 27             self._log.error(f"❌ Ruff failed: {proc.stderr or proc.stdout or 'unknown error'}")
# 28             raise RuntimeError(f"ruff execution error: {proc.stderr or proc.stdout or 'unknown error'}")
# 29 
# 30         #  Log structured info for visibility
# 31         self._log.debug(f"✅ Ruff completed with return code {proc.returncode}")
# 32         self._log.debug(json.dumps(result, indent=2))
# 33 
# 34         return json.dumps(result)


# db\seeders\files\tool_providers\sonarcloud_tool.py

# 1 from __future__ import annotations
# 2 import subprocess
# 3 from pathlib import Path
# 4 from app.providers.tool_provider_base import ToolProviderBase
# 5 
# 6 class SonarCloudToolProvider(ToolProviderBase):
# 7     def _run(self, target: str) -> subprocess.CompletedProcess:
# 8         if not Path(target).exists():
# 9             raise FileNotFoundError(f"{target} does not exist")
# 10 
# 11         return subprocess.CompletedProcess(
# 12             args=["sonarcloud", target],
# 13             returncode=0,
# 14             stdout="{}",
# 15             stderr="",
# 16         )


# db\seeders\files\tool_providers\symbol_graph.py

# 1 from __future__ import annotations
# 2 
# 3 import ast
# 4 import json
# 5 from pathlib import Path
# 6 import subprocess
# 7 from typing import Any, Dict, List, MutableMapping
# 8 
# 9 from app.providers.tool_provider_base import ToolProviderBase
# 10 
# 11 
# 12 class SymbolGraph:
# 13     def __init__(self) -> None:
# 14         self.graph = {}
# 15 
# 16     def parse_file(self, filepath: str | Path) -> None:
# 17         from ast import parse
# 18         filepath = Path(filepath)
# 19         source = filepath.read_text(encoding="utf-8")
# 20         tree = parse(source, filename=str(filepath))
# 21 
# 22         visitor = _SymbolGraphVisitor(filepath.stem, str(filepath), self.graph)
# 23         visitor.visit(tree)
# 24 
# 25 class SymbolGraphToolProvider(ToolProviderBase):
# 26     def _run(self, input: dict) -> str:
# 27         target = input.get("target")
# 28         if not Path(target).exists():
# 29             raise FileNotFoundError(f"{target} does not exist")
# 30 
# 31         symbol_graph_util = SymbolGraph()
# 32         symbol_graph_util.parse_file(target)
# 33         return json.dumps(symbol_graph_util.graph, indent=2)
# 34 
# 35 
# 36 class _SymbolGraphVisitor(ast.NodeVisitor):
# 37     def __init__(self, module: str, file_path: str, graph: MutableMapping[str, Dict[str, Any]]) -> None:
# 38         self.module = module
# 39         self.file_path = file_path
# 40         self.graph = graph
# 41         self.scope: List[str] = []
# 42         self.current: str | None = None
# 43 
# 44     def visit_FunctionDef(self, node: ast.FunctionDef) -> None:
# 45         self._process_function_or_async_function(node)
# 46         self.generic_visit(node)
# 47         self.scope.pop()
# 48         self.current = None
# 49 
# 50     def visit_AsyncFunctionDef(self, node: ast.AsyncFunctionDef) -> None:
# 51         self._process_function_or_async_function(node)
# 52         self.generic_visit(node)
# 53         self.scope.pop()
# 54         self.current = None
# 55 
# 56     def _process_function_or_async_function(self, node: ast.FunctionDef | ast.AsyncFunctionDef) -> None:
# 57         qual = self._qualify(node.name)
# 58         self.graph[qual] = {
# 59             "name": node.name,
# 60             "type": "async_function" if isinstance(node, ast.AsyncFunctionDef) else "function",
# 61             "file": self.file_path,
# 62             "lineno": node.lineno,
# 63             "col_offset": node.col_offset,
# 64             "end_lineno": getattr(node, "end_lineno", node.lineno),
# 65             "end_col_offset": getattr(node, "end_col_offset", node.col_offset),
# 66             "scope": ".".join([self.module, *self.scope]) if self.scope else self.module,
# 67             "calls": [],
# 68         }
# 69         self.scope.append(node.name)
# 70         self.current = qual
# 71 
# 72     def visit_ClassDef(self, node: ast.ClassDef) -> None:
# 73         qual = self._qualify(node.name)
# 74         self._record(qual, "class", node)
# 75         self.scope.append(node.name)
# 76         self.current = qual
# 77         self.generic_visit(node)
# 78         self.scope.pop()
# 79         self.current = None
# 80 
# 81     def visit_Assign(self, node: ast.Assign) -> None:
# 82         for target in node.targets:
# 83             if isinstance(target, ast.Name):
# 84                 self._record(self._qualify(target.id), "variable", target)
# 85         self.generic_visit(node)
# 86 
# 87     def visit_Import(self, node: ast.Import) -> None:
# 88         for alias in node.names:
# 89             name = alias.asname or alias.name
# 90             entry = self._record(self._qualify(name), "import", node)
# 91             entry["target"] = alias.name
# 92         self.generic_visit(node)
# 93 
# 94     def visit_ImportFrom(self, node: ast.ImportFrom) -> None:
# 95         for alias in node.names:
# 96             name = alias.asname or alias.name
# 97             entry = self._record(self._qualify(name), "import", node)
# 98             entry["target"] = f"{node.module}.{alias.name}" if node.module else alias.name
# 99         self.generic_visit(node)
# 100 
# 101     def visit_Call(self, node: ast.Call) -> None:
# 102         current = self.current or self.module
# 103         if isinstance(node.func, ast.Name):
# 104             called_name = node.func.id
# 105         elif isinstance(node.func, ast.Attribute):
# 106             called_name = node.func.attr
# 107         else:
# 108             called_name = None
# 109 
# 110         if called_name:
# 111             self.graph.setdefault(current, {}).setdefault("calls", []).append(called_name)
# 112         self.generic_visit(node)
# 113 
# 114     def _qualify(self, name: str) -> str:
# 115         return ".".join([self.module, *self.scope, name])
# 116 
# 117     def _record(self, qual: str, typ: str, node: ast.AST) -> Dict[str, Any]:
# 118         info = {
# 119             "type": typ,
# 120             "file": self.file_path,
# 121             "lineno": getattr(node, "lineno", None),
# 122             "col_offset": getattr(node, "col_offset", None),
# 123             "end_lineno": getattr(node, "end_lineno", getattr(node, "lineno", None)),
# 124             "end_col_offset": getattr(node, "end_col_offset", getattr(node, "col_offset", None)),
# 125             "scope": ".".join([self.module, *self.scope]) if self.scope else self.module,
# 126             "calls": [],
# 127         }
# 128         self.graph[qual] = info
# 129         return info


# db\seeders\seed_agent_engine_providers.py

# 1 from uuid import uuid4
# 2 from pathlib import Path
# 3 import shutil
# 4 from sqlalchemy.orm import Session
# 5 from app.db.models import AgentEngineProviderConfig
# 6 
# 7 SEED_FILES_DIR = Path(__file__).resolve().parent / "files/agent_engine_providers"
# 8 PROJECT_ROOT = SEED_FILES_DIR.parent.parent.parent.parent.parent
# 9 EXTENSIONS_DIR = PROJECT_ROOT / "extensions"
# 10 
# 11 AGENT_ENGINES = [
# 12     (1, "basic_agent_engine_provider.py", "basic_agent_engine_provider", "Returns a mock LLM response.", "mock-llm", ["default"]),
# 13     (2, "openai_gpt4o_agent_engine_provider.py", "openai_gpt_4o_agent_engine", "Runs GPT-4o via OpenAI API.", "gpt-4o", ["openai", "production"])
# 14 ]
# 15 
# 16 def seed_agent_engine_providers(db_session: Session):
# 17     EXTENSIONS_DIR.mkdir(parents=True, exist_ok=True)
# 18 
# 19     for id_, filename, name, description, model, tags in AGENT_ENGINES:
# 20         guid = str(uuid4())
# 21         source_file = SEED_FILES_DIR / filename
# 22         dest_file = EXTENSIONS_DIR / f"{guid}.py"
# 23 
# 24         if not source_file.exists():
# 25             raise FileNotFoundError(f"Agent engine script not found: {source_file}")
# 26 
# 27         shutil.copy(source_file, dest_file)
# 28 
# 29         config = AgentEngineProviderConfig(
# 30             id=id_,
# 31             guid=guid,
# 32             name=name,
# 33             description=description,
# 34             model=model,
# 35             config={},
# 36             artifact_path=str(dest_file),
# 37             tags=tags,
# 38         )
# 39 
# 40         db_session.add(config)
# 41 
# 42     db_session.commit()
# 43     print("Seeded agent engine configurations successfully.")


# db\seeders\seed_agent_providers.py

# 1 from uuid import uuid4
# 2 from pathlib import Path
# 3 import shutil
# 4 from sqlalchemy.orm import Session
# 5 from app.db.models import AgentProviderConfig
# 6 
# 7 SEED_FILES_DIR = Path(__file__).resolve().parent / "files/agent_providers"
# 8 PROJECT_ROOT = SEED_FILES_DIR.parent.parent.parent.parent.parent
# 9 EXTENSIONS_DIR = PROJECT_ROOT / "extensions"
# 10 
# 11 AGENT_PROVIDERS = [
# 12     {
# 13         "id": 1,
# 14         "filename": "basic_agent_provider.py",
# 15         "name": "basic_agent_provider",
# 16         "description": "Returns a hardcoded result.",
# 17         "tags": ["test"],
# 18         "config": {}
# 19     },
# 20     {
# 21         "id": 2,
# 22         "filename": "linting_generator_agent_provider.py",
# 23         "name": "linting_generator_agent_provider",
# 24         "description": "Runs GPT-4o to generate linting corrections.",
# 25         "tags": ["linting", "generator"],
# 26         "config": {
# 27             "agent_engine_provider_id": 2,
# 28             "prompt_provider_id": 2,
# 29             "context_provider_id": 2,
# 30             "score_provider_id": 1,
# 31             "tool_provider_ids": {
# 32                 "black": 1,
# 33                 "ruff": 3,
# 34                 "mypy": 5,
# 35                 "radon": 4
# 36             }
# 37         }
# 38     },
# 39     {
# 40         "id": 3,
# 41         "filename": "linting_discriminator_agent_provider.py",
# 42         "name": "linting_discriminator_agent_provider",
# 43         "description": "Evaluates generator output for acceptance.",
# 44         "tags": ["linting", "discriminator"],
# 45         "config": {
# 46             "score_provider_id": 1
# 47         }
# 48     }
# 49 ]
# 50 
# 51 def seed_agent_providers(db_session: Session):
# 52     EXTENSIONS_DIR.mkdir(parents=True, exist_ok=True)
# 53 
# 54     for entry in AGENT_PROVIDERS:
# 55         guid = str(uuid4())
# 56         dest_filename = f"{guid}.py"
# 57         source_file = SEED_FILES_DIR / entry["filename"]
# 58         dest_file = EXTENSIONS_DIR / dest_filename
# 59 
# 60         if not source_file.exists():
# 61             raise FileNotFoundError(f"Agent provider script not found: {source_file}")
# 62 
# 63         shutil.copy(source_file, dest_file)
# 64 
# 65         config = AgentProviderConfig(
# 66             id=entry["id"],
# 67             guid=guid,
# 68             name=entry["name"],
# 69             description=entry["description"],
# 70             artifact_path=dest_filename,
# 71             tags=entry["tags"],
# 72             config=entry["config"]
# 73         )
# 74 
# 75         db_session.add(config)
# 76 
# 77     db_session.commit()
# 78     print("Seeded agent provider configurations successfully.")


# db\seeders\seed_context_providers.py

# 1 from uuid import uuid4
# 2 from pathlib import Path
# 3 import shutil
# 4 from sqlalchemy.orm import Session
# 5 from app.db.models import ContextProviderConfig
# 6 
# 7 SEED_FILES_DIR = Path(__file__).resolve().parent / "files/context_providers"
# 8 PROJECT_ROOT = SEED_FILES_DIR.parent.parent.parent.parent.parent
# 9 EXTENSIONS_DIR = PROJECT_ROOT / "extensions"
# 10 
# 11 CONTEXT_PROVIDERS = [
# 12     {
# 13         "id": 1,
# 14         "filename": "basic_context_provider.py",
# 15         "name": "basic_context_provider",
# 16         "description": "Returns static context for testing.",
# 17         "tags": ["default"],
# 18         "config": {}
# 19     },
# 20     {
# 21         "id": 2,
# 22         "filename": "linting_context_provider.py",
# 23         "name": "linting_context_provider",
# 24         "description": "Generates context for the linting system.",
# 25         "tags": ["linting", "score-aware"],
# 26         "config": {
# 27             "score_provider_id": 1,
# 28             "tool_provider_ids": None
# 29         }
# 30     }
# 31 ]
# 32 
# 33 def seed_context_providers(db_session: Session):
# 34     EXTENSIONS_DIR.mkdir(parents=True, exist_ok=True)
# 35 
# 36     for entry in CONTEXT_PROVIDERS:
# 37         guid = str(uuid4())
# 38         dest_filename = f"{guid}.py"
# 39         source_file = SEED_FILES_DIR / entry["filename"]
# 40         dest_file = EXTENSIONS_DIR / dest_filename
# 41 
# 42         if not source_file.exists():
# 43             raise FileNotFoundError(f"Context provider script not found: {source_file}")
# 44 
# 45         shutil.copy(source_file, dest_file)
# 46 
# 47         config = ContextProviderConfig(
# 48             id=entry["id"],
# 49             guid=guid,
# 50             name=entry["name"],
# 51             description=entry["description"],
# 52             artifact_path=dest_filename,
# 53             tags=entry["tags"],
# 54             config=entry["config"]
# 55         )
# 56 
# 57         db_session.add(config)
# 58 
# 59     db_session.commit()
# 60     print("Seeded context provider configurations successfully.")


# db\seeders\seed_controller_providers.py

# 1 #  app/db/seeders/seed_controller_providers.py
# 2 
# 3 from uuid import uuid4
# 4 from pathlib import Path
# 5 import shutil
# 6 from sqlalchemy.orm import Session
# 7 from app.db.models import ControllerProviderConfig
# 8 
# 9 SEED_FILES_DIR = Path(__file__).parent / "files/controller_providers"
# 10 PROJECT_ROOT   = SEED_FILES_DIR.parents[4]
# 11 EXTENSIONS_DIR = PROJECT_ROOT / "extensions"
# 12 
# 13 CONTROLLER_PROVIDERS = [
# 14     {
# 15         "id": 1,
# 16         "filename": "preprocessing_controller_provider.py",
# 17         "name": "preprocessing_controller",
# 18         "description": "Runs only the preprocessing step (e.g. linting).",
# 19         "tags": ["preprocessing", "controller"],
# 20         "config": {
# 21             "systems": {
# 22                 "preprocess": 1   #  ID of your linting_system_provider
# 23             }
# 24         }
# 25     }
# 26 ]
# 27 
# 28 def seed_controller_providers(db_session: Session):
# 29     EXTENSIONS_DIR.mkdir(parents=True, exist_ok=True)
# 30     for entry in CONTROLLER_PROVIDERS:
# 31         guid      = str(uuid4())
# 32         src       = SEED_FILES_DIR / entry["filename"]
# 33         dest      = EXTENSIONS_DIR / f"{guid}.py"
# 34         if not src.exists():
# 35             raise FileNotFoundError(f"Missing script: {src}")
# 36         shutil.copy(src, dest)
# 37 
# 38         rec = ControllerProviderConfig(
# 39             id            = entry["id"],
# 40             guid          = guid,
# 41             name          = entry["name"],
# 42             description   = entry["description"],
# 43             config        = entry["config"],
# 44             artifact_path = dest.name,
# 45             tags          = entry["tags"],
# 46         )
# 47         db_session.add(rec)
# 48     db_session.commit()
# 49     print("✅ Seeded controller providers")


# db\seeders\seed_program_provider.py

# 1 #  app/db/seeders/seed_program_providers.py
# 2 
# 3 from uuid import uuid4
# 4 from pathlib import Path
# 5 import shutil
# 6 from sqlalchemy.orm import Session
# 7 from app.db.models import ProgramProviderConfig
# 8 
# 9 SEED_FILES_DIR = Path(__file__).resolve().parent / "files/program_providers"
# 10 PROJECT_ROOT   = SEED_FILES_DIR.parents[4]
# 11 EXTENSIONS_DIR = PROJECT_ROOT / "extensions"
# 12 
# 13 PROGRAM_PROVIDERS = [
# 14     {
# 15         "id": 1,
# 16         "filename": "codecritic_program_provider.py",
# 17         "name": "codecritic_program",
# 18         "description": "Top-level orchestration of all system controllers.",
# 19         "tags": ["program", "codecritic"],
# 20         "config": {
# 21             "controllers": {
# 22                 "preprocessing": 1
# 23             }
# 24         }
# 25     }
# 26 ]
# 27 
# 28 def seed_program_providers(db_session: Session):
# 29     EXTENSIONS_DIR.mkdir(parents=True, exist_ok=True)
# 30 
# 31     for entry in PROGRAM_PROVIDERS:
# 32         guid = str(uuid4())
# 33         src  = SEED_FILES_DIR / entry["filename"]
# 34         dst  = EXTENSIONS_DIR / f"{guid}.py"
# 35 
# 36         if not src.exists():
# 37             raise FileNotFoundError(f"❌ Missing file: {src}")
# 38 
# 39         shutil.copy(src, dst)
# 40 
# 41         record = ProgramProviderConfig(
# 42             id            = entry["id"],
# 43             guid          = guid,
# 44             name          = entry["name"],
# 45             description   = entry["description"],
# 46             config        = entry["config"],
# 47             artifact_path = dst.name,
# 48             tags          = entry["tags"],
# 49         )
# 50         db_session.add(record)
# 51 
# 52     db_session.commit()
# 53     print("✅ Seeded program providers")


# db\seeders\seed_prompt_providers.py

# 1 from uuid import uuid4
# 2 from pathlib import Path
# 3 import shutil
# 4 from sqlalchemy.orm import Session
# 5 from app.db.models import PromptProviderConfig
# 6 
# 7 CURRENT_DIR = Path(__file__).resolve().parent
# 8 SEED_FILES_DIR = CURRENT_DIR / "files/prompt_providers"
# 9 PROJECT_ROOT = SEED_FILES_DIR.parent.parent.parent.parent.parent
# 10 
# 11 EXTENSIONS_DIR = PROJECT_ROOT / "extensions"
# 12 
# 13 PROVIDERS = [
# 14     {
# 15         "id": 1,
# 16         "filename": "basic_prompt_provider.py",
# 17         "name": "basic_prompt_provider",
# 18         "description": "Returns a simple prompt response.",
# 19         "tags": ["default"],
# 20         "config": {}
# 21     },
# 22     {
# 23         "id": 2,
# 24         "filename": "linting_prompt_provider.py",
# 25         "name": "linting_prompt_provider",
# 26         "description": "Combines linting system and agent prompts.",
# 27         "tags": ["linting", "codequality"],
# 28         "config": {
# 29             "agent_prompt_id": 2,
# 30             "system_prompt_id": 2,
# 31             "context_provider_id": 2
# 32         }
# 33     }
# 34 ]
# 35 
# 36 
# 37 def seed_prompt_providers(db_session: Session):
# 38     EXTENSIONS_DIR.mkdir(parents=True, exist_ok=True)
# 39 
# 40     for entry in PROVIDERS:
# 41         guid = str(uuid4())
# 42         dest_filename = f"{guid}.py"
# 43         source_file = SEED_FILES_DIR / entry["filename"]
# 44         dest_file = EXTENSIONS_DIR / dest_filename
# 45 
# 46         if not source_file.exists():
# 47             raise FileNotFoundError(f"Prompt provider script not found: {source_file}")
# 48 
# 49         shutil.copy(source_file, dest_file)
# 50 
# 51         config = PromptProviderConfig(
# 52             id=entry["id"],
# 53             guid=guid,
# 54             name=entry["name"],
# 55             description=entry["description"],
# 56             config=entry["config"],
# 57             artifact_path=dest_filename,
# 58             tags=entry["tags"]
# 59         )
# 60 
# 61         db_session.add(config)
# 62 
# 63     db_session.commit()
# 64     print("Seeded prompt provider configurations successfully.")


# db\seeders\seed_prompts.py

# 1 from pathlib import Path
# 2 import shutil
# 3 from sqlalchemy.orm import Session
# 4 from uuid import uuid4
# 5 from app.db.models import AgentPrompt, SystemPrompt
# 6 from app.enums.system_enums import SystemType
# 7 
# 8 #  Define paths relative to this script's file location explicitly
# 9 CURRENT_DIR = Path(__file__).resolve().parent
# 10 SEED_FILES_DIR = CURRENT_DIR / "files/prompts"
# 11 PROJECT_ROOT = CURRENT_DIR.parent.parent.parent
# 12 EXTENSIONS_DIR = PROJECT_ROOT / "extensions"
# 13 
# 14 #  Define new prompt files to seed
# 15 AGENT_PROMPT_FILES = [
# 16     {
# 17         "id": 1,
# 18         "name": "generate",
# 19         "filename": "basic_agent_prompt.txt",
# 20         "description": "Prompt for basic code generation.",
# 21         "tags": ["linting", "generator"]
# 22     },
# 23     {
# 24         "id": 2,
# 25         "name": "linting_generator_agent",
# 26         "filename": "linting_generator_agent_prompt.txt",
# 27         "description": "Prompt for linting generator agent.",
# 28         "tags": ["linting", "generator"]
# 29     }
# 30 ]
# 31 
# 32 SYSTEM_PROMPT_FILES = [
# 33     {
# 34         "id": 1,
# 35         "name": "format",
# 36         "filename": "default_system_prompt.txt",
# 37         "system_type": SystemType.LINTING,
# 38         "description": "Default prompt for formatting tasks.",
# 39         "tags": ["formatting", "default"]
# 40     },
# 41     {
# 42         "id": 2,
# 43         "name": "linting_system",
# 44         "filename": "linting_system_prompt.txt",
# 45         "system_type": SystemType.LINTING,
# 46         "description": "System prompt governing the linting subsystem.",
# 47         "tags": ["linting", "system"]
# 48     }
# 49 ]
# 50 
# 51 
# 52 def seed_prompts(db_session: Session):
# 53     EXTENSIONS_DIR.mkdir(parents=True, exist_ok=True)
# 54 
# 55     for entry in AGENT_PROMPT_FILES:
# 56         source_file = SEED_FILES_DIR / entry["filename"]
# 57         if not source_file.exists():
# 58             raise FileNotFoundError(f"Agent prompt source file not found: {source_file}")
# 59 
# 60         guid = str(uuid4())
# 61         dest_filename = f"{guid}.txt"
# 62         dest_file = EXTENSIONS_DIR / dest_filename
# 63         shutil.copy(source_file, dest_file)
# 64 
# 65         prompt = AgentPrompt(
# 66             id=entry["id"],
# 67             guid=guid,
# 68             name=entry["name"],
# 69             description=entry["description"],
# 70             artifact_path=dest_filename,  #  Store relative path
# 71             tags=entry["tags"]
# 72         )
# 73         db_session.add(prompt)
# 74         print(f"Seeded AgentPrompt '{entry['name']}' with ID: {entry['id']} and GUID: {guid}")
# 75 
# 76     for entry in SYSTEM_PROMPT_FILES:
# 77         source_file = SEED_FILES_DIR / entry["filename"]
# 78         if not source_file.exists():
# 79             raise FileNotFoundError(f"System prompt source file not found: {source_file}")
# 80 
# 81         guid = str(uuid4())
# 82         dest_filename = f"{guid}.txt"
# 83         dest_file = EXTENSIONS_DIR / dest_filename
# 84         shutil.copy(source_file, dest_file)
# 85 
# 86         prompt = SystemPrompt(
# 87             id=entry["id"],
# 88             guid=guid,
# 89             name=entry["name"],
# 90             system_type=entry["system_type"],
# 91             description=entry["description"],
# 92             artifact_path=dest_filename,  #  Store relative path
# 93             tags=entry["tags"]
# 94         )
# 95         db_session.add(prompt)
# 96         print(f"Seeded SystemPrompt '{entry['name']}' with ID: {entry['id']} and GUID: {guid}")
# 97 
# 98     db_session.commit()


# db\seeders\seed_score_providers.py

# 1 from uuid import uuid4
# 2 from pathlib import Path
# 3 import shutil
# 4 from sqlalchemy.orm import Session
# 5 from app.db.models import ScoreProviderConfig
# 6 
# 7 CURRENT_DIR = Path(__file__).resolve().parent
# 8 SEED_FILES_DIR = CURRENT_DIR / "files/score_providers"
# 9 PROJECT_ROOT = SEED_FILES_DIR.parent.parent.parent.parent.parent
# 10 EXTENSIONS_DIR = PROJECT_ROOT / "extensions"
# 11 
# 12 SCORES = [
# 13     {
# 14         "id": 1,
# 15         "filename": "linting_score_provider.py",
# 16         "name": "linting_score_provider",
# 17         "description": "Scores lint compliance.",
# 18         "config": {
# 19             "tool_provider_ids": {
# 20                 "black": 1,
# 21                 "ruff": 3,
# 22                 "mypy": 5,
# 23                 "radon": 4
# 24             },
# 25             "context_provider_id": None
# 26         }
# 27     }
# 28 ]
# 29 
# 30 def seed_score_providers(db_session: Session):
# 31     EXTENSIONS_DIR.mkdir(parents=True, exist_ok=True)
# 32 
# 33     for entry in SCORES:
# 34         guid = str(uuid4())
# 35         dest_filename = f"{guid}.py"
# 36         source_file = SEED_FILES_DIR / entry["filename"]
# 37         dest_file = EXTENSIONS_DIR / dest_filename
# 38 
# 39         if not source_file.exists():
# 40             raise FileNotFoundError(f"Score provider script not found: {source_file}")
# 41 
# 42         shutil.copy(source_file, dest_file)
# 43 
# 44         score = ScoreProviderConfig(
# 45             id=entry["id"],
# 46             guid=guid,
# 47             name=entry["name"],
# 48             description=entry["description"],
# 49             config=entry["config"],
# 50             artifact_path=dest_filename
# 51         )
# 52 
# 53         db_session.add(score)
# 54 
# 55     db_session.commit()
# 56     print("Seeded score providers successfully.")


# db\seeders\seed_session_configs.py

# 1 #  app/db/seeders/seed_session_configs.py
# 2 
# 3 from sqlalchemy.orm import Session
# 4 from app.db.models import SessionConfig
# 5 
# 6 SESSION_CONFIGS = [
# 7     {
# 8         "id": 1,
# 9         "program_provider_id": 1,
# 10         "name": "codecritic_test_session",
# 11         "description": "Test session for program 1",
# 12         "environment_type": "experiment",
# 13         "tags": ["test", "experiment"]
# 14     }
# 15 ]
# 16 
# 17 def seed_session_configs(db_session: Session):
# 18     for sess in SESSION_CONFIGS:
# 19         record = SessionConfig(
# 20             id               = sess["id"],
# 21             program_provider_id = sess["program_provider_id"],
# 22             name             = sess["name"],
# 23             description      = sess["description"],
# 24             environment_type = sess["environment_type"],
# 25             tags             = sess["tags"],
# 26         )
# 27         db_session.add(record)
# 28 
# 29     db_session.commit()
# 30     print("✅ Seeded session configs")


# db\seeders\seed_state_providers.py

# 1 from uuid import uuid4
# 2 from pathlib import Path
# 3 import shutil
# 4 from sqlalchemy.orm import Session
# 5 from app.db.models import StateProviderConfig
# 6 
# 7 SEED_FILES_DIR = Path(__file__).resolve().parent / "files/state_providers"
# 8 PROJECT_ROOT = SEED_FILES_DIR.parent.parent.parent.parent.parent
# 9 EXTENSIONS_DIR = PROJECT_ROOT / "extensions"
# 10 
# 11 STATE_PROVIDERS = [
# 12     (
# 13         1,
# 14         "linting_generator_state_provider.py",
# 15         "LintingGeneratorStateProvider",
# 16         "Runs the generator agent in its own FSM wrapper.",
# 17         ["linting", "generator"],
# 18         {
# 19             "agents": {
# 20                 "generate": 2
# 21             }
# 22         }
# 23     ),
# 24     (
# 25         2,
# 26         "linting_discriminator_state_provider.py",
# 27         "LintingDiscriminatorStateProvider",
# 28         "Runs the discriminator agent in its own FSM wrapper.",
# 29         ["linting", "discriminator"],
# 30         {
# 31             "agents": {
# 32                 "discriminate": 3
# 33             }
# 34         }
# 35     ),
# 36 ]
# 37 
# 38 def seed_state_providers(db_session: Session):
# 39     EXTENSIONS_DIR.mkdir(parents=True, exist_ok=True)
# 40 
# 41     for id, filename, name, description, tags, config in STATE_PROVIDERS:
# 42         guid = str(uuid4())
# 43         source_file = SEED_FILES_DIR / filename
# 44         dest_filename = f"{guid}.py"
# 45         dest_file = EXTENSIONS_DIR / dest_filename
# 46 
# 47         if not source_file.exists():
# 48             raise FileNotFoundError(f"State provider script not found: {source_file}")
# 49 
# 50         shutil.copy(source_file, dest_file)
# 51 
# 52         record = StateProviderConfig(
# 53             id=id,
# 54             guid=guid,
# 55             name=name,
# 56             description=description,
# 57             config=config,
# 58             artifact_path=dest_filename,
# 59             tags=tags,
# 60         )
# 61 
# 62         db_session.add(record)
# 63 
# 64     db_session.commit()
# 65     print("✅ Seeded state provider configurations successfully.")


# db\seeders\seed_system_providers.py

# 1 from uuid import uuid4
# 2 from pathlib import Path
# 3 import shutil
# 4 from sqlalchemy.orm import Session
# 5 from app.db.models import SystemProviderConfig
# 6 
# 7 SEED_FILES_DIR = Path(__file__).resolve().parent / "files/system_providers"
# 8 PROJECT_ROOT = SEED_FILES_DIR.parent.parent.parent.parent.parent
# 9 EXTENSIONS_DIR = PROJECT_ROOT / "extensions"
# 10 
# 11 SYSTEM_PROVIDERS = [
# 12     {
# 13         "id": 1,
# 14         "filename": "linting_system_provider.py",
# 15         "name": "linting_system_provider",
# 16         "description": "System to iteratively lint and evaluate Python code.",
# 17         "tags": ["linting", "system"],
# 18         "config": {
# 19             "states": {
# 20                 "generate": 1,     
# 21                 "discriminate": 2  
# 22             }
# 23         }
# 24     }
# 25 ]
# 26 
# 27 def seed_system_providers(db_session: Session):
# 28     EXTENSIONS_DIR.mkdir(parents=True, exist_ok=True)
# 29 
# 30     for entry in SYSTEM_PROVIDERS:
# 31         guid = str(uuid4())
# 32         source_file = SEED_FILES_DIR / entry["filename"]
# 33         dest_filename = f"{guid}.py"
# 34         dest_file = EXTENSIONS_DIR / dest_filename
# 35 
# 36         if not source_file.exists():
# 37             raise FileNotFoundError(f"System provider script not found: {source_file}")
# 38 
# 39         shutil.copy(source_file, dest_file)
# 40 
# 41         record = SystemProviderConfig(
# 42             id=entry["id"],
# 43             guid=guid,
# 44             name=entry["name"],
# 45             description=entry["description"],
# 46             config=entry["config"],
# 47             artifact_path=dest_filename,
# 48             tags=entry["tags"],
# 49         )
# 50 
# 51         db_session.add(record)
# 52 
# 53     db_session.commit()
# 54     print("Seeded system provider configurations successfully.")


# db\seeders\seed_tool_providers.py

# 1 from uuid import uuid4
# 2 from pathlib import Path
# 3 import shutil
# 4 from sqlalchemy.orm import Session
# 5 from app.db.models import ToolProviderConfig
# 6 
# 7 CURRENT_DIR = Path(__file__).resolve().parent
# 8 SEED_FILES_DIR = CURRENT_DIR / "files/tool_providers"
# 9 PROJECT_ROOT = SEED_FILES_DIR.parent.parent.parent.parent.parent
# 10 EXTENSIONS_DIR = PROJECT_ROOT / "extensions"
# 11 
# 12 TOOLS = [
# 13     {
# 14         "id": 1,
# 15         "filename": "black_tool.py",
# 16         "name": "black",
# 17         "description": "Formats Python code using Black.",
# 18         "config": {}
# 19     },
# 20     {
# 21         "id": 2,
# 22         "filename": "sonarcloud_tool.py",
# 23         "name": "sonarcloud",
# 24         "description": "Static analysis via SonarCloud.",
# 25         "config": {}
# 26     },
# 27     {
# 28         "id": 3,
# 29         "filename": "ruff_tool.py",
# 30         "name": "ruff",
# 31         "description": "Python linting with Ruff.",
# 32         "config": {}
# 33     },
# 34     {
# 35         "id": 4,
# 36         "filename": "radon_tool.py",
# 37         "name": "radon",
# 38         "description": "Analyzes Python code complexity.",
# 39         "config": {}
# 40     },
# 41     {
# 42         "id": 5,
# 43         "filename": "mypy_tool.py",
# 44         "name": "mypy",
# 45         "description": "Static type checking with mypy.",
# 46         "config": {}
# 47     },
# 48     {
# 49         "id": 6,
# 50         "filename": "docformatter_tool.py",
# 51         "name": "docformatter",
# 52         "description": "Formats docstrings using docformatter.",
# 53         "config": {}
# 54     },
# 55     {
# 56         "id": 7,
# 57         "filename": "symbol_graph.py",
# 58         "name": "symbol_graph",
# 59         "description": "Extracts and analyzes Python symbols into structured graphs.",
# 60         "config": {}
# 61     }
# 62 ]
# 63 
# 64 def seed_tool_providers(db_session: Session):
# 65     EXTENSIONS_DIR.mkdir(parents=True, exist_ok=True)
# 66 
# 67     for entry in TOOLS:
# 68         guid = str(uuid4())
# 69         dest_filename = f"{guid}.py"
# 70         source_file = SEED_FILES_DIR / entry["filename"]
# 71         dest_file = EXTENSIONS_DIR / dest_filename
# 72 
# 73         if not source_file.exists():
# 74             raise FileNotFoundError(f"Tool script not found: {source_file}")
# 75 
# 76         shutil.copy(source_file, dest_file)
# 77 
# 78         tool = ToolProviderConfig(
# 79             id=entry["id"],
# 80             guid=guid,
# 81             name=entry["name"],
# 82             description=entry["description"],
# 83             config=entry["config"],
# 84             artifact_path=dest_filename
# 85         )
# 86 
# 87         db_session.add(tool)
# 88 
# 89     db_session.commit()
# 90     print("Seeded tool configurations successfully.")


# enums\__init__.py



# enums\agent_enums.py

# 1 from __future__ import annotations
# 2 
# 3 from enum import Enum
# 4 
# 5 
# 6 class AgentRole(str, Enum):
# 7     GENERATOR = "generator"
# 8     DISCRIMINATOR = "discriminator"
# 9     MEDIATOR = "mediator"
# 10     PATCHER = "patcher"
# 11     EVALUATOR = "evaluator"
# 12     RECOMMENDER = "recommender"
# 13 
# 14 
# 15 class AgentState(Enum):
# 16     INIT = "init"
# 17     RUNNING = "running"
# 18     COMPLETE = "complete"


# enums\controller_enums.py

# 1 
# 2 
# 3 from enum import Enum
# 4 
# 5 
# 6 class ControllerType(str, Enum):
# 7     """
# 8     Enum of high-level controllers grouping related SystemTypes.
# 9 
# 10     - PREPROCESSING: Core code transformation systems (linting, formatting, docstrings, etc.)
# 11     - TESTING: Unit, edge, integration tests and related fix generation
# 12     - OBSERVABILITY: Logging injection, trace annotation, error handling
# 13     - DOCUMENTATION: Inline comments, function/file/change summarization
# 14     - REVIEW: Code review, static analysis, regression analysis, mediator reconciliation
# 15     - PLANNING: Symbol graphs, dependency mapping, restructuring, function splitting
# 16     - MLOPS: Data validation, metadata, model docs, pipeline testing, training config audits, feature traceability
# 17     - SAFETY: Security checks, policy compliance, license audits
# 18     - MISC: Patching, prompt refinement, agent recommendation, semantic diff, context filtering
# 19     """
# 20     PREPROCESSING = "preprocessing_controller"
# 21     TESTING = "testing_controller"
# 22     OBSERVABILITY = "observability_controller"
# 23     DOCUMENTATION = "documentation_controller"
# 24     REVIEW = "review_controller"
# 25     PLANNING = "planning_controller"
# 26     MLOPS = "mlops_controller"
# 27     SAFETY = "safety_controller"
# 28     MISC = "misc_controller"


# enums\logging_enums.py

# 1 from enum import Enum
# 2 
# 3 class LogType(str, Enum):
# 4     ERROR = "error_log"
# 5     PROVIDER = "provider_log"
# 6     STATE_TRANSITION = "state_transition_log"
# 7     AGENT_CONVERSATION = "agent_conversation_log"


# enums\scoring_enums.py

# 1 from enum import Enum
# 2 
# 3 
# 4 #  class ScoringMetric(str, Enum):
# 5 #      FUNCTIONAL_CORRECTNESS = "functional_correctness"
# 6 #      MAINTAINABILITY_INDEX = "maintainability_index"
# 7 #      BUG_FIX_SUCCESS = "bug_fix_success_rate"
# 8 #      LINTING_COMPLIANCE = "linting_compliance"
# 9 #      AGENT_SUCCESS_RATE = "agent_role_success_rate"
# 10 #      RETRY_SUCCESS_RATE = "retry_success_rate"
# 11 #      COVERAGE = "coverage"
# 12 #      CYCLOMATIC_COMPLEXITY = "cyclomatic_complexity"
# 13 #      RECOMMENDATION_QUALITY = "recommendation_quality"


# enums\system_enums.py

# 1 from __future__ import annotations
# 2 
# 3 from enum import Enum
# 4 
# 5 
# 6 class SystemType(str, Enum):
# 7     #  Core Code Transformation
# 8     LINTING = "linting"
# 9     FORMATTING = "formatting"
# 10     DOCSTRING = "docstring"
# 11     TYPE_ANNOTATION = "type_annotation"
# 12     REFACTORING = "refactoring"
# 13     DEAD_CODE_REMOVAL = "dead_code_removal"
# 14     COMPLEXITY_REDUCTION = "complexity_reduction"
# 15 
# 16     #  Testing and Validation
# 17     UNIT_TESTING = "unit_testing"
# 18     EDGE_TESTING = "edge_testing"
# 19     INTEGRATION_TESTING = "integration_testing"
# 20     TEST_FIX_GENERATION = "test_fix_generation"
# 21     MOCK_GENERATION = "mock_generation"
# 22     STUB_EXTRACTION = "stub_extraction"
# 23 
# 24     #  Observability and Instrumentation
# 25     LOGGING_INJECTION = "logging_injection"
# 26     TRACE_ANNOTATION = "trace_annotation"
# 27     ERROR_HANDLING = "error_handling"
# 28 
# 29     #  Documentation and Explanation
# 30     INLINE_COMMENTING = "inline_commenting"
# 31     FUNCTION_SUMMARIZATION = "function_summarization"
# 32     FILE_SUMMARIZATION = "file_summarization"
# 33     CHANGE_SUMMARIZATION = "change_summarization"
# 34 
# 35     #  Review and Oversight
# 36     CODE_REVIEW = "code_review"
# 37     STATIC_ANALYSIS = "static_analysis"
# 38     REGRESSION_ANALYSIS = "regression_analysis"
# 39     MEDIATOR_RECONCILIATION = "mediator_reconciliation"
# 40     SCORING_EVALUATION = "scoring_evaluation"
# 41 
# 42     #  Planning and Organization
# 43     SYMBOL_GRAPH_GENERATION = "symbol_graph_generation"
# 44     DEPENDENCY_MAPPING = "dependency_mapping"
# 45     FILE_RESTRUCTURING = "file_restructuring"
# 46     FUNCTION_SPLITTING = "function_splitting"
# 47 
# 48     #  MLOps-Specific
# 49     DATA_VALIDATION = "data_validation"
# 50     METADATA_ANNOTATION = "metadata_annotation"
# 51     MODEL_DOC_GENERATION = "model_doc_generation"
# 52     PIPELINE_TESTING = "pipeline_testing"
# 53     TRAINING_CONFIG_AUDIT = "training_config_audit"
# 54     FEATURE_TRACEABILITY = "feature_traceability"
# 55 
# 56     #  Production Safety
# 57     SECURITY_CHECK = "security_check"
# 58     POLICY_COMPLIANCE = "policy_compliance"
# 59     LICENSE_CHECK = "license_check"
# 60 
# 61     #  Misc / Cross-cutting
# 62     PATCHING = "patching"
# 63     PROMPT_REFINEMENT = "prompt_refinement"
# 64     AGENT_RECOMMENDATION = "agent_recommendation"
# 65     SEMANTIC_DIFF = "semantic_diff"
# 66     CONTEXT_FILTERING = "context_filtering"
# 67 
# 68 
# 69 class Decision(str, Enum):
# 70     initial = "initial"
# 71     improved = "improved"
# 72     rollback = "rollback"
# 73     final = "final"
# 74 
# 75 
# 76 #  class SystemState(Enum):
# 77 #      """Finite state machine states for the CodeCritic system."""
# 78 
# 79 #      START = "start"
# 80 #      GENERATE = "generate"
# 81 #      DISCRIMINATE = "discriminate"
# 82 #      MEDIATE = "mediate"
# 83 #      PATCH = "patch"
# 84 #      EVALUATE = "evaluate"
# 85 #      END = "end"
# 86 
# 87 
# 88 #  class StateTransitionReason(str, Enum):
# 89 #      FIRST_ROUND = "first_round"
# 90 #      MAX_ITERATIONS_REACHED = "max_iterations_reached"
# 91 #      SCORE_THRESHOLD_MET = "score_threshold_met"
# 92 #      SCORE_STAGNATION = "score_stagnation"
# 93 #      AGENT_FAILURE = "agent_failure"
# 94 #      MEDIATOR_OVERRIDE = "mediator_override"
# 95 #      PATCH_RETRY = "patch_retry"
# 96 #      CUSTOM_RULE = "custom_rule"
# 97 #      END_REACHED = "end_reached"


# factories\__init__.py



# factories\agent_engine_provider_factory.py

# 1 from app.db.models import AgentEngineProviderConfig
# 2 from app.factories.base_provider_factory import BaseProviderFactory
# 3 from app.providers.agent_engine_provider_base import AgentEngineProviderBase
# 4 
# 5 
# 6 class AgentEngineProviderFactory(BaseProviderFactory):
# 7     config_model = AgentEngineProviderConfig
# 8     base_class = AgentEngineProviderBase


# factories\agent_provider_factory.py

# 1 from app.factories.base_provider_factory import BaseProviderFactory
# 2 from app.db.models import AgentProviderConfig
# 3 from app.providers.agent_provider_base import AgentProviderBase
# 4 
# 5 class AgentProviderFactory(BaseProviderFactory):
# 6     config_model = AgentProviderConfig
# 7     base_class   = AgentProviderBase
# 8 
# 9     @classmethod
# 10     def create(cls, id: int, **kwargs) -> AgentProviderBase:
# 11         #  delayed imports to avoid circular dependencies
# 12         from app.factories.agent_engine_provider_factory import AgentEngineProviderFactory
# 13         from app.factories.prompt_provider_factory      import PromptProviderFactory
# 14         from app.factories.context_provider_factory     import ContextProviderFactory
# 15         from app.factories.score_provider_factory       import ScoreProviderFactory
# 16         from app.factories.tool_provider_factory        import ToolProviderFactory
# 17 
# 18         #  1) Preload via BaseProviderFactory (it will inject the DB engine for you)
# 19         preload_instance = super().create(id)
# 20         config           = preload_instance.config.config or {}
# 21 
# 22         #  2) Build each dependency exactly once
# 23         agent_engine    = (
# 24             AgentEngineProviderFactory.create(config["agent_engine_provider_id"])
# 25             if config.get("agent_engine_provider_id") else None
# 26         )
# 27         prompt_provider = (
# 28             PromptProviderFactory.create(config["prompt_provider_id"])
# 29             if config.get("prompt_provider_id") else None
# 30         )
# 31         context_provider = (
# 32             ContextProviderFactory.create(config["context_provider_id"])
# 33             if config.get("context_provider_id") else None
# 34         )
# 35         score_provider  = (
# 36             ScoreProviderFactory.create(config["score_provider_id"])
# 37             if config.get("score_provider_id") else None
# 38         )
# 39         tool_providers  = [
# 40             ToolProviderFactory.create(tid)
# 41             for tid in (config.get("tool_provider_ids") or {}).values()
# 42         ]
# 43 
# 44         #  3) Re-instantiate the actual subclass with all dependencies and the injected engine
# 45         cls_type = type(preload_instance)
# 46         return cls_type(
# 47             config          = preload_instance.config,
# 48             engine          = preload_instance._engine,
# 49             agent_engine    = agent_engine,
# 50             prompt_provider = prompt_provider,
# 51             context_provider= context_provider,
# 52             score_provider  = score_provider,
# 53             tool_providers  = tool_providers,
# 54         )


# factories\base_provider_factory.py

# 1 from sqlalchemy.orm import Session
# 2 from importlib.util import spec_from_file_location, module_from_spec
# 3 from pathlib import Path
# 4 from app.db import init_db #  <-- global DB engine instance
# 5 from app.providers.base_provider import BaseProvider
# 6 
# 7 PROJECT_ROOT = Path(__file__).resolve().parents[2]
# 8 EXTENSIONS_DIR = PROJECT_ROOT / "extensions"
# 9 
# 10 engine = init_db(reset=False)
# 11 
# 12 class BaseProviderFactory:
# 13     config_model = None  
# 14     base_class = BaseProvider  
# 15 
# 16     @classmethod
# 17     def create(cls, id: int, **kwargs):
# 18         with Session(bind=engine) as session:
# 19             config = session.get(cls.config_model, int(id))
# 20             if not config:
# 21                 raise ValueError(f"{cls.config_model.__name__} ID {id} not found")
# 22 
# 23             ext_path = (EXTENSIONS_DIR / config.artifact_path).resolve()
# 24             if not ext_path.exists():
# 25                 raise FileNotFoundError(f"Extension not found: {ext_path}")
# 26 
# 27         spec = spec_from_file_location(ext_path.stem, ext_path)
# 28         if spec is None or spec.loader is None:
# 29             raise ImportError(f"Cannot load module spec for {ext_path}")
# 30 
# 31         module = module_from_spec(spec)
# 32         spec.loader.exec_module(module)
# 33 
# 34         provider_class = next(
# 35             (obj for obj in vars(module).values()
# 36              if isinstance(obj, type) and issubclass(obj, cls.base_class) and obj is not cls.base_class),
# 37             None
# 38         )
# 39 
# 40         if provider_class is None:
# 41             raise ImportError(f"No valid subclass of {cls.base_class.__name__} found in {ext_path}")
# 42 
# 43         #  Explicitly pass the global engine here
# 44         instance = provider_class(config=config, engine=engine)
# 45         return instance


# factories\context_provider_factory.py

# 1 from app.factories.base_provider_factory import BaseProviderFactory
# 2 from app.db.models import ContextProviderConfig
# 3 from app.providers.context_provider_base import ContextProviderBase
# 4 
# 5 class ContextProviderFactory(BaseProviderFactory):
# 6     config_model = ContextProviderConfig
# 7     base_class = ContextProviderBase
# 8 
# 9     @classmethod
# 10     def create(cls, id: int, **kwargs) -> ContextProviderBase:
# 11         instance = super().create(id, **kwargs)
# 12         config = instance.config.config or {}
# 13 
# 14         #  ⏱️ Delayed imports to avoid circular dependencies
# 15         if "score_provider_id" in config or "tool_provider_ids" in config:
# 16             from app.factories.score_provider_factory import ScoreProviderFactory
# 17             from app.factories.tool_provider_factory import ToolProviderFactory
# 18 
# 19             if (score_id := config.get("score_provider_id")):
# 20                 if isinstance(score_id, int) and score_id > 0:
# 21                     score = ScoreProviderFactory.create(score_id)
# 22                     instance.set_score_provider(score)
# 23 
# 24             for _, tool_id in (config.get("tool_provider_ids") or {}).items():
# 25                 tool = ToolProviderFactory.create(tool_id)
# 26                 instance.set_tool_provider(tool)
# 27 
# 28         return instance


# factories\controller_provider_factory.py

# 1 from app.factories.base_provider_factory import BaseProviderFactory
# 2 from app.db.models import ControllerProviderConfig
# 3 from app.providers.controller_provider_base import ControllerProviderBase
# 4 
# 5 class ControllerProviderFactory(BaseProviderFactory):
# 6     config_model = ControllerProviderConfig
# 7     base_class   = ControllerProviderBase
# 8 
# 9     @classmethod
# 10     def create(cls, id: int, **kwargs) -> ControllerProviderBase:
# 11         from app.factories.context_provider_factory import ContextProviderFactory
# 12         from app.factories.score_provider_factory   import ScoreProviderFactory
# 13         from app.factories.tool_provider_factory    import ToolProviderFactory
# 14 
# 15         inst   = super().create(id)
# 16         config = inst.config.config or {}
# 17 
# 18         ctx   = (
# 19             ContextProviderFactory.create(config["context_provider_id"])
# 20             if config.get("context_provider_id") else None
# 21         )
# 22         scr   = (
# 23             ScoreProviderFactory.create(config["score_provider_id"])
# 24             if config.get("score_provider_id") else None
# 25         )
# 26         tools = [
# 27             ToolProviderFactory.create(tid)
# 28             for tid in (config.get("tool_provider_ids") or {}).values()
# 29         ]
# 30 
# 31         cls_type = type(inst)
# 32         return cls_type(
# 33             config           = inst.config,
# 34             engine           = inst._engine,
# 35             context_provider = ctx,
# 36             score_provider   = scr,
# 37             tool_providers   = tools,
# 38         )


# factories\program_provider_factory.py

# 1 from app.factories.base_provider_factory import BaseProviderFactory
# 2 from app.db.models import ProgramProviderConfig
# 3 from app.providers.program_provider_base import ProgramProviderBase
# 4 
# 5 class ProgramProviderFactory(BaseProviderFactory):
# 6     config_model = ProgramProviderConfig
# 7     base_class   = ProgramProviderBase
# 8 
# 9     @classmethod
# 10     def create(cls, id: int, **kwargs) -> ProgramProviderBase:
# 11         from app.factories.context_provider_factory import ContextProviderFactory
# 12         from app.factories.score_provider_factory import ScoreProviderFactory
# 13         from app.factories.tool_provider_factory import ToolProviderFactory
# 14 
# 15         inst   = super().create(id)
# 16         config = inst.config.config or {}
# 17 
# 18         ctx = (
# 19             ContextProviderFactory.create(config["context_provider_id"])
# 20             if config.get("context_provider_id") else None
# 21         )
# 22         scr = (
# 23             ScoreProviderFactory.create(config["score_provider_id"])
# 24             if config.get("score_provider_id") else None
# 25         )
# 26         tools = [
# 27             ToolProviderFactory.create(tid)
# 28             for tid in (config.get("tool_provider_ids") or {}).values()
# 29         ]
# 30 
# 31         cls_type = type(inst)
# 32         return cls_type(
# 33             config           = inst.config,
# 34             engine           = inst._engine,
# 35             context_provider = ctx,
# 36             score_provider   = scr,
# 37             tool_providers   = tools,
# 38         )


# factories\prompt_provider_factory.py

# 1 from app.db.models import PromptProviderConfig
# 2 from app.factories.base_provider_factory import BaseProviderFactory
# 3 from app.providers.prompt_provider_base import PromptProviderBase
# 4 
# 5 
# 6 class PromptProviderFactory(BaseProviderFactory):
# 7     config_model = PromptProviderConfig
# 8     base_class = PromptProviderBase


# factories\score_provider_factory.py

# 1 from app.factories.base_provider_factory import BaseProviderFactory
# 2 from app.db.models import ScoreProviderConfig
# 3 from app.providers.score_provider_base import ScoreProviderBase
# 4 
# 5 class ScoreProviderFactory(BaseProviderFactory):
# 6     config_model = ScoreProviderConfig
# 7     base_class = ScoreProviderBase
# 8 
# 9     @classmethod
# 10     def create(cls, id: int, **kwargs) -> ScoreProviderBase:
# 11         instance = super().create(id, **kwargs)
# 12         config = instance.config.config or {}
# 13 
# 14         #  ⏱️ Delayed imports to prevent circular references
# 15         if "tool_provider_ids" in config or "context_provider_id" in config:
# 16             from app.factories.tool_provider_factory import ToolProviderFactory
# 17             from app.factories.context_provider_factory import ContextProviderFactory
# 18 
# 19             if (ctx_id := config.get("context_provider_id")):
# 20                 context = ContextProviderFactory.create(ctx_id)
# 21                 instance.set_context_provider(context)
# 22                 if hasattr(context, "set_score_provider"):
# 23                     context.set_score_provider(instance)
# 24 
# 25             for _, tool_id in (config.get("tool_provider_ids") or {}).items():
# 26                 tool = ToolProviderFactory.create(tool_id)
# 27                 instance.set_tool_provider(tool)
# 28 
# 29         return instance


# factories\state_provider_factory.py

# 1 from app.factories.base_provider_factory import BaseProviderFactory
# 2 from app.db.models import StateProviderConfig
# 3 from app.providers.state_provider_base import StateProviderBase
# 4 
# 5 class StateProviderFactory(BaseProviderFactory):
# 6     config_model = StateProviderConfig
# 7     base_class = StateProviderBase
# 8 
# 9     @classmethod
# 10     def create(cls, id: int, **kwargs) -> StateProviderBase:
# 11         #  Delayed imports to avoid circular dependencies
# 12         from app.factories.agent_provider_factory import AgentProviderFactory
# 13         from app.factories.context_provider_factory import ContextProviderFactory
# 14         from app.factories.score_provider_factory import ScoreProviderFactory
# 15         from app.factories.tool_provider_factory import ToolProviderFactory
# 16 
# 17         engine = kwargs.get("engine")
# 18         preload_instance = super().create(id, engine=engine)
# 19         config = preload_instance.config.config or {}
# 20 
# 21         #  Inject agents into a dict
# 22         kwargs["agents"] = {
# 23             name: AgentProviderFactory.create(agent_id)
# 24             for name, agent_id in config.get("agents", {}).items()
# 25         }
# 26 
# 27         #  Inject optional context/score/tools
# 28         if "context_provider_id" in config:
# 29             kwargs["context_provider"] = ContextProviderFactory.create(config["context_provider_id"])
# 30 
# 31         if "score_provider_id" in config:
# 32             kwargs["score_provider"] = ScoreProviderFactory.create(config["score_provider_id"])
# 33 
# 34         kwargs["tool_providers"] = [
# 35             ToolProviderFactory.create(tool_id)
# 36             for _, tool_id in (config.get("tool_provider_ids") or {}).items()
# 37         ]
# 38 
# 39         return super().create(id, **kwargs)


# factories\system_provider_factory.py

# 1 from app.factories.base_provider_factory import BaseProviderFactory
# 2 from app.db.models import SystemProviderConfig
# 3 from app.providers.system_provider_base import SystemProviderBase
# 4 
# 5 class SystemProviderFactory(BaseProviderFactory):
# 6     config_model = SystemProviderConfig
# 7     base_class = SystemProviderBase
# 8 
# 9     @classmethod
# 10     def create(cls, id: int, **kwargs) -> SystemProviderBase:
# 11         #  ⏱️ Delayed imports to avoid circular dependencies
# 12         from app.factories.state_provider_factory import StateProviderFactory
# 13         from app.factories.context_provider_factory import ContextProviderFactory
# 14         from app.factories.score_provider_factory import ScoreProviderFactory
# 15         from app.factories.tool_provider_factory import ToolProviderFactory
# 16 
# 17         engine = kwargs.get("engine")
# 18         preload_instance = super().create(id, engine=engine)
# 19         config = preload_instance.config.config or {}
# 20 
# 21         #  Inject optional providers
# 22         if "context_provider_id" in config:
# 23             kwargs["context_provider"] = ContextProviderFactory.create(config["context_provider_id"])
# 24 
# 25         if "score_provider_id" in config:
# 26             kwargs["score_provider"] = ScoreProviderFactory.create(config["score_provider_id"])
# 27 
# 28         kwargs["tool_providers"] = [
# 29             ToolProviderFactory.create(tool_id)
# 30             for _, tool_id in (config.get("tool_provider_ids") or {}).items()
# 31         ]
# 32 
# 33         return super().create(id, **kwargs)


# factories\tool_provider_factory.py

# 1 from app.factories.base_provider_factory import BaseProviderFactory
# 2 from app.db.models import ToolProviderConfig
# 3 from app.providers.tool_provider_base import ToolProviderBase
# 4 
# 5 class ToolProviderFactory(BaseProviderFactory):
# 6     config_model = ToolProviderConfig
# 7     base_class = ToolProviderBase
# 8 
# 9     @classmethod
# 10     def create(cls, id: int, **kwargs) -> ToolProviderBase:
# 11         instance = super().create(id, **kwargs)
# 12         config = instance.config.config or {}
# 13 
# 14         #  ⏱️ Delayed imports to avoid circular dependencies
# 15         if "context_provider_id" in config or "score_provider_id" in config:
# 16             from app.factories.context_provider_factory import ContextProviderFactory
# 17             from app.factories.score_provider_factory import ScoreProviderFactory
# 18 
# 19             if (ctx_id := config.get("context_provider_id")):
# 20                 context = ContextProviderFactory.create(ctx_id)
# 21                 instance.set_context_provider(context)
# 22                 if hasattr(context, "set_tool_provider"):
# 23                     context.set_tool_provider(instance)
# 24 
# 25             if (score_id := config.get("score_provider_id")):
# 26                 if isinstance(score_id, int) and score_id > 0:
# 27                     score = ScoreProviderFactory.create(score_id)
# 28                     instance.set_score_provider(score)
# 29                     if hasattr(score, "set_tool_provider"):
# 30                         score.set_tool_provider(instance)
# 31 
# 32         return instance


# providers\__init__.py



# providers\agent_engine_provider_base.py

# 1 from __future__ import annotations
# 2 from abc import abstractmethod
# 3 from app.providers.base_provider import BaseProvider
# 4 
# 5 class AgentEngineProviderBase(BaseProvider):
# 6     """Base class for agent engines that return raw LLM output."""
# 7 
# 8     def _run_provider(self, input: dict) -> str:
# 9         #  Only responsible for calling the underlying engine
# 10         return self._run(input)
# 11 
# 12     @abstractmethod
# 13     def _run(self, input: dict) -> str:
# 14         """Implement this to call the actual LLM engine (e.g., OpenAI GPT-4o)."""
# 15         raise NotImplementedError


# providers\agent_provider_base.py

# 1 from datetime import datetime, timezone
# 2 from pathlib import Path
# 3 from abc import abstractmethod
# 4 
# 5 from app.enums.logging_enums import LogType
# 6 from app.providers.base_provider import BaseProvider
# 7 from app.utilities.metadata.snapshots.snapshot_writer import SnapshotWriter
# 8 from app.db.schemas import AgentConversationLogSchema
# 9 
# 10 class AgentProviderBase(BaseProvider):
# 11     def __init__(
# 12         self,
# 13         config=None,
# 14         engine=None,
# 15         agent_engine=None,
# 16         prompt_provider=None,
# 17         context_provider=None,
# 18         score_provider=None,
# 19         tool_providers=None,
# 20     ):
# 21         super().__init__(config=config, engine=engine)
# 22         self._agent_engine = agent_engine
# 23         self._prompt_provider = prompt_provider
# 24         self._context_provider = context_provider
# 25         self._score_provider = score_provider
# 26         self._tool_providers = tool_providers or []
# 27 
# 28     def _run_provider(self, input: dict) -> str:
# 29         output = self._run(input)
# 30 
# 31         self._log.debug(f"🐛 _run_provider called for: {self.config.name if self.config else 'unknown'}")
# 32 
# 33         #  === Extract structured blocks ===
# 34         code_block = self._extract_block(output, "[CODE]", "[/CODE]") or (
# 35             output.strip() if input.get("before") else None
# 36         )
# 37         log_content = self._extract_block(output, "[CONVERSATION_LOG_ENTRY]", "[/CONVERSATION_LOG_ENTRY]")
# 38 
# 39         #  === Write snapshot ===
# 40         file_path = input.get("before") or input.get("file_path") or (self.config.config or {}).get("before")
# 41         if file_path and code_block:
# 42             before_path = Path(file_path).resolve()
# 43             if before_path.exists():
# 44                 snapshot_path = SnapshotWriter().write_snapshot(
# 45                     before=before_path.read_text(encoding="utf-8"),
# 46                     after=code_block,
# 47                     session_id=self._session_id,
# 48                 )
# 49                 self._snapshot_id = snapshot_path
# 50                 self._log.debug(f"📦 Snapshot written to: {snapshot_path}")
# 51             else:
# 52                 self._log.warning(f"❌ Snapshot skipped: file does not exist → {before_path}")
# 53 
# 54         #  === Log conversation entry ===
# 55         if log_content:
# 56             self.logger.write(LogType.AGENT_CONVERSATION, AgentConversationLogSchema(
# 57                 session_id=self._session_id,
# 58                 system=self._system,
# 59                 agent_provider_config_id=self.config.id if self.config else -1,
# 60                 agent_name=self.config.name if self.config else "unknown",
# 61                 content=log_content,
# 62                 timestamp=datetime.now(timezone.utc),
# 63             ))
# 64             self._log.debug("✅ AGENT_CONVERSATION log write complete")
# 65 
# 66         return output
# 67 
# 68     def _extract_block(self, text: str, start_tag: str, end_tag: str) -> str | None:
# 69         start = text.find(start_tag)
# 70         end = text.find(end_tag)
# 71         if start != -1 and end != -1 and start < end:
# 72             return text[start + len(start_tag):end].strip()
# 73         return None
# 74 
# 75     @abstractmethod
# 76     def _run(self, input: dict) -> str:
# 77         raise NotImplementedError


# providers\base_provider.py

# 1 #  base_provider.py
# 2 from abc import abstractmethod
# 3 from datetime import datetime, timezone
# 4 from pathlib import Path
# 5 import json
# 6 
# 7 from app.utilities.metadata.logging.logging_provider import LoggingMixin, LogType
# 8 from app.db.schemas import ProviderLogSchema, ErrorLogSchema
# 9 
# 10 class BaseProvider(LoggingMixin):
# 11     """Base class for all providers using unified provider log."""
# 12 
# 13     def __init__(self, config=None, engine=None) -> None:
# 14         super().__init__()
# 15         assert engine is not None, "🚨 engine must be injected into BaseProvider"
# 16         self.config = config
# 17         self._engine = engine
# 18 
# 19     def run(self, input: dict | None = None, session_id: str = "") -> str:
# 20         input = input or {}
# 21         self._log.debug("Run started")
# 22         self._session_id = session_id
# 23         self._system = input.get("system", "unknown")
# 24         output = None
# 25 
# 26         try:
# 27             output = self._run_provider(input)
# 28             return output
# 29         except Exception as exc:
# 30             self.logger.write(LogType.ERROR, ErrorLogSchema(
# 31                 session_id=session_id,
# 32                 error_type=type(exc).__name__,
# 33                 message=str(exc),
# 34                 file_path=str(Path(__file__).relative_to(Path.cwd())),
# 35             ))
# 36             raise
# 37         finally:
# 38             log = ProviderLogSchema(
# 39                 session_id=session_id,
# 40                 provider_id=self.config.id if self.config else -1,
# 41                 provider_type = next(
# 42                     base.__name__
# 43                     for base in self.__class__.__mro__
# 44                     if base.__name__.endswith("ProviderBase") and base is not object
# 45                 ),
# 46                 input=json.dumps(input),
# 47                 output=json.dumps(output.model_dump()) if hasattr(output, "model_dump") else json.dumps(output),
# 48                 file_path = getattr(self.config, "artifact_path", None),
# 49                 timestamp=datetime.now(timezone.utc)
# 50             )
# 51             self.logger.write(LogType.PROVIDER, log)
# 52             self._log.debug("Run logged")
# 53 
# 54     @abstractmethod
# 55     def _run_provider(self, input: dict) -> str:
# 56         raise NotImplementedError


# providers\context_provider_base.py

# 1 from __future__ import annotations
# 2 
# 3 from abc import abstractmethod
# 4 
# 5 from app.providers.base_provider import BaseProvider
# 6 
# 7 from typing import TYPE_CHECKING
# 8 if TYPE_CHECKING:
# 9     from app.providers.score_provider_base import ScoreProviderBase
# 10     from app.providers.tool_provider_base import ToolProviderBase
# 11 
# 12 class ContextProviderBase(BaseProvider):
# 13     """Base class for context providers."""
# 14 
# 15     def _run_provider(self, input: dict) -> str:
# 16         return self._run(input)
# 17     
# 18     def set_tool_provider(self, provider: "ToolProviderBase") -> None:
# 19         if not hasattr(self, "tool_providers"):
# 20             self.tool_providers = []
# 21         self.tool_providers.append(provider)
# 22 
# 23     def set_score_provider(self, provider: "ScoreProviderBase") -> None:
# 24         self.score_provider = provider
# 25 
# 26 
# 27     @abstractmethod
# 28     def _run(self, input: dict) -> str:
# 29         raise NotImplementedError


# providers\controller_provider_base.py

# 1 from __future__ import annotations
# 2 from abc import abstractmethod
# 3 from typing import Dict
# 4 from datetime import datetime, timezone
# 5 from pathlib import Path
# 6 import shutil, json
# 7 
# 8 from app.providers.base_provider import BaseProvider
# 9 from app.factories.system_provider_factory import SystemProviderFactory
# 10 from app.db.schemas import StateTransitionLogSchema, ProviderLogSchema
# 11 from app.enums.logging_enums import LogType
# 12 
# 13 class ControllerProviderBase(BaseProvider):
# 14     def __init__(
# 15         self,
# 16         config=None,
# 17         engine=None,
# 18         context_provider=None,
# 19         score_provider=None,
# 20         tool_providers=None,
# 21     ):
# 22         super().__init__(config=config, engine=engine)
# 23         self.context_provider = context_provider
# 24         self.score_provider   = score_provider
# 25         self.tool_providers   = tool_providers or []
# 26 
# 27         #  instantiate sub-systems
# 28         self._systems: Dict[str, object] = {}
# 29         for name, sys_id in (config.config or {}).get("systems", {}).items():
# 30             self._systems[name] = SystemProviderFactory.create(sys_id)
# 31 
# 32     def _run_provider(self, input: dict) -> dict:
# 33         sess = input.get("session_id")
# 34         src  = Path(input["file_name"])
# 35         self.working_file = src.with_name(f"{src.stem}_working{src.suffix}")
# 36         shutil.copy(src, self.working_file)
# 37 
# 38         state = {
# 39             "state":        "start",
# 40             "file_path":    str(src),
# 41             "working_file": str(self.working_file),
# 42             **input,
# 43         }
# 44 
# 45         while True:
# 46             cur = state["state"]
# 47             if cur == "end":
# 48                 #  final provider log
# 49                 self.logger.write(LogType.PROVIDER, ProviderLogSchema(
# 50                     session_id=sess,
# 51                     provider_id=self.config.id,
# 52                     provider_type=self.__class__.__name__,
# 53                     input=json.dumps(input),
# 54                     output=json.dumps(state),
# 55                     file_path=self.config.artifact_path,
# 56                     timestamp=datetime.now(timezone.utc)
# 57                 ))
# 58                 return state
# 59 
# 60             if cur == "start":
# 61                 nxt = self.transition(state, None)
# 62                 state.update(nxt, _last_state="start")
# 63                 continue
# 64 
# 65             #  call the named sub-system
# 66             sysprov = self._systems.get(cur)
# 67             if not sysprov:
# 68                 raise ValueError(f"No system registered for state '{cur}'")
# 69 
# 70             #  pass through everything except the `state` key
# 71             inp = {k: v for k,v in state.items() if k != "state"}
# 72             out = sysprov.run(input=inp, session_id=sess)
# 73 
# 74             nxt = self.transition(state, out)
# 75             flat_output = {k: v for k, v in out.items() if k != "output"}
# 76             state.update(nxt, _last_state=cur, output=flat_output)
# 77 
# 78 
# 79     def transition(self, state: dict, sys_output: str|None) -> dict:
# 80         nxt = self._transition(state, sys_output)
# 81         self.logger.write(LogType.STATE_TRANSITION, StateTransitionLogSchema(
# 82             session_id=state["session_id"],
# 83             entity_type=self.__class__.__name__,
# 84             entity_id=self.config.id,
# 85             from_state=state["state"],
# 86             to_state=nxt.get("state", "unknown"),
# 87             reason=nxt.get("reason", "unspecified"),
# 88             timestamp=datetime.now(timezone.utc)
# 89         ))
# 90         return nxt
# 91 
# 92     @abstractmethod
# 93     def _transition(self, state: dict, sys_output: str|None) -> dict:
# 94         """Implement your FSM logic in the extension."""
# 95         ...


# providers\program_provider_base.py

# 1 from __future__ import annotations
# 2 
# 3 from abc import abstractmethod
# 4 from typing import Dict
# 5 from datetime import datetime, timezone
# 6 from pathlib import Path
# 7 import shutil
# 8 import json
# 9 
# 10 from app.providers.base_provider import BaseProvider
# 11 from app.factories.controller_provider_factory import ControllerProviderFactory
# 12 from app.db.schemas import StateTransitionLogSchema, ProviderLogSchema
# 13 from app.enums.logging_enums import LogType
# 14 
# 15 class ProgramProviderBase(BaseProvider):
# 16     def __init__(
# 17         self,
# 18         config=None,
# 19         engine=None,
# 20         context_provider=None,
# 21         score_provider=None,
# 22         tool_providers=None,
# 23     ):
# 24         super().__init__(config=config, engine=engine)
# 25         self.context_provider = context_provider
# 26         self.score_provider = score_provider
# 27         self.tool_providers = tool_providers or []
# 28         self._controllers: Dict[str, object] = {}
# 29 
# 30         controller_cfg = config.config.get("controllers", {})
# 31         for name, provider_id in controller_cfg.items():
# 32             self._controllers[name] = ControllerProviderFactory.create(provider_id)
# 33 
# 34     def _run_provider(self, input: dict) -> dict:
# 35         session_id = input.get("session_id")
# 36         input_file = Path(input["file_name"])
# 37         self.working_file = input_file.with_name(f"{input_file.stem}_working{input_file.suffix}")
# 38         shutil.copy(input_file, self.working_file)
# 39 
# 40         state = {
# 41             "state": "start",
# 42             "file_name": str(input_file),
# 43             "working_file": str(self.working_file),
# 44             **input,
# 45         }
# 46 
# 47         max_steps = 20
# 48         step_count = 0
# 49 
# 50         while True:
# 51             if step_count >= max_steps:
# 52                 state["state"] = "end"
# 53                 state["reason"] = f"max steps ({max_steps}) reached"
# 54                 break
# 55             step_count += 1
# 56 
# 57             current = state.get("state")
# 58 
# 59             if current == "end":
# 60                 self.logger.write(LogType.PROVIDER, ProviderLogSchema(
# 61                     session_id=session_id,
# 62                     provider_id=self.config.id,
# 63                     provider_type=self.__class__.__name__,
# 64                     input=json.dumps(input),
# 65                     output=json.dumps(state),
# 66                     file_path=self.config.artifact_path,
# 67                     timestamp=datetime.now(timezone.utc)
# 68                 ))
# 69                 return state
# 70 
# 71             if current == "start":
# 72                 state = {**state, **self.transition(state, None), "_last_state": "start"}
# 73                 continue
# 74 
# 75             controller = self._controllers.get(current)
# 76             if not controller:
# 77                 raise ValueError(f"No controller registered for state: {current}")
# 78 
# 79             controller_input = {k: v for k, v in state.items() if k != "state"}
# 80             output = controller.run(input=controller_input, session_id=session_id)
# 81 
# 82             transition_result = self.transition(state, output)
# 83             flat_output = {k: v for k, v in output.items() if k != "output"}
# 84             state = {**state, **transition_result, "_last_state": current, "output": flat_output}
# 85 
# 86         return state
# 87 
# 88     def transition(self, state: dict, controller_output: dict | None) -> dict:
# 89         next_state = self._transition(state, controller_output)
# 90         self.logger.write(LogType.STATE_TRANSITION, StateTransitionLogSchema(
# 91             session_id=state.get("session_id"),
# 92             entity_type=self.__class__.__name__,
# 93             entity_id=self.config.id,
# 94             from_state=state.get("state"),
# 95             to_state=next_state.get("state", "unknown"),
# 96             reason=next_state.get("reason", "unspecified"),
# 97             timestamp=datetime.now(timezone.utc)
# 98         ))
# 99         return next_state
# 100 
# 101     @abstractmethod
# 102     def _transition(self, state: dict, controller_output: dict | None) -> dict:
# 103         raise NotImplementedError


# providers\prompt_provider_base.py

# 1 from __future__ import annotations
# 2 from abc import abstractmethod
# 3 from sqlalchemy.orm import Session
# 4 from pathlib import Path
# 5 
# 6 from app.db.models import AgentPrompt, SystemPrompt
# 7 from app.providers.base_provider import BaseProvider
# 8 from app.factories.context_provider_factory import ContextProviderFactory
# 9 
# 10 
# 11 class PromptProviderBase(BaseProvider):
# 12     """Base class for prompt generators with optional context/agent/system loading."""
# 13 
# 14     def __init__(self, config, engine=None):
# 15         super().__init__(config=config, engine=engine)
# 16 
# 17         self._agent_prompt = None
# 18         self._system_prompt = None
# 19         self._context_provider = None
# 20 
# 21         if not self.config or not self._engine:
# 22             return
# 23 
# 24         with Session(bind=self._engine) as session:
# 25             if agent_id := self.config.config.get("agent_prompt_id"):
# 26                 self._agent_prompt = session.get(AgentPrompt, agent_id)
# 27 
# 28             if system_id := self.config.config.get("system_prompt_id"):
# 29                 self._system_prompt = session.get(SystemPrompt, system_id)
# 30 
# 31             if context_id := self.config.config.get("context_provider_id"):
# 32                 self._context_provider = ContextProviderFactory.create(id=context_id)
# 33 
# 34     def _run_provider(self, input: dict) -> str:
# 35         return self._run(input)
# 36 
# 37     @abstractmethod
# 38     def _run(self, input: dict) -> str:
# 39         raise NotImplementedError


# providers\score_provider_base.py

# 1 from __future__ import annotations
# 2 
# 3 from abc import abstractmethod
# 4 from app.providers.base_provider import BaseProvider
# 5 from typing import TYPE_CHECKING
# 6 if TYPE_CHECKING:
# 7     from app.providers.context_provider_base import ContextProviderBase
# 8     from app.providers.tool_provider_base import ToolProviderBase
# 9 
# 10 class ScoreProviderBase(BaseProvider):
# 11     """Base class for score providers."""
# 12 
# 13     def _run_provider(self, input: dict) -> str:
# 14         return self._run(input)
# 15     
# 16     def set_context_provider(self, provider: "ContextProviderBase") -> None:
# 17         self.context_provider = provider
# 18 
# 19     def set_tool_provider(self, provider: "ToolProviderBase") -> None:
# 20         if not hasattr(self, "tool_providers"):
# 21             self.tool_providers = []
# 22         self.tool_providers.append(provider)
# 23 
# 24     @abstractmethod
# 25     def _run(self, input: dict) -> str:
# 26         raise NotImplementedError


# providers\state_provider_base.py

# 1 from __future__ import annotations
# 2 
# 3 from abc import abstractmethod
# 4 from typing import Dict
# 5 from datetime import datetime, timezone
# 6 import json
# 7 
# 8 from app.providers.base_provider import BaseProvider
# 9 from app.db.schemas import StateTransitionLogSchema, ProviderLogSchema
# 10 from app.enums.logging_enums import LogType
# 11 
# 12 
# 13 class StateProviderBase(BaseProvider):
# 14     """Base class for FSM-driven state providers."""
# 15 
# 16     def __init__(
# 17         self,
# 18         config=None,
# 19         engine=None,
# 20         agents: dict[str, object] = None,
# 21         context_provider=None,
# 22         score_provider=None,
# 23         tool_providers=None
# 24     ):
# 25         super().__init__(config=config, engine=engine)
# 26         self._agents = agents or {}
# 27         self._context_provider = context_provider
# 28         self._score_provider = score_provider
# 29         self._tool_providers = tool_providers or []
# 30 
# 31     def _run_provider(self, input: dict) -> dict:
# 32         session_id = input.get("session_id")
# 33         state = {"state": "start", **input}
# 34 
# 35         while True:
# 36             current = state.get("state")
# 37 
# 38             if current == "end":
# 39                 self.logger.write(LogType.PROVIDER, ProviderLogSchema(
# 40                     session_id=session_id,
# 41                     provider_id=self.config.id,
# 42                     provider_type=self.__class__.__name__,
# 43                     input=json.dumps(input),
# 44                     output=json.dumps(state),
# 45                     file_path=self.config.artifact_path,
# 46                     timestamp=datetime.now(timezone.utc)
# 47                 ))
# 48                 return state
# 49 
# 50             if current == "start":
# 51                 state = {**state, **self.transition(state, None), "_last_state": "start"}
# 52                 continue
# 53 
# 54             agent = self._agents.get(current)
# 55             if not agent:
# 56                 raise ValueError(f"No agent registered for state: {current}")
# 57 
# 58             output = agent.run(input=state, session_id=session_id)
# 59             state = {**state, **self.transition(state, output), "_last_state": current, "output": output}
# 60 
# 61     def transition(self, state: dict, agent_output: str | None) -> dict:
# 62         next_state = self._transition(state, agent_output)
# 63         self.logger.write(LogType.STATE_TRANSITION, StateTransitionLogSchema(
# 64             session_id=state.get("session_id"),
# 65             entity_type=self.__class__.__name__,
# 66             entity_id=self.config.id,
# 67             from_state=state.get("state"),
# 68             to_state=next_state.get("state", "unknown"),
# 69             reason=next_state.get("reason", "unspecified"),
# 70             timestamp=datetime.now(timezone.utc)
# 71         ))
# 72         return next_state
# 73 
# 74     @abstractmethod
# 75     def _transition(self, state: dict, agent_output: str | None) -> dict:
# 76         raise NotImplementedError


# providers\system_provider_base.py

# 1 from __future__ import annotations
# 2 
# 3 from abc import abstractmethod
# 4 from typing import Dict
# 5 from datetime import datetime, timezone
# 6 from pathlib import Path
# 7 import shutil
# 8 import json
# 9 
# 10 from app.providers.base_provider import BaseProvider
# 11 from app.factories.state_provider_factory import StateProviderFactory
# 12 from app.db.schemas import StateTransitionLogSchema, ProviderLogSchema
# 13 from app.enums.logging_enums import LogType
# 14 
# 15 
# 16 class SystemProviderBase(BaseProvider):
# 17     def __init__(self, config=None, engine=None, context_provider=None, score_provider=None, tool_providers=None):
# 18         super().__init__(config=config, engine=engine)
# 19         self.context_provider = context_provider
# 20         self.score_provider = score_provider
# 21         self.tool_providers = tool_providers or []
# 22         self._states: Dict[str, object] = {}
# 23         state_cfg = config.config.get("states", {})
# 24 
# 25         for name, provider_id in state_cfg.items():
# 26             self._states[name] = StateProviderFactory.create(provider_id)
# 27 
# 28     def _run_provider(self, input: dict) -> dict:
# 29         session_id = input.get("session_id")
# 30         input_file = Path(input["file_path"])
# 31         self.working_file = input_file.with_name(f"{input_file.stem}_working{input_file.suffix}")
# 32         shutil.copy(input_file, self.working_file)
# 33 
# 34         state = {
# 35             "state": "start",
# 36             "input_file": str(input_file),
# 37             "working_file": str(self.working_file),
# 38             **input
# 39         }
# 40 
# 41         max_steps = 20
# 42         step_count = 0
# 43 
# 44         while True:
# 45             if step_count >= max_steps:
# 46                 state["state"] = "end"
# 47                 state["reason"] = f"max steps ({max_steps}) reached"
# 48                 break
# 49             step_count += 1
# 50 
# 51             current = state.get("state")
# 52 
# 53             if current == "end":
# 54                 self.logger.write(LogType.PROVIDER, ProviderLogSchema(
# 55                     session_id=session_id,
# 56                     provider_id=self.config.id,
# 57                     provider_type=self.__class__.__name__,
# 58                     input=json.dumps(input),
# 59                     output=json.dumps(state),
# 60                     file_path=self.config.artifact_path,
# 61                     timestamp=datetime.now(timezone.utc)
# 62                 ))
# 63                 return state
# 64 
# 65             if current == "start":
# 66                 state = {**state, **self.transition(state, None), "_last_state": "start"}
# 67                 continue
# 68 
# 69             state_provider = self._states.get(current)
# 70             if not state_provider:
# 71                 raise ValueError(f"No state provider registered for state: {current}")
# 72 
# 73             provider_input = {k: v for k, v in state.items() if k != "state"}
# 74             output = state_provider.run(input=provider_input, session_id=session_id)
# 75             transition_result = self.transition(state, output)
# 76             state = {**state, **transition_result, "_last_state": current}
# 77             if "output" not in output:
# 78                 state["output"] = output
# 79 
# 80         return state
# 81 
# 82     def transition(self, state: dict, agent_output: str | None) -> dict:
# 83         next_state = self._transition(state, agent_output)
# 84         self.logger.write(LogType.STATE_TRANSITION, StateTransitionLogSchema(
# 85             session_id=state.get("session_id"),
# 86             entity_type=self.__class__.__name__,
# 87             entity_id=self.config.id,
# 88             from_state=state.get("state"),
# 89             to_state=next_state.get("state", "unknown"),
# 90             reason=next_state.get("reason", "unspecified"),
# 91             timestamp=datetime.now(timezone.utc)
# 92         ))
# 93         return next_state
# 94 
# 95     def update_working_file_from_snapshot(self, snapshot_path: str):
# 96         after_file = Path(f"experiments/snapshots/{snapshot_path}.after")
# 97         if after_file.exists():
# 98             Path(self.working_file).write_text(after_file.read_text(encoding="utf-8"), encoding="utf-8")
# 99 
# 100     @abstractmethod
# 101     def _transition(self, state: dict, agent_output: str | None) -> dict:
# 102         raise NotImplementedError


# providers\tool_provider_base.py

# 1 from __future__ import annotations
# 2 from abc import abstractmethod
# 3 from app.providers.base_provider import BaseProvider
# 4 
# 5 from typing import TYPE_CHECKING
# 6 
# 7 if TYPE_CHECKING:
# 8     from app.providers.context_provider_base import ContextProviderBase
# 9     from app.providers.score_provider_base import ScoreProviderBase
# 10 
# 11 
# 12 class ToolProviderBase(BaseProvider):
# 13     """Base class for tool providers with optional context/score wiring."""
# 14 
# 15     def _run_provider(self, input: dict) -> str:
# 16         return self._run(input)
# 17 
# 18     def set_context_provider(self, provider: ContextProviderBase) -> None:
# 19         self.context_provider = provider
# 20 
# 21     def set_score_provider(self, provider: ScoreProviderBase) -> None:
# 22         self.score_provider = provider
# 23 
# 24     @abstractmethod
# 25     def _run(self, input: dict) -> str:
# 26         raise NotImplementedError


# utilities\__init__.py



# utilities\diff_utils.py

# 1 import difflib
# 2 
# 3 def summarize_diff(before: str, after: str) -> str:
# 4     """Returns a human-readable summary of what changed between two versions."""
# 5     before_lines = before.strip().splitlines()
# 6     after_lines = after.strip().splitlines()
# 7 
# 8     diff = list(difflib.unified_diff(before_lines, after_lines, lineterm=""))
# 9     if not diff:
# 10         return "No differences detected."
# 11 
# 12     summary = ["Changes:"]
# 13     for line in diff:
# 14         if line.startswith("+ ") and not line.startswith("+++"):
# 15             summary.append(f"Added: {line[2:]}")
# 16         elif line.startswith("- ") and not line.startswith("---"):
# 17             summary.append(f"Removed: {line[2:]}")
# 18 
# 19     return "\n".join(summary[:10])  #  Limit to first 10 lines


# utilities\extension_loader.py

# 1 from importlib.util import spec_from_file_location, module_from_spec
# 2 from pathlib import Path
# 3 from typing import Type
# 4 
# 5 def import_class_from_extension(artifact_path: str | Path, base_cls: Type) -> Type:
# 6     file_path = Path("extensions") / f"{artifact_path}.py"
# 7     if not file_path.exists():
# 8         raise FileNotFoundError(f"Extension file not found: {file_path}")
# 9 
# 10     spec = spec_from_file_location("dynamic_module", file_path)
# 11     if spec is None or spec.loader is None:
# 12         raise ImportError(f"Unable to load: {file_path}")
# 13 
# 14     module = module_from_spec(spec)
# 15     spec.loader.exec_module(module)
# 16 
# 17     for attr in module.__dict__.values():
# 18         if isinstance(attr, type) and issubclass(attr, base_cls) and attr is not base_cls:
# 19             return attr
# 20 
# 21     raise ImportError(f"No subclass of {base_cls.__name__} found in: {file_path}")


# utilities\file_management\__init__.py



# utilities\metadata\__init__.py



# utilities\metadata\footer\__init__.py



# utilities\metadata\footer\code_annnotation_utils.py

# 1 from __future__ import annotations
# 2 from typing import Tuple
# 3 
# 4 FOOTER_START = "#  --- Agent Notes"
# 5 FOOTER_END = "#  -----------------------------------------------"
# 6 
# 7 def strip_agent_notes(file_content: str) -> str:
# 8     """Remove the agent notes footer from the file (if present)."""
# 9     lines = file_content.strip().splitlines()
# 10     start_idx = None
# 11 
# 12     for i, line in enumerate(lines):
# 13         if line.strip().startswith(FOOTER_START):
# 14             start_idx = i
# 15             break
# 16 
# 17     return "\n".join(lines[:start_idx]) if start_idx is not None else file_content.strip()
# 18 
# 19 
# 20 def append_agent_note(file_content: str, system: str, agent_name: str, note: str) -> str:
# 21     """Append a new agent note to the bottom of the file."""
# 22     base = strip_agent_notes(file_content)
# 23 
# 24     formatted_note = (
# 25         f"{FOOTER_START} ({system} / {agent_name}) ---\n" +
# 26         "\n".join(f"#  {line.strip()}" for line in note.strip().splitlines()) +
# 27         f"\n{FOOTER_END}"
# 28     )
# 29 
# 30     return base.rstrip() + "\n\n" + formatted_note
# 31 
# 32 
# 33 def split_code_and_notes(file_content: str) -> Tuple[str, str]:
# 34     """Return (stripped_code, extracted_footer_notes)"""
# 35     lines = file_content.strip().splitlines()
# 36     start_idx = None
# 37 
# 38     for i, line in enumerate(lines):
# 39         if line.strip().startswith(FOOTER_START):
# 40             start_idx = i
# 41             break
# 42 
# 43     if start_idx is not None:
# 44         code_part = "\n".join(lines[:start_idx]).strip()
# 45         notes_part = "\n".join(lines[start_idx:]).strip()
# 46         return code_part, notes_part
# 47 
# 48     return file_content.strip(), ""


# utilities\metadata\logging\__init__.py



# utilities\metadata\logging\conversation_log.py

# 1 from __future__ import annotations
# 2 from typing import List
# 3 from sqlalchemy.orm import Session
# 4 from app.db.models import AgentConversationLog
# 5 
# 6 def get_conversation_log(session: Session, session_id: str, system: str) -> List[str]:
# 7     """Returns logged conversation entries for the specified session and system."""
# 8     logs = (
# 9         session.query(AgentConversationLog)
# 10         .filter_by(session_id=session_id, system=system)
# 11         .order_by(AgentConversationLog.timestamp.asc())
# 12         .all()
# 13     )
# 14 
# 15     return [log.content for log in logs if log.content]


# utilities\metadata\logging\logging_provider.py

# 1 from __future__ import annotations
# 2 
# 3 from datetime import datetime
# 4 import json
# 5 import logging
# 6 import sqlite3
# 7 from dataclasses import asdict, is_dataclass
# 8 from enum import Enum
# 9 from pathlib import Path
# 10 from typing import Any, ClassVar, Iterable
# 11 from app.db.connection import get_connection
# 12 from app.enums.logging_enums import LogType
# 13 from app.db.schemas import (
# 14     StateTransitionLogSchema,
# 15     AgentConversationLogSchema,
# 16     ErrorLogSchema,
# 17     ProviderLogSchema,
# 18 )
# 19 
# 20 LOG_CONFIG_MAP = {
# 21     LogType.STATE_TRANSITION: {"schema": StateTransitionLogSchema, "table": "state_transition_log"},
# 22     LogType.AGENT_CONVERSATION: {"schema": AgentConversationLogSchema, "table": "agent_conversation_log"},
# 23     LogType.PROVIDER: {"schema": ProviderLogSchema, "table": "provider_log"},
# 24     LogType.ERROR: {"schema": ErrorLogSchema, "table": "error_log"},
# 25 }
# 26 
# 27 class LoggingProvider:
# 28     """Centralized provider for structured logging."""
# 29 
# 30     _instance: ClassVar["LoggingProvider" | None] = None
# 31 
# 32     def __new__(cls, *args, **kwargs):
# 33         if cls._instance is None:
# 34             cls._instance = super().__new__(cls)
# 35         return cls._instance
# 36 
# 37     def __init__(
# 38         self,
# 39         db_path: str | Path = "experiments/codecritic.sqlite3",
# 40         output_path: str | Path | None = None,
# 41         connection: sqlite3.Connection | None = None,
# 42     ) -> None:
# 43         if getattr(self, "_initialized", False):
# 44             if connection is not None:
# 45                 self.conn = connection
# 46             return
# 47 
# 48         self.db_path = Path(db_path)
# 49         self.db_path.parent.mkdir(exist_ok=True, parents=True)
# 50 
# 51         self.conn = connection or get_connection()
# 52 
# 53         self.output_path = Path(output_path) if output_path else None
# 54         if self.output_path:
# 55             self.output_path.parent.mkdir(parents=True, exist_ok=True)
# 56 
# 57         self._initialized = True
# 58 
# 59     def _serialize(self, obj: Any) -> dict:
# 60         def _safe(v: Any) -> Any:
# 61             if isinstance(v, (Enum, Path)):
# 62                 return str(v)
# 63             elif isinstance(v, datetime):
# 64                 return v.isoformat()
# 65             elif isinstance(v, list):
# 66                 return [_safe(i) for i in v]
# 67             elif isinstance(v, dict):
# 68                 return json.dumps({k: _safe(val) for k, val in v.items()})
# 69             return v
# 70 
# 71         if not is_dataclass(obj) or isinstance(obj, type):
# 72             raise TypeError(f"Expected dataclass instance, got {type(obj)}")
# 73         raw = asdict(obj)
# 74         return {k: _safe(v) for k, v in raw.items()}
# 75 
# 76     def _insert_many(self, table: str, items: Iterable[dict]) -> None:
# 77         items = list(items)
# 78         if not items:
# 79             return
# 80 
# 81         keys = list(items[0].keys())
# 82         cols = ",".join(keys)
# 83         placeholders = ",".join(["?"] * len(keys))
# 84         values = [tuple(i[k] for k in keys) for i in items]
# 85         cur = self.conn.cursor()
# 86         cur.executemany(f"INSERT INTO {table} ({cols}) VALUES ({placeholders})", values)
# 87         self.conn.commit()
# 88         if self.output_path:
# 89             with self.output_path.open("a", encoding="utf-8") as fh:
# 90                 for item in items:
# 91                     fh.write(json.dumps(item) + "\n")
# 92 
# 93     def write(self, log_type: LogType, entries: list[Any] | Any) -> None:
# 94         if not isinstance(entries, list):
# 95             entries = [entries]
# 96 
# 97         config = LOG_CONFIG_MAP.get(log_type)
# 98         if config is None:
# 99             raise ValueError(f"Unsupported log type: {log_type}")
# 100 
# 101         schema_cls = config["schema"]
# 102         table_name = config["table"]
# 103 
# 104         #  Log and allow class name mismatches as a warning (helpful for shadowed classes)
# 105         for entry in entries:
# 106             if not is_dataclass(entry):
# 107                 raise TypeError(f"Expected a dataclass instance, got {type(entry)}")
# 108             if entry.__class__.__name__ != schema_cls.__name__:
# 109                 logging.getLogger("LoggingProvider").warning(
# 110                     f"⚠️ Log schema mismatch: expected {schema_cls.__name__}, got {entry.__class__.__name__}"
# 111                 )
# 112 
# 113         serialized = [self._serialize(e) for e in entries]
# 114         self._insert_many(table_name, serialized)
# 115 
# 116     def log_provider(self, log: ProviderLogSchema) -> None:
# 117         self.write(LogType.PROVIDER, log)
# 118 
# 119     def log_state_transition(self, log: StateTransitionLogSchema) -> None:
# 120         self.write(LogType.STATE_TRANSITION, log)
# 121 
# 122     def log_agent_conversation(self, log: AgentConversationLogSchema) -> None:
# 123         self.write(LogType.AGENT_CONVERSATION, log)
# 124 
# 125     def log_error(self, log: ErrorLogSchema) -> None:
# 126         self.write(LogType.ERROR, log)
# 127 
# 128     def close(self) -> None:
# 129         self.conn.close()
# 130 
# 131 
# 132 class LoggingMixin:
# 133     """Mixin to provide access to a shared LoggingProvider instance."""
# 134 
# 135     def __init__(self, logger: LoggingProvider | None = None) -> None:
# 136         self.logger = logger or LoggingProvider()
# 137         self._log = logging.getLogger(self.__class__.__name__)
# 138 
# 139     def log_provider(self, log: ProviderLogSchema) -> None:
# 140         self.logger.log_provider(log)
# 141 
# 142     def log_state_transition(self, log: StateTransitionLogSchema) -> None:
# 143         self.logger.log_state_transition(log)
# 144 
# 145     def log_agent_conversation(self, log: AgentConversationLogSchema) -> None:
# 146         self.logger.log_agent_conversation(log)
# 147 
# 148     def log_error(self, log: ErrorLogSchema) -> None:
# 149         self.logger.log_error(log)


# utilities\metadata\snapshots\__init__.py



# utilities\metadata\snapshots\snapshot_reader.py

# 1 import json
# 2 from pathlib import Path
# 3 
# 4 def read_latest_snapshot(session_id: str, root="experiments/snapshots"):
# 5     session_root = Path(root) / session_id
# 6     if not session_root.exists():
# 7         return None
# 8 
# 9     snapshots = sorted(session_root.glob("*.before"), key=lambda p: p.stat().st_mtime, reverse=True)
# 10     if not snapshots:
# 11         return None
# 12 
# 13     latest_snapshot_id = snapshots[0].stem
# 14     before = (session_root / f"{latest_snapshot_id}.before").read_text(encoding="utf-8")
# 15     after = (session_root / f"{latest_snapshot_id}.after").read_text(encoding="utf-8")
# 16 
# 17     return {"before": before, "after": after}


# utilities\metadata\snapshots\snapshot_writer.py

# 1 import uuid
# 2 from pathlib import Path
# 3 
# 4 class SnapshotWriter:
# 5     def __init__(self, root: str | Path = None) -> None:
# 6         if root is None:
# 7             PROJECT_ROOT = Path(__file__).resolve().parent.parent.parent.parent.parent
# 8             self.root = PROJECT_ROOT / "experiments" / "snapshots"
# 9         else:
# 10             self.root = Path(root)
# 11         self.root.mkdir(parents=True, exist_ok=True)
# 12 
# 13     def write_snapshot(self, *, before: str, after: str, session_id: str) -> str:
# 14         if before == after:
# 15             return ""
# 16 
# 17         session_root = self.root / session_id
# 18         session_root.mkdir(parents=True, exist_ok=True)
# 19 
# 20         snapshot_id = str(uuid.uuid4())
# 21         (session_root / f"{snapshot_id}.before").write_text(before, encoding="utf-8")
# 22         (session_root / f"{snapshot_id}.after").write_text(after, encoding="utf-8")
# 23 
# 24         return snapshot_id


# utilities\pydantic_compat.py

# 1 from __future__ import annotations
# 2 
# 3 from typing import TYPE_CHECKING, Callable
# 4 
# 5 try:  #  pragma: no cover - pydantic v1 compatibility
# 6     from pydantic import BaseModel
# 7 
# 8     if not hasattr(BaseModel, "model_dump"):
# 9         from typing import Callable
# 10 
# 11         def _model_dump(self: BaseModel, **kwargs) -> dict:
# 12             return BaseModel.dict(self, **kwargs)
# 13 
# 14         setattr(BaseModel, "model_dump", _model_dump)  #  type: ignore[attr-defined]
# 15 except Exception:  #  pragma: no cover - ignore if pydantic v2
# 16     pass
# 17 
# 18 if TYPE_CHECKING:
# 19     from pydantic import field_validator, model_validator  #  type: ignore
# 20 else:  #  pragma: no cover - runtime fallback for pydantic v1
# 21     try:
# 22         from pydantic import field_validator, model_validator  #  type: ignore
# 23     except ImportError:  #  pragma: no cover - pydantic v1
# 24         from pydantic import validator, root_validator
# 25 
# 26         def field_validator(*fields: str, **kwargs) -> Callable:
# 27             kwargs.pop("mode", None)
# 28             return validator(*fields, **kwargs)
# 29 
# 30         def model_validator(*fields, **kwargs) -> Callable:
# 31             mode = kwargs.pop("mode", "after")
# 32 
# 33             def decorator(func: Callable) -> Callable:
# 34                 if mode == "after":
# 35 
# 36                     def wrapper(cls, values):
# 37                         obj = cls.construct(**values)
# 38                         func(obj)
# 39                         return obj.dict()
# 40 
# 41                     return root_validator(pre=False, allow_reuse=True)(wrapper)
# 42 
# 43                 def wrapper(cls, values):
# 44                     return func(values)
# 45 
# 46                 return root_validator(pre=True, allow_reuse=True)(wrapper)
# 47 
# 48             return decorator


# utilities\schema\__init__.py

# 1 from .create_schema import initialize_database, create_tables, load_seed_data
# 2 
# 3 __all__ = ["initialize_database", "create_tables", "load_seed_data"]


# utilities\schema\create_schema.py

# 1 #  from pydantic import BaseModel, Field
# 2 #  from pathlib import Path
# 3 #  from typing import Dict, List, Optional, ClassVar
# 4 #  from uuid import UUID, uuid4
# 5 #  from app.enums.system_enums import SystemType
# 6 #  from app.utilities.pydantic_compat import field_validator
# 7 
# 8 #  class AgentPromptSchema(BaseModel):
# 9 #      id: Optional[int]
# 10 #      guid: UUID = Field(default_factory=uuid4)
# 11 #      name: str
# 12 #      description: Optional[str]
# 13 #      artifact_path: Path
# 14 #      tags: Optional[List[str]] = None
# 15 
# 16 #      table_name: ClassVar[str] = "agent_prompt"
# 17 
# 18 #      @field_validator("artifact_path")
# 19 #      @classmethod
# 20 #      def _check_path(cls, v: Path) -> Path:
# 21 #          if not v.is_absolute() and ".." in v.parts:
# 22 #              raise ValueError("Invalid artifact path")
# 23 #          return v
# 24 
# 25 #  class SystemPromptSchema(BaseModel):
# 26 #      id: Optional[int]
# 27 #      guid: UUID = Field(default_factory=uuid4)
# 28 #      name: str
# 29 #      system_type: SystemType
# 30 #      description: Optional[str]
# 31 #      artifact_path: Path
# 32 #      tags: Optional[List[str]] = None
# 33 
# 34 #      table_name: ClassVar[str] = "system_prompt"
# 35 
# 36 #      @field_validator("artifact_path")
# 37 #      @classmethod
# 38 #      def _check_path(cls, v: Path) -> Path:
# 39 #          if not v.is_absolute() and ".." in v.parts:
# 40 #              raise ValueError("Invalid artifact path")
# 41 #          return v
# 42 
# 43 #  class ToolConfig(BaseModel):
# 44 #      id: Optional[int] = None
# 45 #      guid: UUID = Field(default_factory=uuid4)
# 46 #      name: str
# 47 #      description: Optional[str] = None
# 48 #      config: Optional[Dict] = None
# 49 #      artifact_path: Path
# 50 
# 51 #      table_name: ClassVar[str] = "tool_config"
# 52 
# 53 #      @field_validator("artifact_path")
# 54 #      @classmethod
# 55 #      def _check_path(cls, v: Path) -> Path:
# 56 #          if not v.is_absolute() and ".." in v.parts:
# 57 #              raise ValueError("Invalid artifact path")
# 58 #          return v
# 59 
# 60 #  class ScoreProvider(BaseModel):
# 61 #      id: Optional[int] = None
# 62 #      guid: UUID = Field(default_factory=uuid4)
# 63 #      name: str
# 64 #      description: Optional[str] = None
# 65 #      artifact_path: Optional[Path] = None
# 66 
# 67 #      table_name: ClassVar[str] = "score_provider"
# 68 
# 69 #      @field_validator("artifact_path")
# 70 #      @classmethod
# 71 #      def _check_path(cls, v: Optional[Path]) -> Optional[Path]:
# 72 #          if v is None:
# 73 #              return v
# 74 #          p = Path(v)
# 75 #          if not p.is_absolute() and ".." in p.parts:
# 76 #              raise ValueError("artifact_path must be absolute or project relative")
# 77 #          return p
# 78 
# 79 #      def model_dump(self, **kwargs) -> dict:
# 80 #          return super().model_dump(**kwargs)



# === NON-PYTHON FILES ===

# db\seeders\files\prompts\basic_agent_prompt.txt

# 1 You are a code generation agent that produces basic, clean, formatted Python code snippets based on given instructions.


# db\seeders\files\prompts\default_system_prompt.txt

# 1 You are the default formatting system. Your task is to ensure all provided Python code strictly adheres to PEP8 standards.


# db\seeders\files\prompts\linting_generator_agent_prompt.txt

# 1 You are the Generator Agent in the Linting System.
# 2 
# 3 You specialize in incrementally improving Python code quality by addressing lint violations, enhancing readability, and promoting best practices.
# 4 
# 5 Your strategy is:
# 6 - Focus on minimal, targeted improvements that preserve original structure
# 7 - Apply type hints and clarify variable names where appropriate
# 8 - Use reasoning to resolve tool-reported issues when they conflict
# 9 
# 10 You will be evaluated by a Discriminator Agent and may be prompted to revise your output. If you are in a retry round, incorporate prior feedback before retrying.
# 11 
# 12 Maintain a helpful, confident tone in your comments. Your improvements should be easy for a human developer to understand and accept.


# db\seeders\files\prompts\linting_system_prompt.txt

# 1 You are operating within the Linting System, which enforces strict Python code quality and formatting compliance.
# 2 
# 3 All Generator Agents must:
# 4 - Conform to Python PEP8 guidelines
# 5 - Use tools such as `black`, `ruff`, `mypy`, and `radon` for diagnostic feedback
# 6 - Preserve original code intent and structure; avoid stylistic or structural rewrites unless necessary
# 7 - Produce valid, executable Python code
# 8 
# 9 Your response must include your corrected code inside a [CODE] block. Any content outside this block will be ignored by the system:
# 10 
# 11 [CODE]
# 12 #  your corrected Python code
# 13 [/CODE]
# 14 
# 15 After the code, include a structured log entry describing your decisions:
# 16 
# 17 [CONVERSATION_LOG_ENTRY]
# 18 Summarize what you changed, why you changed it, and any tradeoffs or notable decisions you made.
# 19 [/CONVERSATION_LOG_ENTRY]


