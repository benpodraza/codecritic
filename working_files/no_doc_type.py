def compute(x: int, y: int) -> int:
    return x * y
# --- Agent Notes (linting / linting_generator_agent_provider) ---
# - Added type hints to the function `compute` to specify that it takes two integers and returns an integer. This improves code clarity and helps with static type checking.
# - Ensured the code adheres to PEP8 guidelines by adding spaces after commas.
# - The original logic and structure of the code were preserved. No significant tradeoffs were made; the changes are minimal and maintain the original intent of the code.
# -----------------------------------------------
