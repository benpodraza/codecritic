from app.systems.preprocessing.agents.discriminator.discriminator_agent import DiscriminatorAgent as PreprocessingDiscriminatorAgent

class DiscriminatorAgent(PreprocessingDiscriminatorAgent):
    pass
