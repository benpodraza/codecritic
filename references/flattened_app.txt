
# === PYTHON FILES ===

# __init__.py



# db\__init__.py

# 1 import gc
# 2 from sqlalchemy import create_engine
# 3 from app.db.connection import DB_PATH, close_connection
# 4 from app.db.models import Base
# 5 
# 6 def init_db(reset: bool = False):
# 7     #  Explicitly close lingering DB-API connections
# 8     close_connection()
# 9 
# 10     #  Explicitly create and dispose of engine before deletion
# 11     engine = create_engine(f"sqlite:///{DB_PATH}")
# 12     engine.dispose()
# 13 
# 14     if reset and DB_PATH.exists():
# 15         try:
# 16             DB_PATH.unlink()
# 17         except PermissionError as e:
# 18             print(f"⚠️ Unable to delete {DB_PATH}: {e}")
# 19             print("⚠️ Attempting to force garbage collection.")
# 20             gc.collect()
# 21             DB_PATH.unlink()  #  try again after GC
# 22 
# 23     #  Recreate engine after deletion
# 24     engine = create_engine(f"sqlite:///{DB_PATH}")
# 25     Base.metadata.create_all(bind=engine)
# 26     
# 27     return engine


# db\base.py

# 1 from sqlalchemy.orm import declarative_base
# 2 
# 3 Base = declarative_base()


# db\connection.py

# 1 from pathlib import Path
# 2 import sqlite3
# 3 
# 4 DB_PATH = Path("experiments") / "codecritic.sqlite3"
# 5 _CONN: sqlite3.Connection | None = None
# 6 
# 7 def get_connection() -> sqlite3.Connection:
# 8     global _CONN
# 9     try:
# 10         if _CONN is None:
# 11             raise RuntimeError
# 12         _CONN.execute("SELECT 1")
# 13     except (sqlite3.ProgrammingError, RuntimeError):
# 14         _CONN = sqlite3.connect(DB_PATH, check_same_thread=False)
# 15     return _CONN
# 16 
# 17 def close_connection() -> None:
# 18     global _CONN
# 19     if _CONN:
# 20         _CONN.close()
# 21         _CONN = None


# db\models.py

# 1 from datetime import datetime, timezone
# 2 from typing import Optional
# 3 from sqlalchemy import Column, DateTime, Enum, Float, Integer, String, JSON
# 4 from sqlalchemy.dialects.sqlite import TEXT
# 5 from app.db.base import Base
# 6 from uuid import uuid4
# 7 
# 8 from app.enums.agent_enums import AGENT
# 9 from app.enums.system_enums import SYSTEM
# 10 from sqlalchemy.orm import Mapped, mapped_column
# 11 
# 12 # #  PROMPT CONFIGS
# 13 
# 14 class AgentPrompt(Base):
# 15     __tablename__ = 'agent_prompt'
# 16     id = Column(Integer, primary_key=True, index=True)
# 17     guid = Column(TEXT, unique=True, default=lambda: str(uuid4()))
# 18     name = Column(String, nullable=False)
# 19     description = Column(String)
# 20     artifact_path = Column(String, nullable=False)
# 21     tags = Column(JSON, nullable=True)
# 22 
# 23 class SystemPrompt(Base):
# 24     __tablename__ = 'system_prompt'
# 25     id = Column(Integer, primary_key=True, index=True)
# 26     guid = Column(TEXT, unique=True, default=lambda: str(uuid4()))
# 27     name = Column(String, nullable=False)
# 28     system_type = Column(Enum(SYSTEM), nullable=False) 
# 29     description = Column(String)
# 30     artifact_path = Column(String, nullable=False)
# 31     tags = Column(JSON, nullable=True)
# 32 
# 33 
# 34 # #  PROVIDER CONFGIS
# 35 
# 36 class PromptProviderConfig(Base):
# 37     __tablename__ = "prompt_provider_config"
# 38 
# 39     id = Column(Integer, primary_key=True, index=True)
# 40     guid = Column(String, unique=True, nullable=False)
# 41     name = Column(String, nullable=False)
# 42     description = Column(String, nullable=True)
# 43     artifact_path = Column(String, nullable=False)
# 44     config = Column(JSON, nullable=False) 
# 45     tags = Column(JSON, nullable=True)
# 46 
# 47 class ToolProviderConfig(Base):
# 48     __tablename__ = "tool_provider_config"
# 49     id = Column(Integer, primary_key=True)
# 50     guid = Column(String, unique=True, nullable=False)
# 51     name = Column(String, nullable=False)
# 52     description = Column(String, nullable=True)
# 53     config = Column(JSON, nullable=True)
# 54     artifact_path = Column(String, nullable=False)
# 55     tags = Column(JSON, nullable=True)
# 56 
# 57 class ScoreProviderConfig(Base):
# 58     __tablename__ = "score_provider_config" 
# 59     id = Column(Integer, primary_key=True)
# 60     guid = Column(String, unique=True, nullable=False)
# 61     name = Column(String, nullable=False)
# 62     description = Column(String, nullable=True)
# 63     config = Column(JSON, nullable=True)
# 64     artifact_path = Column(String, nullable=False)
# 65     tags = Column(JSON, nullable=True)
# 66 
# 67 class ContextProviderConfig(Base):
# 68     __tablename__ = "context_provider_config" 
# 69     id = Column(Integer, primary_key=True)
# 70     guid = Column(String, unique=True, nullable=False)
# 71     name = Column(String, nullable=False)
# 72     description = Column(String, nullable=True)
# 73     config = Column(JSON, nullable=True)
# 74     artifact_path = Column(String, nullable=False)
# 75     tags = Column(JSON, nullable=True)
# 76 
# 77 class AgentEngineProviderConfig(Base):
# 78     __tablename__ = "agent_engine_provider_config" 
# 79 
# 80     id = Column(Integer, primary_key=True)
# 81     guid = Column(String, unique=True, nullable=False)
# 82     name = Column(String, nullable=False)
# 83     description = Column(String, nullable=True)
# 84     model= Column(String, nullable=False)
# 85     config = Column(JSON, nullable=True)
# 86     cost_per_1k_tokens = Column(Float, nullable=True, default=0.0)
# 87     artifact_path = Column(String, nullable=False)
# 88     tags = Column(JSON, nullable=True)
# 89 
# 90 
# 91 class AgentProviderConfig(Base):
# 92     __tablename__ = "agent_provider_config" 
# 93 
# 94     id = Column(Integer, primary_key=True)
# 95     guid = Column(String, unique=True, nullable=False)
# 96     name = Column(String, nullable=False)
# 97     description = Column(String, nullable=True)
# 98     agent_type = Column(Enum(AGENT), nullable=False) 
# 99     config = Column(JSON, nullable=True)
# 100     artifact_path = Column(String, nullable=False)
# 101     tags = Column(JSON, nullable=True)
# 102 
# 103 class StateProviderConfig(Base):
# 104     __tablename__ = "state_provider_config"
# 105 
# 106     id = Column(Integer, primary_key=True)
# 107     guid = Column(TEXT, unique=True, default=lambda: str(uuid4()))
# 108     name = Column(String, nullable=False)
# 109     description = Column(String, nullable=True)
# 110     config = Column(JSON, nullable=True)
# 111     artifact_path = Column(String, nullable=False)
# 112     tags = Column(JSON, nullable=True)
# 113 
# 114 class SystemProviderConfig(Base):
# 115     __tablename__ = "system_provider_config"
# 116 
# 117     id = Column(Integer, primary_key=True)
# 118     guid = Column(TEXT, unique=True, default=lambda: str(uuid4()))
# 119     name = Column(String, nullable=False)
# 120     description = Column(String, nullable=True)
# 121     config = Column(JSON, nullable=True)
# 122     artifact_path = Column(String, nullable=False)
# 123     tags = Column(JSON, nullable=True)
# 124     system_type = Column(String, nullable=False)
# 125 
# 126 class ControllerProviderConfig(Base):
# 127     __tablename__ = "controller_provider_config"
# 128 
# 129     id = Column(Integer, primary_key=True)
# 130     guid = Column(TEXT, unique=True, default=lambda: str(uuid4()))
# 131     name = Column(String, nullable=False)
# 132     description = Column(String, nullable=True)
# 133     config = Column(JSON, nullable=True)   
# 134     artifact_path = Column(String, nullable=False)
# 135     tags = Column(JSON, nullable=True)
# 136 
# 137 
# 138 class ProgramProviderConfig(Base):
# 139     __tablename__ = "program_provider_config"
# 140 
# 141     id = Column(Integer, primary_key=True)
# 142     guid = Column(TEXT, unique=True, default=lambda: str(uuid4()))
# 143     name = Column(String, nullable=False)
# 144     description = Column(String, nullable=True)
# 145     config = Column(JSON, nullable=True)
# 146     artifact_path = Column(String, nullable=False)
# 147     tags = Column(JSON, nullable=True)
# 148 
# 149 class SessionConfig(Base):
# 150     __tablename__ = "session_config"
# 151 
# 152     id = Column(Integer, primary_key=True)
# 153     program_provider_id = Column(String, unique=True, nullable=False)
# 154     name = Column(String, nullable=False)
# 155     description = Column(String, nullable=True)
# 156     environment_type = Column(String, nullable=False)  #  e.g., "experiment", "live", "staging"
# 157     tags = Column(JSON, nullable=True)
# 158 
# 159 
# 160 # #  LOGS
# 161 
# 162 class ProviderLog(Base):
# 163     __tablename__ = "provider_log"
# 164 
# 165     id = Column(Integer, primary_key=True, index=True)
# 166     session_id = Column(String, nullable=False)
# 167     timestamp = Column(DateTime, nullable=False, default=datetime.now(timezone.utc))
# 168     provider_id = Column(Integer, nullable=False)
# 169     provider_type = Column(String, nullable=False)
# 170     input = Column(String, nullable=True)
# 171     output = Column(String, nullable=True)
# 172     output_schema = Column(String, nullable=True) 
# 173     latency_ms = Column(Integer, nullable=True)   
# 174     config_hash = Column(String, nullable=True)   
# 175     file_name = Column(String, nullable=True)
# 176     called_by_type = Column(String, nullable=True)
# 177     called_by_id = Column(Integer, nullable=True)
# 178     run_id = Column(TEXT, nullable=False, index=True)
# 179     file_log_id = Column(TEXT, nullable=True)
# 180     parent_id = Column(TEXT, nullable=True)
# 181     execution_chain = Column(JSON, nullable=False, default=list)
# 182 
# 183     
# 184 class StateTransitionLog(Base):
# 185     __tablename__ = "state_transition_log"
# 186 
# 187     id = Column(Integer, primary_key=True, index=True)
# 188     session_id = Column(String, nullable=False)
# 189     timestamp = Column(DateTime, nullable=False, default=datetime.now(timezone.utc))
# 190     entity_type = Column(String, nullable=False)
# 191     entity_id = Column(Integer, nullable=False)
# 192     from_state = Column(String, nullable=False)
# 193     to_state = Column(String, nullable=False)
# 194     reason = Column(String, nullable=True)
# 195     decision = Column(String, nullable=True)    
# 196     triggered_by = Column(String, nullable=True)   
# 197     step = Column(Integer, nullable=True)   
# 198     transition_metadata: Mapped[Optional[dict]] = mapped_column(JSON, nullable=True)
# 199     input_snapshot_id = Column(String, nullable=True)
# 200     output_snapshot_id = Column(String, nullable=True)
# 201     run_id = Column(TEXT, nullable=False, index=True)
# 202     called_by_type = Column(String, nullable=True)
# 203     called_by_id = Column(Integer, nullable=True) 
# 204     file_log_id = Column(TEXT, nullable=True)
# 205     parent_id = Column(TEXT, nullable=True)
# 206     execution_chain = Column(JSON, nullable=False, default=list)
# 207 
# 208 class AgentConversationLog(Base):
# 209     __tablename__ = "agent_conversation_log"
# 210 
# 211     id = Column(Integer, primary_key=True, index=True)
# 212     session_id = Column(String, nullable=False)
# 213     system = Column(String, nullable=False)
# 214     agent_provider_config_id = Column(Integer, nullable=False)
# 215     agent_type = Column(String, nullable=False) 
# 216     content = Column(String, nullable=False)
# 217     timestamp = Column(DateTime, nullable=False, default=datetime.now(timezone.utc))
# 218     run_id = Column(TEXT, nullable=False, index=True)
# 219     file_log_id = Column(TEXT, nullable=True)
# 220     
# 221 
# 222 class ErrorLog(Base):
# 223     __tablename__ = "error_log"
# 224 
# 225     id = Column(Integer, primary_key=True, index=True)
# 226     session_id = Column(String, nullable=False)
# 227     timestamp = Column(DateTime, nullable=False, default=datetime.now(timezone.utc))
# 228     latency_ms = Column(Integer, nullable=True) 
# 229     provider_id = Column(Integer, nullable=True)
# 230     provider_type = Column(String, nullable=True)
# 231     error_type = Column(String, nullable=False)
# 232     message = Column(String, nullable=False)
# 233     file_path = Column(String, nullable=True)
# 234     called_by_type = Column(String, nullable=True)
# 235     called_by_id = Column(Integer, nullable=True)
# 236     run_id = Column(TEXT, nullable=False, index=True)
# 237     file_log_id = Column(TEXT, nullable=True)
# 238     parent_id = Column(TEXT, nullable=True)
# 239     execution_chain = Column(JSON, nullable=False, default=list)
# 240 
# 241 class SnapshotMetrics(Base):
# 242     __tablename__ = "snapshot_metrics"
# 243 
# 244     id = Column(Integer, primary_key=True)
# 245     session_id = Column(String, nullable=False)
# 246     snapshot_id = Column(String, nullable=False)
# 247     system = Column(String, nullable=True)
# 248     agent = Column(String, nullable=True)
# 249     agent_type = Column(Enum(AGENT), nullable=True)
# 250     agent_id = Column(Integer, nullable=True)
# 251     score = Column(Float, nullable=True)
# 252     state = Column(String, nullable=True)
# 253     decision = Column(String, nullable=True)
# 254     timestamp = Column(DateTime, nullable=False, default=datetime.now(timezone.utc))
# 255     file_log_id = Column(TEXT, nullable=True)
# 256 
# 257     #  structural metrics
# 258     line_count_before = Column(Integer)
# 259     line_count_after = Column(Integer)
# 260     function_count_before = Column(Integer)
# 261     function_count_after = Column(Integer)
# 262     symbol_count_before = Column(Integer)
# 263     symbol_count_after = Column(Integer)
# 264     branch_count_before = Column(Integer)
# 265     branch_count_after = Column(Integer)
# 266     comment_count_before = Column(Integer)
# 267     comment_count_after = Column(Integer)
# 268 
# 269     #  deltas
# 270     line_count_delta = Column(Integer)
# 271     function_count_delta = Column(Integer)
# 272     symbol_count_delta = Column(Integer)
# 273     branch_count_delta = Column(Integer)
# 274     comment_count_delta = Column(Integer)
# 275 
# 276 
# 277 class FileLog(Base):
# 278     __tablename__ = "file_log"
# 279 
# 280     id = Column(Integer, primary_key=True, autoincrement=True)
# 281     session_id = Column(String, nullable=False)
# 282     file_name = Column(String, nullable=False)
# 283     original_path = Column(String)
# 284     length_bytes = Column(Integer)
# 285     created_at = Column(DateTime, default=datetime.now(timezone.utc))


# db\schemas.py

# 1 from dataclasses import dataclass, field
# 2 from datetime import datetime, timezone
# 3 from typing import Any, Dict, List, Optional
# 4 from pathlib import Path
# 5 from uuid import UUID, uuid4
# 6 from pydantic import BaseModel, Field, field_validator
# 7 
# 8 from app.enums.fsm_enums import DECISION_TYPE, STATE_TYPE, TRANSITION_REASON_TYPE
# 9 from app.enums.logging_enums import ERROR_TYPE, PROVIDER_TYPE
# 10 from app.enums.scoring_enums import SCORING_METRIC_TYPE
# 11 from app.enums.controller_enums import CONTROLLER
# 12 from app.enums.system_enums import SYSTEM
# 13 from app.enums.state_enums import STATE
# 14 from app.enums.agent_enums import AGENT
# 15 from app.enums.agent_engine_enums import AGENT_ENGINE_MODEL
# 16 
# 17 
# 18 class AgentPromptSchema(BaseModel):
# 19     id: Optional[int] = None
# 20     guid: UUID = Field(default_factory=uuid4)
# 21     name: str
# 22     description: Optional[str] = None
# 23     artifact_path: Path
# 24     tags: Optional[List[str]] = None
# 25 
# 26     @field_validator("artifact_path")
# 27     @classmethod
# 28     def validate_artifact_path(cls, v: Path) -> Path:
# 29         if not v.is_absolute() and ".." in v.parts:
# 30             raise ValueError("Invalid artifact path")
# 31         return v
# 32 
# 33 class SystemPromptSchema(BaseModel):
# 34     id: Optional[int] = None
# 35     guid: UUID = Field(default_factory=uuid4)
# 36     name: str
# 37     system_type: SYSTEM
# 38     description: Optional[str] = None
# 39     artifact_path: Path
# 40     tags: Optional[List[str]] = None
# 41 
# 42     @field_validator("artifact_path")
# 43     @classmethod
# 44     def validate_artifact_path(cls, v: Path) -> Path:
# 45         if not v.is_absolute() and ".." in v.parts:
# 46             raise ValueError("Invalid artifact path")
# 47         return v
# 48 
# 49 class PromptProviderConfig(BaseModel):
# 50     id: Optional[int] = None
# 51     guid: UUID = Field(default_factory=uuid4)
# 52     name: str
# 53     description: Optional[str] = None
# 54     artifact_path: Path
# 55     config: Optional[Dict[str, Any]] = None
# 56     tags: Optional[List[str]] = Field(default_factory=list)
# 57 
# 58     @field_validator("artifact_path")
# 59     @classmethod
# 60     def validate_artifact_path(cls, v: Path) -> Path:
# 61         if not v.is_absolute() and ".." in v.parts:
# 62             raise ValueError("Invalid artifact path")
# 63         return v
# 64 
# 65 class ToolProviderConfig(BaseModel):
# 66     id: Optional[int] = None
# 67     guid: UUID = Field(default_factory=uuid4)
# 68     name: str
# 69     description: Optional[str] = None
# 70     config: Optional[Dict[str, Any]] = None
# 71     artifact_path: Path
# 72     tags: Optional[List[str]] = None
# 73 
# 74     @field_validator("artifact_path")
# 75     @classmethod
# 76     def validate_artifact_path(cls, v: Path) -> Path:
# 77         if not v.is_absolute() and ".." in v.parts:
# 78             raise ValueError("Invalid artifact path")
# 79         return v
# 80 
# 81 class ScoreOutputSchema(BaseModel):
# 82     name: SCORING_METRIC_TYPE = Field(..., description="The type of scoring metric used")
# 83     value: float = Field(..., description="Final weighted score (normalized 0.0–1.0 scale)")
# 84     components: Dict[str, float] = Field(..., description="Tool/component-specific scores")
# 85     summary: Optional[str] = None
# 86 
# 87 
# 88 class ScoreProviderConfig(BaseModel):
# 89     id: Optional[int] = None
# 90     guid: UUID = Field(default_factory=uuid4)
# 91     name: str
# 92     description: Optional[str] = None
# 93     artifact_path: Optional[Path] = None
# 94     tags: Optional[List[str]] = None
# 95     table_name: str = "score_provider"
# 96 
# 97     @field_validator("artifact_path")
# 98     @classmethod
# 99     def validate_artifact_path(cls, v: Optional[Path]) -> Optional[Path]:
# 100         if v is None:
# 101             return v
# 102         if not v.is_absolute() and ".." in v.parts:
# 103             raise ValueError("Invalid artifact path")
# 104         return v
# 105         
# 106 class AgentEngineProviderConfig(BaseModel):
# 107     id: Optional[int] = None
# 108     guid: UUID = Field(default_factory=uuid4)
# 109     name: str
# 110     description: Optional[str] = None
# 111     model: AGENT_ENGINE_MODEL  #  ✅ Enum enforced
# 112     config: Optional[Dict[str, Any]] = None
# 113     cost_per_1k_tokens: Optional[float] = Field(default=0.0)
# 114     artifact_path: Path
# 115     tags: Optional[List[str]] = None
# 116 
# 117     @field_validator("artifact_path")
# 118     @classmethod
# 119     def validate_artifact_path(cls, v: Path) -> Path:
# 120         if not v.is_absolute() and ".." in v.parts:
# 121             raise ValueError("Invalid artifact path")
# 122         return v
# 123 
# 124 
# 125 class AgentProviderConfigSchema(BaseModel):
# 126     id: Optional[int] = None
# 127     guid: UUID = Field(default_factory=uuid4)
# 128     name: str
# 129     description: Optional[str] = None
# 130     config: Optional[Dict[str, Any]] = None
# 131     artifact_path: Path
# 132     tags: Optional[list[str]] = None
# 133     agent_type: AGENT = AGENT.UNKNOWN 
# 134 
# 135     @field_validator("artifact_path")
# 136     @classmethod
# 137     def validate_artifact_path(cls, v: Path) -> Path:
# 138         if not v.is_absolute() and ".." in v.parts:
# 139             raise ValueError("Invalid artifact path")
# 140         return v
# 141 
# 142 class StateProviderConfigSchema(BaseModel):
# 143     id: Optional[int] = None
# 144     guid: UUID = Field(default_factory=uuid4)
# 145     name: str
# 146     description: Optional[str] = None
# 147     config: Optional[Dict[str, Any]] = None
# 148     artifact_path: Path
# 149     tags: Optional[list[str]] = None
# 150 
# 151     @classmethod
# 152     def validate_artifact_path(cls, v: Path) -> Path:
# 153         if not v.is_absolute() and ".." in v.parts:
# 154             raise ValueError("Invalid artifact path")
# 155         return v
# 156 
# 157 class SystemProviderConfigSchema(BaseModel):
# 158     id: Optional[int] = None
# 159     guid: UUID = Field(default_factory=uuid4)
# 160     name: str
# 161     description: Optional[str] = None
# 162     config: Optional[Dict[str, Any]] = None
# 163     artifact_path: Path
# 164     tags: Optional[list[str]] = None
# 165     system_type: SYSTEM
# 166 
# 167     @classmethod
# 168     def validate_artifact_path(cls, v: Path) -> Path:
# 169         if not v.is_absolute() and ".." in v.parts:
# 170             raise ValueError("Invalid artifact path")
# 171         return v
# 172 
# 173 class ControllerProviderConfigSchema(BaseModel):
# 174     id: Optional[int] = None
# 175     guid: UUID = Field(default_factory=uuid4)
# 176     name: str
# 177     description: Optional[str] = None
# 178     config: Optional[Dict[str, Any]] = None
# 179     artifact_path: Path
# 180     tags: Optional[list[str]] = None
# 181 
# 182     @classmethod
# 183     def validate_artifact_path(cls, v: Path) -> Path:
# 184         if not v.is_absolute() and ".." in v.parts:
# 185             raise ValueError("Invalid artifact path")
# 186         return v
# 187 
# 188 class ProgramProviderConfigSchema(BaseModel):
# 189     id: Optional[int] = None
# 190     guid: UUID = Field(default_factory=uuid4)
# 191     name: str
# 192     description: Optional[str] = None
# 193     config: Optional[Dict[str, Any]] = None
# 194     artifact_path: Path
# 195     tags: Optional[List[str]] = None
# 196 
# 197     @classmethod
# 198     def validate_artifact_path(cls, v: Path) -> Path:
# 199         if not v.is_absolute() and ".." in v.parts:
# 200             raise ValueError("Invalid artifact path")
# 201         return v
# 202 
# 203 class SessionConfigSchema(BaseModel):
# 204     id: Optional[int] = None
# 205     session_id: str
# 206     name: str
# 207     description: Optional[str] = None
# 208     environment_type: str  #  e.g., "experiment", "live", "staging"
# 209     config: Optional[Dict[str, Any]] = None
# 210     tags: Optional[List[str]] = None
# 211     
# 212 
# 213 #  LOGS
# 214 
# 215 @dataclass
# 216 class ProviderLogSchema:
# 217     session_id: str
# 218     provider_id: int
# 219     provider_type: PROVIDER_TYPE
# 220     input: Optional[str] = None
# 221     output: Optional[str] = None
# 222     output_schema: Optional[str] = None
# 223     latency_ms: Optional[int] = None
# 224     config_hash: Optional[str] = None
# 225     file_name: Optional[str] = None
# 226     timestamp: datetime = field(default_factory=lambda: datetime.now(timezone.utc))
# 227     called_by_type: Optional[PROVIDER_TYPE] = None
# 228     called_by_id: Optional[int] = None
# 229     run_id: str = field(default_factory=lambda: str(uuid4()))
# 230     file_log_id: Optional[str] = None
# 231     parent_id: Optional[str] = None
# 232     execution_chain: List[str] = field(default_factory=list)
# 233 
# 234 @dataclass
# 235 class StateTransitionLogSchema:
# 236     session_id: str
# 237     entity_type: PROVIDER_TYPE  #  e.g., "StateProvider", "SystemProvider", etc.
# 238     entity_id: int
# 239     from_state: str
# 240     to_state: str
# 241     reason: TRANSITION_REASON_TYPE
# 242     decision: DECISION_TYPE = DECISION_TYPE.UNKNOWN
# 243     triggered_by: str | None = None
# 244     step: int | None = None
# 245     transition_metadata: dict | None = None
# 246     timestamp: datetime = field(default_factory=lambda: datetime.now(timezone.utc))
# 247     run_id: str = field(default_factory=lambda: str(uuid4()))
# 248     called_by_type: PROVIDER_TYPE | None = None
# 249     called_by_id: int | None = None 
# 250     file_log_id: Optional[str] = None
# 251     parent_id: Optional[str] = None
# 252     execution_chain: List[str] = field(default_factory=list)
# 253 
# 254 @dataclass
# 255 class AgentConversationLogSchema:
# 256     session_id: str
# 257     system: SYSTEM
# 258     agent_type: AGENT
# 259     agent_provider_config_id: int
# 260     content: str
# 261     timestamp: datetime = field(default_factory=lambda: datetime.now(timezone.utc))
# 262     run_id: str = field(default_factory=lambda: str(uuid4()))
# 263     file_log_id: Optional[str] = None
# 264 
# 265 @dataclass
# 266 class ErrorLogSchema:
# 267     session_id: str
# 268     error_type: ERROR_TYPE
# 269     message: str
# 270     file_path: str | None = None
# 271     provider_id: int | None = None
# 272     provider_type: PROVIDER_TYPE = PROVIDER_TYPE.UNKNOWN
# 273     timestamp: datetime = field(default_factory=lambda: datetime.now(timezone.utc))
# 274     latency_ms: Optional[int] = None
# 275     called_by_type: Optional[PROVIDER_TYPE] = None
# 276     called_by_id: Optional[int] = None
# 277     run_id: str = field(default_factory=lambda: str(uuid4()))
# 278     file_log_id: Optional[str] = None
# 279     parent_id: Optional[str] = None
# 280     execution_chain: List[str] = field(default_factory=list)
# 281     
# 282 @dataclass
# 283 class SnapshotMetricsSchema:
# 284     session_id: str
# 285     snapshot_id: str
# 286     system: SYSTEM
# 287     agent: str
# 288     agent_type: AGENT 
# 289     agent_id: int
# 290     score: float
# 291     state: str  #  could be FSM state, usually freeform like "generate" or "stability"
# 292     decision: DECISION_TYPE
# 293     timestamp: datetime
# 294     file_log_id: str
# 295 
# 296     line_count_before: int
# 297     line_count_after: int
# 298     function_count_before: int
# 299     function_count_after: int
# 300     symbol_count_before: int
# 301     symbol_count_after: int
# 302     branch_count_before: int
# 303     branch_count_after: int
# 304     comment_count_before: int
# 305     comment_count_after: int
# 306 
# 307     line_count_delta: int
# 308     function_count_delta: int
# 309     symbol_count_delta: int
# 310     branch_count_delta: int
# 311     comment_count_delta: int
# 312 
# 313 @dataclass
# 314 class FileLogSchema:
# 315     session_id: str
# 316     file_name: str
# 317     id: Optional[int] = None
# 318     original_path: Optional[str] = None
# 319     length_bytes: Optional[int] = None
# 320 
# 321 
# 322 #  Provider output schemas (for logs)
# 323 
# 324 class ToolOutputSchema(BaseModel):
# 325     return_code: int
# 326     stdout: Optional[str] = None
# 327     stderr: Optional[str] = None
# 328     violations: Optional[List[str]] = None  #  for linters like ruff
# 329     metrics: Optional[Dict[str, Any]] = None  #  for tools like sonarcloud
# 330     summary: Optional[str] = None  #  optional human-readable summary
# 331 
# 332 
# 333 class AgentEngineOutput(BaseModel):
# 334     response: str
# 335     token_count: int
# 336     cost_usd: float
# 337     snapshot_id: Optional[str] = None
# 338     summary: Optional[str] = None
# 339     code: Optional[str] = None 
# 340     agent_decision: Optional[str] = None
# 341     conversation_log_entry: Optional[str] = None
# 342 
# 343 
# 344 class AgentOutputSchema(BaseModel):
# 345     decision: DECISION_TYPE = DECISION_TYPE.UNKNOWN
# 346     score: Optional[float] = None
# 347     response: Optional[str] = None
# 348     log: Optional[str] = None
# 349     file_path: Optional[str] = None
# 350 
# 351 
# 352 class ContextOutputSchema(BaseModel):
# 353     context: Dict[str, Any]
# 354     summary: Optional[str] = None
# 355 
# 356 
# 357 class PromptOutputSchema(BaseModel):
# 358     prompt: str
# 359     summary: Optional[str] = None
# 360 
# 361 
# 362 class FSMOutputSchema(BaseModel):
# 363     state: STATE
# 364     previous_state: Optional[STATE] = None
# 365     state_type: STATE_TYPE = STATE_TYPE.INTERMEDIATE
# 366     decision: Optional[DECISION_TYPE] = DECISION_TYPE.UNKNOWN
# 367     steps: int = Field(..., ge=0)
# 368     max_steps: int = Field(..., gt=0)
# 369     summary: Optional[str] = None
# 370     output: Optional[Dict[str, Any]] = None
# 371     provider_name: Optional[str] = None
# 372 
# 373 class SystemOutputSchema(FSMOutputSchema): pass
# 374 
# 375 class StateOutputSchema(FSMOutputSchema): 
# 376     previous_state: Optional[AGENT] = None
# 377 class ProgramOutputSchema(FSMOutputSchema):
# 378     previous_state: Optional[CONTROLLER] = None
# 379 class ControllerOutputSchema(FSMOutputSchema): 
# 380     previous_state: Optional[SYSTEM] = None
# 381 
# 382 
# 383 #  DTOs
# 384 
# 385 class Snapshot(BaseModel):
# 386     timestamp: datetime
# 387     file: str
# 388     score: float
# 389     decision: DECISION_TYPE
# 390 
# 391 class SystemState(BaseModel):
# 392     system: str
# 393     file_path: str
# 394     working_file: str
# 395     final_file: Optional[str] = None
# 396     snapshots: List[Snapshot]
# 397     state: Optional[str] = "active"
# 398 
# 399 


# db\seed.py

# 1 from sqlalchemy.orm import Session
# 2 from sqlalchemy import create_engine
# 3 from pathlib import Path
# 4 from app.db.seeders.seed_controller_providers import seed_orchestrator_providers
# 5 from app.db.seeders.seed_state_providers import seed_state_providers
# 6 from app.db.seeders.seed_agent_engine_providers import seed_agent_engine_providers
# 7 from app.db.seeders.seed_context_providers import seed_context_providers
# 8 from app.db.seeders.seed_prompt_providers import seed_prompt_providers
# 9 from app.db.seeders.seed_score_providers import seed_score_providers
# 10 from app.db.seeders.seed_tool_providers import seed_tool_providers
# 11 from app.db.seeders.seed_prompts import seed_prompts
# 12 
# 13 #  Ensure your project root matches explicitly
# 14 PROJECT_ROOT = Path(__file__).resolve().parents[2]
# 15 DB_PATH = PROJECT_ROOT / "experiments" / "codecritic.sqlite3"
# 16 EXTENSIONS_DIR = PROJECT_ROOT / "extensions"
# 17 
# 18 def main():
# 19     engine = create_engine(f"sqlite:///{DB_PATH}")
# 20     with Session(bind=engine) as session:
# 21         seed_prompts(session)
# 22         seed_prompt_providers(session)
# 23         seed_tool_providers(session)
# 24         seed_score_providers(session)
# 25         seed_context_providers(session)
# 26         seed_agent_engine_providers(session)
# 27         seed_state_providers(session)
# 28         seed_orchestrator_providers(session)
# 29 
# 30     print(f"Database seeded successfully at {DB_PATH.resolve()}")
# 31 
# 32 if __name__ == "__main__":
# 33     main()


# db\seeders\__init__.py



# db\seeders\files\__init__.py



# db\seeders\files\agent_engine_providers\__init__.py



# db\seeders\files\agent_engine_providers\basic_agent_engine_provider.py

# 1 from app.enums.logging_enums import RunContext
# 2 from app.db.schemas import AgentEngineOutput
# 3 from app.providers.agent_engine_provider_base import AgentEngineProviderBase
# 4 
# 5 class BasicAgentEngineProvider(AgentEngineProviderBase):
# 6     def _run(self, input: dict, context: RunContext | None = None) -> AgentEngineOutput:
# 7         response = f"[Basic Response] You said: {input.get('prompt', '')}"
# 8         token_count = len(response.split())
# 9         cost_usd = token_count * (self._config.cost_per_1k_tokens or 0.0) / 1000
# 10         return AgentEngineOutput(response=response, token_count=token_count, cost_usd=cost_usd)


# db\seeders\files\agent_engine_providers\claude_3_sonnet_agent_engine_provider.py

# 1 import os, json, boto3
# 2 from app.enums.logging_enums import RunContext  #  🔧 Needed for typing
# 3 from app.db.schemas import AgentEngineOutput
# 4 from app.providers.agent_engine_provider_base import AgentEngineProviderBase
# 5 
# 6 class Claude3SonnetAgentEngineProvider(AgentEngineProviderBase):
# 7     def _run(self, input: dict, context: RunContext | None = None) -> AgentEngineOutput:
# 8         prompt = input["prompt"]
# 9         region = os.getenv("AWS_REGION", "us-east-1")
# 10 
# 11         client = boto3.client("bedrock-runtime", region_name=region)
# 12         body = {
# 13             "anthropic_version": "bedrock-2023-05-31",
# 14             "messages": [{"role": "user", "content": prompt}],
# 15             "max_tokens": 1024,
# 16         }
# 17 
# 18         response_raw = client.invoke_model(
# 19             modelId="anthropic.claude-3-sonnet-20240229-v1:0",
# 20             contentType="application/json",
# 21             accept="application/json",
# 22             body=json.dumps(body),
# 23         )
# 24 
# 25         content = json.loads(response_raw["body"].read())["content"][0]["text"].strip()
# 26         token_count = len(content.split())
# 27         cost_usd = token_count * (self._config.cost_per_1k_tokens or 0.0) / 1000
# 28 
# 29         return AgentEngineOutput(response=content, token_count=token_count, cost_usd=cost_usd)


# db\seeders\files\agent_engine_providers\gemini_1_5_pro_agent_engine_provider.py

# 1 import os, requests
# 2 from app.enums.logging_enums import RunContext  #  ✅ Add for typing
# 3 from app.db.schemas import AgentEngineOutput
# 4 from app.providers.agent_engine_provider_base import AgentEngineProviderBase
# 5 
# 6 class Gemini1_5ProAgentEngineProvider(AgentEngineProviderBase):
# 7     def _run(self, input: dict, context: RunContext | None = None) -> AgentEngineOutput:
# 8         prompt = input["prompt"]
# 9         api_key = os.getenv("GOOGLE_API_KEY")
# 10         if not api_key:
# 11             raise EnvironmentError("GOOGLE_API_KEY is not set in the environment.")
# 12 
# 13         url = (
# 14             "https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-pro:generateContent"
# 15             f"?key={api_key}"
# 16         )
# 17         response_raw = requests.post(
# 18             url,
# 19             headers={"Content-Type": "application/json"},
# 20             json={"contents": [{"parts": [{"text": prompt}]}]},
# 21         )
# 22         response_raw.raise_for_status()
# 23         content = response_raw.json()["candidates"][0]["content"]["parts"][0]["text"].strip()
# 24 
# 25         token_count = len(content.split())
# 26         cost_usd = token_count * (self._config.cost_per_1k_tokens or 0.0) / 1000
# 27 
# 28         return AgentEngineOutput(response=content, token_count=token_count, cost_usd=cost_usd)


# db\seeders\files\agent_engine_providers\openai_gpt4o_agent_engine_provider.py

# 1 import json
# 2 import os
# 3 from openai import OpenAI
# 4 from app.enums.logging_enums import RunContext  
# 5 from app.db.schemas import AgentEngineOutput
# 6 from app.providers.agent_engine_provider_base import AgentEngineProviderBase
# 7 
# 8 def _safe_prompt_content(content):
# 9     if isinstance(content, str):
# 10         return content
# 11     if hasattr(content, "model_dump_json"):
# 12         return content.model_dump_json()
# 13     if isinstance(content, dict):
# 14         return json.dumps(content)
# 15     return str(content)
# 16 
# 17 class OpenAIGPT4oAgentEngineProvider(AgentEngineProviderBase):
# 18     def _run(self, input: dict, context: RunContext | None = None) -> AgentEngineOutput:
# 19         prompt = input["prompt"]
# 20         api_key = os.getenv("OPENAI_API_KEY")
# 21         if not api_key:
# 22             raise EnvironmentError("OPENAI_API_KEY is not set in the environment.")
# 23 
# 24         client = OpenAI(api_key=api_key)
# 25         response = client.chat.completions.create(
# 26             model="gpt-4o",
# 27             messages=[
# 28                 {"role": "system", "content": "You are a helpful assistant."},
# 29                 {"role": "user", "content": _safe_prompt_content(prompt)},
# 30             ],
# 31             temperature=0.2,
# 32             max_tokens=2048,
# 33         )
# 34 
# 35         content = response.choices[0].message.content.strip()
# 36         token_count = len(content.split())
# 37         cost_usd = token_count * (self._config.cost_per_1k_tokens or 0.0) / 1000
# 38 
# 39         return AgentEngineOutput(response=content, token_count=token_count, cost_usd=cost_usd)


# db\seeders\files\agent_providers\__init__.py



# db\seeders\files\agent_providers\basic_agent_provider.py

# 1 from pathlib import Path
# 2 from app.enums.logging_enums import RunContext  #  ✅ Add for context typing
# 3 from app.providers.agent_provider_base import AgentProviderBase
# 4 from app.db.schemas import AgentOutputSchema
# 5 
# 6 
# 7 class BasicAgentProvider(AgentProviderBase):
# 8     def _run(self, input: dict, context: RunContext | None = None) -> AgentOutputSchema:
# 9         raw_file_path = input.get("before") or input.get("file_name")
# 10         relative_file_path = None
# 11 
# 12         if raw_file_path:
# 13             try:
# 14                 relative_file_path = str(Path(raw_file_path).resolve().relative_to(Path.cwd()))
# 15             except ValueError:
# 16                 relative_file_path = str(Path(raw_file_path).resolve())
# 17 
# 18         response = (
# 19             "[AGENT_DECISION]accept[/AGENT_DECISION]\n"
# 20             "[CONVERSATION_LOG_ENTRY]Basic agent executed successfully.[/CONVERSATION_LOG_ENTRY]\n"
# 21             f"[CODE]{input.get('code', '#  no code provided')}[/CODE]"
# 22         )
# 23 
# 24         return AgentOutputSchema(
# 25             response=response,
# 26             log="Basic agent executed successfully.",
# 27             decision="accept",
# 28             snapshot_id=None,
# 29             file_path=relative_file_path,
# 30         )


# db\seeders\files\agent_providers\code_stability_agent_provider.py

# 1 from pathlib import Path
# 2 from app.enums.fsm_enums import DECISION_TYPE
# 3 from app.providers.agent_provider_base import AgentProviderBase
# 4 from app.db.schemas import AgentOutputSchema
# 5 from app.enums.logging_enums import RunContext  #  needed to type context
# 6 
# 7 
# 8 class CodeStabilityAgentProvider(AgentProviderBase):
# 9     def _run(self, input: dict, context: RunContext | None = None) -> AgentOutputSchema:
# 10         score_result = self._score_provider.run(input, context=self.fork_context())
# 11 
# 12         raw_file_path = input.get("file_path") or input.get("file_name") or input.get("before")
# 13         relative_file_path = None
# 14         if raw_file_path:
# 15             try:
# 16                 relative_file_path = str(Path(raw_file_path).resolve().relative_to(Path.cwd()))
# 17             except ValueError:
# 18                 relative_file_path = str(Path(raw_file_path).resolve())
# 19 
# 20         if score_result.value >= 1.0:
# 21             return AgentOutputSchema(
# 22                 response="""[AGENT_DECISION]accept[/AGENT_DECISION]
# 23 [CONVERSATION_LOG_ENTRY]Code passed all stability checks.[/CONVERSATION_LOG_ENTRY]""",
# 24                 log="Code passed all stability checks.",
# 25                 decision=DECISION_TYPE.ACCEPTED,
# 26                 snapshot_id=None,
# 27                 file_path=relative_file_path,
# 28                 score=score_result.value
# 29             )
# 30 
# 31         return AgentOutputSchema(
# 32             response=f"""[AGENT_DECISION]reject[/AGENT_DECISION]
# 33 [CONVERSATION_LOG_ENTRY]Failed stability checks: {score_result.components}[/CONVERSATION_LOG_ENTRY]""",
# 34             log=f"Failed stability checks: {score_result.components}",
# 35             decision=DECISION_TYPE.REJECTED,
# 36             snapshot_id=None,
# 37             file_path=relative_file_path,
# 38             score=score_result.value
# 39         )


# db\seeders\files\agent_providers\linting_discriminator_agent_provider.py

# 1 from pathlib import Path
# 2 import uuid
# 3 import shutil
# 4 from datetime import datetime
# 5 
# 6 from app.enums.fsm_enums import DECISION_TYPE
# 7 from app.enums.logging_enums import RunContext
# 8 from app.providers.agent_provider_base import AgentProviderBase
# 9 from app.db.schemas import AgentOutputSchema
# 10 from app.utilities.metadata.snapshots.snapshot_reader import read_latest_snapshot
# 11 from app.utilities.diff_utils import summarize_diff
# 12 
# 13 LINTING_PASS_THRESHOLD = 0.85
# 14 
# 15 class LintingDiscriminatorAgentProvider(AgentProviderBase):
# 16     def _run(self, input: dict, context: RunContext | None = None) -> AgentOutputSchema:
# 17         
# 18         snapshot = read_latest_snapshot(session_id=self._session_id)
# 19 
# 20         if not snapshot:
# 21             return AgentOutputSchema(
# 22                 decision=DECISION_TYPE.REJECTED,
# 23                 log="No snapshot found.",
# 24                 response=(
# 25                     "[AGENT_DECISION]reject[/AGENT_DECISION]\n"
# 26                     "[CONVERSATION_LOG_ENTRY]No snapshot found.[/CONVERSATION_LOG_ENTRY]"
# 27                 ),
# 28                 file_path=None,
# 29                 score=None
# 30             )
# 31 
# 32         before_code = snapshot["before"]
# 33         after_code = snapshot["after"]
# 34         before_path = Path(snapshot["before_path"]).resolve()
# 35         after_path = Path(snapshot["after_path"]).resolve()
# 36 
# 37         before_score = self._score_provider.run(
# 38             {"file_path": str(before_path)},
# 39             context=self.fork_context()
# 40         ).value
# 41 
# 42         after_score = self._score_provider.run(
# 43             {"file_path": str(after_path)},
# 44             context=self.fork_context()
# 45         ).value
# 46 
# 47         def safe_relative(path: Path) -> str:
# 48             try:
# 49                 return str(path.relative_to(Path.cwd()))
# 50             except ValueError:
# 51                 return str(path)
# 52 
# 53         if before_code == after_code:
# 54             decision = DECISION_TYPE.ACCEPTED if before_score >= LINTING_PASS_THRESHOLD else DECISION_TYPE.REJECTED
# 55             log_msg = (
# 56                 "No meaningful change, but score already passing."
# 57                 if decision == DECISION_TYPE.ACCEPTED
# 58                 else "No meaningful change and score below threshold."
# 59             )
# 60             return AgentOutputSchema(
# 61                 decision=decision,
# 62                 log=log_msg,
# 63                 response=(
# 64                     f"[AGENT_DECISION]{decision}[/AGENT_DECISION]\n"
# 65                     f"[CONVERSATION_LOG_ENTRY]{log_msg}[/CONVERSATION_LOG_ENTRY]"
# 66                 ),
# 67                 file_path=safe_relative(before_path),
# 68                 score=before_score
# 69             )
# 70 
# 71         #  Summarize the diff if the code has changed
# 72         summary = summarize_diff(before_code, after_code).strip()
# 73         if not summary:
# 74             decision = DECISION_TYPE.ACCEPTED if before_score >= LINTING_PASS_THRESHOLD else DECISION_TYPE.REJECTED
# 75             log_msg = (
# 76                 "Diff could not be summarized, but prior version passes."
# 77                 if decision == DECISION_TYPE.ACCEPTED
# 78                 else "Diff could not be summarized and prior version fails."
# 79             )
# 80             return AgentOutputSchema(
# 81                 decision=decision,
# 82                 log=log_msg,
# 83                 response=(
# 84                     f"[AGENT_DECISION]{decision}[/AGENT_DECISION]\n"
# 85                     f"[CONVERSATION_LOG_ENTRY]{log_msg}[/CONVERSATION_LOG_ENTRY]"
# 86                 ),
# 87                 file_path=safe_relative(before_path),
# 88                 score=before_score
# 89             )
# 90 
# 91         #  Check the scores for acceptance or improvement
# 92         if after_score >= LINTING_PASS_THRESHOLD and after_score > before_score:
# 93             decision = DECISION_TYPE.ACCEPTED
# 94             chosen_code = after_code
# 95             score = after_score
# 96         elif after_score < LINTING_PASS_THRESHOLD and after_score > before_score:
# 97             decision = DECISION_TYPE.IMPROVED
# 98             chosen_code = after_code
# 99             score = after_score
# 100         else:
# 101             decision = DECISION_TYPE.REJECTED
# 102             chosen_code = before_code
# 103             score = before_score
# 104 
# 105         #  Promote improved file to a working path (if chosen_code came from after_code)
# 106         temp_path = Path("working_files") / f"temp_agent_{datetime.now().strftime('%H%M%S%f')[:10]}.py"
# 107         temp_path.parent.mkdir(parents=True, exist_ok=True)
# 108 
# 109         source_path = after_path if chosen_code == after_code else before_path
# 110         shutil.copyfile(source_path, temp_path)
# 111 
# 112         return AgentOutputSchema(
# 113             decision=decision,
# 114             log=summary,
# 115             response=(
# 116                 f"[AGENT_DECISION]{decision}[/AGENT_DECISION]\n"
# 117                 f"[CONVERSATION_LOG_ENTRY]{summary}[/CONVERSATION_LOG_ENTRY]"
# 118             ),
# 119             file_path=safe_relative(temp_path),
# 120             score=score
# 121         )


# db\seeders\files\agent_providers\linting_generator_agent_provider.py

# 1 from app.enums.fsm_enums import DECISION_TYPE
# 2 from app.providers.agent_provider_base import AgentProviderBase
# 3 from app.db.schemas import AgentOutputSchema
# 4 from app.enums.logging_enums import RunContext 
# 5 
# 6 class LintingGeneratorAgentProvider(AgentProviderBase):
# 7     """Runs a generation round using the linting system prompt, context, and snapshot."""
# 8 
# 9     def _run(self, input: dict, context: RunContext | None = None) -> AgentOutputSchema:
# 10         file_path = input.get("file_path")
# 11         system = input.get("system", "linting")
# 12 
# 13         if not self._prompt_provider:
# 14             raise ValueError("Prompt provider is not set")
# 15         if not self._agent_engine:
# 16             raise ValueError("Agent engine is not set")
# 17 
# 18         #  🔧 Propagate context to prompt provider
# 19         final_prompt = self._prompt_provider.run(input=input, context=context)
# 20 
# 21         #  🔧 Propagate context to agent engine
# 22         engine_output = self._agent_engine.run(
# 23             input={
# 24                 "prompt": final_prompt,
# 25                 "before": file_path,
# 26                 "agent_type": self._config.agent_type,
# 27                 "agent_id": self._config.id,
# 28                 "system": system,
# 29                 "state_context": input.get("state_context", {}),
# 30             },
# 31             context=context
# 32         )
# 33 
# 34         response = engine_output.response
# 35         log = self._extract_log(response) or "Generator agent did not return a log entry."
# 36         code = self._extract_code(response)
# 37 
# 38         decision = DECISION_TYPE.ACCEPTED if code else DECISION_TYPE.UNKNOWN
# 39         if decision == DECISION_TYPE.UNKNOWN:
# 40             self._log.warning("⚠️ Generator did not return a code block; decision set to UNKNOWN.")
# 41 
# 42         return AgentOutputSchema(
# 43             response=response,
# 44             log=log,
# 45             decision=decision,
# 46             snapshot_id=None,
# 47             file_path=file_path,
# 48             score=getattr(engine_output, "score", None)
# 49         )


# db\seeders\files\context_providers\__init__.py



# db\seeders\files\context_providers\basic_context_provider.py

# 1 from app.enums.logging_enums import RunContext  #  ✅ Needed for context typing
# 2 from app.providers.context_provider_base import ContextProviderBase
# 3 from app.db.schemas import ContextOutputSchema
# 4 
# 5 class BasicContextProvider(ContextProviderBase):
# 6     def _run(self, input: dict, context: RunContext | None = None) -> ContextOutputSchema:
# 7         return ContextOutputSchema(
# 8             context={"file_path": input.get("file_path")},
# 9             summary="Basic context with file_path only."
# 10         )


# db\seeders\files\context_providers\linting_context_provider.py

# 1 from __future__ import annotations
# 2 from pathlib import Path
# 3 import json
# 4 
# 5 from sqlalchemy.orm import Session
# 6 from app.enums.logging_enums import RunContext  #  ✅ import context type
# 7 from app.providers.context_provider_base import ContextProviderBase
# 8 from app.utilities.metadata.logging.conversation_log import get_conversation_log
# 9 from app.db.schemas import ContextOutputSchema
# 10 
# 11 
# 12 class LintingContextProvider(ContextProviderBase):
# 13     def _run(self, input: dict, context: RunContext | None = None) -> ContextOutputSchema:
# 14         file_path = Path(input["file_path"]).resolve()
# 15         system = input["system"]
# 16 
# 17         if not file_path.exists():
# 18             raise FileNotFoundError(f"❌ File not found: {file_path}")
# 19 
# 20         source_code = file_path.read_text(encoding="utf-8")
# 21 
# 22         #  🔧 propagate context
# 23         score_result = self.score_provider.run(
# 24             input={"file_path": str(file_path)},
# 25             context=context
# 26         )
# 27 
# 28         with Session(bind=self._engine) as session:
# 29             convo_log = get_conversation_log(session, session_id=self._session_id, system=system)
# 30 
# 31         context_data = {
# 32             "file_path": str(file_path),
# 33             "source_code": source_code,
# 34             "score": score_result.model_dump(),
# 35             "conversation_log": convo_log
# 36         }
# 37 
# 38         return ContextOutputSchema(
# 39             context=context_data,
# 40             summary=f"Context for {file_path.name}, {score_result.value} score, {len(convo_log)} log entries"
# 41         )


# db\seeders\files\controller_providers\__init__.py



# db\seeders\files\controller_providers\preprocessing_controller_provider.py

# 1 from app.enums.fsm_enums import DECISION_TYPE, TRANSITION_REASON_TYPE
# 2 from app.enums.system_enums import SYSTEM
# 3 from app.providers.controller_provider_base import ControllerProviderBase
# 4 
# 5 class PreprocessingControllerProvider(ControllerProviderBase):
# 6     def _transition(self, state, sys_output):
# 7         current = SYSTEM(state["state"])
# 8         transition = {}
# 9 
# 10         #  Transition from SYSTEM.START to SYSTEM.LINTING
# 11         if current == SYSTEM.START:
# 12             transition = {
# 13                 "state": SYSTEM.LINTING,
# 14                 "reason": TRANSITION_REASON_TYPE.INITIALIZATION,
# 15             }
# 16 
# 17         #  Transition from SYSTEM.LINTING to SYSTEM.END based on system output decision
# 18         elif current == SYSTEM.LINTING:
# 19             #  Check sys_output directly for decision
# 20             if sys_output:
# 21                 #  Directly check for 'decision' attribute in sys_output
# 22                 decision = sys_output.decision if hasattr(sys_output, 'decision') else DECISION_TYPE.UNKNOWN
# 23 
# 24                 #  Determine the transition reason based on the decision
# 25                 if decision == DECISION_TYPE.ACCEPTED:
# 26                     transition = {
# 27                         "state": SYSTEM.END,
# 28                         "reason": TRANSITION_REASON_TYPE.SUCCESSFUL,
# 29                     }
# 30                 elif decision == DECISION_TYPE.REJECTED:
# 31                     transition = {
# 32                         "state": SYSTEM.END,
# 33                         "reason": TRANSITION_REASON_TYPE.UNSUCCESSFUL,
# 34                     }
# 35                 else:
# 36                     transition = {
# 37                         "state": SYSTEM.END,
# 38                         "reason": TRANSITION_REASON_TYPE.CUSTOM_RULE,
# 39                     }
# 40 
# 41                 #  Propagate the decision and file path to the next state
# 42                 transition["file_path"] = sys_output.file_path if hasattr(sys_output, 'file_path') else state.get("file_path")
# 43                 transition["decision"] = decision
# 44             else:
# 45                 #  Default case if no valid output is found
# 46                 transition = {
# 47                     "state": SYSTEM.END,
# 48                     "reason": TRANSITION_REASON_TYPE.CUSTOM_RULE,
# 49                 }
# 50 
# 51         else:
# 52             #  Handle any other states that don't match SYSTEM.START or SYSTEM.LINTING
# 53             transition = {
# 54                 "state": SYSTEM.END,
# 55                 "reason": TRANSITION_REASON_TYPE.CUSTOM_RULE,
# 56             }
# 57 
# 58         return transition


# db\seeders\files\program_providers\__init__.py



# db\seeders\files\program_providers\codecritic_program_provider.py

# 1 from app.enums.controller_enums import CONTROLLER
# 2 from app.enums.fsm_enums import DECISION_TYPE, TRANSITION_REASON_TYPE
# 3 from app.providers.program_provider_base import ProgramProviderBase
# 4 
# 5 class CodeCriticProgramProvider(ProgramProviderBase):
# 6     def _transition(self, state, ctrl_output):
# 7         current = CONTROLLER(state["state"])
# 8         transition = {}
# 9 
# 10         if current == CONTROLLER.START:
# 11             return {
# 12                 "state": CONTROLLER.PREPROCESSING,
# 13                 "reason": TRANSITION_REASON_TYPE.INITIALIZATION,
# 14             }
# 15 
# 16         elif current == CONTROLLER.PREPROCESSING:
# 17             #  Safely extract decision
# 18             decision = getattr(ctrl_output, "decision", DECISION_TYPE.UNKNOWN)
# 19 
# 20             #  Route based on decision
# 21             if decision == DECISION_TYPE.ACCEPTED:
# 22                 transition = {
# 23                     "state": CONTROLLER.END,
# 24                     "reason": TRANSITION_REASON_TYPE.SUCCESSFUL,
# 25                 }
# 26             elif decision == DECISION_TYPE.REJECTED:
# 27                 transition = {
# 28                     "state": CONTROLLER.END,
# 29                     "reason": TRANSITION_REASON_TYPE.UNSUCCESSFUL,
# 30                 }
# 31             else:
# 32                 transition = {
# 33                     "state": CONTROLLER.END,
# 34                     "reason": TRANSITION_REASON_TYPE.CUSTOM_RULE,
# 35                 }
# 36 
# 37             transition["file_path"] = getattr(ctrl_output, "file_path", state.get("file_path"))
# 38             transition["decision"] = decision
# 39             return transition
# 40 
# 41         else:
# 42             return {
# 43                 "state": CONTROLLER.END,
# 44                 "reason": TRANSITION_REASON_TYPE.CUSTOM_RULE,
# 45             }
# 46     


# db\seeders\files\prompt_providers\__init__.py



# db\seeders\files\prompt_providers\basic_prompt_provider.py

# 1 from app.enums.logging_enums import RunContext  #  ✅ for type annotation
# 2 from app.providers.prompt_provider_base import PromptProviderBase
# 3 from app.db.schemas import PromptOutputSchema
# 4 
# 5 class BasicPromptProvider(PromptProviderBase):
# 6     def _run(self, input: dict, context: RunContext | None = None) -> PromptOutputSchema:
# 7         agent_config = input.get("agent_config", {})
# 8         system_config = input.get("system_config", {})
# 9 
# 10         prompt = (
# 11             f"User: {agent_config.get('input', 'No input provided')}\n"
# 12             f"System: Response mode: {system_config.get('setting', 'default')}"
# 13         )
# 14 
# 15         return PromptOutputSchema(
# 16             prompt=prompt,
# 17             summary="Basic prompt using agent and system config"
# 18         )


# db\seeders\files\prompt_providers\linting_prompt_provider.py

# 1 from __future__ import annotations
# 2 import json
# 3 from pathlib import Path
# 4 
# 5 from app.enums.logging_enums import RunContext  #  ✅ for context type
# 6 from app.providers.prompt_provider_base import PromptProviderBase
# 7 from app.db.schemas import PromptOutputSchema
# 8 
# 9 
# 10 class LintingPromptProvider(PromptProviderBase):
# 11     def _run(self, input: dict, context: RunContext | None = None) -> PromptOutputSchema:
# 12         session_id = self._session_id
# 13         system = input.get("system", "unknown")
# 14         file_path = input.get("file_path")
# 15 
# 16         if not self.agent_text or not self.system_text or not self._context_provider:
# 17             raise ValueError("Missing required prompt text or context provider")
# 18 
# 19         #  ✅ propagate context to context provider
# 20         context_output = self._context_provider.run(
# 21             {
# 22                 "file_path": file_path,
# 23                 "system": system
# 24             },
# 25             context=context
# 26         )
# 27 
# 28         context = (
# 29             context_output.context
# 30             if hasattr(context_output, "context")
# 31             else json.loads(context_output)
# 32         )
# 33 
# 34         convo_section = "\n".join(context.get("conversation_log", [])) or "(none)"
# 35 
# 36         full_prompt = f"""
# 37 {self.system_text}
# 38 
# 39 ---
# 40 
# 41 {self.agent_text}
# 42 
# 43 ---
# 44 
# 45 [CONTEXT]
# 46 {json.dumps(context, indent=2)}
# 47 
# 48 ---
# 49 
# 50 [CONVERSATION LOG]
# 51 {convo_section}
# 52 """.strip()
# 53 
# 54         return PromptOutputSchema(prompt=full_prompt)


# db\seeders\files\prompts\__init__.py



# db\seeders\files\score_providers\__init__.py



# db\seeders\files\score_providers\code_stability_score_provider.py

# 1 import ast
# 2 import json
# 3 import py_compile
# 4 import importlib.util
# 5 from pathlib import Path
# 6 from typing import Dict
# 7 import contextlib
# 8 import sys
# 9 import io
# 10 import uuid
# 11 
# 12 from app.enums.logging_enums import RunContext
# 13 from app.providers.score_provider_base import ScoreProviderBase
# 14 from app.db.schemas import ScoreOutputSchema
# 15 from app.enums.scoring_enums import SCORING_METRIC_TYPE
# 16 from app.utilities.metadata.footer.code_annnotation_utils import split_code_and_notes
# 17 
# 18 
# 19 @contextlib.contextmanager
# 20 def suppress_output():
# 21     original_stdout = sys.stdout
# 22     original_stderr = sys.stderr
# 23     sys.stdout = io.StringIO()
# 24     sys.stderr = io.StringIO()
# 25     try:
# 26         yield
# 27     finally:
# 28         sys.stdout = original_stdout
# 29         sys.stderr = original_stderr
# 30 
# 31 
# 32 class CodeStabilityScoreProvider(ScoreProviderBase):
# 33     def _run(self, input: dict, context: RunContext | None = None) -> ScoreOutputSchema:
# 34         original_path = Path(input["file_path"]).resolve()
# 35         components: Dict[str, bool] = {}
# 36 
# 37         #  Read raw code and verify UTF-8
# 38         try:
# 39             raw_code = original_path.read_text(encoding="utf-8")
# 40             components["utf8_valid"] = True
# 41         except Exception:
# 42             components["utf8_valid"] = False
# 43             return self._final_score(components)
# 44 
# 45         #  Strip footer annotations without modifying actual code structure
# 46         stripped_code, _ = split_code_and_notes(raw_code)
# 47 
# 48         #  Always write a clean temp file with newline termination
# 49         safe_path = Path("working_files") / f"{uuid.uuid4().hex}_stripped.py"
# 50         safe_path.write_text(stripped_code.rstrip() + "\n", encoding="utf-8")
# 51 
# 52         #  Reload stripped_code from disk to ensure parity with lint input
# 53         stripped_code = safe_path.read_text(encoding="utf-8")
# 54 
# 55         with suppress_output():
# 56             #  Syntax and compile check
# 57             try:
# 58                 compile(stripped_code, str(safe_path), "exec")
# 59                 ast.parse(stripped_code)
# 60                 components["syntax_ok"] = True
# 61             except (SyntaxError, IndentationError):
# 62                 components["syntax_ok"] = False
# 63                 return self._final_score(components)
# 64 
# 65             #  py_compile
# 66             try:
# 67                 py_compile.compile(str(safe_path), doraise=True)
# 68                 components["py_compile_ok"] = True
# 69             except Exception:
# 70                 components["py_compile_ok"] = False
# 71                 return self._final_score(components)
# 72 
# 73             #  Import stripped code
# 74             try:
# 75                 spec = importlib.util.spec_from_file_location("mod", str(safe_path))
# 76                 module = importlib.util.module_from_spec(spec)
# 77                 spec.loader.exec_module(module)  #  type: ignore
# 78                 components["can_import"] = True
# 79             except Exception:
# 80                 components["can_import"] = False
# 81                 return self._final_score(components)
# 82 
# 83         #  Special check for Ruff parse error
# 84         ruff_parse_error = "Failed to parse" in stripped_code
# 85         components["ruff_parse_error"] = not ruff_parse_error
# 86         if ruff_parse_error:
# 87             return self._final_score(components)
# 88 
# 89         return self._final_score(components)
# 90 
# 91     def _final_score(self, components: Dict[str, bool]) -> ScoreOutputSchema:
# 92         critical_keys = ["utf8_valid", "syntax_ok", "py_compile_ok", "can_import", "ruff_parse_error"]
# 93         passed = all(components.get(k, False) for k in critical_keys)
# 94         value = 1.0 if passed else 0.0
# 95         summary = "✅ Executable" if passed else "❌ Not executable"
# 96 
# 97         return ScoreOutputSchema(
# 98             name=SCORING_METRIC_TYPE.CODE_STABILITY_SCORE,
# 99             value=value,
# 100             components={k: float(v) for k, v in components.items()},
# 101             summary=summary
# 102         )


# db\seeders\files\score_providers\linting_score_provider.py

# 1 from __future__ import annotations
# 2 
# 3 from copy import deepcopy
# 4 import uuid
# 5 from pathlib import Path
# 6 from typing import Dict, List, Tuple, Any
# 7 
# 8 from app.enums.logging_enums import RunContext
# 9 from app.providers.score_provider_base import ScoreProviderBase
# 10 from app.db.schemas import ScoreOutputSchema
# 11 from app.enums.scoring_enums import SCORING_METRIC_TYPE
# 12 from app.utilities.metadata.footer.code_annnotation_utils import split_code_and_notes
# 13 
# 14 
# 15 class LintingScoreProvider(ScoreProviderBase):
# 16     _DEFAULT_WEIGHTS: Dict[str, float] = {"ruff": 0.7, "black": 0.2, "mypy": 0.1}
# 17     _DEFAULT_THRESHOLD: float | None = None
# 18     _MAX_RUFF_VIOLATIONS_CONSIDERED = 10
# 19 
# 20     def _run(self, input: dict, context: RunContext | None = None) -> ScoreOutputSchema:
# 21         #  context = self.fork_context(context)
# 22         
# 23         file_path = input["file_path"]
# 24         file_path = self._make_path_from_raw(file_path)
# 25         full_code = file_path.read_text(encoding="utf-8")
# 26         clean_code, _ = split_code_and_notes(full_code)
# 27 
# 28         stripped_path = self._write_working_copy(clean_code)
# 29 
# 30         available = {tp._config.name.lower(): tp for tp in self.tool_providers}
# 31         ruff_score, ruff_violations, tool_failures = self._run_ruff(available, stripped_path, context)
# 32         black_score, _ = self._run_tool("black", available, stripped_path, tool_failures, context)
# 33         mypy_score, _ = self._run_tool("mypy", available, stripped_path, tool_failures, context)
# 34 
# 35         weights = self._parse_weights(input.get("weights"))
# 36         weighted_score = round(
# 37             ruff_score * weights["ruff"]
# 38             + black_score * weights["black"]
# 39             + mypy_score * weights["mypy"],
# 40             3,
# 41         )
# 42 
# 43         threshold = input.get("threshold", self._DEFAULT_THRESHOLD)
# 44         meets_threshold = (threshold is None) or (weighted_score >= threshold)
# 45 
# 46         components: Dict[str, float] = {
# 47             "ruff_score": ruff_score,
# 48             "ruff_violations": float(len(ruff_violations)),
# 49             "black_score": black_score,
# 50             "mypy_score": mypy_score,
# 51             "tool_failure_count": float(len(tool_failures)),
# 52             "weight_ruff": weights["ruff"],
# 53             "weight_black": weights["black"],
# 54             "weight_mypy": weights["mypy"],
# 55         }
# 56         if threshold is not None:
# 57             components["threshold"] = threshold
# 58             components["meets_threshold"] = 1.0 if meets_threshold else 0.0
# 59 
# 60         summary = self._build_summary(
# 61             ruff_violations=ruff_violations,
# 62             tool_failures=tool_failures,
# 63             meets_threshold=meets_threshold,
# 64             threshold=threshold,
# 65         )
# 66 
# 67         return ScoreOutputSchema(
# 68             name=SCORING_METRIC_TYPE.LINTING_SCORE,
# 69             value=weighted_score,
# 70             components=components,
# 71             summary=summary,
# 72         )
# 73 
# 74     def _make_path_from_raw(self, maybe_code: str) -> Path:
# 75         if Path(maybe_code).exists() and "\n" not in maybe_code:
# 76             return Path(maybe_code).resolve()
# 77 
# 78         tmp = Path("experiments/snapshots") / f"{uuid.uuid4().hex}.py"
# 79         tmp.parent.mkdir(parents=True, exist_ok=True)
# 80         tmp.write_text(maybe_code, encoding="utf-8")
# 81         print(f"\U0001f4c4  Created temp file for raw code: {tmp}")
# 82         return tmp.resolve()
# 83 
# 84     def _write_working_copy(self, clean_code: str) -> Path:
# 85         stripped = Path("working_files") / f"{uuid.uuid4().hex}_stripped.py"
# 86         stripped.parent.mkdir(parents=True, exist_ok=True)
# 87         stripped.write_text(clean_code.rstrip() + "\n", encoding="utf-8")
# 88         return stripped.resolve()
# 89 
# 90     def _run_ruff(
# 91         self,
# 92         available: Dict[str, Any],
# 93         target_path: Path,
# 94         context: RunContext | None,
# 95     ) -> Tuple[float, List[str], Dict[str, str]]:
# 96         tool_failures: Dict[str, str] = {}
# 97         score, violations = self._run_tool_with_context(
# 98             tool=available["ruff"],
# 99             input={"target": str(target_path), "check": True},
# 100             context=context,
# 101             collect_violations=True,
# 102         )
# 103         score = 1.0 - min(1.0, len(violations) / self._MAX_RUFF_VIOLATIONS_CONSIDERED)
# 104         return score, violations, tool_failures
# 105 
# 106     def _run_tool(
# 107         self,
# 108         name: str,
# 109         available: Dict[str, Any],
# 110         target_path: Path,
# 111         failures: Dict[str, str],
# 112         context: RunContext | None,
# 113         collect_violations: bool = False,
# 114     ) -> Tuple[float, List[str]]:
# 115         if name not in available:
# 116             failures[name] = "not_available"
# 117             return 0.0, []
# 118 
# 119         try:
# 120             score, violations = self._run_tool_with_context(
# 121                 tool=available[name],
# 122                 input={"target": str(target_path), "check": True},
# 123                 context=context,
# 124                 collect_violations=collect_violations,
# 125             )
# 126         except Exception as exc:
# 127             failures[name] = f"crashed: {exc}"
# 128             return 0.0, []
# 129 
# 130         if score == 0.0 and not violations:
# 131             failures[name] = "failed or no output"
# 132             return (0.0, []) if not collect_violations else (0.0, violations)
# 133 
# 134         return (score, violations) if collect_violations else (score, [])
# 135 
# 136     def _run_tool_with_context(
# 137         self,
# 138         tool,
# 139         input: dict,
# 140         context: RunContext | None = None,
# 141         collect_violations: bool = False
# 142     ) -> Tuple[float, List[str]]:
# 143         ctx = self.fork_context()
# 144 
# 145         result = tool.run(input=input, context=ctx)
# 146 
# 147         violations = getattr(result, "violations", []) or []
# 148         rc = getattr(result, "return_code", 0)
# 149 
# 150         if rc not in {0, 1} and not violations:
# 151             return 0.0, violations
# 152 
# 153         count = len(violations)
# 154         score = 1.0 - min(1.0, 0.1 * count)
# 155         return (score, violations) if collect_violations else (score, [])
# 156 
# 157     def _parse_weights(self, custom: Dict[str, float] | None) -> Dict[str, float]:
# 158         weights = {**self._DEFAULT_WEIGHTS}
# 159         if custom:
# 160             weights.update(
# 161                 {k.lower(): float(v) for k, v in custom.items() if k.lower() in weights}
# 162             )
# 163         total = sum(weights.values()) or 1.0
# 164         return {k: v / total for k, v in weights.items()}
# 165 
# 166     def _build_summary(
# 167         self,
# 168         *,
# 169         ruff_violations: List[str],
# 170         tool_failures: Dict[str, str],
# 171         meets_threshold: bool,
# 172         threshold: float | None,
# 173     ) -> str:
# 174         lines: List[str] = []
# 175 
# 176         if ruff_violations:
# 177             top_codes = {}
# 178             for code in ruff_violations:
# 179                 top_codes[code] = top_codes.get(code, 0) + 1
# 180             top_sorted = sorted(top_codes.items(), key=lambda kv: kv[1], reverse=True)
# 181             lines.append(
# 182                 "Top Ruff violations: "
# 183                 + ", ".join(f"{c}×{n}" for c, n in top_sorted[:3])
# 184             )
# 185         else:
# 186             lines.append("No Ruff violations detected")
# 187 
# 188         if tool_failures:
# 189             lines.append("⚠️  Tool failures: " + ", ".join(tool_failures))
# 190 
# 191         if threshold is not None:
# 192             state = "✅ meets" if meets_threshold else "❌ below"
# 193             lines.append(f"{state} threshold {threshold:.2f}")
# 194 
# 195         return "\n".join(lines)


# db\seeders\files\state_providers\__init__.py



# db\seeders\files\state_providers\basic_state_provider.py

# 1 from app.factories.base_provider_factory import BaseProviderFactory
# 2 from app.db.models import StateProviderConfig
# 3 from app.providers.state_provider_base import StateProviderBase
# 4 
# 5 class StateProviderFactory(BaseProviderFactory):
# 6     config_model = StateProviderConfig
# 7     base_class = StateProviderBase


# db\seeders\files\state_providers\code_stability_state_provider.py

# 1 from app.enums.fsm_enums import STATE_TYPE, DECISION_TYPE, TRANSITION_REASON_TYPE
# 2 from app.enums.agent_enums import AGENT
# 3 from app.providers.state_provider_base import StateProviderBase
# 4 from app.db.schemas import AgentOutputSchema
# 5 
# 6 class CodeStabilityStateProvider(StateProviderBase):
# 7     def _transition(self, state: dict, agent_output: AgentOutputSchema | None) -> dict:
# 8         current = AGENT(state.get("state"))
# 9 
# 10         #  If current state is START, transition to STABILITY state
# 11         if current == AGENT.START:
# 12             return {
# 13                 "state": AGENT.STABILITY,
# 14                 "reason": TRANSITION_REASON_TYPE.EVALUATING
# 15             }
# 16 
# 17         #  Get the decision from the agent's output, defaulting to UNKNOWN
# 18         decision = getattr(agent_output, "decision", DECISION_TYPE.UNKNOWN)
# 19 
# 20         #  Determine the next state and transition reason based on the decision
# 21         transition_reason = (
# 22             TRANSITION_REASON_TYPE.SUCCESSFUL if decision == DECISION_TYPE.ACCEPTED
# 23             else TRANSITION_REASON_TYPE.UNSUCCESSFUL
# 24         )
# 25 
# 26         #  Return the transition details
# 27         return {
# 28             "state": AGENT.END,
# 29             "state_type": STATE_TYPE.END,
# 30             "decision": decision,
# 31             "reason": transition_reason
# 32         }


# db\seeders\files\state_providers\linting_discriminator_state_provider.py

# 1 from app.enums.fsm_enums import DECISION_TYPE, TRANSITION_REASON_TYPE
# 2 from app.enums.agent_enums import AGENT
# 3 from app.providers.state_provider_base import StateProviderBase
# 4 from app.db.schemas import AgentOutputSchema
# 5 
# 6 class LintingDiscriminatorStateProvider(StateProviderBase):
# 7     def _transition(self, state: dict, agent_output: AgentOutputSchema | None) -> dict:
# 8         if agent_output is None:
# 9             return {
# 10                 "state": AGENT.DISCRIMINATOR,
# 11                 "reason": TRANSITION_REASON_TYPE.INITIALIZATION
# 12             }
# 13 
# 14         #  Handle the different decision types returned by the agent
# 15         if agent_output.decision == DECISION_TYPE.ACCEPTED:
# 16             return {
# 17                 "state": AGENT.END,
# 18                 "reason": TRANSITION_REASON_TYPE.SUCCESSFUL,
# 19                 "decision": DECISION_TYPE.ACCEPTED,
# 20                 "file_path": agent_output.file_path,
# 21             }
# 22 
# 23         elif agent_output.decision == DECISION_TYPE.IMPROVED:
# 24             return {
# 25                 "state": AGENT.END,
# 26                 "reason": TRANSITION_REASON_TYPE.SUCCESSFUL,  #  Could be improved, still valid
# 27                 "decision": DECISION_TYPE.IMPROVED,
# 28                 "file_path": agent_output.file_path,
# 29             }
# 30 
# 31         #  If the decision is REJECTED, handle accordingly
# 32         return {
# 33             "state": AGENT.END,
# 34             "reason": TRANSITION_REASON_TYPE.UNSUCCESSFUL,
# 35             "decision": DECISION_TYPE.REJECTED,
# 36             "file_path": agent_output.file_path,
# 37         }


# db\seeders\files\state_providers\linting_generator_state_provider.py

# 1 from app.enums.fsm_enums import STATE_TYPE, DECISION_TYPE, TRANSITION_REASON_TYPE
# 2 from app.enums.agent_enums import AGENT
# 3 from app.providers.state_provider_base import StateProviderBase
# 4 from app.db.schemas import AgentOutputSchema
# 5 
# 6 class LintingGeneratorStateProvider(StateProviderBase):
# 7     def _transition(self, state: dict, agent_output: AgentOutputSchema | None) -> dict:
# 8         current = AGENT(state.get("state"))
# 9 
# 10         if current == AGENT.START:
# 11             return {
# 12                 "state": AGENT.GENERATOR,
# 13                 "reason": TRANSITION_REASON_TYPE.INITIALIZATION
# 14             }
# 15 
# 16         if current == AGENT.GENERATOR:
# 17             return {
# 18                 "state": AGENT.END,
# 19                 "state_type": STATE_TYPE.END,
# 20                 "decision": DECISION_TYPE.UNKNOWN,
# 21                 "reason": TRANSITION_REASON_TYPE.SUCCESSFUL
# 22             }
# 23 
# 24         return {
# 25             "state": AGENT.END,
# 26             "state_type": STATE_TYPE.END,
# 27             "decision": DECISION_TYPE.UNKNOWN,
# 28             "reason": TRANSITION_REASON_TYPE.CUSTOM_RULE
# 29         }


# db\seeders\files\system_providers\__init__.py



# db\seeders\files\system_providers\linting_system_provider.py

# 1 from pathlib import Path
# 2 import json
# 3 
# 4 from app.enums.fsm_enums import TRANSITION_REASON_TYPE, DECISION_TYPE
# 5 from app.enums.state_enums import STATE
# 6 from app.providers.system_provider_base import SystemProviderBase
# 7 
# 8 
# 9 class LintingSystemProvider(SystemProviderBase):
# 10     def _transition(self, state: dict, state_output: dict | None) -> dict:
# 11         current = STATE(state.get("state"))
# 12         last = STATE(state.get("_last_state")) if state.get("_last_state") else None
# 13 
# 14         result = DECISION_TYPE.UNKNOWN
# 15         score = 0.0
# 16 
# 17         if state_output and hasattr(state_output, "output"):
# 18             output_obj = state_output.output
# 19             if isinstance(output_obj, str):
# 20                 try:
# 21                     output_obj = json.loads(output_obj)
# 22                 except Exception:
# 23                     output_obj = {}
# 24 
# 25             if isinstance(output_obj, dict):
# 26                 result = output_obj.get("decision") or output_obj.get("result", DECISION_TYPE.UNKNOWN)
# 27                 score = output_obj.get("score", 0.0)
# 28             else:
# 29                 result = getattr(output_obj, "decision", DECISION_TYPE.UNKNOWN)
# 30                 score = getattr(output_obj, "score", 0.0)
# 31 
# 32         transition = {}
# 33 
# 34         #  START → GENERATE
# 35         if current == STATE.START:
# 36             transition = {
# 37                 "state": STATE.GENERATE,
# 38                 "reason": TRANSITION_REASON_TYPE.INITIALIZATION,
# 39                 "file_path": state.get("file_path")
# 40             }
# 41 
# 42 
# 43         #  DISCRIMINATE (first pass) → CODE_STABILITY or GENERATE
# 44         elif current == STATE.DISCRIMINATE and last == STATE.START:
# 45             if score == 1.0:
# 46                 transition = {
# 47                     "state": STATE.CODE_STABILITY,
# 48                     "reason": TRANSITION_REASON_TYPE.EVALUATING,
# 49                     "file_path": state.get("file_path")
# 50                 }
# 51             else:
# 52                 transition = {
# 53                     "state": STATE.GENERATE,
# 54                     "reason": TRANSITION_REASON_TYPE.GENERATING,
# 55                     "file_path": state.get("file_path")
# 56                 }
# 57 
# 58         #  DISCRIMINATE (loop) → CODE_STABILITY or GENERATE
# 59         elif current == STATE.DISCRIMINATE:
# 60             if result == DECISION_TYPE.ACCEPTED:
# 61                 transition = {
# 62                     "state": STATE.CODE_STABILITY,
# 63                     "reason": TRANSITION_REASON_TYPE.EVALUATING,
# 64                     "file_path": state.get("file_path")
# 65                 }
# 66             elif result == DECISION_TYPE.IMPROVED:
# 67                 transition = {
# 68                     "state": STATE.GENERATE,
# 69                     "reason": TRANSITION_REASON_TYPE.GENERATING,
# 70                     "file_path": state.get("file_path")
# 71                 }
# 72             else:
# 73                 transition = {
# 74                     "state": STATE.GENERATE,
# 75                     "reason": TRANSITION_REASON_TYPE.UNSUCCESSFUL,
# 76                     "file_path": state.get("file_path")
# 77                 }
# 78 
# 79         #  GENERATE → DISCRIMINATE
# 80         elif current == STATE.GENERATE:
# 81             transition = {
# 82                 "state": STATE.DISCRIMINATE,
# 83                 "reason": TRANSITION_REASON_TYPE.EVALUATING,
# 84                 "file_path": state.get("file_path")
# 85             }
# 86 
# 87         #  CODE_STABILITY → END or fallback to GENERATE if rejected
# 88         elif current == STATE.CODE_STABILITY:
# 89             if result == DECISION_TYPE.ACCEPTED:
# 90                 transition = {
# 91                     "state": STATE.END,
# 92                     "reason": TRANSITION_REASON_TYPE.SUCCESSFUL,
# 93                     "file_path": state.get("file_path")
# 94                 }
# 95             else:
# 96                 transition = {
# 97                     "state": STATE.GENERATE,
# 98                     "reason": TRANSITION_REASON_TYPE.UNSUCCESSFUL,
# 99                     "file_path": state.get("file_path")
# 100                 }
# 101 
# 102         if not transition:
# 103             transition = {
# 104                 "state": STATE.END,
# 105                 "reason": TRANSITION_REASON_TYPE.CUSTOM_RULE,
# 106                 "decision": result if isinstance(result, DECISION_TYPE) else DECISION_TYPE.UNKNOWN
# 107             }
# 108 
# 109         return transition


# db\seeders\files\tool_providers\__init__.py



# db\seeders\files\tool_providers\black_tool.py

# 1 from __future__ import annotations
# 2 import subprocess
# 3 import sys
# 4 import re
# 5 from typing import Any, Dict, List, Optional
# 6 from app.providers.tool_provider_base import ToolProviderBase
# 7 from app.db.schemas import ToolOutputSchema
# 8 from app.enums.logging_enums import RunContext
# 9 
# 10 
# 11 class BlackToolProviderV2(ToolProviderBase):
# 12     """
# 13     Runs `black` either in --check (diff‑only) mode or full formatting mode and
# 14     maps Black’s native exit‑codes onto the 1 / 0 contract.
# 15     """
# 16 
# 17     def _run(self, input: dict, context: RunContext | None = None) -> ToolOutputSchema:
# 18         target: str = input.get("target")
# 19         check: bool = bool(input.get("check", False))
# 20 
# 21         #  Optionally track context lineage
# 22         if context:
# 23             context.parent_id = self._run_id
# 24             context.execution_chain = context.execution_chain[:] + [self._run_id]
# 25 
# 26         cmd = [
# 27             sys.executable,
# 28             "-m",
# 29             "black",
# 30             "--diff" if check else "--quiet",
# 31             "--check" if check else "",
# 32             target,
# 33         ]
# 34         cmd = [c for c in cmd if c]  #  strip empties
# 35 
# 36         try:
# 37             proc = subprocess.run(
# 38                 cmd, capture_output=True, text=True, encoding="utf‑8", errors="ignore"
# 39             )
# 40         except Exception as exc:
# 41             return self._runtime_error(str(exc))
# 42 
# 43         raw_code = proc.returncode
# 44         stdout = (proc.stdout or "").strip()
# 45         stderr = (proc.stderr or "").strip()
# 46 
# 47         if check:
# 48             if raw_code == 0:
# 49                 return self._success(
# 50                     summary="✅ Black check passed (no reformatting needed)",
# 51                     metrics={
# 52                         "files_checked": 1,
# 53                         "files_reformatted": 0,
# 54                         "lines_changed": 0,
# 55                         "raw_return_code": raw_code,
# 56                     },
# 57                 )
# 58 
# 59             if raw_code == 1:
# 60                 diff_hunks = len(re.findall(r"^@@", stdout, re.MULTILINE))
# 61                 return self._failure(
# 62                     stdout=stdout,
# 63                     summary=(
# 64                         f"❌ Black would reformat the file "
# 65                         f"({diff_hunks} changed hunk{'s' if diff_hunks != 1 else ''})"
# 66                     ),
# 67                     violations=[f"WOULD_REFORMAT:{target}"],
# 68                     metrics={
# 69                         "files_checked": 1,
# 70                         "files_reformatted": 1,
# 71                         "lines_changed": diff_hunks,
# 72                         "raw_return_code": raw_code,
# 73                     },
# 74                 )
# 75 
# 76             return self._runtime_error(stderr or stdout, raw_code)
# 77 
# 78         if raw_code == 0:
# 79             return self._success(
# 80                 summary="✅ Black formatted the file successfully",
# 81                 metrics={
# 82                     "files_reformatted": 1,
# 83                     "raw_return_code": raw_code,
# 84                 },
# 85             )
# 86 
# 87         return self._runtime_error(stderr or stdout, raw_code)
# 88 
# 89     #  ────────────────────────────────────────────────────────────── helpers
# 90     def _success(
# 91         self,
# 92         summary: str,
# 93         stdout: Optional[str] = None,
# 94         metrics: Optional[Dict[str, Any]] = None,
# 95     ) -> ToolOutputSchema:
# 96         return ToolOutputSchema(
# 97             return_code=1,
# 98             stdout=stdout,
# 99             stderr=None,
# 100             violations=None,
# 101             metrics=metrics,
# 102             summary=summary,
# 103         )
# 104 
# 105     def _failure(
# 106         self,
# 107         stdout: Optional[str],
# 108         summary: str,
# 109         violations: List[str],
# 110         metrics: Dict[str, Any],
# 111     ) -> ToolOutputSchema:
# 112         return ToolOutputSchema(
# 113             return_code=0,
# 114             stdout=stdout,
# 115             stderr=None,
# 116             violations=violations,
# 117             metrics=metrics,
# 118             summary=summary,
# 119         )
# 120 
# 121     def _runtime_error(
# 122         self,
# 123         message: str,
# 124         raw_code: int | None = None,
# 125     ) -> ToolOutputSchema:
# 126         return ToolOutputSchema(
# 127             return_code=0,
# 128             stdout=None,
# 129             stderr=message,
# 130             violations=["RUNTIME_ERROR"],
# 131             metrics={"raw_return_code": raw_code, "exception": 1},
# 132             summary=f"❌ Black execution failed: {message}",
# 133         )


# db\seeders\files\tool_providers\docformatter_tool.py

# 1 import subprocess
# 2 import sys
# 3 from copy import deepcopy
# 4 
# 5 from app.providers.tool_provider_base import ToolProviderBase
# 6 from app.db.schemas import ToolOutputSchema
# 7 from app.enums.logging_enums import RunContext
# 8 
# 9 
# 10 class DocFormatterToolProvider(ToolProviderBase):
# 11     def _run(self, input: dict, context: RunContext | None = None) -> ToolOutputSchema:
# 12         target = input.get("target")
# 13         check = input.get("check", False)
# 14 
# 15         #  ✅ Propagate context
# 16         if context:
# 17             context = deepcopy(context)
# 18             context.parent_id = self._run_id
# 19             context.execution_chain = context.execution_chain[:] + [self._run_id]
# 20 
# 21         cmd = [sys.executable, "-m", "docformatter", target, "--in-place"]
# 22         if check:
# 23             cmd.append("--check")
# 24 
# 25         proc = subprocess.run(cmd, capture_output=True, text=True)
# 26         stdout = proc.stdout.strip()
# 27         stderr = proc.stderr.strip()
# 28         raw_code = proc.returncode
# 29 
# 30         if check:
# 31             if raw_code == 0:
# 32                 norm_code = 1
# 33                 summary = "✅ Docstring format check passed"
# 34             elif raw_code == 1:
# 35                 norm_code = 0
# 36                 summary = "❌ Docstring format check failed"
# 37             else:
# 38                 return self._runtime_error(f"Docformatter check error ({raw_code}): {stderr or stdout}")
# 39         else:
# 40             if raw_code == 0:
# 41                 norm_code = 1
# 42                 summary = "✅ Docstrings formatted successfully"
# 43             else:
# 44                 return self._runtime_error(f"Docformatter formatting error ({raw_code}): {stderr or stdout}")
# 45 
# 46         return ToolOutputSchema(
# 47             return_code=norm_code,
# 48             stdout=stdout,
# 49             stderr=stderr,
# 50             summary=summary
# 51         )
# 52 
# 53     def _runtime_error(self, message: str) -> ToolOutputSchema:
# 54         return ToolOutputSchema(
# 55             return_code=0,
# 56             stdout=None,
# 57             stderr=message,
# 58             violations=["RUNTIME_ERROR"],
# 59             metrics={"exception": 1},
# 60             summary=f"❌ docformatter execution failed: {message}",
# 61         )


# db\seeders\files\tool_providers\mypy_tool.py

# 1 import subprocess
# 2 import sys
# 3 from typing import Any, Dict, List, Optional
# 4 from copy import deepcopy
# 5 
# 6 from app.enums.logging_enums import RunContext
# 7 from app.providers.tool_provider_base import ToolProviderBase
# 8 from app.db.schemas import ToolOutputSchema
# 9 
# 10 
# 11 class MypyToolProviderV2(ToolProviderBase):
# 12     """
# 13     Runs `mypy` in *strict* mode and normalises exit‑codes:
# 14     0 ⇒ pass ⇒ return_code 1
# 15     1 ⇒ type errors ⇒ return_code 0
# 16     >1 ⇒ internal / runtime error ⇒ return_code 0 + RUNTIME_ERROR violation
# 17     """
# 18 
# 19     STRICT_ARGS: list[str] = [
# 20         "--strict",
# 21         "--disallow-untyped-defs",
# 22         "--disallow-incomplete-defs",
# 23         "--disallow-untyped-calls",
# 24         "--disallow-untyped-decorators",
# 25         "--disallow-any-generics",
# 26         "--warn-unused-ignores",
# 27         "--warn-return-any",
# 28         "--no-implicit-optional",
# 29         "--strict-equality",
# 30     ]
# 31 
# 32     def _run(self, input: dict, context: RunContext | None = None) -> ToolOutputSchema:
# 33         target: str = input.get("target")
# 34 
# 35         #  ✅ Propagate RunContext
# 36         if context:
# 37             context = deepcopy(context)
# 38             context.parent_id = self._run_id
# 39             context.execution_chain = context.execution_chain[:] + [self._run_id]
# 40 
# 41         cmd = [sys.executable, "-m", "mypy", target, *self.STRICT_ARGS]
# 42 
# 43         try:
# 44             proc = subprocess.run(
# 45                 cmd, capture_output=True, text=True, encoding="utf-8", errors="ignore"
# 46             )
# 47         except Exception as exc:
# 48             return self._runtime_error(str(exc))
# 49 
# 50         raw_code = proc.returncode
# 51         stdout = (proc.stdout or "").strip()
# 52         stderr = (proc.stderr or "").strip()
# 53 
# 54         if raw_code == 0:
# 55             return self._success(
# 56                 summary="✅ Mypy passed — no type errors",
# 57                 metrics={"error_count": 0, "raw_return_code": raw_code},
# 58                 stdout=stdout or None,
# 59             )
# 60 
# 61         if raw_code == 1:
# 62             violations = [line for line in stdout.splitlines() if line.strip()]
# 63             return self._failure(
# 64                 summary=f"❌ Mypy failed with {len(violations)} error(s)",
# 65                 violations=violations,
# 66                 metrics={
# 67                     "error_count": len(violations),
# 68                     "raw_return_code": raw_code,
# 69                 },
# 70                 stdout=stdout or None,
# 71             )
# 72 
# 73         return self._runtime_error(stderr or stdout, raw_code)
# 74 
# 75     #  ────────────────────────────────────────────────────────────── helpers
# 76     def _success(
# 77         self,
# 78         summary: str,
# 79         metrics: Dict[str, Any],
# 80         stdout: Optional[str] = None,
# 81     ) -> ToolOutputSchema:
# 82         return ToolOutputSchema(
# 83             return_code=1,
# 84             stdout=stdout,
# 85             stderr=None,
# 86             violations=None,
# 87             metrics=metrics,
# 88             summary=summary,
# 89         )
# 90 
# 91     def _failure(
# 92         self,
# 93         summary: str,
# 94         violations: List[str],
# 95         metrics: Dict[str, Any],
# 96         stdout: Optional[str] = None,
# 97     ) -> ToolOutputSchema:
# 98         return ToolOutputSchema(
# 99             return_code=0,
# 100             stdout=stdout,
# 101             stderr=None,
# 102             violations=violations,
# 103             metrics=metrics,
# 104             summary=summary,
# 105         )
# 106 
# 107     def _runtime_error(self, message: str, raw_code: int | None = None) -> ToolOutputSchema:
# 108         return ToolOutputSchema(
# 109             return_code=0,
# 110             stdout=None,
# 111             stderr=message,
# 112             violations=["RUNTIME_ERROR"],
# 113             metrics={"raw_return_code": raw_code, "exception": 1},
# 114             summary=f"❌ Mypy execution failed: {message}",
# 115         )


# db\seeders\files\tool_providers\radon_tool.py

# 1 import subprocess
# 2 import sys
# 3 from pathlib import Path
# 4 from copy import deepcopy
# 5 
# 6 from app.enums.logging_enums import RunContext
# 7 from app.providers.tool_provider_base import ToolProviderBase
# 8 from app.db.schemas import ToolOutputSchema
# 9 
# 10 
# 11 class RadonToolProvider(ToolProviderBase):
# 12     def _run(self, input: dict, context: RunContext | None = None) -> ToolOutputSchema:
# 13         target = input.get("target")
# 14         if not Path(target).exists():
# 15             raise FileNotFoundError(f"{target} not found")
# 16 
# 17         #  ✅ Clone context and inject ancestry
# 18         if context:
# 19             context = deepcopy(context)
# 20             context.parent_id = self._run_id
# 21             context.execution_chain = context.execution_chain[:] + [self._run_id]
# 22 
# 23         cmd = [sys.executable, "-m", "radon", "cc", "-s", target]
# 24         proc = subprocess.run(cmd, capture_output=True, text=True)
# 25 
# 26         stdout = proc.stdout.strip()
# 27         stderr = proc.stderr.strip()
# 28         raw_code = proc.returncode
# 29 
# 30         if raw_code != 0:
# 31             raise RuntimeError(f"radon failed: {stderr or stdout or 'unknown error'}")
# 32 
# 33         #  Determine worst grade
# 34         grades = []
# 35         for line in stdout.splitlines():
# 36             if line.strip() and "(" in line and ")" in line:
# 37                 try:
# 38                     grade = line.strip().split("(", 1)[1].split(")")[0]
# 39                     grades.append(grade)
# 40                 except Exception:
# 41                     continue
# 42 
# 43         worst_grade = max(grades, default="A")
# 44         passing = worst_grade in ("A", "B")
# 45 
# 46         norm_code = 1 if passing else 0
# 47         summary = (
# 48             f"Radon complexity OK (worst: {worst_grade})"
# 49             if passing else
# 50             f"Radon complexity too high (worst: {worst_grade})"
# 51         )
# 52 
# 53         return ToolOutputSchema(
# 54             return_code=norm_code,
# 55             stdout=stdout,
# 56             stderr=stderr,
# 57             summary=summary,
# 58         )


# db\seeders\files\tool_providers\ruff_tool.py

# 1 import subprocess
# 2 import json
# 3 import uuid
# 4 from copy import deepcopy
# 5 from typing import Any, Dict, List, Optional
# 6 
# 7 from app.enums.logging_enums import RunContext
# 8 from app.providers.tool_provider_base import ToolProviderBase
# 9 from app.db.schemas import ToolOutputSchema
# 10 
# 11 
# 12 class RuffToolProviderV2(ToolProviderBase):
# 13     """
# 14     Executes `ruff check --output-format json` and normalises results:
# 15     return_code 1 ⇒ pass, 0 ⇒ violation or runtime error.
# 16     """
# 17 
# 18     def _run(self, input: dict, context: RunContext | None = None) -> ToolOutputSchema:
# 19         target: str = input.get("target")
# 20         cmd = ["ruff", "check", "--output-format", "json", target]
# 21 
# 22         #  ✅ Isolate context to avoid corrupting upstream execution_chain
# 23         if context:
# 24             context = deepcopy(context)
# 25             context.parent_id = self._run_id
# 26             context.execution_chain = context.execution_chain[:] + [str(uuid.uuid4())]
# 27 
# 28         try:
# 29             proc = subprocess.run(
# 30                 cmd, capture_output=True, text=True, encoding="utf-8", errors="ignore"
# 31             )
# 32         except Exception as exc:
# 33             return self._runtime_error(str(exc))
# 34 
# 35         raw_code = proc.returncode
# 36         stdout = (proc.stdout or "").strip()
# 37         stderr = (proc.stderr or "").strip()
# 38 
# 39         #  ───────────────────────────────────── parse Ruff JSON output
# 40         try:
# 41             parsed = json.loads(stdout or "[]")
# 42             violations: List[str] = [
# 43                 v["code"]
# 44                 for file_item in parsed
# 45                 for v in file_item.get("violations", [])
# 46             ]
# 47         except json.JSONDecodeError:
# 48             return self._runtime_error("Failed to parse Ruff JSON output", raw_code)
# 49 
# 50         violation_count = len(violations)
# 51 
# 52         #  ───────────────────────────────────── outcome map
# 53         if raw_code == 0:
# 54             return self._success(
# 55                 summary="✅ Ruff passed — no violations",
# 56                 metrics={"violation_count": 0, "raw_return_code": raw_code},
# 57             )
# 58 
# 59         if raw_code == 1:
# 60             return self._failure(
# 61                 summary=f"❌ Ruff failed with {violation_count} violation(s)",
# 62                 violations=violations or ["UNKNOWN_VIOLATION"],
# 63                 metrics={
# 64                     "violation_count": violation_count,
# 65                     "raw_return_code": raw_code,
# 66                 },
# 67                 stdout=stdout or None,
# 68             )
# 69 
# 70         return self._runtime_error(stderr or stdout, raw_code)
# 71 
# 72     #  ───────────────────────────────────── helpers
# 73     def _success(self, summary: str, metrics: Dict[str, Any]) -> ToolOutputSchema:
# 74         return ToolOutputSchema(
# 75             return_code=1,
# 76             stdout=None,
# 77             stderr=None,
# 78             violations=None,
# 79             metrics=metrics,
# 80             summary=summary,
# 81         )
# 82 
# 83     def _failure(
# 84         self,
# 85         summary: str,
# 86         violations: List[str],
# 87         metrics: Dict[str, Any],
# 88         stdout: Optional[str] = None,
# 89     ) -> ToolOutputSchema:
# 90         return ToolOutputSchema(
# 91             return_code=0,
# 92             stdout=stdout,
# 93             stderr=None,
# 94             violations=violations,
# 95             metrics=metrics,
# 96             summary=summary,
# 97         )
# 98 
# 99     def _runtime_error(self, message: str, raw_code: int | None = None) -> ToolOutputSchema:
# 100         return ToolOutputSchema(
# 101             return_code=0,
# 102             stdout=None,
# 103             stderr=message,
# 104             violations=["RUNTIME_ERROR"],
# 105             metrics={"raw_return_code": raw_code, "exception": 1},
# 106             summary=f"❌ Ruff execution failed: {message}",
# 107         )


# db\seeders\files\tool_providers\sonarcloud_tool.py

# 1 import os
# 2 import subprocess
# 3 import time
# 4 import uuid
# 5 import json
# 6 import tempfile
# 7 from pathlib import Path
# 8 from typing import Dict
# 9 from copy import deepcopy
# 10 
# 11 from app.enums.logging_enums import RunContext
# 12 from app.providers.tool_provider_base import ToolProviderBase
# 13 from app.db.schemas import ToolOutputSchema
# 14 
# 15 
# 16 class SonarCloudToolProvider(ToolProviderBase):
# 17     def _run(self, input: dict, context: RunContext | None = None) -> ToolOutputSchema:
# 18         #  ✅ Clone context to maintain execution chain isolation
# 19         if context:
# 20             context = deepcopy(context)
# 21             context.parent_id = self._run_id
# 22             context.execution_chain = context.execution_chain[:] + [str(uuid.uuid4())]
# 23 
# 24         target_path = Path(input.get("target"))
# 25         if not target_path.exists():
# 26             raise FileNotFoundError(f"{target_path} does not exist")
# 27 
# 28         github_token, sonar_token, sonar_project, sonar_org, github_user = self._load_env()
# 29 
# 30         metrics = {}
# 31         stdout_msg = ""
# 32 
# 33         with tempfile.TemporaryDirectory() as tmpdir:
# 34             repo_url = f"https://github.com/{github_user}/codecritic_scoring"
# 35             subprocess.run(["gh", "repo", "clone", repo_url, tmpdir], check=True)
# 36 
# 37             filename = f"test_{uuid.uuid4()}.py"
# 38             src_dir = Path(tmpdir) / "src"
# 39             src_dir.mkdir(parents=True, exist_ok=True)
# 40             target_dest = src_dir / filename
# 41             target_dest.write_text(target_path.read_text(encoding="utf-8"))
# 42 
# 43             self._git_push(filename, cwd=src_dir)
# 44             self._wait_for_scan_completion(sonar_token, sonar_project)
# 45             metrics = self._poll_sonarcloud_metrics(sonar_token, sonar_project)
# 46             stdout_msg = f"Scan complete for {filename}"
# 47 
# 48             if metrics:
# 49                 self._git_cleanup(filename, cwd=src_dir)
# 50 
# 51         violations_present = any(
# 52             metrics.get(key, 0) > 0 for key in ("bugs", "vulnerabilities", "code_smells")
# 53         )
# 54 
# 55         return_code = 0 if violations_present else 1
# 56         summary = (
# 57             "SonarCloud scan clean" if return_code == 1
# 58             else "SonarCloud found issues"
# 59         )
# 60 
# 61         return ToolOutputSchema(
# 62             return_code=return_code,
# 63             stdout=stdout_msg,
# 64             stderr=None,
# 65             violations=None,
# 66             metrics=metrics,
# 67             summary=summary,
# 68         )
# 69 
# 70     def _load_env(self) -> tuple[str, str, str, str, str]:
# 71         github_token = os.getenv("GITHUB_TOKEN")
# 72         sonar_token = os.getenv("SONAR_TOKEN")
# 73         sonar_project = os.getenv("SONAR_PROJECT_KEY")
# 74         sonar_org = os.getenv("SONAR_ORGANIZATION_KEY")
# 75         github_user = os.getenv("GITHUB_USERNAME")
# 76         if not all([github_token, sonar_token, sonar_project, sonar_org, github_user]):
# 77             raise EnvironmentError("Missing one or more required environment variables")
# 78         return github_token, sonar_token, sonar_project, sonar_org, github_user
# 79 
# 80     def _git_push(self, filename: str, cwd: Path) -> None:
# 81         subprocess.run(["git", "checkout", "main"], cwd=cwd.parent, check=True)
# 82         subprocess.run(["git", "pull"], cwd=cwd.parent, check=True)
# 83         subprocess.run(["git", "add", f"src/{filename}"], cwd=cwd.parent, check=True)
# 84         subprocess.run(["git", "commit", "-m", f"Add test file src/{filename}"], cwd=cwd.parent, check=True)
# 85         subprocess.run(["git", "push"], cwd=cwd.parent, check=True)
# 86 
# 87     def _git_cleanup(self, filename: str, cwd: Path) -> None:
# 88         subprocess.run(["git", "rm", f"src/{filename}"], cwd=cwd.parent, check=True)
# 89         subprocess.run(["git", "commit", "-m", f"Remove test file src/{filename}"], cwd=cwd.parent, check=True)
# 90         subprocess.run(["git", "push"], cwd=cwd.parent, check=True)
# 91 
# 92     def _wait_for_scan_completion(self, sonar_token: str, sonar_project: str) -> None:
# 93         url = f"https://sonarcloud.io/api/ce/component?component={sonar_project}"
# 94         for _ in range(20):
# 95             time.sleep(10)
# 96             result = subprocess.run(
# 97                 ["curl", "-s", "-u", f"{sonar_token}:", url],
# 98                 capture_output=True,
# 99                 text=True
# 100             )
# 101             try:
# 102                 parsed = json.loads(result.stdout)
# 103                 status = parsed.get("current", {}).get("status")
# 104                 if status == "SUCCESS":
# 105                     return
# 106             except Exception as e:
# 107                 print(f"⚠️ Error reading scan status: {e}")
# 108 
# 109     def _poll_sonarcloud_metrics(self, sonar_token: str, sonar_project: str) -> Dict[str, int]:
# 110         metric_keys = ",".join([
# 111             "bugs", "vulnerabilities", "security_hotspots", "code_smells",
# 112             "coverage", "line_coverage", "lines_to_cover", "tests",
# 113             "test_errors", "test_failures", "skipped_tests",
# 114             "lines", "ncloc", "functions", "classes", "statements",
# 115             "complexity", "cognitive_complexity",
# 116             "duplicated_lines", "duplicated_blocks", "duplicated_files", "duplicated_lines_density"
# 117         ])
# 118         api_url = (
# 119             f"https://sonarcloud.io/api/measures/component"
# 120             f"?component={sonar_project}&metricKeys={metric_keys}"
# 121         )
# 122 
# 123         for _ in range(10):
# 124             time.sleep(5)
# 125             result = subprocess.run(
# 126                 ["curl", "-s", "-u", f"{sonar_token}:", api_url],
# 127                 capture_output=True,
# 128                 text=True
# 129             )
# 130             try:
# 131                 parsed = json.loads(result.stdout)
# 132                 measures = parsed.get("component", {}).get("measures", [])
# 133                 return {m["metric"]: float(m["value"]) for m in measures}
# 134             except Exception as e:
# 135                 print(f"⚠️ Error parsing metrics: {e}")
# 136         return {}


# db\seeders\files\tool_providers\symbol_graph.py

# 1 from __future__ import annotations
# 2 
# 3 import ast
# 4 import json
# 5 from pathlib import Path
# 6 from typing import Any, Dict
# 7 from copy import deepcopy
# 8 
# 9 from app.enums.logging_enums import RunContext
# 10 from app.providers.tool_provider_base import ToolProviderBase
# 11 from app.db.schemas import ToolOutputSchema
# 12 
# 13 
# 14 class SymbolGraph:
# 15     def __init__(self) -> None:
# 16         self.graph: Dict[str, Any] = {}
# 17 
# 18     def parse_file(self, filepath: str | Path) -> None:
# 19         from ast import parse
# 20 
# 21         filepath = Path(filepath)
# 22         source = filepath.read_text(encoding="utf-8")
# 23         tree = parse(source, filename=str(filepath))
# 24 
# 25         visitor = _SymbolGraphVisitor(filepath.stem, str(filepath), self.graph)
# 26         visitor.visit(tree)
# 27 
# 28 
# 29 class SymbolGraphToolProvider(ToolProviderBase):
# 30     def _run(self, input: dict, context: RunContext | None = None) -> ToolOutputSchema:
# 31         if context:
# 32             context = deepcopy(context)
# 33             context.parent_id = self._run_id
# 34             context.execution_chain = context.execution_chain[:] + [str(self._run_id)]
# 35 
# 36         target = input.get("target")
# 37         path = Path(target)
# 38         if not path.exists():
# 39             raise FileNotFoundError(f"{target} does not exist")
# 40 
# 41         symbol_graph_util = SymbolGraph()
# 42         symbol_graph_util.parse_file(target)
# 43 
# 44         result_json = json.dumps(symbol_graph_util.graph, indent=2)
# 45 
# 46         return ToolOutputSchema(
# 47             return_code=1,
# 48             stdout=result_json,
# 49             stderr=None,
# 50             violations=None,
# 51             metrics=symbol_graph_util.graph,
# 52             summary="Symbol graph extraction successful"
# 53         )
# 54 
# 55 
# 56 class _SymbolGraphVisitor(ast.NodeVisitor):
# 57     def __init__(self, module: str, file_path: str, graph: Dict[str, Any]) -> None:
# 58         self.module = module
# 59         self.file_path = file_path
# 60         self.graph = graph
# 61         self.scope: list[str] = []
# 62         self.current: str | None = None
# 63 
# 64     def visit_FunctionDef(self, node: ast.FunctionDef) -> None:
# 65         self._process_function_or_async_function(node)
# 66         self.generic_visit(node)
# 67         self.scope.pop()
# 68         self.current = None
# 69 
# 70     def visit_AsyncFunctionDef(self, node: ast.AsyncFunctionDef) -> None:
# 71         self._process_function_or_async_function(node)
# 72         self.generic_visit(node)
# 73         self.scope.pop()
# 74         self.current = None
# 75 
# 76     def _process_function_or_async_function(self, node: ast.FunctionDef | ast.AsyncFunctionDef) -> None:
# 77         qual = self._qualify(node.name)
# 78         self.graph[qual] = {
# 79             "name": node.name,
# 80             "type": "async_function" if isinstance(node, ast.AsyncFunctionDef) else "function",
# 81             "file": self.file_path,
# 82             "lineno": node.lineno,
# 83             "col_offset": node.col_offset,
# 84             "end_lineno": getattr(node, "end_lineno", node.lineno),
# 85             "end_col_offset": getattr(node, "end_col_offset", node.col_offset),
# 86             "scope": ".".join([self.module, *self.scope]) if self.scope else self.module,
# 87             "calls": [],
# 88         }
# 89         self.scope.append(node.name)
# 90         self.current = qual
# 91 
# 92     def visit_ClassDef(self, node: ast.ClassDef) -> None:
# 93         qual = self._qualify(node.name)
# 94         self._record(qual, "class", node)
# 95         self.scope.append(node.name)
# 96         self.current = qual
# 97         self.generic_visit(node)
# 98         self.scope.pop()
# 99         self.current = None
# 100 
# 101     def _qualify(self, name: str) -> str:
# 102         return ".".join([self.module, *self.scope, name])
# 103 
# 104     def _record(self, qual: str, typ: str, node: ast.AST) -> Dict[str, Any]:
# 105         info = {
# 106             "type": typ,
# 107             "file": self.file_path,
# 108             "lineno": getattr(node, "lineno", None),
# 109             "col_offset": getattr(node, "col_offset", None),
# 110             "end_lineno": getattr(node, "end_lineno", getattr(node, "lineno", None)),
# 111             "end_col_offset": getattr(node, "end_col_offset", getattr(node, "col_offset", None)),
# 112             "scope": ".".join([self.module, *self.scope]) if self.scope else self.module,
# 113             "calls": [],
# 114         }
# 115         self.graph[qual] = info
# 116         return info


# db\seeders\seed_agent_engine_providers.py

# 1 from uuid import uuid4
# 2 from pathlib import Path
# 3 import shutil
# 4 from sqlalchemy.orm import Session
# 5 from app.db.models import AgentEngineProviderConfig
# 6 
# 7 SEED_FILES_DIR = Path(__file__).resolve().parent / "files/agent_engine_providers"
# 8 PROJECT_ROOT = SEED_FILES_DIR.parent.parent.parent.parent.parent
# 9 EXTENSIONS_DIR = PROJECT_ROOT / "extensions"
# 10 
# 11 AGENT_ENGINES = [
# 12     #  id, filename, name, description, model, tags, cost_per_1k_tokens
# 13     (1, "basic_agent_engine_provider.py", "basic_agent_engine_provider", "Returns a mock LLM response.", "mock-llm", ["default"], 0.0),
# 14     (2, "openai_gpt4o_agent_engine_provider.py", "openai_gpt_4o_agent_engine", "Runs GPT-4o via OpenAI API.", "gpt-4o", ["openai", "production"], 0.005),
# 15     (3, "gemini_1_5_pro_agent_engine_provider.py", "gemini_1_5_pro_agent_engine", "Runs Gemini 1.5 Pro via Google AI API.", "gemini-1.5-pro", ["google", "production"], 0.007),
# 16     (4, "claude_3_sonnet_agent_engine_provider.py", "claude_3_sonnet_agent_engine", "Runs Claude 3 Sonnet via AWS Bedrock.", "claude-3-sonnet", ["aws", "bedrock", "production"], 0.003),
# 17 ]
# 18 
# 19 def seed_agent_engine_providers(db_session: Session):
# 20     EXTENSIONS_DIR.mkdir(parents=True, exist_ok=True)
# 21 
# 22     for id_, filename, name, description, model, tags, cost in AGENT_ENGINES:
# 23         guid = str(uuid4())
# 24         source_file = SEED_FILES_DIR / filename
# 25         dest_file = EXTENSIONS_DIR / f"{guid}.py"
# 26 
# 27         if not source_file.exists():
# 28             raise FileNotFoundError(f"Agent engine script not found: {source_file}")
# 29 
# 30         shutil.copy(source_file, dest_file)
# 31 
# 32         config = AgentEngineProviderConfig(
# 33             id=id_,
# 34             guid=guid,
# 35             name=name,
# 36             description=description,
# 37             model=model,
# 38             config={},
# 39             cost_per_1k_tokens=cost,
# 40             artifact_path=str(dest_file),
# 41             tags=tags,
# 42         )
# 43 
# 44         db_session.add(config)
# 45 
# 46     db_session.commit()
# 47     print("✅ Seeded agent engine configurations successfully.")


# db\seeders\seed_agent_providers.py

# 1 from uuid import uuid4
# 2 from pathlib import Path
# 3 import shutil
# 4 from sqlalchemy.orm import Session
# 5 from app.db.models import AgentProviderConfig
# 6 
# 7 SEED_FILES_DIR = Path(__file__).resolve().parent / "files/agent_providers"
# 8 PROJECT_ROOT = SEED_FILES_DIR.parent.parent.parent.parent.parent
# 9 EXTENSIONS_DIR = PROJECT_ROOT / "extensions"
# 10 
# 11 AGENT_PROVIDERS = [
# 12     {
# 13         "id": 1,
# 14         "filename": "basic_agent_provider.py",
# 15         "name": "basic_agent_provider",
# 16         "description": "Returns a hardcoded result.",
# 17         "agent_type": "basic",
# 18         "tags": ["test"],
# 19         "config": {}
# 20     },
# 21     {
# 22         "id": 2,
# 23         "filename": "linting_generator_agent_provider.py",
# 24         "name": "linting_generator_agent_provider",
# 25         "description": "Runs GPT-4o to generate linting corrections.",
# 26         "agent_type": "generator",
# 27         "tags": ["linting", "generator"],
# 28         "config": {
# 29             "agent_engine_provider_id": 2,
# 30             "prompt_provider_id": 2,
# 31             "context_provider_id": 2,
# 32             "score_provider_id": 1,
# 33             "tool_provider_ids": {
# 34                 "black": 1,
# 35                 "ruff": 3,
# 36                 "mypy": 5,
# 37                 "radon": 4
# 38             }
# 39         }
# 40     },
# 41     {
# 42         "id": 3,
# 43         "filename": "linting_discriminator_agent_provider.py",
# 44         "name": "linting_discriminator_agent_provider",
# 45         "description": "Evaluates generator output for acceptance.",
# 46         "agent_type": "discriminator",
# 47         "tags": ["linting", "discriminator"],
# 48         "config": {
# 49             "score_provider_id": 1
# 50         }
# 51     },
# 52     {
# 53         "id": 4,
# 54         "filename": "code_stability_agent_provider.py",
# 55         "name": "code_stability_agent_provider",
# 56         "description": "Rejects output if code is not parseable, importable, compilable, or type-valid.",
# 57         "agent_type": "stability",
# 58         "tags": ["stability"],
# 59         "config": {
# 60             "score_provider_id": 2
# 61         }
# 62     }
# 63 ]
# 64 
# 65 
# 66 def seed_agent_providers(db_session: Session):
# 67     EXTENSIONS_DIR.mkdir(parents=True, exist_ok=True)
# 68 
# 69     for entry in AGENT_PROVIDERS:
# 70         guid = str(uuid4())
# 71         dest_filename = f"{guid}.py"
# 72         source_file = SEED_FILES_DIR / entry["filename"]
# 73         dest_file = EXTENSIONS_DIR / dest_filename
# 74 
# 75         if not source_file.exists():
# 76             raise FileNotFoundError(f"Agent provider script not found: {source_file}")
# 77 
# 78         shutil.copy(source_file, dest_file)
# 79 
# 80         config = AgentProviderConfig(
# 81             id=entry["id"],
# 82             guid=guid,
# 83             name=entry["name"],
# 84             description=entry["description"],
# 85             artifact_path=dest_filename,
# 86             tags=entry["tags"],
# 87             config=entry["config"],
# 88             agent_type=entry["agent_type"]
# 89         )
# 90 
# 91         db_session.add(config)
# 92 
# 93     db_session.commit()
# 94     print("Seeded agent provider configurations successfully.")


# db\seeders\seed_context_providers.py

# 1 from uuid import uuid4
# 2 from pathlib import Path
# 3 import shutil
# 4 from sqlalchemy.orm import Session
# 5 from app.db.models import ContextProviderConfig
# 6 
# 7 SEED_FILES_DIR = Path(__file__).resolve().parent / "files/context_providers"
# 8 PROJECT_ROOT = SEED_FILES_DIR.parent.parent.parent.parent.parent
# 9 EXTENSIONS_DIR = PROJECT_ROOT / "extensions"
# 10 
# 11 CONTEXT_PROVIDERS = [
# 12     {
# 13         "id": 1,
# 14         "filename": "basic_context_provider.py",
# 15         "name": "basic_context_provider",
# 16         "description": "Returns static context for testing.",
# 17         "tags": ["default"],
# 18         "config": {}
# 19     },
# 20     {
# 21         "id": 2,
# 22         "filename": "linting_context_provider.py",
# 23         "name": "linting_context_provider",
# 24         "description": "Generates context for the linting system.",
# 25         "tags": ["linting", "score-aware"],
# 26         "config": {
# 27             "score_provider_id": 1,
# 28             "tool_provider_ids": None
# 29         }
# 30     }
# 31 ]
# 32 
# 33 def seed_context_providers(db_session: Session):
# 34     EXTENSIONS_DIR.mkdir(parents=True, exist_ok=True)
# 35 
# 36     for entry in CONTEXT_PROVIDERS:
# 37         guid = str(uuid4())
# 38         dest_filename = f"{guid}.py"
# 39         source_file = SEED_FILES_DIR / entry["filename"]
# 40         dest_file = EXTENSIONS_DIR / dest_filename
# 41 
# 42         if not source_file.exists():
# 43             raise FileNotFoundError(f"Context provider script not found: {source_file}")
# 44 
# 45         shutil.copy(source_file, dest_file)
# 46 
# 47         config = ContextProviderConfig(
# 48             id=entry["id"],
# 49             guid=guid,
# 50             name=entry["name"],
# 51             description=entry["description"],
# 52             artifact_path=dest_filename,
# 53             tags=entry["tags"],
# 54             config=entry["config"]
# 55         )
# 56 
# 57         db_session.add(config)
# 58 
# 59     db_session.commit()
# 60     print("Seeded context provider configurations successfully.")


# db\seeders\seed_controller_providers.py

# 1 from uuid import uuid4
# 2 from pathlib import Path
# 3 import shutil
# 4 from sqlalchemy.orm import Session
# 5 from app.db.models import ControllerProviderConfig
# 6 from app.enums.system_enums import SYSTEM 
# 7 
# 8 SEED_FILES_DIR = Path(__file__).parent / "files/controller_providers"
# 9 PROJECT_ROOT   = SEED_FILES_DIR.parents[4]
# 10 EXTENSIONS_DIR = PROJECT_ROOT / "extensions"
# 11 
# 12 CONTROLLER_PROVIDERS = [
# 13     {
# 14         "id": 1,
# 15         "filename": "preprocessing_controller_provider.py",
# 16         "name": "preprocessing_controller",
# 17         "description": "Runs only the preprocessing step (e.g. linting).",
# 18         "tags": ["preprocessing", "controller"],
# 19         "config": {
# 20             "score_provider_id": 1,
# 21             "systems": {
# 22                 SYSTEM.LINTING.value: 1
# 23             }
# 24         }
# 25     }
# 26 ]
# 27 
# 28 def seed_controller_providers(db_session: Session):
# 29     EXTENSIONS_DIR.mkdir(parents=True, exist_ok=True)
# 30     for entry in CONTROLLER_PROVIDERS:
# 31         guid = str(uuid4())
# 32         src = SEED_FILES_DIR / entry["filename"]
# 33         dest = EXTENSIONS_DIR / f"{guid}.py"
# 34         if not src.exists():
# 35             raise FileNotFoundError(f"Missing script: {src}")
# 36         shutil.copy(src, dest)
# 37 
# 38         rec = ControllerProviderConfig(
# 39             id=entry["id"],
# 40             guid=guid,
# 41             name=entry["name"],
# 42             description=entry["description"],
# 43             config=entry["config"],
# 44             artifact_path=dest.name,
# 45             tags=entry["tags"],
# 46         )
# 47         db_session.add(rec)
# 48     db_session.commit()
# 49     print("✅ Seeded controller providers")


# db\seeders\seed_program_provider.py

# 1 from uuid import uuid4
# 2 from pathlib import Path
# 3 import shutil
# 4 from sqlalchemy.orm import Session
# 5 from app.db.models import ProgramProviderConfig
# 6 from app.enums.controller_enums import CONTROLLER  #  ✅ Import your FSM state enum
# 7 
# 8 SEED_FILES_DIR = Path(__file__).resolve().parent / "files/program_providers"
# 9 PROJECT_ROOT   = SEED_FILES_DIR.parents[4]
# 10 EXTENSIONS_DIR = PROJECT_ROOT / "extensions"
# 11 
# 12 PROGRAM_PROVIDERS = [
# 13     {
# 14         "id": 1,
# 15         "filename": "codecritic_program_provider.py",
# 16         "name": "codecritic_program",
# 17         "description": "Top-level orchestration of all system controllers.",
# 18         "tags": ["program", "codecritic"],
# 19         "config": {
# 20             "score_provider_id": 1,
# 21             "controllers": {
# 22                 CONTROLLER.PREPROCESSING.value: 1
# 23             }
# 24         }
# 25     }
# 26 ]
# 27 
# 28 def seed_program_providers(db_session: Session):
# 29     EXTENSIONS_DIR.mkdir(parents=True, exist_ok=True)
# 30 
# 31     for entry in PROGRAM_PROVIDERS:
# 32         guid = str(uuid4())
# 33         src  = SEED_FILES_DIR / entry["filename"]
# 34         dst  = EXTENSIONS_DIR / f"{guid}.py"
# 35 
# 36         if not src.exists():
# 37             raise FileNotFoundError(f"❌ Missing file: {src}")
# 38 
# 39         shutil.copy(src, dst)
# 40 
# 41         record = ProgramProviderConfig(
# 42             id            = entry["id"],
# 43             guid          = guid,
# 44             name          = entry["name"],
# 45             description   = entry["description"],
# 46             config        = entry["config"],
# 47             artifact_path = dst.name,
# 48             tags          = entry["tags"],
# 49         )
# 50         db_session.add(record)
# 51 
# 52     db_session.commit()
# 53     print("✅ Seeded program providers")


# db\seeders\seed_prompt_providers.py

# 1 from uuid import uuid4
# 2 from pathlib import Path
# 3 import shutil
# 4 from sqlalchemy.orm import Session
# 5 from app.db.models import PromptProviderConfig
# 6 
# 7 CURRENT_DIR = Path(__file__).resolve().parent
# 8 SEED_FILES_DIR = CURRENT_DIR / "files/prompt_providers"
# 9 PROJECT_ROOT = SEED_FILES_DIR.parent.parent.parent.parent.parent
# 10 
# 11 EXTENSIONS_DIR = PROJECT_ROOT / "extensions"
# 12 
# 13 PROVIDERS = [
# 14     {
# 15         "id": 1,
# 16         "filename": "basic_prompt_provider.py",
# 17         "name": "basic_prompt_provider",
# 18         "description": "Returns a simple prompt response.",
# 19         "tags": ["default"],
# 20         "config": {}
# 21     },
# 22     {
# 23         "id": 2,
# 24         "filename": "linting_prompt_provider.py",
# 25         "name": "linting_prompt_provider",
# 26         "description": "Combines linting system and agent prompts.",
# 27         "tags": ["linting", "codequality"],
# 28         "config": {
# 29             "agent_prompt_id": 2,
# 30             "system_prompt_id": 2,
# 31             "context_provider_id": 2
# 32         }
# 33     }
# 34 ]
# 35 
# 36 
# 37 def seed_prompt_providers(db_session: Session):
# 38     EXTENSIONS_DIR.mkdir(parents=True, exist_ok=True)
# 39 
# 40     for entry in PROVIDERS:
# 41         guid = str(uuid4())
# 42         dest_filename = f"{guid}.py"
# 43         source_file = SEED_FILES_DIR / entry["filename"]
# 44         dest_file = EXTENSIONS_DIR / dest_filename
# 45 
# 46         if not source_file.exists():
# 47             raise FileNotFoundError(f"Prompt provider script not found: {source_file}")
# 48 
# 49         shutil.copy(source_file, dest_file)
# 50 
# 51         config = PromptProviderConfig(
# 52             id=entry["id"],
# 53             guid=guid,
# 54             name=entry["name"],
# 55             description=entry["description"],
# 56             config=entry["config"],
# 57             artifact_path=dest_filename,
# 58             tags=entry["tags"]
# 59         )
# 60 
# 61         db_session.add(config)
# 62 
# 63     db_session.commit()
# 64     print("Seeded prompt provider configurations successfully.")


# db\seeders\seed_prompts.py

# 1 from pathlib import Path
# 2 import shutil
# 3 from sqlalchemy.orm import Session
# 4 from uuid import uuid4
# 5 from app.db.models import AgentPrompt, SystemPrompt
# 6 from app.enums.system_enums import SYSTEM
# 7 
# 8 #  Define paths relative to this script's file location explicitly
# 9 CURRENT_DIR = Path(__file__).resolve().parent
# 10 SEED_FILES_DIR = CURRENT_DIR / "files/prompts"
# 11 PROJECT_ROOT = CURRENT_DIR.parent.parent.parent
# 12 EXTENSIONS_DIR = PROJECT_ROOT / "extensions"
# 13 
# 14 #  Define new prompt files to seed
# 15 AGENT_PROMPT_FILES = [
# 16     {
# 17         "id": 1,
# 18         "name": "generate",
# 19         "filename": "basic_agent_prompt.txt",
# 20         "description": "Prompt for basic code generation.",
# 21         "tags": ["linting", "generator"]
# 22     },
# 23     {
# 24         "id": 2,
# 25         "name": "linting_generator_agent",
# 26         "filename": "linting_generator_agent_prompt.txt",
# 27         "description": "Prompt for linting generator agent.",
# 28         "tags": ["linting", "generator"]
# 29     }
# 30 ]
# 31 
# 32 SYSTEM_PROMPT_FILES = [
# 33     {
# 34         "id": 1,
# 35         "name": "format",
# 36         "filename": "default_system_prompt.txt",
# 37         "system_type": SYSTEM.LINTING,
# 38         "description": "Default prompt for formatting tasks.",
# 39         "tags": ["formatting", "default"]
# 40     },
# 41     {
# 42         "id": 2,
# 43         "name": "linting_system",
# 44         "filename": "linting_system_prompt.txt",
# 45         "system_type": SYSTEM.LINTING,
# 46         "description": "System prompt governing the linting subsystem.",
# 47         "tags": ["linting", "system"]
# 48     }
# 49 ]
# 50 
# 51 
# 52 def seed_prompts(db_session: Session):
# 53     EXTENSIONS_DIR.mkdir(parents=True, exist_ok=True)
# 54 
# 55     for entry in AGENT_PROMPT_FILES:
# 56         source_file = SEED_FILES_DIR / entry["filename"]
# 57         if not source_file.exists():
# 58             raise FileNotFoundError(f"Agent prompt source file not found: {source_file}")
# 59 
# 60         guid = str(uuid4())
# 61         dest_filename = f"{guid}.txt"
# 62         dest_file = EXTENSIONS_DIR / dest_filename
# 63         shutil.copy(source_file, dest_file)
# 64 
# 65         prompt = AgentPrompt(
# 66             id=entry["id"],
# 67             guid=guid,
# 68             name=entry["name"],
# 69             description=entry["description"],
# 70             artifact_path=dest_filename,  #  Store relative path
# 71             tags=entry["tags"]
# 72         )
# 73         db_session.add(prompt)
# 74         print(f"Seeded AgentPrompt '{entry['name']}' with ID: {entry['id']} and GUID: {guid}")
# 75 
# 76     for entry in SYSTEM_PROMPT_FILES:
# 77         source_file = SEED_FILES_DIR / entry["filename"]
# 78         if not source_file.exists():
# 79             raise FileNotFoundError(f"System prompt source file not found: {source_file}")
# 80 
# 81         guid = str(uuid4())
# 82         dest_filename = f"{guid}.txt"
# 83         dest_file = EXTENSIONS_DIR / dest_filename
# 84         shutil.copy(source_file, dest_file)
# 85 
# 86         prompt = SystemPrompt(
# 87             id=entry["id"],
# 88             guid=guid,
# 89             name=entry["name"],
# 90             system_type=entry["system_type"],
# 91             description=entry["description"],
# 92             artifact_path=dest_filename,  #  Store relative path
# 93             tags=entry["tags"]
# 94         )
# 95         db_session.add(prompt)
# 96         print(f"Seeded SystemPrompt '{entry['name']}' with ID: {entry['id']} and GUID: {guid}")
# 97 
# 98     db_session.commit()


# db\seeders\seed_score_providers.py

# 1 from uuid import uuid4
# 2 from pathlib import Path
# 3 import shutil
# 4 from sqlalchemy.orm import Session
# 5 from app.db.models import ScoreProviderConfig
# 6 
# 7 CURRENT_DIR = Path(__file__).resolve().parent
# 8 SEED_FILES_DIR = CURRENT_DIR / "files/score_providers"
# 9 PROJECT_ROOT = SEED_FILES_DIR.parent.parent.parent.parent.parent
# 10 EXTENSIONS_DIR = PROJECT_ROOT / "extensions"
# 11 
# 12 SCORES = [
# 13     {
# 14         "id": 1,
# 15         "filename": "linting_score_provider.py",
# 16         "name": "linting_score_provider",
# 17         "description": "Scores lint compliance.",
# 18         "config": {
# 19             "tool_provider_ids": {
# 20                 "black": 1,
# 21                 "ruff": 3,
# 22                 "mypy": 5,
# 23                 "radon": 4
# 24             },
# 25             "context_provider_id": None
# 26         },
# 27         "tags": ["linting"]
# 28     },
# 29     {
# 30         "id": 2,
# 31         "filename": "code_stability_score_provider.py",
# 32         "name": "code_stability_score_provider",
# 33         "description": "Ensures code is structurally and syntactically safe for downstream use.",
# 34         "config": {
# 35             "tool_provider_ids": {
# 36                 "black": 1,
# 37                 "mypy": 5,
# 38                 "symbol_graph": 7
# 39             }
# 40         },
# 41         "tags": ["stability", "safety"]
# 42     }
# 43 ]
# 44 
# 45 def seed_score_providers(db_session: Session):
# 46     EXTENSIONS_DIR.mkdir(parents=True, exist_ok=True)
# 47 
# 48     for entry in SCORES:
# 49         guid = str(uuid4())
# 50         dest_filename = f"{guid}.py"
# 51         source_file = SEED_FILES_DIR / entry["filename"]
# 52         dest_file = EXTENSIONS_DIR / dest_filename
# 53 
# 54         if not source_file.exists():
# 55             raise FileNotFoundError(f"Score provider script not found: {source_file}")
# 56 
# 57         shutil.copy(source_file, dest_file)
# 58 
# 59         score = ScoreProviderConfig(
# 60             id=entry["id"],
# 61             guid=guid,
# 62             name=entry["name"],
# 63             description=entry["description"],
# 64             config=entry["config"],
# 65             artifact_path=dest_filename
# 66         )
# 67 
# 68         db_session.add(score)
# 69 
# 70     db_session.commit()
# 71     print("Seeded score providers successfully.")


# db\seeders\seed_session_configs.py

# 1 #  app/db/seeders/seed_session_configs.py
# 2 
# 3 from sqlalchemy.orm import Session
# 4 from app.db.models import SessionConfig
# 5 
# 6 SESSION_CONFIGS = [
# 7     {
# 8         "id": 1,
# 9         "program_provider_id": 1,
# 10         "name": "codecritic_test_session",
# 11         "description": "Test session for program 1",
# 12         "environment_type": "experiment",
# 13         "tags": ["test", "experiment"]
# 14     }
# 15 ]
# 16 
# 17 def seed_session_configs(db_session: Session):
# 18     for sess in SESSION_CONFIGS:
# 19         record = SessionConfig(
# 20             id               = sess["id"],
# 21             program_provider_id = sess["program_provider_id"],
# 22             name             = sess["name"],
# 23             description      = sess["description"],
# 24             environment_type = sess["environment_type"],
# 25             tags             = sess["tags"],
# 26         )
# 27         db_session.add(record)
# 28 
# 29     db_session.commit()
# 30     print("✅ Seeded session configs")


# db\seeders\seed_state_providers.py

# 1 from uuid import uuid4
# 2 from pathlib import Path
# 3 import shutil
# 4 from sqlalchemy.orm import Session
# 5 from app.db.models import StateProviderConfig
# 6 from app.enums.state_enums import STATE
# 7 from app.enums.agent_enums import AGENT
# 8 
# 9 SEED_FILES_DIR = Path(__file__).resolve().parent / "files/state_providers"
# 10 PROJECT_ROOT = SEED_FILES_DIR.parent.parent.parent.parent.parent
# 11 EXTENSIONS_DIR = PROJECT_ROOT / "extensions"
# 12 
# 13 STATE_PROVIDERS = [
# 14     (
# 15         1,
# 16         "linting_generator_state_provider.py",
# 17         "LintingGeneratorStateProvider",
# 18         "Runs the generator agent in its own FSM wrapper.",
# 19         ["linting", "generator"],
# 20         {
# 21             "score_provider_id": 1,
# 22             "agents": {
# 23                 AGENT.GENERATOR.value: 2 
# 24             }
# 25         }
# 26     ),
# 27     (
# 28         2,
# 29         "linting_discriminator_state_provider.py",
# 30         "LintingDiscriminatorStateProvider",
# 31         "Runs the discriminator agent in its own FSM wrapper.",
# 32         ["linting", "discriminator"],
# 33         {
# 34             "score_provider_id": 1,
# 35             "agents": {
# 36                 AGENT.DISCRIMINATOR.value: 3 
# 37             }
# 38         }
# 39     ),
# 40     (
# 41         3,
# 42         "code_stability_state_provider.py",
# 43         "CodeStabilityStateProvider",
# 44         "Gates code based on parse, compile, import, and static validation success.",
# 45         ["stability"],
# 46         {
# 47             "score_provider_id": 1,
# 48             "agents": {
# 49                 AGENT.STABILITY.value: 4 
# 50             }
# 51         }
# 52     )
# 53 ]
# 54 
# 55 def seed_state_providers(db_session: Session):
# 56     EXTENSIONS_DIR.mkdir(parents=True, exist_ok=True)
# 57 
# 58     for id, filename, name, description, tags, config in STATE_PROVIDERS:
# 59         guid = str(uuid4())
# 60         source_file = SEED_FILES_DIR / filename
# 61         dest_filename = f"{guid}.py"
# 62         dest_file = EXTENSIONS_DIR / dest_filename
# 63 
# 64         if not source_file.exists():
# 65             raise FileNotFoundError(f"State provider script not found: {source_file}")
# 66 
# 67         shutil.copy(source_file, dest_file)
# 68 
# 69         record = StateProviderConfig(
# 70             id=id,
# 71             guid=guid,
# 72             name=name,
# 73             description=description,
# 74             config=config,
# 75             artifact_path=dest_filename,
# 76             tags=tags,
# 77         )
# 78 
# 79         db_session.add(record)
# 80 
# 81     db_session.commit()
# 82     print("✅ Seeded state provider configurations successfully.")


# db\seeders\seed_system_providers.py

# 1 from uuid import uuid4
# 2 from pathlib import Path
# 3 import shutil
# 4 from sqlalchemy.orm import Session
# 5 from app.db.models import SystemProviderConfig
# 6 from app.enums.state_enums import STATE
# 7 from app.enums.system_enums import SYSTEM
# 8 
# 9 SEED_FILES_DIR = Path(__file__).resolve().parent / "files/system_providers"
# 10 PROJECT_ROOT = SEED_FILES_DIR.parent.parent.parent.parent.parent
# 11 EXTENSIONS_DIR = PROJECT_ROOT / "extensions"
# 12 
# 13 SYSTEM_PROVIDERS = [
# 14     {
# 15         "id": 1,
# 16         "filename": "linting_system_provider.py",
# 17         "name": "linting_system_provider",
# 18         "description": "System to iteratively lint and evaluate Python code.",
# 19         "system_type": SYSTEM.LINTING.value,
# 20         "tags": ["linting", "system"],
# 21         "config": {
# 22             "score_provider_id": 1,
# 23             "states": {
# 24                 STATE.GENERATE.value: 1,
# 25                 STATE.DISCRIMINATE.value: 2,
# 26                 STATE.CODE_STABILITY.value: 3
# 27             }
# 28         }
# 29     }
# 30 ]
# 31 
# 32 def seed_system_providers(db_session: Session):
# 33     EXTENSIONS_DIR.mkdir(parents=True, exist_ok=True)
# 34 
# 35     for entry in SYSTEM_PROVIDERS:
# 36         guid = str(uuid4())
# 37         source_file = SEED_FILES_DIR / entry["filename"]
# 38         dest_filename = f"{guid}.py"
# 39         dest_file = EXTENSIONS_DIR / dest_filename
# 40 
# 41         if not source_file.exists():
# 42             raise FileNotFoundError(f"System provider script not found: {source_file}")
# 43 
# 44         shutil.copy(source_file, dest_file)
# 45 
# 46         record = SystemProviderConfig(
# 47             id=entry["id"],
# 48             guid=guid,
# 49             name=entry["name"],
# 50             description=entry["description"],
# 51             system_type=entry["system_type"],
# 52             config=entry["config"],
# 53             artifact_path=dest_filename,
# 54             tags=entry["tags"],
# 55         )
# 56 
# 57         db_session.add(record)
# 58 
# 59     db_session.commit()
# 60     print("✅ Seeded system provider configurations successfully.")


# db\seeders\seed_tool_providers.py

# 1 from uuid import uuid4
# 2 from pathlib import Path
# 3 import shutil
# 4 from sqlalchemy.orm import Session
# 5 from app.db.models import ToolProviderConfig
# 6 
# 7 CURRENT_DIR = Path(__file__).resolve().parent
# 8 SEED_FILES_DIR = CURRENT_DIR / "files/tool_providers"
# 9 PROJECT_ROOT = SEED_FILES_DIR.parent.parent.parent.parent.parent
# 10 EXTENSIONS_DIR = PROJECT_ROOT / "extensions"
# 11 
# 12 TOOLS = [
# 13     {
# 14         "id": 1,
# 15         "filename": "black_tool.py",
# 16         "name": "black",
# 17         "description": "Formats Python code using Black.",
# 18         "config": {}
# 19     },
# 20     {
# 21         "id": 2,
# 22         "filename": "sonarcloud_tool.py",
# 23         "name": "sonarcloud",
# 24         "description": "Static analysis via SonarCloud.",
# 25         "config": {}
# 26     },
# 27     {
# 28         "id": 3,
# 29         "filename": "ruff_tool.py",
# 30         "name": "ruff",
# 31         "description": "Python linting with Ruff.",
# 32         "config": {}
# 33     },
# 34     {
# 35         "id": 4,
# 36         "filename": "radon_tool.py",
# 37         "name": "radon",
# 38         "description": "Analyzes Python code complexity.",
# 39         "config": {}
# 40     },
# 41     {
# 42         "id": 5,
# 43         "filename": "mypy_tool.py",
# 44         "name": "mypy",
# 45         "description": "Static type checking with mypy.",
# 46         "config": {}
# 47     },
# 48     {
# 49         "id": 6,
# 50         "filename": "docformatter_tool.py",
# 51         "name": "docformatter",
# 52         "description": "Formats docstrings using docformatter.",
# 53         "config": {}
# 54     },
# 55     {
# 56         "id": 7,
# 57         "filename": "symbol_graph.py",
# 58         "name": "symbol_graph",
# 59         "description": "Extracts and analyzes Python symbols into structured graphs.",
# 60         "config": {}
# 61     }
# 62 ]
# 63 
# 64 def seed_tool_providers(db_session: Session):
# 65     EXTENSIONS_DIR.mkdir(parents=True, exist_ok=True)
# 66 
# 67     for entry in TOOLS:
# 68         guid = str(uuid4())
# 69         dest_filename = f"{guid}.py"
# 70         source_file = SEED_FILES_DIR / entry["filename"]
# 71         dest_file = EXTENSIONS_DIR / dest_filename
# 72 
# 73         if not source_file.exists():
# 74             raise FileNotFoundError(f"Tool script not found: {source_file}")
# 75 
# 76         shutil.copy(source_file, dest_file)
# 77 
# 78         tool = ToolProviderConfig(
# 79             id=entry["id"],
# 80             guid=guid,
# 81             name=entry["name"],
# 82             description=entry["description"],
# 83             config=entry["config"],
# 84             artifact_path=dest_filename
# 85         )
# 86 
# 87         db_session.add(tool)
# 88 
# 89     db_session.commit()
# 90     print("Seeded tool configurations successfully.")


# enums\__init__.py



# enums\agent_engine_enums.py

# 1 from enum import Enum
# 2 
# 3 
# 4 class AGENT_ENGINE_MODEL(str, Enum):
# 5     MOCK = "mock-llm"
# 6     GPT_4O = "gpt-4o"
# 7     GEMINI_1_5_PRO = "gemini-1.5-pro"
# 8     CLAUDE_3_SONNET = "claude-3-sonnet"


# enums\agent_enums.py

# 1 from __future__ import annotations
# 2 
# 3 from enum import Enum
# 4 
# 5 
# 6 class AGENT(str, Enum):
# 7     START = "start"
# 8     END = "end"
# 9     GENERATOR = "generator"
# 10     DISCRIMINATOR = "discriminator"
# 11     MEDIATOR = "mediator"
# 12     STABILITY = "stability"
# 13     BASIC = "basic"
# 14     UNKNOWN = "unknown"
# 15 
# 16 class AGENT_STATE(Enum):
# 17     INIT = "init"
# 18     RUNNING = "running"
# 19     COMPLETE = "complete"


# enums\controller_enums.py

# 1 from enum import Enum
# 2 
# 3 class CONTROLLER(str, Enum):
# 4     START = "start"
# 5     END = "end"
# 6     PREPROCESSING = "preprocessing_controller"
# 7     TESTING = "testing_controller"
# 8     OBSERVABILITY = "observability_controller"
# 9     DOCUMENTATION = "documentation_controller"
# 10     REVIEW = "review_controller"
# 11     PLANNING = "planning_controller"
# 12     MLOPS = "mlops_controller"
# 13     SAFETY = "safety_controller"
# 14     MISC = "misc_controller"


# enums\fsm_enums.py

# 1 from enum import Enum
# 2 
# 3 class STATE_TYPE(str, Enum):
# 4     START = "start"
# 5     INTERMEDIATE = "intermediate"
# 6     END = "end"
# 7 
# 8 
# 9 class DECISION_TYPE(str, Enum):
# 10     INITIAL = "initial"
# 11     IMPROVED = "improved"
# 12     ACCEPTED = "accepted"
# 13     REJECTED = "rejected"
# 14     UNKNOWN = "unknown"
# 15 
# 16 class TRANSITION_REASON_TYPE(str, Enum):
# 17     INITIALIZATION = "initialization"
# 18     SUCCESSFUL = "completed successfully"
# 19     UNSUCCESSFUL = "completed unsuccessfully"
# 20     GENERATING = "generating"
# 21     EVALUATING = "evaluating" 
# 22     ERROR = "error"
# 23     CUSTOM_RULE = "custom_rule"
# 24 


# enums\logging_enums.py

# 1 from enum import Enum
# 2 from typing import List, Optional
# 3 
# 4 class LOG_TYPE(str, Enum):
# 5     ERROR = "error_log"
# 6     PROVIDER = "provider_log"
# 7     STATE_TRANSITION = "state_transition_log"
# 8     AGENT_CONVERSATION = "agent_conversation_log"
# 9     SNAPSHOT_METRICS = "snapshot_metrics"
# 10     FILE = "file_log"
# 11 
# 12 class PROVIDER_TYPE(str, Enum):
# 13     TOOL = "tool"
# 14     AGENT_ENGINE = "agent_engine"
# 15     AGENT = "agent"
# 16     PROMPT = "prompt"
# 17     CONTEXT = "context"
# 18     SCORE = "score"
# 19     STATE = "state"
# 20     SYSTEM = "system"
# 21     CONTROLLER = "controller"
# 22     PROGRAM = "program"
# 23     SESSION = "session"
# 24     UNKNOWN = "unknown"
# 25 
# 26 class ERROR_TYPE(str, Enum):
# 27     RUNTIME = "runtime"
# 28     VALIDATION = "validation"
# 29     CONFIGURATION = "configuration"
# 30     CONNECTION = "connection"
# 31     AUTHENTICATION = "authentication"
# 32     TIMEOUT = "timeout"
# 33     UNKNOWN = "unknown"
# 34 
# 35 class RunContext:
# 36     def __init__(
# 37         self,
# 38         *,
# 39         called_by_type: PROVIDER_TYPE,
# 40         called_by_id: int,
# 41         session_id: str,
# 42         file_log_id: str,
# 43         parent_id: Optional[str] = None,
# 44         execution_chain: Optional[List[str]] = None,
# 45     ):
# 46         self.called_by_type = called_by_type
# 47         self.called_by_id = called_by_id
# 48         self.session_id = session_id
# 49         self.file_log_id = file_log_id
# 50         self.parent_id = parent_id
# 51         self.execution_chain = execution_chain or []


# enums\scoring_enums.py

# 1 from enum import Enum
# 2 
# 3 class SCORING_METRIC_TYPE(str, Enum):
# 4     LINTING_SCORE = "linting_score"
# 5     CODE_STABILITY_SCORE = "code_stability_score"


# enums\state_enums.py

# 1 from enum import Enum
# 2 
# 3 class STATE(str, Enum):
# 4     START = "start"
# 5     END = "end"
# 6     PREPROCESS = "preprocess"
# 7     CODE_STABILITY = "code_stability"
# 8     GENERATE = "generate"
# 9     DISCRIMINATE = "discriminate"


# enums\system_enums.py

# 1 from __future__ import annotations
# 2 from enum import Enum
# 3 
# 4 
# 5 class SYSTEM(str, Enum):
# 6     START = "start"
# 7     END = "end"
# 8 
# 9     #  Core Code Transformation
# 10     LINTING = "linting"
# 11     FORMATTING = "formatting"
# 12     DOCSTRING = "docstring"
# 13     TYPE_ANNOTATION = "type_annotation"
# 14     REFACTORING = "refactoring"
# 15     DEAD_CODE_REMOVAL = "dead_code_removal"
# 16     COMPLEXITY_REDUCTION = "complexity_reduction"
# 17 
# 18     #  Testing and Validation
# 19     UNIT_TESTING = "unit_testing"
# 20     EDGE_TESTING = "edge_testing"
# 21     INTEGRATION_TESTING = "integration_testing"
# 22     TEST_FIX_GENERATION = "test_fix_generation"
# 23     MOCK_GENERATION = "mock_generation"
# 24     STUB_EXTRACTION = "stub_extraction"
# 25 
# 26     #  Observability and Instrumentation
# 27     LOGGING_INJECTION = "logging_injection"
# 28     TRACE_ANNOTATION = "trace_annotation"
# 29     ERROR_HANDLING = "error_handling"
# 30 
# 31     #  Documentation and Explanation
# 32     INLINE_COMMENTING = "inline_commenting"
# 33     FUNCTION_SUMMARIZATION = "function_summarization"
# 34     FILE_SUMMARIZATION = "file_summarization"
# 35     CHANGE_SUMMARIZATION = "change_summarization"
# 36 
# 37     #  Review and Oversight
# 38     CODE_REVIEW = "code_review"
# 39     STATIC_ANALYSIS = "static_analysis"
# 40     REGRESSION_ANALYSIS = "regression_analysis"
# 41     MEDIATOR_RECONCILIATION = "mediator_reconciliation"
# 42     SCORING_EVALUATION = "scoring_evaluation"
# 43 
# 44     #  Planning and Organization
# 45     SYMBOL_GRAPH_GENERATION = "symbol_graph_generation"
# 46     DEPENDENCY_MAPPING = "dependency_mapping"
# 47     FILE_RESTRUCTURING = "file_restructuring"
# 48     FUNCTION_SPLITTING = "function_splitting"
# 49 
# 50     #  MLOps-Specific
# 51     DATA_VALIDATION = "data_validation"
# 52     METADATA_ANNOTATION = "metadata_annotation"
# 53     MODEL_DOC_GENERATION = "model_doc_generation"
# 54     PIPELINE_TESTING = "pipeline_testing"
# 55     TRAINING_CONFIG_AUDIT = "training_config_audit"
# 56     FEATURE_TRACEABILITY = "feature_traceability"
# 57 
# 58     #  Production Safety
# 59     SECURITY_CHECK = "security_check"
# 60     POLICY_COMPLIANCE = "policy_compliance"
# 61     LICENSE_CHECK = "license_check"
# 62 
# 63     #  Misc / Cross-cutting
# 64     PATCHING = "patching"
# 65     PROMPT_REFINEMENT = "prompt_refinement"
# 66     AGENT_RECOMMENDATION = "agent_recommendation"
# 67     SEMANTIC_DIFF = "semantic_diff"
# 68     CONTEXT_FILTERING = "context_filtering"
# 69 
# 70     UNKNOWN = "unknown"
# 71 
# 72 
# 73 
# 74 


# enums\tool_enums.py

# 1 from enum import Enum
# 2 
# 3 class ToolProvider(Enum):
# 4     BLACK = 1
# 5     SONARCLOUD = 2
# 6     RUFF = 3
# 7     RADON = 4
# 8     MYPY = 5
# 9     DOCFORMATTER = 6
# 10     SYMBOL_GRAPH = 7
# 11 
# 12     @classmethod
# 13     def get_name(cls, id_):
# 14         for tool in cls:
# 15             if tool.value == id_:
# 16                 return tool.name.lower()
# 17         return f"Unknown ({id_})"


# factories\__init__.py



# factories\agent_engine_provider_factory.py

# 1 from app.db.models import AgentEngineProviderConfig
# 2 from app.enums.logging_enums import RunContext
# 3 from app.factories.base_provider_factory import BaseProviderFactory
# 4 from app.providers.agent_engine_provider_base import AgentEngineProviderBase
# 5 
# 6 
# 7 class AgentEngineProviderFactory(BaseProviderFactory):
# 8     config_model = AgentEngineProviderConfig
# 9     base_class = AgentEngineProviderBase
# 10 
# 11     @classmethod
# 12     def create(
# 13         cls,
# 14         id: int,
# 15         *,
# 16         context: RunContext,
# 17         **kwargs
# 18     ) -> AgentEngineProviderBase:
# 19         instance = super().create(
# 20             id,
# 21             context=context,
# 22             **kwargs
# 23         )
# 24         return instance


# factories\agent_provider_factory.py

# 1 from app.factories.base_provider_factory import BaseProviderFactory
# 2 from app.db.models import AgentProviderConfig
# 3 from app.providers.agent_provider_base import AgentProviderBase
# 4 from app.enums.logging_enums import PROVIDER_TYPE, RunContext
# 5 
# 6 
# 7 class AgentProviderFactory(BaseProviderFactory):
# 8     config_model = AgentProviderConfig
# 9     base_class = AgentProviderBase
# 10 
# 11     @classmethod
# 12     def create(
# 13         cls,
# 14         id: int,
# 15         *,
# 16         context: RunContext,
# 17         **kwargs
# 18     ) -> AgentProviderBase:
# 19         from app.factories.agent_engine_provider_factory import AgentEngineProviderFactory
# 20         from app.factories.prompt_provider_factory import PromptProviderFactory
# 21         from app.factories.context_provider_factory import ContextProviderFactory
# 22         from app.factories.score_provider_factory import ScoreProviderFactory
# 23         from app.factories.tool_provider_factory import ToolProviderFactory
# 24 
# 25         preload_instance = super().create(id, context=context)
# 26         config = preload_instance._config.config or {}
# 27         provider_id = preload_instance._config.id
# 28         provider_type = preload_instance._infer_provider_type()
# 29 
# 30         #  ─── Child context for subproviders ─────────────────────────────
# 31         child_context = RunContext(
# 32             called_by_type=provider_type,
# 33             called_by_id=provider_id,
# 34             session_id=context.session_id,
# 35             file_log_id=context.file_log_id,
# 36             parent_id=preload_instance._run_id,
# 37             execution_chain=context.execution_chain.copy()
# 38         )
# 39 
# 40         #  ─── Create subproviders ───────────────────────────────────────
# 41         agent_engine = (
# 42             AgentEngineProviderFactory.create(
# 43                 config["agent_engine_provider_id"],
# 44                 context=child_context,
# 45             ) if config.get("agent_engine_provider_id") else None
# 46         )
# 47 
# 48         prompt_provider = (
# 49             PromptProviderFactory.create(
# 50                 config["prompt_provider_id"],
# 51                 context=child_context,
# 52             ) if config.get("prompt_provider_id") else None
# 53         )
# 54 
# 55         context_provider = (
# 56             ContextProviderFactory.create(
# 57                 config["context_provider_id"],
# 58                 context=child_context,
# 59             ) if config.get("context_provider_id") else None
# 60         )
# 61 
# 62         score_provider = (
# 63             ScoreProviderFactory.create(
# 64                 config["score_provider_id"],
# 65                 context=child_context,
# 66             ) if config.get("score_provider_id") else None
# 67         )
# 68 
# 69         tool_providers = [
# 70             ToolProviderFactory.create(
# 71                 tid,
# 72                 context=child_context,
# 73             )
# 74             for tid in (config.get("tool_provider_ids") or {}).values()
# 75         ]
# 76 
# 77         #  ─── Final instance construction ───────────────────────────────
# 78         cls_type = type(preload_instance)
# 79         return cls_type(
# 80             config=preload_instance._config,
# 81             agent_engine=agent_engine,
# 82             prompt_provider=prompt_provider,
# 83             context_provider=context_provider,
# 84             score_provider=score_provider,
# 85             tool_providers=tool_providers,
# 86             context=context
# 87         )


# factories\base_provider_factory.py

# 1 from sqlalchemy.orm import Session
# 2 from importlib.util import spec_from_file_location, module_from_spec
# 3 from pathlib import Path
# 4 
# 5 from app.db import init_db  #  global DB engine instance
# 6 from app.providers.base_provider import BaseProvider
# 7 from app.enums.logging_enums import PROVIDER_TYPE, RunContext
# 8 
# 9 PROJECT_ROOT = Path(__file__).resolve().parents[2]
# 10 EXTENSIONS_DIR = PROJECT_ROOT / "extensions"
# 11 
# 12 engine = init_db(reset=False)
# 13 
# 14 class BaseProviderFactory:
# 15     config_model = None
# 16     base_class = BaseProvider
# 17 
# 18     @classmethod
# 19     def create(
# 20         cls,
# 21         id: int,
# 22         *,
# 23         context: RunContext,
# 24         **kwargs
# 25     ) -> BaseProvider:
# 26         with Session(bind=engine) as session:
# 27             config = session.get(cls.config_model, int(id))
# 28             if not config:
# 29                 raise ValueError(f"{cls.config_model.__name__} ID {id} not found")
# 30 
# 31             ext_path = (EXTENSIONS_DIR / config.artifact_path).resolve()
# 32             if not ext_path.exists():
# 33                 raise FileNotFoundError(f"Extension not found: {ext_path}")
# 34 
# 35         spec = spec_from_file_location(ext_path.stem, ext_path)
# 36         if spec is None or spec.loader is None:
# 37             raise ImportError(f"Cannot load module spec for {ext_path}")
# 38 
# 39         module = module_from_spec(spec)
# 40         spec.loader.exec_module(module)
# 41 
# 42         provider_class = next(
# 43             (obj for obj in vars(module).values()
# 44              if isinstance(obj, type) and issubclass(obj, cls.base_class) and obj is not cls.base_class),
# 45             None
# 46         )
# 47 
# 48         if provider_class is None:
# 49             raise ImportError(f"No valid subclass of {cls.base_class.__name__} found in {ext_path}")
# 50 
# 51         instance = provider_class(config=config, context=context, **kwargs)
# 52         return instance


# factories\context_provider_factory.py

# 1 from app.factories.base_provider_factory import BaseProviderFactory
# 2 from app.db.models import ContextProviderConfig
# 3 from app.providers.context_provider_base import ContextProviderBase
# 4 from app.enums.logging_enums import RunContext
# 5 
# 6 
# 7 class ContextProviderFactory(BaseProviderFactory):
# 8     config_model = ContextProviderConfig
# 9     base_class = ContextProviderBase
# 10 
# 11     @classmethod
# 12     def create(
# 13         cls,
# 14         id: int,
# 15         *,
# 16         context: RunContext,
# 17         **kwargs
# 18     ) -> ContextProviderBase:
# 19         from app.factories.score_provider_factory import ScoreProviderFactory
# 20         from app.factories.tool_provider_factory import ToolProviderFactory
# 21 
# 22         instance = super().create(id, context=context)
# 23         config = instance._config.config or {}
# 24         provider_id = instance._config.id
# 25         provider_type = instance._infer_provider_type()
# 26 
# 27         #  ─── Child context for subproviders ─────────────────────────────
# 28         child_context = RunContext(
# 29             called_by_type=provider_type,
# 30             called_by_id=provider_id,
# 31             session_id=context.session_id,
# 32             file_log_id=context.file_log_id,
# 33             parent_id=instance._run_id,
# 34             execution_chain=context.execution_chain.copy()
# 35         )
# 36 
# 37         if (score_id := config.get("score_provider_id")):
# 38             if isinstance(score_id, int) and score_id > 0:
# 39                 score = ScoreProviderFactory.create(
# 40                     score_id,
# 41                     context=child_context
# 42                 )
# 43                 instance.set_score_provider(score)
# 44                 if hasattr(score, "set_context_provider"):
# 45                     score.set_context_provider(instance)
# 46 
# 47         for _, tool_id in (config.get("tool_provider_ids") or {}).items():
# 48             tool = ToolProviderFactory.create(
# 49                 tool_id,
# 50                 context=child_context
# 51             )
# 52             instance.set_tool_provider(tool)
# 53 
# 54         return instance


# factories\controller_provider_factory.py

# 1 from app.factories.base_provider_factory import BaseProviderFactory
# 2 from app.db.models import ControllerProviderConfig
# 3 from app.providers.controller_provider_base import ControllerProviderBase
# 4 from app.enums.logging_enums import RunContext
# 5 
# 6 
# 7 class ControllerProviderFactory(BaseProviderFactory):
# 8     config_model = ControllerProviderConfig
# 9     base_class = ControllerProviderBase
# 10 
# 11     @classmethod
# 12     def create(
# 13         cls,
# 14         id: int,
# 15         *,
# 16         context: RunContext,
# 17         **kwargs
# 18     ) -> ControllerProviderBase:
# 19         from app.factories.context_provider_factory import ContextProviderFactory
# 20         from app.factories.score_provider_factory import ScoreProviderFactory
# 21         from app.factories.tool_provider_factory import ToolProviderFactory
# 22         from app.factories.system_provider_factory import SystemProviderFactory
# 23 
# 24         inst = super().create(id, context=context)
# 25         config = inst._config.config or {}
# 26         provider_id = inst._config.id
# 27         provider_type = inst._infer_provider_type()
# 28 
# 29         #  ─── Child context ─────────────────────────────────────────────
# 30         child_context = RunContext(
# 31             called_by_type=provider_type,
# 32             called_by_id=provider_id,
# 33             session_id=context.session_id,
# 34             file_log_id=context.file_log_id,
# 35             parent_id=inst._run_id,
# 36             execution_chain=context.execution_chain.copy()
# 37         )
# 38 
# 39         #  ─── Subproviders ──────────────────────────────────────────────
# 40         context_provider = (
# 41             ContextProviderFactory.create(
# 42                 config["context_provider_id"],
# 43                 context=child_context
# 44             ) if config.get("context_provider_id") else None
# 45         )
# 46 
# 47         score_provider = (
# 48             ScoreProviderFactory.create(
# 49                 config["score_provider_id"],
# 50                 context=child_context
# 51             ) if config.get("score_provider_id") else None
# 52         )
# 53 
# 54         tool_providers = [
# 55             ToolProviderFactory.create(
# 56                 tid,
# 57                 context=child_context
# 58             )
# 59             for tid in (config.get("tool_provider_ids") or {}).values()
# 60         ]
# 61 
# 62         system_providers = {
# 63             name: SystemProviderFactory.create(
# 64                 sys_id,
# 65                 context=child_context
# 66             )
# 67             for name, sys_id in (config.get("systems") or {}).items()
# 68         }
# 69 
# 70         #  ─── Final instantiation ───────────────────────────────────────
# 71         cls_type = type(inst)
# 72         return cls_type(
# 73             config=inst._config,
# 74             context_provider=context_provider,
# 75             score_provider=score_provider,
# 76             tool_providers=tool_providers,
# 77             system_providers=system_providers,
# 78             context=context
# 79         )


# factories\program_provider_factory.py

# 1 from app.factories.base_provider_factory import BaseProviderFactory
# 2 from app.db.models import ProgramProviderConfig
# 3 from app.providers.program_provider_base import ProgramProviderBase
# 4 from app.enums.logging_enums import RunContext
# 5 
# 6 
# 7 class ProgramProviderFactory(BaseProviderFactory):
# 8     config_model = ProgramProviderConfig
# 9     base_class = ProgramProviderBase
# 10 
# 11     @classmethod
# 12     def create(
# 13         cls,
# 14         id: int,
# 15         *,
# 16         context: RunContext,
# 17         **kwargs
# 18     ) -> ProgramProviderBase:
# 19         from app.factories.context_provider_factory import ContextProviderFactory
# 20         from app.factories.score_provider_factory import ScoreProviderFactory
# 21         from app.factories.tool_provider_factory import ToolProviderFactory
# 22         from app.factories.controller_provider_factory import ControllerProviderFactory
# 23 
# 24         inst = super().create(id, context=context)
# 25         config = inst._config.config or {}
# 26         provider_id = inst._config.id
# 27         provider_type = inst._infer_provider_type()
# 28 
# 29         #  ─── Child context ─────────────────────────────────────────────
# 30         child_context = RunContext(
# 31             called_by_type=provider_type,
# 32             called_by_id=provider_id,
# 33             session_id=context.session_id,
# 34             file_log_id=context.file_log_id,
# 35             parent_id=inst._run_id,
# 36             execution_chain=context.execution_chain.copy()
# 37         )
# 38 
# 39         #  ─── Subproviders ──────────────────────────────────────────────
# 40         context_provider = (
# 41             ContextProviderFactory.create(
# 42                 config["context_provider_id"],
# 43                 context=child_context
# 44             ) if config.get("context_provider_id") else None
# 45         )
# 46 
# 47         score_provider = (
# 48             ScoreProviderFactory.create(
# 49                 config["score_provider_id"],
# 50                 context=child_context
# 51             ) if config.get("score_provider_id") else None
# 52         )
# 53 
# 54         tool_providers = [
# 55             ToolProviderFactory.create(
# 56                 tid,
# 57                 context=child_context
# 58             )
# 59             for tid in (config.get("tool_provider_ids") or {}).values()
# 60         ]
# 61 
# 62         controller_providers = {
# 63             name: ControllerProviderFactory.create(
# 64                 controller_id,
# 65                 context=child_context
# 66             )
# 67             for name, controller_id in (config.get("controllers") or {}).items()
# 68         }
# 69 
# 70         #  ─── Final instantiation ───────────────────────────────────────
# 71         cls_type = type(inst)
# 72         instance = cls_type(
# 73             config=inst._config,
# 74             context_provider=context_provider,
# 75             score_provider=score_provider,
# 76             tool_providers=tool_providers,
# 77             controller_providers=controller_providers,
# 78             context=context
# 79         )
# 80 
# 81         return instance


# factories\prompt_provider_factory.py

# 1 from app.db.models import PromptProviderConfig
# 2 from app.factories.base_provider_factory import BaseProviderFactory
# 3 from app.providers.prompt_provider_base import PromptProviderBase
# 4 from app.factories.context_provider_factory import ContextProviderFactory
# 5 from app.enums.logging_enums import RunContext
# 6 from pathlib import Path
# 7 
# 8 
# 9 class PromptProviderFactory(BaseProviderFactory):
# 10     config_model = PromptProviderConfig
# 11     base_class = PromptProviderBase
# 12 
# 13     @classmethod
# 14     def create(
# 15         cls,
# 16         id: int,
# 17         *,
# 18         context: RunContext,
# 19         **kwargs
# 20     ) -> PromptProviderBase:
# 21         instance = super().create(id, context=context)
# 22 
# 23         config = instance._config.config or {}
# 24         provider_id = instance._config.id
# 25         provider_type = instance._infer_provider_type()
# 26 
# 27         #  ─── Child context ─────────────────────────────────────────────
# 28         child_context = RunContext(
# 29             called_by_type=provider_type,
# 30             called_by_id=provider_id,
# 31             session_id=context.session_id,
# 32             file_log_id=context.file_log_id,
# 33             parent_id=instance._run_id,
# 34             execution_chain=context.execution_chain.copy()
# 35         )
# 36 
# 37         #  ─── Prompt loading ────────────────────────────────────────────
# 38         def load_prompt(prompt_id: int, table: str) -> str | None:
# 39             if not prompt_id:
# 40                 return None
# 41             conn = instance._engine.raw_connection()
# 42             try:
# 43                 cur = conn.cursor()
# 44                 cur.execute(f"SELECT artifact_path FROM {table} WHERE id = ?", (prompt_id,))
# 45                 row = cur.fetchone()
# 46                 if row:
# 47                     artifact_path = row[0]
# 48                     if artifact_path:
# 49                         return (Path("extensions") / artifact_path).read_text(encoding="utf-8").strip()
# 50             finally:
# 51                 conn.close()
# 52             return None
# 53 
# 54         agent_prompt_text = load_prompt(config.get("agent_prompt_id"), "agent_prompt")
# 55         system_prompt_text = load_prompt(config.get("system_prompt_id"), "system_prompt")
# 56 
# 57         context_provider = None
# 58         if config.get("context_provider_id"):
# 59             context_provider = ContextProviderFactory.create(
# 60                 config["context_provider_id"],
# 61                 context=child_context
# 62             )
# 63 
# 64         cls_type = type(instance)
# 65         return cls_type(
# 66             config=instance._config,
# 67             agent_text=agent_prompt_text,
# 68             system_text=system_prompt_text,
# 69             context_provider=context_provider,
# 70             context=context
# 71         )


# factories\score_provider_factory.py

# 1 from app.factories.base_provider_factory import BaseProviderFactory
# 2 from app.db.models import ScoreProviderConfig
# 3 from app.providers.score_provider_base import ScoreProviderBase
# 4 from app.enums.logging_enums import RunContext
# 5 
# 6 
# 7 class ScoreProviderFactory(BaseProviderFactory):
# 8     config_model = ScoreProviderConfig
# 9     base_class = ScoreProviderBase
# 10 
# 11     @classmethod
# 12     def create(
# 13         cls,
# 14         id: int,
# 15         *,
# 16         context: RunContext,
# 17         **kwargs
# 18     ) -> ScoreProviderBase:
# 19         from app.factories.tool_provider_factory import ToolProviderFactory
# 20         from app.factories.context_provider_factory import ContextProviderFactory
# 21 
# 22         instance = super().create(id, context=context)
# 23         config = instance._config.config or {}
# 24         provider_id = instance._config.id
# 25         provider_type = instance._infer_provider_type()
# 26 
# 27         #  ─── Child context ─────────────────────────────────────────────
# 28         child_context = RunContext(
# 29             called_by_type=provider_type,
# 30             called_by_id=provider_id,
# 31             session_id=context.session_id,
# 32             file_log_id=context.file_log_id,
# 33             parent_id=instance._run_id,
# 34             execution_chain=context.execution_chain.copy()
# 35         )
# 36 
# 37         if (ctx_id := config.get("context_provider_id")):
# 38             context_provider = ContextProviderFactory.create(
# 39                 ctx_id,
# 40                 context=child_context
# 41             )
# 42             instance.set_context_provider(context_provider)
# 43             if hasattr(context_provider, "set_score_provider"):
# 44                 context_provider.set_score_provider(instance)
# 45 
# 46         for _, tool_id in (config.get("tool_provider_ids") or {}).items():
# 47             tool = ToolProviderFactory.create(
# 48                 tool_id,
# 49                 context=child_context
# 50             )
# 51             instance.set_tool_provider(tool)
# 52 
# 53         return instance


# factories\state_provider_factory.py

# 1 from app.factories.base_provider_factory import BaseProviderFactory
# 2 from app.db.models import StateProviderConfig
# 3 from app.providers.state_provider_base import StateProviderBase
# 4 from app.enums.logging_enums import RunContext
# 5 
# 6 
# 7 class StateProviderFactory(BaseProviderFactory):
# 8     config_model = StateProviderConfig
# 9     base_class = StateProviderBase
# 10 
# 11     @classmethod
# 12     def create(
# 13         cls,
# 14         id: int,
# 15         *,
# 16         context: RunContext,
# 17         **kwargs
# 18     ) -> StateProviderBase:
# 19         from app.factories.agent_provider_factory import AgentProviderFactory
# 20         from app.factories.context_provider_factory import ContextProviderFactory
# 21         from app.factories.score_provider_factory import ScoreProviderFactory
# 22         from app.factories.tool_provider_factory import ToolProviderFactory
# 23 
# 24         preload_instance = super().create(id, context=context)
# 25         config = preload_instance._config.config or {}
# 26         provider_id = preload_instance._config.id
# 27         provider_type = preload_instance._infer_provider_type()
# 28 
# 29         #  ─── Child context ─────────────────────────────────────────────
# 30         child_context = RunContext(
# 31             called_by_type=provider_type,
# 32             called_by_id=provider_id,
# 33             session_id=context.session_id,
# 34             file_log_id=context.file_log_id,
# 35             parent_id=preload_instance._run_id,
# 36             execution_chain=context.execution_chain.copy()
# 37         )
# 38 
# 39         #  ─── Subproviders ──────────────────────────────────────────────
# 40         agent_providers = {
# 41             name: AgentProviderFactory.create(
# 42                 agent_id,
# 43                 context=child_context
# 44             )
# 45             for name, agent_id in config.get("agents", {}).items()
# 46         }
# 47 
# 48         context_provider = (
# 49             ContextProviderFactory.create(
# 50                 config["context_provider_id"],
# 51                 context=child_context
# 52             )
# 53             if config.get("context_provider_id") else None
# 54         )
# 55 
# 56         score_provider = (
# 57             ScoreProviderFactory.create(
# 58                 config["score_provider_id"],
# 59                 context=child_context
# 60             )
# 61             if config.get("score_provider_id") else None
# 62         )
# 63 
# 64         tool_providers = [
# 65             ToolProviderFactory.create(
# 66                 tool_id,
# 67                 context=child_context
# 68             )
# 69             for _, tool_id in (config.get("tool_provider_ids") or {}).items()
# 70         ]
# 71 
# 72         #  ─── Final instantiation ───────────────────────────────────────
# 73         cls_type = type(preload_instance)
# 74         return cls_type(
# 75             config=preload_instance._config,
# 76             agent_providers=agent_providers,
# 77             context_provider=context_provider,
# 78             score_provider=score_provider,
# 79             tool_providers=tool_providers,
# 80             context=context
# 81         )


# factories\system_provider_factory.py

# 1 from app.factories.base_provider_factory import BaseProviderFactory
# 2 from app.db.models import SystemProviderConfig
# 3 from app.providers.system_provider_base import SystemProviderBase
# 4 from app.enums.logging_enums import RunContext
# 5 
# 6 
# 7 class SystemProviderFactory(BaseProviderFactory):
# 8     config_model = SystemProviderConfig
# 9     base_class = SystemProviderBase
# 10 
# 11     @classmethod
# 12     def create(
# 13         cls,
# 14         id: int,
# 15         *,
# 16         context: RunContext,
# 17         **kwargs
# 18     ) -> SystemProviderBase:
# 19         #  ⏱️ Delayed imports to avoid circular dependencies
# 20         from app.factories.state_provider_factory import StateProviderFactory
# 21         from app.factories.context_provider_factory import ContextProviderFactory
# 22         from app.factories.score_provider_factory import ScoreProviderFactory
# 23         from app.factories.tool_provider_factory import ToolProviderFactory
# 24 
# 25         preload_instance = super().create(id, context=context)
# 26         config = preload_instance._config.config or {}
# 27         provider_id = preload_instance._config.id
# 28         provider_type = preload_instance._infer_provider_type()
# 29 
# 30         #  ─── Child context ─────────────────────────────────────────────
# 31         child_context = RunContext(
# 32             called_by_type=provider_type,
# 33             called_by_id=provider_id,
# 34             session_id=context.session_id,
# 35             file_log_id=context.file_log_id,
# 36             parent_id=preload_instance._run_id,
# 37             execution_chain=context.execution_chain.copy()
# 38         )
# 39 
# 40         #  ─── Subproviders ──────────────────────────────────────────────
# 41         context_provider = (
# 42             ContextProviderFactory.create(
# 43                 config["context_provider_id"],
# 44                 context=child_context
# 45             ) if config.get("context_provider_id") else None
# 46         )
# 47 
# 48         score_provider = (
# 49             ScoreProviderFactory.create(
# 50                 config["score_provider_id"],
# 51                 context=child_context
# 52             ) if config.get("score_provider_id") else None
# 53         )
# 54 
# 55         tool_providers = [
# 56             ToolProviderFactory.create(
# 57                 tool_id,
# 58                 context=child_context
# 59             )
# 60             for _, tool_id in (config.get("tool_provider_ids") or {}).items()
# 61         ]
# 62 
# 63         state_providers = {
# 64             name: StateProviderFactory.create(
# 65                 state_id,
# 66                 context=child_context
# 67             )
# 68             for name, state_id in (config.get("states") or {}).items()
# 69         }
# 70 
# 71         #  ─── Final instantiation ───────────────────────────────────────
# 72         cls_type = type(preload_instance)
# 73         return cls_type(
# 74             config=preload_instance._config,
# 75             context_provider=context_provider,
# 76             score_provider=score_provider,
# 77             tool_providers=tool_providers,
# 78             state_providers=state_providers,
# 79             context=context
# 80         )


# factories\tool_provider_factory.py

# 1 from app.factories.base_provider_factory import BaseProviderFactory
# 2 from app.db.models import ToolProviderConfig
# 3 from app.providers.tool_provider_base import ToolProviderBase
# 4 from app.enums.logging_enums import RunContext
# 5 
# 6 
# 7 class ToolProviderFactory(BaseProviderFactory):
# 8     config_model = ToolProviderConfig
# 9     base_class = ToolProviderBase
# 10 
# 11     @classmethod
# 12     def create(
# 13         cls,
# 14         id: int,
# 15         *,
# 16         context: RunContext,
# 17         **kwargs
# 18     ) -> ToolProviderBase:
# 19         from app.factories.context_provider_factory import ContextProviderFactory
# 20         from app.factories.score_provider_factory import ScoreProviderFactory
# 21 
# 22         instance = super().create(id, context=context)
# 23         config = instance._config.config or {}
# 24         provider_id = instance._config.id
# 25         provider_type = instance._infer_provider_type()
# 26 
# 27         #  ─── Child context ─────────────────────────────────────────────
# 28         child_context = RunContext(
# 29             called_by_type=provider_type,
# 30             called_by_id=provider_id,
# 31             session_id=context.session_id,
# 32             file_log_id=context.file_log_id,
# 33             parent_id=instance._run_id,
# 34             execution_chain=context.execution_chain.copy()
# 35         )
# 36 
# 37         if (ctx_id := config.get("context_provider_id")):
# 38             context_provider = ContextProviderFactory.create(
# 39                 ctx_id,
# 40                 context=child_context
# 41             )
# 42             instance.set_context_provider(context_provider)
# 43             if hasattr(context_provider, "set_tool_provider"):
# 44                 context_provider.set_tool_provider(instance)
# 45 
# 46         if (score_id := config.get("score_provider_id")):
# 47             if isinstance(score_id, int) and score_id > 0:
# 48                 score_provider = ScoreProviderFactory.create(
# 49                     score_id,
# 50                     context=child_context
# 51                 )
# 52                 instance.set_score_provider(score_provider)
# 53                 if hasattr(score_provider, "set_tool_provider"):
# 54                     score_provider.set_tool_provider(instance)
# 55 
# 56         return instance


# providers\__init__.py



# providers\agent_engine_provider_base.py

# 1 from __future__ import annotations
# 2 from abc import abstractmethod
# 3 from copy import deepcopy
# 4 from datetime import datetime, timezone
# 5 from pathlib import Path
# 6 from sqlalchemy.orm import Session
# 7 
# 8 from app.db.models import SnapshotMetrics
# 9 from app.db.schemas import AgentEngineOutput, SnapshotMetricsSchema
# 10 from app.enums.system_enums import SYSTEM
# 11 from app.enums.agent_enums import AGENT
# 12 from app.enums.fsm_enums import DECISION_TYPE
# 13 from app.enums.logging_enums import LOG_TYPE, RunContext
# 14 
# 15 from app.providers.base_provider import BaseProvider
# 16 from app.utilities.metadata.snapshots.analyze_code_metrics import analyze_code, compute_deltas
# 17 from app.utilities.metadata.snapshots.snapshot_writer import SnapshotWriter
# 18 from app.utilities.metadata.footer.code_annnotation_utils import (
# 19     split_code_and_notes,
# 20     append_agent_note
# 21 )
# 22 
# 23 
# 24 class AgentEngineProviderBase(BaseProvider):
# 25     def __init__(
# 26         self,
# 27         config=None,
# 28         prompt_provider=None,
# 29         context_provider=None,
# 30         score_provider=None,
# 31         tool_providers=None,
# 32         context: RunContext = None,
# 33         **kwargs
# 34     ):
# 35         super().__init__(config=config, context=context, **kwargs)
# 36         self.prompt_provider = prompt_provider
# 37         self.context_provider = context_provider
# 38         self.score_provider = score_provider
# 39         self.tool_providers = tool_providers or []
# 40 
# 41     def _run_provider(self, input: dict) -> AgentEngineOutput:
# 42         file_path = input.get("before") or input.get("file_path") or (self._config.config or {}).get("before")
# 43         session_id = self._session_id
# 44         system = SYSTEM(input.get("system", SYSTEM.UNKNOWN))
# 45         agent_type = AGENT(input.get("agent_type", AGENT.UNKNOWN))
# 46         agent_id = input.get("agent_id", -1)
# 47 
# 48         before_code = ""
# 49         prior_notes = ""
# 50         if file_path:
# 51             path = Path(file_path).resolve()
# 52             if path.exists():
# 53                 full_code = path.read_text(encoding="utf-8")
# 54                 before_code, prior_notes = split_code_and_notes(full_code)
# 55 
# 56         response_text = ""
# 57         snapshot_id = None
# 58         summary = None
# 59         token_count = 0
# 60 
# 61         try:
# 62             raw_output = self._run(input=input, context=self.fork_context())
# 63             if isinstance(raw_output, AgentEngineOutput):
# 64                 response_text = raw_output.response
# 65                 token_count = raw_output.token_count
# 66                 cost_usd = raw_output.cost_usd
# 67             else:
# 68                 response_text = raw_output
# 69                 token_count = len(response_text.split())
# 70                 cost_usd = token_count * (self._config.cost_per_1k_tokens or 0.0) / 1000
# 71 
# 72             code_block = self._extract_block(response_text, "[CODE]", "[/CODE]")
# 73             log_block = self._extract_block(response_text, "[CONVERSATION_LOG_ENTRY]", "[/CONVERSATION_LOG_ENTRY]")
# 74 
# 75             if "[AGENT_DECISION]accept" in response_text:
# 76                 decision = DECISION_TYPE.ACCEPTED
# 77             elif "[AGENT_DECISION]reject" in response_text:
# 78                 decision = DECISION_TYPE.REJECTED
# 79             else:
# 80                 decision = DECISION_TYPE.UNKNOWN
# 81 
# 82             def normalize_code(code: str) -> str:
# 83                 return "\n".join(line.strip() for line in code.strip().splitlines() if line.strip())
# 84 
# 85             if not code_block or not before_code or normalize_code(code_block) == normalize_code(before_code):
# 86                 self._log.debug("🟡 Generator returned output matching input (normalized)")
# 87                 self._log.debug(f"🔍 BEFORE:\n{before_code}")
# 88                 self._log.debug(f"🆕 AFTER:\n{code_block}")
# 89                 return AgentEngineOutput(
# 90                     response=response_text,
# 91                     token_count=token_count,
# 92                     cost_usd=cost_usd,
# 93                     snapshot_id=None,
# 94                     summary="No change detected (normalized)",
# 95                     code=code_block,
# 96                     agent_decision=decision.value,
# 97                     conversation_log_entry=log_block,
# 98                 )
# 99 
# 100             after_code = code_block.rstrip()
# 101             if prior_notes:
# 102                 after_code += f"\n\n{prior_notes}"
# 103             if log_block:
# 104                 after_code = append_agent_note(after_code, system=system.value, agent_name=self._config.name, note=log_block)
# 105 
# 106             before_metrics = analyze_code(before_code)
# 107             after_metrics = analyze_code(after_code)
# 108             deltas = compute_deltas(before_metrics, after_metrics)
# 109 
# 110             timestamp = datetime.now(timezone.utc)
# 111             metadata = {
# 112                 "system": system.value,
# 113                 "agent": self._config.name,
# 114                 "agent_type": agent_type.value,
# 115                 "agent_id": agent_id,
# 116                 "score": None,
# 117                 "state": str(input.get("state_context", {}).get("state", "unknown")),
# 118                 "decision": decision.value,
# 119                 "timestamp": timestamp.isoformat(),
# 120                 **{f"{k}_before": v for k, v in before_metrics.items()},
# 121                 **{f"{k}_after": v for k, v in after_metrics.items()},
# 122                 **deltas,
# 123             }
# 124 
# 125             snapshot_id = SnapshotWriter().write_snapshot(
# 126                 before=before_code,
# 127                 after=after_code,
# 128                 session_id=session_id,
# 129                 metadata=metadata,
# 130             )
# 131 
# 132             with Session(bind=self._engine) as session:
# 133                 entry = SnapshotMetrics(
# 134                     session_id=session_id,
# 135                     snapshot_id=snapshot_id,
# 136                     system=system.value,
# 137                     agent=self._config.name,
# 138                     agent_type=agent_type.value,
# 139                     agent_id=agent_id,
# 140                     score=None,
# 141                     state=metadata["state"],
# 142                     decision=decision.value,
# 143                     timestamp=timestamp,
# 144                     **{
# 145                         k: metadata.get(k)
# 146                         for k in SnapshotMetrics.__table__.columns.keys()
# 147                         if k.endswith("_before") or k.endswith("_after") or k.endswith("_delta")
# 148                     },
# 149                 )
# 150                 session.add(entry)
# 151                 session.commit()
# 152 
# 153             self.logger.write(LOG_TYPE.SNAPSHOT_METRICS, SnapshotMetricsSchema(
# 154                 session_id=session_id,
# 155                 file_log_id=self._file_log_id,
# 156                 snapshot_id=snapshot_id,
# 157                 system=system,
# 158                 agent=self._config.name,
# 159                 agent_type=agent_type,
# 160                 agent_id=agent_id,
# 161                 score=None,
# 162                 state=metadata["state"],
# 163                 decision=decision,
# 164                 timestamp=timestamp,
# 165                 line_count_before=metadata["line_count_before"],
# 166                 line_count_after=metadata["line_count_after"],
# 167                 function_count_before=metadata["function_count_before"],
# 168                 function_count_after=metadata["function_count_after"],
# 169                 symbol_count_before=metadata["symbol_count_before"],
# 170                 symbol_count_after=metadata["symbol_count_after"],
# 171                 branch_count_before=metadata["branch_count_before"],
# 172                 branch_count_after=metadata["branch_count_after"],
# 173                 comment_count_before=metadata["comment_count_before"],
# 174                 comment_count_after=metadata["comment_count_after"],
# 175                 line_count_delta=metadata["line_count_delta"],
# 176                 function_count_delta=metadata["function_count_delta"],
# 177                 symbol_count_delta=metadata["symbol_count_delta"],
# 178                 branch_count_delta=metadata["branch_count_delta"],
# 179                 comment_count_delta=metadata["comment_count_delta"],
# 180             ))
# 181 
# 182             self._log.debug(f"📦 Snapshot written: {snapshot_id}")
# 183             summary = log_block or "Snapshot successfully written"
# 184 
# 185         except Exception as exc:
# 186             self._log.error(f"❌ Error during agent run: {exc}")
# 187             summary = f"Error: {str(exc)}"
# 188             cost_usd = 0.0
# 189 
# 190         return AgentEngineOutput(
# 191             response=response_text,
# 192             token_count=token_count,
# 193             cost_usd=cost_usd,
# 194             snapshot_id=snapshot_id,
# 195             summary=summary,
# 196             code=code_block,
# 197             agent_decision=decision.value,
# 198             conversation_log_entry=log_block,
# 199         )
# 200 
# 201     def _extract_block(self, text: str, start_tag: str, end_tag: str) -> str | None:
# 202         start = text.find(start_tag)
# 203         end = text.find(end_tag)
# 204         if start != -1 and end != -1 and start < end:
# 205             return text[start + len(start_tag):end].strip()
# 206         return None
# 207 
# 208     @abstractmethod
# 209     def _run(self, input: dict, context: RunContext | None = None) -> str:
# 210         raise NotImplementedError


# providers\agent_provider_base.py

# 1 from copy import deepcopy
# 2 from datetime import datetime, timezone
# 3 from pathlib import Path
# 4 from abc import abstractmethod
# 5 from sqlalchemy.orm import Session
# 6 
# 7 from app.db.models import SnapshotMetrics
# 8 from app.enums.logging_enums import LOG_TYPE, RunContext
# 9 from app.enums.agent_enums import AGENT
# 10 from app.enums.fsm_enums import DECISION_TYPE
# 11 from app.providers.base_provider import BaseProvider
# 12 from app.utilities.metadata.footer.code_annnotation_utils import append_agent_note
# 13 from app.utilities.metadata.snapshots.analyze_code_metrics import analyze_code, compute_deltas
# 14 from app.utilities.metadata.snapshots.snapshot_writer import SnapshotWriter
# 15 from app.db.schemas import AgentConversationLogSchema, AgentOutputSchema
# 16 
# 17 
# 18 class AgentProviderBase(BaseProvider):
# 19     def __init__(
# 20         self,
# 21         config=None,
# 22         agent_engine=None,
# 23         prompt_provider=None,
# 24         context_provider=None,
# 25         score_provider=None,
# 26         tool_providers=None,
# 27         context: RunContext = None,
# 28         **kwargs
# 29     ):
# 30         super().__init__(config=config, context=context, **kwargs)
# 31         self._agent_engine = agent_engine
# 32         self._prompt_provider = prompt_provider
# 33         self._context_provider = context_provider
# 34         self._score_provider = score_provider
# 35         self._tool_providers = tool_providers or []
# 36 
# 37     def _run_provider(self, input: dict) -> AgentOutputSchema:
# 38         context = self.fork_context()
# 39         output = self._run(input=input, context=context)
# 40         self._log.debug(f"\U0001f9fb _run_provider called for: {self._config.name if self._config else 'unknown'}")
# 41 
# 42         response = output.response if hasattr(output, "response") else str(output)
# 43         decision = self._infer_decision(response)
# 44         log_content = self._extract_log(response) or self._default_log(decision)
# 45 
# 46         snapshot_id = None
# 47         file_name = input.get("before") or input.get("file_path") or (self._config.config or {}).get("before")
# 48 
# 49         if file_name and (code_block := self._extract_code(response)):
# 50             before_path = Path(file_name)
# 51             try:
# 52                 relative_path = before_path.relative_to(Path.cwd())
# 53             except ValueError:
# 54                 relative_path = before_path
# 55             before_path = relative_path
# 56 
# 57             if before_path.exists():
# 58                 before_code = before_path.read_text(encoding="utf-8")
# 59                 after_code = code_block
# 60 
# 61                 before_metrics = analyze_code(before_code)
# 62                 after_metrics = analyze_code(after_code)
# 63                 deltas = compute_deltas(before_metrics, after_metrics)
# 64 
# 65                 ctx = self.fork_context()
# 66                 score_result = (
# 67                     self._score_provider.run({"file_path": str(before_path)}, context=ctx)
# 68                     if self._score_provider else None
# 69                 )
# 70 
# 71                 metadata = {
# 72                     "system": input.get("system", "unknown"),
# 73                     "agent": self._config.name if self._config else "unknown",
# 74                     "score": score_result.value if score_result else None if not hasattr(score_result, "model_dump") else score_result.model_dump(),
# 75                     "state": str(input.get("state_context", {}).get("state", "unknown")),
# 76                     "decision": decision.value,
# 77                     "timestamp": datetime.now(timezone.utc).isoformat(),
# 78                     **{f"{k}_before": v for k, v in before_metrics.items()},
# 79                     **{f"{k}_after": v for k, v in after_metrics.items()},
# 80                     **deltas,
# 81                 }
# 82 
# 83                 after_code = after_code.rstrip() + "\n\n"
# 84 
# 85                 after_code = append_agent_note(
# 86                     file_content=after_code,
# 87                     system=self._called_by_type,
# 88                     agent_name=self._config.name if self._config else "unknown",
# 89                     note=log_content
# 90                 )
# 91 
# 92                 snapshot_id = SnapshotWriter().write_snapshot(
# 93                     before=before_code,
# 94                     after=after_code,
# 95                     session_id=self._session_id,
# 96                     metadata=metadata
# 97                 )
# 98                 with Session(bind=self._engine) as session:
# 99                     entry = SnapshotMetrics(
# 100                         session_id=self._session_id,
# 101                         file_log_id=self._file_log_id,
# 102                         snapshot_id=snapshot_id,
# 103                         system=self._called_by_type,
# 104                         agent=self._config.name,
# 105                         score=metadata.get("score"),
# 106                         state=metadata.get("state"),
# 107                         decision=DECISION_TYPE(metadata.get("decision", "unknown")),
# 108                         timestamp=datetime.fromisoformat(metadata["timestamp"]),
# 109                         **{
# 110                             k: metadata.get(k)
# 111                             for k in SnapshotMetrics.__table__.columns.keys()
# 112                             if k.endswith("_before") or k.endswith("_after") or k.endswith("_delta")
# 113                         }
# 114                     )
# 115                     session.add(entry)
# 116                     session.commit()
# 117                 self._snapshot_id = snapshot_id
# 118                 self._log.debug(f"\U0001f4e6 Snapshot written to: {snapshot_id}")
# 119             else:
# 120                 self._log.warning(f"❌ Snapshot skipped: file does not exist → {before_path}")
# 121 
# 122         #  Always log conversation
# 123         self.logger.write(LOG_TYPE.AGENT_CONVERSATION, AgentConversationLogSchema(
# 124             session_id=self._session_id,
# 125             file_log_id=self._file_log_id,
# 126             system=input.get("system", "unknown"),
# 127             agent_type=self._config.agent_type if hasattr(self._config, "agent_type") else AGENT.BASIC,
# 128             agent_provider_config_id=self._config.id if self._config else -1,
# 129             content=log_content,
# 130             timestamp=datetime.now(timezone.utc),
# 131         ))
# 132         self._log.debug("✅ AGENT_CONVERSATION log write complete")
# 133 
# 134         return output
# 135 
# 136     def _infer_decision(self, text: str) -> DECISION_TYPE:
# 137         if "[AGENT_DECISION]accept" in text:
# 138             return DECISION_TYPE.ACCEPTED
# 139         if "[AGENT_DECISION]reject" in text:
# 140             return DECISION_TYPE.REJECTED
# 141         return DECISION_TYPE.UNKNOWN
# 142 
# 143     def _extract_log(self, text: str) -> str | None:
# 144         return self._extract_block(text, "[CONVERSATION_LOG_ENTRY]", "[/CONVERSATION_LOG_ENTRY]")
# 145 
# 146     def _default_log(self, decision: DECISION_TYPE) -> str:
# 147         return f"No conversation log entry found. Agent decision was: {decision.value}."
# 148 
# 149     def _extract_code(self, text: str) -> str | None:
# 150         return self._extract_block(text, "[CODE]", "[/CODE]")
# 151 
# 152     def _extract_block(self, text: str, start_tag: str, end_tag: str) -> str | None:
# 153         import re
# 154         pattern = re.compile(re.escape(start_tag) + r"(.*?)" + re.escape(end_tag), re.DOTALL)
# 155         match = pattern.search(text)
# 156         return match.group(1).strip() if match else None
# 157 
# 158     @abstractmethod
# 159     def _run(self, input: dict, context: RunContext | None = None) -> AgentOutputSchema:
# 160         raise NotImplementedError


# providers\base_provider.py

# 1 from abc import abstractmethod
# 2 from copy import deepcopy
# 3 from datetime import datetime, timezone
# 4 import hashlib
# 5 import json
# 6 import logging
# 7 import time
# 8 from pathlib import Path
# 9 from typing import Dict, Optional
# 10 from uuid import uuid4
# 11 
# 12 from app.db import init_db
# 13 from app.utilities.metadata.logging.logging_provider import LoggingMixin, LOG_TYPE, LoggingProvider
# 14 from app.db.schemas import ProviderLogSchema, ErrorLogSchema
# 15 from app.enums.logging_enums import PROVIDER_TYPE, ERROR_TYPE, RunContext
# 16 
# 17 class BaseProvider:
# 18     def __init__(
# 19         self,
# 20         config=None,
# 21         context: RunContext = None,
# 22         **kwargs
# 23     ):
# 24         self._config = config
# 25         self._context = deepcopy(context) if context else None
# 26         self._run_id = str(uuid4())
# 27         self._engine = init_db(reset=False)
# 28         self.logger = LoggingProvider()
# 29         self._log = logging.getLogger(self.__class__.__name__)
# 30 
# 31         if self._context:
# 32             if not self._context.execution_chain or self._context.execution_chain[-1] != self._run_id:
# 33                 self._context.parent_id = self._context.execution_chain[-1] if self._context.execution_chain else None
# 34                 self._context.execution_chain.append(self._run_id)
# 35             self._session_id = self._context.session_id
# 36             self._file_log_id = self._context.file_log_id
# 37             self._called_by_type = self._context.called_by_type
# 38             self._called_by_id = self._context.called_by_id
# 39         else:
# 40             self._session_id = None
# 41             self._file_log_id = None
# 42             self._called_by_type = None
# 43             self._called_by_id = None
# 44 
# 45     def run(self, input: dict | None = None, context: RunContext = None) -> str:
# 46         input = input or {}
# 47         self._run_id = str(uuid4())
# 48 
# 49         if context:
# 50             self._context = deepcopy(context)
# 51             self._session_id     = self._context.session_id
# 52             self._file_log_id    = self._context.file_log_id
# 53             self._called_by_type = self._context.called_by_type
# 54             self._called_by_id   = self._context.called_by_id
# 55             if not self._context.execution_chain or self._context.execution_chain[-1] != self._run_id:
# 56                 self._context.parent_id = self._context.execution_chain[-1] if self._context.execution_chain else None
# 57                 self._context.execution_chain.append(self._run_id)
# 58 
# 59         start_clock = time.perf_counter()
# 60         start_time = datetime.now(timezone.utc)
# 61 
# 62         try:
# 63             output = self._run_provider(input)
# 64         except Exception as exc:
# 65             latency_ms = int((time.perf_counter() - start_clock) * 1000)
# 66             try:
# 67                 self.logger.write(
# 68                     LOG_TYPE.ERROR,
# 69                     ErrorLogSchema(
# 70                         session_id=self._session_id,
# 71                         file_log_id=self._file_log_id,
# 72                         error_type=self._map_error_type(exc).value,
# 73                         message=str(exc),
# 74                         file_path=str(Path(__file__).relative_to(Path.cwd())),
# 75                         provider_id=self._config.id if self._config else None,
# 76                         provider_type=self._infer_provider_type(),
# 77                         timestamp=start_time,
# 78                         latency_ms=latency_ms,
# 79                         called_by_type=self._called_by_type if self._called_by_type else None,
# 80                         called_by_id=self._called_by_id,
# 81                         run_id=self._run_id,
# 82                         parent_id=self._context.parent_id if self._context else None,
# 83                         execution_chain=self._context.execution_chain[:] if self._context else [],
# 84                     ),
# 85                 )
# 86             except Exception:
# 87                 pass
# 88             raise
# 89 
# 90         latency_ms = int((time.perf_counter() - start_clock) * 1000)
# 91 
# 92         try:
# 93             input_str = json.dumps(
# 94                 {k: v.model_dump() if hasattr(v, "model_dump") else v for k, v in input.items()},
# 95                 default=str,
# 96             )
# 97         except Exception:
# 98             input_str = ""
# 99 
# 100         try:
# 101             if hasattr(output, "model_dump"):
# 102                 dumped = output.model_dump()
# 103                 flat_dump = {}
# 104                 for k, v in dumped.items():
# 105                     if isinstance(v, dict):
# 106                         for sub_k, sub_v in v.items():
# 107                             flat_dump[f"{k}.{sub_k}"] = sub_v
# 108                     else:
# 109                         flat_dump[k] = v
# 110 
# 111                 output_str = json.dumps(flat_dump, default=str)
# 112                 output_schema = output.__class__.__name__
# 113             else:
# 114                 output_str = json.dumps(output, default=str)
# 115                 output_schema = None
# 116         except Exception:
# 117             output_str = ""
# 118             output_schema = None
# 119 
# 120         try:
# 121             self.logger.write(
# 122                 LOG_TYPE.PROVIDER,
# 123                 ProviderLogSchema(
# 124                     session_id=self._session_id,
# 125                     file_log_id=self._file_log_id,
# 126                     provider_id=self._config.id if self._config else -1,
# 127                     provider_type=self._infer_provider_type(),
# 128                     input=input_str,
# 129                     output=output_str,
# 130                     output_schema=output_schema,
# 131                     latency_ms=latency_ms,
# 132                     config_hash=self._compute_config_hash(),
# 133                     file_name=getattr(self._config, "artifact_path", "").split("/")[-1]
# 134                     if getattr(self._config, "artifact_path", None)
# 135                     else None,
# 136                     timestamp=start_time,
# 137                     called_by_type=self._called_by_type if self._called_by_type else None,
# 138                     called_by_id=self._called_by_id,
# 139                     run_id=self._run_id,
# 140                     parent_id=self._context.parent_id if self._context else None,
# 141                     execution_chain=self._context.execution_chain[:] if self._context else [],
# 142                 ),
# 143             )
# 144         except Exception:
# 145             raise
# 146 
# 147         self._log.debug("✅ Provider run logged")
# 148         return output
# 149 
# 150     def _map_error_type(self, exc: Exception) -> ERROR_TYPE:
# 151         return ERROR_TYPE.RUNTIME
# 152 
# 153     def _infer_provider_type(self) -> PROVIDER_TYPE:
# 154         cls = self.__class__.__name__.lower()
# 155         if "tool" in cls:
# 156             return PROVIDER_TYPE.TOOL
# 157         if "engine" in cls:
# 158             return PROVIDER_TYPE.AGENT_ENGINE
# 159         if "agent" in cls:
# 160             return PROVIDER_TYPE.AGENT
# 161         if "prompt" in cls:
# 162             return PROVIDER_TYPE.PROMPT
# 163         if "context" in cls:
# 164             return PROVIDER_TYPE.CONTEXT
# 165         if "score" in cls:
# 166             return PROVIDER_TYPE.SCORE
# 167         if "state" in cls:
# 168             return PROVIDER_TYPE.STATE
# 169         if "system" in cls:
# 170             return PROVIDER_TYPE.SYSTEM
# 171         if "controller" in cls:
# 172             return PROVIDER_TYPE.CONTROLLER
# 173         if "program" in cls:
# 174             return PROVIDER_TYPE.PROGRAM
# 175         return PROVIDER_TYPE.UNKNOWN
# 176 
# 177     def _compute_config_hash(self) -> Optional[str]:
# 178         if not self._config or not getattr(self._config, "config", None):
# 179             return None
# 180         return hashlib.md5(json.dumps(self._config.config, sort_keys=True).encode()).hexdigest()
# 181 
# 182     def propagate_file_log_id(self, file_log_id: str):
# 183         self._file_log_id = file_log_id
# 184 
# 185         for attr_name in dir(self):
# 186             attr = getattr(self, attr_name, None)
# 187 
# 188             if isinstance(attr, BaseProvider):
# 189                 attr.propagate_file_log_id(file_log_id)
# 190 
# 191             elif isinstance(attr, list):
# 192                 for item in attr:
# 193                     if isinstance(item, BaseProvider):
# 194                         item.propagate_file_log_id(file_log_id)
# 195 
# 196             elif isinstance(attr, dict):
# 197                 for item in attr.values():
# 198                     if isinstance(item, BaseProvider):
# 199                         item.propagate_file_log_id(file_log_id)
# 200 
# 201     def fork_context(self) -> RunContext:
# 202         forked = deepcopy(self._context or RunContext())
# 203         forked.parent_id = self._run_id
# 204         return forked


# providers\context_provider_base.py

# 1 from __future__ import annotations
# 2 
# 3 from abc import abstractmethod
# 4 
# 5 from app.db.schemas import ContextOutputSchema
# 6 from app.enums.logging_enums import RunContext
# 7 from app.providers.base_provider import BaseProvider
# 8 
# 9 from typing import TYPE_CHECKING
# 10 if TYPE_CHECKING:
# 11     from app.providers.score_provider_base import ScoreProviderBase
# 12     from app.providers.tool_provider_base import ToolProviderBase
# 13 
# 14 class ContextProviderBase(BaseProvider):
# 15     def __init__(self, config=None, context: RunContext = None, **kwargs):
# 16         super().__init__(config=config, context=context, **kwargs)
# 17 
# 18     def _run_provider(self, input: dict) -> ContextOutputSchema:
# 19         return self._run(input=input, context=self.fork_context())
# 20     
# 21     def set_tool_provider(self, provider: "ToolProviderBase") -> None:
# 22         if not hasattr(self, "tool_providers"):
# 23             self.tool_providers = []
# 24         self.tool_providers.append(provider)
# 25 
# 26     def set_score_provider(self, provider: "ScoreProviderBase") -> None:
# 27         self.score_provider = provider
# 28 
# 29 
# 30     @abstractmethod
# 31     def _run(self, input: dict, context: RunContext | None = None) -> ContextOutputSchema:
# 32         raise NotImplementedError


# providers\controller_provider_base.py

# 1 from __future__ import annotations
# 2 from abc import abstractmethod
# 3 from copy import deepcopy
# 4 from datetime import datetime
# 5 from pathlib import Path
# 6 import shutil
# 7 from typing import Dict
# 8 
# 9 from app.enums.fsm_enums import DECISION_TYPE, STATE_TYPE
# 10 from app.enums.logging_enums import RunContext
# 11 from app.enums.system_enums import SYSTEM
# 12 from app.providers.fsm_provider_base import FSMProviderBase
# 13 from app.db.schemas import ControllerOutputSchema
# 14 from app.utilities.extract_base_filename import extract_base_filename
# 15 from app.utilities.select_best_file_by_score import select_best_file_by_score
# 16 
# 17 
# 18 class ControllerProviderBase(FSMProviderBase):
# 19     def __init__(
# 20         self,
# 21         config=None,
# 22         system_providers=None,
# 23         context_provider=None,
# 24         score_provider=None,
# 25         tool_providers=None,
# 26         context: RunContext = None,
# 27         **kwargs
# 28     ):
# 29         super().__init__(config=config, context=context, **kwargs)
# 30         self._systems = system_providers or {}
# 31         self.context_provider = context_provider
# 32         self.score_provider = score_provider
# 33         self.tool_providers = tool_providers or []
# 34         self._generated_files = []
# 35 
# 36     def _run_provider(self, input: dict) -> ControllerOutputSchema:
# 37         session_id = input.get("session_id")
# 38         max_steps = input.get("max_steps", 10)
# 39 
# 40         incoming_file = input.get("file_path") or input.get("file_name") or input.get("before")
# 41         if not incoming_file:
# 42             raise ValueError("❌ ControllerProvider requires 'file_path', 'file_name', or 'before' in input")
# 43 
# 44         self.incoming_file = incoming_file
# 45         src = Path(incoming_file).resolve()
# 46         timestamp = datetime.now().strftime('%H%M%S%f')[:10]
# 47         working_dir = Path("working_files").resolve()
# 48         working_dir.mkdir(parents=True, exist_ok=True)
# 49         root = extract_base_filename(src)
# 50         self.working_file = working_dir / f"{root}__ctrl_{timestamp}{src.suffix}"
# 51         shutil.copy(src, self.working_file)
# 52         self._generated_files.append(self.working_file)
# 53 
# 54         input_path = Path(incoming_file)
# 55         try:
# 56             relative_path = str(input_path.relative_to(Path.cwd()))
# 57         except ValueError:
# 58             relative_path = str(input_path)
# 59 
# 60         state = {
# 61             "state": SYSTEM.START,
# 62             "file_path": str(self.working_file),
# 63             "session_id": session_id,
# 64             "system": input.get("system", "unknown"),
# 65             "reason": input.get("reason", SYSTEM.START.value),
# 66             "steps": input.get("steps", 0),
# 67             "retry_count": input.get("retry_count", 0),
# 68             "_last_state": input.get("_last_state", SYSTEM.START),
# 69             "decision": DECISION_TYPE.UNKNOWN,
# 70             "original_file": relative_path,
# 71             "run_id": self._run_id,
# 72         }
# 73 
# 74         step_count = 0
# 75 
# 76         while True:
# 77             current = state.get("state")
# 78 
# 79             if step_count >= max_steps:
# 80                 transition = self.transition(state, output)
# 81                 state.update({
# 82                     **state,
# 83                     **transition,
# 84                     "state": SYSTEM.END,
# 85                     "state_type": STATE_TYPE.END,
# 86                     "reason": f"Max steps ({max_steps}) reached"
# 87                 })
# 88                 return ControllerOutputSchema(
# 89                     state=SYSTEM.END,
# 90                     previous_state=current,
# 91                     state_type=SYSTEM.END,
# 92                     decision=DECISION_TYPE.REJECTED,
# 93                     steps=step_count,
# 94                     max_steps=max_steps,
# 95                     summary=f"Max steps ({max_steps}) reached",
# 96                     output=state,
# 97                     provider_name=self._config.name,
# 98                 )
# 99 
# 100             if current == SYSTEM.END:
# 101                 best_file = select_best_file_by_score(
# 102                     file_a=state["file_path"],
# 103                     file_b=self.incoming_file,
# 104                     score_provider=self.score_provider,
# 105                     context=self._context
# 106                 )
# 107 
# 108                 temp_path = Path("working_files") / f"temp_ctrl_{datetime.now().strftime('%H%M%S%f')[:10]}.py"
# 109                 shutil.copy(Path(best_file), temp_path)
# 110                 state["file_path"] = str(temp_path)
# 111 
# 112                 for f in Path("working_files").glob("temp_ctrl_*.py"):
# 113                     if f.resolve() != temp_path.resolve():
# 114                         try:
# 115                             f.unlink()
# 116                         except Exception:
# 117                             pass
# 118 
# 119                 for f in Path("working_files").glob("*_stripped.py"):
# 120                     try:
# 121                         f.unlink()
# 122                     except Exception:
# 123                         pass
# 124 
# 125                 for path in self._generated_files:
# 126                     if path.exists():
# 127                         try:
# 128                             path.unlink()
# 129                         except Exception:
# 130                             pass
# 131 
# 132                 return ControllerOutputSchema(
# 133                     state=SYSTEM.END,
# 134                     previous_state=state.get("_last_state", SYSTEM.START),
# 135                     state_type=STATE_TYPE.END,
# 136                     decision=state.get("decision", DECISION_TYPE.UNKNOWN),
# 137                     steps=step_count,
# 138                     max_steps=max_steps,
# 139                     summary=state.get("summary", "Completed"),
# 140                     output=state,
# 141                     provider_name=self._config.name,
# 142                 )
# 143 
# 144             step_count += 1
# 145 
# 146             if current == SYSTEM.START:
# 147                 transition = self.transition(state, None)
# 148                 state.update(transition)
# 149                 state["_last_state"] = SYSTEM.START
# 150                 continue
# 151 
# 152             provider = self._systems.get(current.value)
# 153             if not provider:
# 154                 raise ValueError(f"No system provider registered for state: {current.value}")
# 155 
# 156             provider_input = {k: v for k, v in state.items() if k != "state"}
# 157             output = provider.run(input=provider_input, context=self.fork_context())
# 158 
# 159             flat_output = output.model_dump(exclude={"output"}) if hasattr(output, "model_dump") else dict(output)
# 160             promoted_path = Path("working_files") / f"temp_ctrl_{datetime.now().strftime('%H%M%S%f')[:10]}.py"
# 161             if "file_path" in output.output:
# 162                 final_path = Path(output.output["file_path"]).resolve()
# 163                 shutil.copy(final_path, promoted_path)
# 164                 self._generated_files.append(promoted_path)
# 165                 state["file_path"] = str(promoted_path)
# 166 
# 167             if hasattr(output, "decision") and output.decision is not None:
# 168                 state["decision"] = output.decision
# 169 
# 170             transition_result = self.transition(state, output)
# 171             transition_result.pop("file_path", None)
# 172 
# 173             transition_metadata = transition_result.get("transition_metadata", {}) or {}
# 174             transition_metadata.update({
# 175                 "agent_output": flat_output.get("output", {}),
# 176                 "file_path": flat_output.get("file_path"),
# 177             })
# 178             transition_result["transition_metadata"] = transition_metadata
# 179 
# 180             raw_state = transition_result.get("state", current)
# 181             state_enum = raw_state if isinstance(raw_state, SYSTEM) else SYSTEM(raw_state)
# 182 
# 183             state = {
# 184                 **state,
# 185                 **transition_result,
# 186                 "state": state_enum,
# 187                 "_last_state": current,
# 188                 "state_output": flat_output,
# 189             }
# 190 
# 191 
# 192     @abstractmethod
# 193     def _transition(self, state: dict, output: dict | None) -> dict:
# 194         ...


# providers\fsm_provider_base.py

# 1 from __future__ import annotations
# 2 
# 3 from abc import abstractmethod
# 4 from datetime import datetime, timezone
# 5 from typing import Optional
# 6 
# 7 from app.enums.fsm_enums import TRANSITION_REASON_TYPE, DECISION_TYPE
# 8 from app.enums.logging_enums import LOG_TYPE, PROVIDER_TYPE, RunContext
# 9 from app.db.schemas import StateTransitionLogSchema
# 10 from app.providers.base_provider import BaseProvider
# 11 
# 12 
# 13 class FSMProviderBase(BaseProvider):
# 14     """
# 15     Base class for FSM-enabled providers (Program, Controller, System, State).
# 16     Provides a shared `transition` method and logging of state transitions.
# 17     """
# 18 
# 19     def __init__(
# 20         self,
# 21         config=None,
# 22         context: RunContext = None,
# 23         **kwargs
# 24     ) -> None:
# 25         super().__init__(
# 26             config=config,
# 27             context=context,
# 28             **kwargs
# 29         )
# 30 
# 31     def transition(self, state: dict, result: dict | None) -> dict:
# 32         next_state = self._transition(state, result)
# 33 
# 34         required_keys = {"state", "reason"}
# 35         missing_keys = required_keys - next_state.keys()
# 36         if missing_keys:
# 37             raise ValueError(f"❌ Transition result missing keys: {missing_keys}\nReturned: {next_state}")
# 38         state["steps"] = state.get("steps", 0) + 1
# 39 
# 40         #  Ensure original_file is carried forward
# 41         original_file = state.get("original_file")
# 42 
# 43         #  Initialize or update transition_metadata
# 44         transition_metadata = next_state.get("transition_metadata", {}) or {}
# 45         transition_metadata["original_file"] = original_file
# 46 
# 47         #  Optional: Add run_id to the metadata as well
# 48         transition_metadata["run_id"] = state.get("run_id")
# 49 
# 50         #  Optional: agent_output propagation (if result has a dumpable output)
# 51         if isinstance(result, dict):
# 52             transition_metadata["agent_output"] = result.get("output")
# 53             transition_metadata["file_path"] = result.get("file_path")
# 54 
# 55         next_state["transition_metadata"] = transition_metadata
# 56 
# 57         is_terminal = next_state.get("state_type") == "end" or next_state.get("state") == "end"
# 58 
# 59         decision = getattr(result, "decision", DECISION_TYPE.UNKNOWN)
# 60         #  Log the state transition with relevant details
# 61         self.logger.write(
# 62             LOG_TYPE.STATE_TRANSITION,
# 63             StateTransitionLogSchema(
# 64                 session_id=self._session_id,
# 65                 file_log_id=self._file_log_id, 
# 66                 entity_type=self._infer_provider_type(),
# 67                 entity_id=self._config.id,
# 68                 from_state=state.get("state"),
# 69                 to_state=next_state.get("state", "unknown"),
# 70                 reason=TRANSITION_REASON_TYPE(next_state["reason"]),
# 71                 decision=decision,
# 72                 triggered_by=self._config.name,
# 73                 step=state["steps"],
# 74                 transition_metadata=transition_metadata,
# 75                 timestamp=datetime.now(timezone.utc),
# 76                 run_id=self._run_id,
# 77                 called_by_type=self._called_by_type if self._called_by_type else None,
# 78                 called_by_id=self._called_by_id,
# 79                 parent_id=self._context.parent_id if self._context else None,
# 80                 execution_chain=self._context.execution_chain[:] if self._context else [],
# 81             ),
# 82         )
# 83         return next_state
# 84 
# 85 
# 86     @abstractmethod
# 87     def _transition(self, state: dict, result: dict | None) -> dict:
# 88         raise NotImplementedError


# providers\program_provider_base.py

# 1 from __future__ import annotations
# 2 from abc import abstractmethod
# 3 from copy import deepcopy
# 4 from datetime import datetime
# 5 from pathlib import Path
# 6 import shutil
# 7 from typing import Dict
# 8 
# 9 from app.enums.controller_enums import CONTROLLER
# 10 from app.enums.fsm_enums import STATE_TYPE, DECISION_TYPE
# 11 from app.enums.logging_enums import RunContext, LOG_TYPE
# 12 from app.providers.fsm_provider_base import FSMProviderBase
# 13 from app.db.schemas import ProgramOutputSchema, FileLogSchema
# 14 from app.utilities.extract_base_filename import extract_base_filename
# 15 from app.utilities.select_best_file_by_score import select_best_file_by_score
# 16 from app.utilities.metadata.logging.logging_provider import LoggingProvider
# 17 
# 18 class ProgramProviderBase(FSMProviderBase):
# 19     def __init__(
# 20         self,
# 21         config=None,
# 22         controller_providers: Dict[str, object] = None,
# 23         context_provider=None,
# 24         score_provider=None,
# 25         tool_providers=None,
# 26         context: RunContext = None,
# 27     ):
# 28         super().__init__(
# 29             config=config,
# 30             context=context
# 31         )
# 32         self._states = controller_providers or {}
# 33         self.context_provider = context_provider
# 34         self.score_provider = score_provider
# 35         self.tool_providers = tool_providers or []
# 36         self._session_id = context.session_id
# 37         self._file_log_id = context.file_log_id
# 38         self._generated_files: list[Path] = []
# 39 
# 40     def _run_provider(self, input: dict) -> ProgramOutputSchema:
# 41         session_id = self._session_id
# 42         max_steps = input.get("max_steps", 20)
# 43 
# 44         incoming_file = input.get("file_path")
# 45         self.incoming_file = incoming_file
# 46 
# 47         output_path = input.get("output_path", "working_files")
# 48 
# 49         src = Path(incoming_file).resolve()
# 50         root = extract_base_filename(src)
# 51         timestamp = datetime.now().strftime('%H%M%S%f')[:10]
# 52         working_dir = Path("working_files").resolve()
# 53         working_dir.mkdir(parents=True, exist_ok=True)
# 54         self.working_file = working_dir / f"{root}.__prog_{timestamp}{src.suffix}"
# 55         shutil.copy(src, self.working_file)
# 56         self._generated_files.append(self.working_file)
# 57 
# 58         #  🔹 Log file and get file_log_id
# 59         original_path = str(src)
# 60         file_name = src.name
# 61         length_bytes = src.stat().st_size
# 62 
# 63         file_log = FileLogSchema(
# 64             session_id=session_id,
# 65             file_name=file_name,
# 66             original_path=original_path,
# 67             length_bytes=length_bytes
# 68         )
# 69         self._file_log_id = LoggingProvider().write(LOG_TYPE.FILE, file_log)
# 70         self._context.file_log_id = self._file_log_id
# 71         self.context = self._context
# 72 
# 73         input_path = Path(incoming_file)
# 74         try:
# 75             relative_path = str(input_path.relative_to(Path.cwd()))
# 76         except ValueError:
# 77             relative_path = str(input_path)
# 78 
# 79         #  🔹 Build initial FSM state
# 80         state = {
# 81             "state": CONTROLLER.START,
# 82             "file_path": str(self.working_file),
# 83             "session_id": session_id,
# 84             "file_log_id": self._file_log_id,
# 85             "system": input.get("system", "unknown"),
# 86             "reason": input.get("reason", CONTROLLER.START.value),
# 87             "steps": input.get("steps", 0),
# 88             "retry_count": input.get("retry_count", 0),
# 89             "_last_state": input.get("_last_state", CONTROLLER.START),
# 90             "decision": DECISION_TYPE.UNKNOWN,
# 91             "original_file": relative_path,
# 92             "run_id": self._run_id,
# 93         }
# 94 
# 95         step_count = 0
# 96 
# 97         while True:
# 98             current = state.get("state")
# 99 
# 100             if step_count >= max_steps:
# 101                 transition = self.transition(state, output)
# 102                 state.update({
# 103                     **state,
# 104                     **transition,
# 105                     "state": CONTROLLER.END,
# 106                     "state_type": STATE_TYPE.END,
# 107                     "reason": f"Max steps ({max_steps}) reached"
# 108                 })
# 109                 return ProgramOutputSchema(
# 110                     state=CONTROLLER.END,
# 111                     previous_state=current,
# 112                     state_type=STATE_TYPE.END,
# 113                     decision=DECISION_TYPE.UNKNOWN,
# 114                     steps=step_count,
# 115                     max_steps=max_steps,
# 116                     summary=f"Max steps ({max_steps}) reached",
# 117                     output=state,
# 118                     provider_name=self._config.name
# 119                 )
# 120 
# 121             if current == CONTROLLER.END:
# 122                 final_decision = state.get("decision")
# 123                 nested = state.get("output") or {}
# 124                 if isinstance(nested, dict):
# 125                     final_decision = final_decision or nested.get("decision")
# 126 
# 127                 best_file = select_best_file_by_score(
# 128                     file_a=state.get("file_path"),
# 129                     file_b=self.incoming_file,
# 130                     score_provider=self.score_provider,
# 131                     context=self._context
# 132                 )
# 133 
# 134                 final_path = Path(output_path) / Path(self.incoming_file).name
# 135                 shutil.copy(Path(best_file).resolve(), final_path)
# 136                 state["file_path"] = str(final_path)
# 137                 state["decision"] = final_decision
# 138 
# 139                 for f in Path("working_files").glob("temp_*.py"):
# 140                     if f.resolve() != final_path.resolve():
# 141                         try:
# 142                             f.unlink()
# 143                         except Exception:
# 144                             pass
# 145 
# 146                 for f in Path("working_files").glob("*_stripped.py"):
# 147                     try:
# 148                         f.unlink()
# 149                     except Exception:
# 150                         pass
# 151 
# 152                 for path in self._generated_files:
# 153                     if path.exists():
# 154                         try:
# 155                             path.unlink()
# 156                         except Exception:
# 157                             pass
# 158 
# 159                 return ProgramOutputSchema(
# 160                     state=CONTROLLER.END,
# 161                     previous_state=state.get("_last_state", CONTROLLER.START),
# 162                     state_type=STATE_TYPE.END,
# 163                     decision=final_decision or state.get("decision", DECISION_TYPE.UNKNOWN),
# 164                     steps=step_count,
# 165                     max_steps=max_steps,
# 166                     summary=state.get("reason", "Completed"),
# 167                     output=state,
# 168                     provider_name=self._config.name
# 169                 )
# 170 
# 171             step_count += 1
# 172 
# 173             if current == CONTROLLER.START:
# 174                 transition = self.transition(state, None)
# 175                 state.update(transition)
# 176                 state["_last_state"] = CONTROLLER.START
# 177                 continue
# 178 
# 179             provider = self._states.get(current)
# 180             if not provider:
# 181                 raise ValueError(f"No controller provider registered for state: {current}")
# 182 
# 183             provider_input = {k: v for k, v in state.items() if k != "state"}
# 184             output = provider.run(input=provider_input, context=self.fork_context())
# 185 
# 186             flat_output = output.model_dump(exclude={"output"}) if hasattr(output, "model_dump") else dict(output)
# 187             promoted_path = Path("working_files") / f"temp_prog_{datetime.now().strftime('%H%M%S%f')[:10]}.py"
# 188             if "file_path" in output.output:
# 189                 src_path = Path(output.output["file_path"]).resolve()
# 190                 shutil.copy(src_path, promoted_path)
# 191                 self._generated_files.append(promoted_path)
# 192                 state["file_path"] = str(promoted_path)
# 193 
# 194             transition_result = self.transition(state, output)
# 195             transition_result.pop("file_path", None)
# 196 
# 197             transition_metadata = transition_result.get("transition_metadata", {}) or {}
# 198             transition_metadata.update({
# 199                 "agent_output": flat_output.get("output", {}),
# 200                 "file_path": flat_output.get("file_path"),
# 201             })
# 202             transition_result["transition_metadata"] = transition_metadata
# 203 
# 204             state = {
# 205                 **state,
# 206                 **transition_result,
# 207                 "state": CONTROLLER(transition_result.get("state", current)),
# 208                 "_last_state": current,
# 209                 "state_output": flat_output,
# 210             }
# 211 
# 212     @abstractmethod
# 213     def _transition(self, state: dict, output: dict | None) -> dict:
# 214         ...


# providers\prompt_provider_base.py

# 1 from __future__ import annotations
# 2 from abc import abstractmethod
# 3 from copy import deepcopy
# 4 from sqlalchemy.orm import Session
# 5 from pathlib import Path
# 6 
# 7 from app.db.models import AgentPrompt, SystemPrompt
# 8 from app.db.schemas import PromptOutputSchema
# 9 from app.enums.logging_enums import RunContext
# 10 from app.providers.base_provider import BaseProvider
# 11 from app.factories.context_provider_factory import ContextProviderFactory
# 12 
# 13 
# 14 class PromptProviderBase(BaseProvider):
# 15     def __init__(
# 16         self,
# 17         config=None,
# 18         agent_text=None,
# 19         system_text=None,
# 20         context_provider=None,
# 21         context: RunContext = None,
# 22         **kwargs
# 23     ):
# 24         super().__init__(config=config, context=context, **kwargs)
# 25         self.agent_text = agent_text
# 26         self.system_text = system_text
# 27         self._context_provider = context_provider
# 28 
# 29     def _run_provider(self, input: dict) -> PromptOutputSchema:
# 30         return self._run(input=input, context=self.fork_context())
# 31 
# 32     @abstractmethod
# 33     def _run(self, input: dict, context: RunContext | None = None) -> PromptOutputSchema:
# 34         raise NotImplementedError


# providers\score_provider_base.py

# 1 from __future__ import annotations
# 2 from abc import abstractmethod
# 3 from copy import deepcopy
# 4 from app.enums.logging_enums import RunContext
# 5 from app.providers.base_provider import BaseProvider
# 6 from typing import TYPE_CHECKING
# 7 if TYPE_CHECKING:
# 8     from app.providers.context_provider_base import ContextProviderBase
# 9     from app.providers.tool_provider_base import ToolProviderBase
# 10 
# 11 from app.db.schemas import ScoreOutputSchema
# 12 
# 13 class ScoreProviderBase(BaseProvider):
# 14     def __init__(
# 15         self,
# 16         config=None,
# 17         context_provider=None,
# 18         tool_providers=None,
# 19         context: RunContext = None,
# 20         **kwargs
# 21     ):
# 22         super().__init__(config=config, context=context, **kwargs)
# 23         self._context_provider = context_provider
# 24         self._tool_providers = tool_providers or []
# 25 
# 26     def _run_provider(self, input: dict) -> ScoreOutputSchema:
# 27         caller = self._context.called_by_type.name if self._context and self._context.called_by_type else "UNKNOWN"
# 28         return self._run(input=input, context=self.fork_context())
# 29 
# 30     def set_context_provider(self, provider: ContextProviderBase) -> None:
# 31         self.context_provider = provider
# 32 
# 33     def set_tool_provider(self, provider: ToolProviderBase) -> None:
# 34         if not hasattr(self, "tool_providers"):
# 35             self.tool_providers = []
# 36         self.tool_providers.append(provider)
# 37 
# 38     @abstractmethod
# 39     def _run(self, input: dict, context: RunContext | None = None) -> ScoreOutputSchema:
# 40         raise NotImplementedError


# providers\state_provider_base.py

# 1 from __future__ import annotations
# 2 from abc import abstractmethod
# 3 from copy import deepcopy
# 4 from datetime import datetime
# 5 from pathlib import Path
# 6 import shutil
# 7 from typing import Dict
# 8 
# 9 from app.enums.fsm_enums import STATE_TYPE, DECISION_TYPE, TRANSITION_REASON_TYPE
# 10 from app.enums.logging_enums import RunContext
# 11 from app.enums.state_enums import STATE
# 12 from app.enums.agent_enums import AGENT
# 13 from app.providers.fsm_provider_base import FSMProviderBase
# 14 from app.db.schemas import StateOutputSchema
# 15 from app.utilities.extract_base_filename import extract_base_filename
# 16 from app.utilities.select_best_file_by_score import select_best_file_by_score
# 17 
# 18 
# 19 class StateProviderBase(FSMProviderBase):
# 20     def __init__(
# 21         self,
# 22         config=None,
# 23         agent_providers=None,
# 24         context_provider=None,
# 25         score_provider=None,
# 26         tool_providers=None,
# 27         context: RunContext = None,
# 28         **kwargs
# 29     ):
# 30         super().__init__(config=config, context=context, **kwargs)
# 31         self._agents = agent_providers or {}
# 32         self.context_provider = context_provider
# 33         self.score_provider = score_provider
# 34         self.tool_providers = tool_providers or []
# 35         self._generated_files = []
# 36 
# 37     def _run_provider(self, input: dict) -> StateOutputSchema:
# 38         session_id = input.get("session_id")
# 39         max_steps = input.get("max_steps", 10)
# 40 
# 41         incoming_file = input.get("file_path") or input.get("file_name") or input.get("before")
# 42         if not incoming_file:
# 43             raise ValueError("❌ StateProvider requires 'file_path', 'file_name', or 'before' in input")
# 44 
# 45         self.incoming_file = incoming_file
# 46         src = Path(incoming_file).resolve()
# 47         timestamp = datetime.now().strftime('%H%M%S%f')[:10]
# 48         working_dir = Path("working_files").resolve()
# 49         working_dir.mkdir(parents=True, exist_ok=True)
# 50         root = extract_base_filename(src)
# 51         self.working_file = working_dir / f"{root}__state_{timestamp}{src.suffix}"
# 52         shutil.copy(src, self.working_file)
# 53         self._generated_files.append(self.working_file)
# 54 
# 55         input_path = Path(incoming_file)
# 56         try:
# 57             relative_path = str(input_path.relative_to(Path.cwd()))
# 58         except ValueError:
# 59             relative_path = str(input_path)
# 60 
# 61         state = {
# 62             "state": AGENT.START,
# 63             "file_path": str(self.working_file),
# 64             "session_id": session_id,
# 65             "system": input.get("system", "unknown"),
# 66             "reason": input.get("reason", AGENT.START.value),
# 67             "steps": input.get("steps", 0),
# 68             "retry_count": input.get("retry_count", 0),
# 69             "_last_state": input.get("_last_state", AGENT.START),
# 70             "decision": DECISION_TYPE.UNKNOWN,
# 71             "original_file": relative_path,
# 72             "run_id": self._run_id,
# 73         }
# 74 
# 75         step_count = 0
# 76 
# 77         while True:
# 78             current = state["state"]
# 79 
# 80             if step_count >= max_steps:
# 81                 transition = self.transition(state, None)
# 82                 state.update({
# 83                     **state,
# 84                     **transition,
# 85                     "state": STATE.END,
# 86                     "state_type": STATE_TYPE.END,
# 87                     "reason": TRANSITION_REASON_TYPE.UNSUCCESSFUL
# 88                 })
# 89                 return StateOutputSchema(
# 90                     state=AGENT.END,
# 91                     previous_state=current,
# 92                     state_type=STATE_TYPE.END,
# 93                     decision=DECISION_TYPE.REJECTED,
# 94                     steps=step_count,
# 95                     max_steps=max_steps,
# 96                     summary=f"Max steps ({max_steps}) reached",
# 97                     output=state,
# 98                     provider_name=self._config.name,
# 99                 )
# 100 
# 101             if current == STATE.END:
# 102                 if self.score_provider:
# 103                     best_file = select_best_file_by_score(
# 104                         file_a=state["file_path"],
# 105                         file_b=self.incoming_file,
# 106                         score_provider=self.score_provider,
# 107                         context=self._context
# 108                     )
# 109 
# 110                     temp_path = Path("working_files") / f"temp_state_{datetime.now().strftime('%H%M%S%f')[:10]}.py"
# 111                     shutil.copy(Path(best_file), temp_path)
# 112                     state["file_path"] = str(temp_path)
# 113 
# 114                     for f in Path("working_files").glob("temp_state_*.py"):
# 115                         if f.resolve() != temp_path.resolve():
# 116                             try:
# 117                                 f.unlink()
# 118                             except Exception:
# 119                                 pass
# 120 
# 121                     for f in Path("working_files").glob("temp_agent_*.py"):
# 122                         if f.resolve() != temp_path.resolve():
# 123                             try:
# 124                                 f.unlink()
# 125                             except Exception:
# 126                                 pass
# 127 
# 128                     for f in Path("working_files").glob("*_stripped.py"):
# 129                         try:
# 130                             f.unlink()
# 131                         except Exception:
# 132                             pass
# 133 
# 134                     ctx = self._context or RunContext()
# 135 
# 136                 for path in self._generated_files:
# 137                     if path.exists():
# 138                         try:
# 139                             path.unlink()
# 140                         except Exception:
# 141                             pass
# 142 
# 143                 return StateOutputSchema(
# 144                     state=AGENT.END,
# 145                     previous_state=state.get("_last_state", AGENT.START),
# 146                     state_type=STATE_TYPE.END,
# 147                     decision=state.get("decision", DECISION_TYPE.UNKNOWN),
# 148                     steps=step_count,
# 149                     max_steps=max_steps,
# 150                     summary=state.get("summary", "Completed"),
# 151                     output=state,
# 152                     provider_name=self._config.name,
# 153                 )
# 154 
# 155             step_count += 1
# 156 
# 157             if current == AGENT.START:
# 158                 transition = self.transition(state, None)
# 159                 state.update(transition)
# 160                 state["_last_state"] = AGENT.START
# 161                 continue
# 162 
# 163             provider = self._agents.get(current.value)
# 164 
# 165             provider_input = {k: v for k, v in state.items() if k != "state"}
# 166             forked = self.fork_context()
# 167 
# 168             output = provider.run(input=provider_input, context=forked)
# 169 
# 170             transition_result = self.transition(state, output)
# 171 
# 172             flat_output = output.model_dump(exclude={"output"}) if hasattr(output, "model_dump") else dict(output or {})
# 173             agent_file_path = flat_output.get("file_path")
# 174 
# 175             if agent_file_path:
# 176                 src_path = Path(agent_file_path).resolve()
# 177                 if src_path.exists():
# 178                     promoted_path = Path("working_files") / f"temp_state_{datetime.now().strftime('%H%M%S%f')[:10]}.py"
# 179                     shutil.copy(src_path, promoted_path)
# 180                     self._generated_files.append(promoted_path)
# 181                     state["file_path"] = str(promoted_path)
# 182 
# 183             if hasattr(output, "decision") and output.decision:
# 184                 state["decision"] = output.decision
# 185 
# 186             transition_metadata = transition_result.get("transition_metadata", {}) or {}
# 187             transition_metadata.update({
# 188                 "agent_output": flat_output.get("output", {}),
# 189                 "file_path": flat_output.get("file_path"),
# 190             })
# 191             transition_result["transition_metadata"] = transition_metadata
# 192 
# 193             raw_state = transition_result.get("state", current)
# 194             state_enum = raw_state if isinstance(raw_state, AGENT) else AGENT(raw_state)
# 195 
# 196             transition_result.pop("file_path", None)
# 197 
# 198             state = {
# 199                 **state,
# 200                 **transition_result,
# 201                 "state": state_enum,
# 202                 "_last_state": current,
# 203                 "state_output": flat_output,
# 204             }
# 205 
# 206     @abstractmethod
# 207     def _transition(self, state: dict, output: dict | None) -> dict:
# 208         ...


# providers\system_provider_base.py

# 1 from __future__ import annotations
# 2 from abc import abstractmethod
# 3 from copy import deepcopy
# 4 from datetime import datetime
# 5 from pathlib import Path
# 6 import shutil
# 7 from typing import Dict
# 8 
# 9 from app.enums.fsm_enums import STATE_TYPE, DECISION_TYPE, TRANSITION_REASON_TYPE
# 10 from app.enums.logging_enums import RunContext
# 11 from app.enums.state_enums import STATE
# 12 from app.enums.system_enums import SYSTEM
# 13 from app.providers.fsm_provider_base import FSMProviderBase
# 14 from app.db.schemas import FSMOutputSchema, SystemOutputSchema
# 15 from app.utilities.extract_base_filename import extract_base_filename
# 16 from app.utilities.select_best_file_by_score import select_best_file_by_score
# 17 
# 18 
# 19 class SystemProviderBase(FSMProviderBase):
# 20     def __init__(
# 21         self,
# 22         config=None,
# 23         state_providers=None,
# 24         context_provider=None,
# 25         score_provider=None,
# 26         tool_providers=None,
# 27         context: RunContext = None,
# 28         **kwargs
# 29     ):
# 30         super().__init__(config=config, context=context, **kwargs)
# 31         self._states= state_providers or {}
# 32         self.context_provider = context_provider
# 33         self.score_provider = score_provider
# 34         self.tool_providers = tool_providers or []
# 35         self._generated_files = []
# 36 
# 37     def _run_provider(self, input: dict) -> SystemOutputSchema:
# 38         session_id = input.get("session_id")
# 39         max_steps = input.get("max_steps", 7)
# 40 
# 41         incoming_file = input.get("file_path") or input.get("file_name") or input.get("before")
# 42         if not incoming_file:
# 43             raise ValueError("❌ SystemProvider requires 'file_path', 'file_name', or 'before' in input")
# 44 
# 45         self.incoming_file = incoming_file
# 46         src = Path(incoming_file).resolve()
# 47         timestamp = datetime.now().strftime('%H%M%S%f')[:10]
# 48         working_dir = Path("working_files").resolve()
# 49         working_dir.mkdir(parents=True, exist_ok=True)
# 50         root = extract_base_filename(src)
# 51         self.working_file = working_dir / f"{root}.__sys_{timestamp}{src.suffix}"
# 52         shutil.copy(src, self.working_file)
# 53         self._generated_files.append(self.working_file)
# 54 
# 55         input_path = Path(incoming_file)
# 56         try:
# 57             relative_path = str(input_path.relative_to(Path.cwd()))
# 58         except ValueError:
# 59             relative_path = str(input_path)
# 60 
# 61         state = {
# 62             "state": STATE.START,
# 63             "file_path": str(self.working_file),
# 64             "session_id": session_id,
# 65             "system": getattr(self._config, "system", SYSTEM.UNKNOWN),
# 66             "reason": input.get("reason", STATE.START.value),
# 67             "steps": input.get("steps", 0),
# 68             "retry_count": input.get("retry_count", 0),
# 69             "_last_state": input.get("_last_state", STATE.START),
# 70             "decision": DECISION_TYPE.UNKNOWN,
# 71             "original_file": relative_path,
# 72             "run_id": self._run_id,
# 73         }
# 74 
# 75         step_count = 0
# 76 
# 77         while True:
# 78             current = state["state"]
# 79 
# 80             if step_count >= max_steps:
# 81                 transition = self.transition(state, output)
# 82                 state.update({
# 83                     **state,
# 84                     **transition,
# 85                     "state": STATE.END,
# 86                     "state_type": STATE_TYPE.END,
# 87                     "reason": TRANSITION_REASON_TYPE.UNSUCCESSFUL
# 88                 })
# 89                 return SystemOutputSchema(
# 90                     state=STATE.END,
# 91                     previous_state=current,
# 92                     state_type=STATE_TYPE.END,
# 93                     decision=DECISION_TYPE.REJECTED,
# 94                     steps=step_count,
# 95                     max_steps=max_steps,
# 96                     summary=f"Max steps ({max_steps}) reached",
# 97                     output=state,
# 98                     provider_name=self._config.name
# 99                 )
# 100 
# 101             if current == STATE.END:
# 102                 transition = self.transition(state, output)
# 103                 state.update(transition)
# 104 
# 105                 best_file = select_best_file_by_score(
# 106                     file_a=state["file_path"],
# 107                     file_b=self.incoming_file,
# 108                     score_provider=self.score_provider,
# 109                     context=self._context
# 110                 )
# 111 
# 112                 temp_path = Path("working_files") / f"temp_sys_{datetime.now().strftime('%H%M%S%f')[:10]}.py"
# 113                 shutil.copy(Path(best_file), temp_path)
# 114                 state["file_path"] = str(temp_path)
# 115 
# 116                 #  Clean up old temporary files
# 117                 for f in Path("working_files").glob("temp_sys_*.py"):
# 118                     if f.resolve() != temp_path.resolve():
# 119                         try:
# 120                             f.unlink()
# 121                         except Exception:
# 122                             pass
# 123 
# 124                 for f in Path("working_files").glob("*_stripped.py"):
# 125                     try:
# 126                         f.unlink()
# 127                     except Exception:
# 128                         pass
# 129 
# 130                 for path in self._generated_files:
# 131                     if path.exists():
# 132                         try:
# 133                             path.unlink()
# 134                         except Exception:
# 135                             pass
# 136 
# 137                 return SystemOutputSchema(
# 138                     state=STATE.END,
# 139                     previous_state=state.get("_last_state", STATE.START),
# 140                     state_type=STATE_TYPE.END,
# 141                     decision=state.get("decision", DECISION_TYPE.UNKNOWN),
# 142                     steps=step_count,
# 143                     max_steps=max_steps,
# 144                     summary=state.get("summary", "Completed"),
# 145                     output=state,
# 146                     provider_name=self._config.name
# 147                 )
# 148 
# 149             step_count += 1
# 150 
# 151             if current == STATE.START:
# 152                 transition = self.transition(state, None)
# 153                 state.update(transition)
# 154                 state["_last_state"] = STATE.START
# 155                 continue
# 156 
# 157             provider = self._states.get(current.value)
# 158             if not provider:
# 159                 raise ValueError(f"No state provider registered for state: {current.value}")
# 160 
# 161             provider_input = {k: v for k, v in state.items() if k != "state"}
# 162             output = provider.run(input=provider_input, context=self.fork_context())
# 163 
# 164 
# 165             new_file_path = output.output.get("file_path")
# 166             if new_file_path:
# 167                 state["file_path"] = str(new_file_path)
# 168 
# 169             if hasattr(output, "decision") and output.decision:
# 170                 state["decision"] = output.decision
# 171 
# 172             transition_result = self.transition(state, output)
# 173             flat_output = output.model_dump(exclude={"output"}) if hasattr(output, "model_dump") else dict(output)
# 174 
# 175             transition_metadata = transition_result.get("transition_metadata", {}) or {}
# 176             transition_metadata.update({
# 177                 "agent_output": flat_output.get("output", {}),
# 178                 "file_path": flat_output.get("file_path"),
# 179             })
# 180             transition_result["transition_metadata"] = transition_metadata
# 181 
# 182             raw_state = transition_result.get("state", current)
# 183             state_enum = raw_state if isinstance(raw_state, STATE) else STATE(raw_state)
# 184 
# 185             transition_result.pop("file_path", None)
# 186 
# 187             state = {
# 188                 **state,
# 189                 **transition_result,
# 190                 "state": state_enum,
# 191                 "_last_state": current,
# 192                 "state_output": flat_output,
# 193             }
# 194 
# 195 
# 196 
# 197     @abstractmethod
# 198     def _transition(self, state: dict, output: dict | None) -> dict:
# 199         ...


# providers\tool_provider_base.py

# 1 from __future__ import annotations
# 2 from abc import abstractmethod
# 3 from copy import deepcopy
# 4 from typing import TYPE_CHECKING
# 5 
# 6 from app.enums.logging_enums import RunContext
# 7 from app.providers.base_provider import BaseProvider
# 8 from app.db.schemas import ToolOutputSchema
# 9 
# 10 if TYPE_CHECKING:
# 11     from app.providers.context_provider_base import ContextProviderBase
# 12     from app.providers.score_provider_base import ScoreProviderBase
# 13 
# 14 
# 15 class ToolProviderBase(BaseProvider):
# 16     def __init__(
# 17         self,
# 18         config=None,
# 19         context_provider: ContextProviderBase | None = None,
# 20         score_provider: ScoreProviderBase | None = None,
# 21         context: RunContext = None,
# 22         **kwargs
# 23     ):
# 24         super().__init__(config=config, context=context, **kwargs)
# 25         self._context_provider = context_provider
# 26         self._score_provider = score_provider
# 27 
# 28     def _run_provider(self, input: dict) -> ToolOutputSchema:
# 29         #  ✅ Explicitly pass self._context
# 30         output = self._run(input=input, context=self.fork_context())
# 31 
# 32         if not isinstance(output, ToolOutputSchema):
# 33             raise TypeError(f"Expected ToolOutputSchema, got {type(output).__name__}")
# 34         if output.return_code is None:
# 35             raise ValueError("Tool output is missing `return_code`")
# 36         if output.summary is None:
# 37             output.summary = "⚠️ Missing summary"
# 38         if output.metrics is None:
# 39             output.metrics = {}
# 40 
# 41         return output
# 42 
# 43     def set_context_provider(self, provider: ContextProviderBase) -> None:
# 44         self.context_provider = provider
# 45 
# 46     def set_score_provider(self, provider: ScoreProviderBase) -> None:
# 47         self.score_provider = provider
# 48 
# 49     @abstractmethod
# 50     def _run(self, input: dict, context: RunContext | None = None) -> ToolOutputSchema:
# 51         raise NotImplementedError


# utilities\__init__.py



# utilities\diff_utils.py

# 1 import difflib
# 2 
# 3 def summarize_diff(before: str, after: str) -> str:
# 4     """Returns a human-readable summary of what changed between two versions."""
# 5     before_lines = before.strip().splitlines()
# 6     after_lines = after.strip().splitlines()
# 7     diff = list(difflib.unified_diff(before_lines, after_lines, lineterm=""))
# 8     if not diff:
# 9         return "No differences detected."
# 10 
# 11     summary = ["Changes:"]
# 12     for line in diff:
# 13         if line.startswith("+ ") and not line.startswith("+++"):
# 14             summary.append(f"Added: {line[2:]}")
# 15         elif line.startswith("- ") and not line.startswith("---"):
# 16             summary.append(f"Removed: {line[2:]}")
# 17 
# 18     return "\n".join(summary[:10])  #  Limit to first 10 lines


# utilities\extension_loader.py

# 1 from importlib.util import spec_from_file_location, module_from_spec
# 2 from pathlib import Path
# 3 from typing import Type
# 4 
# 5 def import_class_from_extension(artifact_path: str | Path, base_cls: Type) -> Type:
# 6     file_path = Path("extensions") / f"{artifact_path}.py"
# 7     if not file_path.exists():
# 8         raise FileNotFoundError(f"Extension file not found: {file_path}")
# 9 
# 10     spec = spec_from_file_location("dynamic_module", file_path)
# 11     if spec is None or spec.loader is None:
# 12         raise ImportError(f"Unable to load: {file_path}")
# 13 
# 14     module = module_from_spec(spec)
# 15     spec.loader.exec_module(module)
# 16 
# 17     for attr in module.__dict__.values():
# 18         if isinstance(attr, type) and issubclass(attr, base_cls) and attr is not base_cls:
# 19             return attr
# 20 
# 21     raise ImportError(f"No subclass of {base_cls.__name__} found in: {file_path}")


# utilities\extract_base_filename.py

# 1 from pathlib import Path
# 2 import re
# 3 
# 4 
# 5 def extract_base_filename(path: Path) -> str:
# 6     name = path.stem
# 7     #  Strip known suffixes like __state_12345
# 8     return re.sub(r"(__state_|__ctrl_|__sys_|__prog_)\d{10}$", "", name)


# utilities\file_management\__init__.py



# utilities\metadata\__init__.py



# utilities\metadata\footer\__init__.py



# utilities\metadata\footer\code_annnotation_utils.py

# 1 import ast
# 2 import uuid
# 3 from pathlib import Path
# 4 from typing import Tuple
# 5 
# 6 FOOTER_START = "#  --- Agent Notes"
# 7 FOOTER_END = "#  -----------------------------------------------"
# 8 
# 9 def split_code_and_notes(file_content: str) -> Tuple[str, str]:
# 10     """Return (code_with_safe_footer_removed, extracted_notes), preserving syntax."""
# 11     footer_start = None
# 12     line_sep = "\r\n" if "\r\n" in file_content else "\n"
# 13     lines = file_content.splitlines(keepends=True)
# 14 
# 15     for i, line in enumerate(lines):
# 16         if line.lstrip().startswith(FOOTER_START):
# 17             footer_start = i
# 18             break
# 19 
# 20     if footer_start is not None:
# 21         code_part = "".join(lines[:footer_start])
# 22         notes_part = "".join(lines[footer_start:])
# 23 
# 24         try:
# 25             ast.parse(code_part)
# 26             return code_part, notes_part
# 27         except SyntaxError:
# 28             safe_footer = (
# 29                 f"{line_sep}{FOOTER_START} (removed safely)\n"
# 30                 f"pass  #  [Agent notes stripped for syntax integrity]{line_sep}"
# 31                 f"{FOOTER_END}{line_sep}"
# 32             )
# 33             return code_part + safe_footer, notes_part
# 34 
# 35     return file_content, ""
# 36 
# 37 def append_agent_note(file_content: str, system: str, agent_name: str, note: str) -> str:
# 38     base, _ = split_code_and_notes(file_content)  #  strip if possible, fallback-safe
# 39 
# 40     formatted_note = (
# 41         f"{FOOTER_START} ({system} / {agent_name}) ---\n" +
# 42         "".join(f"#  {line}\n" for line in note.strip().splitlines()) +
# 43         f"{FOOTER_END}\n"
# 44     )
# 45 
# 46     return base.rstrip() + "\n\n" + formatted_note
# 47 
# 48 
# 49 def prepare_file_for_linting(original_path: str) -> str:
# 50     original = Path(original_path)
# 51     raw = original.read_text(encoding="utf-8")
# 52 
# 53     from .code_annnotation_utils import split_code_and_notes
# 54     code_part, _ = split_code_and_notes(raw)
# 55 
# 56     try:
# 57         ast.parse(code_part)
# 58     except SyntaxError:
# 59         #  Append safety pass to guarantee valid syntax
# 60         code_part += "\npass  #  [Auto-patched for linting]\n"
# 61 
# 62     temp_file = Path("working_files") / f"{uuid.uuid4().hex}_stripped.py"
# 63     temp_file.write_text(code_part.rstrip() + "\n", encoding="utf-8")
# 64 
# 65     return str(temp_file)
# 66 
# 67 


# utilities\metadata\logging\__init__.py



# utilities\metadata\logging\conversation_log.py

# 1 from __future__ import annotations
# 2 from typing import List
# 3 from sqlalchemy.orm import Session
# 4 from app.db.models import AgentConversationLog
# 5 
# 6 def get_conversation_log(session: Session, session_id: str, system: str) -> List[str]:
# 7     """Returns logged conversation entries for the specified session and system."""
# 8     logs = (
# 9         session.query(AgentConversationLog)
# 10         .filter_by(session_id=session_id, system=system)
# 11         .order_by(AgentConversationLog.timestamp.asc())
# 12         .all()
# 13     )
# 14 
# 15     return [log.content for log in logs if log.content]


# utilities\metadata\logging\logging_provider.py

# 1 from __future__ import annotations
# 2 
# 3 from datetime import datetime
# 4 import json
# 5 import logging
# 6 import sqlite3
# 7 from dataclasses import asdict, is_dataclass
# 8 from enum import Enum
# 9 from pathlib import Path
# 10 from typing import Any, ClassVar, Iterable
# 11 from app.db.connection import get_connection
# 12 from app.enums.logging_enums import LOG_TYPE
# 13 from app.db.schemas import (
# 14     SnapshotMetricsSchema,
# 15     StateTransitionLogSchema,
# 16     AgentConversationLogSchema,
# 17     ErrorLogSchema,
# 18     ProviderLogSchema,
# 19     FileLogSchema
# 20 )
# 21 
# 22 LOG_CONFIG_MAP = {
# 23     LOG_TYPE.STATE_TRANSITION: {
# 24         "schema": StateTransitionLogSchema,
# 25         "table": "state_transition_log",
# 26     },
# 27     LOG_TYPE.AGENT_CONVERSATION: {
# 28         "schema": AgentConversationLogSchema,
# 29         "table": "agent_conversation_log",
# 30     },
# 31     LOG_TYPE.PROVIDER: {
# 32         "schema": ProviderLogSchema,
# 33         "table": "provider_log",
# 34     },
# 35     LOG_TYPE.ERROR: {
# 36         "schema": ErrorLogSchema,
# 37         "table": "error_log",
# 38     },
# 39     LOG_TYPE.SNAPSHOT_METRICS: {
# 40         "schema": SnapshotMetricsSchema,  
# 41         "table": "snapshot_metrics",   
# 42     },
# 43     LOG_TYPE.FILE: {  
# 44         "schema": FileLogSchema,
# 45         "table": "file_log",
# 46     },
# 47 }
# 48 
# 49 class LoggingProvider:
# 50     """Centralized provider for structured logging."""
# 51 
# 52     _instance: ClassVar["LoggingProvider" | None] = None
# 53 
# 54     def __new__(cls, *args, **kwargs):
# 55         if cls._instance is None:
# 56             cls._instance = super().__new__(cls)
# 57         return cls._instance
# 58 
# 59     def __init__(
# 60         self,
# 61         db_path: str | Path = "experiments/codecritic.sqlite3",
# 62         output_path: str | Path | None = None,
# 63         connection: sqlite3.Connection | None = None,
# 64     ) -> None:
# 65         if getattr(self, "_initialized", False):
# 66             if connection is not None:
# 67                 self.conn = connection
# 68             return
# 69 
# 70         self.db_path = Path(db_path)
# 71         self.db_path.parent.mkdir(exist_ok=True, parents=True)
# 72 
# 73         self._conn = connection or get_connection()
# 74 
# 75         self.output_path = Path(output_path) if output_path else None
# 76         if self.output_path:
# 77             self.output_path.parent.mkdir(parents=True, exist_ok=True)
# 78 
# 79         self._initialized = True
# 80 
# 81     @property
# 82     def conn(self):
# 83         if getattr(self, "_conn", None) is None:
# 84             from app.db import init_db
# 85             self._conn = init_db(reset=False).raw_connection()
# 86         try:
# 87             self._conn.cursor()
# 88         except Exception:
# 89             from app.db import init_db
# 90             self._conn = init_db(reset=False).raw_connection()
# 91         return self._conn
# 92 
# 93     def _serialize(self, obj: Any) -> dict:
# 94         def _safe(v: Any) -> Any:
# 95             if isinstance(v, (Enum, Path)):
# 96                 return str(v)
# 97             elif isinstance(v, datetime):
# 98                 return v.isoformat()
# 99             elif isinstance(v, list):
# 100                 #  ✅ Serialize list directly
# 101                 return json.dumps([_safe(i) for i in v])
# 102             elif isinstance(v, dict):
# 103                 #  ✅ Serialize dict directly
# 104                 return json.dumps({k: _safe(val) for k, val in v.items()})
# 105             return v
# 106 
# 107         if not is_dataclass(obj) or isinstance(obj, type):
# 108             raise TypeError(f"Expected dataclass instance, got {type(obj)}")
# 109 
# 110         raw = asdict(obj)
# 111         return {k: _safe(v) for k, v in raw.items()}
# 112 
# 113 
# 114 
# 115     def _insert_many(self, table: str, items: Iterable[dict]) -> None:
# 116         items = list(items)
# 117         if not items:
# 118             return
# 119 
# 120         keys = list(items[0].keys())
# 121         cols = ",".join(keys)
# 122         placeholders = ",".join(["?"] * len(keys))
# 123         values = [tuple(i[k] for k in keys) for i in items]
# 124         cur = self.conn.cursor()
# 125         cur.executemany(f"INSERT INTO {table} ({cols}) VALUES ({placeholders})", values)
# 126         self.conn.commit()
# 127         if self.output_path:
# 128             with self.output_path.open("a", encoding="utf-8") as fh:
# 129                 for item in items:
# 130                     fh.write(json.dumps(item) + "\n")
# 131 
# 132     def write(self, log_type: LOG_TYPE, entries: list[Any] | Any) -> int | None:
# 133         if not isinstance(entries, list):
# 134             entries = [entries]
# 135 
# 136         config = LOG_CONFIG_MAP.get(log_type)
# 137         if config is None:
# 138             raise ValueError(f"Unsupported log type: {log_type}")
# 139 
# 140         schema_cls = config["schema"]
# 141         table_name = config["table"]
# 142 
# 143         for entry in entries:
# 144             if not is_dataclass(entry):
# 145                 raise TypeError(f"Expected a dataclass instance, got {type(entry)}")
# 146             if entry.__class__.__name__ != schema_cls.__name__:
# 147                 logging.getLogger("LoggingProvider").warning(
# 148                     f"⚠️ Log schema mismatch: expected {schema_cls.__name__}, got {entry.__class__.__name__}"
# 149                 )
# 150 
# 151         serialized = [self._serialize(e) for e in entries]
# 152         
# 153         cur = self.conn.cursor()
# 154 
# 155         if len(serialized) == 1:
# 156             keys = list(serialized[0].keys())
# 157             cols = ",".join(keys)
# 158             placeholders = ",".join(["?"] * len(keys))
# 159             values = tuple(serialized[0][k] for k in keys)
# 160 
# 161             cur.execute(f"INSERT INTO {table_name} ({cols}) VALUES ({placeholders})", values)
# 162             self.conn.commit()
# 163 
# 164             return cur.lastrowid  #  ✅ Return the inserted row ID
# 165 
# 166         #  If multiple, just bulk insert without return
# 167         self._insert_many(table_name, serialized)
# 168         return None
# 169 
# 170     def log_provider(self, log: ProviderLogSchema) -> None:
# 171         self.write(LOG_TYPE.PROVIDER, log)
# 172 
# 173     def log_state_transition(self, log: StateTransitionLogSchema) -> None:
# 174         self.write(LOG_TYPE.STATE_TRANSITION, log)
# 175 
# 176     def log_agent_conversation(self, log: AgentConversationLogSchema) -> None:
# 177         self.write(LOG_TYPE.AGENT_CONVERSATION, log)
# 178 
# 179     def log_error(self, log: ErrorLogSchema) -> None:
# 180         self.write(LOG_TYPE.ERROR, log)
# 181 
# 182     def log_snapshot_metrics(self, log: SnapshotMetricsSchema) -> None:
# 183         self.write(LOG_TYPE.SNAPSHOT_METRICS, log)
# 184     
# 185     def log_file(self, log: FileLogSchema) -> None:
# 186         self.write(LOG_TYPE.FILE, log)
# 187 
# 188     def close(self) -> None:
# 189         self.conn.close()
# 190 
# 191 
# 192 class LoggingMixin:
# 193     """Mixin to provide access to a shared LoggingProvider instance."""
# 194 
# 195     def __init__(self, logger: LoggingProvider | None = None) -> None:
# 196         self.logger = logger or LoggingProvider()
# 197         self._log = logging.getLogger(self.__class__.__name__)
# 198 
# 199     def log_provider(self, log: ProviderLogSchema) -> None:
# 200         self.logger.log_provider(log)
# 201 
# 202     def log_state_transition(self, log: StateTransitionLogSchema) -> None:
# 203         self.logger.log_state_transition(log)
# 204 
# 205     def log_agent_conversation(self, log: AgentConversationLogSchema) -> None:
# 206         self.logger.log_agent_conversation(log)
# 207 
# 208     def log_error(self, log: ErrorLogSchema) -> None:
# 209         self.logger.log_error(log)
# 210 
# 211     def log_snapshot_metrics(self, log: SnapshotMetricsSchema) -> None:
# 212         self.logger.log_snapshot_metrics(log)
# 213     
# 214     def log_file(self, log: FileLogSchema) -> None:
# 215         self.logger.log_file(log)


# utilities\metadata\snapshots\__init__.py



# utilities\metadata\snapshots\analyze_code_metrics.py

# 1 from __future__ import annotations
# 2 
# 3 import ast
# 4 import tokenize
# 5 from io import StringIO
# 6 from typing import Any, Dict
# 7 
# 8 
# 9 def analyze_code(source: str) -> Dict[str, Any]:
# 10     """Analyze Python source code and return structural metrics safely."""
# 11     metrics: Dict[str, Any] = {
# 12         #  Default all metrics to zero
# 13         "line_count": len(source.strip().splitlines()),
# 14         "function_count": 0,
# 15         "class_count": 0,
# 16         "branch_count": 0,
# 17         "call_count": 0,
# 18         "assignment_count": 0,
# 19         "return_count": 0,
# 20         "exception_count": 0,
# 21         "docstring_count": 0,
# 22         "symbol_count": 0,
# 23         "comment_count": 0,
# 24         "import_count": 0,
# 25         "analysis_failed": False,
# 26     }
# 27 
# 28     #  === AST-based metrics ===
# 29     try:
# 30         tree = ast.parse(source)
# 31         symbols = set()
# 32 
# 33         for node in ast.walk(tree):
# 34             if isinstance(node, ast.FunctionDef):
# 35                 metrics["function_count"] += 1
# 36                 symbols.add(node.name)
# 37                 if ast.get_docstring(node):
# 38                     metrics["docstring_count"] += 1
# 39             elif isinstance(node, ast.ClassDef):
# 40                 metrics["class_count"] += 1
# 41                 symbols.add(node.name)
# 42                 if ast.get_docstring(node):
# 43                     metrics["docstring_count"] += 1
# 44             elif isinstance(node, (ast.If, ast.For, ast.While, ast.Match, ast.With, ast.Try)):
# 45                 metrics["branch_count"] += 1
# 46             elif isinstance(node, ast.Call):
# 47                 metrics["call_count"] += 1
# 48             elif isinstance(node, ast.Assign):
# 49                 metrics["assignment_count"] += 1
# 50             elif isinstance(node, ast.Return):
# 51                 metrics["return_count"] += 1
# 52             elif isinstance(node, (ast.Try, ast.ExceptHandler, ast.Raise)):
# 53                 metrics["exception_count"] += 1
# 54 
# 55         metrics["symbol_count"] = len(symbols)
# 56 
# 57     except (SyntaxError, ValueError) as e:
# 58         metrics["analysis_failed"] = True
# 59         return metrics
# 60 
# 61     #  === Token-based metrics ===
# 62     try:
# 63         tokens = tokenize.generate_tokens(StringIO(source).readline)
# 64         for toknum, tokval, *_ in tokens:
# 65             if toknum == tokenize.COMMENT:
# 66                 metrics["comment_count"] += 1
# 67             elif toknum == tokenize.NAME and tokval in {"import", "from"}:
# 68                 metrics["import_count"] += 1
# 69     except tokenize.TokenError:
# 70         metrics["analysis_failed"] = True
# 71 
# 72     return metrics
# 73 
# 74 def compute_deltas(before_metrics: dict, after_metrics: dict) -> dict:
# 75     """Compute deltas between before/after metric sets."""
# 76     return {
# 77         f"{key}_delta": after_metrics.get(key, 0) - before_metrics.get(key, 0)
# 78         for key in after_metrics
# 79         if key in before_metrics
# 80     }


# utilities\metadata\snapshots\snapshot_reader.py

# 1 from pathlib import Path
# 2 
# 3 SNAPSHOT_ROOT = Path(__file__).resolve().parents[4] / "experiments" / "snapshots"
# 4 
# 5 def read_latest_snapshot(session_id: str, root: Path | str | None = None):
# 6     session_root = Path(root) if root else SNAPSHOT_ROOT
# 7     session_root = session_root / str(session_id)
# 8     
# 9     if not session_root.exists():
# 10         return None
# 11 
# 12     snapshots = sorted(session_root.glob("*.before"), key=lambda p: p.stat().st_mtime, reverse=True)
# 13 
# 14     if not snapshots:
# 15         return None
# 16 
# 17     latest_snapshot_id = snapshots[0].stem
# 18     before_path = session_root / f"{latest_snapshot_id}.before"
# 19     after_path = session_root / f"{latest_snapshot_id}.after"
# 20 
# 21     return {
# 22         "before": before_path.read_text(encoding="utf-8"),
# 23         "after": after_path.read_text(encoding="utf-8"),
# 24         "before_path": str(before_path),
# 25         "after_path": str(after_path)
# 26     }
# 27 


# utilities\metadata\snapshots\snapshot_writer.py

# 1 import json
# 2 import uuid
# 3 from datetime import datetime, timezone
# 4 from pathlib import Path
# 5 
# 6 SNAPSHOT_ROOT = Path(__file__).resolve().parents[4] / "experiments" / "snapshots"
# 7 
# 8 class SnapshotWriter:
# 9     def __init__(self, root: Path | str | None = None) -> None:
# 10         self.root = Path(root) if root else SNAPSHOT_ROOT
# 11         self.root.mkdir(parents=True, exist_ok=True)
# 12 
# 13     def write_snapshot(
# 14         self, *, before: str, after: str, session_id: str, metadata: dict | None = None
# 15     ) -> str:
# 16 
# 17         if before == after:
# 18             return ""
# 19         session_root = self.root / str(session_id) 
# 20         session_root.mkdir(parents=True, exist_ok=True)
# 21 
# 22         snapshot_id = str(uuid.uuid4())
# 23 
# 24         (session_root / f"{snapshot_id}.before").write_text(before, encoding="utf-8")
# 25         (session_root / f"{snapshot_id}.after").write_text(after, encoding="utf-8")
# 26 
# 27         if metadata:
# 28             metadata.setdefault("timestamp", datetime.now(timezone.utc).isoformat())
# 29             meta_path = session_root / f"{snapshot_id}.meta.json"
# 30             meta_path.write_text(json.dumps(metadata, indent=2), encoding="utf-8")
# 31 
# 32         return snapshot_id


# utilities\provider_mixin_injector.py

# 1 class ProviderContextInjectorWrapper:
# 2     def __init__(self, provider, session_id=None, file_log_id=None):
# 3         self._provider = provider
# 4         self._injected_session_id = session_id
# 5         self._injected_file_log_id = file_log_id
# 6 
# 7         if session_id:
# 8             setattr(self._provider, "_session_id", session_id)
# 9         if file_log_id:
# 10             setattr(self._provider, "_file_log_id", file_log_id)
# 11         if hasattr(self._provider, "propagate_file_log_id") and file_log_id:
# 12             self._provider.propagate_file_log_id(file_log_id)
# 13 
# 14     def run(self, input: dict | None = None):
# 15         input = input or {}
# 16         input.setdefault("session_id", self._injected_session_id)
# 17         input.setdefault("file_log_id", self._injected_file_log_id)
# 18 
# 19         #  Inject into provider directly
# 20         self._provider._session_id = self._injected_session_id
# 21         self._provider._file_log_id = self._injected_file_log_id
# 22 
# 23         return self._provider.run(input=input)
# 24 
# 25     def __getattr__(self, attr):
# 26         return getattr(self._provider, attr)


# utilities\pydantic_compat.py

# 1 from __future__ import annotations
# 2 
# 3 from typing import TYPE_CHECKING, Callable
# 4 
# 5 try:  #  pragma: no cover - pydantic v1 compatibility
# 6     from pydantic import BaseModel
# 7 
# 8     if not hasattr(BaseModel, "model_dump"):
# 9         from typing import Callable
# 10 
# 11         def _model_dump(self: BaseModel, **kwargs) -> dict:
# 12             return BaseModel.dict(self, **kwargs)
# 13 
# 14         setattr(BaseModel, "model_dump", _model_dump)  #  type: ignore[attr-defined]
# 15 except Exception:  #  pragma: no cover - ignore if pydantic v2
# 16     pass
# 17 
# 18 if TYPE_CHECKING:
# 19     from pydantic import field_validator, model_validator  #  type: ignore
# 20 else:  #  pragma: no cover - runtime fallback for pydantic v1
# 21     try:
# 22         from pydantic import field_validator, model_validator  #  type: ignore
# 23     except ImportError:  #  pragma: no cover - pydantic v1
# 24         from pydantic import validator, root_validator
# 25 
# 26         def field_validator(*fields: str, **kwargs) -> Callable:
# 27             kwargs.pop("mode", None)
# 28             return validator(*fields, **kwargs)
# 29 
# 30         def model_validator(*fields, **kwargs) -> Callable:
# 31             mode = kwargs.pop("mode", "after")
# 32 
# 33             def decorator(func: Callable) -> Callable:
# 34                 if mode == "after":
# 35 
# 36                     def wrapper(cls, values):
# 37                         obj = cls.construct(**values)
# 38                         func(obj)
# 39                         return obj.dict()
# 40 
# 41                     return root_validator(pre=False, allow_reuse=True)(wrapper)
# 42 
# 43                 def wrapper(cls, values):
# 44                     return func(values)
# 45 
# 46                 return root_validator(pre=True, allow_reuse=True)(wrapper)
# 47 
# 48             return decorator


# utilities\run_context.py

# 1 #  from contextvars import ContextVar
# 2 
# 3 #  #  Global context variables
# 4 #  _session_id: ContextVar[str | None] = ContextVar("session_id", default=None)
# 5 #  _file_log_id: ContextVar[str | None] = ContextVar("file_log_id", default=None)
# 6 
# 7 #  #  ─────────────────────────────────────────────────────────── Accessors
# 8 
# 9 #  def set_run_context(session_id: str | None = None, file_log_id: str | None = None) -> None:
# 10 #      if session_id is not None:
# 11 #          _session_id.set(session_id)
# 12 #      if file_log_id is not None:
# 13 #          _file_log_id.set(file_log_id)
# 14 
# 15 #  def clear_run_context() -> None:
# 16 #      _session_id.set(None)
# 17 #      _file_log_id.set(None)
# 18 
# 19 #  def get_session_id() -> str | None:
# 20 #      return _session_id.get()
# 21 
# 22 #  def get_file_log_id() -> str | None:
# 23 #      return _file_log_id.get()
# 24 
# 25 #  #  ─────────────────────────────────────────────────────────── Propagation
# 26 
# 27 #  def propagate_run_context_if_needed(obj: object) -> None:
# 28 #      from app.providers.base_provider import BaseProvider
# 29 #      try:
# 30 #          from app.utilities.provider_mixin_injector import ProviderContextInjectorWrapper
# 31 #      except ImportError:
# 32 #          ProviderContextInjectorWrapper = None
# 33 
# 34 #      session_id = get_session_id()
# 35 #      file_log_id = get_file_log_id()
# 36 
# 37 #      #  Unwrap injector wrappers
# 38 #      if ProviderContextInjectorWrapper and isinstance(obj, ProviderContextInjectorWrapper):
# 39 #          for attr in ('_wrapped_instance', 'instance', '_wrapped', 'wrapped'):
# 40 #              inner = getattr(obj, attr, None)
# 41 #              if inner:
# 42 #                  propagate_run_context_if_needed(inner)
# 43 #                  return
# 44 #          return
# 45 
# 46 #      #  Only proceed for BaseProvider instances
# 47 #      if not isinstance(obj, BaseProvider):
# 48 #          return
# 49 
# 50 #      #  Apply context
# 51 #      if session_id and not getattr(obj, '_session_id', None):
# 52 #          obj._session_id = session_id
# 53 #      if file_log_id and not getattr(obj, '_file_log_id', None):
# 54 #          obj._file_log_id = file_log_id
# 55 
# 56 #      #  Propagate to subcomponents
# 57 #      for attr_name in dir(obj):
# 58 #          try:
# 59 #              attr = getattr(obj, attr_name)
# 60 #              if isinstance(attr, BaseProvider):
# 61 #                  propagate_run_context_if_needed(attr)
# 62 #              elif isinstance(attr, list):
# 63 #                  for item in attr:
# 64 #                      if isinstance(item, BaseProvider):
# 65 #                          propagate_run_context_if_needed(item)
# 66 #              elif isinstance(attr, dict):
# 67 #                  for item in attr.values():
# 68 #                      if isinstance(item, BaseProvider):
# 69 #                          propagate_run_context_if_needed(item)
# 70 #          except Exception:
# 71 #              continue
# 72 
# 73 #  #  ─────────────────────────────────────────────────────────── Patch BaseProvider
# 74 
# 75 #  def _patch_base_provider_class():
# 76 #      from app.providers.base_provider import BaseProvider
# 77 #      from app.utilities.run_context import get_session_id, get_file_log_id
# 78 
# 79 #      orig_init = BaseProvider.__init__
# 80 #      def wrapped_init(self, *args, **kwargs):
# 81 #          orig_init(self, *args, **kwargs)
# 82 #          propagate_run_context_if_needed(self)
# 83 #      BaseProvider.__init__ = wrapped_init
# 84 
# 85 #      if hasattr(BaseProvider, 'run'):
# 86 #          orig_run = BaseProvider.run
# 87 #          def wrapped_run(self, *args, **kwargs):
# 88 #              propagate_run_context_if_needed(self)
# 89 #              session_id = get_session_id()
# 90 #              file_log_id = get_file_log_id()
# 91 #              if 'input' in kwargs and isinstance(kwargs['input'], dict):
# 92 #                  inp = kwargs['input']
# 93 #                  if session_id and not inp.get('session_id'):
# 94 #                      inp['session_id'] = session_id
# 95 #                  if file_log_id and not inp.get('file_log_id'):
# 96 #                      inp['file_log_id'] = file_log_id
# 97 #              return orig_run(self, *args, **kwargs)
# 98 #          BaseProvider.run = wrapped_run
# 99 
# 100 #  _patch_base_provider_class()


# utilities\schema\__init__.py

# 1 from .create_schema import initialize_database, create_tables, load_seed_data
# 2 
# 3 __all__ = ["initialize_database", "create_tables", "load_seed_data"]


# utilities\schema\create_schema.py

# 1 #  from pydantic import BaseModel, Field
# 2 #  from pathlib import Path
# 3 #  from typing import Dict, List, Optional, ClassVar
# 4 #  from uuid import UUID, uuid4
# 5 #  from app.enums.system_enums import SystemType
# 6 #  from app.utilities.pydantic_compat import field_validator
# 7 
# 8 #  class AgentPromptSchema(BaseModel):
# 9 #      id: Optional[int]
# 10 #      guid: UUID = Field(default_factory=uuid4)
# 11 #      name: str
# 12 #      description: Optional[str]
# 13 #      artifact_path: Path
# 14 #      tags: Optional[List[str]] = None
# 15 
# 16 #      table_name: ClassVar[str] = "agent_prompt"
# 17 
# 18 #      @field_validator("artifact_path")
# 19 #      @classmethod
# 20 #      def _check_path(cls, v: Path) -> Path:
# 21 #          if not v.is_absolute() and ".." in v.parts:
# 22 #              raise ValueError("Invalid artifact path")
# 23 #          return v
# 24 
# 25 #  class SystemPromptSchema(BaseModel):
# 26 #      id: Optional[int]
# 27 #      guid: UUID = Field(default_factory=uuid4)
# 28 #      name: str
# 29 #      system_type: SystemType
# 30 #      description: Optional[str]
# 31 #      artifact_path: Path
# 32 #      tags: Optional[List[str]] = None
# 33 
# 34 #      table_name: ClassVar[str] = "system_prompt"
# 35 
# 36 #      @field_validator("artifact_path")
# 37 #      @classmethod
# 38 #      def _check_path(cls, v: Path) -> Path:
# 39 #          if not v.is_absolute() and ".." in v.parts:
# 40 #              raise ValueError("Invalid artifact path")
# 41 #          return v
# 42 
# 43 #  class ToolConfig(BaseModel):
# 44 #      id: Optional[int] = None
# 45 #      guid: UUID = Field(default_factory=uuid4)
# 46 #      name: str
# 47 #      description: Optional[str] = None
# 48 #      config: Optional[Dict] = None
# 49 #      artifact_path: Path
# 50 
# 51 #      table_name: ClassVar[str] = "tool_config"
# 52 
# 53 #      @field_validator("artifact_path")
# 54 #      @classmethod
# 55 #      def _check_path(cls, v: Path) -> Path:
# 56 #          if not v.is_absolute() and ".." in v.parts:
# 57 #              raise ValueError("Invalid artifact path")
# 58 #          return v
# 59 
# 60 #  class ScoreProvider(BaseModel):
# 61 #      id: Optional[int] = None
# 62 #      guid: UUID = Field(default_factory=uuid4)
# 63 #      name: str
# 64 #      description: Optional[str] = None
# 65 #      artifact_path: Optional[Path] = None
# 66 
# 67 #      table_name: ClassVar[str] = "score_provider"
# 68 
# 69 #      @field_validator("artifact_path")
# 70 #      @classmethod
# 71 #      def _check_path(cls, v: Optional[Path]) -> Optional[Path]:
# 72 #          if v is None:
# 73 #              return v
# 74 #          p = Path(v)
# 75 #          if not p.is_absolute() and ".." in p.parts:
# 76 #              raise ValueError("artifact_path must be absolute or project relative")
# 77 #          return p
# 78 
# 79 #      def model_dump(self, **kwargs) -> dict:
# 80 #          return super().model_dump(**kwargs)


# utilities\select_best_file_by_score.py

# 1 from pathlib import Path
# 2 from typing import Callable
# 3 
# 4 from app.providers.score_provider_base import ScoreProviderBase
# 5 
# 6 
# 7 def select_best_file_by_score(
# 8     file_a: str,
# 9     file_b: str,
# 10     score_provider: ScoreProviderBase,
# 11     context
# 12 ) -> str:
# 13     """
# 14     Compare two file paths using a scoring provider (with .run(input, session_id).value)
# 15     and return the file path with the higher score.
# 16 
# 17     Args:
# 18         file_a (str): First file path to compare.
# 19         file_b (str): Second file path to compare.
# 20         scoring_provider: Object with a .run(dict, session_id) method that returns .value.
# 21         system (str): System identifier passed into the scoring input.
# 22         session_id (str): Session identifier passed into the scoring call.
# 23 
# 24     Returns:
# 25         str: File path with the higher score.
# 26     """
# 27     path_a = Path(file_a).resolve()
# 28     path_b = Path(file_b).resolve()
# 29 
# 30     score_a = score_provider.run({"file_path": str(path_a)}, context=context).value
# 31     score_b = score_provider.run({"file_path": str(path_b)}, context=context).value
# 32 
# 33     return str(path_a if score_a >= score_b else path_b)



# === PYTHON-NOTEBOOKS FILES ===


# === NON-PYTHON FILES ===

# db\seeders\files\prompts\basic_agent_prompt.txt

# 1 You are a code generation agent that produces basic, clean, formatted Python code snippets based on given instructions.


# db\seeders\files\prompts\default_system_prompt.txt

# 1 You are the default formatting system. Your task is to ensure all provided Python code strictly adheres to PEP8 standards.


# db\seeders\files\prompts\linting_generator_agent_prompt.txt

# 1 You are the Generator Agent in the Linting System.
# 2 
# 3 You specialize in incrementally improving Python code quality by addressing lint violations, enhancing readability, and promoting best practices.
# 4 
# 5 Your strategy is:
# 6 
# 7 Focus on minimal, targeted improvements that preserve original structure
# 8 
# 9 Apply type hints and clarify variable names where appropriate
# 10 
# 11 Use reasoning to resolve tool-reported issues when they conflict
# 12 
# 13 You will be evaluated by a Discriminator Agent and may be prompted to revise your output. If you are in a retry round, incorporate prior feedback before retrying.
# 14 
# 15 Maintain a helpful, confident tone in your comments. Your improvements should be easy for a human developer to understand and accept.
# 16 
# 17 🚫 Your response must strictly follow this format:
# 18 
# 19 [CODE]
# 20 #  your revised Python code here
# 21 [/CODE]
# 22 
# 23 [CONVERSATION_LOG_ENTRY]
# 24 #  bullet list of what changed, why, and tradeoffs if any
# 25 [/CONVERSATION_LOG_ENTRY]
# 26 
# 27 ⚠️ Anything outside these blocks will be ignored.
# 28 


# db\seeders\files\prompts\linting_system_prompt.txt

# 1 You are operating within the Linting System, which enforces strict Python code quality and formatting compliance.
# 2 
# 3 All Generator Agents must:
# 4 - Conform to Python PEP8 guidelines
# 5 - Use tools such as `black`, `ruff`, `mypy`, and `radon` for diagnostic feedback
# 6 - Preserve original code intent and structure; avoid stylistic or structural rewrites unless necessary
# 7 - Produce valid, executable Python code
# 8 
# 9 Your response must include your corrected code inside a [CODE] block. Any content outside this block will be ignored by the system:
# 10 
# 11 [CODE]
# 12 #  your corrected Python code
# 13 [/CODE]
# 14 
# 15 After the code, include a structured log entry describing your decisions:
# 16 
# 17 [CONVERSATION_LOG_ENTRY]
# 18 Summarize what you changed, why you changed it, and any tradeoffs or notable decisions you made.
# 19 [/CONVERSATION_LOG_ENTRY]


