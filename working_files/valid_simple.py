def add(a: int, b: int) -> int:
    return a + b

# --- Agent Notes (PROVIDER_TYPE.STATE / linting_generator_agent_provider) ---
# - Added type hints to the function `add` to specify that it takes two integers and returns an integer. This improves code clarity and helps with static type checking using tools like `mypy`.
# - No changes were made to the function logic or structure, preserving the original intent.
# -----------------------------------------------
