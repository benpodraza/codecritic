
# === PYTHON FILES ===

# __init__.py



# db\__init__.py

# 1 import gc
# 2 from sqlalchemy import create_engine
# 3 from app.db.connection import DB_PATH, close_connection
# 4 from app.db.models import Base
# 5 
# 6 def init_db(reset: bool = False):
# 7     #  Explicitly close lingering DB-API connections
# 8     close_connection()
# 9 
# 10     #  Explicitly create and dispose of engine before deletion
# 11     engine = create_engine(f"sqlite:///{DB_PATH}")
# 12     engine.dispose()
# 13 
# 14     if reset and DB_PATH.exists():
# 15         try:
# 16             DB_PATH.unlink()
# 17         except PermissionError as e:
# 18             print(f"⚠️ Unable to delete {DB_PATH}: {e}")
# 19             print("⚠️ Attempting to force garbage collection.")
# 20             gc.collect()
# 21             DB_PATH.unlink()  #  try again after GC
# 22 
# 23     #  Recreate engine after deletion
# 24     engine = create_engine(f"sqlite:///{DB_PATH}")
# 25     Base.metadata.create_all(bind=engine)
# 26     
# 27     return engine


# db\base.py

# 1 from sqlalchemy.orm import declarative_base
# 2 
# 3 Base = declarative_base()


# db\connection.py

# 1 from pathlib import Path
# 2 import sqlite3
# 3 
# 4 DB_PATH = Path("experiments") / "codecritic.sqlite3"
# 5 _CONN: sqlite3.Connection | None = None
# 6 
# 7 def get_connection() -> sqlite3.Connection:
# 8     global _CONN
# 9     try:
# 10         if _CONN is None:
# 11             raise RuntimeError
# 12         _CONN.execute("SELECT 1")
# 13     except (sqlite3.ProgrammingError, RuntimeError):
# 14         _CONN = sqlite3.connect(DB_PATH, check_same_thread=False)
# 15     return _CONN
# 16 
# 17 def close_connection() -> None:
# 18     global _CONN
# 19     if _CONN:
# 20         _CONN.close()
# 21         _CONN = None


# db\models.py

# 1 from datetime import datetime, timezone
# 2 from typing import Optional
# 3 from sqlalchemy import Column, DateTime, Enum, Float, Integer, String, JSON
# 4 from sqlalchemy.dialects.sqlite import TEXT
# 5 from app.db.base import Base
# 6 from uuid import uuid4
# 7 
# 8 from app.enums.agent_enums import AGENT
# 9 from app.enums.system_enums import SYSTEM
# 10 from sqlalchemy.orm import Mapped, mapped_column
# 11 
# 12 # #  PROMPT CONFIGS
# 13 
# 14 class AgentPrompt(Base):
# 15     __tablename__ = 'agent_prompt'
# 16     id = Column(Integer, primary_key=True, index=True)
# 17     guid = Column(TEXT, unique=True, default=lambda: str(uuid4()))
# 18     name = Column(String, nullable=False)
# 19     description = Column(String)
# 20     artifact_path = Column(String, nullable=False)
# 21     tags = Column(JSON, nullable=True)
# 22 
# 23 class SystemPrompt(Base):
# 24     __tablename__ = 'system_prompt'
# 25     id = Column(Integer, primary_key=True, index=True)
# 26     guid = Column(TEXT, unique=True, default=lambda: str(uuid4()))
# 27     name = Column(String, nullable=False)
# 28     system_type = Column(Enum(SYSTEM), nullable=False) 
# 29     description = Column(String)
# 30     artifact_path = Column(String, nullable=False)
# 31     tags = Column(JSON, nullable=True)
# 32 
# 33 
# 34 # #  PROVIDER CONFGIS
# 35 
# 36 class PromptProviderConfig(Base):
# 37     __tablename__ = "prompt_provider_config"
# 38 
# 39     id = Column(Integer, primary_key=True, index=True)
# 40     guid = Column(String, unique=True, nullable=False)
# 41     name = Column(String, nullable=False)
# 42     description = Column(String, nullable=True)
# 43     artifact_path = Column(String, nullable=False)
# 44     config = Column(JSON, nullable=False) 
# 45     tags = Column(JSON, nullable=True)
# 46 
# 47 class ToolProviderConfig(Base):
# 48     __tablename__ = "tool_provider_config"
# 49     id = Column(Integer, primary_key=True)
# 50     guid = Column(String, unique=True, nullable=False)
# 51     name = Column(String, nullable=False)
# 52     description = Column(String, nullable=True)
# 53     config = Column(JSON, nullable=True)
# 54     artifact_path = Column(String, nullable=False)
# 55     tags = Column(JSON, nullable=True)
# 56 
# 57 class ScoreProviderConfig(Base):
# 58     __tablename__ = "score_provider_config" 
# 59     id = Column(Integer, primary_key=True)
# 60     guid = Column(String, unique=True, nullable=False)
# 61     name = Column(String, nullable=False)
# 62     description = Column(String, nullable=True)
# 63     config = Column(JSON, nullable=True)
# 64     artifact_path = Column(String, nullable=False)
# 65     tags = Column(JSON, nullable=True)
# 66 
# 67 class ContextProviderConfig(Base):
# 68     __tablename__ = "context_provider_config" 
# 69     id = Column(Integer, primary_key=True)
# 70     guid = Column(String, unique=True, nullable=False)
# 71     name = Column(String, nullable=False)
# 72     description = Column(String, nullable=True)
# 73     config = Column(JSON, nullable=True)
# 74     artifact_path = Column(String, nullable=False)
# 75     tags = Column(JSON, nullable=True)
# 76 
# 77 class AgentEngineProviderConfig(Base):
# 78     __tablename__ = "agent_engine_provider_config" 
# 79 
# 80     id = Column(Integer, primary_key=True)
# 81     guid = Column(String, unique=True, nullable=False)
# 82     name = Column(String, nullable=False)
# 83     description = Column(String, nullable=True)
# 84     model= Column(String, nullable=False)
# 85     config = Column(JSON, nullable=True)
# 86     cost_per_1k_tokens = Column(Float, nullable=True, default=0.0)
# 87     artifact_path = Column(String, nullable=False)
# 88     tags = Column(JSON, nullable=True)
# 89 
# 90 
# 91 class AgentProviderConfig(Base):
# 92     __tablename__ = "agent_provider_config" 
# 93 
# 94     id = Column(Integer, primary_key=True)
# 95     guid = Column(String, unique=True, nullable=False)
# 96     name = Column(String, nullable=False)
# 97     description = Column(String, nullable=True)
# 98     agent_type = Column(Enum(AGENT), nullable=False) 
# 99     config = Column(JSON, nullable=True)
# 100     artifact_path = Column(String, nullable=False)
# 101     tags = Column(JSON, nullable=True)
# 102 
# 103 class StateProviderConfig(Base):
# 104     __tablename__ = "state_provider_config"
# 105 
# 106     id = Column(Integer, primary_key=True)
# 107     guid = Column(TEXT, unique=True, default=lambda: str(uuid4()))
# 108     name = Column(String, nullable=False)
# 109     description = Column(String, nullable=True)
# 110     config = Column(JSON, nullable=True)
# 111     artifact_path = Column(String, nullable=False)
# 112     tags = Column(JSON, nullable=True)
# 113 
# 114 class SystemProviderConfig(Base):
# 115     __tablename__ = "system_provider_config"
# 116 
# 117     id = Column(Integer, primary_key=True)
# 118     guid = Column(TEXT, unique=True, default=lambda: str(uuid4()))
# 119     name = Column(String, nullable=False)
# 120     description = Column(String, nullable=True)
# 121     config = Column(JSON, nullable=True)
# 122     artifact_path = Column(String, nullable=False)
# 123     tags = Column(JSON, nullable=True)
# 124 
# 125 class ControllerProviderConfig(Base):
# 126     __tablename__ = "controller_provider_config"
# 127 
# 128     id = Column(Integer, primary_key=True)
# 129     guid = Column(TEXT, unique=True, default=lambda: str(uuid4()))
# 130     name = Column(String, nullable=False)
# 131     description = Column(String, nullable=True)
# 132     config = Column(JSON, nullable=True)   
# 133     artifact_path = Column(String, nullable=False)
# 134     tags = Column(JSON, nullable=True)
# 135 
# 136 
# 137 class ProgramProviderConfig(Base):
# 138     __tablename__ = "program_provider_config"
# 139 
# 140     id = Column(Integer, primary_key=True)
# 141     guid = Column(TEXT, unique=True, default=lambda: str(uuid4()))
# 142     name = Column(String, nullable=False)
# 143     description = Column(String, nullable=True)
# 144     config = Column(JSON, nullable=True)
# 145     artifact_path = Column(String, nullable=False)
# 146     tags = Column(JSON, nullable=True)
# 147 
# 148 class SessionConfig(Base):
# 149     __tablename__ = "session_config"
# 150 
# 151     id = Column(Integer, primary_key=True)
# 152     program_provider_id = Column(String, unique=True, nullable=False)
# 153     name = Column(String, nullable=False)
# 154     description = Column(String, nullable=True)
# 155     environment_type = Column(String, nullable=False)  #  e.g., "experiment", "live", "staging"
# 156     tags = Column(JSON, nullable=True)
# 157 
# 158 
# 159 # #  LOGS
# 160 
# 161 class ProviderLog(Base):
# 162     __tablename__ = "provider_log"
# 163 
# 164     id = Column(Integer, primary_key=True, index=True)
# 165     session_id = Column(String, nullable=False)
# 166     timestamp = Column(DateTime, nullable=False, default=datetime.now(timezone.utc))
# 167     provider_id = Column(Integer, nullable=False)
# 168     provider_type = Column(String, nullable=False)
# 169     input = Column(String, nullable=True)
# 170     output = Column(String, nullable=True)
# 171     output_schema = Column(String, nullable=True) 
# 172     latency_ms = Column(Integer, nullable=True)   
# 173     config_hash = Column(String, nullable=True)   
# 174     file_name = Column(String, nullable=True)
# 175     called_by_type = Column(String, nullable=True)
# 176     called_by_id = Column(Integer, nullable=True)
# 177     run_id = Column(TEXT, nullable=False, index=True)
# 178 
# 179     
# 180 class StateTransitionLog(Base):
# 181     __tablename__ = "state_transition_log"
# 182 
# 183     id = Column(Integer, primary_key=True, index=True)
# 184     session_id = Column(String, nullable=False)
# 185     timestamp = Column(DateTime, nullable=False, default=datetime.now(timezone.utc))
# 186     entity_type = Column(String, nullable=False)
# 187     entity_id = Column(Integer, nullable=False)
# 188     from_state = Column(String, nullable=False)
# 189     to_state = Column(String, nullable=False)
# 190     reason = Column(String, nullable=True)
# 191     decision = Column(String, nullable=True)    
# 192     triggered_by = Column(String, nullable=True)   
# 193     step = Column(Integer, nullable=True)   
# 194     transition_metadata: Mapped[Optional[dict]] = mapped_column(JSON, nullable=True)
# 195     input_snapshot_id = Column(String, nullable=True)
# 196     output_snapshot_id = Column(String, nullable=True)
# 197     run_id = Column(TEXT, nullable=False, index=True)
# 198     called_by_type = Column(String, nullable=True)
# 199     called_by_id = Column(Integer, nullable=True) 
# 200 
# 201 class AgentConversationLog(Base):
# 202     __tablename__ = "agent_conversation_log"
# 203 
# 204     id = Column(Integer, primary_key=True, index=True)
# 205     session_id = Column(String, nullable=False)
# 206     system = Column(String, nullable=False)
# 207     agent_provider_config_id = Column(Integer, nullable=False)
# 208     agent_type = Column(String, nullable=False) 
# 209     content = Column(String, nullable=False)
# 210     timestamp = Column(DateTime, nullable=False, default=datetime.now(timezone.utc))
# 211     run_id = Column(TEXT, nullable=False, index=True)
# 212 
# 213 class ErrorLog(Base):
# 214     __tablename__ = "error_log"
# 215 
# 216     id = Column(Integer, primary_key=True, index=True)
# 217     session_id = Column(String, nullable=False)
# 218     timestamp = Column(DateTime, nullable=False, default=datetime.now(timezone.utc))
# 219     latency_ms = Column(Integer, nullable=True) 
# 220     provider_id = Column(Integer, nullable=True)
# 221     provider_type = Column(String, nullable=True)
# 222     error_type = Column(String, nullable=False)
# 223     message = Column(String, nullable=False)
# 224     file_path = Column(String, nullable=True)
# 225     called_by_type = Column(String, nullable=True)
# 226     called_by_id = Column(Integer, nullable=True)
# 227     run_id = Column(TEXT, nullable=False, index=True)
# 228 
# 229 class SnapshotMetrics(Base):
# 230     __tablename__ = "snapshot_metrics"
# 231 
# 232     id = Column(Integer, primary_key=True)
# 233     session_id = Column(String, nullable=False)
# 234     snapshot_id = Column(String, nullable=False)
# 235     system = Column(String, nullable=True)
# 236     agent = Column(String, nullable=True)
# 237     agent_type = Column(Enum(AGENT), nullable=True)
# 238     agent_id = Column(Integer, nullable=True)
# 239     score = Column(Float, nullable=True)
# 240     state = Column(String, nullable=True)
# 241     decision = Column(String, nullable=True)
# 242     timestamp = Column(DateTime, nullable=False, default=datetime.now(timezone.utc))
# 243 
# 244     #  structural metrics
# 245     line_count_before = Column(Integer)
# 246     line_count_after = Column(Integer)
# 247     function_count_before = Column(Integer)
# 248     function_count_after = Column(Integer)
# 249     symbol_count_before = Column(Integer)
# 250     symbol_count_after = Column(Integer)
# 251     branch_count_before = Column(Integer)
# 252     branch_count_after = Column(Integer)
# 253     comment_count_before = Column(Integer)
# 254     comment_count_after = Column(Integer)
# 255 
# 256     #  deltas
# 257     line_count_delta = Column(Integer)
# 258     function_count_delta = Column(Integer)
# 259     symbol_count_delta = Column(Integer)
# 260     branch_count_delta = Column(Integer)
# 261     comment_count_delta = Column(Integer)
# 262 
# 263 


# db\schemas.py

# 1 from dataclasses import dataclass, field
# 2 from datetime import datetime, timezone
# 3 from typing import Any, Dict, List, Optional
# 4 from pathlib import Path
# 5 from uuid import UUID, uuid4
# 6 from pydantic import BaseModel, Field, field_validator
# 7 
# 8 from app.enums.fsm_enums import DECISION_TYPE, STATE_TYPE, TRANSITION_REASON_TYPE
# 9 from app.enums.logging_enums import ERROR_TYPE, PROVIDER_TYPE
# 10 from app.enums.scoring_enums import SCORING_METRIC_TYPE
# 11 from app.enums.controller_enums import CONTROLLER
# 12 from app.enums.system_enums import SYSTEM
# 13 from app.enums.state_enums import STATE
# 14 from app.enums.agent_enums import AGENT
# 15 from app.enums.agent_engine_enums import AGENT_ENGINE_MODEL
# 16 
# 17 
# 18 class AgentPromptSchema(BaseModel):
# 19     id: Optional[int] = None
# 20     guid: UUID = Field(default_factory=uuid4)
# 21     name: str
# 22     description: Optional[str] = None
# 23     artifact_path: Path
# 24     tags: Optional[List[str]] = None
# 25 
# 26     @field_validator("artifact_path")
# 27     @classmethod
# 28     def validate_artifact_path(cls, v: Path) -> Path:
# 29         if not v.is_absolute() and ".." in v.parts:
# 30             raise ValueError("Invalid artifact path")
# 31         return v
# 32 
# 33 class SystemPromptSchema(BaseModel):
# 34     id: Optional[int] = None
# 35     guid: UUID = Field(default_factory=uuid4)
# 36     name: str
# 37     system_type: SYSTEM
# 38     description: Optional[str] = None
# 39     artifact_path: Path
# 40     tags: Optional[List[str]] = None
# 41 
# 42     @field_validator("artifact_path")
# 43     @classmethod
# 44     def validate_artifact_path(cls, v: Path) -> Path:
# 45         if not v.is_absolute() and ".." in v.parts:
# 46             raise ValueError("Invalid artifact path")
# 47         return v
# 48 
# 49 class PromptProviderConfig(BaseModel):
# 50     id: Optional[int] = None
# 51     guid: UUID = Field(default_factory=uuid4)
# 52     name: str
# 53     description: Optional[str] = None
# 54     artifact_path: Path
# 55     config: Optional[Dict[str, Any]] = None
# 56     tags: Optional[List[str]] = Field(default_factory=list)
# 57 
# 58     @field_validator("artifact_path")
# 59     @classmethod
# 60     def validate_artifact_path(cls, v: Path) -> Path:
# 61         if not v.is_absolute() and ".." in v.parts:
# 62             raise ValueError("Invalid artifact path")
# 63         return v
# 64 
# 65 class ToolProviderConfig(BaseModel):
# 66     id: Optional[int] = None
# 67     guid: UUID = Field(default_factory=uuid4)
# 68     name: str
# 69     description: Optional[str] = None
# 70     config: Optional[Dict[str, Any]] = None
# 71     artifact_path: Path
# 72     tags: Optional[List[str]] = None
# 73 
# 74     @field_validator("artifact_path")
# 75     @classmethod
# 76     def validate_artifact_path(cls, v: Path) -> Path:
# 77         if not v.is_absolute() and ".." in v.parts:
# 78             raise ValueError("Invalid artifact path")
# 79         return v
# 80 
# 81 class ScoreOutputSchema(BaseModel):
# 82     name: SCORING_METRIC_TYPE = Field(..., description="The type of scoring metric used")
# 83     value: float = Field(..., description="Final weighted score (normalized 0.0–1.0 scale)")
# 84     components: Dict[str, float] = Field(..., description="Tool/component-specific scores")
# 85     summary: Optional[str] = None
# 86 
# 87 
# 88 class ScoreProviderConfig(BaseModel):
# 89     id: Optional[int] = None
# 90     guid: UUID = Field(default_factory=uuid4)
# 91     name: str
# 92     description: Optional[str] = None
# 93     artifact_path: Optional[Path] = None
# 94     tags: Optional[List[str]] = None
# 95     table_name: str = "score_provider"
# 96 
# 97     @field_validator("artifact_path")
# 98     @classmethod
# 99     def validate_artifact_path(cls, v: Optional[Path]) -> Optional[Path]:
# 100         if v is None:
# 101             return v
# 102         if not v.is_absolute() and ".." in v.parts:
# 103             raise ValueError("Invalid artifact path")
# 104         return v
# 105         
# 106 class AgentEngineProviderConfig(BaseModel):
# 107     id: Optional[int] = None
# 108     guid: UUID = Field(default_factory=uuid4)
# 109     name: str
# 110     description: Optional[str] = None
# 111     model: AGENT_ENGINE_MODEL  #  ✅ Enum enforced
# 112     config: Optional[Dict[str, Any]] = None
# 113     cost_per_1k_tokens: Optional[float] = Field(default=0.0)
# 114     artifact_path: Path
# 115     tags: Optional[List[str]] = None
# 116 
# 117     @field_validator("artifact_path")
# 118     @classmethod
# 119     def validate_artifact_path(cls, v: Path) -> Path:
# 120         if not v.is_absolute() and ".." in v.parts:
# 121             raise ValueError("Invalid artifact path")
# 122         return v
# 123 
# 124 
# 125 class AgentProviderConfigSchema(BaseModel):
# 126     id: Optional[int] = None
# 127     guid: UUID = Field(default_factory=uuid4)
# 128     name: str
# 129     description: Optional[str] = None
# 130     config: Optional[Dict[str, Any]] = None
# 131     artifact_path: Path
# 132     tags: Optional[list[str]] = None
# 133     agent_type: AGENT = AGENT.UNKNOWN 
# 134 
# 135     @field_validator("artifact_path")
# 136     @classmethod
# 137     def validate_artifact_path(cls, v: Path) -> Path:
# 138         if not v.is_absolute() and ".." in v.parts:
# 139             raise ValueError("Invalid artifact path")
# 140         return v
# 141 
# 142 class StateProviderConfigSchema(BaseModel):
# 143     id: Optional[int] = None
# 144     guid: UUID = Field(default_factory=uuid4)
# 145     name: str
# 146     description: Optional[str] = None
# 147     config: Optional[Dict[str, Any]] = None
# 148     artifact_path: Path
# 149     tags: Optional[list[str]] = None
# 150 
# 151     @classmethod
# 152     def validate_artifact_path(cls, v: Path) -> Path:
# 153         if not v.is_absolute() and ".." in v.parts:
# 154             raise ValueError("Invalid artifact path")
# 155         return v
# 156 
# 157 class SystemProviderConfigSchema(BaseModel):
# 158     id: Optional[int] = None
# 159     guid: UUID = Field(default_factory=uuid4)
# 160     name: str
# 161     description: Optional[str] = None
# 162     config: Optional[Dict[str, Any]] = None
# 163     artifact_path: Path
# 164     tags: Optional[list[str]] = None
# 165 
# 166     @classmethod
# 167     def validate_artifact_path(cls, v: Path) -> Path:
# 168         if not v.is_absolute() and ".." in v.parts:
# 169             raise ValueError("Invalid artifact path")
# 170         return v
# 171 
# 172 class ControllerProviderConfigSchema(BaseModel):
# 173     id: Optional[int] = None
# 174     guid: UUID = Field(default_factory=uuid4)
# 175     name: str
# 176     description: Optional[str] = None
# 177     config: Optional[Dict[str, Any]] = None
# 178     artifact_path: Path
# 179     tags: Optional[list[str]] = None
# 180 
# 181     @classmethod
# 182     def validate_artifact_path(cls, v: Path) -> Path:
# 183         if not v.is_absolute() and ".." in v.parts:
# 184             raise ValueError("Invalid artifact path")
# 185         return v
# 186 
# 187 class ProgramProviderConfigSchema(BaseModel):
# 188     id: Optional[int] = None
# 189     guid: UUID = Field(default_factory=uuid4)
# 190     name: str
# 191     description: Optional[str] = None
# 192     config: Optional[Dict[str, Any]] = None
# 193     artifact_path: Path
# 194     tags: Optional[List[str]] = None
# 195 
# 196     @classmethod
# 197     def validate_artifact_path(cls, v: Path) -> Path:
# 198         if not v.is_absolute() and ".." in v.parts:
# 199             raise ValueError("Invalid artifact path")
# 200         return v
# 201 
# 202 class SessionConfigSchema(BaseModel):
# 203     id: Optional[int] = None
# 204     session_id: str
# 205     name: str
# 206     description: Optional[str] = None
# 207     environment_type: str  #  e.g., "experiment", "live", "staging"
# 208     config: Optional[Dict[str, Any]] = None
# 209     tags: Optional[List[str]] = None
# 210     
# 211 
# 212 #  LOGS
# 213 
# 214 @dataclass
# 215 class ProviderLogSchema:
# 216     session_id: str
# 217     provider_id: int
# 218     provider_type: PROVIDER_TYPE
# 219     input: Optional[str] = None
# 220     output: Optional[str] = None
# 221     output_schema: Optional[str] = None
# 222     latency_ms: Optional[int] = None
# 223     config_hash: Optional[str] = None
# 224     file_name: Optional[str] = None
# 225     timestamp: datetime = field(default_factory=lambda: datetime.now(timezone.utc))
# 226     called_by_type: Optional[PROVIDER_TYPE] = None
# 227     called_by_id: Optional[int] = None
# 228     run_id: str = field(default_factory=lambda: str(uuid4()))
# 229 
# 230 @dataclass
# 231 class StateTransitionLogSchema:
# 232     session_id: str
# 233     entity_type: PROVIDER_TYPE  #  e.g., "StateProvider", "SystemProvider", etc.
# 234     entity_id: int
# 235     from_state: str
# 236     to_state: str
# 237     reason: TRANSITION_REASON_TYPE
# 238     decision: DECISION_TYPE = DECISION_TYPE.UNKNOWN
# 239     triggered_by: str | None = None
# 240     step: int | None = None
# 241     transition_metadata: dict | None = None
# 242     timestamp: datetime = field(default_factory=lambda: datetime.now(timezone.utc))
# 243     run_id: str = field(default_factory=lambda: str(uuid4()))
# 244     called_by_type: PROVIDER_TYPE | None = None
# 245     called_by_id: int | None = None 
# 246 
# 247 @dataclass
# 248 class AgentConversationLogSchema:
# 249     session_id: str
# 250     system: SYSTEM
# 251     agent_type: AGENT
# 252     agent_provider_config_id: int
# 253     content: str
# 254     timestamp: datetime = field(default_factory=lambda: datetime.now(timezone.utc))
# 255     run_id: str = field(default_factory=lambda: str(uuid4()))
# 256 
# 257 @dataclass
# 258 class ErrorLogSchema:
# 259     session_id: str
# 260     error_type: ERROR_TYPE
# 261     message: str
# 262     file_path: str | None = None
# 263     provider_id: int | None = None
# 264     provider_type: PROVIDER_TYPE = PROVIDER_TYPE.UNKNOWN
# 265     timestamp: datetime = field(default_factory=lambda: datetime.now(timezone.utc))
# 266     latency_ms: Optional[int] = None
# 267     called_by_type: Optional[PROVIDER_TYPE] = None
# 268     called_by_id: Optional[int] = None
# 269     run_id: str = field(default_factory=lambda: str(uuid4()))
# 270     
# 271 @dataclass
# 272 class SnapshotMetricsSchema:
# 273     session_id: str
# 274     snapshot_id: str
# 275     system: SYSTEM
# 276     agent: str
# 277     agent_type: AGENT 
# 278     agent_id: int
# 279     score: float
# 280     state: str  #  could be FSM state, usually freeform like "generate" or "stability"
# 281     decision: DECISION_TYPE
# 282     timestamp: datetime
# 283 
# 284     line_count_before: int
# 285     line_count_after: int
# 286     function_count_before: int
# 287     function_count_after: int
# 288     symbol_count_before: int
# 289     symbol_count_after: int
# 290     branch_count_before: int
# 291     branch_count_after: int
# 292     comment_count_before: int
# 293     comment_count_after: int
# 294 
# 295     line_count_delta: int
# 296     function_count_delta: int
# 297     symbol_count_delta: int
# 298     branch_count_delta: int
# 299     comment_count_delta: int
# 300 
# 301 #  Provider output schemas (for logs)
# 302 
# 303 class ToolOutputSchema(BaseModel):
# 304     return_code: int
# 305     stdout: Optional[str] = None
# 306     stderr: Optional[str] = None
# 307     violations: Optional[List[str]] = None  #  for linters like ruff
# 308     metrics: Optional[Dict[str, Any]] = None  #  for tools like sonarcloud
# 309     summary: Optional[str] = None  #  optional human-readable summary
# 310 
# 311 
# 312 class AgentEngineOutput(BaseModel):
# 313     response: str
# 314     token_count: int
# 315     cost_usd: float
# 316     snapshot_id: Optional[str] = None
# 317     summary: Optional[str] = None
# 318 
# 319 
# 320 class AgentOutputSchema(BaseModel):
# 321     decision: DECISION_TYPE = DECISION_TYPE.UNKNOWN
# 322     score: Optional[float] = None
# 323     response: Optional[str] = None
# 324     log: Optional[str] = None
# 325     file_path: Optional[str] = None
# 326 
# 327 
# 328 class ContextOutputSchema(BaseModel):
# 329     context: Dict[str, Any]
# 330     summary: Optional[str] = None
# 331 
# 332 
# 333 class PromptOutputSchema(BaseModel):
# 334     prompt: str
# 335     summary: Optional[str] = None
# 336 
# 337 
# 338 class FSMOutputSchema(BaseModel):
# 339     state: STATE
# 340     previous_state: Optional[STATE] = None
# 341     state_type: STATE_TYPE = STATE_TYPE.INTERMEDIATE
# 342     decision: Optional[DECISION_TYPE] = DECISION_TYPE.UNKNOWN
# 343     steps: int = Field(..., ge=0)
# 344     max_steps: int = Field(..., gt=0)
# 345     summary: Optional[str] = None
# 346     output: Optional[Dict[str, Any]] = None
# 347     provider_name: Optional[str] = None
# 348 
# 349 class SystemOutputSchema(FSMOutputSchema): pass
# 350 
# 351 class StateOutputSchema(FSMOutputSchema): 
# 352     previous_state: Optional[AGENT] = None
# 353 class ProgramOutputSchema(FSMOutputSchema):
# 354     previous_state: Optional[CONTROLLER] = None
# 355 class ControllerOutputSchema(FSMOutputSchema): 
# 356     previous_state: Optional[SYSTEM] = None
# 357 
# 358 
# 359 #  DTOs
# 360 
# 361 class Snapshot(BaseModel):
# 362     timestamp: datetime
# 363     file: str
# 364     score: float
# 365     decision: DECISION_TYPE
# 366 
# 367 class SystemState(BaseModel):
# 368     system: str
# 369     file_path: str
# 370     working_file: str
# 371     final_file: Optional[str] = None
# 372     snapshots: List[Snapshot]
# 373     state: Optional[str] = "active"
# 374 
# 375 


# db\seed.py

# 1 from sqlalchemy.orm import Session
# 2 from sqlalchemy import create_engine
# 3 from pathlib import Path
# 4 from app.db.seeders.seed_controller_providers import seed_orchestrator_providers
# 5 from app.db.seeders.seed_state_providers import seed_state_providers
# 6 from app.db.seeders.seed_agent_engine_providers import seed_agent_engine_providers
# 7 from app.db.seeders.seed_context_providers import seed_context_providers
# 8 from app.db.seeders.seed_prompt_providers import seed_prompt_providers
# 9 from app.db.seeders.seed_score_providers import seed_score_providers
# 10 from app.db.seeders.seed_tool_providers import seed_tool_providers
# 11 from app.db.seeders.seed_prompts import seed_prompts
# 12 
# 13 #  Ensure your project root matches explicitly
# 14 PROJECT_ROOT = Path(__file__).resolve().parents[2]
# 15 DB_PATH = PROJECT_ROOT / "experiments" / "codecritic.sqlite3"
# 16 EXTENSIONS_DIR = PROJECT_ROOT / "extensions"
# 17 
# 18 def main():
# 19     engine = create_engine(f"sqlite:///{DB_PATH}")
# 20     with Session(bind=engine) as session:
# 21         seed_prompts(session)
# 22         seed_prompt_providers(session)
# 23         seed_tool_providers(session)
# 24         seed_score_providers(session)
# 25         seed_context_providers(session)
# 26         seed_agent_engine_providers(session)
# 27         seed_state_providers(session)
# 28         seed_orchestrator_providers(session)
# 29 
# 30     print(f"Database seeded successfully at {DB_PATH.resolve()}")
# 31 
# 32 if __name__ == "__main__":
# 33     main()


# db\seeders\__init__.py



# db\seeders\files\__init__.py



# db\seeders\files\agent_engine_providers\__init__.py



# db\seeders\files\agent_engine_providers\basic_agent_engine_provider.py

# 1 from app.db.schemas import AgentEngineOutput
# 2 from app.providers.agent_engine_provider_base import AgentEngineProviderBase
# 3 
# 4 class BasicAgentEngineProvider(AgentEngineProviderBase):
# 5     def _run(self, input: dict) -> AgentEngineOutput:
# 6         response = f"[Basic Response] You said: {input.get('prompt', '')}"
# 7         token_count = len(response.split())
# 8         cost_usd = token_count * (self._config.cost_per_1k_tokens or 0.0) / 1000
# 9         return AgentEngineOutput(response=response, token_count=token_count, cost_usd=cost_usd)


# db\seeders\files\agent_engine_providers\claude_3_sonnet_agent_engine_provider.py

# 1 import os, json, boto3
# 2 from app.db.schemas import AgentEngineOutput
# 3 from app.providers.agent_engine_provider_base import AgentEngineProviderBase
# 4 
# 5 class Claude3SonnetAgentEngineProvider(AgentEngineProviderBase):
# 6     def _run(self, input: dict) -> AgentEngineOutput:
# 7         prompt = input["prompt"]
# 8         region = os.getenv("AWS_REGION", "us-east-1")
# 9 
# 10         client = boto3.client("bedrock-runtime", region_name=region)
# 11         body = {
# 12             "anthropic_version": "bedrock-2023-05-31",
# 13             "messages": [{"role": "user", "content": prompt}],
# 14             "max_tokens": 1024,
# 15         }
# 16 
# 17         response_raw = client.invoke_model(
# 18             modelId="anthropic.claude-3-sonnet-20240229-v1:0",
# 19             contentType="application/json",
# 20             accept="application/json",
# 21             body=json.dumps(body),
# 22         )
# 23 
# 24         content = json.loads(response_raw["body"].read())["content"][0]["text"].strip()
# 25         token_count = len(content.split())
# 26         cost_usd = token_count * (self._config.cost_per_1k_tokens or 0.0) / 1000
# 27 
# 28         return AgentEngineOutput(response=content, token_count=token_count, cost_usd=cost_usd)


# db\seeders\files\agent_engine_providers\gemini_1_5_pro_agent_engine_provider.py

# 1 import os, requests
# 2 from app.db.schemas import AgentEngineOutput
# 3 from app.providers.agent_engine_provider_base import AgentEngineProviderBase
# 4 
# 5 class Gemini1_5ProAgentEngineProvider(AgentEngineProviderBase):
# 6     def _run(self, input: dict) -> AgentEngineOutput:
# 7         prompt = input["prompt"]
# 8         api_key = os.getenv("GOOGLE_API_KEY")
# 9         if not api_key:
# 10             raise EnvironmentError("GOOGLE_API_KEY is not set in the environment.")
# 11 
# 12         url = (
# 13             "https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-pro:generateContent"
# 14             f"?key={api_key}"
# 15         )
# 16         response_raw = requests.post(
# 17             url,
# 18             headers={"Content-Type": "application/json"},
# 19             json={"contents": [{"parts": [{"text": prompt}]}]},
# 20         )
# 21         response_raw.raise_for_status()
# 22         content = response_raw.json()["candidates"][0]["content"]["parts"][0]["text"].strip()
# 23 
# 24         token_count = len(content.split())
# 25         cost_usd = token_count * (self._config.cost_per_1k_tokens or 0.0) / 1000
# 26 
# 27         return AgentEngineOutput(response=content, token_count=token_count, cost_usd=cost_usd)


# db\seeders\files\agent_engine_providers\openai_gpt4o_agent_engine_provider.py

# 1 import json
# 2 import os
# 3 from openai import OpenAI
# 4 from app.db.schemas import AgentEngineOutput
# 5 from app.providers.agent_engine_provider_base import AgentEngineProviderBase
# 6 
# 7 def _safe_prompt_content(content):
# 8     if isinstance(content, str):
# 9         return content
# 10     if hasattr(content, "model_dump_json"):
# 11         return content.model_dump_json()
# 12     if isinstance(content, dict):
# 13         return json.dumps(content)
# 14     return str(content)
# 15 
# 16 class OpenAIGPT4oAgentEngineProvider(AgentEngineProviderBase):
# 17     def _run(self, input: dict) -> AgentEngineOutput:
# 18         prompt = input["prompt"]
# 19         api_key = os.getenv("OPENAI_API_KEY")
# 20         if not api_key:
# 21             raise EnvironmentError("OPENAI_API_KEY is not set in the environment.")
# 22 
# 23         client = OpenAI(api_key=api_key)
# 24         response = client.chat.completions.create(
# 25             model="gpt-4o",
# 26             messages = [
# 27                 {"role": "system", "content": "You are a helpful assistant."},
# 28                 {"role": "user", "content": _safe_prompt_content(prompt)},
# 29             ],
# 30             temperature=0.2,
# 31             max_tokens=2048,
# 32         )
# 33 
# 34         content = response.choices[0].message.content.strip()
# 35         token_count = len(content.split())
# 36         cost_usd = token_count * (self._config.cost_per_1k_tokens or 0.0) / 1000
# 37 
# 38         return AgentEngineOutput(response=content, token_count=token_count, cost_usd=cost_usd)


# db\seeders\files\agent_providers\__init__.py



# db\seeders\files\agent_providers\basic_agent_provider.py

# 1 from pathlib import Path
# 2 from app.providers.agent_provider_base import AgentProviderBase
# 3 from app.db.schemas import AgentOutputSchema
# 4 
# 5 
# 6 class BasicAgentProvider(AgentProviderBase):
# 7     def _run(self, input: dict) -> AgentOutputSchema:
# 8         raw_file_path = input.get("before") or input.get("file_name")
# 9         relative_file_path = None
# 10 
# 11         if raw_file_path:
# 12             try:
# 13                 relative_file_path = str(Path(raw_file_path).resolve().relative_to(Path.cwd()))
# 14             except ValueError:
# 15                 relative_file_path = str(Path(raw_file_path).resolve())
# 16 
# 17         response = (
# 18             "[AGENT_DECISION]accept[/AGENT_DECISION]\n"
# 19             "[CONVERSATION_LOG_ENTRY]Basic agent executed successfully.[/CONVERSATION_LOG_ENTRY]\n"
# 20             f"[CODE]{input.get('code', '#  no code provided')}[/CODE]"
# 21         )
# 22 
# 23         return AgentOutputSchema(
# 24             response=response,
# 25             log="Basic agent executed successfully.",
# 26             decision="accept",
# 27             snapshot_id=None,
# 28             file_path=relative_file_path,
# 29         )


# db\seeders\files\agent_providers\code_stability_agent_provider.py

# 1 from pathlib import Path
# 2 from app.providers.agent_provider_base import AgentProviderBase
# 3 from app.db.schemas import AgentOutputSchema
# 4 
# 5 
# 6 class CodeStabilityAgentProvider(AgentProviderBase):
# 7     def _run(self, input: dict) -> AgentOutputSchema:
# 8         score_result = self._score_provider.run(input, session_id=input["session_id"])
# 9 
# 10         raw_file_path = input.get("file_path") or input.get("file_name") or input.get("before")
# 11         relative_file_path = None
# 12         if raw_file_path:
# 13             try:
# 14                 relative_file_path = str(Path(raw_file_path).resolve().relative_to(Path.cwd()))
# 15             except ValueError:
# 16                 relative_file_path = str(Path(raw_file_path).resolve())
# 17 
# 18         if score_result.value >= 1.0:
# 19             return AgentOutputSchema(
# 20                 response="""[AGENT_DECISION]accept[/AGENT_DECISION]
# 21 [CONVERSATION_LOG_ENTRY]Code passed all stability checks.[/CONVERSATION_LOG_ENTRY]""",
# 22                 log="Code passed all stability checks.",
# 23                 decision="accept",
# 24                 snapshot_id=None,
# 25                 file_path=relative_file_path,
# 26                 score=score_result.value
# 27             )
# 28 
# 29         return AgentOutputSchema(
# 30             response=f"""[AGENT_DECISION]reject[/AGENT_DECISION]
# 31 [CONVERSATION_LOG_ENTRY]Failed stability checks: {score_result.components}[/CONVERSATION_LOG_ENTRY]""",
# 32             log=f"Failed stability checks: {score_result.components}",
# 33             decision="reject",
# 34             snapshot_id=None,
# 35             file_path=relative_file_path,
# 36             score=score_result.value
# 37         )


# db\seeders\files\agent_providers\linting_discriminator_agent_provider.py

# 1 from __future__ import annotations
# 2 from pathlib import Path
# 3 import uuid
# 4 
# 5 from app.providers.agent_provider_base import AgentProviderBase
# 6 from app.db.schemas import AgentOutputSchema
# 7 from app.utilities.metadata.snapshots.snapshot_reader import read_latest_snapshot
# 8 from app.utilities.diff_utils import summarize_diff
# 9 
# 10 LINTING_PASS_THRESHOLD = 0.85
# 11 
# 12 class LintingDiscriminatorAgentProvider(AgentProviderBase):
# 13     def _run(self, input: dict) -> AgentOutputSchema:
# 14         snapshot = read_latest_snapshot(session_id=self._session_id)
# 15 
# 16         if not snapshot:
# 17             return AgentOutputSchema(
# 18                 decision="reject",
# 19                 log="No snapshot found.",
# 20                 response=(
# 21                     "[AGENT_DECISION]reject[/AGENT_DECISION]\n"
# 22                     "[CONVERSATION_LOG_ENTRY]No snapshot found.[/CONVERSATION_LOG_ENTRY]"
# 23                 ),
# 24                 file_path=None,
# 25                 score=None
# 26             )
# 27 
# 28         before_code = snapshot["before"]
# 29         after_code = snapshot["after"]
# 30         before_path = Path(snapshot["before_path"]).resolve()
# 31         after_path = Path(snapshot["after_path"]).resolve()
# 32 
# 33         before_score = self._score_provider.run({"file_path": str(before_path)}, session_id=self._session_id).value
# 34         after_score = self._score_provider.run({"file_path": str(after_path)}, session_id=self._session_id).value
# 35 
# 36         def safe_relative(path: Path) -> str:
# 37             try:
# 38                 return str(path.relative_to(Path.cwd()))
# 39             except ValueError:
# 40                 return str(path)
# 41 
# 42         if before_code == after_code:
# 43             decision = "accept" if before_score >= LINTING_PASS_THRESHOLD else "reject"
# 44             log_msg = (
# 45                 "No meaningful change, but score already passing."
# 46                 if decision == "accept"
# 47                 else "No meaningful change and score below threshold."
# 48             )
# 49             return AgentOutputSchema(
# 50                 decision=decision,
# 51                 log=log_msg,
# 52                 response=(
# 53                     f"[AGENT_DECISION]{decision}[/AGENT_DECISION]\n"
# 54                     f"[CONVERSATION_LOG_ENTRY]{log_msg}[/CONVERSATION_LOG_ENTRY]"
# 55                 ),
# 56                 file_path=safe_relative(before_path),
# 57                 score=before_score
# 58             )
# 59 
# 60         summary = summarize_diff(before_code, after_code).strip()
# 61         if not summary:
# 62             decision = "accept" if before_score >= LINTING_PASS_THRESHOLD else "reject"
# 63             log_msg = (
# 64                 "Diff could not be summarized, but prior version passes."
# 65                 if decision == "accept"
# 66                 else "Diff could not be summarized and prior version fails."
# 67             )
# 68             return AgentOutputSchema(
# 69                 decision=decision,
# 70                 log=log_msg,
# 71                 response=(
# 72                     f"[AGENT_DECISION]{decision}[/AGENT_DECISION]\n"
# 73                     f"[CONVERSATION_LOG_ENTRY]{log_msg}[/CONVERSATION_LOG_ENTRY]"
# 74                 ),
# 75                 file_path=safe_relative(before_path),
# 76                 score=before_score
# 77             )
# 78 
# 79         if after_score > before_score and after_score >= LINTING_PASS_THRESHOLD:
# 80             decision = "accept"
# 81             chosen_code = after_code
# 82             score = after_score
# 83         elif before_score >= LINTING_PASS_THRESHOLD:
# 84             decision = "accept"
# 85             chosen_code = before_code
# 86             score = before_score
# 87         else:
# 88             decision = "reject"
# 89             chosen_code = after_code
# 90             score = after_score
# 91 
# 92         output_path = Path("working_files") / f"{uuid.uuid4().hex}.py"
# 93         output_path.parent.mkdir(parents=True, exist_ok=True)
# 94         output_path.write_text(chosen_code, encoding="utf-8")
# 95 
# 96         return AgentOutputSchema(
# 97             decision=decision,
# 98             log=summary,
# 99             response=(
# 100                 f"[AGENT_DECISION]{decision}[/AGENT_DECISION]\n"
# 101                 f"[CONVERSATION_LOG_ENTRY]{summary}[/CONVERSATION_LOG_ENTRY]"
# 102             ),
# 103             file_path=safe_relative(output_path),
# 104             score=score
# 105         )


# db\seeders\files\agent_providers\linting_generator_agent_provider.py

# 1 from pathlib import Path
# 2 from app.providers.agent_provider_base import AgentProviderBase
# 3 from app.db.schemas import AgentOutputSchema
# 4 
# 5 
# 6 class LintingGeneratorAgentProvider(AgentProviderBase):
# 7     """Runs a generation round using the linting system prompt, context, and snapshot."""
# 8     def _run(self, input: dict) -> AgentOutputSchema:
# 9         session_id = self._session_id
# 10         file_path = input["file_path"]
# 11         system = input.get("system", "linting")
# 12 
# 13         if not self._prompt_provider:
# 14             raise ValueError("Prompt provider is not set")
# 15         if not self._agent_engine:
# 16             raise ValueError("Agent engine is not set")
# 17 
# 18         final_prompt = self._prompt_provider.run(
# 19             input=input,
# 20             session_id=session_id
# 21         )
# 22 
# 23         engine_output = self._agent_engine.run(
# 24             input={
# 25                 "prompt": final_prompt,
# 26                 "before": file_path,
# 27                 "agent_type": self._config.agent_type,
# 28                 "agent_id": self._config.id,
# 29                 "system": system,
# 30                 "state_context": input.get("state_context", {}),
# 31             },
# 32             session_id=session_id
# 33         )
# 34 
# 35         response = engine_output.response
# 36         decision = (
# 37             "accept" if "[AGENT_DECISION]accept" in response else
# 38             "reject" if "[AGENT_DECISION]reject" in response else
# 39             "unknown"
# 40         )
# 41 
# 42         log = None
# 43         if "[CONVERSATION_LOG_ENTRY]" in response:
# 44             start = response.find("[CONVERSATION_LOG_ENTRY]") + len("[CONVERSATION_LOG_ENTRY]")
# 45             end = response.find("[/CONVERSATION_LOG_ENTRY]")
# 46             log = response[start:end].strip() if start < end else None
# 47 
# 48         code = self._extract_block(response, "[CODE]", "[/CODE]")
# 49         log_entry = self._extract_block(response, "[CONVERSATION_LOG_ENTRY]", "[/CONVERSATION_LOG_ENTRY]")
# 50 
# 51         if decision == "unknown" and code and log_entry:
# 52             decision = "accept"
# 53             self._log.debug("✅ Generator decision inferred as 'accept' based on presence of code and log.")
# 54         elif decision == "unknown":
# 55             self._log.warning("⚠️ Generator decision remained 'unknown'; [AGENT_DECISION] tag may be missing.")
# 56 
# 57         #  Ensure relative path if file_path is present
# 58         relative_file_path = None
# 59         if hasattr(engine_output, "file_path") and engine_output.file_path:
# 60             try:
# 61                 relative_file_path = str(Path(engine_output.file_path).resolve().relative_to(Path.cwd()))
# 62             except ValueError:
# 63                 relative_file_path = str(engine_output.file_path)
# 64 
# 65         return AgentOutputSchema(
# 66             response=response,
# 67             log=log,
# 68             decision=decision,
# 69             snapshot_id=engine_output.snapshot_id,
# 70             file_path=relative_file_path,
# 71             score=engine_output.score if hasattr(engine_output, "score") else None
# 72         )


# db\seeders\files\context_providers\__init__.py



# db\seeders\files\context_providers\basic_context_provider.py

# 1 from app.providers.context_provider_base import ContextProviderBase
# 2 from app.db.schemas import ContextOutputSchema
# 3 
# 4 class BasicContextProvider(ContextProviderBase):
# 5     def _run(self, input: dict) -> ContextOutputSchema:
# 6         return ContextOutputSchema(
# 7             context={"file_path": input.get("file_path")},
# 8             summary="Basic context with file_path only."
# 9         )


# db\seeders\files\context_providers\linting_context_provider.py

# 1 from __future__ import annotations
# 2 from pathlib import Path
# 3 import json
# 4 
# 5 from sqlalchemy.orm import Session
# 6 from app.providers.context_provider_base import ContextProviderBase
# 7 from app.utilities.metadata.logging.conversation_log import get_conversation_log
# 8 from app.db.schemas import ContextOutputSchema
# 9 
# 10 
# 11 class LintingContextProvider(ContextProviderBase):
# 12     def _run(self, input: dict) -> ContextOutputSchema:
# 13         file_path = Path(input["file_path"]).resolve()
# 14         system = input["system"]
# 15 
# 16         if not self._session_id:
# 17             raise ValueError("ContextProvider requires session_id to be set on self")
# 18 
# 19         if not file_path.exists():
# 20             raise FileNotFoundError(f"❌ File not found: {file_path}")
# 21 
# 22         source_code = file_path.read_text(encoding="utf-8")
# 23 
# 24         score_result = self.score_provider.run(
# 25             input={"file_path": str(file_path)},
# 26             session_id=self._session_id
# 27         )
# 28 
# 29         with Session(bind=self._engine) as session:
# 30             convo_log = get_conversation_log(session, session_id=self._session_id, system=system)
# 31 
# 32         context = {
# 33             "file_path": str(file_path),
# 34             "source_code": source_code,
# 35             "score": score_result.model_dump(),  #  No 'pass' key inserted
# 36             "conversation_log": convo_log
# 37         }
# 38 
# 39         return ContextOutputSchema(
# 40             context=context,
# 41             summary=f"Context for {file_path.name}, {score_result.value} score, {len(convo_log)} log entries"
# 42         )


# db\seeders\files\controller_providers\__init__.py



# db\seeders\files\controller_providers\preprocessing_controller_provider.py

# 1 from app.enums.fsm_enums import DECISION_TYPE, TRANSITION_REASON_TYPE
# 2 from app.enums.system_enums import SYSTEM
# 3 from app.providers.controller_provider_base import ControllerProviderBase
# 4 
# 5 class PreprocessingControllerProvider(ControllerProviderBase):
# 6     def _transition(self, state, sys_output):
# 7         current = SYSTEM(state["state"])
# 8         transition = {}
# 9 
# 10         if current == SYSTEM.START:
# 11             transition = {
# 12                 "state": SYSTEM.LINTING,
# 13                 "reason": TRANSITION_REASON_TYPE.INITIALIZATION,
# 14             }
# 15         elif current == SYSTEM.LINTING:
# 16             transition = {
# 17                 "state": SYSTEM.END,
# 18                 "reason": TRANSITION_REASON_TYPE.SUCCESSFUL,
# 19             }
# 20         else:
# 21             transition = {
# 22                 "state": SYSTEM.END,
# 23                 "reason": TRANSITION_REASON_TYPE.CUSTOM_RULE,
# 24             }
# 25 
# 26         #  🔁 Propagate outputs from system FSM (without score)
# 27         if sys_output:
# 28             if hasattr(sys_output, "output") and isinstance(sys_output.output, dict):
# 29                 output_dict = sys_output.output
# 30                 transition["file_path"] = output_dict.get("file_path", state.get("file_path"))
# 31                 transition["decision"] = output_dict.get("decision", DECISION_TYPE.UNKNOWN)
# 32 
# 33         return transition


# db\seeders\files\program_providers\__init__.py



# db\seeders\files\program_providers\codecritic_program_provider.py

# 1 from app.enums.controller_enums import CONTROLLER
# 2 from app.enums.fsm_enums import DECISION_TYPE, TRANSITION_REASON_TYPE
# 3 from app.providers.program_provider_base import ProgramProviderBase
# 4 
# 5 class CodeCriticProgramProvider(ProgramProviderBase):
# 6     def _transition(self, state, ctrl_output):
# 7         current = CONTROLLER(state["state"])
# 8         transition = {}
# 9 
# 10         if current == CONTROLLER.START:
# 11             transition = {
# 12                 "state": CONTROLLER.PREPROCESSING,
# 13                 "reason": TRANSITION_REASON_TYPE.INITIALIZATION,
# 14             }
# 15 
# 16         elif current == CONTROLLER.PREPROCESSING:
# 17             transition = {
# 18                 "state": CONTROLLER.END,
# 19                 "reason": TRANSITION_REASON_TYPE.SUCCESSFUL,
# 20             }
# 21 
# 22         else:
# 23             transition = {
# 24                 "state": CONTROLLER.END,
# 25                 "reason": TRANSITION_REASON_TYPE.CUSTOM_RULE,
# 26             }
# 27 
# 28         if ctrl_output:
# 29             if hasattr(ctrl_output, "output") and isinstance(ctrl_output.output, dict):
# 30                 output_dict = ctrl_output.output
# 31                 transition["file_path"] = output_dict.get("file_path") or state.get("file_path")
# 32                 transition["decision"] = output_dict.get("decision", DECISION_TYPE.UNKNOWN)
# 33 
# 34         return transition


# db\seeders\files\prompt_providers\__init__.py



# db\seeders\files\prompt_providers\basic_prompt_provider.py

# 1 from app.providers.prompt_provider_base import PromptProviderBase
# 2 from app.db.schemas import PromptOutputSchema
# 3 
# 4 class BasicPromptProvider(PromptProviderBase):
# 5     def _run(self, input: dict) -> PromptOutputSchema:
# 6         agent_config = input.get("agent_config", {})
# 7         system_config = input.get("system_config", {})
# 8 
# 9         prompt = (
# 10             f"User: {agent_config.get('input', 'No input provided')}\n"
# 11             f"System: Response mode: {system_config.get('setting', 'default')}"
# 12         )
# 13 
# 14         return PromptOutputSchema(
# 15             prompt=prompt,
# 16             summary="Basic prompt using agent and system config"
# 17         )


# db\seeders\files\prompt_providers\linting_prompt_provider.py

# 1 from __future__ import annotations
# 2 
# 3 import json
# 4 from pathlib import Path
# 5 
# 6 from app.providers.prompt_provider_base import PromptProviderBase
# 7 from app.db.schemas import PromptOutputSchema
# 8 
# 9 
# 10 class LintingPromptProvider(PromptProviderBase):
# 11     def _run(self, input: dict) -> PromptOutputSchema:
# 12         session_id = input.get("session_id")
# 13         system = input.get("system", "unknown")
# 14         file_path = input.get("file_path")
# 15 
# 16         if not session_id or not file_path:
# 17             raise ValueError("PromptProvider requires both session_id and file_path")
# 18 
# 19         if not self.agent_text or not self.system_text or not self._context_provider:
# 20             raise ValueError("Missing required prompt text or context provider")
# 21 
# 22         context_output = self._context_provider.run(
# 23             {
# 24                 "file_path": file_path,
# 25                 "system": system
# 26             },
# 27             session_id=session_id
# 28         )
# 29         context = context_output.context if hasattr(context_output, "context") else json.loads(context_output)
# 30 
# 31         convo_section = "\n".join(context.get("conversation_log", [])) or "(none)"
# 32 
# 33         full_prompt = f"""
# 34 {self.system_text}
# 35 
# 36 ---
# 37 
# 38 {self.agent_text}
# 39 
# 40 ---
# 41 
# 42 [CONTEXT]
# 43 {json.dumps(context, indent=2)}
# 44 
# 45 ---
# 46 
# 47 [CONVERSATION LOG]
# 48 {convo_section}
# 49 """.strip()
# 50 
# 51         return PromptOutputSchema(prompt=full_prompt)


# db\seeders\files\prompts\__init__.py



# db\seeders\files\score_providers\__init__.py



# db\seeders\files\score_providers\code_stability_score_provider.py

# 1 import ast
# 2 import json
# 3 import py_compile
# 4 import importlib.util
# 5 from pathlib import Path
# 6 from typing import Dict
# 7 import contextlib
# 8 import sys
# 9 import io
# 10 
# 11 from app.providers.score_provider_base import ScoreProviderBase
# 12 from app.db.schemas import ScoreOutputSchema
# 13 from app.enums.scoring_enums import SCORING_METRIC_TYPE
# 14 
# 15 
# 16 @contextlib.contextmanager
# 17 def suppress_output():
# 18     original_stdout = sys.stdout
# 19     original_stderr = sys.stderr
# 20     sys.stdout = io.StringIO()
# 21     sys.stderr = io.StringIO()
# 22     try:
# 23         yield
# 24     finally:
# 25         sys.stdout = original_stdout
# 26         sys.stderr = original_stderr
# 27 
# 28 
# 29 class CodeStabilityScoreProvider(ScoreProviderBase):
# 30     def _run(self, input: dict) -> ScoreOutputSchema:
# 31         file_name = Path(input["file_path"]).resolve()
# 32         components: Dict[str, bool] = {}
# 33 
# 34         try:
# 35             source_code = file_name.read_text(encoding="utf-8")
# 36             components["utf8_valid"] = True
# 37         except Exception:
# 38             components["utf8_valid"] = False
# 39             return self._final_score(components)
# 40 
# 41         with suppress_output():
# 42             try:
# 43                 ast.parse(source_code)
# 44                 components["syntax_ok"] = True
# 45             except SyntaxError:
# 46                 components["syntax_ok"] = False
# 47                 return self._final_score(components)
# 48 
# 49             try:
# 50                 compile(source_code, str(file_name), "exec")
# 51                 components["can_compile"] = True
# 52             except Exception:
# 53                 components["can_compile"] = False
# 54                 return self._final_score(components)
# 55 
# 56             try:
# 57                 py_compile.compile(str(file_name), doraise=True)
# 58                 components["py_compile_ok"] = True
# 59             except Exception:
# 60                 components["py_compile_ok"] = False
# 61                 return self._final_score(components)
# 62 
# 63             try:
# 64                 spec = importlib.util.spec_from_file_location("mod", str(file_name))
# 65                 module = importlib.util.module_from_spec(spec)
# 66                 spec.loader.exec_module(module)  #  type: ignore
# 67                 components["can_import"] = True
# 68             except Exception:
# 69                 components["can_import"] = False
# 70                 return self._final_score(components)
# 71 
# 72         return self._final_score(components)
# 73 
# 74     def _final_score(self, components: Dict[str, bool]) -> ScoreOutputSchema:
# 75         critical_keys = ["utf8_valid", "syntax_ok", "can_compile", "py_compile_ok", "can_import"]
# 76         passed = all(components.get(k, False) for k in critical_keys)
# 77         value = 1.0 if passed else 0.0
# 78         summary = "✅ Executable" if passed else "❌ Not executable"
# 79 
# 80         return ScoreOutputSchema(
# 81             name=SCORING_METRIC_TYPE.CODE_STABILITY_SCORE,
# 82             value=value,
# 83             components={k: float(v) for k, v in components.items()},
# 84             summary=summary
# 85         )


# db\seeders\files\score_providers\linting_score_provider.py

# 1 from app.providers.score_provider_base import ScoreProviderBase
# 2 from app.db.schemas import ScoreOutputSchema
# 3 from app.enums.scoring_enums import SCORING_METRIC_TYPE
# 4 from app.utilities.metadata.footer.code_annnotation_utils import split_code_and_notes
# 5 import json
# 6 from pathlib import Path
# 7 import uuid
# 8 
# 9 
# 10 class LintingScoreProvider(ScoreProviderBase):
# 11     def _run(self, input: dict) -> ScoreOutputSchema:
# 12         file_path = input["file_path"]
# 13         session_id = input.get("session_id", self._session_id)
# 14 
# 15         #  🧠 Materialize code to disk if file_path is code content or invalid path
# 16         if not Path(file_path).exists() or "\n" in file_path or "def " in file_path:
# 17             temp_file = Path("experiments/snapshots") / f"{uuid.uuid4().hex}.py"
# 18             temp_file.parent.mkdir(parents=True, exist_ok=True)
# 19             temp_file.write_text(file_path, encoding="utf-8")
# 20             file_path = str(temp_file)
# 21 
# 22         file_path = Path(file_path).resolve()
# 23         full_code = file_path.read_text(encoding="utf-8")
# 24         clean_code, _ = split_code_and_notes(full_code)
# 25 
# 26         #  Write stripped code to temp file for evaluation
# 27         stripped_path = Path("working_files") / f"{uuid.uuid4().hex}_stripped.py"
# 28         stripped_path.parent.mkdir(parents=True, exist_ok=True)
# 29         stripped_path.write_text(clean_code.rstrip() + "\n", encoding="utf-8")
# 30 
# 31         #  Build a dict of available tools, keyed by exactly tool.config.name.lower()
# 32         available = {tool._config.name.lower(): tool for tool in self.tool_providers}
# 33 
# 34         def run_tool(name: str, collect_violations: bool = False):
# 35             if name not in available:
# 36                 return 0.0, []
# 37 
# 38             try:
# 39                 raw = available[name].run({"target": str(stripped_path), "check": True}, session_id=session_id)
# 40                 parsed = raw.model_dump()
# 41             except Exception as e:
# 42                 return 0.0, []
# 43 
# 44             score = 1.0 if parsed.get("return_code", 0) else 0.0
# 45             if collect_violations:
# 46                 return score, parsed.get("violations", []) or []
# 47             else:
# 48                 return score, []
# 49 
# 50         #  Run each tool exactly once
# 51         ruff_score, ruff_violations = run_tool("ruff", collect_violations=True)
# 52         black_score, _ = run_tool("black", collect_violations=False)
# 53         mypy_score, _ = run_tool("mypy", collect_violations=False)
# 54 
# 55         #  Compute weighted score
# 56         weighted = round(ruff_score * 0.7 + black_score * 0.2 + mypy_score * 0.1, 3)
# 57 
# 58         components = {
# 59             "ruff": ruff_score,
# 60             "black": black_score,
# 61             "mypy": mypy_score
# 62         }
# 63 
# 64         #  Top Ruff Violations
# 65         top_violations = []
# 66         if ruff_violations:
# 67             freq = {}
# 68             for code in ruff_violations:
# 69                 freq[code] = freq.get(code, 0) + 1
# 70             sorted_codes = sorted(freq.items(), key=lambda kv: kv[1], reverse=True)
# 71             top_violations = [code for code, _ in sorted_codes[:3]]
# 72 
# 73         summary = (
# 74             f"Top Ruff Violations: {', '.join(top_violations)}"
# 75             if top_violations else "No Ruff violations"
# 76         )
# 77 
# 78         return ScoreOutputSchema(
# 79             name=SCORING_METRIC_TYPE.LINTING_SCORE,
# 80             value=weighted,
# 81             components=components,
# 82             summary=summary
# 83         )


# db\seeders\files\state_providers\__init__.py



# db\seeders\files\state_providers\basic_state_provider.py

# 1 from app.factories.base_provider_factory import BaseProviderFactory
# 2 from app.db.models import StateProviderConfig
# 3 from app.providers.state_provider_base import StateProviderBase
# 4 
# 5 class StateProviderFactory(BaseProviderFactory):
# 6     config_model = StateProviderConfig
# 7     base_class = StateProviderBase


# db\seeders\files\state_providers\code_stability_state_provider.py

# 1 from app.enums.fsm_enums import STATE_TYPE, DECISION_TYPE, TRANSITION_REASON_TYPE
# 2 from app.enums.agent_enums import AGENT
# 3 from app.providers.state_provider_base import StateProviderBase
# 4 from app.db.schemas import AgentOutputSchema
# 5 
# 6 class CodeStabilityStateProvider(StateProviderBase):
# 7     def _transition(self, state: dict, agent_output: AgentOutputSchema | None) -> dict:
# 8         current = AGENT(state.get("state"))
# 9 
# 10         if current == AGENT.START:
# 11             return {
# 12                 "state": AGENT.STABILITY,
# 13                 "reason": TRANSITION_REASON_TYPE.EVALUATING
# 14             }
# 15 
# 16         decision = getattr(agent_output, "decision", DECISION_TYPE.UNKNOWN)
# 17 
# 18         return {
# 19             "state": AGENT.END,
# 20             "state_type": STATE_TYPE.END,
# 21             "decision": decision,
# 22             "reason": (
# 23                 TRANSITION_REASON_TYPE.PASSED
# 24                 if decision == DECISION_TYPE.ACCEPT
# 25                 else TRANSITION_REASON_TYPE.FAILED
# 26             )
# 27         }


# db\seeders\files\state_providers\linting_discriminator_state_provider.py

# 1 from app.enums.fsm_enums import DECISION_TYPE, TRANSITION_REASON_TYPE
# 2 from app.enums.agent_enums import AGENT
# 3 from app.providers.state_provider_base import StateProviderBase
# 4 from app.db.schemas import AgentOutputSchema
# 5 
# 6 class LintingDiscriminatorStateProvider(StateProviderBase):
# 7     def _transition(self, state: dict, agent_output: AgentOutputSchema | None) -> dict:
# 8         if agent_output is None:
# 9             return {
# 10                 "state": AGENT.DISCRIMINATOR,
# 11                 "reason": TRANSITION_REASON_TYPE.INITIALIZATION
# 12             }
# 13 
# 14         if agent_output.decision == DECISION_TYPE.ACCEPT:
# 15             return {
# 16                 "state": AGENT.END,
# 17                 "reason": TRANSITION_REASON_TYPE.PASSED,
# 18                 "decision": DECISION_TYPE.ACCEPT,
# 19                 "file_path": agent_output.file_path,
# 20             }
# 21 
# 22         return {
# 23             "state": AGENT.END,
# 24             "reason": TRANSITION_REASON_TYPE.FAILED,
# 25             "decision": DECISION_TYPE.REJECT,
# 26             "file_path": agent_output.file_path,
# 27         }


# db\seeders\files\state_providers\linting_generator_state_provider.py

# 1 from app.enums.fsm_enums import STATE_TYPE, DECISION_TYPE, TRANSITION_REASON_TYPE
# 2 from app.enums.agent_enums import AGENT
# 3 from app.providers.state_provider_base import StateProviderBase
# 4 from app.db.schemas import AgentOutputSchema
# 5 
# 6 class LintingGeneratorStateProvider(StateProviderBase):
# 7     def _transition(self, state: dict, agent_output: AgentOutputSchema | None) -> dict:
# 8         current = AGENT(state.get("state"))
# 9         decision = getattr(agent_output, "decision", DECISION_TYPE.UNKNOWN)
# 10 
# 11         if current == AGENT.START:
# 12             return {
# 13                 "state": AGENT.GENERATOR,
# 14                 "reason": TRANSITION_REASON_TYPE.INITIALIZATION
# 15             }
# 16 
# 17         if current == AGENT.GENERATOR:
# 18             return {
# 19                 "state": AGENT.END,
# 20                 "state_type": STATE_TYPE.END,
# 21                 "decision": DECISION_TYPE.UNKNOWN,
# 22                 "reason": TRANSITION_REASON_TYPE.SUCCESSFUL
# 23             }
# 24 
# 25         return {
# 26             "state": AGENT.END,
# 27             "state_type": STATE_TYPE.END,
# 28             "decision": DECISION_TYPE.UNKNOWN,
# 29             "reason": TRANSITION_REASON_TYPE.CUSTOM_RULE
# 30         }


# db\seeders\files\system_providers\__init__.py



# db\seeders\files\system_providers\linting_system_provider.py

# 1 from pathlib import Path
# 2 import json
# 3 
# 4 from app.enums.fsm_enums import TRANSITION_REASON_TYPE
# 5 from app.enums.state_enums import STATE
# 6 from app.providers.system_provider_base import SystemProviderBase
# 7 
# 8 
# 9 class LintingSystemProvider(SystemProviderBase):
# 10     SCORE_THRESHOLD = 0.85
# 11 
# 12     def _transition(self, state: dict, state_output: dict | None) -> dict:
# 13         current = STATE(state.get("state"))
# 14         last = STATE(state.get("_last_state")) if state.get("_last_state") else None
# 15 
# 16         result = None
# 17 
# 18         if state_output and hasattr(state_output, "output"):
# 19             output_obj = state_output.output
# 20             if isinstance(output_obj, str):
# 21                 try:
# 22                     output_obj = json.loads(output_obj)
# 23                 except Exception:
# 24                     output_obj = {}
# 25 
# 26             if isinstance(output_obj, dict):
# 27                 result = output_obj.get("decision") or output_obj.get("result")
# 28             else:
# 29                 result = getattr(output_obj, "decision", None)
# 30 
# 31         transition = {}
# 32 
# 33         if current == STATE.START:
# 34             transition = {
# 35                 "state": STATE.CODE_STABILITY,
# 36                 "reason": TRANSITION_REASON_TYPE.EVALUATING
# 37             }
# 38 
# 39         elif current == STATE.CODE_STABILITY:
# 40             if last == STATE.START:
# 41                 if result == "accept":
# 42                     transition = {
# 43                         "state": STATE.GENERATE,
# 44                         "reason": TRANSITION_REASON_TYPE.PASSED,
# 45                         "file_path": state.get("file_path")
# 46                     }
# 47                 else:
# 48                     transition = {
# 49                         "state": STATE.END,
# 50                         "reason": TRANSITION_REASON_TYPE.FAILED
# 51                     }
# 52             elif last == STATE.GENERATE:
# 53                 if result == "accept":
# 54                     transition = {
# 55                         "state": STATE.DISCRIMINATE,
# 56                         "reason": TRANSITION_REASON_TYPE.PASSED,
# 57                         "file_path": state.get("file_path")
# 58                     }
# 59                 else:
# 60                     transition = {
# 61                         "state": STATE.GENERATE,
# 62                         "reason": TRANSITION_REASON_TYPE.FAILED,
# 63                         "file_path": state.get("file_path")
# 64                     }
# 65 
# 66         elif current == STATE.GENERATE:
# 67             transition = {
# 68                 "state": STATE.CODE_STABILITY,
# 69                 "reason": TRANSITION_REASON_TYPE.EVALUATING,
# 70                 "file_path": state.get("file_path")
# 71             }
# 72 
# 73         elif current == STATE.DISCRIMINATE:
# 74             if result == "accept":
# 75                 transition = {
# 76                     "state": STATE.END,
# 77                     "reason": TRANSITION_REASON_TYPE.PASSED,
# 78                     "file_path": state.get("file_path")
# 79                 }
# 80             else:
# 81                 transition = {
# 82                     "state": STATE.GENERATE,
# 83                     "reason": TRANSITION_REASON_TYPE.FAILED,
# 84                     "file_path": state.get("file_path")
# 85                 }
# 86 
# 87         return transition


# db\seeders\files\tool_providers\__init__.py



# db\seeders\files\tool_providers\black_tool.py

# 1 import subprocess
# 2 import sys
# 3 import re
# 4 
# 5 from app.providers.tool_provider_base import ToolProviderBase
# 6 from app.db.schemas import ToolOutputSchema
# 7 
# 8 
# 9 class BlackToolProvider(ToolProviderBase):
# 10     def _run(self, input: dict) -> ToolOutputSchema:
# 11         target = input.get("target")
# 12         check = input.get("check", False)
# 13 
# 14         #  Build the Black command
# 15         if check:
# 16             cmd = [
# 17                 sys.executable,
# 18                 "-m",
# 19                 "black",
# 20                 "--diff",
# 21                 "--check",
# 22                 "--verbose",
# 23                 target,
# 24             ]
# 25         else:
# 26             cmd = [sys.executable, "-m", "black", "--quiet", target]
# 27 
# 28         proc = subprocess.run(
# 29             cmd,
# 30             capture_output=True,
# 31             text=True,
# 32             encoding="utf-8",
# 33             errors="ignore",
# 34         )
# 35 
# 36         raw_stdout = proc.stdout or ""
# 37         raw_stderr = proc.stderr or ""
# 38         stdout = raw_stdout.rstrip()
# 39         stderr = raw_stderr.rstrip()
# 40         violations: list[str] = []
# 41         metrics: dict[str, int | None] = {}
# 42 
# 43         if check:
# 44             #  Black’s exit codes in check mode:
# 45             #    1 = clean (nothing to reformat)
# 46             #    0 = would reformat
# 47             #   ≥2 = internal error
# 48             if proc.returncode == 0:
# 49                 return_code = 1
# 50                 summary = "Black check passed (no formatting needed)"
# 51                 metrics = {
# 52                     "files_checked": 1,
# 53                     "files_reformatted": 0,
# 54                     "lines_changed": 0,
# 55                 }
# 56             elif proc.returncode == 1:
# 57                 return_code = 0
# 58                 summary = "Black check failed"
# 59                 diff_hunks = len(re.findall(r"^@@", stdout, re.MULTILINE))
# 60                 metrics = {
# 61                     "files_checked": 1,
# 62                     "files_reformatted": 1,
# 63                     "lines_changed": diff_hunks,
# 64                 }
# 65                 violations = [f"Would reformat {target}"]
# 66             else:
# 67                 raise RuntimeError(
# 68                     f"Black execution error ({proc.returncode}): {stderr or stdout}"
# 69                 )
# 70         else:
# 71             if proc.returncode != 0:
# 72                 raise RuntimeError(f"Black formatting error: {stderr or stdout}")
# 73             return_code = 1
# 74             summary = "Black formatting applied"
# 75             metrics = {
# 76                 "files_checked": 1,
# 77                 "files_reformatted": 1,
# 78                 "lines_changed": None,
# 79             }
# 80 
# 81         response = ToolOutputSchema(
# 82             return_code=return_code,
# 83             stdout=stdout or None,
# 84             stderr=stderr or None,
# 85             violations=violations or None,
# 86             metrics=metrics or None,
# 87             summary=summary,
# 88         )
# 89         return response


# db\seeders\files\tool_providers\docformatter_tool.py

# 1 import subprocess
# 2 import sys
# 3 from app.providers.tool_provider_base import ToolProviderBase
# 4 from app.db.schemas import ToolOutputSchema
# 5 
# 6 class DocFormatterToolProvider(ToolProviderBase):
# 7     def _run(self, input: dict) -> ToolOutputSchema:
# 8         target = input.get("target")
# 9         check = input.get("check", False)
# 10 
# 11         #  Build command
# 12         cmd = [sys.executable, "-m", "docformatter", target, "--in-place"]
# 13         if check:
# 14             cmd.append("--check")
# 15 
# 16         proc = subprocess.run(cmd, capture_output=True, text=True)
# 17         stdout = proc.stdout.strip()
# 18         stderr = proc.stderr.strip()
# 19         raw_code = proc.returncode
# 20 
# 21         if check:
# 22             if raw_code == 0:
# 23                 norm_code = 1
# 24                 summary = "Docstring format check passed"
# 25             elif raw_code == 1:
# 26                 norm_code = 0
# 27                 summary = "Docstring format check failed"
# 28             else:
# 29                 raise RuntimeError(f"docformatter check error ({raw_code}): {stderr or stdout}")
# 30         else:
# 31             if raw_code == 0:
# 32                 norm_code = 1
# 33                 summary = "Docstrings formatted successfully"
# 34             else:
# 35                 raise RuntimeError(f"docformatter formatting error ({raw_code}): {stderr or stdout}")
# 36 
# 37         return ToolOutputSchema(
# 38             return_code=norm_code,
# 39             stdout=stdout,
# 40             stderr=stderr,
# 41             summary=summary
# 42         )


# db\seeders\files\tool_providers\mypy_tool.py

# 1 import subprocess
# 2 import sys
# 3 import json
# 4 from app.providers.tool_provider_base import ToolProviderBase
# 5 from app.db.schemas import ToolOutputSchema
# 6 
# 7 class MypyToolProvider(ToolProviderBase):
# 8     def _run(self, input: dict) -> ToolOutputSchema:
# 9         target = input.get("target")
# 10 
# 11         #  Use strictest mypy options available inline
# 12         cmd = [
# 13             sys.executable, "-m", "mypy", target,
# 14             "--strict",
# 15             "--disallow-untyped-defs",
# 16             "--disallow-incomplete-defs",
# 17             "--disallow-untyped-calls",
# 18             "--disallow-untyped-decorators",
# 19             "--disallow-any-generics",
# 20             "--warn-unused-ignores",
# 21             "--warn-return-any",
# 22             "--no-implicit-optional",
# 23             "--strict-equality"
# 24         ]
# 25 
# 26         proc = subprocess.run(cmd, capture_output=True, text=True)
# 27 
# 28         raw_code = proc.returncode
# 29         raw_stdout = proc.stdout.strip()
# 30         raw_stderr = proc.stderr.strip()
# 31 
# 32         violations: list[str] = []
# 33         metrics: dict[str, int] = {}
# 34 
# 35         if raw_code == 0:
# 36             norm_code = 1
# 37             summary = "Mypy strict check passed"
# 38             metrics = {"error_count": 0}
# 39         elif raw_code == 1:
# 40             norm_code = 0
# 41             summary = "Mypy strict violations detected"
# 42             violations = [line.strip() for line in raw_stdout.splitlines() if line.strip()]
# 43             metrics = {"error_count": len(violations)}
# 44         else:
# 45             raise RuntimeError(f"Mypy execution error ({raw_code}): {raw_stderr or raw_stdout}")
# 46 
# 47         return ToolOutputSchema(
# 48             return_code=norm_code,
# 49             stdout=raw_stdout or None,
# 50             stderr=raw_stderr or None,
# 51             violations=violations or None,
# 52             metrics=metrics or None,
# 53             summary=summary
# 54         )


# db\seeders\files\tool_providers\radon_tool.py

# 1 import subprocess
# 2 import sys
# 3 import json
# 4 from pathlib import Path
# 5 from app.providers.tool_provider_base import ToolProviderBase
# 6 from app.db.schemas import ToolOutputSchema
# 7 
# 8 class RadonToolProvider(ToolProviderBase):
# 9     def _run(self, input: dict) -> ToolOutputSchema:
# 10         target = input.get("target")
# 11         if not Path(target).exists():
# 12             raise FileNotFoundError(f"{target} not found")
# 13 
# 14         cmd = [sys.executable, "-m", "radon", "cc", "-s", target]
# 15         proc = subprocess.run(cmd, capture_output=True, text=True)
# 16 
# 17         stdout = proc.stdout.strip()
# 18         stderr = proc.stderr.strip()
# 19         raw_code = proc.returncode
# 20 
# 21         if raw_code != 0:
# 22             raise RuntimeError(f"radon failed: {stderr or stdout or 'unknown error'}")
# 23 
# 24         #  Determine worst grade
# 25         grades = []
# 26         for line in stdout.splitlines():
# 27             if line.strip() and "(" in line and ")" in line:
# 28                 try:
# 29                     grade = line.strip().split("(", 1)[1].split(")")[0]
# 30                     grades.append(grade)
# 31                 except Exception:
# 32                     continue
# 33 
# 34         #  Define clean threshold: worst grade must be A or B
# 35         worst_grade = max(grades, default="A")
# 36         passing = worst_grade in ("A", "B")
# 37 
# 38         norm_code = 1 if passing else 0
# 39         summary = (
# 40             f"Radon complexity OK (worst: {worst_grade})"
# 41             if passing else
# 42             f"Radon complexity too high (worst: {worst_grade})"
# 43         )
# 44 
# 45         return ToolOutputSchema(
# 46             return_code=norm_code,
# 47             stdout=stdout,
# 48             stderr=stderr,
# 49             summary=summary
# 50         )


# db\seeders\files\tool_providers\ruff_tool.py

# 1 import subprocess
# 2 import sys
# 3 import json
# 4 from app.providers.tool_provider_base import ToolProviderBase
# 5 from app.db.schemas import ToolOutputSchema
# 6 
# 7 class RuffToolProvider(ToolProviderBase):
# 8     def _run(self, input: dict) -> ToolOutputSchema:
# 9         target = input.get("target")
# 10 
# 11         cmd = [sys.executable, "-m", "ruff", "check", target]
# 12         proc = subprocess.run(cmd, capture_output=True, text=True)
# 13 
# 14         stdout = proc.stdout.strip()
# 15         stderr = proc.stderr.strip()
# 16         raw_code = proc.returncode
# 17 
# 18         violations = []
# 19 
# 20         if raw_code in (0, 1):  #  1 = violations, 0 = clean
# 21             for line in stdout.splitlines():
# 22                 if line.strip().startswith(target):
# 23                     parts = line.strip().split()
# 24                     if parts:
# 25                         violations.append(parts[-1])  #  Capture violation code
# 26 
# 27         if raw_code == 0:
# 28             norm_code = 1  #  ✅ pass
# 29             summary = "Ruff check passed"
# 30         elif raw_code == 1:
# 31             norm_code = 0  #  ❌ fail
# 32             summary = "Ruff rule violations found"
# 33         else:
# 34             raise RuntimeError(f"Ruff execution error ({raw_code}): {stderr or stdout or 'unknown error'}")
# 35 
# 36         return ToolOutputSchema(
# 37             return_code=norm_code,
# 38             stdout=stdout,
# 39             stderr=stderr,
# 40             violations=violations or None,
# 41             summary=summary
# 42         )


# db\seeders\files\tool_providers\sonarcloud_tool.py

# 1 import os
# 2 import subprocess
# 3 import time
# 4 import uuid
# 5 import json
# 6 import tempfile
# 7 from pathlib import Path
# 8 from typing import Dict
# 9 from app.providers.tool_provider_base import ToolProviderBase
# 10 from app.db.schemas import ToolOutputSchema
# 11 
# 12 import os
# 13 import subprocess
# 14 import time
# 15 import uuid
# 16 import json
# 17 import tempfile
# 18 from pathlib import Path
# 19 from typing import Dict
# 20 from app.providers.tool_provider_base import ToolProviderBase
# 21 from app.db.schemas import ToolOutputSchema
# 22 
# 23 class SonarCloudToolProvider(ToolProviderBase):
# 24     def _run(self, input: dict) -> ToolOutputSchema:
# 25         target_path = Path(input.get("target"))
# 26         if not target_path.exists():
# 27             raise FileNotFoundError(f"{target_path} does not exist")
# 28 
# 29         github_token, sonar_token, sonar_project, sonar_org, github_user = self._load_env()
# 30 
# 31         metrics = {}
# 32         stdout_msg = ""
# 33 
# 34         with tempfile.TemporaryDirectory() as tmpdir:
# 35             repo_url = f"https://github.com/{github_user}/codecritic_scoring"
# 36             subprocess.run(["gh", "repo", "clone", repo_url, tmpdir], check=True)
# 37 
# 38             filename = f"test_{uuid.uuid4()}.py"
# 39             src_dir = Path(tmpdir) / "src"
# 40             src_dir.mkdir(parents=True, exist_ok=True)
# 41             target_dest = src_dir / filename
# 42             target_dest.write_text(target_path.read_text(encoding="utf-8"))
# 43 
# 44             self._git_push(filename, cwd=src_dir)
# 45             self._wait_for_scan_completion(sonar_token, sonar_project)
# 46             metrics = self._poll_sonarcloud_metrics(sonar_token, sonar_project)
# 47             stdout_msg = f"Scan complete for {filename}"
# 48 
# 49             if metrics:
# 50                 self._git_cleanup(filename, cwd=src_dir)
# 51 
# 52         #  Determine if the scan passes your criteria
# 53         violations_present = any(
# 54             metrics.get(key, 0) > 0 for key in ("bugs", "vulnerabilities", "code_smells")
# 55         )
# 56 
# 57         return_code = 0 if violations_present else 1
# 58         summary = (
# 59             "SonarCloud scan clean" if return_code == 1
# 60             else "SonarCloud found issues"
# 61         )
# 62 
# 63         return ToolOutputSchema(
# 64             return_code=return_code,
# 65             stdout=stdout_msg,
# 66             metrics=metrics,
# 67             summary=summary
# 68         )
# 69 
# 70 
# 71     def _load_env(self) -> tuple[str, str, str, str, str]:
# 72         github_token = os.getenv("GITHUB_TOKEN")
# 73         sonar_token = os.getenv("SONAR_TOKEN")
# 74         sonar_project = os.getenv("SONAR_PROJECT_KEY")
# 75         sonar_org = os.getenv("SONAR_ORGANIZATION_KEY")
# 76         github_user = os.getenv("GITHUB_USERNAME")
# 77         if not all([github_token, sonar_token, sonar_project, sonar_org, github_user]):
# 78             raise EnvironmentError("Missing one or more required environment variables")
# 79         return github_token, sonar_token, sonar_project, sonar_org, github_user
# 80 
# 81     def _git_push(self, filename: str, cwd: Path) -> None:
# 82         subprocess.run(["git", "checkout", "main"], cwd=cwd.parent, check=True)
# 83         subprocess.run(["git", "pull"], cwd=cwd.parent, check=True)
# 84         subprocess.run(["git", "add", f"src/{filename}"], cwd=cwd.parent, check=True)
# 85         subprocess.run(["git", "commit", "-m", f"Add test file src/{filename}"], cwd=cwd.parent, check=True)
# 86         subprocess.run(["git", "push"], cwd=cwd.parent, check=True)
# 87 
# 88     def _git_cleanup(self, filename: str, cwd: Path) -> None:
# 89         subprocess.run(["git", "rm", f"src/{filename}"], cwd=cwd.parent, check=True)
# 90         subprocess.run(["git", "commit", "-m", f"Remove test file src/{filename}"], cwd=cwd.parent, check=True)
# 91         subprocess.run(["git", "push"], cwd=cwd.parent, check=True)
# 92 
# 93     def _wait_for_scan_completion(self, sonar_token: str, sonar_project: str) -> None:
# 94         url = f"https://sonarcloud.io/api/ce/component?component={sonar_project}"
# 95         for attempt in range(20):
# 96             time.sleep(10)
# 97             result = subprocess.run(
# 98                 ["curl", "-s", "-u", f"{sonar_token}:", url],
# 99                 capture_output=True,
# 100                 text=True
# 101             )
# 102             try:
# 103                 parsed = json.loads(result.stdout)
# 104                 status = parsed.get("current", {}).get("status")
# 105                 if status == "SUCCESS":
# 106                     return
# 107             except Exception as e:
# 108                 print(f"⚠️ Error reading scan status: {e}")
# 109 
# 110     def _poll_sonarcloud_metrics(self, sonar_token: str, sonar_project: str) -> Dict[str, int]:
# 111         metric_keys = ",".join([
# 112             "bugs", "vulnerabilities", "security_hotspots", "code_smells",
# 113             "coverage", "line_coverage", "lines_to_cover", "tests",
# 114             "test_errors", "test_failures", "skipped_tests",
# 115             "lines", "ncloc", "functions", "classes", "statements",
# 116             "complexity", "cognitive_complexity",
# 117             "duplicated_lines", "duplicated_blocks", "duplicated_files", "duplicated_lines_density"
# 118         ])
# 119 
# 120         api_url = (
# 121             f"https://sonarcloud.io/api/measures/component"
# 122             f"?component={sonar_project}&metricKeys={metric_keys}"
# 123         )
# 124 
# 125         for attempt in range(10):
# 126             time.sleep(5)
# 127             result = subprocess.run(
# 128                 ["curl", "-s", "-u", f"{sonar_token}:", api_url],
# 129                 capture_output=True,
# 130                 text=True
# 131             )
# 132             try:
# 133                 parsed = json.loads(result.stdout)
# 134                 measures = parsed.get("component", {}).get("measures", [])
# 135                 return {m["metric"]: float(m["value"]) for m in measures}
# 136             except Exception as e:
# 137                 print(f"⚠️ Error parsing metrics: {e}")
# 138         return {}
# 139 


# db\seeders\files\tool_providers\symbol_graph.py

# 1 from __future__ import annotations
# 2 
# 3 import ast
# 4 import json
# 5 from pathlib import Path
# 6 from typing import Any, Dict
# 7 
# 8 from app.providers.tool_provider_base import ToolProviderBase
# 9 from app.db.schemas import ToolOutputSchema
# 10 
# 11 
# 12 class SymbolGraph:
# 13     def __init__(self) -> None:
# 14         self.graph: Dict[str, Any] = {}
# 15 
# 16     def parse_file(self, filepath: str | Path) -> None:
# 17         from ast import parse
# 18 
# 19         filepath = Path(filepath)
# 20         source = filepath.read_text(encoding="utf-8")
# 21         tree = parse(source, filename=str(filepath))
# 22 
# 23         visitor = _SymbolGraphVisitor(filepath.stem, str(filepath), self.graph)
# 24         visitor.visit(tree)
# 25 
# 26 
# 27 class SymbolGraphToolProvider(ToolProviderBase):
# 28     def _run(self, input: dict) -> ToolOutputSchema:
# 29         target = input.get("target")
# 30         path = Path(target)
# 31         if not path.exists():
# 32             raise FileNotFoundError(f"{target} does not exist")
# 33 
# 34         symbol_graph_util = SymbolGraph()
# 35         symbol_graph_util.parse_file(target)
# 36 
# 37         result_json = json.dumps(symbol_graph_util.graph, indent=2)
# 38 
# 39         #  Return 1 to indicate “error free / success”
# 40         return ToolOutputSchema(
# 41             return_code=1,
# 42             stdout=result_json,
# 43             metrics=symbol_graph_util.graph,
# 44             summary="Symbol graph extraction successful"
# 45         )
# 46 
# 47 
# 48 class _SymbolGraphVisitor(ast.NodeVisitor):
# 49     def __init__(self, module: str, file_path: str, graph: Dict[str, Any]) -> None:
# 50         self.module = module
# 51         self.file_path = file_path
# 52         self.graph = graph
# 53         self.scope: list[str] = []
# 54         self.current: str | None = None
# 55 
# 56     def visit_FunctionDef(self, node: ast.FunctionDef) -> None:
# 57         self._process_function_or_async_function(node)
# 58         self.generic_visit(node)
# 59         self.scope.pop()
# 60         self.current = None
# 61 
# 62     def visit_AsyncFunctionDef(self, node: ast.AsyncFunctionDef) -> None:
# 63         self._process_function_or_async_function(node)
# 64         self.generic_visit(node)
# 65         self.scope.pop()
# 66         self.current = None
# 67 
# 68     def _process_function_or_async_function(self, node: ast.FunctionDef | ast.AsyncFunctionDef) -> None:
# 69         qual = self._qualify(node.name)
# 70         self.graph[qual] = {
# 71             "name": node.name,
# 72             "type": "async_function" if isinstance(node, ast.AsyncFunctionDef) else "function",
# 73             "file": self.file_path,
# 74             "lineno": node.lineno,
# 75             "col_offset": node.col_offset,
# 76             "end_lineno": getattr(node, "end_lineno", node.lineno),
# 77             "end_col_offset": getattr(node, "end_col_offset", node.col_offset),
# 78             "scope": ".".join([self.module, *self.scope]) if self.scope else self.module,
# 79             "calls": [],
# 80         }
# 81         self.scope.append(node.name)
# 82         self.current = qual
# 83 
# 84     def visit_ClassDef(self, node: ast.ClassDef) -> None:
# 85         qual = self._qualify(node.name)
# 86         self._record(qual, "class", node)
# 87         self.scope.append(node.name)
# 88         self.current = qual
# 89         self.generic_visit(node)
# 90         self.scope.pop()
# 91         self.current = None
# 92 
# 93     def _qualify(self, name: str) -> str:
# 94         return ".".join([self.module, *self.scope, name])
# 95 
# 96     def _record(self, qual: str, typ: str, node: ast.AST) -> Dict[str, Any]:
# 97         info = {
# 98             "type": typ,
# 99             "file": self.file_path,
# 100             "lineno": getattr(node, "lineno", None),
# 101             "col_offset": getattr(node, "col_offset", None),
# 102             "end_lineno": getattr(node, "end_lineno", getattr(node, "lineno", None)),
# 103             "end_col_offset": getattr(node, "end_col_offset", getattr(node, "col_offset", None)),
# 104             "scope": ".".join([self.module, *self.scope]) if self.scope else self.module,
# 105             "calls": [],
# 106         }
# 107         self.graph[qual] = info
# 108         return info


# db\seeders\seed_agent_engine_providers.py

# 1 from uuid import uuid4
# 2 from pathlib import Path
# 3 import shutil
# 4 from sqlalchemy.orm import Session
# 5 from app.db.models import AgentEngineProviderConfig
# 6 
# 7 SEED_FILES_DIR = Path(__file__).resolve().parent / "files/agent_engine_providers"
# 8 PROJECT_ROOT = SEED_FILES_DIR.parent.parent.parent.parent.parent
# 9 EXTENSIONS_DIR = PROJECT_ROOT / "extensions"
# 10 
# 11 AGENT_ENGINES = [
# 12     (1, "basic_agent_engine_provider.py", "basic_agent_engine_provider", "Returns a mock LLM response.", "mock-llm", ["default"]),
# 13     (2, "openai_gpt4o_agent_engine_provider.py", "openai_gpt_4o_agent_engine", "Runs GPT-4o via OpenAI API.", "gpt-4o", ["openai", "production"]),
# 14     (3, "gemini_1_5_pro_agent_engine_provider.py", "gemini_1_5_pro_agent_engine", "Runs Gemini 1.5 Pro via Google AI API.", "gemini-1.5-pro", ["google", "production"]),
# 15     (4, "claude_3_sonnet_agent_engine_provider.py", "claude_3_sonnet_agent_engine", "Runs Claude 3 Sonnet via AWS Bedrock.", "claude-3-sonnet", ["aws", "bedrock", "production"]),
# 16 ]
# 17 
# 18 def seed_agent_engine_providers(db_session: Session):
# 19     EXTENSIONS_DIR.mkdir(parents=True, exist_ok=True)
# 20 
# 21     for id_, filename, name, description, model, tags in AGENT_ENGINES:
# 22         guid = str(uuid4())
# 23         source_file = SEED_FILES_DIR / filename
# 24         dest_file = EXTENSIONS_DIR / f"{guid}.py"
# 25 
# 26         if not source_file.exists():
# 27             raise FileNotFoundError(f"Agent engine script not found: {source_file}")
# 28 
# 29         shutil.copy(source_file, dest_file)
# 30 
# 31         config = AgentEngineProviderConfig(
# 32             id=id_,
# 33             guid=guid,
# 34             name=name,
# 35             description=description,
# 36             model=model,
# 37             config={},
# 38             artifact_path=str(dest_file),
# 39             tags=tags,
# 40         )
# 41 
# 42         db_session.add(config)
# 43 
# 44     db_session.commit()
# 45     print("Seeded agent engine configurations successfully.")


# db\seeders\seed_agent_providers.py

# 1 from uuid import uuid4
# 2 from pathlib import Path
# 3 import shutil
# 4 from sqlalchemy.orm import Session
# 5 from app.db.models import AgentProviderConfig
# 6 
# 7 SEED_FILES_DIR = Path(__file__).resolve().parent / "files/agent_providers"
# 8 PROJECT_ROOT = SEED_FILES_DIR.parent.parent.parent.parent.parent
# 9 EXTENSIONS_DIR = PROJECT_ROOT / "extensions"
# 10 
# 11 AGENT_PROVIDERS = [
# 12     {
# 13         "id": 1,
# 14         "filename": "basic_agent_provider.py",
# 15         "name": "basic_agent_provider",
# 16         "description": "Returns a hardcoded result.",
# 17         "agent_type": "basic",
# 18         "tags": ["test"],
# 19         "config": {}
# 20     },
# 21     {
# 22         "id": 2,
# 23         "filename": "linting_generator_agent_provider.py",
# 24         "name": "linting_generator_agent_provider",
# 25         "description": "Runs GPT-4o to generate linting corrections.",
# 26         "agent_type": "generator",
# 27         "tags": ["linting", "generator"],
# 28         "config": {
# 29             "agent_engine_provider_id": 2,
# 30             "prompt_provider_id": 2,
# 31             "context_provider_id": 2,
# 32             "score_provider_id": 1,
# 33             "tool_provider_ids": {
# 34                 "black": 1,
# 35                 "ruff": 3,
# 36                 "mypy": 5,
# 37                 "radon": 4
# 38             }
# 39         }
# 40     },
# 41     {
# 42         "id": 3,
# 43         "filename": "linting_discriminator_agent_provider.py",
# 44         "name": "linting_discriminator_agent_provider",
# 45         "description": "Evaluates generator output for acceptance.",
# 46         "agent_type": "discriminator",
# 47         "tags": ["linting", "discriminator"],
# 48         "config": {
# 49             "score_provider_id": 1
# 50         }
# 51     },
# 52     {
# 53         "id": 4,
# 54         "filename": "code_stability_agent_provider.py",
# 55         "name": "code_stability_agent_provider",
# 56         "description": "Rejects output if code is not parseable, importable, compilable, or type-valid.",
# 57         "agent_type": "stability",
# 58         "tags": ["stability"],
# 59         "config": {
# 60             "score_provider_id": 2
# 61         }
# 62     }
# 63 ]
# 64 
# 65 
# 66 def seed_agent_providers(db_session: Session):
# 67     EXTENSIONS_DIR.mkdir(parents=True, exist_ok=True)
# 68 
# 69     for entry in AGENT_PROVIDERS:
# 70         guid = str(uuid4())
# 71         dest_filename = f"{guid}.py"
# 72         source_file = SEED_FILES_DIR / entry["filename"]
# 73         dest_file = EXTENSIONS_DIR / dest_filename
# 74 
# 75         if not source_file.exists():
# 76             raise FileNotFoundError(f"Agent provider script not found: {source_file}")
# 77 
# 78         shutil.copy(source_file, dest_file)
# 79 
# 80         config = AgentProviderConfig(
# 81             id=entry["id"],
# 82             guid=guid,
# 83             name=entry["name"],
# 84             description=entry["description"],
# 85             artifact_path=dest_filename,
# 86             tags=entry["tags"],
# 87             config=entry["config"],
# 88             agent_type=entry["agent_type"]
# 89         )
# 90 
# 91         db_session.add(config)
# 92 
# 93     db_session.commit()
# 94     print("Seeded agent provider configurations successfully.")


# db\seeders\seed_context_providers.py

# 1 from uuid import uuid4
# 2 from pathlib import Path
# 3 import shutil
# 4 from sqlalchemy.orm import Session
# 5 from app.db.models import ContextProviderConfig
# 6 
# 7 SEED_FILES_DIR = Path(__file__).resolve().parent / "files/context_providers"
# 8 PROJECT_ROOT = SEED_FILES_DIR.parent.parent.parent.parent.parent
# 9 EXTENSIONS_DIR = PROJECT_ROOT / "extensions"
# 10 
# 11 CONTEXT_PROVIDERS = [
# 12     {
# 13         "id": 1,
# 14         "filename": "basic_context_provider.py",
# 15         "name": "basic_context_provider",
# 16         "description": "Returns static context for testing.",
# 17         "tags": ["default"],
# 18         "config": {}
# 19     },
# 20     {
# 21         "id": 2,
# 22         "filename": "linting_context_provider.py",
# 23         "name": "linting_context_provider",
# 24         "description": "Generates context for the linting system.",
# 25         "tags": ["linting", "score-aware"],
# 26         "config": {
# 27             "score_provider_id": 1,
# 28             "tool_provider_ids": None
# 29         }
# 30     }
# 31 ]
# 32 
# 33 def seed_context_providers(db_session: Session):
# 34     EXTENSIONS_DIR.mkdir(parents=True, exist_ok=True)
# 35 
# 36     for entry in CONTEXT_PROVIDERS:
# 37         guid = str(uuid4())
# 38         dest_filename = f"{guid}.py"
# 39         source_file = SEED_FILES_DIR / entry["filename"]
# 40         dest_file = EXTENSIONS_DIR / dest_filename
# 41 
# 42         if not source_file.exists():
# 43             raise FileNotFoundError(f"Context provider script not found: {source_file}")
# 44 
# 45         shutil.copy(source_file, dest_file)
# 46 
# 47         config = ContextProviderConfig(
# 48             id=entry["id"],
# 49             guid=guid,
# 50             name=entry["name"],
# 51             description=entry["description"],
# 52             artifact_path=dest_filename,
# 53             tags=entry["tags"],
# 54             config=entry["config"]
# 55         )
# 56 
# 57         db_session.add(config)
# 58 
# 59     db_session.commit()
# 60     print("Seeded context provider configurations successfully.")


# db\seeders\seed_controller_providers.py

# 1 from uuid import uuid4
# 2 from pathlib import Path
# 3 import shutil
# 4 from sqlalchemy.orm import Session
# 5 from app.db.models import ControllerProviderConfig
# 6 from app.enums.system_enums import SYSTEM 
# 7 
# 8 SEED_FILES_DIR = Path(__file__).parent / "files/controller_providers"
# 9 PROJECT_ROOT   = SEED_FILES_DIR.parents[4]
# 10 EXTENSIONS_DIR = PROJECT_ROOT / "extensions"
# 11 
# 12 CONTROLLER_PROVIDERS = [
# 13     {
# 14         "id": 1,
# 15         "filename": "preprocessing_controller_provider.py",
# 16         "name": "preprocessing_controller",
# 17         "description": "Runs only the preprocessing step (e.g. linting).",
# 18         "tags": ["preprocessing", "controller"],
# 19         "config": {
# 20             "score_provider_id": 1,
# 21             "systems": {
# 22                 SYSTEM.LINTING.value: 1
# 23             }
# 24         }
# 25     }
# 26 ]
# 27 
# 28 def seed_controller_providers(db_session: Session):
# 29     EXTENSIONS_DIR.mkdir(parents=True, exist_ok=True)
# 30     for entry in CONTROLLER_PROVIDERS:
# 31         guid = str(uuid4())
# 32         src = SEED_FILES_DIR / entry["filename"]
# 33         dest = EXTENSIONS_DIR / f"{guid}.py"
# 34         if not src.exists():
# 35             raise FileNotFoundError(f"Missing script: {src}")
# 36         shutil.copy(src, dest)
# 37 
# 38         rec = ControllerProviderConfig(
# 39             id=entry["id"],
# 40             guid=guid,
# 41             name=entry["name"],
# 42             description=entry["description"],
# 43             config=entry["config"],
# 44             artifact_path=dest.name,
# 45             tags=entry["tags"],
# 46         )
# 47         db_session.add(rec)
# 48     db_session.commit()
# 49     print("✅ Seeded controller providers")


# db\seeders\seed_program_provider.py

# 1 from uuid import uuid4
# 2 from pathlib import Path
# 3 import shutil
# 4 from sqlalchemy.orm import Session
# 5 from app.db.models import ProgramProviderConfig
# 6 from app.enums.controller_enums import CONTROLLER  #  ✅ Import your FSM state enum
# 7 
# 8 SEED_FILES_DIR = Path(__file__).resolve().parent / "files/program_providers"
# 9 PROJECT_ROOT   = SEED_FILES_DIR.parents[4]
# 10 EXTENSIONS_DIR = PROJECT_ROOT / "extensions"
# 11 
# 12 PROGRAM_PROVIDERS = [
# 13     {
# 14         "id": 1,
# 15         "filename": "codecritic_program_provider.py",
# 16         "name": "codecritic_program",
# 17         "description": "Top-level orchestration of all system controllers.",
# 18         "tags": ["program", "codecritic"],
# 19         "config": {
# 20             "score_provider_id": 1,
# 21             "controllers": {
# 22                 CONTROLLER.PREPROCESSING.value: 1
# 23             }
# 24         }
# 25     }
# 26 ]
# 27 
# 28 def seed_program_providers(db_session: Session):
# 29     EXTENSIONS_DIR.mkdir(parents=True, exist_ok=True)
# 30 
# 31     for entry in PROGRAM_PROVIDERS:
# 32         guid = str(uuid4())
# 33         src  = SEED_FILES_DIR / entry["filename"]
# 34         dst  = EXTENSIONS_DIR / f"{guid}.py"
# 35 
# 36         if not src.exists():
# 37             raise FileNotFoundError(f"❌ Missing file: {src}")
# 38 
# 39         shutil.copy(src, dst)
# 40 
# 41         record = ProgramProviderConfig(
# 42             id            = entry["id"],
# 43             guid          = guid,
# 44             name          = entry["name"],
# 45             description   = entry["description"],
# 46             config        = entry["config"],
# 47             artifact_path = dst.name,
# 48             tags          = entry["tags"],
# 49         )
# 50         db_session.add(record)
# 51 
# 52     db_session.commit()
# 53     print("✅ Seeded program providers")


# db\seeders\seed_prompt_providers.py

# 1 from uuid import uuid4
# 2 from pathlib import Path
# 3 import shutil
# 4 from sqlalchemy.orm import Session
# 5 from app.db.models import PromptProviderConfig
# 6 
# 7 CURRENT_DIR = Path(__file__).resolve().parent
# 8 SEED_FILES_DIR = CURRENT_DIR / "files/prompt_providers"
# 9 PROJECT_ROOT = SEED_FILES_DIR.parent.parent.parent.parent.parent
# 10 
# 11 EXTENSIONS_DIR = PROJECT_ROOT / "extensions"
# 12 
# 13 PROVIDERS = [
# 14     {
# 15         "id": 1,
# 16         "filename": "basic_prompt_provider.py",
# 17         "name": "basic_prompt_provider",
# 18         "description": "Returns a simple prompt response.",
# 19         "tags": ["default"],
# 20         "config": {}
# 21     },
# 22     {
# 23         "id": 2,
# 24         "filename": "linting_prompt_provider.py",
# 25         "name": "linting_prompt_provider",
# 26         "description": "Combines linting system and agent prompts.",
# 27         "tags": ["linting", "codequality"],
# 28         "config": {
# 29             "agent_prompt_id": 2,
# 30             "system_prompt_id": 2,
# 31             "context_provider_id": 2
# 32         }
# 33     }
# 34 ]
# 35 
# 36 
# 37 def seed_prompt_providers(db_session: Session):
# 38     EXTENSIONS_DIR.mkdir(parents=True, exist_ok=True)
# 39 
# 40     for entry in PROVIDERS:
# 41         guid = str(uuid4())
# 42         dest_filename = f"{guid}.py"
# 43         source_file = SEED_FILES_DIR / entry["filename"]
# 44         dest_file = EXTENSIONS_DIR / dest_filename
# 45 
# 46         if not source_file.exists():
# 47             raise FileNotFoundError(f"Prompt provider script not found: {source_file}")
# 48 
# 49         shutil.copy(source_file, dest_file)
# 50 
# 51         config = PromptProviderConfig(
# 52             id=entry["id"],
# 53             guid=guid,
# 54             name=entry["name"],
# 55             description=entry["description"],
# 56             config=entry["config"],
# 57             artifact_path=dest_filename,
# 58             tags=entry["tags"]
# 59         )
# 60 
# 61         db_session.add(config)
# 62 
# 63     db_session.commit()
# 64     print("Seeded prompt provider configurations successfully.")


# db\seeders\seed_prompts.py

# 1 from pathlib import Path
# 2 import shutil
# 3 from sqlalchemy.orm import Session
# 4 from uuid import uuid4
# 5 from app.db.models import AgentPrompt, SystemPrompt
# 6 from app.enums.system_enums import SYSTEM
# 7 
# 8 #  Define paths relative to this script's file location explicitly
# 9 CURRENT_DIR = Path(__file__).resolve().parent
# 10 SEED_FILES_DIR = CURRENT_DIR / "files/prompts"
# 11 PROJECT_ROOT = CURRENT_DIR.parent.parent.parent
# 12 EXTENSIONS_DIR = PROJECT_ROOT / "extensions"
# 13 
# 14 #  Define new prompt files to seed
# 15 AGENT_PROMPT_FILES = [
# 16     {
# 17         "id": 1,
# 18         "name": "generate",
# 19         "filename": "basic_agent_prompt.txt",
# 20         "description": "Prompt for basic code generation.",
# 21         "tags": ["linting", "generator"]
# 22     },
# 23     {
# 24         "id": 2,
# 25         "name": "linting_generator_agent",
# 26         "filename": "linting_generator_agent_prompt.txt",
# 27         "description": "Prompt for linting generator agent.",
# 28         "tags": ["linting", "generator"]
# 29     }
# 30 ]
# 31 
# 32 SYSTEM_PROMPT_FILES = [
# 33     {
# 34         "id": 1,
# 35         "name": "format",
# 36         "filename": "default_system_prompt.txt",
# 37         "system_type": SYSTEM.LINTING,
# 38         "description": "Default prompt for formatting tasks.",
# 39         "tags": ["formatting", "default"]
# 40     },
# 41     {
# 42         "id": 2,
# 43         "name": "linting_system",
# 44         "filename": "linting_system_prompt.txt",
# 45         "system_type": SYSTEM.LINTING,
# 46         "description": "System prompt governing the linting subsystem.",
# 47         "tags": ["linting", "system"]
# 48     }
# 49 ]
# 50 
# 51 
# 52 def seed_prompts(db_session: Session):
# 53     EXTENSIONS_DIR.mkdir(parents=True, exist_ok=True)
# 54 
# 55     for entry in AGENT_PROMPT_FILES:
# 56         source_file = SEED_FILES_DIR / entry["filename"]
# 57         if not source_file.exists():
# 58             raise FileNotFoundError(f"Agent prompt source file not found: {source_file}")
# 59 
# 60         guid = str(uuid4())
# 61         dest_filename = f"{guid}.txt"
# 62         dest_file = EXTENSIONS_DIR / dest_filename
# 63         shutil.copy(source_file, dest_file)
# 64 
# 65         prompt = AgentPrompt(
# 66             id=entry["id"],
# 67             guid=guid,
# 68             name=entry["name"],
# 69             description=entry["description"],
# 70             artifact_path=dest_filename,  #  Store relative path
# 71             tags=entry["tags"]
# 72         )
# 73         db_session.add(prompt)
# 74         print(f"Seeded AgentPrompt '{entry['name']}' with ID: {entry['id']} and GUID: {guid}")
# 75 
# 76     for entry in SYSTEM_PROMPT_FILES:
# 77         source_file = SEED_FILES_DIR / entry["filename"]
# 78         if not source_file.exists():
# 79             raise FileNotFoundError(f"System prompt source file not found: {source_file}")
# 80 
# 81         guid = str(uuid4())
# 82         dest_filename = f"{guid}.txt"
# 83         dest_file = EXTENSIONS_DIR / dest_filename
# 84         shutil.copy(source_file, dest_file)
# 85 
# 86         prompt = SystemPrompt(
# 87             id=entry["id"],
# 88             guid=guid,
# 89             name=entry["name"],
# 90             system_type=entry["system_type"],
# 91             description=entry["description"],
# 92             artifact_path=dest_filename,  #  Store relative path
# 93             tags=entry["tags"]
# 94         )
# 95         db_session.add(prompt)
# 96         print(f"Seeded SystemPrompt '{entry['name']}' with ID: {entry['id']} and GUID: {guid}")
# 97 
# 98     db_session.commit()


# db\seeders\seed_score_providers.py

# 1 from uuid import uuid4
# 2 from pathlib import Path
# 3 import shutil
# 4 from sqlalchemy.orm import Session
# 5 from app.db.models import ScoreProviderConfig
# 6 
# 7 CURRENT_DIR = Path(__file__).resolve().parent
# 8 SEED_FILES_DIR = CURRENT_DIR / "files/score_providers"
# 9 PROJECT_ROOT = SEED_FILES_DIR.parent.parent.parent.parent.parent
# 10 EXTENSIONS_DIR = PROJECT_ROOT / "extensions"
# 11 
# 12 SCORES = [
# 13     {
# 14         "id": 1,
# 15         "filename": "linting_score_provider.py",
# 16         "name": "linting_score_provider",
# 17         "description": "Scores lint compliance.",
# 18         "config": {
# 19             "tool_provider_ids": {
# 20                 "black": 1,
# 21                 "ruff": 3,
# 22                 "mypy": 5,
# 23                 "radon": 4
# 24             },
# 25             "context_provider_id": None
# 26         },
# 27         "tags": ["linting"]
# 28     },
# 29     {
# 30         "id": 2,
# 31         "filename": "code_stability_score_provider.py",
# 32         "name": "code_stability_score_provider",
# 33         "description": "Ensures code is structurally and syntactically safe for downstream use.",
# 34         "config": {
# 35             "tool_provider_ids": {
# 36                 "black": 1,
# 37                 "mypy": 5,
# 38                 "symbol_graph": 7
# 39             }
# 40         },
# 41         "tags": ["stability", "safety"]
# 42     }
# 43 ]
# 44 
# 45 def seed_score_providers(db_session: Session):
# 46     EXTENSIONS_DIR.mkdir(parents=True, exist_ok=True)
# 47 
# 48     for entry in SCORES:
# 49         guid = str(uuid4())
# 50         dest_filename = f"{guid}.py"
# 51         source_file = SEED_FILES_DIR / entry["filename"]
# 52         dest_file = EXTENSIONS_DIR / dest_filename
# 53 
# 54         if not source_file.exists():
# 55             raise FileNotFoundError(f"Score provider script not found: {source_file}")
# 56 
# 57         shutil.copy(source_file, dest_file)
# 58 
# 59         score = ScoreProviderConfig(
# 60             id=entry["id"],
# 61             guid=guid,
# 62             name=entry["name"],
# 63             description=entry["description"],
# 64             config=entry["config"],
# 65             artifact_path=dest_filename
# 66         )
# 67 
# 68         db_session.add(score)
# 69 
# 70     db_session.commit()
# 71     print("Seeded score providers successfully.")


# db\seeders\seed_session_configs.py

# 1 #  app/db/seeders/seed_session_configs.py
# 2 
# 3 from sqlalchemy.orm import Session
# 4 from app.db.models import SessionConfig
# 5 
# 6 SESSION_CONFIGS = [
# 7     {
# 8         "id": 1,
# 9         "program_provider_id": 1,
# 10         "name": "codecritic_test_session",
# 11         "description": "Test session for program 1",
# 12         "environment_type": "experiment",
# 13         "tags": ["test", "experiment"]
# 14     }
# 15 ]
# 16 
# 17 def seed_session_configs(db_session: Session):
# 18     for sess in SESSION_CONFIGS:
# 19         record = SessionConfig(
# 20             id               = sess["id"],
# 21             program_provider_id = sess["program_provider_id"],
# 22             name             = sess["name"],
# 23             description      = sess["description"],
# 24             environment_type = sess["environment_type"],
# 25             tags             = sess["tags"],
# 26         )
# 27         db_session.add(record)
# 28 
# 29     db_session.commit()
# 30     print("✅ Seeded session configs")


# db\seeders\seed_state_providers.py

# 1 from uuid import uuid4
# 2 from pathlib import Path
# 3 import shutil
# 4 from sqlalchemy.orm import Session
# 5 from app.db.models import StateProviderConfig
# 6 from app.enums.state_enums import STATE
# 7 from app.enums.agent_enums import AGENT
# 8 
# 9 SEED_FILES_DIR = Path(__file__).resolve().parent / "files/state_providers"
# 10 PROJECT_ROOT = SEED_FILES_DIR.parent.parent.parent.parent.parent
# 11 EXTENSIONS_DIR = PROJECT_ROOT / "extensions"
# 12 
# 13 STATE_PROVIDERS = [
# 14     (
# 15         1,
# 16         "linting_generator_state_provider.py",
# 17         "LintingGeneratorStateProvider",
# 18         "Runs the generator agent in its own FSM wrapper.",
# 19         ["linting", "generator"],
# 20         {
# 21             "score_provider_id": 1,
# 22             "agents": {
# 23                 AGENT.GENERATOR.value: 2 
# 24             }
# 25         }
# 26     ),
# 27     (
# 28         2,
# 29         "linting_discriminator_state_provider.py",
# 30         "LintingDiscriminatorStateProvider",
# 31         "Runs the discriminator agent in its own FSM wrapper.",
# 32         ["linting", "discriminator"],
# 33         {
# 34             "score_provider_id": 1,
# 35             "agents": {
# 36                 AGENT.DISCRIMINATOR.value: 3 
# 37             }
# 38         }
# 39     ),
# 40     (
# 41         3,
# 42         "code_stability_state_provider.py",
# 43         "CodeStabilityStateProvider",
# 44         "Gates code based on parse, compile, import, and static validation success.",
# 45         ["stability"],
# 46         {
# 47             "score_provider_id": 1,
# 48             "agents": {
# 49                 AGENT.STABILITY.value: 4 
# 50             }
# 51         }
# 52     )
# 53 ]
# 54 
# 55 def seed_state_providers(db_session: Session):
# 56     EXTENSIONS_DIR.mkdir(parents=True, exist_ok=True)
# 57 
# 58     for id, filename, name, description, tags, config in STATE_PROVIDERS:
# 59         guid = str(uuid4())
# 60         source_file = SEED_FILES_DIR / filename
# 61         dest_filename = f"{guid}.py"
# 62         dest_file = EXTENSIONS_DIR / dest_filename
# 63 
# 64         if not source_file.exists():
# 65             raise FileNotFoundError(f"State provider script not found: {source_file}")
# 66 
# 67         shutil.copy(source_file, dest_file)
# 68 
# 69         record = StateProviderConfig(
# 70             id=id,
# 71             guid=guid,
# 72             name=name,
# 73             description=description,
# 74             config=config,
# 75             artifact_path=dest_filename,
# 76             tags=tags,
# 77         )
# 78 
# 79         db_session.add(record)
# 80 
# 81     db_session.commit()
# 82     print("✅ Seeded state provider configurations successfully.")


# db\seeders\seed_system_providers.py

# 1 from uuid import uuid4
# 2 from pathlib import Path
# 3 import shutil
# 4 from sqlalchemy.orm import Session
# 5 from app.db.models import SystemProviderConfig
# 6 from app.enums.state_enums import STATE
# 7 
# 8 SEED_FILES_DIR = Path(__file__).resolve().parent / "files/system_providers"
# 9 PROJECT_ROOT = SEED_FILES_DIR.parent.parent.parent.parent.parent
# 10 EXTENSIONS_DIR = PROJECT_ROOT / "extensions"
# 11 
# 12 SYSTEM_PROVIDERS = [
# 13     {
# 14         "id": 1,
# 15         "filename": "linting_system_provider.py",
# 16         "name": "linting_system_provider",
# 17         "description": "System to iteratively lint and evaluate Python code.",
# 18         "tags": ["linting", "system"],
# 19         "config": {
# 20             "score_provider_id": 1,
# 21             "states": {
# 22                 STATE.GENERATE.value: 1,
# 23                 STATE.DISCRIMINATE.value: 2,
# 24                 STATE.CODE_STABILITY.value: 3
# 25             }
# 26         }
# 27     }
# 28 ]
# 29 
# 30 def seed_system_providers(db_session: Session):
# 31     EXTENSIONS_DIR.mkdir(parents=True, exist_ok=True)
# 32 
# 33     for entry in SYSTEM_PROVIDERS:
# 34         guid = str(uuid4())
# 35         source_file = SEED_FILES_DIR / entry["filename"]
# 36         dest_filename = f"{guid}.py"
# 37         dest_file = EXTENSIONS_DIR / dest_filename
# 38 
# 39         if not source_file.exists():
# 40             raise FileNotFoundError(f"System provider script not found: {source_file}")
# 41 
# 42         shutil.copy(source_file, dest_file)
# 43 
# 44         record = SystemProviderConfig(
# 45             id=entry["id"],
# 46             guid=guid,
# 47             name=entry["name"],
# 48             description=entry["description"],
# 49             config=entry["config"],
# 50             artifact_path=dest_filename,
# 51             tags=entry["tags"],
# 52         )
# 53 
# 54         db_session.add(record)
# 55 
# 56     db_session.commit()
# 57     print("✅ Seeded system provider configurations successfully.")


# db\seeders\seed_tool_providers.py

# 1 from uuid import uuid4
# 2 from pathlib import Path
# 3 import shutil
# 4 from sqlalchemy.orm import Session
# 5 from app.db.models import ToolProviderConfig
# 6 
# 7 CURRENT_DIR = Path(__file__).resolve().parent
# 8 SEED_FILES_DIR = CURRENT_DIR / "files/tool_providers"
# 9 PROJECT_ROOT = SEED_FILES_DIR.parent.parent.parent.parent.parent
# 10 EXTENSIONS_DIR = PROJECT_ROOT / "extensions"
# 11 
# 12 TOOLS = [
# 13     {
# 14         "id": 1,
# 15         "filename": "black_tool.py",
# 16         "name": "black",
# 17         "description": "Formats Python code using Black.",
# 18         "config": {}
# 19     },
# 20     {
# 21         "id": 2,
# 22         "filename": "sonarcloud_tool.py",
# 23         "name": "sonarcloud",
# 24         "description": "Static analysis via SonarCloud.",
# 25         "config": {}
# 26     },
# 27     {
# 28         "id": 3,
# 29         "filename": "ruff_tool.py",
# 30         "name": "ruff",
# 31         "description": "Python linting with Ruff.",
# 32         "config": {}
# 33     },
# 34     {
# 35         "id": 4,
# 36         "filename": "radon_tool.py",
# 37         "name": "radon",
# 38         "description": "Analyzes Python code complexity.",
# 39         "config": {}
# 40     },
# 41     {
# 42         "id": 5,
# 43         "filename": "mypy_tool.py",
# 44         "name": "mypy",
# 45         "description": "Static type checking with mypy.",
# 46         "config": {}
# 47     },
# 48     {
# 49         "id": 6,
# 50         "filename": "docformatter_tool.py",
# 51         "name": "docformatter",
# 52         "description": "Formats docstrings using docformatter.",
# 53         "config": {}
# 54     },
# 55     {
# 56         "id": 7,
# 57         "filename": "symbol_graph.py",
# 58         "name": "symbol_graph",
# 59         "description": "Extracts and analyzes Python symbols into structured graphs.",
# 60         "config": {}
# 61     }
# 62 ]
# 63 
# 64 def seed_tool_providers(db_session: Session):
# 65     EXTENSIONS_DIR.mkdir(parents=True, exist_ok=True)
# 66 
# 67     for entry in TOOLS:
# 68         guid = str(uuid4())
# 69         dest_filename = f"{guid}.py"
# 70         source_file = SEED_FILES_DIR / entry["filename"]
# 71         dest_file = EXTENSIONS_DIR / dest_filename
# 72 
# 73         if not source_file.exists():
# 74             raise FileNotFoundError(f"Tool script not found: {source_file}")
# 75 
# 76         shutil.copy(source_file, dest_file)
# 77 
# 78         tool = ToolProviderConfig(
# 79             id=entry["id"],
# 80             guid=guid,
# 81             name=entry["name"],
# 82             description=entry["description"],
# 83             config=entry["config"],
# 84             artifact_path=dest_filename
# 85         )
# 86 
# 87         db_session.add(tool)
# 88 
# 89     db_session.commit()
# 90     print("Seeded tool configurations successfully.")


# enums\__init__.py



# enums\agent_engine_enums.py

# 1 from enum import Enum
# 2 
# 3 
# 4 class AGENT_ENGINE_MODEL(str, Enum):
# 5     MOCK = "mock-llm"
# 6     GPT_4O = "gpt-4o"
# 7     GEMINI_1_5_PRO = "gemini-1.5-pro"
# 8     CLAUDE_3_SONNET = "claude-3-sonnet"


# enums\agent_enums.py

# 1 from __future__ import annotations
# 2 
# 3 from enum import Enum
# 4 
# 5 
# 6 class AGENT(str, Enum):
# 7     START = "start"
# 8     END = "end"
# 9     GENERATOR = "generator"
# 10     DISCRIMINATOR = "discriminator"
# 11     MEDIATOR = "mediator"
# 12     STABILITY = "stability"
# 13     BASIC = "basic"
# 14     UNKNOWN = "unknown"
# 15 
# 16 class AGENT_STATE(Enum):
# 17     INIT = "init"
# 18     RUNNING = "running"
# 19     COMPLETE = "complete"


# enums\controller_enums.py

# 1 from enum import Enum
# 2 
# 3 class CONTROLLER(str, Enum):
# 4     START = "start"
# 5     END = "end"
# 6     PREPROCESSING = "preprocessing_controller"
# 7     TESTING = "testing_controller"
# 8     OBSERVABILITY = "observability_controller"
# 9     DOCUMENTATION = "documentation_controller"
# 10     REVIEW = "review_controller"
# 11     PLANNING = "planning_controller"
# 12     MLOPS = "mlops_controller"
# 13     SAFETY = "safety_controller"
# 14     MISC = "misc_controller"


# enums\fsm_enums.py

# 1 from enum import Enum
# 2 
# 3 class STATE_TYPE(str, Enum):
# 4     START = "start"
# 5     INTERMEDIATE = "intermediate"
# 6     END = "end"
# 7 
# 8 
# 9 class STATE_DECISION_TYPE(str, Enum):
# 10     INITIAL = "initial"
# 11     IMPROVED = "improved"
# 12     REJECTED = "rejected"
# 13     FINAL = "final"
# 14     UNKNOWN = "unknown"
# 15 
# 16 
# 17 class DECISION_TYPE(str, Enum):
# 18     ACCEPT = "accept"
# 19     REJECT = "reject"
# 20     UNKNOWN = "unknown"
# 21 
# 22 
# 23 class TRANSITION_REASON_TYPE(str, Enum):
# 24     INITIALIZATION = "initialization"
# 25     SUCCESSFUL = "completed successfully"
# 26     UNSUCCESSFUL = "completed unsuccessfully"
# 27     GENERATING = "generating"
# 28     EVALUATING = "evaluating"
# 29     PASSED = "passed"
# 30     FAILED = "failed"    
# 31     ERROR = "error"
# 32     CUSTOM_RULE = "custom_rule"
# 33 


# enums\logging_enums.py

# 1 from enum import Enum
# 2 
# 3 class LOG_TYPE(str, Enum):
# 4     ERROR = "error_log"
# 5     PROVIDER = "provider_log"
# 6     STATE_TRANSITION = "state_transition_log"
# 7     AGENT_CONVERSATION = "agent_conversation_log"
# 8     SNAPSHOT_METRICS = "snapshot_metrics"
# 9 
# 10 class PROVIDER_TYPE(str, Enum):
# 11     TOOL = "tool"
# 12     AGENT_ENGINE = "agent_engine"
# 13     AGENT = "agent"
# 14     PROMPT = "prompt"
# 15     CONTEXT = "context"
# 16     SCORE = "score"
# 17     STATE = "state"
# 18     SYSTEM = "system"
# 19     CONTROLLER = "controller"
# 20     PROGRAM = "program"
# 21     SESSION = "session"
# 22     UNKNOWN = "unknown"
# 23 
# 24 class ERROR_TYPE(str, Enum):
# 25     RUNTIME = "runtime"
# 26     VALIDATION = "validation"
# 27     CONFIGURATION = "configuration"
# 28     CONNECTION = "connection"
# 29     AUTHENTICATION = "authentication"
# 30     TIMEOUT = "timeout"
# 31     UNKNOWN = "unknown"


# enums\scoring_enums.py

# 1 from enum import Enum
# 2 
# 3 class SCORING_METRIC_TYPE(str, Enum):
# 4     LINTING_SCORE = "linting_score"
# 5     CODE_STABILITY_SCORE = "code_stability_score"


# enums\state_enums.py

# 1 from enum import Enum
# 2 
# 3 class STATE(str, Enum):
# 4     START = "start"
# 5     END = "end"
# 6     PREPROCESS = "preprocess"
# 7     CODE_STABILITY = "code_stability"
# 8     GENERATE = "generate"
# 9     DISCRIMINATE = "discriminate"


# enums\system_enums.py

# 1 from __future__ import annotations
# 2 from enum import Enum
# 3 
# 4 
# 5 class SYSTEM(str, Enum):
# 6     START = "start"
# 7     END = "end"
# 8 
# 9     #  Core Code Transformation
# 10     LINTING = "linting"
# 11     FORMATTING = "formatting"
# 12     DOCSTRING = "docstring"
# 13     TYPE_ANNOTATION = "type_annotation"
# 14     REFACTORING = "refactoring"
# 15     DEAD_CODE_REMOVAL = "dead_code_removal"
# 16     COMPLEXITY_REDUCTION = "complexity_reduction"
# 17 
# 18     #  Testing and Validation
# 19     UNIT_TESTING = "unit_testing"
# 20     EDGE_TESTING = "edge_testing"
# 21     INTEGRATION_TESTING = "integration_testing"
# 22     TEST_FIX_GENERATION = "test_fix_generation"
# 23     MOCK_GENERATION = "mock_generation"
# 24     STUB_EXTRACTION = "stub_extraction"
# 25 
# 26     #  Observability and Instrumentation
# 27     LOGGING_INJECTION = "logging_injection"
# 28     TRACE_ANNOTATION = "trace_annotation"
# 29     ERROR_HANDLING = "error_handling"
# 30 
# 31     #  Documentation and Explanation
# 32     INLINE_COMMENTING = "inline_commenting"
# 33     FUNCTION_SUMMARIZATION = "function_summarization"
# 34     FILE_SUMMARIZATION = "file_summarization"
# 35     CHANGE_SUMMARIZATION = "change_summarization"
# 36 
# 37     #  Review and Oversight
# 38     CODE_REVIEW = "code_review"
# 39     STATIC_ANALYSIS = "static_analysis"
# 40     REGRESSION_ANALYSIS = "regression_analysis"
# 41     MEDIATOR_RECONCILIATION = "mediator_reconciliation"
# 42     SCORING_EVALUATION = "scoring_evaluation"
# 43 
# 44     #  Planning and Organization
# 45     SYMBOL_GRAPH_GENERATION = "symbol_graph_generation"
# 46     DEPENDENCY_MAPPING = "dependency_mapping"
# 47     FILE_RESTRUCTURING = "file_restructuring"
# 48     FUNCTION_SPLITTING = "function_splitting"
# 49 
# 50     #  MLOps-Specific
# 51     DATA_VALIDATION = "data_validation"
# 52     METADATA_ANNOTATION = "metadata_annotation"
# 53     MODEL_DOC_GENERATION = "model_doc_generation"
# 54     PIPELINE_TESTING = "pipeline_testing"
# 55     TRAINING_CONFIG_AUDIT = "training_config_audit"
# 56     FEATURE_TRACEABILITY = "feature_traceability"
# 57 
# 58     #  Production Safety
# 59     SECURITY_CHECK = "security_check"
# 60     POLICY_COMPLIANCE = "policy_compliance"
# 61     LICENSE_CHECK = "license_check"
# 62 
# 63     #  Misc / Cross-cutting
# 64     PATCHING = "patching"
# 65     PROMPT_REFINEMENT = "prompt_refinement"
# 66     AGENT_RECOMMENDATION = "agent_recommendation"
# 67     SEMANTIC_DIFF = "semantic_diff"
# 68     CONTEXT_FILTERING = "context_filtering"
# 69 
# 70     UNKNOWN = "unknown"
# 71 
# 72 
# 73 
# 74 


# factories\__init__.py



# factories\agent_engine_provider_factory.py

# 1 from app.db.models import AgentEngineProviderConfig
# 2 from app.factories.base_provider_factory import BaseProviderFactory
# 3 from app.providers.agent_engine_provider_base import AgentEngineProviderBase
# 4 
# 5 
# 6 class AgentEngineProviderFactory(BaseProviderFactory):
# 7     config_model = AgentEngineProviderConfig
# 8     base_class = AgentEngineProviderBase


# factories\agent_provider_factory.py

# 1 from app.factories.base_provider_factory import BaseProviderFactory
# 2 from app.db.models import AgentProviderConfig
# 3 from app.providers.agent_provider_base import AgentProviderBase
# 4 from app.enums.logging_enums import PROVIDER_TYPE
# 5 
# 6 
# 7 class AgentProviderFactory(BaseProviderFactory):
# 8     config_model = AgentProviderConfig
# 9     base_class   = AgentProviderBase
# 10 
# 11     @classmethod
# 12     def create(
# 13         cls,
# 14         id: int,
# 15         *,
# 16         called_by_type: PROVIDER_TYPE | None = None,
# 17         called_by_id: int | None = None,
# 18         **kwargs
# 19     ) -> AgentProviderBase:
# 20         #  delayed imports to avoid circular dependencies
# 21         from app.factories.agent_engine_provider_factory import AgentEngineProviderFactory
# 22         from app.factories.prompt_provider_factory      import PromptProviderFactory
# 23         from app.factories.context_provider_factory     import ContextProviderFactory
# 24         from app.factories.score_provider_factory       import ScoreProviderFactory
# 25         from app.factories.tool_provider_factory        import ToolProviderFactory
# 26 
# 27         #  1) Preload base instance to get config/engine/type
# 28         preload_instance = super().create(
# 29             id,
# 30             called_by_type=called_by_type,
# 31             called_by_id=called_by_id,
# 32         )
# 33 
# 34         config = preload_instance._config.config or {}
# 35         provider_id = preload_instance._config.id
# 36         provider_type = preload_instance._infer_provider_type()
# 37 
# 38         #  2) Construct dependencies, passing caller identity
# 39         agent_engine = (
# 40             AgentEngineProviderFactory.create(
# 41                 config["agent_engine_provider_id"],
# 42                 called_by_type=provider_type,
# 43                 called_by_id=provider_id
# 44             ) if config.get("agent_engine_provider_id") else None
# 45         )
# 46 
# 47         prompt_provider = (
# 48             PromptProviderFactory.create(
# 49                 config["prompt_provider_id"],
# 50                 called_by_type=provider_type,
# 51                 called_by_id=provider_id
# 52             ) if config.get("prompt_provider_id") else None
# 53         )
# 54 
# 55         context_provider = (
# 56             ContextProviderFactory.create(
# 57                 config["context_provider_id"],
# 58                 called_by_type=provider_type,
# 59                 called_by_id=provider_id
# 60             ) if config.get("context_provider_id") else None
# 61         )
# 62 
# 63         score_provider = (
# 64             ScoreProviderFactory.create(
# 65                 config["score_provider_id"],
# 66                 called_by_type=provider_type,
# 67                 called_by_id=provider_id
# 68             ) if config.get("score_provider_id") else None
# 69         )
# 70 
# 71         tool_providers = [
# 72             ToolProviderFactory.create(
# 73                 tid,
# 74                 called_by_type=provider_type,
# 75                 called_by_id=provider_id
# 76             )
# 77             for tid in (config.get("tool_provider_ids") or {}).values()
# 78         ]
# 79 
# 80         #  3) Build final typed instance with dependencies
# 81         cls_type = type(preload_instance)
# 82         return cls_type(
# 83             config           = preload_instance._config,
# 84             agent_engine     = agent_engine,
# 85             prompt_provider  = prompt_provider,
# 86             context_provider = context_provider,
# 87             score_provider   = score_provider,
# 88             tool_providers   = tool_providers,
# 89             called_by_type   = called_by_type,
# 90             called_by_id     = called_by_id,
# 91         )


# factories\base_provider_factory.py

# 1 from sqlalchemy.orm import Session
# 2 from importlib.util import spec_from_file_location, module_from_spec
# 3 from pathlib import Path
# 4 
# 5 from app.db import init_db  #  global DB engine instance
# 6 from app.providers.base_provider import BaseProvider
# 7 from app.enums.logging_enums import PROVIDER_TYPE
# 8 
# 9 PROJECT_ROOT = Path(__file__).resolve().parents[2]
# 10 EXTENSIONS_DIR = PROJECT_ROOT / "extensions"
# 11 
# 12 engine = init_db(reset=False)
# 13 
# 14 class BaseProviderFactory:
# 15     config_model = None
# 16     base_class = BaseProvider
# 17 
# 18     @classmethod
# 19     def create(
# 20         cls,
# 21         id: int,
# 22         *,
# 23         called_by_type: PROVIDER_TYPE | None = None,
# 24         called_by_id: int | None = None,
# 25         **kwargs,
# 26     ) -> BaseProvider:
# 27         with Session(bind=engine) as session:
# 28             config = session.get(cls.config_model, int(id))
# 29             if not config:
# 30                 raise ValueError(f"{cls.config_model.__name__} ID {id} not found")
# 31 
# 32             ext_path = (EXTENSIONS_DIR / config.artifact_path).resolve()
# 33             if not ext_path.exists():
# 34                 raise FileNotFoundError(f"Extension not found: {ext_path}")
# 35 
# 36         spec = spec_from_file_location(ext_path.stem, ext_path)
# 37         if spec is None or spec.loader is None:
# 38             raise ImportError(f"Cannot load module spec for {ext_path}")
# 39 
# 40         module = module_from_spec(spec)
# 41         spec.loader.exec_module(module)
# 42 
# 43         provider_class = next(
# 44             (obj for obj in vars(module).values()
# 45              if isinstance(obj, type) and issubclass(obj, cls.base_class) and obj is not cls.base_class),
# 46             None
# 47         )
# 48 
# 49         if provider_class is None:
# 50             raise ImportError(f"No valid subclass of {cls.base_class.__name__} found in {ext_path}")
# 51 
# 52         instance = provider_class(
# 53             config=config,
# 54             called_by_type=called_by_type,
# 55             called_by_id=called_by_id,
# 56             **kwargs
# 57         )
# 58         return instance


# factories\context_provider_factory.py

# 1 from app.factories.base_provider_factory import BaseProviderFactory
# 2 from app.db.models import ContextProviderConfig
# 3 from app.providers.context_provider_base import ContextProviderBase
# 4 from app.enums.logging_enums import PROVIDER_TYPE
# 5 
# 6 
# 7 class ContextProviderFactory(BaseProviderFactory):
# 8     config_model = ContextProviderConfig
# 9     base_class = ContextProviderBase
# 10 
# 11     @classmethod
# 12     def create(
# 13         cls,
# 14         id: int,
# 15         *,
# 16         called_by_type: PROVIDER_TYPE | None = None,
# 17         called_by_id: int | None = None,
# 18         **kwargs
# 19     ) -> ContextProviderBase:
# 20         instance = super().create(
# 21             id,
# 22             called_by_type=called_by_type,
# 23             called_by_id=called_by_id,
# 24             **kwargs
# 25         )
# 26         config = instance._config.config or {}
# 27         provider_id = instance._config.id
# 28         provider_type = instance._infer_provider_type()
# 29 
# 30         #  ⏱️ Delayed imports to avoid circular dependencies
# 31         if "score_provider_id" in config or "tool_provider_ids" in config:
# 32             from app.factories.score_provider_factory import ScoreProviderFactory
# 33             from app.factories.tool_provider_factory import ToolProviderFactory
# 34 
# 35             if (score_id := config.get("score_provider_id")):
# 36                 if isinstance(score_id, int) and score_id > 0:
# 37                     score = ScoreProviderFactory.create(
# 38                         score_id,
# 39                         called_by_type=provider_type,
# 40                         called_by_id=provider_id
# 41                     )
# 42                     instance.set_score_provider(score)
# 43 
# 44             for _, tool_id in (config.get("tool_provider_ids") or {}).items():
# 45                 tool = ToolProviderFactory.create(
# 46                     tool_id,
# 47                     called_by_type=provider_type,
# 48                     called_by_id=provider_id
# 49                 )
# 50                 instance.set_tool_provider(tool)
# 51 
# 52         return instance


# factories\controller_provider_factory.py

# 1 from app.factories.base_provider_factory import BaseProviderFactory
# 2 from app.db.models import ControllerProviderConfig
# 3 from app.providers.controller_provider_base import ControllerProviderBase
# 4 from app.enums.logging_enums import PROVIDER_TYPE
# 5 
# 6 
# 7 class ControllerProviderFactory(BaseProviderFactory):
# 8     config_model = ControllerProviderConfig
# 9     base_class = ControllerProviderBase
# 10 
# 11     @classmethod
# 12     def create(
# 13         cls,
# 14         id: int,
# 15         *,
# 16         called_by_type: PROVIDER_TYPE | None = None,
# 17         called_by_id: int | None = None,
# 18         **kwargs
# 19     ) -> ControllerProviderBase:
# 20         from app.factories.context_provider_factory import ContextProviderFactory
# 21         from app.factories.score_provider_factory   import ScoreProviderFactory
# 22         from app.factories.tool_provider_factory    import ToolProviderFactory
# 23         from app.factories.system_provider_factory  import SystemProviderFactory
# 24 
# 25         inst = super().create(
# 26             id,
# 27             called_by_type=called_by_type,
# 28             called_by_id=called_by_id,
# 29             **kwargs
# 30         )
# 31         config = inst._config.config or {}
# 32         provider_id = inst._config.id
# 33         provider_type = inst._infer_provider_type()
# 34 
# 35         context_provider = (
# 36             ContextProviderFactory.create(
# 37                 config["context_provider_id"],
# 38                 called_by_type=provider_type,
# 39                 called_by_id=provider_id
# 40             ) if config.get("context_provider_id") else None
# 41         )
# 42 
# 43         score_provider = (
# 44             ScoreProviderFactory.create(
# 45                 config["score_provider_id"],
# 46                 called_by_type=provider_type,
# 47                 called_by_id=provider_id
# 48             ) if config.get("score_provider_id") else None
# 49         )
# 50 
# 51         tool_providers = [
# 52             ToolProviderFactory.create(
# 53                 tid,
# 54                 called_by_type=provider_type,
# 55                 called_by_id=provider_id
# 56             )
# 57             for tid in (config.get("tool_provider_ids") or {}).values()
# 58         ]
# 59 
# 60         system_providers = {
# 61             name: SystemProviderFactory.create(
# 62                 sys_id,
# 63                 called_by_type=provider_type,
# 64                 called_by_id=provider_id
# 65             )
# 66             for name, sys_id in (config.get("systems") or {}).items()
# 67         }
# 68 
# 69         cls_type = type(inst)
# 70         return cls_type(
# 71             config           = inst._config,
# 72             context_provider = context_provider,
# 73             score_provider   = score_provider,
# 74             tool_providers   = tool_providers,
# 75             system_providers = system_providers, 
# 76             called_by_type   = called_by_type,
# 77             called_by_id     = called_by_id,
# 78         )


# factories\program_provider_factory.py

# 1 from app.factories.base_provider_factory import BaseProviderFactory
# 2 from app.db.models import ProgramProviderConfig
# 3 from app.providers.program_provider_base import ProgramProviderBase
# 4 from app.enums.logging_enums import PROVIDER_TYPE
# 5 
# 6 
# 7 class ProgramProviderFactory(BaseProviderFactory):
# 8     config_model = ProgramProviderConfig
# 9     base_class = ProgramProviderBase
# 10 
# 11     @classmethod
# 12     def create(
# 13         cls,
# 14         id: int,
# 15         *,
# 16         called_by_type: PROVIDER_TYPE | None = None,
# 17         called_by_id: int | None = None,
# 18         **kwargs
# 19     ) -> ProgramProviderBase:
# 20         from app.factories.context_provider_factory import ContextProviderFactory
# 21         from app.factories.score_provider_factory   import ScoreProviderFactory
# 22         from app.factories.tool_provider_factory    import ToolProviderFactory
# 23         from app.factories.controller_provider_factory import ControllerProviderFactory
# 24 
# 25         inst = super().create(
# 26             id,
# 27             called_by_type=called_by_type,
# 28             called_by_id=called_by_id,
# 29             **kwargs
# 30         )
# 31         config = inst._config.config or {}
# 32         provider_id = inst._config.id
# 33         provider_type = inst._infer_provider_type()
# 34 
# 35         context_provider = (
# 36             ContextProviderFactory.create(
# 37                 config["context_provider_id"],
# 38                 called_by_type=provider_type,
# 39                 called_by_id=provider_id
# 40             ) if config.get("context_provider_id") else None
# 41         )
# 42 
# 43         score_provider = (
# 44             ScoreProviderFactory.create(
# 45                 config["score_provider_id"],
# 46                 called_by_type=provider_type,
# 47                 called_by_id=provider_id
# 48             ) if config.get("score_provider_id") else None
# 49         )
# 50 
# 51         tool_providers = [
# 52             ToolProviderFactory.create(
# 53                 tid,
# 54                 called_by_type=provider_type,
# 55                 called_by_id=provider_id
# 56             )
# 57             for tid in (config.get("tool_provider_ids") or {}).values()
# 58         ]
# 59 
# 60         controller_providers = {
# 61             name: ControllerProviderFactory.create(
# 62                 controller_id,
# 63                 called_by_type=provider_type,
# 64                 called_by_id=provider_id
# 65             )
# 66             for name, controller_id in (config.get("controllers") or {}).items()
# 67         }
# 68 
# 69         cls_type = type(inst)
# 70         return cls_type(
# 71             config=inst._config,
# 72             context_provider=context_provider,
# 73             score_provider=score_provider,
# 74             tool_providers=tool_providers,
# 75             controller_providers=controller_providers,
# 76             called_by_type=called_by_type,
# 77             called_by_id=called_by_id,
# 78         )


# factories\prompt_provider_factory.py

# 1 from app.db.models import PromptProviderConfig
# 2 from app.factories.base_provider_factory import BaseProviderFactory
# 3 from app.providers.prompt_provider_base import PromptProviderBase
# 4 
# 5 
# 6 class PromptProviderFactory(BaseProviderFactory):
# 7     config_model = PromptProviderConfig
# 8     base_class = PromptProviderBase


# factories\score_provider_factory.py

# 1 from app.factories.base_provider_factory import BaseProviderFactory
# 2 from app.db.models import ScoreProviderConfig
# 3 from app.providers.score_provider_base import ScoreProviderBase
# 4 from app.enums.logging_enums import PROVIDER_TYPE
# 5 
# 6 
# 7 class ScoreProviderFactory(BaseProviderFactory):
# 8     config_model = ScoreProviderConfig
# 9     base_class = ScoreProviderBase
# 10 
# 11     @classmethod
# 12     def create(
# 13         cls,
# 14         id: int,
# 15         *,
# 16         called_by_type: PROVIDER_TYPE | None = None,
# 17         called_by_id: int | None = None,
# 18         **kwargs
# 19     ) -> ScoreProviderBase:
# 20         instance = super().create(
# 21             id,
# 22             called_by_type=called_by_type,
# 23             called_by_id=called_by_id,
# 24             **kwargs
# 25         )
# 26         config = instance._config.config or {}
# 27         provider_id = instance._config.id
# 28         provider_type = instance._infer_provider_type()
# 29 
# 30         #  ⏱️ Delayed imports to prevent circular references
# 31         if "tool_provider_ids" in config or "context_provider_id" in config:
# 32             from app.factories.tool_provider_factory import ToolProviderFactory
# 33             from app.factories.context_provider_factory import ContextProviderFactory
# 34 
# 35             if (ctx_id := config.get("context_provider_id")):
# 36                 context = ContextProviderFactory.create(
# 37                     ctx_id,
# 38                     called_by_type=provider_type,
# 39                     called_by_id=provider_id
# 40                 )
# 41                 instance.set_context_provider(context)
# 42                 if hasattr(context, "set_score_provider"):
# 43                     context.set_score_provider(instance)
# 44 
# 45             for _, tool_id in (config.get("tool_provider_ids") or {}).items():
# 46                 tool = ToolProviderFactory.create(
# 47                     tool_id,
# 48                     called_by_type=provider_type,
# 49                     called_by_id=provider_id
# 50                 )
# 51                 instance.set_tool_provider(tool)
# 52 
# 53         return instance


# factories\state_provider_factory.py

# 1 from app.factories.base_provider_factory import BaseProviderFactory
# 2 from app.db.models import StateProviderConfig
# 3 from app.providers.state_provider_base import StateProviderBase
# 4 from app.enums.logging_enums import PROVIDER_TYPE
# 5 
# 6 
# 7 class StateProviderFactory(BaseProviderFactory):
# 8     config_model = StateProviderConfig
# 9     base_class = StateProviderBase
# 10 
# 11     @classmethod
# 12     def create(
# 13         cls,
# 14         id: int,
# 15         *,
# 16         called_by_type: PROVIDER_TYPE | None = None,
# 17         called_by_id: int | None = None,
# 18         **kwargs
# 19     ) -> StateProviderBase:
# 20         from app.factories.agent_provider_factory import AgentProviderFactory
# 21         from app.factories.context_provider_factory import ContextProviderFactory
# 22         from app.factories.score_provider_factory import ScoreProviderFactory
# 23         from app.factories.tool_provider_factory import ToolProviderFactory
# 24 
# 25         preload_instance = super().create(
# 26             id,
# 27             called_by_type=called_by_type,
# 28             called_by_id=called_by_id,
# 29             **kwargs
# 30         )
# 31 
# 32         config = preload_instance._config.config or {}
# 33         provider_id = preload_instance._config.id
# 34         provider_type = preload_instance._infer_provider_type()
# 35 
# 36         agent_providers = {
# 37             name: AgentProviderFactory.create(
# 38                 agent_id,
# 39                 called_by_type=provider_type,
# 40                 called_by_id=provider_id
# 41             )
# 42             for name, agent_id in config.get("agents", {}).items()
# 43         }
# 44 
# 45         context_provider = (
# 46             ContextProviderFactory.create(
# 47                 config["context_provider_id"],
# 48                 called_by_type=provider_type,
# 49                 called_by_id=provider_id
# 50             )
# 51             if config.get("context_provider_id") else None
# 52         )
# 53 
# 54         score_provider = (
# 55             ScoreProviderFactory.create(
# 56                 config["score_provider_id"],
# 57                 called_by_type=provider_type,
# 58                 called_by_id=provider_id
# 59             )
# 60             if config.get("score_provider_id") else None
# 61         )
# 62 
# 63         tool_providers = [
# 64             ToolProviderFactory.create(
# 65                 tool_id,
# 66                 called_by_type=provider_type,
# 67                 called_by_id=provider_id
# 68             )
# 69             for _, tool_id in (config.get("tool_provider_ids") or {}).items()
# 70         ]
# 71 
# 72         cls_type = type(preload_instance)
# 73         return cls_type(
# 74             config=preload_instance._config,
# 75             agent_providers=agent_providers,
# 76             context_provider=context_provider,
# 77             score_provider=score_provider,
# 78             tool_providers=tool_providers,
# 79             called_by_type=called_by_type,
# 80             called_by_id=called_by_id,
# 81         )


# factories\system_provider_factory.py

# 1 from app.factories.base_provider_factory import BaseProviderFactory
# 2 from app.db.models import SystemProviderConfig
# 3 from app.providers.system_provider_base import SystemProviderBase
# 4 from app.enums.logging_enums import PROVIDER_TYPE
# 5 
# 6 
# 7 class SystemProviderFactory(BaseProviderFactory):
# 8     config_model = SystemProviderConfig
# 9     base_class = SystemProviderBase
# 10 
# 11     @classmethod
# 12     def create(
# 13         cls,
# 14         id: int,
# 15         *,
# 16         called_by_type: PROVIDER_TYPE | None = None,
# 17         called_by_id: int | None = None,
# 18         **kwargs
# 19     ) -> SystemProviderBase:
# 20         #  ⏱️ Delayed imports to avoid circular dependencies
# 21         from app.factories.state_provider_factory import StateProviderFactory
# 22         from app.factories.context_provider_factory import ContextProviderFactory
# 23         from app.factories.score_provider_factory import ScoreProviderFactory
# 24         from app.factories.tool_provider_factory import ToolProviderFactory
# 25 
# 26         preload_instance = super().create(
# 27             id,
# 28             called_by_type=called_by_type,
# 29             called_by_id=called_by_id,
# 30             **kwargs
# 31         )
# 32 
# 33         config = preload_instance._config.config or {}
# 34         provider_id = preload_instance._config.id
# 35         provider_type = preload_instance._infer_provider_type()
# 36 
# 37         context_provider = (
# 38             ContextProviderFactory.create(
# 39                 config["context_provider_id"],
# 40                 called_by_type=provider_type,
# 41                 called_by_id=provider_id
# 42             ) if config.get("context_provider_id") else None
# 43         )
# 44 
# 45         score_provider = (
# 46             ScoreProviderFactory.create(
# 47                 config["score_provider_id"],
# 48                 called_by_type=provider_type,
# 49                 called_by_id=provider_id
# 50             ) if config.get("score_provider_id") else None
# 51         )
# 52 
# 53         tool_providers = [
# 54             ToolProviderFactory.create(
# 55                 tool_id,
# 56                 called_by_type=provider_type,
# 57                 called_by_id=provider_id
# 58             )
# 59             for _, tool_id in (config.get("tool_provider_ids") or {}).items()
# 60         ]
# 61 
# 62         state_providers = {
# 63             name: StateProviderFactory.create(
# 64                 state_id,
# 65                 called_by_type=provider_type,
# 66                 called_by_id=provider_id
# 67             )
# 68             for name, state_id in (config.get("states") or {}).items()
# 69         }
# 70 
# 71         cls_type = type(preload_instance)
# 72         return cls_type(
# 73             config=preload_instance._config,
# 74             context_provider=context_provider,
# 75             score_provider=score_provider,
# 76             tool_providers=tool_providers,
# 77             state_providers=state_providers,
# 78             called_by_type=called_by_type,
# 79             called_by_id=called_by_id,
# 80         )


# factories\tool_provider_factory.py

# 1 from app.factories.base_provider_factory import BaseProviderFactory
# 2 from app.db.models import ToolProviderConfig
# 3 from app.providers.tool_provider_base import ToolProviderBase
# 4 from app.enums.logging_enums import PROVIDER_TYPE
# 5 
# 6 
# 7 class ToolProviderFactory(BaseProviderFactory):
# 8     config_model = ToolProviderConfig
# 9     base_class = ToolProviderBase
# 10 
# 11     @classmethod
# 12     def create(
# 13         cls,
# 14         id: int,
# 15         *,
# 16         called_by_type: PROVIDER_TYPE | None = None,
# 17         called_by_id: int | None = None,
# 18         **kwargs
# 19     ) -> ToolProviderBase:
# 20         instance = super().create(
# 21             id,
# 22             called_by_type=called_by_type,
# 23             called_by_id=called_by_id,
# 24             **kwargs
# 25         )
# 26         config = instance._config.config or {}
# 27         provider_id = instance._config.id
# 28         provider_type = instance._infer_provider_type()
# 29 
# 30         #  ⏱️ Delayed imports to avoid circular dependencies
# 31         if "context_provider_id" in config or "score_provider_id" in config:
# 32             from app.factories.context_provider_factory import ContextProviderFactory
# 33             from app.factories.score_provider_factory import ScoreProviderFactory
# 34 
# 35             if (ctx_id := config.get("context_provider_id")):
# 36                 context = ContextProviderFactory.create(
# 37                     ctx_id,
# 38                     called_by_type=provider_type,
# 39                     called_by_id=provider_id
# 40                 )
# 41                 instance.set_context_provider(context)
# 42                 if hasattr(context, "set_tool_provider"):
# 43                     context.set_tool_provider(instance)
# 44 
# 45             if (score_id := config.get("score_provider_id")):
# 46                 if isinstance(score_id, int) and score_id > 0:
# 47                     score = ScoreProviderFactory.create(
# 48                         score_id,
# 49                         called_by_type=provider_type,
# 50                         called_by_id=provider_id
# 51                     )
# 52                     instance.set_score_provider(score)
# 53                     if hasattr(score, "set_tool_provider"):
# 54                         score.set_tool_provider(instance)
# 55 
# 56         return instance


# providers\__init__.py



# providers\agent_engine_provider_base.py

# 1 from __future__ import annotations
# 2 from abc import abstractmethod
# 3 from datetime import datetime, timezone
# 4 from pathlib import Path
# 5 from sqlalchemy.orm import Session
# 6 
# 7 from app.db.models import SnapshotMetrics
# 8 from app.db.schemas import AgentEngineOutput, SnapshotMetricsSchema
# 9 from app.enums.system_enums import SYSTEM
# 10 from app.enums.agent_enums import AGENT
# 11 from app.enums.fsm_enums import DECISION_TYPE
# 12 from app.enums.logging_enums import LOG_TYPE
# 13 
# 14 from app.providers.base_provider import BaseProvider
# 15 from app.utilities.metadata.snapshots.analyze_code_metrics import analyze_code, compute_deltas
# 16 from app.utilities.metadata.snapshots.snapshot_writer import SnapshotWriter
# 17 from app.utilities.metadata.footer.code_annnotation_utils import (
# 18     split_code_and_notes,
# 19     append_agent_note
# 20 )
# 21 
# 22 
# 23 class AgentEngineProviderBase(BaseProvider):
# 24     """Base class for agent engines that return raw LLM output and manage snapshots."""
# 25 
# 26     def __init__(
# 27         self,
# 28         config=None,
# 29         prompt_provider=None,
# 30         context_provider=None,
# 31         score_provider=None,
# 32         tool_providers=None,
# 33         called_by_type=None,
# 34         called_by_id=None,
# 35     ):
# 36         super().__init__(config=config, called_by_type=called_by_type, called_by_id=called_by_id)
# 37         self.prompt_provider = prompt_provider
# 38         self.context_provider = context_provider
# 39         self.score_provider = score_provider
# 40         self.tool_providers = tool_providers or []
# 41 
# 42     def _run_provider(self, input: dict) -> AgentEngineOutput:
# 43         file_path = input.get("before") or input.get("file_path") or (self._config.config or {}).get("before")
# 44         session_id = input.get("session_id", "")
# 45         system = SYSTEM(input.get("system", SYSTEM.UNKNOWN))
# 46         agent_type = AGENT(input.get("agent_type", AGENT.UNKNOWN))
# 47         agent_id = input.get("agent_id", -1)
# 48 
# 49         before_code = ""
# 50         prior_notes = ""
# 51         if file_path:
# 52             path = Path(file_path).resolve()
# 53             if path.exists():
# 54                 full_code = path.read_text(encoding="utf-8")
# 55                 before_code, prior_notes = split_code_and_notes(full_code)
# 56 
# 57         response_text = ""
# 58         snapshot_id = None
# 59         summary = None
# 60         token_count = 0
# 61 
# 62         try:
# 63             raw_output = self._run(input)
# 64             if isinstance(raw_output, AgentEngineOutput):
# 65                 response_text = raw_output.response
# 66                 token_count = raw_output.token_count
# 67                 cost_usd = raw_output.cost_usd
# 68             else:
# 69                 response_text = raw_output
# 70                 token_count = len(response_text.split())
# 71                 cost_usd = token_count * (self._config.cost_per_1k_tokens or 0.0) / 1000
# 72 
# 73             code_block = self._extract_block(response_text, "[CODE]", "[/CODE]")
# 74             log_block = self._extract_block(response_text, "[CONVERSATION_LOG_ENTRY]", "[/CONVERSATION_LOG_ENTRY]")
# 75 
# 76             if "[AGENT_DECISION]accept" in response_text:
# 77                 decision = DECISION_TYPE.ACCEPT
# 78             elif "[AGENT_DECISION]reject" in response_text:
# 79                 decision = DECISION_TYPE.REJECT
# 80             else:
# 81                 decision = DECISION_TYPE.UNKNOWN
# 82 
# 83             def normalize_code(code: str) -> str:
# 84                 return "\n".join(line.strip() for line in code.strip().splitlines() if line.strip())
# 85 
# 86             if not code_block or not before_code or normalize_code(code_block) == normalize_code(before_code):
# 87                 self._log.debug("🟡 Generator returned output matching input (normalized)")
# 88                 self._log.debug(f"🔍 BEFORE:\n{before_code}")
# 89                 self._log.debug(f"🆕 AFTER:\n{code_block}")
# 90                 return AgentEngineOutput(
# 91                     response=response_text,
# 92                     token_count=token_count,
# 93                     cost_usd=cost_usd,
# 94                     snapshot_id=None,
# 95                     summary="No change detected (normalized)",
# 96                 )
# 97 
# 98             after_code = code_block.rstrip()
# 99             if prior_notes:
# 100                 after_code += f"\n\n{prior_notes}"
# 101             if log_block:
# 102                 after_code = append_agent_note(after_code, system=system.value, agent_name=self._config.name, note=log_block)
# 103 
# 104             before_metrics = analyze_code(before_code)
# 105             after_metrics = analyze_code(after_code)
# 106             deltas = compute_deltas(before_metrics, after_metrics)
# 107 
# 108             timestamp = datetime.now(timezone.utc)
# 109             metadata = {
# 110                 "system": system.value,
# 111                 "agent": self._config.name,
# 112                 "agent_type": agent_type.value,
# 113                 "agent_id": agent_id,
# 114                 "score": None,
# 115                 "state": str(input.get("state_context", {}).get("state", "unknown")),
# 116                 "decision": decision.value,
# 117                 "timestamp": timestamp.isoformat(),
# 118                 **{f"{k}_before": v for k, v in before_metrics.items()},
# 119                 **{f"{k}_after": v for k, v in after_metrics.items()},
# 120                 **deltas,
# 121             }
# 122 
# 123             snapshot_id = SnapshotWriter().write_snapshot(
# 124                 before=before_code,
# 125                 after=after_code,
# 126                 session_id=session_id,
# 127                 metadata=metadata,
# 128             )
# 129 
# 130             with Session(bind=self._engine) as session:
# 131                 entry = SnapshotMetrics(
# 132                     session_id=session_id,
# 133                     snapshot_id=snapshot_id,
# 134                     system=system.value,
# 135                     agent=self._config.name,
# 136                     agent_type=agent_type.value,
# 137                     agent_id=agent_id,
# 138                     score=None,
# 139                     state=metadata["state"],
# 140                     decision=decision.value,
# 141                     timestamp=timestamp,
# 142                     **{
# 143                         k: metadata.get(k)
# 144                         for k in SnapshotMetrics.__table__.columns.keys()
# 145                         if k.endswith("_before") or k.endswith("_after") or k.endswith("_delta")
# 146                     },
# 147                 )
# 148                 session.add(entry)
# 149                 session.commit()
# 150 
# 151             self.logger.write(LOG_TYPE.SNAPSHOT_METRICS, SnapshotMetricsSchema(
# 152                 session_id=session_id,
# 153                 snapshot_id=snapshot_id,
# 154                 system=system,
# 155                 agent=self._config.name,
# 156                 agent_type=agent_type,
# 157                 agent_id=agent_id,
# 158                 score=None,
# 159                 state=metadata["state"],
# 160                 decision=decision,
# 161                 timestamp=timestamp,
# 162                 line_count_before=metadata["line_count_before"],
# 163                 line_count_after=metadata["line_count_after"],
# 164                 function_count_before=metadata["function_count_before"],
# 165                 function_count_after=metadata["function_count_after"],
# 166                 symbol_count_before=metadata["symbol_count_before"],
# 167                 symbol_count_after=metadata["symbol_count_after"],
# 168                 branch_count_before=metadata["branch_count_before"],
# 169                 branch_count_after=metadata["branch_count_after"],
# 170                 comment_count_before=metadata["comment_count_before"],
# 171                 comment_count_after=metadata["comment_count_after"],
# 172                 line_count_delta=metadata["line_count_delta"],
# 173                 function_count_delta=metadata["function_count_delta"],
# 174                 symbol_count_delta=metadata["symbol_count_delta"],
# 175                 branch_count_delta=metadata["branch_count_delta"],
# 176                 comment_count_delta=metadata["comment_count_delta"],
# 177             ))
# 178 
# 179             self._log.debug(f"📦 Snapshot written: {snapshot_id}")
# 180             summary = log_block or "Snapshot successfully written"
# 181 
# 182         except Exception as exc:
# 183             self._log.error(f"❌ Error during agent run: {exc}")
# 184             summary = f"Error: {str(exc)}"
# 185             cost_usd = 0.0
# 186 
# 187         return AgentEngineOutput(
# 188             response=response_text,
# 189             token_count=token_count,
# 190             cost_usd=cost_usd,
# 191             snapshot_id=snapshot_id,
# 192             summary=summary,
# 193         )
# 194 
# 195     def _extract_block(self, text: str, start_tag: str, end_tag: str) -> str | None:
# 196         start = text.find(start_tag)
# 197         end = text.find(end_tag)
# 198         if start != -1 and end != -1 and start < end:
# 199             return text[start + len(start_tag):end].strip()
# 200         return None
# 201 
# 202     @abstractmethod
# 203     def _run(self, input: dict) -> str:
# 204         raise NotImplementedError


# providers\agent_provider_base.py

# 1 from datetime import datetime, timezone
# 2 from pathlib import Path
# 3 from abc import abstractmethod
# 4 from sqlalchemy.orm import Session
# 5 
# 6 from app.db.models import SnapshotMetrics
# 7 from app.enums.logging_enums import LOG_TYPE
# 8 from app.enums.agent_enums import AGENT
# 9 from app.enums.fsm_enums import DECISION_TYPE
# 10 from app.providers.base_provider import BaseProvider
# 11 from app.utilities.metadata.footer.code_annnotation_utils import append_agent_note
# 12 from app.utilities.metadata.snapshots.analyze_code_metrics import analyze_code, compute_deltas
# 13 from app.utilities.metadata.snapshots.snapshot_writer import SnapshotWriter
# 14 from app.db.schemas import AgentConversationLogSchema, AgentOutputSchema
# 15 
# 16 
# 17 class AgentProviderBase(BaseProvider):
# 18     def __init__(
# 19         self,
# 20         config=None,
# 21         agent_engine=None,
# 22         prompt_provider=None,
# 23         context_provider=None,
# 24         score_provider=None,
# 25         tool_providers=None,
# 26         called_by_type=None,
# 27         called_by_id=None,
# 28     ):
# 29         super().__init__(config=config, called_by_type=called_by_type, called_by_id=called_by_id)
# 30         self._agent_engine = agent_engine
# 31         self._prompt_provider = prompt_provider
# 32         self._context_provider = context_provider
# 33         self._score_provider = score_provider
# 34         self._tool_providers = tool_providers or []
# 35 
# 36     def _run_provider(self, input: dict) -> AgentOutputSchema:
# 37         output = self._run(input)
# 38         self._log.debug(f"🧻 _run_provider called for: {self._config.name if self._config else 'unknown'}")
# 39 
# 40         response = output.response if hasattr(output, "response") else str(output)
# 41         decision = self._infer_decision(response)
# 42         log_content = self._extract_log(response) or self._default_log(decision)
# 43 
# 44         snapshot_id = None
# 45         file_name = input.get("before") or input.get("file_name") or (self._config.config or {}).get("before")
# 46         if file_name and (code_block := self._extract_code(response)):
# 47             before_path = Path(file_name)
# 48             try:
# 49                 relative_path = before_path.relative_to(Path.cwd())
# 50             except ValueError:
# 51                 relative_path = before_path
# 52             before_path = relative_path
# 53 
# 54             if before_path.exists():
# 55                 before_code = before_path.read_text(encoding="utf-8")
# 56                 after_code = code_block
# 57 
# 58                 before_metrics = analyze_code(before_code)
# 59                 after_metrics = analyze_code(after_code)
# 60                 deltas = compute_deltas(before_metrics, after_metrics)
# 61 
# 62                 metadata = {
# 63                     "system": self._system,
# 64                     "agent": self._config.name if self._config else "unknown",
# 65                     "score": (
# 66                         self._score_provider.run(
# 67                             {"file_name": str(before_path)}, session_id=self._session_id
# 68                         ).value
# 69                         if self._score_provider else None
# 70                     ),
# 71                     "state": str(input.get("state_context", {}).get("state", "unknown")),
# 72                     "decision": decision.value,
# 73                     "timestamp": datetime.now(timezone.utc).isoformat(),
# 74                     **{f"{k}_before": v for k, v in before_metrics.items()},
# 75                     **{f"{k}_after": v for k, v in after_metrics.items()},
# 76                     **deltas,
# 77                 }
# 78 
# 79                 after_code = append_agent_note(
# 80                     file_content=after_code,
# 81                     system=self._system,
# 82                     agent_name=self._config.name if self._config else "unknown",
# 83                     note=log_content
# 84                 )
# 85 
# 86                 snapshot_id = SnapshotWriter().write_snapshot(
# 87                     before=before_code,
# 88                     after=after_code,
# 89                     session_id=self._session_id,
# 90                     metadata=metadata
# 91                 )
# 92                 with Session(bind=self._engine) as session:
# 93                     entry = SnapshotMetrics(
# 94                         session_id=self._session_id,
# 95                         snapshot_id=snapshot_id,
# 96                         system=self._system,
# 97                         agent=self._config.name,
# 98                         score=metadata.get("score"),
# 99                         state=metadata.get("state"),
# 100                         decision=DECISION_TYPE(metadata.get("decision", "unknown")),
# 101                         timestamp=datetime.fromisoformat(metadata["timestamp"]),
# 102                         **{
# 103                             k: metadata.get(k)
# 104                             for k in SnapshotMetrics.__table__.columns.keys()
# 105                             if k.endswith("_before") or k.endswith("_after") or k.endswith("_delta")
# 106                         }
# 107                     )
# 108                     session.add(entry)
# 109                     session.commit()
# 110                 self._snapshot_id = snapshot_id
# 111                 self._log.debug(f"📦 Snapshot written to: {snapshot_id}")
# 112             else:
# 113                 self._log.warning(f"❌ Snapshot skipped: file does not exist → {before_path}")
# 114 
# 115         #  Always log conversation
# 116         self.logger.write(LOG_TYPE.AGENT_CONVERSATION, AgentConversationLogSchema(
# 117             session_id=self._session_id,
# 118             system=self._system,
# 119             agent_type=self._config.agent_type if hasattr(self._config, "agent_type") else AGENT.BASIC,
# 120             agent_provider_config_id=self._config.id if self._config else -1,
# 121             content=log_content,
# 122             timestamp=datetime.now(timezone.utc),
# 123         ))
# 124         self._log.debug("✅ AGENT_CONVERSATION log write complete")
# 125 
# 126         return output
# 127 
# 128     def _infer_decision(self, text: str) -> DECISION_TYPE:
# 129         if "[AGENT_DECISION]accept" in text:
# 130             return DECISION_TYPE.ACCEPT
# 131         if "[AGENT_DECISION]reject" in text:
# 132             return DECISION_TYPE.REJECT
# 133         return DECISION_TYPE.UNKNOWN
# 134 
# 135     def _extract_log(self, text: str) -> str | None:
# 136         return self._extract_block(text, "[CONVERSATION_LOG_ENTRY]", "[/CONVERSATION_LOG_ENTRY]")
# 137 
# 138     def _default_log(self, decision: DECISION_TYPE) -> str:
# 139         return f"No conversation log entry found. Agent decision was: {decision.value}."
# 140 
# 141     def _extract_code(self, text: str) -> str | None:
# 142         return self._extract_block(text, "[CODE]", "[/CODE]")
# 143 
# 144     def _extract_block(self, text: str, start_tag: str, end_tag: str) -> str | None:
# 145         start = text.find(start_tag)
# 146         end = text.find(end_tag)
# 147         if start != -1 and end != -1 and start < end:
# 148             return text[start + len(start_tag):end].strip()
# 149         return None
# 150 
# 151     @abstractmethod
# 152     def _run(self, input: dict) -> AgentOutputSchema:
# 153         raise NotImplementedError


# providers\base_provider.py

# 1 from abc import abstractmethod
# 2 from datetime import datetime, timezone
# 3 import hashlib
# 4 import json
# 5 import time
# 6 from pathlib import Path
# 7 from typing import Dict, Optional
# 8 from uuid import uuid4
# 9 
# 10 from app.db import init_db
# 11 from app.utilities.metadata.logging.logging_provider import LoggingMixin, LOG_TYPE
# 12 from app.db.schemas import ProviderLogSchema, ErrorLogSchema
# 13 from app.enums.logging_enums import PROVIDER_TYPE, ERROR_TYPE
# 14 
# 15 class BaseProvider(LoggingMixin):
# 16     def __init__(
# 17         self,
# 18         called_by_type: PROVIDER_TYPE,  
# 19         called_by_id: int,  
# 20         config=None,
# 21     ) -> None:
# 22         super().__init__()
# 23         assert config is not None, "🚨 engine must be injected into BaseProvider"
# 24         self._config = config
# 25         self._engine = init_db(reset=False)
# 26         self._called_by_type = called_by_type
# 27         self._called_by_id = called_by_id
# 28 
# 29     def run(self, input: dict | None = None, session_id: str = "") -> str:
# 30         input = input or {}
# 31         if not session_id:
# 32             raise ValueError("session_id must be provided to run()")
# 33 
# 34         self._session_id = session_id
# 35         self._system = input.get("system", "unknown")
# 36         self._run_id = str(uuid4())
# 37 
# 38         start_clock = time.perf_counter()
# 39         start_time = datetime.now(timezone.utc)
# 40 
# 41         try:
# 42             output = self._run_provider(input)
# 43         except Exception as exc:
# 44             latency_ms = int((time.perf_counter() - start_clock) * 1000)
# 45             try:
# 46                 self.logger.write(
# 47                     LOG_TYPE.ERROR,
# 48                     ErrorLogSchema(
# 49                         session_id=session_id,
# 50                         error_type=self._map_error_type(exc).value,
# 51                         message=str(exc),
# 52                         file_path=str(Path(__file__).relative_to(Path.cwd())),
# 53                         provider_id=self._config.id if self._config else None,
# 54                         provider_type=self._infer_provider_type().value,
# 55                         timestamp=start_time,
# 56                         latency_ms=latency_ms,
# 57                         called_by_type=self._called_by_type.value if self._called_by_type else None,
# 58                         called_by_id=self._called_by_id,
# 59                         run_id=self._run_id,
# 60                     ),
# 61                 )
# 62             except Exception:
# 63                 pass
# 64             raise
# 65 
# 66         latency_ms = int((time.perf_counter() - start_clock) * 1000)
# 67 
# 68         try:
# 69             input_str = json.dumps(
# 70                 {k: v.model_dump() if hasattr(v, "model_dump") else v for k, v in input.items()},
# 71                 default=str,
# 72             )
# 73         except Exception:
# 74             input_str = ""
# 75 
# 76         try:
# 77             if hasattr(output, "model_dump"):
# 78                 dumped = output.model_dump()
# 79                 for k, v in dumped.items():
# 80                     if isinstance(v, dict):
# 81                         dumped[k] = json.dumps(v, default=str)
# 82                 output_str = json.dumps(dumped, default=str)
# 83                 output_schema = output.__class__.__name__
# 84             else:
# 85                 output_str = json.dumps(output, default=str)
# 86                 output_schema = None
# 87         except Exception:
# 88             output_str = ""
# 89             output_schema = None
# 90 
# 91         try:
# 92             self.logger.write(
# 93                 LOG_TYPE.PROVIDER,
# 94                 ProviderLogSchema(
# 95                     session_id=session_id,
# 96                     provider_id=self._config.id if self._config else -1,
# 97                     provider_type=self._infer_provider_type().value,
# 98                     input=input_str,
# 99                     output=output_str,
# 100                     output_schema=output_schema,
# 101                     latency_ms=latency_ms,
# 102                     config_hash=self._compute_config_hash(),
# 103                     file_name=getattr(self._config, "artifact_path", "").split("/")[-1]
# 104                     if getattr(self._config, "artifact_path", None)
# 105                     else None,
# 106                     timestamp=start_time,
# 107                     called_by_type=self._called_by_type if self._called_by_type else None,
# 108                     called_by_id=self._called_by_id,
# 109                     run_id=self._run_id,
# 110                 ),
# 111             )
# 112         except Exception:
# 113             raise
# 114 
# 115         self._log.debug("✅ Provider run logged")
# 116         return output
# 117 
# 118     def _map_error_type(self, exc: Exception) -> ERROR_TYPE:
# 119         return ERROR_TYPE.RUNTIME
# 120 
# 121     def _infer_provider_type(self) -> PROVIDER_TYPE:
# 122         cls = self.__class__.__name__.lower()
# 123         if "tool" in cls:
# 124             return PROVIDER_TYPE.TOOL
# 125         if "engine" in cls:
# 126             return PROVIDER_TYPE.AGENT_ENGINE
# 127         if "agent" in cls:
# 128             return PROVIDER_TYPE.AGENT
# 129         if "prompt" in cls:
# 130             return PROVIDER_TYPE.PROMPT
# 131         if "context" in cls:
# 132             return PROVIDER_TYPE.CONTEXT
# 133         if "score" in cls:
# 134             return PROVIDER_TYPE.SCORE
# 135         if "state" in cls:
# 136             return PROVIDER_TYPE.STATE
# 137         if "system" in cls:
# 138             return PROVIDER_TYPE.SYSTEM
# 139         if "controller" in cls:
# 140             return PROVIDER_TYPE.CONTROLLER
# 141         if "program" in cls:
# 142             return PROVIDER_TYPE.PROGRAM
# 143         return PROVIDER_TYPE.UNKNOWN
# 144 
# 145     def _compute_config_hash(self) -> Optional[str]:
# 146         if not self._config or not getattr(self._config, "config", None):
# 147             return None
# 148         return hashlib.md5(json.dumps(self._config.config, sort_keys=True).encode()).hexdigest()


# providers\context_provider_base.py

# 1 from __future__ import annotations
# 2 
# 3 from abc import abstractmethod
# 4 
# 5 from app.db.schemas import ContextOutputSchema
# 6 from app.providers.base_provider import BaseProvider
# 7 
# 8 from typing import TYPE_CHECKING
# 9 if TYPE_CHECKING:
# 10     from app.providers.score_provider_base import ScoreProviderBase
# 11     from app.providers.tool_provider_base import ToolProviderBase
# 12 
# 13 class ContextProviderBase(BaseProvider):
# 14     """Base class for context providers."""
# 15 
# 16     def _run_provider(self, input: dict) -> ContextOutputSchema:
# 17         return self._run(input)
# 18     
# 19     def set_tool_provider(self, provider: "ToolProviderBase") -> None:
# 20         if not hasattr(self, "tool_providers"):
# 21             self.tool_providers = []
# 22         self.tool_providers.append(provider)
# 23 
# 24     def set_score_provider(self, provider: "ScoreProviderBase") -> None:
# 25         self.score_provider = provider
# 26 
# 27 
# 28     @abstractmethod
# 29     def _run(self, input: dict) -> ContextOutputSchema:
# 30         raise NotImplementedError


# providers\controller_provider_base.py

# 1 from __future__ import annotations
# 2 from abc import abstractmethod
# 3 from datetime import datetime
# 4 from pathlib import Path
# 5 import shutil
# 6 from typing import Dict
# 7 
# 8 from app.enums.fsm_enums import DECISION_TYPE
# 9 from app.enums.system_enums import SYSTEM
# 10 from app.providers.fsm_provider_base import FSMProviderBase
# 11 from app.db.schemas import ControllerOutputSchema
# 12 from app.utilities.extract_base_filename import extract_base_filename
# 13 from app.utilities.select_best_file_by_score import select_best_file_by_score
# 14 
# 15 
# 16 class ControllerProviderBase(FSMProviderBase):
# 17     def __init__(
# 18         self,
# 19         config=None,
# 20         system_providers: Dict[str, object] = None,
# 21         context_provider=None,
# 22         score_provider=None,
# 23         tool_providers=None,
# 24         called_by_type=None,
# 25         called_by_id=None,
# 26     ):
# 27         super().__init__(
# 28             config=config,
# 29             called_by_type=called_by_type,
# 30             called_by_id=called_by_id,
# 31         )
# 32         self._systems = system_providers or {}
# 33         self.context_provider = context_provider
# 34         self.score_provider = score_provider
# 35         self.tool_providers = tool_providers or []
# 36         self._generated_files: list[Path] = []
# 37 
# 38     def _run_provider(self, input: dict) -> ControllerOutputSchema:
# 39         session_id = input.get("session_id")
# 40         max_steps = input.get("max_steps", 10)
# 41 
# 42         incoming_file = input.get("file_path") or input.get("file_name") or input.get("before")
# 43         if not incoming_file:
# 44             raise ValueError("❌ ControllerProvider requires 'file_path', 'file_name', or 'before' in input")
# 45 
# 46         self.incoming_file = incoming_file
# 47         src = Path(incoming_file).resolve()
# 48         timestamp = datetime.now().strftime('%H%M%S%f')[:10]
# 49         working_dir = Path("working_files").resolve()
# 50         working_dir.mkdir(parents=True, exist_ok=True)
# 51         root = extract_base_filename(src)
# 52         self.working_file = working_dir / f"{root}__ctrl_{timestamp}{src.suffix}"
# 53         shutil.copy(src, self.working_file)
# 54         self._generated_files.append(self.working_file)
# 55 
# 56         input_path = Path(incoming_file)
# 57         try:
# 58             relative_path = str(input_path.relative_to(Path.cwd()))
# 59         except ValueError:
# 60             relative_path = str(input_path)
# 61 
# 62         state = {
# 63             "state": SYSTEM.START,
# 64             "file_path": str(self.working_file),
# 65             "session_id": session_id,
# 66             "system": input.get("system", "unknown"),
# 67             "reason": input.get("reason",SYSTEM.START.value),
# 68             "steps": input.get("steps", 0),
# 69             "retry_count": input.get("retry_count", 0),
# 70             "_last_state": input.get("_last_state", SYSTEM.START),
# 71             "decision": DECISION_TYPE.UNKNOWN,
# 72             "original_file": relative_path,
# 73             "run_id": self._run_id,
# 74         }
# 75 
# 76         step_count = 0
# 77 
# 78         while True:
# 79             current = state.get("state")
# 80 
# 81             if step_count >= max_steps:
# 82                 return ControllerOutputSchema(
# 83                     state=SYSTEM.END,
# 84                     previous_state=current,
# 85                     state_type=SYSTEM.END,
# 86                     decision=DECISION_TYPE.REJECT,
# 87                     steps=step_count,
# 88                     max_steps=max_steps,
# 89                     summary=f"Max steps ({max_steps}) reached",
# 90                     output=state,
# 91                     provider_name=self._config.name,
# 92                 )
# 93 
# 94             if current == SYSTEM.END:
# 95                 best_file = select_best_file_by_score(
# 96                     file_a=state["file_path"],
# 97                     file_b=self.incoming_file,
# 98                     score_provider=self.score_provider,
# 99                     system=state.get("system", "unknown"),
# 100                     session_id=session_id,
# 101                 )
# 102 
# 103                 final_path = Path("working_files") / f"final_ctrl_{datetime.now().strftime('%H%M%S%f')[:10]}.py"
# 104                 shutil.copy(Path(best_file), final_path)
# 105                 state["file_path"] = str(final_path)
# 106 
# 107                 for f in Path("working_files").glob("final_*.py"):
# 108                     if f.resolve() != final_path.resolve():
# 109                         try:
# 110                             f.unlink()
# 111                         except Exception:
# 112                             pass
# 113 
# 114                 for f in Path("working_files").glob("*_stripped.py"):
# 115                     try:
# 116                         f.unlink()
# 117                     except Exception:
# 118                         pass
# 119 
# 120                 for path in self._generated_files:
# 121                     if path.exists():
# 122                         try:
# 123                             path.unlink()
# 124                         except Exception:
# 125                             pass
# 126 
# 127                 return ControllerOutputSchema(
# 128                     state=SYSTEM.END,
# 129                     previous_state=state.get("_last_state", SYSTEM.START),
# 130                     state_type=SYSTEM.END,
# 131                     decision=state.get("decision", DECISION_TYPE.UNKNOWN),
# 132                     steps=step_count,
# 133                     max_steps=max_steps,
# 134                     summary=state.get("summary", "Completed"),
# 135                     output=state,
# 136                     provider_name=self._config.name,
# 137                 )
# 138 
# 139             step_count += 1
# 140 
# 141             if current == SYSTEM.START:
# 142                 transition = self.transition(state, None)
# 143                 state.update(transition)
# 144                 state["_last_state"] = SYSTEM.START
# 145                 continue
# 146 
# 147             provider = self._systems.get(current.value)
# 148             if not provider:
# 149                 raise ValueError(f"No system provider registered for state: {current.value}")
# 150 
# 151             provider_input = {k: v for k, v in state.items() if k != "state"}
# 152             output = provider.run(input=provider_input, session_id=session_id)
# 153 
# 154             new_file_path = getattr(output, "file_path", None)
# 155             if new_file_path:
# 156                 new_file_path = Path(new_file_path).resolve()
# 157                 current_path = Path(state["file_path"]).resolve()
# 158                 if new_file_path != current_path:
# 159                     shutil.copy(new_file_path, current_path)
# 160                     self._generated_files.append(new_file_path)
# 161                     state["file_path"] = str(new_file_path)
# 162 
# 163             if hasattr(output, "decision") and output.decision:
# 164                 state["decision"] = output.decision
# 165 
# 166             transition_result = self.transition(state, output)
# 167             flat_output = output.model_dump(exclude={"output"}) if hasattr(output, "model_dump") else dict(output)
# 168 
# 169             transition_metadata = transition_result.get("transition_metadata", {}) or {}
# 170             transition_metadata.update({
# 171                 "agent_output": flat_output.get("output", {}),
# 172                 "file_path": flat_output.get("file_path"),
# 173             })
# 174             transition_result["transition_metadata"] = transition_metadata
# 175 
# 176             raw_state = transition_result.get("state", current)
# 177             state_enum = raw_state if isinstance(raw_state, SYSTEM) else SYSTEM(raw_state)
# 178 
# 179             state = {
# 180                 **state,
# 181                 **transition_result,
# 182                 "state": state_enum,
# 183                 "file_path": transition_result.get("file_path") or getattr(output, "file_path", state.get("file_path")),
# 184                 "_last_state": current,
# 185                 "state_output": flat_output,
# 186             }
# 187 
# 188     @abstractmethod
# 189     def _transition(self, state: dict, output: dict | None) -> dict:
# 190         ...


# providers\fsm_provider_base.py

# 1 from __future__ import annotations
# 2 
# 3 from abc import abstractmethod
# 4 from datetime import datetime, timezone
# 5 from typing import Optional
# 6 
# 7 from app.enums.fsm_enums import TRANSITION_REASON_TYPE, STATE_DECISION_TYPE
# 8 from app.enums.logging_enums import LOG_TYPE, PROVIDER_TYPE
# 9 from app.db.schemas import StateTransitionLogSchema
# 10 from app.providers.base_provider import BaseProvider
# 11 
# 12 
# 13 class FSMProviderBase(BaseProvider):
# 14     """
# 15     Base class for FSM-enabled providers (Program, Controller, System, State).
# 16     Provides a shared `transition` method and logging of state transitions.
# 17     """
# 18 
# 19     def __init__(
# 20         self,
# 21         config=None,
# 22         called_by_type: Optional[PROVIDER_TYPE] = None,
# 23         called_by_id: Optional[int] = None,
# 24     ) -> None:
# 25         super().__init__(
# 26             config=config,
# 27             called_by_type=called_by_type,
# 28             called_by_id=called_by_id,
# 29         )
# 30 
# 31     def transition(self, state: dict, result: dict | None) -> dict:
# 32         next_state = self._transition(state, result)
# 33         state["steps"] = state.get("steps", 0) + 1
# 34 
# 35         #  Ensure original_file is carried forward
# 36         original_file = state.get("original_file")
# 37 
# 38         #  Initialize or update transition_metadata
# 39         transition_metadata = next_state.get("transition_metadata", {}) or {}
# 40         transition_metadata["original_file"] = original_file
# 41 
# 42         #  Optional: Add run_id to the metadata as well
# 43         transition_metadata["run_id"] = state.get("run_id")
# 44 
# 45         #  Optional: agent_output propagation (if result has a dumpable output)
# 46         if isinstance(result, dict):
# 47             transition_metadata["agent_output"] = result.get("output")
# 48             transition_metadata["file_path"] = result.get("file_path")
# 49 
# 50         next_state["transition_metadata"] = transition_metadata
# 51 
# 52         is_terminal = next_state.get("state_type") == "end" or next_state.get("state") == "end"
# 53 
# 54         raw_decision = getattr(result, "decision", "").lower()
# 55 
# 56         if raw_decision == "accept":
# 57             decision = STATE_DECISION_TYPE.FINAL if is_terminal else STATE_DECISION_TYPE.IMPROVED
# 58         elif raw_decision == "reject":
# 59             decision = STATE_DECISION_TYPE.REJECTED
# 60         else:
# 61             decision = STATE_DECISION_TYPE.INITIAL
# 62 
# 63         #  Log the state transition with relevant details
# 64         self.logger.write(
# 65             LOG_TYPE.STATE_TRANSITION,
# 66             StateTransitionLogSchema(
# 67                 session_id=self._session_id,
# 68                 entity_type=self._infer_provider_type(),
# 69                 entity_id=self._config.id,
# 70                 from_state=state.get("state"),
# 71                 to_state=next_state.get("state", "unknown"),
# 72                 reason=TRANSITION_REASON_TYPE(next_state["reason"]),
# 73                 decision=decision,
# 74                 triggered_by=self._config.name,
# 75                 step=state["steps"],
# 76                 transition_metadata=transition_metadata,
# 77                 timestamp=datetime.now(timezone.utc),
# 78                 run_id=self._run_id,
# 79                 called_by_type=self._called_by_type if self._called_by_type else None,
# 80                 called_by_id=self._called_by_id,
# 81             ),
# 82         )
# 83         return next_state
# 84 
# 85 
# 86     @abstractmethod
# 87     def _transition(self, state: dict, result: dict | None) -> dict:
# 88         raise NotImplementedError


# providers\program_provider_base.py

# 1 from __future__ import annotations
# 2 from abc import abstractmethod
# 3 from datetime import datetime
# 4 from pathlib import Path
# 5 import shutil
# 6 from typing import Dict
# 7 
# 8 from app.enums.controller_enums import CONTROLLER
# 9 from app.enums.fsm_enums import STATE_TYPE, DECISION_TYPE
# 10 from app.providers.fsm_provider_base import FSMProviderBase
# 11 from app.db.schemas import ProgramOutputSchema
# 12 from app.utilities.extract_base_filename import extract_base_filename
# 13 from app.utilities.select_best_file_by_score import select_best_file_by_score
# 14 
# 15 
# 16 class ProgramProviderBase(FSMProviderBase):
# 17     def __init__(
# 18         self,
# 19         config=None,
# 20         controller_providers: Dict[str, object] = None,
# 21         context_provider=None,
# 22         score_provider=None,
# 23         tool_providers=None,
# 24         called_by_type=None,
# 25         called_by_id=None,
# 26     ):
# 27         super().__init__(
# 28             config=config,
# 29             called_by_type=called_by_type,
# 30             called_by_id=called_by_id
# 31         )
# 32         self._states = controller_providers or {}
# 33         self.context_provider = context_provider
# 34         self.score_provider = score_provider
# 35         self.tool_providers = tool_providers or []
# 36         self._generated_files: list[Path] = []
# 37 
# 38     def _run_provider(self, input: dict) -> ProgramOutputSchema:
# 39         session_id = input.get("session_id")
# 40         max_steps = input.get("max_steps", 20)
# 41 
# 42         incoming_file = input.get("file_path")
# 43         self.incoming_file = incoming_file
# 44 
# 45         src = Path(incoming_file).resolve()
# 46         root = extract_base_filename(src)
# 47         timestamp = datetime.now().strftime('%H%M%S%f')[:10]
# 48         working_dir = Path("working_files").resolve()
# 49         working_dir.mkdir(parents=True, exist_ok=True)
# 50         self.working_file = working_dir / f"{root}.__prog_{timestamp}{src.suffix}"
# 51         shutil.copy(src, self.working_file)
# 52         self._generated_files.append(self.working_file)
# 53 
# 54         input_path = Path(incoming_file)
# 55         try:
# 56             relative_path = str(input_path.relative_to(Path.cwd()))
# 57         except ValueError:
# 58             relative_path = str(input_path)
# 59 
# 60         state = {
# 61             "state": CONTROLLER.START,
# 62             "file_path": str(self.working_file),
# 63             "session_id": session_id,
# 64             "system": input.get("system", "unknown"),
# 65             "reason": input.get("reason", CONTROLLER.START.value),
# 66             "steps": input.get("steps", 0),
# 67             "retry_count": input.get("retry_count", 0),
# 68             "_last_state": input.get("_last_state", CONTROLLER.START),
# 69             "original_file": relative_path,
# 70             "run_id": self._run_id,
# 71         }
# 72 
# 73         step_count = 0
# 74 
# 75         while True:
# 76             current = state.get("state")
# 77 
# 78             if step_count >= max_steps:
# 79                 return ProgramOutputSchema(
# 80                     state=CONTROLLER.END,
# 81                     previous_state=current,
# 82                     state_type=STATE_TYPE.END,
# 83                     decision=DECISION_TYPE.UNKNOWN,
# 84                     steps=step_count,
# 85                     max_steps=max_steps,
# 86                     summary=f"Max steps ({max_steps}) reached",
# 87                     output=state,
# 88                     provider_name=self._config.name
# 89                 )
# 90 
# 91             if current == CONTROLLER.END:
# 92                 final_decision = state.get("decision")
# 93                 nested = state.get("output") or {}
# 94 
# 95                 if isinstance(nested, dict):
# 96                     final_decision = final_decision or nested.get("decision")
# 97 
# 98                 best_file = select_best_file_by_score(
# 99                     file_a=state.get("file_path"),
# 100                     file_b=self.incoming_file,
# 101                     score_provider=self.score_provider,
# 102                     system=state.get("system", "unknown"),
# 103                     session_id=session_id
# 104                 )
# 105 
# 106                 final_path = Path("working_files") / f"final_prog_{datetime.now().strftime('%H%M%S%f')[:10]}.py"
# 107                 shutil.copy(Path(best_file).resolve(), final_path)
# 108                 state["file_path"] = str(final_path)
# 109                 state["decision"] = final_decision
# 110 
# 111                 for f in Path("working_files").glob("final_*.py"):
# 112                     if f.resolve() != final_path.resolve():
# 113                         try:
# 114                             f.unlink()
# 115                         except Exception:
# 116                             pass
# 117 
# 118                 for f in Path("working_files").glob("*_stripped.py"):
# 119                     try:
# 120                         f.unlink()
# 121                     except Exception:
# 122                         pass
# 123 
# 124                 for path in self._generated_files:
# 125                     if path.exists():
# 126                         try:
# 127                             path.unlink()
# 128                         except Exception:
# 129                             pass
# 130 
# 131                 return ProgramOutputSchema(
# 132                     state=CONTROLLER.END,
# 133                     previous_state=state.get("_last_state", CONTROLLER.START),
# 134                     state_type=STATE_TYPE.END,
# 135                     decision=final_decision or DECISION_TYPE.UNKNOWN,
# 136                     steps=step_count,
# 137                     max_steps=max_steps,
# 138                     summary=state.get("reason", "Completed"),
# 139                     output=state,
# 140                     provider_name=self._config.name
# 141                 )
# 142 
# 143             step_count += 1
# 144 
# 145             if current == CONTROLLER.START:
# 146                 transition = self.transition(state, None)
# 147                 state.update(transition)
# 148                 state["_last_state"] = CONTROLLER.START
# 149                 continue
# 150 
# 151             provider = self._states.get(current)
# 152             if not provider:
# 153                 raise ValueError(f"No controller provider registered for state: {current}")
# 154 
# 155             provider_input = {k: v for k, v in state.items() if k != "state"}
# 156             output = provider.run(input=provider_input, session_id=session_id)
# 157 
# 158             transition_result = self.transition(state, output)
# 159             flat_output = output.model_dump(exclude={"output"}) if hasattr(output, "model_dump") else dict(output)
# 160 
# 161             transition_metadata = transition_result.get("transition_metadata", {}) or {}
# 162             transition_metadata.update({
# 163                 "agent_output": flat_output.get("output", {}),
# 164                 "file_path": flat_output.get("file_path"),
# 165             })
# 166             transition_result["transition_metadata"] = transition_metadata
# 167 
# 168             state = {
# 169                 **state,
# 170                 **transition_result,
# 171                 "state": CONTROLLER(transition_result.get("state", current)),
# 172                 "file_path": transition_result.get("file_path") or getattr(output, "file_path", state.get("file_path")),
# 173                 "_last_state": current,
# 174                 "state_output": flat_output,
# 175             }
# 176 
# 177     @abstractmethod
# 178     def _transition(self, state: dict, output: dict | None) -> dict:
# 179         ...


# providers\prompt_provider_base.py

# 1 from __future__ import annotations
# 2 from abc import abstractmethod
# 3 from sqlalchemy.orm import Session
# 4 from pathlib import Path
# 5 
# 6 from app.db.models import AgentPrompt, SystemPrompt
# 7 from app.db.schemas import PromptOutputSchema
# 8 from app.providers.base_provider import BaseProvider
# 9 from app.factories.context_provider_factory import ContextProviderFactory
# 10 
# 11 
# 12 class PromptProviderBase(BaseProvider):
# 13     """Base class for prompt generators with optional context/agent/system loading."""
# 14 
# 15     def __init__(self, config, called_by_type=None, called_by_id=None):
# 16         super().__init__(config=config, called_by_type=called_by_type, called_by_id=called_by_id)
# 17 
# 18         self._agent_prompt = None
# 19         self._system_prompt = None
# 20         self._context_provider = None
# 21         self.agent_text = None
# 22         self.system_text = None
# 23 
# 24         if not self._config or not self._engine:
# 25             return
# 26 
# 27         with Session(bind=self._engine) as session:
# 28             if agent_id := self._config.config.get("agent_prompt_id"):
# 29                 self._agent_prompt = session.get(AgentPrompt, agent_id)
# 30                 if self._agent_prompt:
# 31                     self.agent_text = Path("extensions") / self._agent_prompt.artifact_path
# 32                     self.agent_text = self.agent_text.read_text(encoding="utf-8").strip()
# 33 
# 34             if system_id := self._config.config.get("system_prompt_id"):
# 35                 self._system_prompt = session.get(SystemPrompt, system_id)
# 36                 if self._system_prompt:
# 37                     self.system_text = Path("extensions") / self._system_prompt.artifact_path
# 38                     self.system_text = self.system_text.read_text(encoding="utf-8").strip()
# 39 
# 40             if context_id := self._config.config.get("context_provider_id"):
# 41                 self._context_provider = ContextProviderFactory.create(id=context_id)
# 42 
# 43     def _run_provider(self, input: dict) -> PromptOutputSchema:
# 44         return self._run(input)
# 45 
# 46     @abstractmethod
# 47     def _run(self, input: dict) -> PromptOutputSchema:
# 48         raise NotImplementedError


# providers\score_provider_base.py

# 1 from __future__ import annotations
# 2 
# 3 from abc import abstractmethod
# 4 from app.providers.base_provider import BaseProvider
# 5 from typing import TYPE_CHECKING
# 6 if TYPE_CHECKING:
# 7     from app.providers.context_provider_base import ContextProviderBase
# 8     from app.providers.tool_provider_base import ToolProviderBase
# 9 
# 10 from app.db.schemas import ScoreOutputSchema
# 11 
# 12 class ScoreProviderBase(BaseProvider):
# 13     """Base class for score providers."""
# 14 
# 15     def _run_provider(self, input: dict) -> ScoreOutputSchema:
# 16         return self._run(input)
# 17 
# 18     def set_context_provider(self, provider: ContextProviderBase) -> None:
# 19         self.context_provider = provider
# 20 
# 21     def set_tool_provider(self, provider: ToolProviderBase) -> None:
# 22         if not hasattr(self, "tool_providers"):
# 23             self.tool_providers = []
# 24         self.tool_providers.append(provider)
# 25 
# 26     @abstractmethod
# 27     def _run(self, input: dict) -> ScoreOutputSchema:
# 28         raise NotImplementedError


# providers\state_provider_base.py

# 1 from __future__ import annotations
# 2 from abc import abstractmethod
# 3 from datetime import datetime
# 4 from pathlib import Path
# 5 import shutil
# 6 from typing import Dict
# 7 
# 8 from app.enums.fsm_enums import STATE_TYPE, DECISION_TYPE
# 9 from app.enums.state_enums import STATE
# 10 from app.enums.agent_enums import AGENT
# 11 from app.providers.fsm_provider_base import FSMProviderBase
# 12 from app.db.schemas import StateOutputSchema
# 13 from app.utilities.extract_base_filename import extract_base_filename
# 14 from app.utilities.select_best_file_by_score import select_best_file_by_score
# 15 
# 16 
# 17 class StateProviderBase(FSMProviderBase):
# 18     def __init__(
# 19         self,
# 20         config=None,
# 21         agent_providers: Dict[str, object] = None,
# 22         context_provider=None,
# 23         score_provider=None,
# 24         tool_providers=None,
# 25         called_by_type=None,
# 26         called_by_id=None,
# 27     ):
# 28         super().__init__(
# 29             config=config,
# 30             called_by_type=called_by_type,
# 31             called_by_id=called_by_id,
# 32         )
# 33         self.agent_providers = agent_providers or {}
# 34         self.context_provider = context_provider
# 35         self.score_provider = score_provider
# 36         self.tool_providers = tool_providers or []
# 37         self._generated_files: list[Path] = []
# 38 
# 39     def _run_provider(self, input: dict) -> StateOutputSchema:
# 40         session_id = input.get("session_id")
# 41         max_steps = input.get("max_steps", 10)
# 42 
# 43         incoming_file = input.get("file_path") or input.get("file_name") or input.get("before")
# 44         if not incoming_file:
# 45             raise ValueError("❌ StateProvider requires 'file_path', 'file_name', or 'before' in input")
# 46 
# 47         self.incoming_file = incoming_file
# 48         src = Path(incoming_file).resolve()
# 49         timestamp = datetime.now().strftime('%H%M%S%f')[:10]
# 50         working_dir = Path("working_files").resolve()
# 51         working_dir.mkdir(parents=True, exist_ok=True)
# 52         root = extract_base_filename(src)
# 53         self.working_file = working_dir / f"{root}__state_{timestamp}{src.suffix}"
# 54         shutil.copy(src, self.working_file)
# 55         self._generated_files.append(self.working_file)
# 56 
# 57         input_path = Path(incoming_file)
# 58         try:
# 59             relative_path = str(input_path.relative_to(Path.cwd()))
# 60         except ValueError:
# 61             relative_path = str(input_path)
# 62 
# 63         state = {
# 64             "state": AGENT.START,
# 65             "file_path": str(self.working_file),
# 66             "session_id": session_id,
# 67             "system": input.get("system", "unknown"),
# 68             "reason": input.get("reason", AGENT.START.value),
# 69             "steps": input.get("steps", 0),
# 70             "retry_count": input.get("retry_count", 0),
# 71             "_last_state": input.get("_last_state", AGENT.START),
# 72             "decision": DECISION_TYPE.UNKNOWN,
# 73             "original_file": relative_path,
# 74             "run_id": self._run_id,
# 75         }
# 76 
# 77         step_count = 0
# 78 
# 79         while True:
# 80             current = state["state"]
# 81 
# 82             if step_count >= max_steps:
# 83                 return StateOutputSchema(
# 84                     state=AGENT.END,
# 85                     previous_state=current,
# 86                     state_type=STATE_TYPE.END,
# 87                     decision=DECISION_TYPE.REJECT,
# 88                     steps=step_count,
# 89                     max_steps=max_steps,
# 90                     summary=f"Max steps ({max_steps}) reached",
# 91                     output=state,
# 92                     provider_name=self._config.name,
# 93                 )
# 94 
# 95             if current == STATE.END:
# 96                 if self.score_provider:
# 97                     best_file = select_best_file_by_score(
# 98                         file_a=state["file_path"],
# 99                         file_b=self.incoming_file,
# 100                         score_provider=self.score_provider,
# 101                         system=state.get("system", "unknown"),
# 102                         session_id=session_id
# 103                     )
# 104 
# 105                     final_path = Path("working_files") / f"final_state_{datetime.now().strftime('%H%M%S%f')[:10]}.py"
# 106                     shutil.copy(Path(best_file), final_path)
# 107                     state["file_path"] = str(final_path)
# 108 
# 109                     for f in Path("working_files").glob("final_*.py"):
# 110                         if f.resolve() != final_path.resolve():
# 111                             try:
# 112                                 f.unlink()
# 113                             except Exception:
# 114                                 pass
# 115 
# 116                     for f in Path("working_files").glob("*_stripped.py"):
# 117                         try:
# 118                             f.unlink()
# 119                         except Exception:
# 120                             pass
# 121 
# 122                 for path in self._generated_files:
# 123                     if path.exists():
# 124                         try:
# 125                             path.unlink()
# 126                         except Exception:
# 127                             pass
# 128 
# 129                 return StateOutputSchema(
# 130                     state=AGENT.END,
# 131                     previous_state=state.get("_last_state", AGENT.START),
# 132                     state_type=STATE_TYPE.END,
# 133                     decision=state.get("decision", DECISION_TYPE.UNKNOWN),
# 134                     steps=step_count,
# 135                     max_steps=max_steps,
# 136                     summary=state.get("summary", "Completed"),
# 137                     output=state,
# 138                     provider_name=self._config.name,
# 139                 )
# 140 
# 141             step_count += 1
# 142 
# 143             if current == AGENT.START:
# 144                 transition = self._transition(state, None)
# 145                 state.update(transition)
# 146                 state["_last_state"] = AGENT.START
# 147                 continue
# 148 
# 149             provider = self.agent_providers.get(current.value)
# 150             agent_output = provider.run(input=state, session_id=session_id) if provider else None
# 151 
# 152             transition_result = self._transition(state, agent_output)
# 153 
# 154             new_file_path = transition_result.get("file_path") or getattr(agent_output, "file_path", None)
# 155             if new_file_path:
# 156                 new_file_path = Path(new_file_path).resolve()
# 157                 current_path = Path(state["file_path"]).resolve()
# 158                 if new_file_path != current_path:
# 159                     shutil.copy(new_file_path, current_path)
# 160                     self._generated_files.append(new_file_path)
# 161                     state["file_path"] = str(new_file_path)
# 162 
# 163             if hasattr(agent_output, "decision") and agent_output.decision:
# 164                 state["decision"] = agent_output.decision
# 165 
# 166             flat_output = agent_output.model_dump(exclude={"output"}) if hasattr(agent_output, "model_dump") else dict(agent_output or {})
# 167 
# 168             transition_metadata = transition_result.get("transition_metadata", {}) or {}
# 169             transition_metadata.update({
# 170                 "agent_output": flat_output.get("output", {}),
# 171                 "file_path": flat_output.get("file_path"),
# 172             })
# 173             transition_result["transition_metadata"] = transition_metadata
# 174 
# 175             raw_state = transition_result.get("state", current)
# 176             state_enum = raw_state if isinstance(raw_state, AGENT) else AGENT(raw_state)
# 177 
# 178             state = {
# 179                 **state,
# 180                 **transition_result,
# 181                 "state": state_enum,
# 182                 "file_path": transition_result.get("file_path") or state.get("file_path"),
# 183                 "_last_state": current,
# 184                 "state_output": flat_output,
# 185             }
# 186 
# 187     @abstractmethod
# 188     def _transition(self, state: dict, output: dict | None) -> dict:
# 189         ...


# providers\system_provider_base.py

# 1 from __future__ import annotations
# 2 from abc import abstractmethod
# 3 from datetime import datetime
# 4 from pathlib import Path
# 5 import shutil
# 6 from typing import Dict
# 7 
# 8 from app.enums.fsm_enums import STATE_TYPE, DECISION_TYPE
# 9 from app.enums.state_enums import STATE
# 10 from app.providers.fsm_provider_base import FSMProviderBase
# 11 from app.db.schemas import SystemOutputSchema
# 12 from app.utilities.extract_base_filename import extract_base_filename
# 13 from app.utilities.select_best_file_by_score import select_best_file_by_score
# 14 
# 15 
# 16 class SystemProviderBase(FSMProviderBase):
# 17     def __init__(
# 18         self,
# 19         config=None,
# 20         state_providers: Dict[str, object] = None,
# 21         context_provider=None,
# 22         score_provider=None,
# 23         tool_providers=None,
# 24         called_by_type=None,
# 25         called_by_id=None,
# 26     ):
# 27         super().__init__(
# 28             config=config,
# 29             called_by_type=called_by_type,
# 30             called_by_id=called_by_id
# 31         )
# 32         self._states = state_providers or {}
# 33         self.context_provider = context_provider
# 34         self.score_provider = score_provider
# 35         self.tool_providers = tool_providers or []
# 36         self._generated_files: list[Path] = []
# 37 
# 38     def _run_provider(self, input: dict) -> SystemOutputSchema:
# 39         session_id = input.get("session_id")
# 40         max_steps = input.get("max_steps", 10)
# 41 
# 42         incoming_file = input.get("file_path") or input.get("file_name") or input.get("before")
# 43         if not incoming_file:
# 44             raise ValueError("❌ SystemProvider requires 'file_path', 'file_name', or 'before' in input")
# 45 
# 46         self.incoming_file = incoming_file
# 47         src = Path(incoming_file).resolve()
# 48         timestamp = datetime.now().strftime('%H%M%S%f')[:10]
# 49         working_dir = Path("working_files").resolve()
# 50         working_dir.mkdir(parents=True, exist_ok=True)
# 51         root = extract_base_filename(src)
# 52         self.working_file = working_dir / f"{root}__sys_{timestamp}{src.suffix}"
# 53         shutil.copy(src, self.working_file)
# 54         self._generated_files.append(self.working_file)
# 55 
# 56         input_path = Path(incoming_file)
# 57         try:
# 58             relative_path = str(input_path.relative_to(Path.cwd()))
# 59         except ValueError:
# 60             relative_path = str(input_path)
# 61 
# 62         state = {
# 63             "state": STATE.START,
# 64             "file_path": str(self.working_file),
# 65             "session_id": session_id,
# 66             "system": input.get("system", "unknown"),
# 67             "reason": input.get("reason", STATE.START.value),
# 68             "steps": input.get("steps", 0),
# 69             "retry_count": input.get("retry_count", 0),
# 70             "_last_state": input.get("_last_state", STATE.START),
# 71             "decision": DECISION_TYPE.UNKNOWN,
# 72             "original_file": relative_path,
# 73             "run_id": self._run_id,
# 74         }
# 75 
# 76         step_count = 0
# 77 
# 78         while True:
# 79             current = state["state"]
# 80 
# 81             if step_count >= max_steps:
# 82                 return SystemOutputSchema(
# 83                     state=STATE.END,
# 84                     previous_state=current,
# 85                     state_type=STATE_TYPE.END,
# 86                     decision=DECISION_TYPE.REJECT,
# 87                     steps=step_count,
# 88                     max_steps=max_steps,
# 89                     summary=f"Max steps ({max_steps}) reached",
# 90                     output=state,
# 91                     provider_name=self._config.name
# 92                 )
# 93 
# 94             if current == STATE.END:
# 95                 best_file = select_best_file_by_score(
# 96                     file_a=state["file_path"],
# 97                     file_b=self.incoming_file,
# 98                     score_provider=self.score_provider,
# 99                     system=state.get("system", "unknown"),
# 100                     session_id=session_id
# 101                 )
# 102 
# 103                 final_path = Path("working_files") / f"final_sys_{datetime.now().strftime('%H%M%S%f')[:10]}.py"
# 104                 shutil.copy(Path(best_file), final_path)
# 105                 state["file_path"] = str(final_path)
# 106 
# 107                 for f in Path("working_files").glob("final_*.py"):
# 108                     if f.resolve() != final_path.resolve():
# 109                         try:
# 110                             f.unlink()
# 111                         except Exception:
# 112                             pass
# 113 
# 114                 for f in Path("working_files").glob("*_stripped.py"):
# 115                     try:
# 116                         f.unlink()
# 117                     except Exception:
# 118                         pass
# 119 
# 120                 for path in self._generated_files:
# 121                     if path.exists():
# 122                         try:
# 123                             path.unlink()
# 124                         except Exception:
# 125                             pass
# 126 
# 127                 return SystemOutputSchema(
# 128                     state=STATE.END,
# 129                     previous_state=state.get("_last_state", STATE.START),
# 130                     state_type=STATE_TYPE.END,
# 131                     decision=state.get("decision", DECISION_TYPE.UNKNOWN),
# 132                     steps=step_count,
# 133                     max_steps=max_steps,
# 134                     summary=state.get("summary", "Completed"),
# 135                     output=state,
# 136                     provider_name=self._config.name
# 137                 )
# 138 
# 139             step_count += 1
# 140 
# 141             if current == STATE.START:
# 142                 transition = self.transition(state, None)
# 143                 state.update(transition)
# 144                 state["_last_state"] = STATE.START
# 145                 continue
# 146 
# 147             provider = self._states.get(current.value)
# 148             if not provider:
# 149                 raise ValueError(f"No state provider registered for state: {current.value}")
# 150 
# 151             provider_input = {k: v for k, v in state.items() if k != "state"}
# 152             output = provider.run(input=provider_input, session_id=session_id)
# 153 
# 154             new_file_path = output.output.get("file_path")
# 155             if new_file_path:
# 156                 state["file_path"] = str(new_file_path)
# 157 
# 158             if hasattr(output, "decision") and output.decision:
# 159                 state["decision"] = output.decision
# 160 
# 161             transition_result = self.transition(state, output)
# 162             flat_output = output.model_dump(exclude={"output"}) if hasattr(output, "model_dump") else dict(output)
# 163 
# 164             transition_metadata = transition_result.get("transition_metadata", {}) or {}
# 165             transition_metadata.update({
# 166                 "agent_output": flat_output.get("output", {}),
# 167                 "file_path": flat_output.get("file_path"),
# 168             })
# 169             transition_result["transition_metadata"] = transition_metadata
# 170 
# 171             raw_state = transition_result.get("state", current)
# 172             state_enum = raw_state if isinstance(raw_state, STATE) else STATE(raw_state)
# 173 
# 174             state = {
# 175                 **state,
# 176                 **transition_result,
# 177                 "state": state_enum,
# 178                 "file_path": transition_result.get("file_path") or getattr(output, "file_path", state.get("file_path")),
# 179                 "_last_state": current,
# 180                 "state_output": flat_output,
# 181             }
# 182 
# 183     @abstractmethod
# 184     def _transition(self, state: dict, output: dict | None) -> dict:
# 185         ...


# providers\tool_provider_base.py

# 1 from __future__ import annotations
# 2 from abc import abstractmethod
# 3 from app.providers.base_provider import BaseProvider
# 4 
# 5 from typing import TYPE_CHECKING
# 6 
# 7 if TYPE_CHECKING:
# 8     from app.providers.context_provider_base import ContextProviderBase
# 9     from app.providers.score_provider_base import ScoreProviderBase
# 10 
# 11 
# 12 from app.db.schemas import ToolOutputSchema
# 13 
# 14 class ToolProviderBase(BaseProvider):
# 15     """Base class for tool providers with optional context/score wiring."""
# 16 
# 17     def _run_provider(self, input: dict) -> ToolOutputSchema:
# 18         return self._run(input)
# 19 
# 20     def set_context_provider(self, provider: ContextProviderBase) -> None:
# 21         self.context_provider = provider
# 22 
# 23     def set_score_provider(self, provider: ScoreProviderBase) -> None:
# 24         self.score_provider = provider
# 25 
# 26     @abstractmethod
# 27     def _run(self, input: dict) -> ToolOutputSchema:
# 28         raise NotImplementedError
# 29 


# utilities\__init__.py



# utilities\diff_utils.py

# 1 import difflib
# 2 
# 3 def summarize_diff(before: str, after: str) -> str:
# 4     """Returns a human-readable summary of what changed between two versions."""
# 5     before_lines = before.strip().splitlines()
# 6     after_lines = after.strip().splitlines()
# 7     diff = list(difflib.unified_diff(before_lines, after_lines, lineterm=""))
# 8     if not diff:
# 9         return "No differences detected."
# 10 
# 11     summary = ["Changes:"]
# 12     for line in diff:
# 13         if line.startswith("+ ") and not line.startswith("+++"):
# 14             summary.append(f"Added: {line[2:]}")
# 15         elif line.startswith("- ") and not line.startswith("---"):
# 16             summary.append(f"Removed: {line[2:]}")
# 17 
# 18     return "\n".join(summary[:10])  #  Limit to first 10 lines


# utilities\extension_loader.py

# 1 from importlib.util import spec_from_file_location, module_from_spec
# 2 from pathlib import Path
# 3 from typing import Type
# 4 
# 5 def import_class_from_extension(artifact_path: str | Path, base_cls: Type) -> Type:
# 6     file_path = Path("extensions") / f"{artifact_path}.py"
# 7     if not file_path.exists():
# 8         raise FileNotFoundError(f"Extension file not found: {file_path}")
# 9 
# 10     spec = spec_from_file_location("dynamic_module", file_path)
# 11     if spec is None or spec.loader is None:
# 12         raise ImportError(f"Unable to load: {file_path}")
# 13 
# 14     module = module_from_spec(spec)
# 15     spec.loader.exec_module(module)
# 16 
# 17     for attr in module.__dict__.values():
# 18         if isinstance(attr, type) and issubclass(attr, base_cls) and attr is not base_cls:
# 19             return attr
# 20 
# 21     raise ImportError(f"No subclass of {base_cls.__name__} found in: {file_path}")


# utilities\extract_base_filename.py

# 1 from pathlib import Path
# 2 import re
# 3 
# 4 
# 5 def extract_base_filename(path: Path) -> str:
# 6     name = path.stem
# 7     #  Strip known suffixes like __state_12345
# 8     return re.sub(r"(__state_|__ctrl_|__sys_|__prog_)\d{10}$", "", name)


# utilities\file_management\__init__.py



# utilities\metadata\__init__.py



# utilities\metadata\footer\__init__.py



# utilities\metadata\footer\code_annnotation_utils.py

# 1 from __future__ import annotations
# 2 from typing import Tuple
# 3 
# 4 FOOTER_START = "#  --- Agent Notes"
# 5 FOOTER_END = "#  -----------------------------------------------"
# 6 
# 7 def strip_agent_notes(file_content: str) -> str:
# 8     """Remove the agent notes footer from the file (if present)."""
# 9     lines = file_content.strip().splitlines()
# 10     start_idx = None
# 11 
# 12     for i, line in enumerate(lines):
# 13         if line.strip().startswith(FOOTER_START):
# 14             start_idx = i
# 15             break
# 16 
# 17     return "\n".join(lines[:start_idx]) if start_idx is not None else file_content.strip()
# 18 
# 19 
# 20 def append_agent_note(file_content: str, system: str, agent_name: str, note: str) -> str:
# 21     """Append a new agent note to the bottom of the file."""
# 22     base = strip_agent_notes(file_content)
# 23 
# 24     formatted_note = (
# 25         f"{FOOTER_START} ({system} / {agent_name}) ---\n" +
# 26         "\n".join(f"#  {line.strip()}" for line in note.strip().splitlines()) +
# 27         f"\n{FOOTER_END}"
# 28     )
# 29 
# 30     return base.rstrip() + "\n\n" + formatted_note
# 31 
# 32 
# 33 def split_code_and_notes(file_content: str) -> Tuple[str, str]:
# 34     """Return (raw_code, extracted_footer_notes) without modifying whitespace"""
# 35     lines = file_content.splitlines()  #  No .strip() here
# 36     start_idx = None
# 37 
# 38     for i, line in enumerate(lines):
# 39         if line.strip().startswith(FOOTER_START):  #  You may keep this .strip() for detecting footer
# 40             start_idx = i
# 41             break
# 42 
# 43     if start_idx is not None:
# 44         code_part = "\n".join(lines[:start_idx])  #  No .strip() here to preserve whitespace
# 45         notes_part = "\n".join(lines[start_idx:])
# 46         return code_part, notes_part
# 47 
# 48     return file_content, ""  


# utilities\metadata\logging\__init__.py



# utilities\metadata\logging\conversation_log.py

# 1 from __future__ import annotations
# 2 from typing import List
# 3 from sqlalchemy.orm import Session
# 4 from app.db.models import AgentConversationLog
# 5 
# 6 def get_conversation_log(session: Session, session_id: str, system: str) -> List[str]:
# 7     """Returns logged conversation entries for the specified session and system."""
# 8     logs = (
# 9         session.query(AgentConversationLog)
# 10         .filter_by(session_id=session_id, system=system)
# 11         .order_by(AgentConversationLog.timestamp.asc())
# 12         .all()
# 13     )
# 14 
# 15     return [log.content for log in logs if log.content]


# utilities\metadata\logging\logging_provider.py

# 1 from __future__ import annotations
# 2 
# 3 from datetime import datetime
# 4 import json
# 5 import logging
# 6 import sqlite3
# 7 from dataclasses import asdict, is_dataclass
# 8 from enum import Enum
# 9 from pathlib import Path
# 10 from typing import Any, ClassVar, Iterable
# 11 from app.db.connection import get_connection
# 12 from app.enums.logging_enums import LOG_TYPE
# 13 from app.db.schemas import (
# 14     SnapshotMetricsSchema,
# 15     StateTransitionLogSchema,
# 16     AgentConversationLogSchema,
# 17     ErrorLogSchema,
# 18     ProviderLogSchema,
# 19 )
# 20 
# 21 LOG_CONFIG_MAP = {
# 22     LOG_TYPE.STATE_TRANSITION: {
# 23         "schema": StateTransitionLogSchema,
# 24         "table": "state_transition_log",
# 25     },
# 26     LOG_TYPE.AGENT_CONVERSATION: {
# 27         "schema": AgentConversationLogSchema,
# 28         "table": "agent_conversation_log",
# 29     },
# 30     LOG_TYPE.PROVIDER: {
# 31         "schema": ProviderLogSchema,
# 32         "table": "provider_log",
# 33     },
# 34     LOG_TYPE.ERROR: {
# 35         "schema": ErrorLogSchema,
# 36         "table": "error_log",
# 37     },
# 38     LOG_TYPE.SNAPSHOT_METRICS: {
# 39         "schema": SnapshotMetricsSchema,  
# 40         "table": "snapshot_metrics",   
# 41     },
# 42 }
# 43 
# 44 class LoggingProvider:
# 45     """Centralized provider for structured logging."""
# 46 
# 47     _instance: ClassVar["LoggingProvider" | None] = None
# 48 
# 49     def __new__(cls, *args, **kwargs):
# 50         if cls._instance is None:
# 51             cls._instance = super().__new__(cls)
# 52         return cls._instance
# 53 
# 54     def __init__(
# 55         self,
# 56         db_path: str | Path = "experiments/codecritic.sqlite3",
# 57         output_path: str | Path | None = None,
# 58         connection: sqlite3.Connection | None = None,
# 59     ) -> None:
# 60         if getattr(self, "_initialized", False):
# 61             if connection is not None:
# 62                 self.conn = connection
# 63             return
# 64 
# 65         self.db_path = Path(db_path)
# 66         self.db_path.parent.mkdir(exist_ok=True, parents=True)
# 67 
# 68         self._conn = connection or get_connection()
# 69 
# 70         self.output_path = Path(output_path) if output_path else None
# 71         if self.output_path:
# 72             self.output_path.parent.mkdir(parents=True, exist_ok=True)
# 73 
# 74         self._initialized = True
# 75 
# 76     @property
# 77     def conn(self):
# 78         if getattr(self, "_conn", None) is None:
# 79             from app.db import init_db
# 80             self._conn = init_db(reset=False).raw_connection()
# 81         try:
# 82             self._conn.cursor()
# 83         except Exception:
# 84             from app.db import init_db
# 85             self._conn = init_db(reset=False).raw_connection()
# 86         return self._conn
# 87 
# 88     def _serialize(self, obj: Any) -> dict:
# 89         def _safe(v: Any) -> Any:
# 90             if isinstance(v, (Enum, Path)):
# 91                 return str(v)
# 92             elif isinstance(v, datetime):
# 93                 return v.isoformat()
# 94             elif isinstance(v, list):
# 95                 return [_safe(i) for i in v]
# 96             elif isinstance(v, dict):
# 97                 #  Turn any nested dict into a JSON string here:
# 98                 return json.dumps({k: _safe(val) for k, val in v.items()}, default=str)
# 99             return v
# 100 
# 101         if not is_dataclass(obj) or isinstance(obj, type):
# 102             raise TypeError(f"Expected dataclass instance, got {type(obj)}")
# 103 
# 104         raw = asdict(obj)
# 105         return {k: _safe(v) for k, v in raw.items()}
# 106 
# 107 
# 108 
# 109     def _insert_many(self, table: str, items: Iterable[dict]) -> None:
# 110         items = list(items)
# 111         if not items:
# 112             return
# 113 
# 114         keys = list(items[0].keys())
# 115         cols = ",".join(keys)
# 116         placeholders = ",".join(["?"] * len(keys))
# 117         values = [tuple(i[k] for k in keys) for i in items]
# 118         cur = self.conn.cursor()
# 119         cur.executemany(f"INSERT INTO {table} ({cols}) VALUES ({placeholders})", values)
# 120         self.conn.commit()
# 121         if self.output_path:
# 122             with self.output_path.open("a", encoding="utf-8") as fh:
# 123                 for item in items:
# 124                     fh.write(json.dumps(item) + "\n")
# 125 
# 126     def write(self, log_type: LOG_TYPE, entries: list[Any] | Any) -> None:
# 127         if not isinstance(entries, list):
# 128             entries = [entries]
# 129 
# 130         config = LOG_CONFIG_MAP.get(log_type)
# 131         if config is None:
# 132             raise ValueError(f"Unsupported log type: {log_type}")
# 133 
# 134         schema_cls = config["schema"]
# 135         table_name = config["table"]
# 136 
# 137         #  Log and allow class name mismatches as a warning (helpful for shadowed classes)
# 138         for entry in entries:
# 139             if not is_dataclass(entry):
# 140                 raise TypeError(f"Expected a dataclass instance, got {type(entry)}")
# 141             if entry.__class__.__name__ != schema_cls.__name__:
# 142                 logging.getLogger("LoggingProvider").warning(
# 143                     f"⚠️ Log schema mismatch: expected {schema_cls.__name__}, got {entry.__class__.__name__}"
# 144                 )
# 145 
# 146         serialized = [self._serialize(e) for e in entries]
# 147         self._insert_many(table_name, serialized)
# 148 
# 149     def log_provider(self, log: ProviderLogSchema) -> None:
# 150         self.write(LOG_TYPE.PROVIDER, log)
# 151 
# 152     def log_state_transition(self, log: StateTransitionLogSchema) -> None:
# 153         self.write(LOG_TYPE.STATE_TRANSITION, log)
# 154 
# 155     def log_agent_conversation(self, log: AgentConversationLogSchema) -> None:
# 156         self.write(LOG_TYPE.AGENT_CONVERSATION, log)
# 157 
# 158     def log_error(self, log: ErrorLogSchema) -> None:
# 159         self.write(LOG_TYPE.ERROR, log)
# 160 
# 161     def log_snapshot_metrics(self, log: SnapshotMetricsSchema) -> None:
# 162         self.write(LOG_TYPE.SNAPSHOT_METRICS, log)
# 163 
# 164     def close(self) -> None:
# 165         self.conn.close()
# 166 
# 167 
# 168 class LoggingMixin:
# 169     """Mixin to provide access to a shared LoggingProvider instance."""
# 170 
# 171     def __init__(self, logger: LoggingProvider | None = None) -> None:
# 172         self.logger = logger or LoggingProvider()
# 173         self._log = logging.getLogger(self.__class__.__name__)
# 174 
# 175     def log_provider(self, log: ProviderLogSchema) -> None:
# 176         self.logger.log_provider(log)
# 177 
# 178     def log_state_transition(self, log: StateTransitionLogSchema) -> None:
# 179         self.logger.log_state_transition(log)
# 180 
# 181     def log_agent_conversation(self, log: AgentConversationLogSchema) -> None:
# 182         self.logger.log_agent_conversation(log)
# 183 
# 184     def log_error(self, log: ErrorLogSchema) -> None:
# 185         self.logger.log_error(log)
# 186 
# 187     def log_snapshot_metrics(self, log: SnapshotMetricsSchema) -> None:
# 188         self.logger.log_snapshot_metrics(log)


# utilities\metadata\snapshots\__init__.py



# utilities\metadata\snapshots\analyze_code_metrics.py

# 1 from __future__ import annotations
# 2 
# 3 import ast
# 4 import tokenize
# 5 from io import StringIO
# 6 from typing import Any, Dict
# 7 
# 8 
# 9 def analyze_code(source: str) -> Dict[str, Any]:
# 10     """Analyze Python source code and return structural metrics."""
# 11     metrics: Dict[str, Any] = {}
# 12 
# 13     #  === Basic line count ===
# 14     lines = source.strip().splitlines()
# 15     metrics["line_count"] = len(lines)
# 16 
# 17     #  === AST-based metrics ===
# 18     tree = ast.parse(source)
# 19     symbols = set()
# 20 
# 21     metrics.update({
# 22         "function_count": 0,
# 23         "class_count": 0,
# 24         "branch_count": 0,
# 25         "call_count": 0,
# 26         "assignment_count": 0,
# 27         "return_count": 0,
# 28         "exception_count": 0,
# 29         "docstring_count": 0,
# 30     })
# 31 
# 32     for node in ast.walk(tree):
# 33         if isinstance(node, ast.FunctionDef):
# 34             metrics["function_count"] += 1
# 35             symbols.add(node.name)
# 36             if ast.get_docstring(node):
# 37                 metrics["docstring_count"] += 1
# 38         elif isinstance(node, ast.ClassDef):
# 39             metrics["class_count"] += 1
# 40             symbols.add(node.name)
# 41             if ast.get_docstring(node):
# 42                 metrics["docstring_count"] += 1
# 43         elif isinstance(node, (ast.If, ast.For, ast.While, ast.Match, ast.With, ast.Try)):
# 44             metrics["branch_count"] += 1
# 45         elif isinstance(node, ast.Call):
# 46             metrics["call_count"] += 1
# 47         elif isinstance(node, ast.Assign):
# 48             metrics["assignment_count"] += 1
# 49         elif isinstance(node, ast.Return):
# 50             metrics["return_count"] += 1
# 51         elif isinstance(node, (ast.Try, ast.ExceptHandler, ast.Raise)):
# 52             metrics["exception_count"] += 1
# 53 
# 54     metrics["symbol_count"] = len(symbols)
# 55 
# 56     #  === Token-based metrics ===
# 57     comment_count = 0
# 58     import_count = 0
# 59     try:
# 60         tokens = tokenize.generate_tokens(StringIO(source).readline)
# 61         for toknum, tokval, *_ in tokens:
# 62             if toknum == tokenize.COMMENT:
# 63                 comment_count += 1
# 64             elif toknum == tokenize.NAME and tokval in {"import", "from"}:
# 65                 import_count += 1
# 66     except tokenize.TokenError:
# 67         pass
# 68 
# 69     metrics["comment_count"] = comment_count
# 70     metrics["import_count"] = import_count
# 71 
# 72     return metrics
# 73 
# 74 
# 75 def compute_deltas(before_metrics: dict, after_metrics: dict) -> dict:
# 76     """Compute deltas between before/after metric sets."""
# 77     return {
# 78         f"{key}_delta": after_metrics.get(key, 0) - before_metrics.get(key, 0)
# 79         for key in after_metrics
# 80         if key in before_metrics
# 81     }


# utilities\metadata\snapshots\snapshot_reader.py

# 1 from pathlib import Path
# 2 
# 3 SNAPSHOT_ROOT = Path(__file__).resolve().parents[4] / "experiments" / "snapshots"
# 4 
# 5 def read_latest_snapshot(session_id: str, root: Path | str | None = None):
# 6     session_root = Path(root) if root else SNAPSHOT_ROOT
# 7     session_root = session_root
# 8     
# 9     if not session_root.exists():
# 10         return None
# 11 
# 12     snapshots = sorted(session_root.glob("*.before"), key=lambda p: p.stat().st_mtime, reverse=True)
# 13 
# 14     if not snapshots:
# 15         return None
# 16 
# 17     latest_snapshot_id = snapshots[0].stem
# 18     before_path = session_root / f"{latest_snapshot_id}.before"
# 19     after_path = session_root / f"{latest_snapshot_id}.after"
# 20 
# 21     return {
# 22         "before": before_path.read_text(encoding="utf-8"),
# 23         "after": after_path.read_text(encoding="utf-8"),
# 24         "before_path": str(before_path),
# 25         "after_path": str(after_path)
# 26     }
# 27 


# utilities\metadata\snapshots\snapshot_writer.py

# 1 import json
# 2 import uuid
# 3 from datetime import datetime, timezone
# 4 from pathlib import Path
# 5 
# 6 SNAPSHOT_ROOT = Path(__file__).resolve().parents[4] / "experiments" / "snapshots"
# 7 
# 8 
# 9 class SnapshotWriter:
# 10     def __init__(self, root: Path | str | None = None) -> None:
# 11         self.root = Path(root) if root else SNAPSHOT_ROOT
# 12         self.root.mkdir(parents=True, exist_ok=True)
# 13 
# 14     def write_snapshot(
# 15         self, *, before: str, after: str, session_id: str, metadata: dict | None = None
# 16     ) -> str:
# 17 
# 18         if before == after:
# 19             return ""
# 20 
# 21         session_root = self.root / session_id
# 22         session_root.mkdir(parents=True, exist_ok=True)
# 23 
# 24         snapshot_id = str(uuid.uuid4())
# 25         (session_root / f"{snapshot_id}.before").write_text(before, encoding="utf-8")
# 26         (session_root / f"{snapshot_id}.after").write_text(after, encoding="utf-8")
# 27 
# 28         if metadata:
# 29             metadata.setdefault("timestamp", datetime.now(timezone.utc).isoformat())
# 30             meta_path = session_root / f"{snapshot_id}.meta.json"
# 31             meta_path.write_text(json.dumps(metadata, indent=2), encoding="utf-8")
# 32 
# 33         return snapshot_id


# utilities\pydantic_compat.py

# 1 from __future__ import annotations
# 2 
# 3 from typing import TYPE_CHECKING, Callable
# 4 
# 5 try:  #  pragma: no cover - pydantic v1 compatibility
# 6     from pydantic import BaseModel
# 7 
# 8     if not hasattr(BaseModel, "model_dump"):
# 9         from typing import Callable
# 10 
# 11         def _model_dump(self: BaseModel, **kwargs) -> dict:
# 12             return BaseModel.dict(self, **kwargs)
# 13 
# 14         setattr(BaseModel, "model_dump", _model_dump)  #  type: ignore[attr-defined]
# 15 except Exception:  #  pragma: no cover - ignore if pydantic v2
# 16     pass
# 17 
# 18 if TYPE_CHECKING:
# 19     from pydantic import field_validator, model_validator  #  type: ignore
# 20 else:  #  pragma: no cover - runtime fallback for pydantic v1
# 21     try:
# 22         from pydantic import field_validator, model_validator  #  type: ignore
# 23     except ImportError:  #  pragma: no cover - pydantic v1
# 24         from pydantic import validator, root_validator
# 25 
# 26         def field_validator(*fields: str, **kwargs) -> Callable:
# 27             kwargs.pop("mode", None)
# 28             return validator(*fields, **kwargs)
# 29 
# 30         def model_validator(*fields, **kwargs) -> Callable:
# 31             mode = kwargs.pop("mode", "after")
# 32 
# 33             def decorator(func: Callable) -> Callable:
# 34                 if mode == "after":
# 35 
# 36                     def wrapper(cls, values):
# 37                         obj = cls.construct(**values)
# 38                         func(obj)
# 39                         return obj.dict()
# 40 
# 41                     return root_validator(pre=False, allow_reuse=True)(wrapper)
# 42 
# 43                 def wrapper(cls, values):
# 44                     return func(values)
# 45 
# 46                 return root_validator(pre=True, allow_reuse=True)(wrapper)
# 47 
# 48             return decorator


# utilities\schema\__init__.py

# 1 from .create_schema import initialize_database, create_tables, load_seed_data
# 2 
# 3 __all__ = ["initialize_database", "create_tables", "load_seed_data"]


# utilities\schema\create_schema.py

# 1 #  from pydantic import BaseModel, Field
# 2 #  from pathlib import Path
# 3 #  from typing import Dict, List, Optional, ClassVar
# 4 #  from uuid import UUID, uuid4
# 5 #  from app.enums.system_enums import SystemType
# 6 #  from app.utilities.pydantic_compat import field_validator
# 7 
# 8 #  class AgentPromptSchema(BaseModel):
# 9 #      id: Optional[int]
# 10 #      guid: UUID = Field(default_factory=uuid4)
# 11 #      name: str
# 12 #      description: Optional[str]
# 13 #      artifact_path: Path
# 14 #      tags: Optional[List[str]] = None
# 15 
# 16 #      table_name: ClassVar[str] = "agent_prompt"
# 17 
# 18 #      @field_validator("artifact_path")
# 19 #      @classmethod
# 20 #      def _check_path(cls, v: Path) -> Path:
# 21 #          if not v.is_absolute() and ".." in v.parts:
# 22 #              raise ValueError("Invalid artifact path")
# 23 #          return v
# 24 
# 25 #  class SystemPromptSchema(BaseModel):
# 26 #      id: Optional[int]
# 27 #      guid: UUID = Field(default_factory=uuid4)
# 28 #      name: str
# 29 #      system_type: SystemType
# 30 #      description: Optional[str]
# 31 #      artifact_path: Path
# 32 #      tags: Optional[List[str]] = None
# 33 
# 34 #      table_name: ClassVar[str] = "system_prompt"
# 35 
# 36 #      @field_validator("artifact_path")
# 37 #      @classmethod
# 38 #      def _check_path(cls, v: Path) -> Path:
# 39 #          if not v.is_absolute() and ".." in v.parts:
# 40 #              raise ValueError("Invalid artifact path")
# 41 #          return v
# 42 
# 43 #  class ToolConfig(BaseModel):
# 44 #      id: Optional[int] = None
# 45 #      guid: UUID = Field(default_factory=uuid4)
# 46 #      name: str
# 47 #      description: Optional[str] = None
# 48 #      config: Optional[Dict] = None
# 49 #      artifact_path: Path
# 50 
# 51 #      table_name: ClassVar[str] = "tool_config"
# 52 
# 53 #      @field_validator("artifact_path")
# 54 #      @classmethod
# 55 #      def _check_path(cls, v: Path) -> Path:
# 56 #          if not v.is_absolute() and ".." in v.parts:
# 57 #              raise ValueError("Invalid artifact path")
# 58 #          return v
# 59 
# 60 #  class ScoreProvider(BaseModel):
# 61 #      id: Optional[int] = None
# 62 #      guid: UUID = Field(default_factory=uuid4)
# 63 #      name: str
# 64 #      description: Optional[str] = None
# 65 #      artifact_path: Optional[Path] = None
# 66 
# 67 #      table_name: ClassVar[str] = "score_provider"
# 68 
# 69 #      @field_validator("artifact_path")
# 70 #      @classmethod
# 71 #      def _check_path(cls, v: Optional[Path]) -> Optional[Path]:
# 72 #          if v is None:
# 73 #              return v
# 74 #          p = Path(v)
# 75 #          if not p.is_absolute() and ".." in p.parts:
# 76 #              raise ValueError("artifact_path must be absolute or project relative")
# 77 #          return p
# 78 
# 79 #      def model_dump(self, **kwargs) -> dict:
# 80 #          return super().model_dump(**kwargs)


# utilities\select_best_file_by_score.py

# 1 from pathlib import Path
# 2 from typing import Callable
# 3 
# 4 from app.providers.score_provider_base import ScoreProviderBase
# 5 
# 6 
# 7 def select_best_file_by_score(
# 8     file_a: str,
# 9     file_b: str,
# 10     score_provider: ScoreProviderBase,
# 11     system: str = "unknown",
# 12     session_id: str = "",
# 13 ) -> str:
# 14     """
# 15     Compare two file paths using a scoring provider (with .run(input, session_id).value)
# 16     and return the file path with the higher score.
# 17 
# 18     Args:
# 19         file_a (str): First file path to compare.
# 20         file_b (str): Second file path to compare.
# 21         scoring_provider: Object with a .run(dict, session_id) method that returns .value.
# 22         system (str): System identifier passed into the scoring input.
# 23         session_id (str): Session identifier passed into the scoring call.
# 24 
# 25     Returns:
# 26         str: File path with the higher score.
# 27     """
# 28     path_a = Path(file_a).resolve()
# 29     path_b = Path(file_b).resolve()
# 30 
# 31     score_a = score_provider.run({"file_path": str(path_a), "system": system}, session_id).value
# 32     score_b = score_provider.run({"file_path": str(path_b), "system": system}, session_id).value
# 33 
# 34     return str(path_a if score_a >= score_b else path_b)



# === PYTHON-NOTEBOOKS FILES ===


# === NON-PYTHON FILES ===

# db\seeders\files\prompts\basic_agent_prompt.txt

# 1 You are a code generation agent that produces basic, clean, formatted Python code snippets based on given instructions.


# db\seeders\files\prompts\default_system_prompt.txt

# 1 You are the default formatting system. Your task is to ensure all provided Python code strictly adheres to PEP8 standards.


# db\seeders\files\prompts\linting_generator_agent_prompt.txt

# 1 You are the Generator Agent in the Linting System.
# 2 
# 3 You specialize in incrementally improving Python code quality by addressing lint violations, enhancing readability, and promoting best practices.
# 4 
# 5 Your strategy is:
# 6 
# 7 Focus on minimal, targeted improvements that preserve original structure
# 8 
# 9 Apply type hints and clarify variable names where appropriate
# 10 
# 11 Use reasoning to resolve tool-reported issues when they conflict
# 12 
# 13 You will be evaluated by a Discriminator Agent and may be prompted to revise your output. If you are in a retry round, incorporate prior feedback before retrying.
# 14 
# 15 Maintain a helpful, confident tone in your comments. Your improvements should be easy for a human developer to understand and accept.
# 16 
# 17 🚫 Your response must strictly follow this format:
# 18 
# 19 [CODE]
# 20 #  your revised Python code here
# 21 [/CODE]
# 22 
# 23 [CONVERSATION_LOG_ENTRY]
# 24 #  bullet list of what changed, why, and tradeoffs if any
# 25 [/CONVERSATION_LOG_ENTRY]
# 26 
# 27 ⚠️ Anything outside these blocks will be ignored.
# 28 


# db\seeders\files\prompts\linting_system_prompt.txt

# 1 You are operating within the Linting System, which enforces strict Python code quality and formatting compliance.
# 2 
# 3 All Generator Agents must:
# 4 - Conform to Python PEP8 guidelines
# 5 - Use tools such as `black`, `ruff`, `mypy`, and `radon` for diagnostic feedback
# 6 - Preserve original code intent and structure; avoid stylistic or structural rewrites unless necessary
# 7 - Produce valid, executable Python code
# 8 
# 9 Your response must include your corrected code inside a [CODE] block. Any content outside this block will be ignored by the system:
# 10 
# 11 [CODE]
# 12 #  your corrected Python code
# 13 [/CODE]
# 14 
# 15 After the code, include a structured log entry describing your decisions:
# 16 
# 17 [CONVERSATION_LOG_ENTRY]
# 18 Summarize what you changed, why you changed it, and any tradeoffs or notable decisions you made.
# 19 [/CONVERSATION_LOG_ENTRY]


